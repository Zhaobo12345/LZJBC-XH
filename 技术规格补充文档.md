# 良造家管理平台 — 技术规格补充文档

> **文档信息**
> - 创建日期：2026-06-29
> - 文档版本：v1.0
> - 定位：补充产品需求说明书中缺失或细化不足的技术规格部分，使其达到可交付研发的标准
> - 适用范围：PC管理端、服务方小程序、业主小程序

---

## 目录

1. [数据模型与字段定义](#1-数据模型与字段定义)
2. [状态机定义](#2-状态机定义)
3. [角色权限矩阵](#3-角色权限矩阵)
4. [API 接口规范](#4-api-接口规范)
5. [扫码登录与授权机制](#5-扫码登录与授权机制)
6. [合同正文编辑器方案](#6-合同正文编辑器方案)
7. [阶段任务模板体系](#7-阶段任务模板体系)
8. [项目架构层级规则](#8-项目架构层级规则)
9. [项目成员管理规则](#9-项目成员管理规则)
10. [创建项目流程](#10-创建项目流程)
11. [消息推送机制](#11-消息推送机制)
12. [分享功能方案](#12-分享功能方案)
13. [临时任务定义](#13-临时任务定义)
14. [活动列表定义](#14-活动列表定义)
15. [对账单财务关联规则](#15-对账单财务关联规则)
16. [评价体系设计](#16-评价体系设计)
17. [异常场景与边界条件](#17-异常场景与边界条件)

---

## 1. 数据模型与字段定义

### 1.1 用户表（users）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键，UUID |
| phone | String(11) | 是 | — | 手机号，唯一索引 |
| name | String(50) | 是 | — | 姓名 |
| avatar | String(URL) | 否 | null | 头像URL |
| user_type | Enum | 是 | — | 枚举：`service`（服务方）、`owner`（业主）、`admin`（平台运营） |
| promoter_verified | Boolean | 否 | false | 是否已通过推广人认证 |
| promoter_code | String(20) | 否 | null | 推广人码 |
| status | Enum | 是 | `active` | 枚举：`active`（正常）、`disabled`（禁用） |
| last_login_at | DateTime | 否 | null | 最后登录时间 |
| created_at | DateTime | 是 | now() | 创建时间 |
| updated_at | DateTime | 是 | now() | 更新时间 |
| deleted_at | DateTime | 否 | null | 软删除时间，null 表示未删除 |

### 1.2 项目表（projects）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| project_no | String(20) | 是 | — | 项目编号，格式：`PJ` + yyyyMMdd + 4位流水号，如 `PJ202606290001` |
| name | String(100) | 是 | — | 项目名称 |
| type | Enum | 是 | — | 枚举：`decoration`（家装）、`partial`（局部改造）、`repair`（维修） |
| address | String(200) | 是 | — | 项目地址 |
| city_code | String(6) | 是 | — | 城市编码（行政区划代码） |
| budget | Decimal(12,2) | 否 | null | 项目预算（元） |
| start_date | Date | 否 | null | 计划开工日期 |
| end_date | Date | 否 | null | 计划竣工日期 |
| status | Enum | 是 | `pending` | 枚举：`pending`（待启动）、`in_progress`（进行中）、`completed`（已竣工）、`terminated`（已终止） |
| owner_id | String(UUID) | 是 | — | 业主用户ID（关联 users.id） |
| creator_id | String(UUID) | 是 | — | 创建人用户ID |
| description | Text | 否 | null | 项目描述 |
| created_at | DateTime | 是 | now() | — |
| updated_at | DateTime | 是 | now() | — |
| deleted_at | DateTime | 否 | null | 软删除 |

### 1.3 项目成员表（project_members）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| project_id | String(UUID) | 是 | — | 关联 projects.id |
| user_id | String(UUID) | 是 | — | 关联 users.id |
| role | Enum | 是 | — | 枚举：`project_manager`（项目经理）、`foreman`（工长）、`worker`（工人）、`supervisor`（监理）、`owner`（业主） |
| join_method | Enum | 是 | — | 枚举：`invite`（邀请加入）、`scan`（扫码加入）、`assign`（管理员分配） |
| status | Enum | 是 | `active` | 枚举：`active`（在项目中）、`removed`（已移除） |
| joined_at | DateTime | 是 | now() | 加入时间 |
| removed_at | DateTime | 否 | null | 移除时间 |
| created_at | DateTime | 是 | now() | — |
| updated_at | DateTime | 是 | now() | — |

**约束：** (project_id, user_id) 联合唯一索引。

### 1.4 项目架构表（project_architecture）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| project_id | String(UUID) | 是 | — | 关联 projects.id |
| parent_id | String(UUID) | 否 | null | 父级架构节点ID，null 表示顶级（项目部） |
| level | Enum | 是 | — | 枚举：`1_department`（项目部）、`2_group`（工作组）、`3_team`（施工组）、`4_package`（任务包） |
| name | String(100) | 是 | — | 架构节点名称 |
| leader_id | String(UUID) | 否 | null | 负责人用户ID |
| sort_order | Integer | 是 | 0 | 同级排序序号 |
| status | Enum | 是 | `active` | 枚举：`active`（活跃）、`inactive`（停用） |
| created_at | DateTime | 是 | now() | — |
| updated_at | DateTime | 是 | now() | — |

**约束：**
- parent_id 为 null 时 level 必须为 `1_department`
- parent_id 不为 null 时 level 必须比父节点 level 低一级
- 一个项目部下最多 20 个工作组，一个工作组下最多 20 个施工组

### 1.5 合同表（contracts）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| contract_no | String(30) | 是 | — | 合同编号，格式：`HT` + yyyyMMdd + 4位流水号 |
| project_id | String(UUID) | 是 | — | 关联 projects.id |
| name | String(200) | 是 | — | 合同名称 |
| type | Enum | 是 | — | 枚举：`base_construction`（基础施工合同）、`hydropower`（水电分包合同）、`carpentry`（木工分包合同）、`masonry`（泥瓦分包合同）、`painting`（油漆分包合同）、`overall`（整体项目） |
| amount | Decimal(12,2) | 是 | — | 合同金额（元） |
| party_a_id | String(UUID) | 是 | — | 甲方用户ID |
| party_b_id | String(UUID) | 是 | — | 乙方用户ID |
| status | Enum | 是 | `drafting` | 枚举见状态机定义（第2节） |
| contract_body | JSON | 是 | — | 合同正文结构化内容（见 1.5.1） |
| attachments | JSON | 否 | `[]` | 附件列表，格式：`[{name, url, size, type}]` |
| template_id | String(UUID) | 否 | null | 使用的合同模板ID |
| reject_reason | String(500) | 否 | null | 最近一次驳回原因 |
| submitter_id | String(UUID) | 是 | — | 合同发起方用户ID |
| reviewed_by | String(UUID) | 否 | null | 审核人用户ID |
| reviewed_at | DateTime | 否 | null | 审核时间 |
| confirmed_at | DateTime | 否 | null | 业主确认时间 |
| signed_at | DateTime | 否 | null | 签约时间 |
| version | Integer | 是 | 1 | 版本号，每次变更 +1 |
| created_at | DateTime | 是 | now() | — |
| updated_at | DateTime | 是 | now() | — |
| deleted_at | DateTime | 否 | null | 软删除 |

#### 1.5.1 contract_body 结构定义

```json
{
  "sections": [
    {
      "title": "第一章 合同标的",
      "content": "甲方委托乙方承担..."
    },
    {
      "title": "第二章 工程概况",
      "content": "..."
    }
  ],
  "variables": {
    "project_name": "xxx装修项目",
    "total_amount": "100000.00",
    "party_a_name": "张三",
    "party_b_name": "某装修公司",
    "sign_date": "2026-06-29"
  }
}
```

### 1.6 合同变更记录表（contract_changes）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| contract_id | String(UUID) | 是 | — | 关联 contracts.id |
| change_type | Enum | 是 | — | 枚举：`content`（正文变更）、`amount`（金额变更）、`party`（甲乙方变更）、`other`（其他） |
| change_reason | Text | 是 | — | 变更原因 |
| change_content | JSON | 是 | — | 变更内容快照（变更前 + 变更后） |
| status | Enum | 是 | `pending_review` | 枚举：`pending_review`（待审核）、`approved`（已通过）、`rejected`（已驳回）、`pending_confirm`（待业主确认）、`effective`（已生效） |
| submitter_id | String(UUID) | 是 | — | 发起人用户ID |
| reviewer_id | String(UUID) | 否 | null | 审核人ID |
| reviewer_remark | String(500) | 否 | null | 审核备注/驳回原因 |
| confirmed_by | String(UUID) | 否 | null | 业主确认人ID |
| created_at | DateTime | 是 | now() | — |
| updated_at | DateTime | 是 | now() | — |

### 1.7 阶段表（stages）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| contract_id | String(UUID) | 是 | — | 关联 contracts.id |
| name | String(100) | 是 | — | 阶段名称（如"水电工程"、"泥瓦工程"） |
| sort_order | Integer | 是 | 0 | 阶段顺序 |
| status | Enum | 是 | `pending` | 枚举：`pending`（未开始）、`in_progress`（进行中）、`completed`（已完成） |
| template_id | String(UUID) | 否 | null | 来源模板ID |
| created_at | DateTime | 是 | now() | — |
| updated_at | DateTime | 是 | now() | — |

### 1.8 任务表（tasks）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| task_no | String(30) | 是 | — | 任务编号，格式：`TK` + yyyyMMdd + 4位流水号 |
| stage_id | String(UUID) | 否 | null | 关联 stages.id，临时任务为 null |
| project_id | String(UUID) | 是 | — | 关联 projects.id |
| contract_id | String(UUID) | 否 | null | 关联 contracts.id |
| arch_node_id | String(UUID) | 否 | null | 关联 project_architecture.id（任务所属架构节点） |
| name | String(200) | 是 | — | 任务名称 |
| is_temporary | Boolean | 是 | false | 是否临时任务 |
| execution_standard | Text | 否 | null | 执行标准 |
| confirmation_standard | Text | 否 | null | 确认标准 |
| responsibility_standard | Text | 否 | null | 担责标准 |
| status | Enum | 是 | `pending` | 枚举见状态机定义（第2节） |
| executor_id | String(UUID) | 否 | null | 执行人用户ID |
| confirmer_id | String(UUID) | 否 | null | 确认人用户ID |
| start_at | DateTime | 否 | null | 开始执行时间 |
| submit_at | DateTime | 否 | null | 提交验收时间 |
| complete_at | DateTime | 否 | null | 完成时间 |
| reject_reason | String(500) | 否 | null | 最近一次驳回原因 |
| reject_count | Integer | 是 | 0 | 被驳回次数 |
| sort_order | Integer | 是 | 0 | 同阶段内排序 |
| created_at | DateTime | 是 | now() | — |
| updated_at | DateTime | 是 | now() | — |

### 1.9 执行记录表（execution_records）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| task_id | String(UUID) | 是 | — | 关联 tasks.id |
| uploader_id | String(UUID) | 是 | — | 上传人用户ID |
| content | Text | 否 | null | 说明文字 |
| media_files | JSON | 是 | `[]` | 媒体文件列表，格式：`[{name, url, type, size, duration}]` |
| upload_round | Integer | 是 | 1 | 上传轮次（首次为1，被驳回后重新上传为2，依此类推） |
| created_at | DateTime | 是 | now() | — |

### 1.10 对账单表（statements）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| statement_no | String(30) | 是 | — | 对账单编号，格式：`DZ` + yyyyMMdd + 4位流水号 |
| contract_id | String(UUID) | 是 | — | 关联 contracts.id |
| project_id | String(UUID) | 是 | — | 关联 projects.id |
| bill_type | Enum | 是 | — | 枚举：`worker_wage`（工友工资）、`material`（辅料费）、`measure`（措施费）、`insurance`（保险费）、`foreman_wage`（工长工资） |
| amount | Decimal(12,2) | 是 | — | 金额（元），正数为支出，负数为冲销/退款 |
| purpose | String(200) | 是 | — | 用途描述 |
| status | Enum | 是 | `pending` | 枚举：`pending`（待确认）、`effective`（已生效）、`paid`（已支付）、`rejected`（已驳回） |
| submitter_id | String(UUID) | 是 | — | 提交人用户ID |
| confirmer_id | String(UUID) | 否 | null | 确认人用户ID |
| confirmed_at | DateTime | 否 | null | 确认时间 |
| reject_reason | String(500) | 否 | null | 驳回原因 |
| related_statement_id | String(UUID) | 否 | null | 关联原对账单ID（驳回后重新提交时使用） |
| voucher_files | JSON | 否 | `[]` | 支付凭证图片列表，格式：`[{name, url, type, size}]` |
| voucher_uploader_id | String(UUID) | 否 | null | 支付凭证上传人用户ID |
| voucher_uploaded_at | DateTime | 否 | null | 支付凭证上传时间 |
| created_at | DateTime | 是 | now() | — |
| updated_at | DateTime | 是 | now() | — |

### 1.11 对账单操作记录表（statement_operations）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| statement_id | String(UUID) | 是 | — | 关联 statements.id |
| operator_id | String(UUID) | 是 | — | 操作人用户ID |
| operation | Enum | 是 | — | 枚举：`create`（创建）、`confirm`（确认）、`reject`（驳回）、`resubmit`（重新提交）、`upload_voucher`（上传支付凭证） |
| remark | String(500) | 否 | null | 操作备注 |
| created_at | DateTime | 是 | now() | — |

### 1.12 任务评价表（task_evaluations）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| task_id | String(UUID) | 是 | — | 关联 tasks.id |
| evaluator_id | String(UUID) | 是 | — | 评价人用户ID |
| rating | Integer | 是 | — | 星级评分：1-5 |
| tags | JSON | 否 | `[]` | 评价标签，格式：`["按时完成", "质量好"]` |
| comment | Text | 否 | null | 文字评价 |
| created_at | DateTime | 是 | now() | — |

**约束：** (task_id) 唯一索引，一个任务只能有一条评价。

### 1.13 合同文本模板表（contract_templates）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| name | String(100) | 是 | — | 模板名称 |
| type | Enum | 是 | — | 合同类型枚举（同 contracts.type） |
| city_code | String(6) | 否 | null | 适用城市编码，null 为通用 |
| body_template | JSON | 是 | — | 合同正文模板（含变量占位符） |
| stages_template | JSON | 否 | `[]` | 默认阶段任务模板 |
| status | Enum | 是 | `active` | 枚举：`active`（启用）、`disabled`（停用） |
| creator_id | String(UUID) | 是 | — | 创建人用户ID |
| version | Integer | 是 | 1 | 模板版本号 |
| created_at | DateTime | 是 | now() | — |
| updated_at | DateTime | 是 | now() | — |

### 1.14 阶段任务模板表（stage_templates）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| name | String(100) | 是 | — | 模板名称 |
| city_code | String(6) | 否 | null | 适用城市编码 |
| project_type | Enum | 否 | null | 适用项目类型（同 projects.type），null 为通用 |
| stages | JSON | 是 | — | 阶段任务结构，格式见下方 |
| status | Enum | 是 | `active` | 枚举：`active`、`disabled` |
| creator_id | String(UUID) | 是 | — | 创建人 |
| created_at | DateTime | 是 | now() | — |
| updated_at | DateTime | 是 | now() | — |

**stages 字段结构：**

```json
[
  {
    "stage_name": "水电工程",
    "sort_order": 1,
    "tasks": [
      {
        "name": "水电交底",
        "execution_standard": "...",
        "confirmation_standard": "...",
        "responsibility_standard": "..."
      }
    ]
  }
]
```

### 1.15 消息表（messages）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| recipient_id | String(UUID) | 是 | — | 接收人用户ID |
| sender_id | String(UUID) | 否 | null | 发送人用户ID，null 为系统消息 |
| type | Enum | 是 | — | 枚举：`contract_pending`（合同待确认）、`contract_reviewed`（合同已审核）、`task_pending`（任务待验收）、`task_rejected`（任务被驳回）、`statement_pending`（对账单待确认）、`statement_rejected`（对账单被驳回）、`change_pending`（变更待确认）、`system`（系统通知） |
| title | String(100) | 是 | — | 消息标题 |
| content | Text | 是 | — | 消息内容 |
| link_type | Enum | 否 | null | 枚举：`contract`、`task`、`statement`、`project`、`change` |
| link_id | String(UUID) | 否 | null | 关联业务对象ID |
| is_read | Boolean | 是 | false | 是否已读 |
| read_at | DateTime | 否 | null | 已读时间 |
| created_at | DateTime | 是 | now() | — |

### 1.16 分享记录表（shares）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| sharer_id | String(UUID) | 是 | — | 分享人用户ID |
| target_type | Enum | 是 | — | 枚举：`project`、`contract`、`task`、`statement` |
| target_id | String(UUID) | 是 | — | 分享对象ID |
| share_code | String(32) | 是 | — | 分享码，唯一 |
| expire_at | DateTime | 是 | — | 过期时间（默认7天后） |
| max_views | Integer | 否 | null | 最大查看次数，null 为不限 |
| view_count | Integer | 是 | 0 | 已查看次数 |
| created_at | DateTime | 是 | now() | — |

### 1.17 项目动态表（project_activities）

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | String(UUID) | 是 | — | 主键 |
| project_id | String(UUID) | 是 | — | 关联 projects.id |
| type | Enum | 是 | — | 枚举见第14节活动类型定义 |
| operator_id | String(UUID) | 是 | — | 操作人用户ID |
| target_type | Enum | 否 | null | 枚举：`contract`、`task`、`statement`、`member` |
| target_id | String(UUID) | 否 | null | 关联对象ID |
| description | String(500) | 是 | — | 动态描述文字 |
| created_at | DateTime | 是 | now() | — |

### 1.18 数据字典

#### 1.18.1 合同类型

| 值 | 标签 |
|----|------|
| `base_construction` | 基础施工合同 |
| `hydropower` | 水电分包合同 |
| `carpentry` | 木工分包合同 |
| `masonry` | 泥瓦分包合同 |
| `painting` | 油漆分包合同 |
| `overall` | 整体项目 |

#### 1.18.2 对账单类型

| 值 | 标签 |
|----|------|
| `worker_wage` | 工友工资 |
| `material` | 辅料费 |
| `measure` | 措施费 |
| `insurance` | 保险费 |
| `foreman_wage` | 工长工资 |

#### 1.18.3 评价标签

| 值 | 分类 | 标签 |
|----|------|------|
| `on_time` | 正面 | 按时完成 |
| `good_quality` | 正面 | 质量好 |
| `professional` | 正面 | 专业规范 |
| `clean_site` | 正面 | 现场整洁 |
| `good_attitude` | 正面 | 态度认真 |
| `needs_improvement` | 负面 | 需要改进 |
| `delayed` | 负面 | 进度拖延 |
| `quality_issue` | 负面 | 质量问题 |
| `messy_site` | 负面 | 现场凌乱 |
| `poor_communication` | 负面 | 沟通不畅 |

---

## 2. 状态机定义

### 2.1 合同状态机

| 当前状态 | 触发事件 | 目标状态 | 操作人 | 可执行操作 | 说明 |
|---------|---------|---------|--------|-----------|------|
| `drafting`（拟定中） | 提交审核 | `pending_review` | 合同发起方 | 编辑、保存、提交审核 | 仅拟定中状态可编辑内容 |
| `pending_review`（待审核） | 审核通过 | `pending_confirm` | 运营 | 审核通过 | 进入业主确认环节 |
| `pending_review`（待审核） | 审核驳回 | `drafting` | 运营 | 审核驳回（填写原因） | 退回合同发起方修改 |
| `pending_confirm`（确认中） | 业主确认 | `pending_sign` | 业主 | 确认合同 | — |
| `pending_confirm`（确认中） | 业主驳回 | `drafting` | 业主 | 驳回（填写原因） | 退回合同发起方修改 |
| `pending_sign`（待签约） | 确认签约 | `signed` | 甲乙双方 | 确认签约 | 合同生效 |
| `signed`（已签约） | 发起变更 | `changing` | 合同发起方 | 发起变更申请 | 变更审核流程 |
| `changing`（变更待审核） | 变更审核通过 | `change_pending_confirm` | 运营 | 审核通过 | — |
| `changing`（变更待审核） | 变更审核驳回 | `signed` | 运营 | 审核驳回 | 变更不生效，合同维持原状 |
| `change_pending_confirm`（变更待确认） | 业主确认变更 | `signed` | 业主 | 确认变更 | 变更生效，合同版本号+1 |
| `change_pending_confirm`（变更待确认） | 业主驳回变更 | `signed` | 业主 | 驳回变更 | 变更不生效 |
| `signed`（已签约） | 项目竣工 | `completed` | 系统 | 自动标记 | 项目状态为已竣工时触发 |

**合同状态流转图：**

```
drafting → pending_review → pending_confirm → pending_sign → signed → completed
   ↑           ↓                  ↓                              ↓ ↑
   └─── 驳回 ──┘         └── 驳回 ─┘                     changing → change_pending_confirm
                                                                  ↓              ↓
                                                              审核驳回       业主确认/驳回
                                                              (回 signed)
```

### 2.2 任务状态机

| 当前状态 | 触发事件 | 目标状态 | 操作人 | 说明 |
|---------|---------|---------|--------|------|
| `pending`（待开始） | 开始执行 | `in_progress` | 执行人 | 需检查前置任务是否完成 |
| `in_progress`（执行中） | 提交验收 | `confirming` | 执行人 | 需至少一条执行记录 |
| `confirming`（确认中） | 业主确认验收 | `completed` | 确认人 | 业主可同时提交评价 |
| `confirming`（确认中） | 业主驳回 | `in_progress` | 确认人 | 驳回原因写入 reject_reason，reject_count +1 |
| `completed`（已完成） | — | — | — | 终态 |

**任务前置依赖规则：**
- 同一阶段内，前一个任务未完成时，后一个任务不可开始执行
- 临时任务（is_temporary=true）无前置依赖限制

### 2.3 对账单状态机

| 当前状态 | 触发事件 | 目标状态 | 操作人 | 说明 |
|---------|---------|---------|--------|------|
| `pending`（待确认） | 业主确认 | `effective` | 业主 | 对账单生效 |
| `pending`（待确认） | 业主驳回 | `rejected` | 业主 | 填写驳回原因 |
| `effective`（已生效） | 服务方上传支付凭证 | `paid` | 服务方 | 上传支付凭证图片，最多9张 |
| `rejected`（已驳回） | 服务方重新提交 | `pending` | 服务方 | 创建新的对账单记录，原单保留为已驳回状态（不修改原单） |

> **注意：** 对账单被驳回后，不修改原单，而是创建一条新对账单。原单状态保持 `rejected`，通过 statement_operations 记录操作历史。新对账单可关联原对账单ID作为 `related_statement_id`。

---

## 3. 角色权限矩阵

### 3.1 RBAC 权限矩阵

| 功能模块 | 操作 | 平台运营 | 合同发起方 | 服务方（项目经理/工长） | 服务方（工人） | 业主 |
|---------|------|---------|-----------|----------------------|--------------|------|
| **合同管理** | 创建合同 | ❌ | ✅ | ✅ | ❌ | ❌ |
| | 编辑合同（拟定中） | ❌ | ✅ | ✅ | ❌ | ❌ |
| | 提交审核 | ❌ | ✅ | ✅ | ❌ | ❌ |
| | 审核通过/驳回 | ✅ | ❌ | ❌ | ❌ | ❌ |
| | 确认合同 | ❌ | ❌ | ❌ | ❌ | ✅ |
| | 驳回合同 | ❌ | ❌ | ❌ | ❌ | ✅ |
| | 确认签约 | ❌ | ❌ | ✅（乙方） | ❌ | ✅（甲方） |
| | 发起变更 | ❌ | ✅ | ✅ | ❌ | ❌ |
| | 审核变更 | ✅ | ❌ | ❌ | ❌ | ❌ |
| | 确认变更 | ❌ | ❌ | ❌ | ❌ | ✅ |
| | 查看合同 | ✅ | ✅（自己发起的） | ✅（项目相关） | ✅（项目相关） | ✅（自己的） |
| **任务管理** | 查看任务 | ❌ | ❌ | ✅ | ✅（分配给自己的） | ✅（自己的项目） |
| | 开始执行 | ❌ | ❌ | ✅ | ✅（本人任务） | ❌ |
| | 上传执行记录 | ❌ | ❌ | ✅ | ✅（本人任务） | ❌ |
| | 提交验收 | ❌ | ❌ | ✅ | ✅（本人任务） | ❌ |
| | 确认验收 | ❌ | ❌ | ❌ | ❌ | ✅ |
| | 驳回任务 | ❌ | ❌ | ❌ | ❌ | ✅ |
| | 创建临时任务 | ❌ | ❌ | ✅ | ❌ | ❌ |
| | 分配执行人 | ❌ | ❌ | ✅ | ❌ | ❌ |
| **项目管理** | 创建项目 | ❌ | ❌ | ✅ | ❌ | ❌ |
| | 编辑项目信息 | ❌ | ❌ | ✅（创建人） | ❌ | ❌ |
| | 查看项目 | ✅ | ✅（相关项目） | ✅（相关项目） | ✅（相关项目） | ✅（自己的项目） |
| | 管理成员 | ❌ | ❌ | ✅（项目经理） | ❌ | ❌ |
| | 管理架构 | ❌ | ❌ | ✅（项目经理） | ❌ | ❌ |
| **对账单** | 新增对账单 | ❌ | ❌ | ✅ | ❌ | ❌ |
| | 确认/驳回 | ❌ | ❌ | ❌ | ❌ | ✅ |
| | 上传支付凭证 | ❌ | ❌ | ✅ | ❌ | ❌ |
| | 查看 | ✅ | ❌ | ✅（自己提交的） | ❌ | ✅（自己的） |
| **模板管理** | 创建/编辑模板 | ✅ | ❌ | ❌ | ❌ | ❌ |
| | 查看模板 | ✅ | ✅ | ✅ | ❌ | ❌ |
| **评价** | 评价任务 | ❌ | ❌ | ❌ | ❌ | ✅ |
| | 查看评价 | ❌ | ❌ | ✅（被评价的） | ❌ | ✅（自己评价的） |
| **分享** | 分享内容 | ❌ | ❌ | ✅ | ❌ | ❌ |
| | 接收分享 | ❌ | ❌ | ❌ | ❌ | ✅ |

### 3.2 补充说明

- **合同发起方**是 PC 端角色，通常由服务方中的项目经理或专职合同管理员担任
- **工人**角色仅能操作分配给自己的任务，不能创建项目、合同或临时任务
- **监理**角色权限与项目经理相同但不包含创建/编辑权限，仅可查看
- 一个用户可在不同项目中担任不同角色（如 A 项目中是项目经理，B 项目中是业主）

---

## 4. API 接口规范

### 4.1 通用规范

**Base URL：** `/api/v1`

**请求格式：** `application/json`

**认证方式：** Bearer Token

```
Authorization: Bearer <access_token>
```

**统一响应格式：**

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

**错误响应：**

```json
{
  "code": 40001,
  "message": "参数校验失败",
  "data": {
    "errors": [
      { "field": "phone", "message": "手机号格式不正确" }
    ]
  }
}
```

**分页参数：**

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | Integer | 1 | 页码 |
| page_size | Integer | 20 | 每页条数（最大100） |
| sort | String | created_at:desc | 排序字段:方向 |

**分页响应：**

```json
{
  "code": 0,
  "data": {
    "list": [],
    "total": 100,
    "page": 1,
    "page_size": 20
  }
}
```

### 4.2 错误码定义

| 错误码 | HTTP Status | 说明 |
|--------|-------------|------|
| 0 | 200 | 成功 |
| 40001 | 400 | 参数校验失败 |
| 40101 | 401 | 未登录或 token 过期 |
| 40301 | 403 | 无权限 |
| 40401 | 404 | 资源不存在 |
| 40901 | 409 | 状态冲突（如合同状态不允许此操作） |
| 42201 | 422 | 业务校验失败（如前置任务未完成） |
| 42901 | 429 | 请求过于频繁 |
| 50001 | 500 | 服务器内部错误 |

### 4.3 接口清单

#### 4.3.1 认证模块

| 接口 | Method | Path | 说明 |
|------|--------|------|------|
| 账号密码登录 | POST | `/auth/login` | 账号密码登录，返回 token |
| 扫码登录-获取二维码 | POST | `/auth/qrcode/generate` | 生成PC端登录二维码 |
| 扫码登录-小程序扫码 | POST | `/auth/qrcode/scan` | 小程序扫码确认登录 |
| 扫码登录-轮询状态 | GET | `/auth/qrcode/status?ticket=xxx` | PC端轮询扫码状态 |
| 获取当前用户信息 | GET | `/auth/me` | 获取当前登录用户信息 |
| 退出登录 | POST | `/auth/logout` | 退出登录 |

#### 4.3.2 项目模块

| 接口 | Method | Path | 说明 |
|------|--------|------|------|
| 项目列表 | GET | `/projects` | 分页查询项目列表 |
| 创建项目 | POST | `/projects` | 创建新项目 |
| 项目详情 | GET | `/projects/:id` | 获取项目详情 |
| 编辑项目 | PUT | `/projects/:id` | 编辑项目信息 |
| 项目成员列表 | GET | `/projects/:id/members` | 获取项目成员 |
| 添加项目成员 | POST | `/projects/:id/members` | 添加成员到项目 |
| 移除项目成员 | DELETE | `/projects/:id/members/:userId` | 从项目移除成员 |
| 项目架构树 | GET | `/projects/:id/architecture` | 获取项目架构树 |
| 创建架构节点 | POST | `/projects/:id/architecture` | 创建架构节点 |
| 更新架构节点 | PUT | `/projects/:id/architecture/:nodeId` | 更新架构节点 |
| 删除架构节点 | DELETE | `/projects/:id/architecture/:nodeId` | 删除架构节点 |
| 项目动态列表 | GET | `/projects/:id/activities` | 获取项目动态 |

#### 4.3.3 合同模块

| 接口 | Method | Path | 说明 |
|------|--------|------|------|
| 合同列表 | GET | `/contracts` | 分页查询合同，支持状态/项目筛选 |
| 创建合同 | POST | `/contracts` | 创建新合同 |
| 合同详情 | GET | `/contracts/:id` | 获取合同详情 |
| 编辑合同 | PUT | `/contracts/:id` | 编辑合同（仅拟定中状态） |
| 提交审核 | POST | `/contracts/:id/submit` | 提交合同审核 |
| 审核通过 | POST | `/contracts/:id/approve` | 运营审核通过 |
| 审核驳回 | POST | `/contracts/:id/reject` | 运营审核驳回 |
| 业主确认合同 | POST | `/contracts/:id/confirm` | 业主确认合同 |
| 业主驳回合同 | POST | `/contracts/:id/owner-reject` | 业主驳回合同 |
| 确认签约 | POST | `/contracts/:id/sign` | 确认签约 |
| 发起变更 | POST | `/contracts/:id/changes` | 发起合同变更 |
| 变更列表 | GET | `/contracts/:id/changes` | 获取合同变更记录 |
| 变更审核通过 | POST | `/contracts/:id/changes/:changeId/approve` | 审核通过变更 |
| 变更审核驳回 | POST | `/contracts/:id/changes/:changeId/reject` | 审核驳回变更 |
| 业主确认变更 | POST | `/contracts/:id/changes/:changeId/confirm` | 业主确认变更 |
| 业主驳回变更 | POST | `/contracts/:id/changes/:changeId/owner-reject` | 业主驳回变更 |
| 导出合同 | GET | `/contracts/:id/export` | 导出合同PDF |

#### 4.3.4 任务模块

| 接口 | Method | Path | 说明 |
|------|--------|------|------|
| 任务列表 | GET | `/tasks` | 分页查询任务 |
| 任务详情 | GET | `/tasks/:id` | 获取任务详情 |
| 开始执行 | POST | `/tasks/:id/start` | 开始执行任务 |
| 上传执行记录 | POST | `/tasks/:id/records` | 上传执行记录 |
| 执行记录列表 | GET | `/tasks/:id/records` | 获取执行记录列表 |
| 提交验收 | POST | `/tasks/:id/submit` | 提交验收申请 |
| 业主确认验收 | POST | `/tasks/:id/accept` | 业主确认验收 |
| 业主驳回 | POST | `/tasks/:id/reject` | 业主驳回任务 |
| 创建临时任务 | POST | `/tasks/temporary` | 创建临时任务 |
| 分配执行人 | PUT | `/tasks/:id/executor` | 设置任务执行人 |
| 分配确认人 | PUT | `/tasks/:id/confirmer` | 设置任务确认人 |
| 任务评价 | POST | `/tasks/:id/evaluation` | 提交任务评价 |
| 获取评价 | GET | `/tasks/:id/evaluation` | 获取任务评价 |

#### 4.3.5 对账单模块

| 接口 | Method | Path | 说明 |
|------|--------|------|------|
| 对账单列表 | GET | `/statements` | 分页查询，支持合同/类型/状态筛选 |
| 对账单详情 | GET | `/statements/:id` | 获取对账单详情 |
| 新增对账单 | POST | `/statements` | 新增对账单 |
| 业主确认 | POST | `/statements/:id/confirm` | 业主确认对账单 |
| 业主驳回 | POST | `/statements/:id/reject` | 业主驳回对账单 |
| 操作记录 | GET | `/statements/:id/operations` | 获取对账单操作记录 |
| 金额统计 | GET | `/statements/summary` | 按合同/项目统计金额 |
| 上传支付凭证 | POST | `/statements/:id/voucher` | 上传支付凭证图片（最多9张） |

#### 4.3.6 模板模块

| 接口 | Method | Path | 说明 |
|------|--------|------|------|
| 合同模板列表 | GET | `/templates/contracts` | 获取合同模板列表 |
| 创建合同模板 | POST | `/templates/contracts` | 创建合同模板 |
| 编辑合同模板 | PUT | `/templates/contracts/:id` | 编辑合同模板 |
| 删除合同模板 | DELETE | `/templates/contracts/:id` | 删除合同模板（软删除） |
| 阶段任务模板列表 | GET | `/templates/stages` | 获取阶段任务模板列表 |
| 创建阶段任务模板 | POST | `/templates/stages` | 创建阶段任务模板 |
| 编辑阶段任务模板 | PUT | `/templates/stages/:id` | 编辑阶段任务模板 |
| 删除阶段任务模板 | DELETE | `/templates/stages/:id` | 删除阶段任务模板 |

#### 4.3.7 消息模块

| 接口 | Method | Path | 说明 |
|------|--------|------|------|
| 消息列表 | GET | `/messages` | 分页查询消息 |
| 未读数量 | GET | `/messages/unread-count` | 获取未读消息数量 |
| 标记已读 | POST | `/messages/:id/read` | 标记单条消息已读 |
| 全部标记已读 | POST | `/messages/read-all` | 全部标记已读 |

#### 4.3.8 文件上传

| 接口 | Method | Path | 说明 |
|------|--------|------|------|
| 上传文件 | POST | `/files/upload` | 通用文件上传 |

**文件上传限制：**

| 类型 | 格式 | 大小限制 |
|------|------|---------|
| 图片 | jpg, jpeg, png, webp | 10MB |
| 视频 | mp4, mov | 100MB |
| 文档 | pdf, doc, docx | 20MB |

#### 4.3.9 分享模块

| 接口 | Method | Path | 说明 |
|------|--------|------|------|
| 创建分享 | POST | `/shares` | 创建分享链接 |
| 查看分享内容 | GET | `/shares/:code` | 通过分享码查看内容 |

---

## 5. 扫码登录与授权机制

### 5.1 流程概述

PC 端扫码登录分为两种场景：
- **场景A：运营/合同发起方直接登录** — 在 PC 端输入账号密码登录
- **场景B：合同发起方扫码授权登录** — 从小程序跳转到 PC 端编辑合同

### 5.2 场景B 详细流程

```
┌──────────┐     ┌───────────┐     ┌──────────┐
│  小程序   │     │  PC浏览器  │     │  服务端   │
└────┬─────┘     └─────┬─────┘     └────┬─────┘
     │                 │                 │
     │  1.点击"在电脑端  │                 │
     │    打开"，获取    │                 │
     │    授权链接       │                 │
     │◄──────────────────────────────────│
     │                 │                 │
     │  2.显示链接+二维码 │                 │
     │    （含授权ticket）│                 │
     │─────────────────►│                 │
     │                 │                 │
     │                 │  3.用户在浏览器   │
     │                 │    打开链接，显示  │
     │                 │    登录二维码      │
     │                 │─────────────────►│
     │                 │                 │
     │  4.用户用小程序   │                 │
     │    扫描二维码     │                 │
     │──────────────────────────────────►│
     │                 │                 │
     │  5.小程序显示授权  │                 │
     │    确认页          │                 │
     │◄──────────────────────────────────│
     │                 │                 │
     │  6.用户确认授权   │                 │
     │──────────────────────────────────►│
     │                 │                 │
     │                 │  7.PC端轮询检测  │
     │                 │    到授权成功    │
     │                 │◄────────────────│
     │                 │                 │
     │                 │  8.携带合同参数  │
     │                 │    跳转到编辑页   │
     │                 │                 │
```

### 5.3 技术实现要点

**二维码内容：**

```json
{
  "ticket": "UUID",
  "type": "contract_edit",
  "contract_id": "xxx",
  "expire_at": 1748200000000
}
```

**安全规则：**
- 二维码有效期：**5 分钟**，过期需刷新
- 授权 ticket 使用一次后失效
- 授权后 PC 端 session 有效期：**2 小时**，超时需重新扫码
- 同一用户同一时间最多 **2 个**活跃 PC 端 session
- 二维码使用签名机制防伪造（HMAC-SHA256）
- 扫码时校验小程序用户身份与合同发起方角色是否匹配

### 5.4 角色判断逻辑

```
扫码登录后，系统根据用户身份判断跳转目标：
├── 运营角色 → 合同列表页（pc-contract-list.html）
├── 合同发起方角色 + 携带 contract_id 参数 → 合同编辑页
├── 合同发起方角色 + 携带 contract_id + change=true → 合同变更编辑页
└── 合同发起方角色 + 无参数 → 错误页（缺少必要参数）
```

---

## 6. 合同正文编辑器方案

### 6.1 编辑器类型

采用 **结构化富文本编辑器**，基于 TipTap（ProseMirror）实现。

### 6.2 功能定义

| 功能 | 支持 | 说明 |
|------|------|------|
| 标题（H1-H4） | ✅ | — |
| 正文段落 | ✅ | — |
| 加粗/斜体/下划线 | ✅ | — |
| 有序/无序列表 | ✅ | — |
| 表格 | ✅ | 支持合并单元格 |
| 图片插入 | ✅ | 最多20张，单张≤5MB |
| 变量占位符 | ✅ | `{{project_name}}`、`{{total_amount}}` 等 |
| 分页符 | ✅ | 用于打印/PDF分页 |
| 页面预览 | ✅ | 模拟A4纸面预览 |

### 6.3 变量系统

从项目信息和合同基本信息中自动提取变量，在编辑器中以蓝色标签展示：

| 变量名 | 来源 | 示例 |
|--------|------|------|
| `{{project_name}}` | 项目名称 | xxx装修项目 |
| `{{project_address}}` | 项目地址 | xx市xx区xx路xx号 |
| `{{total_amount}}` | 合同金额 | 100,000.00 |
| `{{party_a_name}}` | 甲方姓名 | 张三 |
| `{{party_b_name}}` | 乙方名称 | 某装修公司 |
| `{{start_date}}` | 开工日期 | 2026-07-01 |
| `{{end_date}}` | 竣工日期 | 2026-10-01 |
| `{{sign_date}}` | 签约日期 | 2026-06-29 |

### 6.4 阶段任务关联

阶段任务独立于合同正文存在，在编辑页面作为独立模块展示和编辑。合同正文可通过"插入阶段任务引用"按钮在正文中插入一个阶段任务的概览卡片（只读），点击可跳转到阶段任务模块。

### 6.5 字数限制

- 合同正文最大字数：**50,000 字**（含格式标记）
- 超出时提示"合同正文超出长度限制"

---

## 7. 阶段任务模板体系

### 7.1 模板层级结构

```
阶段任务模板
├── 基本信息：名称、适用城市、适用项目类型
└── 阶段列表
    └── 阶段1
        └── 任务列表
            └── 任务1：名称、执行标准、确认标准、担责标准
```

### 7.2 创建合同时的模板应用流程

1. 服务方创建合同，选择合同类型
2. 系统根据合同类型 + 项目所在城市，推荐匹配的阶段任务模板
3. 服务方选择模板（可跳过，手动创建阶段任务）
4. 系统将模板中的阶段和任务自动填充到合同中
5. 服务方可在模板基础上修改、增删阶段和任务
6. 保存合同时，模板数据被复制为合同数据，与模板解耦（后续模板修改不影响已创建的合同）

### 7.3 模板匹配规则

```
匹配优先级：
1. city_code 精确匹配 + project_type 精确匹配（最高）
2. city_code 精确匹配 + project_type 为 null（通用）
3. city_code 为 null + project_type 精确匹配
4. city_code 为 null + project_type 为 null（全通用）
```

### 7.4 版本管理

- 模板修改后版本号 +1
- 已被合同引用的旧版本模板数据不受影响（复制机制）
- 模板列表显示当前版本号和最后更新时间
- 不支持版本回退（如需回退，编辑内容即可）

---

## 8. 项目架构层级规则

### 8.1 层级定义

| 层级 | 名称 | 说明 | 典型实例 |
|------|------|------|---------|
| L1 | 项目部 | 顶层节点，一个项目一个项目部 | xx装修项目部 |
| L2 | 工作组 | 按工种或区域划分 | 水电组、泥瓦组、木工组 |
| L3 | 施工组 | 工作组下的执行单元 | 水电1组、水电2组 |
| L4 | 任务包 | 最小执行单元，关联具体任务 | xx户水电安装 |

### 8.2 管理规则

- **创建权限**：仅项目经理可创建和管理架构
- **创建方式**：逐级创建，必须先有 L1 才能创建 L2，依此类推
- **节点数量限制**：
  - L1：每个项目仅 1 个
  - L2：每个 L1 下最多 20 个
  - L3：每个 L2 下最多 20 个
  - L4：每个 L3 下最多 50 个
- **删除规则**：
  - 有子节点的节点不能删除（需先删除子节点）
  - 有关联任务的节点不能删除（需先转移任务）
  - 删除为软删除，数据保留
- **负责人**：每个节点可设置一个负责人（项目成员）

### 8.3 任务与架构的关联

- 创建任务时可选择关联到 L3 或 L4 层级的架构节点
- 临时任务可不关联架构节点
- 架构页面点击节点可查看该节点下的所有任务

### 8.4 架构图可视化

- 采用 **树形结构图** 展示（横向展开）
- 默认展开 L1-L2，点击节点展开下级
- 每个节点显示：名称、负责人、任务数
- 支持搜索和筛选

---

## 9. 项目成员管理规则

### 9.1 成员加入方式

| 方式 | 操作人 | 说明 |
|------|--------|------|
| 邀请加入 | 项目经理 | 项目经理输入手机号，系统发送邀请，用户确认后加入 |
| 扫码加入 | 成员本人 | 项目经理生成项目二维码，成员用小程序扫码加入 |
| 管理员分配 | 项目经理 | 项目经理直接将已有用户分配为项目成员 |

### 9.2 成员角色与权限

| 角色 | 可执行操作 |
|------|-----------|
| 项目经理 | 全部项目管理操作（创建/编辑/成员管理/架构管理/合同/任务/临时任务） |
| 工长 | 创建合同、创建临时任务、执行任务、上传记录、提交验收、新增对账单 |
| 工人 | 仅可执行分配给自己的任务、上传记录、提交验收 |
| 监理 | 查看全部项目信息（只读），不可编辑 |
| 业主 | 查看项目、确认合同、确认验收、确认对账单 |

### 9.3 成员退出规则

- 仅项目经理可移除成员
- 业主不可被移除（业主是项目核心方）
- 成员被移除后：
  - 已分配的待执行任务需重新分配
  - 历史操作记录保留
  - 成员状态变为 `removed`，不在成员列表中显示
- 成员可主动退出项目（非项目经理、非业主角色），退出前需完成已分配的执行中任务

---

## 10. 创建项目流程

### 10.1 必填字段

| 字段 | 必填 | 校验规则 |
|------|------|---------|
| 项目名称 | 是 | 1-100字符 |
| 项目类型 | 是 | 枚举选择 |
| 项目地址 | 是 | 1-200字符 |
| 所在城市 | 是 | 省市区级联选择 |
| 业主 | 是 | 从用户列表选择或输入手机号新建 |
| 项目预算 | 否 | 数字，≤99,999,999.99 |
| 计划开工日期 | 否 | 日期格式 |
| 计划竣工日期 | 否 | 日期格式，须晚于开工日期 |
| 项目描述 | 否 | 1-500字符 |

### 10.2 创建流程

1. 服务方（项目经理）在小程序点击"创建项目"
2. 填写项目基本信息
3. 选择/新建业主
4. 系统自动生成项目编号
5. 系统自动创建 L1 架构节点（项目部）
6. 创建人和业主自动加入项目成员
7. 项目状态默认为 `pending`（待启动）
8. 创建成功后跳转到项目详情页

### 10.3 项目状态流转

```
pending（待启动）→ in_progress（进行中）→ completed（已竣工）
                      ↓
               terminated（已终止）
```

- `pending` → `in_progress`：项目经理手动启动
- `in_progress` → `completed`：所有合同签约且所有任务完成
- `in_progress` → `terminated`：项目经理操作终止（需填写终止原因）

---

## 11. 消息推送机制

### 11.1 推送渠道

| 渠道 | 适用场景 | 实现方式 |
|------|---------|---------|
| 微信小程序订阅消息 | 小程序用户，重要业务通知 | 微信订阅消息模板 |
| 站内消息 | 所有用户，全部通知 | messages 表 + 消息中心页面 |

### 11.2 推送规则

| 通知类型 | 触发时机 | 接收人 | 渠道 | 模板内容 |
|---------|---------|--------|------|---------|
| 合同待审核 | 合同发起方提交审核 | 运营人员 | 站内消息 | "【合同审核】{合同名称}待您审核" |
| 合同审核结果 | 运营审核完成 | 合同发起方 | 订阅+站内 | "【审核结果】{合同名称}已{通过/驳回}" |
| 合同待确认 | 审核通过后 | 业主 | 订阅+站内 | "【合同确认】{合同名称}待您确认" |
| 合同被业主驳回 | 业主驳回合同 | 合同发起方 | 订阅+站内 | "【合同驳回】业主已驳回{合同名称}" |
| 任务待验收 | 服务方提交验收 | 业主 | 订阅+站内 | "【任务验收】{任务名称}待您验收" |
| 任务被驳回 | 业主驳回任务 | 执行人 | 订阅+站内 | "【任务驳回】{任务名称}被驳回，请处理" |
| 对账单待确认 | 服务方提交对账单 | 业主 | 订阅+站内 | "【对账单】{金额}元对账单待您确认" |
| 对账单被驳回 | 业主驳回对账单 | 服务方 | 订阅+站内 | "【对账单驳回】{金额}元对账单被驳回" |
| 变更待审核 | 合同发起方提交变更 | 运营人员 | 站内消息 | "【变更审核】{合同名称}变更申请待审核" |
| 变更待确认 | 变更审核通过 | 业主 | 订阅+站内 | "【变更确认】{合同名称}变更待您确认" |

### 11.3 订阅消息授权策略

- 在用户首次进入相关页面时引导授权订阅消息
- 每次发送订阅消息消耗一次授权，需定期引导用户重新授权
- 对于重要通知（如待验收），建议在提交时弹出授权引导

### 11.4 消息保留策略

- 已读消息保留 **90 天**，超期自动归档
- 未读消息保留 **180 天**
- 系统通知保留 **365 天**

---

## 12. 分享功能方案

### 12.1 分享载体

采用 **微信小程序转发卡片** 作为主要分享方式，同时支持生成 **H5 分享链接** 供非小程序用户查看。

### 12.2 分享内容与权限

| 分享类型 | 可分享内容 | 接收方权限 |
|---------|-----------|-----------|
| 项目 | 项目基本信息、进度概览 | 仅查看，不可操作 |
| 合同 | 合同基本信息、阶段任务概览 | 仅查看，不可编辑 |
| 任务 | 任务名称、执行标准、执行记录 | 仅查看，不可操作 |
| 对账单 | 对账单详情 | 仅查看，不可操作 |

### 12.3 小程序转发卡片

- 转发时生成卡片：标题 + 摘要 + 缩略图
- 接收方点击卡片进入小程序对应详情页
- 接收方需有查看权限（项目成员或业主）

### 12.4 H5 分享链接

- 生成带 share_code 的 H5 链接
- 有效期：**7 天**
- 访问限制：最多 **50 次** 查看
- H5 页面为只读视图，无操作按钮
- 不展示敏感信息（如金额在非项目成员查看时脱敏）

### 12.5 安全规则

- 分享操作需验证分享人是否有该对象的查看权限
- 分享链接使用签名防伪造
- 业主不可分享（保护业主隐私）
- 分享统计：记录分享人、分享时间、查看次数

---

## 13. 临时任务定义

### 13.1 概念

临时任务是 **不在合同阶段任务计划内** 的零散任务，用于处理项目执行过程中的临时性工作（如临时修补、额外增项等）。

### 13.2 与合同阶段任务的区别

| 维度 | 合同阶段任务 | 临时任务 |
|------|------------|---------|
| 来源 | 合同阶段任务模板 | 手动创建 |
| 关联合同 | 必须关联 | 可不关联 |
| 关联阶段 | 必须关联 | 无阶段 |
| 创建权限 | 系统自动生成（模板） | 项目经理/工长 |
| 验收流程 | 同合同任务 | 同合同任务 |
| 编号前缀 | TK | TK-T |

### 13.3 创建规则

- 创建人：项目经理、工长
- 必填字段：任务名称、所属项目、执行人
- 可选字段：关联合同、执行标准、确认标准、担责标准、关联架构节点
- 验收流程与合同阶段任务完全一致（执行 → 上传记录 → 提交验收 → 业主确认/驳回）

---

## 14. 活动列表定义

### 14.1 概念

"活动"即 **项目动态**，记录项目中发生的关键事件，供项目成员了解项目进展。

### 14.2 活动类型

| 类型 | 触发事件 | 显示内容 |
|------|---------|---------|
| 合同创建 | 创建合同 | "{创建人}创建了合同{合同名称}" |
| 合同签约 | 合同签约 | "{签约人}签约了合同{合同名称}" |
| 合同变更 | 发起变更 | "{发起人}发起了合同{合同名称}的变更申请" |
| 任务开始 | 开始执行任务 | "{执行人}开始执行任务{任务名称}" |
| 任务提交 | 提交验收 | "{执行人}提交了任务{任务名称}的验收申请" |
| 任务完成 | 业主确认验收 | "{确认人}确认验收了任务{任务名称}" |
| 任务驳回 | 业主驳回任务 | "{确认人}驳回了任务{任务名称}" |
| 对账单提交 | 新增对账单 | "{提交人}提交了{金额}元对账单" |
| 对账单确认 | 业主确认对账单 | "{确认人}确认了{金额}元对账单" |
| 对账单支付 | 上传支付凭证 | "{上传人}上传了支付凭证，对账单已支付" |
| 成员加入 | 成员加入项目 | "{成员姓名}加入了项目" |
| 成员移除 | 成员被移除 | "{操作人}将{成员姓名}移出了项目" |

### 14.3 展示规则

- 按时间倒序排列
- 每页 20 条，支持分页
- 支持按类型筛选
- 项目详情页"今日动态"卡片仅展示当天活动
- 点击活动可跳转到关联的业务详情页

---

## 15. 对账单财务关联规则

### 15.1 金额规则

- 金额精度：**2 位小数**
- 正数表示支出（服务方向业主请款）
- 负数表示冲销/退款（如多收金额退回）
- 单笔金额上限：**9,999,999.99 元**

### 15.2 累计统计

- "累计已生效金额" = 该合同下所有 `effective` 状态对账单金额之和
- "待确认金额" = 该合同下所有 `pending` 状态对账单金额之和
- 按项目维度也可统计：项目下所有合同的对账单汇总

### 15.3 驳回后处理

- 对账单被驳回后，原单状态保持 `rejected`，**不修改原单**
- 服务方可查看驳回原因，然后**新建一条对账单**重新提交
- 新对账单可选关联原对账单（`related_statement_id` 字段），便于追溯
- 已生效的对账单不可撤销或修改

### 15.3.1 支付凭证规则

- 支付凭证仅在对账单状态为 `effective`（已生效）时可上传
- 服务方（项目经理/工长）可上传支付凭证图片，最多**9张**
- 支付凭证支持格式：jpg、jpeg、png、webp，单张图片最大**10MB**
- 上传支付凭证后，对账单状态变为 `paid`（已支付）
- 已支付状态的对账单不可再次上传支付凭证
- 支付凭证支持点击预览大图

### 15.4 导出功能

- 支持按合同/项目/时间范围导出对账单列表
- 导出格式：Excel（.xlsx）
- 导出字段：对账单编号、合同名称、类型、金额、用途、状态、提交人、提交时间、确认人、确认时间

### 15.5 与实际收付款的关系

> MVP 版本不接入支付系统，对账单仅作为双方确认的账务记录，不直接关联支付。后续版本可考虑接入微信支付或线下收款记录。

---

## 16. 评价体系设计

### 16.1 评价规则

| 规则 | 说明 |
|------|------|
| 评价人 | 仅业主可评价 |
| 评价时机 | 业主确认验收时同时提交评价 |
| 评价对象 | 任务（非项目、非服务方个人） |
| 唯一性 | 一个任务仅一条评价，提交后不可修改 |
| 必填项 | 星级评分（必填）、标签（至少选1个）、文字评价（选填，1-300字） |

### 16.2 评价标签

**正面标签：**
- 按时完成、质量好、专业规范、现场整洁、态度认真

**负面标签：**
- 需要改进、进度拖延、质量问题、现场凌乱、沟通不畅

### 16.3 评价可见性

| 角色 | 是否可见 |
|------|---------|
| 业主（评价人） | ✅ 可见 |
| 服务方（被评价方） | ✅ 可见（仅看自己项目的评价） |
| 其他业主 | ❌ 不可见 |
| 平台运营 | ✅ 可见 |

### 16.4 后续扩展（第二版）

- 服务方个人累计评价展示（电子名片上显示平均星级）
- 项目维度评价汇总
- 评价标签云展示

---

## 17. 异常场景与边界条件

### 17.1 合同相关

| 场景 | 处理规则 |
|------|---------|
| 合同审核驳回后重新提交 | 无次数限制；每次驳回原因保留在合同变更记录中；驳回后合同状态回到 `drafting` |
| 业主长期不确认合同 | 系统 72 小时后发送提醒通知；无自动超时机制（需业主主动操作） |
| 合同金额修改 | 编辑合同金额时需填写修改原因；金额变更记录在合同变更记录中 |
| 甲乙方信息变更 | 需发起合同变更流程，不能直接编辑 |
| 合同附件删除 | 拟定中状态可删除；其他状态不可删除，仅可追加 |

### 17.2 任务相关

| 场景 | 处理规则 |
|------|---------|
| 任务被驳回后重新执行 | 保留之前所有轮次的执行记录（按 upload_round 区分）；驳回原因在任务详情中展示 |
| 业主长期不验收 | 系统 72 小时后发送提醒通知；无自动超时机制 |
| 执行人被移除项目 | 待执行任务需项目经理重新分配；进行中任务暂停，等待重新分配 |
| 多人同时上传执行记录 | 允许同一任务多个有权限的用户上传记录，按时间排序合并展示 |
| 任务执行中但合同被终止 | 任务状态自动标记为 `cancelled`，不计入完成率统计 |

### 17.3 对账单相关

| 场景 | 处理规则 |
|------|---------|
| 对账单被驳回后 | 不修改原单，新建对账单重新提交，可关联原单 |
| 业主长期不确认对账单 | 系统 72 小时后发送提醒通知 |
| 金额为负数 | 允许，用于冲销/退款；列表中负数金额红色显示 |
| 对账单确认后发现错误 | 已生效对账单不可撤销；需新建一笔负数对账单冲销 |
| 并发新增对账单 | 允许，对账单编号通过数据库序列保证唯一 |
| 支付凭证上传失败 | 前端提示"上传失败，请重试"，支持重新上传 |
| 支付凭证图片超限 | 前端拦截，提示"单张图片最大10MB" |
| 已支付后发现凭证错误 | 已支付状态不可修改；需联系平台运营处理 |

### 17.4 权限相关

| 场景 | 处理规则 |
|------|---------|
| 用户同时在多个项目 | 允许，各项目数据隔离 |
| 用户角色变更 | 由项目经理在项目成员管理中修改角色；变更后即时生效 |
| 业主更换 | 仅平台运营可操作，需同时更新所有关联合同的甲方信息 |
| Token 过期 | 返回 40101 错误码，前端跳转登录页 |

### 17.5 文件上传相关

| 场景 | 处理规则 |
|------|---------|
| 网络中断 | 前端缓存文件，网络恢复后自动重试（最多3次） |
| 文件超限 | 前端拦截，提示文件大小限制 |
| 不支持的格式 | 前端拦截，提示支持的格式 |
| 上传后文件损坏 | 后端校验文件完整性，损坏文件返回错误 |
| 离线缓存上限 | 单个任务最多缓存 20 个文件（约 200MB） |

---

## 附录：待确认事项

以下事项需与 Z 进一步确认后补充：

1. **项目终止后的数据处理**：已签约合同是否继续有效？进行中任务如何处理？
2. **数据导出需求**：除了对账单外，合同、任务等是否需要支持导出？
3. **多语言**：当前仅中文，是否需要预留多语言支持？
4. **审计日志**：是否需要记录所有关键操作的审计日志（合规要求）？
5. **数据备份策略**：数据库备份频率和保留周期？
6. **SLA 要求**：系统可用性目标（99.5%? 99.9%?）？

---

_文档结束。后续根据 Z 反馈持续迭代。_
