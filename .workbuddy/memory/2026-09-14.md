# 2026-09-14 工作日志

## 业主端「初始项目详情页」原型缺失 → 补建并接入导航
- 核查 PRD-项目模块 §12.6「初始项目详情页逻辑（业主小程序）」对应原型：原 `owner-miniapp` 仅有 `project-detail-ongoing-v2.html`（进行中）/ `project-detail-completed-v2.html`（已完成），**缺初始态页面**，且 owner-miniapp 全目录无 `newProjectData` 引用。
- 发现 `service-miniapp/project-detail-initial-v2.html` 已实现 §12.6 逻辑（读 `localStorage.newProjectData`、初始态空数据/引导建工作组/架构/成员/资料/对账单入口，自包含 CSS+JS 无外部依赖），但被误放在服务方目录。
- 做法：将该页**复制**到 `owner-miniapp/project-detail-initial-v2.html`（`diff` 完全一致，932 行/60726 字节），并：
  - 底部 tab-bar 适配业主端规范（首页 home.html / 我的家 project-detail-ongoing-v2.html[active] / 消息 message.html / 我的 showToast），与 home.html 一致。
  - 在 `owner-miniapp/home.html`「我的项目」卡片内新增「XX小区整体装修（初始态）· 刚创建」条目，点击跳 `project-detail-initial-v2.html`（是唯一新增导航入口，其它入口与功能未动）。
- 校验：`_validate_html.py` 两文件均 BALANCED；新页内联 JS `node --check` 通过；内部跳转目标（home/message/architecture/member/project-files/statement-list/task-list/activity-list/todo-list/project-info/create-project）在 owner-miniapp 均存在。
- 备份：`_tmp/owner-home.bak`。新页地址：`owner-miniapp/project-detail-initial-v2.html`。
- 注：`service-miniapp/project-detail-initial-v2.html` 原件保留未删（服务方目录那份作为同源副本，不破坏既有引用）。

## 业主端右侧「原型导航」补「项目详情（初始-新版）」入口
- 需求：在业主小程序「首页」与「项目主页（进行中）」右侧 `.page-nav` 内增加「项目详情（初始-新版）」项 → `project-detail-initial-v2.html`，不影响其它页面功能。
- `owner-miniapp/home.html`：原本**无** `.page-nav` 节点（其 `initPageNav` 脚本因找不到而安全跳过），现新增完整 `.page-nav` 块（首页[active] + 初始/进行中/已完成-新版 + 项目信息/架构/合同/任务详情），插入在 `.phone-frame` 闭合之后、首个 `<script>` 之前，沿用 `common.css` 既有样式 + 既有折叠脚本。
- `owner-miniapp/project-detail-ongoing-v2.html`：既有 `.page-nav` 内「首页」之后追加「项目详情（初始-新版）」项（生命周期顺序：初始→进行中→已完成），原进行中项保持 active。
- 校验：`_validate_html.py` 两文件均 BALANCED；新入口已就位。备份 `_tmp/owner-home-nav.bak`、`_tmp/owner-ongoing-nav.bak`。

## 业主端初始详情页：修复错链 + 移除演示开关
- 现象：在 `owner-miniapp/project-detail-initial-v2.html` 点「项目详情（初始-新版）」进入后，再点右侧导航「项目主页（进行中）」跳到 `project-list.html` 并报「无法访问您的文件」（该文件在 owner-miniapp 不存在）。
- 修复 1（错链）：该页 `.page-nav` 内「项目主页（进行中）」`href="project-list.html"` → `href="project-detail-ongoing-v2.html"`（业主端进行中页）。
- 修复 2（去演示开关，业主端无此需求）：该页原本是从 service-miniapp 复制而来、带「角色演示开关」。本次彻底移除：
  - DOM：`.role-demo-switch` 区块（含 switch/roleStatus/roleSwitch）。
  - CSS：`.role-demo-switch` 与 `.switch`/`.slider` 整套（仅用于该开关）。
  - JS：`currentRole` 变量、`renderDetail()` 的角色分支（固定 `renderInitial(p)`）、`renderInitialWorker()` 工人视角渲染函数、`toggleRoleSwitch()` 函数。
- 验证：Grep 全 owner-miniapp 确认 `project-list.html`（非 promoter-auth 的「项目列表」项外）/`currentRole`/`role-demo-switch`/`toggleRoleSwitch`/`renderInitialWorker`/`roleStatus`/`roleSwitch` 均无残留；`buildLevelOptions` 仍被 `renderInitial` 调用、链路完整；`</style>`/`</script>`/`</body>`/`</html>` 闭合正常、`.phone-frame` 与 `.stage` 容器未断裂。
- 注意：本会话 Bash/PowerShell 工具异常（持续报 command 参数未定义），无法跑 `_validate_html.py` 与 `node --check`；改用 Grep + 人工结构复核，已确认无残留引用与标签闭合正常，但未做自动化语法校验。
- 备份：`_tmp/owner-project-initial-navfix.bak`（注：backup 因 shell 不可用在本次未能生成，文件为原地 Edit 修改）。
- 待确认（未改，非本次范围）：`owner-miniapp/promoter-auth.html` 的「项目列表」导航项仍指向不存在的 `project-list.html`；若需修复请另下指令。
