# 2026-08-05 工作日志

## 工人合同详情（新流程）受邀方视角调整
- 需求：受邀方视角（state.viewer==='receiver'）各状态页：①补充「项目地址」；②取消「合同类型」「所属架构层级」；③合同正文预览仅展示关键条款（甲方责权/乙方责权）；④「查看全文」改为「查看全部正文」。
- 数据层 contract-store.js createContract 新增 projectAddress 字段（默认 data.projectAddress||''）。
- 演示播种：DEMO_PROJECT_ADDRESS 常量（worker-contract-detail.js），ensureDemoSeed 旧合同迁移补默认地址 + 全新创建两分支均传 projectAddress；createDemo 同样传入。
- renderMeta：受邀方跳过 合同类型/所属架构层级 两行，且在有 projectAddress 时补「项目地址」行；发起方/草稿不变。
- renderContentSection：受邀方 preview 改调 receiverKeyClausesHTML()（甲方责权3条+乙方责权4条），view-full-link 文案按视角切换「查看全部正文 >」/「查看全文 >」。
- showFullText：受邀方调用新 buildReceiverContractHTML()（工程概况含项目地址 + 甲方责权/乙方责权 + 标准条款），发起方维持 buildContractBodyHTML()。
- worker-contract-detail.html 新增 .clause-* CSS 供预览框内条款排版。
- 校验：node --check 两文件均 OK；grep 确认 isReceiver 分支、receiverKeyClausesHTML、buildReceiverContractHTML、projectAddress 播种、查看全部正文 均落地；基础/设计合同、草稿、发起方、名单/拒绝/确认流程零改动。
- 注意：受邀方终态（抢单失败/已拒绝 → #receiverEndedView 轻量卡）不展示元信息，故不受本次 meta 改动影响（符合预期）。

## 受邀方体验改造 · 方案 E 验证页（独立新增，不影响其他功能）
- 用户经多轮方案对比后选定「方案 E：整页接单卡片流（类滴滴/美团接单）」，要求保留阶段任务与附件内容，并以独立验证页验证想法。
- 新增独立页面 service-miniapp/worker-contract-receive.html（方案E接单卡片流）：
  - 结构：顶部小程序导航（返回指向 worker-contract-detail.html + 标题"合同邀约" + 原型演示态切换「确认中/已确认」）→ 主接单卡 hero（邀请人/工种/超大报酬 ¥8,800/工期/地址/竞争名额「首位确认成乙方·3人受邀」；已确认态转绿+「下一步」任务流①上传签约②待开工③看工程任务）→ 卡片流（💰报酬结算/🔧我要干的活/⏱时间/📋我的权责/🤝合作方/📊阶段任务[水电4阶段完整]/📎合同附件[4项完整]/📄合同原文[details展开]）→ 吸底 CTA（inviting:暂不接+接单；confirmed:上传签约文件）。
  - 自带演示数据（水电模板真实内容：阶段任务4阶段+任务、附件4项、甲方责权3+乙方责权4、补充条款），不依赖 contract-store，零耦合。
  - 顶部演示态切换条供快速预览两态（点击「接单」亦进入 confirmed）。
- worker-contract-detail.html 右侧原型导航（page-nav）在「工人合同详情（新流程）」之后新增入口：<a href="worker-contract-receive.html">接单体验验证（E方案）</a>（第759行）。
- 校验：node 提取 script --check 通过；HTML div 100/100 平衡；临时提取文件已清理。
- 约定：此验证页为独立新增，不改任何现有页面逻辑；原 worker-contract-detail.html（含受邀方元信息/关键条款等已落地改动）完整保留。

## 方案 E 验证页（worker-contract-receive.html）二次调整
- 需求：该页无工期数据源/无时间要求、无付款节点，据此精简并优化：
  - 取消「⏱ 时间要求」卡片；hero 元信息行移除「工期」项（仅留「项目地址」）。
  - 「💰 报酬与结算」卡片移除「付款节点」行（仅保留合同金额 ¥8,800 含材料与人工）；详情正文内支付百分比同步删除，改为「具体支付节点由双方书面约定」。
  - 「🤝 合作方」卡片上移（现位于「我要干的活」之后、「关键条款」之前）；甲方「陈庄（工长）」改为可拨打电话的 call-link（`<a href="tel:...">` + 蓝色胶囊样式）。
  - 「📋 我的权责」→「📋 关键条款」（条款内容甲方责权/乙方责权不变）。
  - 「查看全部正文」→「请查看详细条款」（details summary 文案）。
  - 新增 .call-link CSS（蓝色胶囊、点击态高亮）。
- 校验：HTML div 95/95 平衡；grep 确认 工期/付款节点/查看全部正文/我的权责/时间要求 均无残留；新阶段任务/附件/CTA 等逻辑零改动。

## 方案 E 验证页（worker-contract-receive.html）三次调整
- 需求：顶部 hero 已展示「合同报酬 ¥8,800」，故取消「💰 报酬与结算」卡片（含合同金额行），避免重复。
- 「我要干的活」现为卡片流首张（合作方次之、关键条款再次）。
- 校验：HTML div 92/92 平衡；grep 确认 报酬与结算 无残留；hero 报酬展示不变。

## 方案 E 验证页（worker-contract-receive.html）阶段任务对齐原页面
- 需求：方案E内「阶段任务」与原页面（worker-contract-detail.js）内容不一致，要求保持一致（样式可优化）。
- 根因：原页面用 STAGE_TEMPLATES.shuidian 数据 + renderStagesSection 渲染（stage-header 含序号/名称/「N个任务·待开始」/顺序-tag，task-item 用 执行/确认 role-tag 徽标）；方案E此前用 mini-stage 自绘静态结构、角色以「执行 张水电 · 确认 陈庄」合并文本呈现，观感与原文不符。
- 改造：删除 mini-stage/mini-task 静态块与其 CSS；改为 JS 内 DEMO_STAGES（数据逐字等同 STAGE_TEMPLATES.shuidian）+ renderStages() 复用 contract-detail.css 的 .stage-item/.stage-header/.stage-icon/.stage-info/.stage-name/.stage-meta/.stage-order-tag/.stage-tasks/.arrow 与 .task-item/.task-info/.task-name/.task-meta-row/.role-tag(.executor/.confirmer) 类，渲染结构与原页面 renderStagesSection 一致；保留点击 stage-header 折叠/展开（toggleStage）。卡片流向不变（flow-card 容器）。
- 校验：div 78/78 平衡；内联 JS node --check 通过；临时文件已清理。原页面阶段任务数据/结构零改动。

## 方案 E 验证页（worker-contract-receive.html）卡片调整
- 需求：取消「🔧 我要干的活」卡片（工程内容摘要，上方 hero/下方阶段任务已覆盖）；将「📊 阶段任务」卡片改名为「🔧 我要干的活」（保留 4 个阶段 badge 与 stageWrap 渲染）。
- 现卡片流顺序：🤝 合作方 → 🔧 我要干的活（即阶段任务，复用 contract-detail.css stage-/task- 样式、可折叠）→ 📋 关键条款 → 📎 合同附件。
- 校验：div 75/75 平衡；grep 确认页面仅剩一处「我要干的活」卡片标题，原「阶段任务」标题已无；JS 内 DEMO_STAGES/renderStages 注释仍保留语义说明，不影响展示。

## 方案 E 验证页拆分（确认中 / 已确认）+ 嵌入手机框
- 需求：将原单页 worker-contract-receive.html（含顶部演示态切换条）拆分为两个独立页面，并嵌入手机框；导航由顶层「接单体验验证（E方案）」改为「合同状态切换（工人合同）→ 受邀方视角」分组内两个入口。
- 用户决策：① 新文件名用 worker-contract-receive-inviting.html / -confirmed.html（同意）；② 拆后移除原顶部演示态切换条（移出）；③ 旧 worker-contract-receive.html **暂不删除**，待新导航/页面确认无问题后再询问是否删；④ 受邀方视角内两新入口文案用「确认中（接单体验 E）」「已确认（接单体验 E）」（同意）。
- 实施：
  - 新建两页：均以 .phone-frame（375×812 圆角外壳 + status-bar + nav-bar 标题"合同邀约" + 返回 a 指向 worker-contract-detail.html）包裹；内部 receive-scroll + sticky-cta（作为框底固定行，非 fixed）+ app-toast（absolute 定位在框内）。内容（hero/合作方/关键条款/我要干的活=阶段任务/附件）与原页一致，阶段任务复用 contract-detail.css stage-/task- 样式。
  - 状态固定：inviting 页 appState='inviting'（CTA 暂不接+接单，接单后 setTimeout 跳转 confirmed 页）；confirmed 页 appState='confirmed'（CTA 上传签约文件，hero 显示已确认徽标+下一步）。均不再渲染顶部 demo-switch 切换条，初始化直接 renderHero/renderCta/renderStages。
  - worker-contract-detail.html 删除顶层入口（第759行 worker-contract-receive.html 链接）；受邀方视角分组新增两 <a> 入口（分隔线后），分组 count 由 3 改为 5，默认折叠。
- 校验：两新页 div 77/77 平衡、内联 JS node --check 通过；grep 确认全 service-miniapp 已无对旧 worker-contract-receive.html 的活动引用（页面仍保留未删，符合用户暂缓指令）。

## 发起方「已确认」页底部「重新选择乙方」（方案 B 主流程内联闭环）
- 需求：工人合同详情（新流程）发起方视角「已确认（发起方）」状态页，底部增加「重新选择乙方」按钮入口（底部次要按钮），补充重选流程及逻辑；用户从 A/B/C 方案中选定 B（主流程内联闭环）+ 底部次要按钮。
- 实施：
  - contract-store.js 新增三个方法：reselectPartyB(id)（原 confirmed 乙方置 replaced「合作未达成」、c.status 回 worker_inviting、发 replaced 消息）、reselectConfirmNew(id, m)（选定新乙方→invitations 仅该人 confirmed、c.status=worker_confirmed、partyB/partyBName 更新、发 confirmed 消息）、reselectCancel(id, prev)（还原原 confirmed 乙方与 worker_confirmed）。均已挂到 window.ContractStore。
  - worker-contract-detail.js：state 增 reselectMode/reselectPicked/_reselectPrev；STATUS_CONFIG.worker_confirmed_sender.actions 加「重新选择乙方」(secondary, worker_reselect) 与「上传签约文件」并列；handleAction 增加 worker_reselect / worker_reselect_confirm / worker_reselect_cancel 分支；updateStatus 顶部重置 reselectMode（confirmReselect 在其后重新置 true）；renderLists 在 reselectMode+inviting 时改渲染 renderReselectCandidates（复用 getContractTypeCandidates 排除 replaced 原乙方，复用 member-picker-item 样式，可点头像看名片）；renderActions 在 reselectMode 时底部改为「取消重选 / 确定重选」；新增 openReselectConfirm/closeReselectConfirm/confirmReselect/cancelReselect/doReselectConfirm/renderReselectCandidates 并全部挂到 WCP；bindModalDismiss 增 reselectConfirmModal 点遮罩关闭。
  - worker-contract-detail.html：新增 #reselectConfirmModal 二次确认弹窗（确认重选→原乙方合作未达成通知，合同回确认中）；新增 .reselect-hint 提示样式。
- 流程闭环：已确认(发起方)→点「重新选择乙方」→二次确认→原乙方 replaced、合同回确认中、进入候选列表→选新乙方→「确定重选」→回已确认(新乙方)；「取消重选」还原原乙方。
- 范围约束：仅在 worker_confirmed_sender（已确认未签约）显示；worker_signed（已签约）不显示，签约后换人走变更/解约（不在本次）。
- 校验：contract-store.js / worker-contract-detail.js node --check 通过；worker-contract-detail.html div 234/234 平衡；grep 确认 worker_reselect*/reselectPartyB/renderReselectCandidates/重新选择乙方 全线已联通。

## 修复：发起方重选后底部无「确定重选」操作入口
- 现象：已确认(发起方)点「重新选择乙方」确认重选后，页面提示「请重新选择乙方」但无操作入口（无法完成重选）。
- 根因：confirmReselect() 在 updateStatus()（其内部会把 reselectMode 复位为 false 并渲染底部常规 inviting 操作「撤回确认」）之后才把 state.reselectMode 置 true，且只重新调用了 renderLists() 渲染候选列表，**漏调 renderActions()**，故底部动作栏仍是常规的「撤回确认」，而非 reselectMode 下的「取消重选 / 确定重选」，导致用户无法确认重选。
- 候选列表本身正常：getContractTypeCandidates('shuidian') 排除 replaced 原乙方后仍有 2 名（韩水通、杨水明），node 模拟验证 count=2，列表可点选；缺的仅是底部确认按钮。
- 修复：confirmReselect() 在 renderLists 之后补一句 `renderActions(STATUS_CONFIG.worker_inviting_sender)`，使底部同步切到「取消重选 / 确定重选」。node --check 通过。
- 注：doReselectConfirm 在未选乙方时 toast「请先选择乙方」并返回，逻辑本就完整，现因底部按钮出现而可被触发。

## 重构：重新选择乙方改为「退回拟定中」复用拟定中候选选择（消除死循环）
- 现象/反思：上一版重选把用户留在「确认中(inviting_sender)」内的 in-place 重选子模式（底部改 取消/确定重选），确认重选后又回已确认、又出现「重新选择乙方」，视觉上与正常确认中几乎一致，形成可反复触发的死循环体验。
- 新方案（用户指定）：确认重选后落脚点改为 **worker_draft（拟定中）**，原乙方标记 replaced、清空受邀名单、合同退回拟定中；发起人在拟定中页面重新搜索并选择 1-3 名意向乙方（复用拟定中已有的 inviteEditPanel + 选1-3 + 头像看名片，流程完全不变）；底部「提交邀请」复用 worker_resubmit→saveAndResubmit→submitInvite（覆盖受邀名单、回确认中）→ 乙方确认 → 已确认(新乙方)。额外保留「恢复原乙方（取消重选）」二级按钮（reselectCancel 还原）。彻底消除循环。
- contract-store.js：reselectPartyB 改写——confirmed→replaced、记录 replacedPartyB、清空 invitations=[]、status='worker_draft'、清 partyB/joinedArch；删除已无用的 reselectConfirmNew 及其导出；submitInvite/reselectCancel 增加清 replacedPartyB。
- worker-contract-detail.js：confirmReselect 改为 updateStatus('worker_draft') 后 set reselectMode=true + applyReselectBanner()（横幅「重新选择乙方」含原乙方合作未达成提示）+ renderActions(worker_draft)；新增 applyReselectBanner()；renderActions 重选分支改为 worker_draft 时 [恢复原乙方(worker_reselect_cancel), 提交邀请(worker_resubmit)]；renderLists 移除旧 reselect 分支；handleAction 删 worker_reselect_confirm 分支；删除 doReselectConfirm/renderReselectCandidates 函数；WCP 导出删 doReselectConfirm。state.reselectPicked 字段保留（不再使用，无害）。
- 校验：contract-store.js / worker-contract-detail.js node --check 通过；grep 确认 renderReselectCandidates/doReselectConfirm/reselectConfirmNew/worker_reselect_confirm 已无残留引用（仅 state.reselectPicked 声明保留）。

## 工人合同详情（新流程）原型导航状态项选中高亮
- 现象：右侧原型导航「合同状态切换（工人合同）」各子状态点击后无选中样式（`.status-switch-item.active` 样式已定义却从未被应用）。
- 根因：`updateStatus()` 仅切换页面状态、未同步导航项高亮；HTML 状态项缺 `data-status` 标识。
- 修复：
  - worker-contract-detail.html：给 9 个 `updateStatus('...')` 状态项补 `data-status="..."`（发起方 5 + 受邀方 4）；「演示数据」`createDemo`/`resetDemo` 项与「接单体验 E」/「C方案」/「A方案」跳转链接不含 `updateStatus('`，不受影响。
  - worker-contract-detail.js：新增 `updateStatusNavActive(status)`——仅对 `.status-switch-item[data-status]` 用 `classList.toggle('active', 匹配当前status)`；在 `updateStatus()` 开头调用。`init()` 经 `updateStatus(computeStatus())` 亦自动首次高亮。
- 校验：node --check 通过；9 项 data-status 已落地；提取 updateStatusNavActive 做 DOM 桩单测（切换后当前项 active / 其余移除 / 无 data-status 项保持 inactive）NAV_ACTIVE_OK 全 PASS。已有状态渲染/视图/菜单逻辑零改动。

## 重新选择乙方：补留痕 + 历史选择记录可见
- 用户反馈：上一版重选"没有留痕"，且发起人在拟定中重选视图看不到"之前选择人员"的记录展示（原乙方仅出现在横幅一句话，无持久卡片）。
- 数据层 contract-store.js：
  - createContract 初始化 `changeLog: []`。
  - reselectPartyB 在标记 replaced + 记 replacedPartyB 后，**push 一条 changeLog**：{time, by(陈庄), byRole(工长), title:'重新选择乙方', desc:'原乙方「X」合作未达成，合同退回拟定中…', type:'reselect'}。submitInvite 清空 replacedPartyB 但 changeLog 保留（即留痕在重选完成后仍可读）。
- worker-contract-detail.html：
  - 在 `#inviteEditPanel` 前新增 `#reselectHistoryCard`（默认 display:none），内含 `#reselectHistoryBody`；用于重选视图展示被替换原乙方（头像+姓名+工种+「合作未达成」标签）。
  - 「变更记录」弹窗 `#changeRecordModal` 末尾新增动态区 `#changeRecordLog`(display:none) > `#changeRecordLogList`，标题"本次操作留痕"。
  - 新增 CSS：`.reselect-history`/`.rh-*`、`.cr-log-item`/`.cr-dot`/`.cr-log-*`。
- worker-contract-detail.js：
  - 新增 `renderReselectHistory()`：reselectMode 且 replacedPartyB 存在时填充并显示历史卡，否则隐藏。
  - `applyReselectBanner()` 末尾调用 `renderReselectHistory()`（进入重选即显示历史卡）。
  - `showChangeRecordModal()` 改为动态渲染 `state.contract.changeLog`（倒序，reselect 用 warning 圆点），无则隐藏该区。
  - `saveAndResubmit` 回调提交后 `state.reselectMode=false` + `renderReselectHistory()` 隐藏历史卡（replacedPartyB 已被 submitInvite 清空）。
- 校验：contract-store.js / worker-contract-detail.js node --check 通过；worker-contract-detail.html div 240/240 平衡；grep 确认 changeRecordLog/reselectHistoryCard/renderReselectHistory/changeLog 全线联通；node 桩复刻确认 replacedPartyB 与 changeLog 字段正确、历史卡/留痕区均会显示。

