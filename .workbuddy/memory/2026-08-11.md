# 2026-08-11 工作日志

## 更换阶段任务模板：移除金额字段 + 修复结构不匹配（重要 bug）

用户要求：更换阶段任务模板的选择展示字段须与 PC 端阶段任务模板字段对应（PC 模型 `[{name, order, tasks:[{name,execStandard,confirmStandard,respStandard}]}]` 无 amount）；使用模板后展示须与默认草稿阶段任务一致（含 添加任务/删除任务/按序执行开关/编辑及删除任务/添加阶段，且无金额字段值）。

### 根因（比"金额字段"更深）
`service-miniapp/js/contract-detail.js` 的 `applyStageTemplate` 原本把模板渲染成 `.edit-stage-item`（带 `.task-amount-input` 金额输入框），但：
1. 默认草稿阶段容器 `#editStageList` 用的是 `.stage-card`（`renderStagesFromSnapshot` / `addNewStageDraft` 生成）；
2. `collectDraftSnapshot` / `validateDraft` 只读取 `.stage-card`，**应用模板后若仍是 `.edit-stage-item` 会被保存逻辑完全忽略 → 阶段任务静默丢失**；
3. 该函数调用的 `addTaskToStage(stageIndex)`（数字参数）因函数提升被同名的 `.stage-card` 版 `addTaskToStage(btn)` 覆盖，实际已失效（遗留死代码）。

### 改动
- **重写 `applyStageTemplate`**：改为生成与 `renderStagesFromSnapshot` 完全一致的 `.stage-card`（阶段名称输入框 + 按序执行开关 + +添加任务/×删除阶段；任务为 `.task-edit-item`，含 名称输入框 + ✎编辑/×删除），复用 `addTaskToStage/deleteStage/toggleStageSequential/editTaskDetail/deleteTask`（均为 window 全局）。无金额。
- 删除遗留死代码：`addTaskToStage(stageIndex)`、`removeEditStage`、`removeEditTask`、`addNewStage`（`.edit-stage-item` 版）及对应导出（`ContractDetailPage` 对象与 `window.*`）。
- `stageTemplates` 数据移除所有 `amount`；模板列表 `renderStageTemplateList` 移除「总金额」元信息；`previewStageTemplate` 移除 `¥金额` 展示。
- CSS：删除死规则 `.edit-stage-item`/`.edit-stage-header`/`.edit-task-item`/`.task-amount-input`/`.preview-task-amount`（`.stage-card`/`.task-edit-item` 有独立 CSS，不受影响）。
- 验证：`node --check` 通过。

### 文档
- `PRD-合同模块详细规格.html`：
  - §3.7.2.1「添加任务」字段表移除错误的 `金额[数字，可空]`（add/edit 任务弹窗实际无金额字段，与默认一致）；
  - §3.7.2.2：模板列表元信息标签移除「总金额 ¥X,XXX」；预览「阶段任务明细」移除「¥金额」；使用结果描述改为「与默认草稿一致的 `.stage-card`（不含金额字段）」。
- §7.6.3（工人合同）原本即正确（STAGE_TEMPLATES 无 amount、预览为「任务名称 + 执行 + 确认」），无需改。

### 结论/约束（新增不变式）
**阶段任务（草稿 / 模板 / 工人合同）一律不含金额字段，与 PC 端阶段任务模板字段对齐。** 任何阶段任务模板或新增阶段任务的代码都不得引入金额输入/展示。

## 更换模板弹窗交互一致性修复（合规版 ↔ 工人合同）

用户三诉求：① 合规版点击更换模板先弹「请选择合同正文/阶段任务模板」toast 才出选择弹窗，须与工人合同拟定中选择模板效果一致（直接弹窗）；② 同 Tab 下两条示例预览效果须一致；③ 工人合同更换模板弹窗缺样式（错位/苍白），预览须与合规版一致。

### 改动
- **合规版（service-miniapp/js/contract-detail.js）**：`showChangeTemplatePicker` 与 `showDraftTemplatePicker` 删除三处 `showCustomToast('请选择合同正文/阶段任务模板'/'请选择附件模板')`，改为按当前 Tab 直接 `showTemplatePicker()`/`showStageTemplatePicker()`（附件仍走 `showAttachmentTemplatePicker()` 的「开发中」提示）。与工人合同 `openTemplatePicker` 直接 `classList.add('show')` 行为对齐。
- **工人合同（worker-contract-detail.html 内联 CSS）**：覆盖 `.wc-modal .modal-content` 的 `text-align:center` 对 `#wcTemplatePicker`/`#wcTemplatePreview` 的影响 → 左对齐；为 `.preview-section-title` 加左侧 accent（`border-left:3px solid var(--primary-color)`）对齐合规版；`#wcTplList .member-picker-item` 加分隔线；新增 `.wc-preview-meta` 元信息条样式（类型/阶段数/模板）。
- **工人合同预览弹窗（HTML + JS）**：`wcTemplatePreview` 内新增 `#wcPreviewMeta` 元信息条（替换已移除的 `#wcPreviewType`）；`previewDraftTemplate` 按 kind 填充 类型 +（阶段数｜模板名），与合规版 `templatePreviewModal` 的「类型/城市/更新时间」元信息条视觉对齐。
- 验证：`node --check` 合规版与工人合同 JS 均通过；grep 确认无 `wcPreviewType` 残留引用。

### 说明（待用户确认）
- 合规版同 Tab 下两条示例（tpl_001 标准 / tpl_002 精简；stage_tpl_001 4阶段 / stage_tpl_002 3阶段）预览**框架已一致**（均为 `preview-section` + 标题 + 内容/阶段列表，无金额），差异仅在于模板内容本身（标准版 vs 精简版）。若用户所指「效果不同」是希望两套模板内容排版也统一（如均用「第一条/一、」同一体例），需另行改写模板正文数据。
- 工人合同预览容器仍是居中对话框（`.wc-modal`），合规版预览为底部抽屉（`.template-preview-modal`）；本次仅对齐了「内容渲染效果」（元信息条 + 分区标题 + 阶段卡片），未改容器类型（避免与工人端其它弹窗风格冲突）。如用户要求容器类型也一致需再调整。

## 工人合同更换模板弹窗/预览 → 底部抽屉，与合规版完全统一（2026-08-11 续）

用户新诉求：工人合同点击更换模板的弹窗风格须与「合同详情（合规版）」统一，且**合同预览同步改为底部抽屉形式**。

### 改动
- **HTML（worker-contract-detail.html）**：`#wcTemplatePicker` 与 `#wcTemplatePreview` 由居中 `.wc-modal`/`.modal-box` 改为与合规版一致的结构——`.wc-template-modal` / `.wc-template-preview-modal`（含 `.modal-overlay` + 锚定底部的 `.modal-content` + `.modal-header`/`.modal-body`/`.modal-footer` + 顶/底 sticky 头尾）；新增 `#wcTplFilterType` 筛选信息（替换已无用的 `#wcTplTip`），新增 `#wcTplEmpty` 空状态；预览元信息容器 `#wcPreviewMeta`（`.preview-meta`）与正文 `#wcPreviewContent`（`.preview-content`）。
- **CSS（worker 内联样式）**：以合规版 `contract-detail.css` 的 `.template-picker-modal`/`.template-preview-modal`/`.template-select-item`/`.preview-meta` 为蓝本，改为工人变量（`--primary-color` 等）等价 CSS，替换原 `#wcTemplatePicker .modal-content`/`.wc-preview-meta`/`#wcTplList .member-picker-item`/`.wc-primary-btn` 覆盖块；`.preview-*` 间距与颜色对齐合规版（stage 列表去整框只留顶线、任务项 12px 次级色等）；补回 `.member-picker-empty`（意向乙方空态仍用）。
- **JS（worker-contract-detail.js）**：`renderTemplatePickerList` 列表项由 `.member-picker-item` 改为合规版 `.template-select-item`（item-left 图标 + item-content 名称/描述 + item-actions 预览/使用 `action-btn`）；设置 `#wcTplFilterType` 为当前合同类型、控制 `#wcTplEmpty`；`previewDraftTemplate` 的 `#wcPreviewMeta` 改为 `.preview-meta-item` 包裹（类型 + 阶段数 / 模板），与合规版预览元信息条一致。
- 验证：`node --check worker-contract-detail.js` 通过；grep 确认 `wcTplTip`/`.wc-preview-meta`/`.wc-primary-btn`/`.tpl-preview-content` 已无任何引用。

### 结论/约束（更新不变式）
**工人合同「更换模板」选择弹窗与预览弹窗现与合规版同款底部抽屉 + 模板选择列表 + 预览元信息条**；两页「更换模板」交互视觉已统一。后续工人端新增弹窗若属同类选择/预览，沿用 `.wc-template-modal`/`.wc-template-preview-modal` 体系，勿再退回 `.wc-modal` 居中框。

### 修复：工人合同「更换模板」点击无反应（内联 display:none 压制 .show）
- **根因**：转换弹窗为底部抽屉时，HTML 给 `#wcTemplatePicker`/`#wcTemplatePreview` 加了内联 `style="display:none;"`。JS 用 `classList.add('show')` 切换显隐（CSS `.wc-template-modal{display:none}` / `.wc-template-modal.show{display:block}`），但**内联样式优先级高于类选择器**，`.show` 永远盖不过 `display:none`，弹窗始终隐藏 → 点击「更换模板」无反应。
- **修复**：移除两弹窗的内联 `style="display:none;"`（CSS 的默认 `display:none` 已负责隐藏，`show` 类正常生效）。`WCP.showDraftTemplatePicker` 及 `applyTemplate/closeTemplatePicker/previewDraftTemplate/...` 导出均正常。
- 验证：`node --check worker-contract-detail.js` 通过；grep 确认两弹窗无残留内联 `display:none`。
- **复用经验**：任何用 `.show` 类切换显隐的抽屉/弹窗，**不要在元素上加内联 `display:none`**（或必须加 `!important` 到 `.show` 规则），否则内联样式会压住类切换。

## PC 合同审核管理 PRD 细化补充
- 需求：结合 `pc/pc-contract-list.*` 与 `pc/pc-contract-detail.*` 页面实际效果，将 PRD 第一章（PC 合同列表）与第二章（PC 合同详情页）进行细化补充，避免冗余。
- **1.3 筛选条件规格**：状态筛选由"全部 12 类"更新为 PC 运营端实际的三类归纳（合同待审核 pending_review / 变更待审核 change_review / 已审核 reviewed）；新增 note 说明 PC 仅展示需平台审核的六种状态；补充重置按钮行与筛选逻辑说明（filterContracts 客户端按 data-status/data-type 匹配）。
- **1.4 列表字段规格**：合同名称行补充点击跳转行为（viewContract 函数，pending_review→platform_reviewing，changing→change_reviewing）。
- **1.5 操作按钮矩阵**：按实际页面改写为六状态操作表（状态/文案/样式类/操作区/行为说明）；新增列表审核弹窗（#reviewModal）说明；新增合同名称点击与审核弹窗交互描述。
- **2.6 状态横幅规格**：按 JS statusConfig（11 种实际状态）完整重写状态横幅表（platform_reviewing 审核面板显示/status 横幅隐藏，change_reviewing 附变更对比等）；新增审核面板显示规则说明。
- **2.7 按钮规格**：拆分为 A/B 两组——审核面板按钮（驳回/确认驳回/审核通过，含 showRejectBtn 展开交互与 validation.js 校验）和状态横幅操作区按钮（导出合同等）。
- **2.8 交互流程**：由通用 code block 改为三小节——2.8.1 审核通过流程、2.8.2 审核驳回流程（含 showRejectInput→confirmRejectBtn→Validation.validate.textLength 校验→showConfirmModal→switchStatus 完整链）、2.8.3 变更审核附加逻辑（变更原因 banner+合同正文变更高亮+阶段任务变更摘要+变更记录 Tab）。
- 校验：356 div/72 table/439 tr/1389 td 全配对；80 锚点 id，79 个 TOC href（1 个预存孤点 sec-3-7-2-2 非本次引入）。
- 交付：`PRD-合同模块详细规格.html`。

## 工人合同更换模板：预览内容丰满 + 预览关闭返回选择弹窗

用户两诉求：① 拟定中「更换模板」预览合同正文示例内容过少（仅「工程内容+补充条款」两段），达不到演示效果；② 更换模板弹窗→预览弹窗，关闭预览后当前直接回详情页，应返回选择弹窗——问是否合理。

### 分析结论
- **流程合理性**：合规版 `previewTemplate`/`previewStageTemplate` 在开预览前也调 `closeTemplatePicker()/closeStageTemplatePicker()` 隐藏选择弹窗，关闭预览仅 `closeTemplatePreviewModal` 隐藏预览 → **合规版原本也是「预览关闭回详情页」**。所以工人合同行为其实与合规版一致（非工人特有 bug）。但从 UX 看，「预览」是选择弹窗的下钻视图，关闭应回选择弹窗（可对比另一模板/再决定），直接回详情页会丢失选择上下文、需重开弹窗。故采用「关闭预览返回选择弹窗」更合理，且**同时改合规版以贯彻「工人合同与合规版统一」约定**。

### 改动
- **工人合同（worker-contract-detail.js）**：
  - 新增 `buildTemplateTextPreview(id)`：标准版生成完整合同正文（工程概况/合同金额/工期/付款方式/双方权利义务/质量标准/验收标准/违约责任/争议解决，含甲乙方签章位），精简版生成核心条款（工程名称/地点/内容/金额/工期/付款/质量/争议），从 `STAGE_TEMPLATES[type].contentIntro` + 合同 `name`/`projectAddress` 取值，与合规版 `tpl.content` 体量对齐。
  - `previewDraftTemplate` 的 text 分支改用 `buildTemplateTextPreview(id)` 渲染（替换原两段稀疏文本）；**移除开预览前 `picker.classList.remove('show')`**，使预览为选择弹窗上层，关闭预览后选择弹窗自然透出。
- **合规版（contract-detail.js）**：`previewTemplate` 移除 `closeTemplatePicker()`、`previewStageTemplate` 移除 `closeStageTemplatePicker()`；并在 `applyContractTemplate`/`applyStageTemplate` 末尾补 `closeTemplatePicker()/closeStageTemplatePicker()`，确保从预览「使用此模板」时选择弹窗被关闭（否则残留）。`applyTemplateFromPreview` 经此路径正确关闭两弹窗。
- 验证：`node --check` 工人与合规版 JS 均通过；grep 确认两页预览函数均不再 hide 选择弹窗、apply 已补关闭。

### 不变式（更新）
**工人合同与合规版「更换模板→预览→使用」交互统一**：关闭预览返回选择弹窗；仅「使用此模板」时 close 选择弹窗 + 关闭预览。后续新增模板相关弹窗须遵守此链——选择→预览→使用→关闭，不可在预览阶段隐藏选择弹窗。

## PC 合同审核管理：合同类型改为基础施工/设计 + 分页统一 + 文档同步
- 页面改动（`pc-contract-list.html/.js`）：
  - 合同类型筛选改为"全部/基础施工服务合同(base_construction)/设计服务合同(design)"
  - 12 条示例数据全部改写为基础施工/设计服务双类型
  - 新增 `.pc-pagination` 分页（#resultCount + ‹ 页码 ›），renderPagination 动态生成，pageSize=10，筛选/搜索自动重置
  - `filterContracts` 简化为 currentPage=1 + renderContractList 重渲（与分页融合）
- PRD 同步：
  - 1.3 合同类型 filter options 同步更新
  - 1.6 分页规格完全重写（5 属性行 + note 框注明与模板管理页一致）
- 交付：`pc-contract-list.html/.js`、`PRD-合同模块详细规格.html`。

## 修复：状态筛选逻辑丢失 + 数据不渲染
- 问题：上一轮"分页统一"时误将 filterContracts 改为空壳（仅重置页码），renderContractList 内置的筛选逻辑未处理原"change_review/reviewed"分类映射，导致列表数据丢失。
- 修复：
  - 状态筛选 HTML 选项改为 6 个精准 status 值（pending_review/changing/reviewed_pass/reviewed_reject/change_reviewed_pass/change_reviewed_reject），不再使用三类归纳
  - 移除 JS 中 statusFilter.innerHTML 覆盖代码
  - renderContractList 筛选逻辑简化为 straight match（无分类映射）
- PRD 1.3 同步：状态行选项与筛选说明更新为 6 个真实状态值
- 交付：`pc-contract-list.html/.js`、`PRD-合同模块详细规格.html`。
**更换模板的「预览」为选择弹窗的下钻视图：打开预览不隐藏选择弹窗，关闭预览返回选择弹窗；从预览「使用此模板」才关闭两个弹窗。** 工人合同与合规版此流程一致。

## 预览阶段任务模板页面：布局规范化 + 任务项左对齐（工人/合规版样式统一）
- 用户诉求：预览「阶段任务模板」页面布局排版规范化，任务项左对齐，工人合同与非工人（合规版）合同此处样式统一。
- **根因**：两页 `.preview-task-item` 规则形状不一致——工人端为 `flex-direction:column`（名称+执行·确认 上下排），合规版为 `flex` + `justify-content:space-between`（行布局，仅单名称）；合规版 `.preview-stage-num` 缺 `flex:0 0 auto`（工人端有）。主题变量名不同（`--primary-color` vs `--color-primary`）但均解析为 `#1890FF`，视觉一致。
- **改动**：
  - 工人端（worker-contract-detail.html 内联 CSS）：`.preview-task-item` 补 `align-items:flex-start; justify-content:flex-start;` 显式左对齐；`.preview-task-sub` 补 `margin-top:3px`。（`.preview-content{padding:16px 0}` 已在该文件 429 行存在，未重复添加）。
  - 合规版（contract-detail.css）：`.preview-task-item` 由 `align-items:center; justify-content:space-between`（行）改为 `flex-direction:column; align-items:flex-start; justify-content:flex-start;`（列）；`.preview-stage-num` 补 `flex:0 0 auto;`；`.preview-task-name` 补 `font-size:12px;`；新增 `.preview-task-sub{font-size:11px; color:var(--text-tertiary); margin-top:3px;}` 与工人端对齐（合规版数据仅名称，规则保留为组件 parity）。
- **结果**：两页「预览阶段任务明细」的 `.preview-section/.preview-stage-*/.preview-task-*` 规则形状现已完全一致，任务项均为左对齐列布局。仅 accent 变量名按各自主题保留。
- 注：工人端 `.preview-stage-order`（阶段「按序/可并行」徽标，margin-left:auto 右对齐）属阶段头而非任务项，按数据保留；合规版无该数据故无此徽标。

## PRD 文档修复：去除重复拼接 + 同步「更换模板」最新调整
- **乱码/重复根因**：`PRD-合同模块详细规格.html` 实际含两份拼接文档——**copy1**（仅 §1.1–1.6 截断，无 `</html>`）+ **copy2**（完整文档，但其 `<!DOCTYPE>/<html>/<head>/<meta charset>` 开头缺失，line 570 残留乱码 `<div class="section" id="page-detail">set="UTF-8">`）。导致两个 `<title>`、两个 `<h1>`、两个「合同列表页（PC端）」。
- **修复**：删除 copy1，修复 copy2 缺失的开头标签（`<!DOCTYPE html><html lang><head><meta charset>`），保留 copy2 完整内容（原描述不变）。现单 title/h1/章节，结构正常。（临时备份 `.bak-dup` 已移除）
- **文档同步「更换模板」最新调整**：§3.7.2.2（合规版）与 §7.6.3（工人合同）「预览」流程统一改为：**预览不再关闭选择弹窗**，而是选择弹窗的上层（下钻视图）；**关闭预览返回选择弹窗**（非详情页）；仅「使用此模板」同时关闭两者。并补充：选择/预览弹窗均为**底部抽屉**（工人合同与合规版同款风格）；预览阶段任务**任务项左对齐**排布。阶段任务无金额字段已在 earlier 修订中。

## PC端 合同详情页：功能范围对齐实际 + 取消导出合同
- **背景**：`PRD-合同模块详细规格.html` 第二章（PC端 合同详情页）功能范围与实际代码不符——文档列了 `版本记录Tab / 导出合同 / 分享功能`，但 PC端 实际仅有 Tab：概览|合同正文|阶段任务|变更记录（**无版本记录Tab**），`pc/` 内**无分享功能代码**，且 `导出合同` 应取消；而实际存在的 **操作记录**（右侧栏时间线，已有 `#operationLog`）却未列入功能范围。
- **代码改动**（`pc/pc-contract-detail.js`）：移除 `updateStatusActions()` 内 `reviewed_pass` 与 `signed` 两处 `导出合同` 按钮，并删除 `exportContract()` 函数定义。`pc/` 内已无 `exportContract/导出/分享` 任何引用；`node --check` 通过。
- **文档改动（仅第二章 PC端）**：§2.3 功能范围 移除 `版本记录Tab/导出合同/分享功能` 三行、新增 `操作记录` 行；§2.6 状态横幅（reviewed_pass/signed 去掉"操作区显示导出合同按钮"）；§2.7 按钮规格（reviewed_pass/signed 改为"无"）；§2.9 权限表（移除 导出合同/分享合同 行）；§2 末尾「PC端 vs 小程序」对比表（移除 导出合同/分享合同 行）。
- **约束遵守**：小程序端（`service-miniapp`/`owner-miniapp`）代码与 PRD §3/§7 文档描述均未改动——导出合同文件（§3.7.2.2 等）、分享合同（小程序端 §3/§7）保留不变。
- **注意**：PRD 中 `导出合同`/`分享合同` 的其余出现（如 line 1533 小程序端「导出合同文件」、line 1910 小程序端「分享合同」）属小程序端，非本次范围，保持原样。

## PRD 第二章（PC端 合同详情页）按状态重组织
- **用户诉求**：合同详情页文档应「根据不同的状态对应说明包含的功能、字段规格、操作项、交互流程」，原文档这些信息散落在 §2.6 状态横幅规格 / §2.7 按钮规格 / §2.8 交互流程 多个表格，混乱。
- **重组织方案**（仅第二章，确认范围为 PC端）：
  - 旧 §2.6 状态横幅规格 → 新 **§2.6 状态总览**：单表覆盖 11 个状态的 `状态key/横幅/样式class/可见模块/操作项`；并纠正旧文档错误——审核态「状态横幅隐藏」实际应为「横幅（#statusBanner）在所有状态均显示，仅审核操作区（#statusReviewActions）在 platform_reviewing/change_reviewing 显示」。
  - 旧 §2.7 按钮规格 + §2.8 交互流程 → 合并为新 **§2.7 状态详解（按状态）**：按状态组（2.7.1 平台审核中 / 2.7.2 变更审核中 / 2.7.3 驳回态四态 / 2.7.4 通过态 / 2.7.5 签约流程态）逐一说明「功能与可见模块 / 状态相关字段 / 操作项 / 交互流程」，末尾 2.7.6 统一交互流程（审核通过/驳回/变更附加逻辑，搬自旧 §2.8）。
  - 删除旧 §2.8 独立小节；旧 §2.9 权限控制→**§2.8**、旧 §2.10 异常处理→**§2.9**；同步更新第二章 sub-TOC（lines 270-274）。
  - 用 Python 脚本按 `id="sec-2-X"` 锚点定位替换（旧 §2.7 含 mojibake em-dash 导致 Edit 精确匹配失败，故用脚本）。
- **约束**：仅动第二章 PC端，小程序端（§3/§4/§7/§8）文档未改。
- **遗留（非本次范围）**：第一章（合同列表页 PC端）line 487/508/552 仍残留 `â`/`â` mojibake（与第二章无关，用户此前修复的"乱码"是重复 title，未覆盖此章），已向用户标记，待确认是否修复。

## 第一章 合同列表页 PC端 乱码修复
- **用户确认**：将上述第一章遗留的 mojibake 一并修复。
- **根因**：经典「UTF-8 多字节字符被按 Latin-1 误读后重新存为 UTF-8」——`â` + 两字节 三字符序列，latin1→utf8 还原即得正确符号。涉及 12 处（`â`（U+00E2）三字节序列）：`â\x80\x94`→`—`、`â\x86\x92`→`→`、`â\x9c\x93`→`✓`、`â\x9c\x97`→`✗`、`â\x89¤`→`≤`。
- **修复**：Python 正则 `re.sub(r'\u00e2..', ...)` 全局将每个 `â`+两字节 三字符按 `encode('latin-1').decode('utf-8')` 还原；修复前 12 处、修复后 0 处。覆盖 line 487（重置 备注 `—`）、line 508（跳转映射 `→`）、line 552（操作区 `✓`/`✗`/`≤20字`/`—`）。全文 grep 确认无 `â/Ã/Â` 残留。
- **注意**：正文原有的真破折号 `—`（非乱码）未被误伤（其前无 `â`）。

## PC端 合同审核管理：撤回合同标识 + 多次变更一条数据
- **用户决策**：撤回的合同在 PC端 列表「标识已撤回并保留」（非移出）；同一合同多次变更在列表「始终一条数据」。
- **背景事实**：小程序非工人合同撤回——`撤回申请`(platform_reviewing→draft)、`撤回变更`/`撤回变更申请`(→signed)；目标态 draft/signed 本就不在 PC端 展示的 6 状态内，故此前"被动消失"。PC端 与小程序数据不共享（各自静态），撤回在 PC端 属规格约定。
- **代码改动（pc/）**：
  - `pc-contract-list.js`：allContracts 加 `contract_withdrawn`(合同已撤回)/`change_withdrawn`(变更已撤回) 两条演示数据；renderContractList 过滤纳入两态；action 渲染「查看」(viewContract→status=withdrawn)；viewContract 映射 withdrawn→详情页。
  - `pc-contract-list.html`：statusFilter 加「合同已撤回」「变更已撤回」两个 option。
  - `pc-contract-list.css`：加 `.status-tag.withdrawn`（灰 #F5F5F5/#8C8C8C）。
  - `pc-contract-detail.js`：statusConfig 加 `withdrawn`（灰横幅、只读、无操作，updateStatusActions 对未列状态默认空操作区）。
  - `pc-contract-detail.css`：加 `.status-banner.withdrawn`（灰渐变）。
- **PRD（§1.5 合同列表页 PC端 操作按钮矩阵）**：加 合同已撤回/变更已撤回 两行（status/状态名/样式类 withdrawn/操作区「查看(只读)」/审计留痕说明）；note-title 改为"展示需平台审核的六种状态并保留撤回态"；note <p> 补充"撤回保留灰标签仅供审计留痕 + 列表合同维度、同一合同多次变更仅一条、历史见详情页变更记录Tab(§5.11)"。

## PRD 第二章编号/层级修复
- **问题**：用户截图显示第二章目录顺序错乱：2.7 → 2.10 → 2.8 → 2.10，且 2.8 标题闭合标签为 `</h4>`。
- **根因**：第二章重组织时 §2.7 `<div class="subsection">` 未正确闭合，导致 §2.8/§2.9/§2.10 全部嵌套在 §2.7 内部；§2.8 的 `subsection-title` 闭合标签误写为 `</h4>`。
- **修复**：在 §2.7 内容末尾补 `</div>`，使 §2.8/§2.9/§2.10 与 §2.7 平级；修正 §2.8 标题闭合为 `</div>`；补全 §2.8/§2.9 HTML 注释锚点；§2.9 缩进对齐。
- **校验**：Chapter 2 区域 div open/close 均为 52（diff 0）；h4 open/close 均为 4（2.7.1–2.7.4 合法标题）；正文编号顺序 2.7 → 2.8 → 2.9 → 2.10；sub-TOC 与正文一致。
- **校验**：两份 JS node --check 通过；grep 确认状态/样式/选项/文档行齐备；小程序端未动。

## PRD 文档补充（合同列表页 PC端 + §5.11，纯文档不改代码）
- **§1.7 数据展示规则（新增）**：含两块——①「撤回合同：标识并保留（列表示例）」：contract_withdrawn(合同已撤回)/change_withdrawn(变更已撤回) 灰标签行、只读查看、筛选器选项，引用 1.5 末两行演示数据（基础施工服务合同-临安区撤回示例 / 设计服务合同-建德市撤回示例）；②「同一合同多次变更：始终一条数据」：列表合同维度、一行、状态反映最新待审态、历史见详情页变更记录Tab。Chapter 1 sub-TOC 同步加 §1.7。
- **§5.11 变更记录生成（扩展）**：原仅描述「发起变更」单节点；扩展为「变更流程时间线」六节点表（发起变更/提交平台审核/平台审核驳回/平台审核通过/变更确认/变更生效），标签样式 primary/warning/error/success 与演示数据一致；新增「变更类型（自动检测，与实现一致）」note，明确仅阶段任务变更(primary)/含其他内容变更(warning) 及 `needPlatformReview = hasAmountChange||hasContentChange||hasAttachmentChange`。
- **核实结论**：§5.3/§5.7/§5.9 状态机（changing/change_platform_reviewing/change_confirming_sender/change_platform_rejected 等）与小程序端 contract-detail.js statusConfig 一致，无需改。
- **遗留提示（待用户确认）**：PC端 列表演示数据名用了 临安区/建德市（浙江·越界城市），违反项目「示例城市仅限 北京/南阳/西安」约定；本次按"不改代码"约束未动，建议后续将演示名改为 西安/北京/南阳 范围内。

## PC端 撤回示例：列表默认展示 + 详情 操作记录/变更记录 补充（收尾）
- **列表默认展示**：`pc-contract-list.js` 的 `renderContractList` 过滤数组本就纳入 `contract_withdrawn`/`change_withdrawn`，状态下拉默认「全部状态」→ 两条撤回示例（id 13 基础施工-雁塔区撤回示例 / id 14 设计-未央区撤回示例）默认即展示，无需额外改动。
- **城市修正**：原 id 13/14 演示名用 临安区/建德市（浙江·越界），已改为 西安市内 雁塔区/未央区，符合「示例城市仅限 北京/南阳/西安」约定（解决上条遗留）。
- **详情页 wtype 区分**：`pc-contract-list.js` 的 `viewContract` 对 contract_withdrawn→`wtype=contract`、change_withdrawn→`wtype=change`，URL 追加 `&wtype=…`；`pc-contract-detail.js` 读取 `withdrawType` 参数。
- **操作记录（#operationLog）**：`updateOperationLog()` 新增 `withdrawn` 分支——contract：进入平台审核/撤回合同(陈庄·含详情)/合同审核终止(系统)；change：合同审核通过/发起变更申请/撤回变更申请(陈庄·含详情)/变更审核终止(系统)。
- **变更记录（#changesContent）**：`updateChangesContent()` 新增 `withdrawn` 分支——change：复用 changeRecord1 并将状态徽标+正文「变更状态」置为「已撤回」(灰)、隐藏 Record2/3；contract：隐藏所有变更记录、noChangeRecord 显示「合同已被发起方撤回，暂无变更记录」。
- **CSS**：`pc-contract-detail.css` 为 `.change-record-status` 补 `withdrawn`(灰 #F5F5F5/#8C8C8C) 与 `rejected`(红，此前被引用但未定义) 两态样式。
- 验证：`node --check` 两个 JS 通过；小程序端未动。

## 【修正】PC端 列表"默认看不到已撤回"根因 = 分页（非逻辑缺失）
- **用户反馈**：打开 PC 合同审核管理页，列表无已撤回状态项；点进详情页（其他状态）也未见「已撤回」字样。
- **排查结论**：列表 JS 过滤、`statusFilter` 选项、详情页 `withdrawn` 分支（`updateOperationLog`/`updateChangesContent`/`withdrawType`/`statusConfig.withdrawn`）**全部正确**。详情页右侧原型导航只有「小程序首页」选项，无状态切换器，详情页状态完全由列表 URL 参数驱动 → **查看撤回详情的唯一入口是列表「查看」按钮**。
- **真正根因**：`allContracts` 共 14 条，`pageSize=10` 且 id 13/14（撤回示例）排在数组**末尾** → 落在**第 2 页**。默认打开看第 1 页（id 1–10），自然看不到撤回项，也点不进撤回详情。上一轮"默认即展示"判断忽略了分页。
- **修复**：将 id 13/14 两条撤回示例**移到数组最前面**（现前两条），确保默认第 1 页即可见；数组仍 14 条、id 唯一无重复。`node --check` 通过。
- **经验**：演示数据若需"默认可见"，必须落在首页分页窗口内（或调整 pageSize/排序），不可只看过滤逻辑。
- 详情页撤回逻辑本身无需改动：经列表「查看」进入后，状态横幅显示「↩ 已撤回」、操作记录/变更记录按 wtype 显示撤回节点。

## PRD 补充：PC 端详情页撤回场景（§2）+ 示例城市名同步
- **用户确认**：撤回场景需在文档详细说明。核查发现 列表层（§1.5 操作按钮矩阵 / §1.7 数据展示规则）已覆盖，但 **§2（PC 端详情页）完全未写 `withdrawn` 状态**。
- **改动（PRD-合同模块详细规格.html）**：
  - §2.6 状态总览表：在 `signed` 行后新增 `withdrawn`（↩ 已撤回 / class withdrawn / 可见模块·操作项），说明段补"withdrawn 为审计保留态，仅从列表查看进入，无审核操作"。
  - 新增 **§2.7.5 已撤回（withdrawn）**：功能与可见模块（灰渐变横幅 .status-banner.withdrawn、仅从列表查看带 status=withdrawn&wtype、withdrawType 区分）、状态相关字段（wtype contract/change）、操作项（无）、操作记录撤回节点（contract：进入平台审核→撤回合同→合同审核终止；change：合同审核通过→发起变更→撤回变更→变更审核终止）、变更记录撤回表现（change 复用 #changeRecord1 置灰"已撤回"；contract 隐藏全部变更记录显示 #noChangeRecord 暂无变更记录）。
  - §1.7 示例演示数据城市名同步：临安区→**雁塔区**、建德市→**未央区**（与代码一致，符合城市约定）。
- 校验：div 366/366、h4 20/20 平衡；无重复锚点；临安区/建德市 全文 0 处。仅动 PC 端第二章与第一章 §1.7，小程序端未动。

## 驳回态一致性收敛（代码 + 文档）
- **问题**：文档 §2.6/§2.7.3 将 `platform_rejected`（平台审核驳回）、`review_rejected`（审核驳回）、`reviewed_reject`（审核已驳回）并列，但 PC 详情页 `statusConfig` 中前两者为**死代码**（列表走 `reviewed_reject`、驳回动作也跳 `reviewed_reject`，PC 详情页无状态切换器引用它俩）；真正接通的合同驳回态仅 `reviewed_reject`。
- **关键区分**：`platform_rejected` 在 service-miniapp/owner-miniapp 是**合法 live 状态**（小程序端被驳回后可编辑重提，属另一套状态机），与 PC 端 `reviewed_reject`（运营只读态）是**同一合同被驳回后的双视角**，并非真正冲突——故小程序端及其 §3 文档的 `platform_rejected` **不可删、不可并入**。
- **代码改动（pc/pc-contract-detail.js）**：删除 `statusConfig` 中 `platform_rejected`、`review_rejected` 两个死 entry；`rejectedStatuses` 收敛为 `['reviewed_reject','change_reviewed_reject']`；删除 `updateOperationLog` 内 `platform_rejected` 死分支。`node --check` 通过；全文 platform_rejected/review_rejected 计数 0。
- **文档改动（PRD-合同模块详细规格.html）**：§2.6 状态总览表删 platform_rejected/review_rejected 两行，说明段删死 key 提及；§2.7.3 标题与子表收敛为 `reviewed_reject · change_reviewed_reject`，"四者"→"二者"、"四态"→"两态"；§6.3 全局状态机保留 `platform_rejected`（指向小程序可编辑态），新增「驳回态双视角说明」澄清 PC reviewed_reject 只读态与小程序 platform_rejected 可编辑态的关系。
- **校验**：div 366/366、h4 20/20 平衡；`review_rejected` 文档计数 0；`platform_rejected` 文档计数 25（均 §3 小程序 + §6.3，正确保留）。

## §2.9 权限控制收敛（仅文档）
- **问题**：§2.9 是 PC 端合同审核管理专属权限表，但「查看合同详情」给服务方/业主打了 ✅（项目相关合同/自己的合同），且混入了「发起变更」行（运营 ❌/服务方 ✅/业主 ❌）——而 PC 端合同审核管理仅运营可登录查看操作，服务方/业主无入口；发起变更本属服务方小程序端功能（已在 §5.12 与第三/四章描述），不该出现在 PC 端表内。
- **改动（PRD-合同模块详细规格.html §2.9）**：①「查看合同详情」服务方/业主 改为 ❌（仅运营 ✅ 待审核合同）；② 移除「发起变更」整行；③ 新增运营专属说明 note（服务方/业主无入口、发起变更不在 PC 端展示，指向 §5.12/第三四章）。
- **§5.12 不动**：其列是 服务方/业主/平台运营，且「发起变更按钮」已正确标注 平台运营 ❌、服务方 ✅ 已签约状态可见，属变更流程权限表，与 §2.9 不冲突。
- **校验**：div 368/368、h4 20/20 平衡；无重复锚点；§2.9 已无发起变更行、note 落位；§5.12 未变。

## 工人合同拟定中·补充条款文本域对齐合规版（仅 HTML）
- **问题**：`service-miniapp/worker-contract-detail.html` 拟定中可编辑区 `#draftContentWrap`（745 行）只用了 `id`、缺 `draft-edit-form` 类；而合规版 `contract-detail.html` 的拟定中编辑区包在 `class="draft-edit-form"`（413 行）内，文本域样式 `.draft-edit-form .form-textarea`（边框/内边距12px/圆角8px/聚焦高亮/ min-height120px/resize）仅在该作用域生效。CSS 无独立 `.form-textarea` 基础规则，故工人页补充条款文本域退化为浏览器默认样式，与合规版不一致。
- **改动**：给 `#draftContentWrap` 补 `class="draft-edit-form"`（JS 仍按 id 定位，无冲突）。`draftContentWrap` 内仅含 `.form-textarea`（补充条款）、`.form-group`、`.form-label-row`、阶段卡片（用 `.stage-name-input` 非 `.form-input`）、附件区，加类后仅文本域与表单组间距/底部留白与合规版一致，无误伤。
- **校验**：common.css 无冲突 textarea 基础规则；阶段卡片用 `.stage-name-input` 不受影响。

