# 2026-08-06 工作日志

## 重新选择乙方：修正留痕位置 + 历史卡片显示（按用户反馈）
- 用户反馈两点：(1) 重选跳转到「拟定中（撤回后）」后，页面内**没有之前选择过的乙方历史（及乙方操作结果）**——实际是 bug：`renderReselectHistory` 以 `state.reselectMode` 为开关，而 `updateStatus` 顶部（第508行）会把它复位为 false，导致历史卡片永不显示；(2) 重新选择乙方的记录**不应进「变更记录」**，应进「历史版本（版本记录）」，因为重选不属于合同变更。
- 落地改动（worker-contract-detail.js + contract-store.js）：
  - 重选落点仍是 `worker_draft`（拟定中·撤回后），符合用户要求。
  - **数据层**：`reselectPartyB` 把留痕从 `changeLog.push` 改为 `versionLog.push`（名称「重新选择乙方」+ 原乙方合作未达成描述）；`createContract` 初始化 `versionLog: []`。
  - **历史卡片修复**：`renderReselectHistory` 改为以 `state.contract.replacedPartyB` 是否存在为开关（不再依赖 reselectMode）；并在 `updateStatus` 草稿分支（replacedPartyB 存在时）调用 `applyReselectBanner()`，确保直接经导航进入也能显示横幅 + 历史卡（头像/姓名/工种/「合作未达成」标签=乙方操作结果）。
  - **底部操作数据驱动**：`renderActions` 重选分支改为以 `state.contract.replacedPartyB` 为开关（撤回后态显示「恢复原乙方 / 提交邀请」），跨导航进入也稳定显示。
  - **历史版本（版本记录）**：`getVersionData()['worker_draft']` 改为 IIFE，合并 `c.versionLog`（倒序为 R1/R2…，再追加 V1 当前）并在 timeline 追加「重新选择乙方」warning 节点。
  - **清理死代码**：移除已无用的 `state.reselectMode` / `reselectPicked` / `_reselectPrev` 字段及复位行；`cancelReselect` 改用 `state.contract.replacedPartyB` 作为恢复原乙方的权威来源；`confirmReselect` 简化（不再手动置 reselectMode/调 renderActions，`updateStatus` 已据数据渲染）。
  - 「变更记录」弹窗因 changeLog 不再被写入重选，动态区自动隐藏——满足用户「重选不属于变更」的要求。
- 校验：contract-store.js / worker-contract-detail.js node --check 通过；worker-contract-detail.html div 240/240 平衡；node 桩验证 versionLog 合并进历史版本、历史卡片显示条件、变更记录不含重选，三项均符合预期。

## 拟定中（发起方）底部按钮文案调整
- 用户要求将「拟定中（发起方）」底部按钮文案「提交邀请」改为「提交并邀请乙方」。
- 落地：worker-contract-detail.js 中 `worker_draft_initial` / `worker_draft` 两个状态的 actions 按钮、`renderActions` 重选分支（恢复原乙方 / 提交并邀请乙方）、以及确认弹窗 `ACTION_TEXT.worker_resubmit.title` 全部改为「提交并邀请乙方」；配套 HTML 注释与两处代码注释同步更新。desc/banner 描述性文案与历史版本记录（2024 历史数据）中的「提交邀请」属说明/历史数据，保持不变。
- 校验：worker-contract-detail.js node --check 通过。

## 工人合同详情（新流程）新增「变更阶段」入口 + 7 个变更状态流程（参考合规版）
- 用户要求：以工人合同详情（新流程）现有流程为主，参考「合同详情（合规版）」内「变更阶段」各状态流程，在右侧原型导航「合同状态切换（工人合同）」下方补充「变更阶段」入口；不改变已有其它功能/页面。
- 落地（worker-contract-detail.html + worker-contract-detail.js）：
  - **导航**：在「合同状态切换（工人合同）」容器内、演示数据分组之后新增「变更阶段」分组（默认折叠 ▶，count 7），含 7 个条目：变更中(发起方) changing / 待确认变更(受邀方) change_confirming / 变更审核中 change_platform_reviewing / 变更审核驳回 change_platform_rejected / 变更确认中(发起方) change_confirming_sender / 变更确认中(待确认方) change_confirming_receiver / 变更签约中 change_signing_wait，均 `WCP.updateStatus(...)`；另在 STATUS_CONFIG 补 change_confirmed（变更已完成，作流程终点，不在导航列出）。
  - **状态配置**：STATUS_CONFIG 增 7+1 个变更态，含 text/desc/bannerClass/actions，文案与动作对齐合规版；change_platform_rejected 带 showRejectReason。
  - **Banner 配色**：CSS 新增 .wc-banner.changing / .change-confirming / .reviewing / .changedone；新增 .change-reject-reason 块展示平台驳回原因（changeRejectReason 容器，updateStatus 按 cfg.showRejectReason 显隐）。
  - **视角/流程**：updateStatus 视角逻辑补 `change*` 分支（_receiver 结尾为待确认方视角，其余按发起方）；renderFlow 对变更态整体置为「已签约」步进（变更由 banner 体现）。
  - **动作推进**：ACTION_TEXT 补 8 个变更动作；handleAction 增加变更分支，经 showConfirm 后由 applyChangeAction 推进状态（如 确认变更→change_platform_reviewing、确认变更_flow→change_signing_wait、上传变更签约文件→change_confirmed），并 pushChangeLog 写入合同 changeLog（对接已有「变更记录」弹窗），持久化用 ContractStore.patchContract。
- 校验：worker-contract-detail.js node --check 通过；worker-contract-detail.html div 251/251 平衡；变更记录弹窗元素（changeRecordLog/changeRecordLogList）已存在；ContractStore.patchContract 可用。

## 已签约发起方「发起变更」入口 + 流程交互 + 变更内容高亮（补齐缺失环节）
- 用户指出：已签约（发起方）应可发起变更，该入口此前缺失；要求补充发起变更入口、对应流程交互与页面；且变更阶段内对应页面**仍是已签约对应内容、但高亮标记出变更的内容**。
- 落地（worker-contract-detail.html + worker-contract-detail.js）：
  - **入口**：`STATUS_CONFIG.worker_signed` actions 由 `[]` 改为 `[{ text:'发起变更', type:'primary', action:'start_change' }]`；desc 补充"可发起合同变更"提示。
  - **流程交互**：`handleAction` 新增 `start_change` 分支（不走 showConfirm）→ `openChangeEditModal()` 打开「发起变更」编辑弹窗（变更原因*必填* + 合同金额变更[当前→变更后] + 补充条款变更 + 阶段任务变更说明）。`submitChangeProposal()` 校验原因非空 → 组装 `changeProposal{reason,amountOld,amountNew,extraOld,extraNew,stageNote,time,by}` 写入合同（ContractStore.patchContract）→ pushChangeLog("发起变更") → `updateStatus('changing')`（迁至「变更中」等待对方确认）；撤回/驳回变更回到已签约时 `clearChangeProposal()` 清除提案恢复原始内容。
  - **高亮标记（变更阶段页面）**：新增 `getActiveChangeProposal()`（真实 changeProposal 优先；导航直接预览 `changing` 等变更态且无真实数据时给演示提案，便于直观查看高亮）；`renderChangeHighlight()` 渲染 `#changeHighlightCard`（变更原因 + 合同金额旧→新高亮 + 补充条款原/变更后 + 阶段任务变更说明，含"变更进行中/已生效(V2)/审核驳回"状态标）；`renderMeta` 合同金额行在变更态以 `metaRowHighlight` 旧→新高亮；`renderContentSection` 补充条款/阶段任务变更以 `.text-content-change` 高亮并打"变更后"标。
  - **WCP 暴露**：openChangeEditModal / closeChangeEditModal / submitChangeProposal / onChangeReasonInput。
  - **CSS/HTML**：新增 `#changeHighlightCard` 容器（mainView 内，变更态显示）、`#changeEditModal` 弹窗、`.change-highlight-card` / `.meta-row.highlight` / `.text-content-change` 等样式；补 `.edit-textarea`、`.modal-btn.primary`。
- 约束：不改变已有其它功能/页面；`c.status` 仍保持 `worker_signed`（不持久化 changing，避免 computeStatus 落入草稿分支），变更提案持久化于 changeProposal，刷新后经导航预览 changing 仍显示高亮。
- 校验：worker-contract-detail.js node --check 通过；worker-contract-detail.html div 267/267 平衡；弹窗元素 id 与 JS 引用一一对应。

## 「发起变更」改为「二次确认弹窗 → 独立发起变更页面」（非弹窗）
- 用户反馈：点「发起变更」应**先弹窗二次确认**，确认后再**跳转到发起变更页面**（弹窗无法承载较多内容）。
- 落地：
  - **worker-contract-detail.js**：
    - `handleAction` 的 `start_change` 分支由 `openChangeEditModal()` 改为 `confirmStartChange()`（先 `showConfirm` 二次确认，确认按钮文案「去填写」、主色 primary）。
    - 删除 `openChangeEditModal / closeChangeEditModal / onChangeReasonInput / submitChangeProposal` 四个弹窗函数，改为 `confirmStartChange()`（弹二次确认）+ `goChangePage()`（`location.href = 'worker-contract-change.html?id=' + workerId`）。
    - `init()` 增加 `preview` URL 参数支持：若 `getParam('preview')` 存在则 `updateStatus(preview)`（如 preview=changing），否则按 computeStatus() 推导——用于发起变更页提交/取消后回跳预览。
    - 保留 `clearChangeProposal`（撤回/驳回变更时清除提案）。
    - WCP 导出改为 `confirmStartChange / goChangePage`。
  - **worker-contract-detail.html**：删除 `#changeEditModal` 弹窗块（已无引用）；`.wc-modal .modal-content` 加 `white-space: pre-line` 使二次确认的多行文案（含 `\n\n`）正确换行。
  - **新增 worker-contract-change.html**（独立页面，phone-frame 小程序外壳）：顶部 nav「发起变更」+ 返回；合同上下文卡（名称/类型/当前金额/架构层级）；变更信息表单（变更原因*必填*+字数计数+错误提示、合同金额变更[当前→变更后]、补充条款变更、阶段任务变更说明）；底部固定「取消 / 提交变更」操作栏。JS 自包含：加载 contract-store.js，按 id 读合同预填，提交时写入 `changeProposal` + `changeLog` 并经 `ContractStore.patchContract` 持久化，再 `location.href` 回跳 `worker-contract-detail.html?id=...&viewer=sender&preview=changing`；取消回跳 `preview=worker_signed`。
- 校验：worker-contract-detail.js node --check 通过；worker-contract-detail.html div 252/252 平衡；worker-contract-change.html div 30/30 平衡；无残留 changeEditModal/openChangeEditModal/submitChangeProposal 引用（新页 id 不复用冲突，contract-detail.html 自带同名 textarea 属独立页面不影响）。

## 工人合同变更去除「平台审核」环节
- 用户指出：工人合同**不需要平台审核**，故发起变更后**无需平台审核流程，直接变更确认中**；要求从「发起变更」弹窗提示内容及流程对应调整。
- 落地（worker-contract-detail.js / worker-contract-detail.html / worker-contract-change.html）：
  - **删除平台审核相关状态**：STATUS_CONFIG 移除 `change_platform_reviewing`（变更审核中）、`change_platform_rejected`（变更审核驳回）、`change_confirming_receiver`（变更确认中·待确认方，原平台审核后二次确认态）；保留 `change_confirmed`（变更已完成·已生效）作流程终点并新增导航预览项。
  - **动作链路收敛**：`applyChangeAction` 中 `confirm_change`（待确认方确认变更）由跳转 `change_platform_reviewing` 改为**直接跳转 `change_confirming_sender`（变更确认中）**；删除 `withdraw_change_review` / `resubmit_change` / `reject_change_flow` / `confirm_change_flow` 四个动作及对应 ACTION_TEXT；`handleAction` 分支条件移除 `resubmit_change` 引用。
  - **文案调整（去平台化）**：`worker_signed` desc 改为"需对方确认后生效，无需平台审核"；`change_confirming_sender` desc 改为"对方已确认变更，请上传变更签约文件"；`confirm_change` 确认弹窗文案改为"确认后双方即达成变更，需上传变更签约文件方可生效"；`confirmStartChange` 二次确认文案改为"变更需对方确认，确认后直接生效（无需平台审核）"；worker-contract-change.html 顶部提示条同步改为"无需平台审核"。
  - **清理**：删除 `#changeRejectReason` HTML 容器、`.change-reject-reason` CSS、`updateStatus` 内 `showRejectReason` 渲染分支；版本记录弹窗内演示时间线"2024-03-08 平台审核通过"改为"对方确认变更（无需平台审核）"。
  - **导航**：变更阶段分组条目由 7 减为 5（变更中(发起方)/待确认变更(受邀方)/变更确认中(发起方)/变更签约中/变更已完成(已生效)），count 同步改为 5。
- 校验：worker-contract-detail.js node --check 通过；worker-contract-detail.html div 249/249 平衡；worker-contract-* 文件已无 change_platform/change_confirming_receiver/changeRejectReason/平台审核/平台运营/平台驳回/变更审核 残留（仅"无需平台审核"说明性文案保留）。

## 补充「变更已驳回（受邀方驳回）」独立页面
- 用户指出变更阶段各页面均缺少"变更受邀方驳回情况"的页面，要求补充（状态步骤图仍保持已签约不变、变更状态单独走变更流程的既有约定不变）。
- 落地（worker-contract-detail.js / worker-contract-detail.html）：
  - STATUS_CONFIG 新增 `change_rejected`（变更已驳回）：banner `rejected`（红）、desc"对方（乙方）已驳回…可重新发起变更"、actions=[重新发起变更(start_change) / 返回已签约(change_rejected_back)]、含 showRejectReason + rejectReason（乙方驳回原因）。
  - `applyChangeAction`：`reject_change` 由跳转 `worker_signed` 改为**跳转 `change_rejected`**（保留 changeProposal 用于展示被驳回的变更内容，不清空）；新增 `change_rejected_back` 跳转 `worker_signed`（触发 clearChangeProposal）。
  - `goChangePage` 进入发起变更页前先 `clearChangeProposal()`，保证从"变更已驳回"重新发起时以当前合同内容全新填写、导航预览也回退到演示提案。
  - `updateStatus` 重新加回 `changeRejectReason` 渲染分支（status.showRejectReason 时显示乙方驳回原因）；HTML 重新加入 `#changeRejectReason` 容器与 `.change-reject-reason` CSS。
  - `renderChangeHighlight` 状态标区分：`change_rejected` → "变更已驳回（乙方）"（原笼统"审核驳回"去掉）。
  - 右侧导航「变更阶段」分组新增「变更已驳回(受邀方驳回)」项（置于「待确认变更(受邀方)」之后，体现其为待确认步骤的分支结果），count 由 5 改为 6。
- 校验：worker-contract-detail.js node --check 通过；worker-contract-detail.html div 251/251 平衡；node 桩验证 reject_change→change_rejected 保留提案、change_rejected_back→worker_signed 清空提案，符合预期。

## 修复：变更中（发起方）状态步进图应为已签约
- 现象：预览「变更中（发起方）」`changing` 页面，状态步进图（拟定中→确认中→已确认→已签约）停在「确认中」，而非预期的「已签约」；与变更阶段其余状态（已签约步进、变更由 banner 体现）不一致。
- 根因：`renderFlow()` 变更分支判断 `status.indexOf('change') === 0`（以 `change` 开头），但 `changing` = "chang"+"ing" 不含子串 "change"，不命中，current 落回默认 `inviting`。其余 change_* 状态均以 `change` 开头故正常。
- 修复：renderFlow 变更分支改为 `status.indexOf('change') === 0 || status === 'changing'`，将 `changing` 一并置为已签约步进。
- 校验：node --check 通过；提取 renderFlow 做 DOM 桩测试（10 状态断言全 PASS）：changing 及所有 change_*（除 change_confirmed 因含 confirmed 子串走原"已确认"行为外）均映射已签约，worker_signed→已签约/worker_confirmed_*/inviting→已确认/worker_draft→拟定中 映射不变。改动仅一行，不影响其他状态与既有功能。

## 修复：变更已完成（已生效）状态步进图也应为已签约
- 现象：用户反馈「变更中已完成」即 `change_confirmed`（导航「变更已完成(已生效)」）状态步进图也停在「已确认」而非「已签约」，与变更阶段其余状态不一致。
- 根因：上一轮修复仅把 `changing` 补进变更分支，但 RenderFlow 中 `status.indexOf('confirmed') > -1` 判断位于变更分支**之前**；`change_confirmed` 同时含 `change` 与 `confirmed` 子串 → 在 confirmed 判断处先命中，被误判为「已确认」，走不到后续变更分支。
- 修复：将变更分支（`status.indexOf('change') === 0 || status === 'changing'`）整体上移，置于 `confirmed` 判断之前；`change_confirmed` 及全部 change_* / changing 均优先命中 → 置「已签约」步进。`worker_confirmed_*` 等非变更态不受影响。
- 校验：node --check 通过；提取 renderFlow 做 DOM 桩测试（12 状态断言全 PASS）：change_confirmed/changing/change_confirming/change_confirming_sender/change_signing_wait/change_rejected 全部映射已签约；worker_signed→已签约、worker_confirmed_sender/receiver→已确认、worker_inviting_*→确认中、worker_draft→拟定中 映射不变。改动不影响其他状态与既有功能。

## 修复：发起变更弹窗文案与确认按钮显示，并参照合规版重排发起变更页
- 用户反馈：
  1. 发起变更二次确认弹窗内容应改为"变更需对方确认，确认后需上传签约文件，上传后生成新版本（V2）。变更确认前阶段任务暂停流转。"；
  2. 弹窗右侧确认按钮文字不显示（空白），截图显示仅"取消"可见；
  3. 发起变更页面应参照「合同详情（合规版）」发起变更布局，但主要字段以工人合同为准。
- 落地：
  - `worker-contract-detail.js`：`confirmStartChange()` 弹窗文案改为用户指定文本，去掉前缀"确认要发起合同变更吗？"。
  - `worker-contract-detail.html`：修复 `.modal-btn.primary` 与 `.wc-modal .modal-btn` 同优先级、后被后者覆盖导致背景透明+白字不可见的问题；将选择器提升为 `.wc-modal .modal-btn.primary`（更高优先级），确保确认按钮背景为主色、文字白色可见。
  - `worker-contract-change.html` 参照「合同详情（合规版）」发起变更页重排：
    - 顶部「变更须知」提示块（含上传签约文件、生成 V2、阶段任务暂停）；
    - 「变更原因」独立卡片（带 📝 标题、必填、字数统计、提示语）；
    - 新增「合同基本信息」卡片（2 列网格：合同名称/类型/甲方/乙方/当前金额/架构层级）；
    - 「合同金额变更」卡片（当前金额 + 变更后金额输入 + 实时差额显示）；
    - 「补充条款变更」「阶段任务变更说明」独立卡片；
    - 底部操作栏改为左「取消」右「提交变更」的合规版样式。
  - 交互补充：金额输入实时计算并显示差额；乙方名称优先取 `partyBName`，否则从 invitations 中找 `status='confirmed'` 的 name。
- 校验：worker-contract-detail.js node --check 通过；worker-contract-detail.html / worker-contract-change.html div 均平衡（251/251、57/57）；worker-contract-change.html 内联 JS 提取后 node --check 通过。


## 变更签约中：上传变更签约文件流程参考合规版 + 上传后跳已签约
- 用户要求：工人合同「变更签约中」`change_signing_wait` 状态，点"上传变更签约文件"流程应参考「合同详情（合规版）」内的上传变更签约文件，且上传成功后跳转到「已签约」页面（而非原 `change_confirmed` 变更已完成）。
- 合规版对照：合规版 `js/contract-detail.js` 中 `upload` 与 `upload_change_sign` 在 handleAction 均 `closeModal(); showSignUploadPage(); return;`（直接打开文件选择上传页面，不弹二次确认），`submitSignFile()` 提交后 `updateContractStatus('signed')` 跳已签约。
- 落地（worker-contract-detail.js，仅改该文件）：
  - `handleAction` 在 `upload` 分支后新增 `if (action === 'upload_change_sign') { pickSignFile(); return; }`：直接打开文件选择（复用 `pickSignFile`/`onSignFilePicked` 范式，与合规版 `showSignUploadPage` 精神一致，不弹二次确认）。
  - 从 change 分支条件移除 `|| action.indexOf('upload_change_sign') > -1`（不再走 applyChangeAction）。
  - 删除 `applyChangeAction` 的 `case 'upload_change_sign'`（原返回 next: 'change_confirmed'）。
  - 删除 `ACTION_TEXT.upload_change_sign` 定义（改为直接开上传页后该 confirm 文案成为死代码）。
  - 复用既有 `onSignFilePicked`：上传后 `updateStatus('worker_signed')` 跳已签约、`showToast('签约文件已上传，合同已生效')`，与合规版"上传后跳已签约"等价。
- 校验：node --check 通过；grep 确认 upload_change_sign 仅剩 2 处合理引用（line 136 按钮 action 定义、line 980 handleAction 分支），applyChangeAction case / ACTION_TEXT / change 分支条件均已移除；流程闭环 = `change_signing_wait` 按钮 → handleAction → pickSignFile（开文件选择）→ 选文件 → onSignFilePicked → updateStatus('worker_signed')，与原 worker_confirmed 的 upload 路径一致且已验证可跳已签约。不影响其他状态与既有功能。

## 补齐：发起变更页真正对齐「合同详情（合规版）」布局（含合同内容三标签页）
- 背景：前一轮"参照合规版重排发起变更页"仅做了高层级 section（变更须知/变更原因/基本信息/金额变更/补充条款/阶段任务说明），把"阶段任务""补充条款"简化成纯文本框，**未对齐合规版发起变更页的「合同内容」三标签页（合同正文 / 阶段任务 / 合同附件）+ 可编辑阶段任务结构**。用户追问"你是没有执行完吗"——确认是布局对齐未完成，本次补齐。
- 落地（worker-contract-change.html 重写 + worker-contract-detail.js 补 V2 应用）：
  - 复用 `css/contract-detail.css` 的合规版样式类（`change-content / change-tabs / change-tab / change-tab-content / stage-edit-item / task-edit-item / switch / add-stage-btn / change-footer` 等），仅补 `.content-toolbar / .more-ops-panel / .add-attachment-btn` 三个 css 缺失类（页面内联）。
  - 新增"变更辅助操作"工具条（更多 → 版本记录 / 变更记录，弹窗读取 contract.versionLog / changeLog）。
  - 「合同内容」section 改为三标签页：① 合同正文（动态生成正文预览 + 展开全文 + 补充条款可编辑 textarea）；② 阶段任务（**可编辑**：阶段名输入、按序执行开关、添加/删除阶段、添加/删除任务、任务名+执行+确认输入，数据来自 contract.stages 本地副本）；③ 合同附件（附件列表 + 添加/删除附件）。
  - 提交时把 `reason / amountOld / amountNew / extraOld / extraNew / stagesNew / attachmentsNew` 一并写入 `changeProposal` 并 patchContract，跳 `worker-contract-detail.html?preview=changing`。
  - `worker-contract-detail.js` 新增 `applyChangeProposal()`：上传变更签约文件（`onSignFilePicked` 检测到 changeProposal 存在）时，将提案的 金额/补充条款/阶段/附件 **落到合同**，写入 versionLog「合同变更 V2」，清除 changeProposal，再按既定约定 `updateStatus('worker_signed')`（遵守前一轮"上传后跳已签约"指令，不在 change_confirmed 停留）。
- 校验：worker-contract-detail.js node --check 通过；worker-contract-change.html div 106/106 平衡；grep 确认旧简化文本框（阶段任务变更说明 / changeStageInput）已移除；合规版关键类均已接入。

## 变更签约中：上传改为「独立上传页」（参考合规版样式）+ 删除冗余 change_confirmed
- 用户纠正：上一轮把 `upload_change_sign` 改成直接 `pickSignFile()`（弹文件选择）不符合要求；应为**跳转到新页面上传**，参考「合同详情（合规版）」`sign-upload-page` 整页上传流程样式；上传成功后跳「已签约」；完成后删除冗余「变更已完成（已生效）」页面。
- 落地：
  - 新增 `service-miniapp/worker-change-sign-upload.html`（独立上传页）：复用 `common.css` + `css/contract-detail.css` 的 `.sign-upload-page` 整页样式（上传说明 / 变更签约文件上传框 / 文件列表 / 提交按钮），标题"上传变更签约文件"、说明适配变更场景；处理文件选择/列表/删除/提交（无文件 toast 提示），返回 → `worker-contract-detail.html?preview=change_signing_wait`，提交成功 → `worker-contract-detail.html?preview=worker_signed`。轻量 wc-toast 内联。
  - `worker-contract-detail.js`：`handleAction('upload_change_sign')` 改为 `global.location.href='worker-change-sign-upload.html'`（不再 pickSignFile）；从 STATUS_CONFIG 删除 `change_confirmed`；删除 renderChangeContent 中 `if(state.status==='change_confirmed')` 分支（改为统一 `变更进行中（待上传签约文件生效）`）；`onSignFilePicked` 注释更正（变更上传已走独立页，此处仅处理常规签约）；renderFlow 注释同步。
  - `worker-contract-detail.html`：删除「变更阶段」分组下 `change_confirmed` 状态项、分组计数 6→5；新增导航链接 `worker-change-sign-upload.html`（「上传变更签约文件（新页面）」，置于工人合同退出/重选链接之后），便于原型直接预览。
- 校验：worker-contract-detail.js node --check 通过；新页内联脚本 node --check 通过；grep 确认 worker 系文件已无 change_confirmed 残留（js/contract-detail.js 合规版及 .bak 备份的 change_confirmed 属参考基准/备份，不动）。

## 变更名单逻辑修正（仅乙方待确认，其他人=历史邀约记录）
- 用户纠正：变更非"邀请人"逻辑，本次仅乙方需确认；其他人不应出现「待确认」状态，且样式不应与乙方同样突出——应暗色呈现或加「邀约记录」标识。
- 落地：
  - `inviteDisplayStatus` 变更分支（`changing` / `change_confirming_sender`）：乙方→`change_pending`；其他人**强制**映射为 `rejected`/`lost`（不再 `return inv.status`，杜绝其他人落入 pending）。
  - `renderLists`：新增 `isChangeView` 判断；乙方行以「乙方 {人员}」呈现（乙方标签前置）+ 保持 `is-partyb` 高亮 + 突出「待确认（变更）」；其他人行加 `is-record` 暗色类 + 名称后附「邀约记录」标识，状态徽标加 `record` 类降级为灰底暗色，状态文本仍显示其历史结果（已拒绝/抢单失败）。
  - `worker-contract-detail.html` CSS：补 `.invite-record-tag`（灰底标签）、`.invite-row.is-record`（opacity .55 + 次要色名）、`.invite-avatar.muted`（灰头像）、`.invite-status.record`（灰底暗色徽标）；`.invite-partyb-tag` 由 `margin-left` 改 `margin-right`（标签前置）。
- 校验：worker-contract-detail.js node --check 通过；worker-contract-detail.html div 250/250 平衡；关键类均命中。

## 变更名单逻辑同步至全部变更阶段状态
- 用户要求：上条"仅乙方待确认、其他人暗色邀约记录"的样式应同样适用于工人合同详情内「变更阶段」各状态页，但乙方状态需按变更逻辑随状态演进同步更新。
- 落地：
  - `inviteDisplayStatus` 变更分支由仅 `changing`/`change_confirming_sender` 扩展为覆盖全部发起方变更态：`changing`→乙方 `change_pending`（待确认（变更））；`change_confirming_sender`·`change_signing_wait`→乙方 `change_confirmed`（已确认（变更））；`change_rejected`→乙方 `change_rejected`（已驳回（变更））；其余状态其他人一律 `rejected`/`lost`（不出现 pending）。
  - `renderLists` `isChangeView` 覆盖 `changing`/`change_confirming_sender`/`change_signing_wait`/`change_rejected` 四态；新增 `change_confirmed`→「已确认（变更）」、`change_rejected`→「已驳回（变更）」文案映射。
  - `worker-contract-detail.html` 补 `.invite-status.change-confirmed`（绿描边）/`.invite-status.change-rejected`（红描边）两种乙方变更状态徽标；`is-record` 暗色 + 「邀约记录」标识在四态统一生效。
- 校验：worker-contract-detail.js node --check 通过；worker-contract-detail.html div 250/250 平衡；四态乙方分支与 CSS 均命中。

## 补：待确认变更（受邀方 change_confirming）视角与乙方状态同步
- 用户指出漏掉「待确认变更（受邀方）」`change_confirming` 的同步。核查发现两处遗漏：
  1. 视角判定：原 `status.indexOf('change')===0` 分支仅对 `_receiver` 后缀判为受邀方，`change_confirming` 无此后缀→被误判为**发起方**视角（与导航标签"（受邀方）"矛盾）。
  2. 名单：原 `showInviteList` 对全部受邀方视角隐藏名单，故 `change_confirming` 从未展示乙方待确认行。
- 落地：
  - `updateStatus` 视角分支：显式 `status === 'change_confirming'` 也判为 receiver（asUserId 取 firstId=乙方）。
  - `inviteDisplayStatus`：将 `change_confirming` 并入变更待确认分支（`change_confirming || changing`），乙方→`change_pending`（待确认（变更）），其他人→`rejected`/`lost`。
  - `showInviteList`：对 `change_confirming` 放开名单显示（尽管是受邀方视角），以便展示「乙方（我）待确认（变更）」行。
  - `renderLists`：新增 `isReceiverChangeView`；受邀方变更态仅渲染乙方本人行（`inv.userId !== firstId` 直接 return 跳过其他人），遵守"受邀方不展示其他被邀请人"规则；`isChangeView` 纳入 `change_confirming`。
- 效果：导航「待确认变更(受邀方)」现为正确受邀方视角，乙方（即受邀方本人）以「乙方 {人员}（我）」+「待确认（变更）」高亮行呈现，不暴露其他受邀人；确认/驳回按钮仍正常（renderActions 与视角无关）。
- 校验：worker-contract-detail.js node --check 通过；worker-contract-detail.html div 242/242 平衡；change_confirming 各处理点均命中。

## 变更中（发起方）页：乙方显示「待确认（变更）」+ 他人邀约结果标识
- 用户要求：发起变更提交后跳「变更中（发起方）」页，此页应展示乙方（乙方应为待确认状态），其他人仅标识其乙方邀约记录含邀约结果即可。
- 现状：changing 状态 `renderLists` 已渲染 `#inviteListBox` 邀约名单，但 `inviteDisplayStatus` 对变更态兜底 `return inv.status`，使已确认乙方显示"已确认（乙方）"，与"变更待确认"语义不符。
- 落地（worker-contract-detail.js + worker-contract-detail.html）：
  - `inviteDisplayStatus` 新增变更中分支（changing / change_confirming_sender）：乙方（firstId）→ 返回 `'change_pending'`；其他人 → 返回其真实邀约结果（rejected/lost/inviting 兜底）。
  - `renderLists` 增加 `change_pending` → 文案"待确认（变更）"、样式类 `change-pending`；并给乙方行加 `invite-partyb-tag`（"乙方"徽标）+ `is-partyb` 行高亮；补 `firstId` 局部变量。
  - CSS 新增 `.invite-status.change-pending`（主色描边，区别于普通 pending）与 `.invite-partyb-tag` / `.invite-row.is-partyb`。
- 校验：worker-contract-detail.js node --check 通过；worker-contract-detail.html div 250/250 平衡；key 改动均命中。

## 上传变更签约文件页：导航栏对齐微信小程序默认规范
- 现象/要求：独立上传页 `worker-change-sign-upload.html` 原用 `.sign-upload-page` 的自定义 `.page-header`（返回+标题+空占位），**缺少微信原生胶囊占位**，与其他 worker 原型页（工人合同详情 / 退出重选等）的微信原生导航栏不一致，不符微信小程序默认规范。
- 落地（仅改该独立页，不影响其余功能/页面）：
  - 将 `.page-header` 块替换为标准微信原生导航栏：`.status-bar`（9:41 📶🔋）+ `.nav-bar`（返回「← 返回」+ 居中标题「上传变更签约文件」+ `.actions` 内 `.native-capsule` 占位 `⋯｜○`），与 worker-contract-detail.html 的导航栏结构完全一致。
  - `<style>` 内联补 `.nav-bar .native-capsule` 系列样式（从工人合同详情页移植），并加 `.sign-upload-page > .status-bar, .sign-upload-page > .nav-bar { flex-shrink:0 }` 防止 flex 列布局下收缩。
  - `<title>` 标签保持「家装平台小程序原型 - 工人合同上传变更签约文件」格式不变（仅页面内可见导航栏需对齐微信默认规范）。
- 校验：grep 确认 `page-header / header-title / header-back / header-placeholder` 零残留；状态栏(44)+导航栏(44)+page-content(flex:1)+footer 在 `.sign-upload-page` 覆盖层内布局正常；`.nav-bar` 含 `position:relative` + `border-bottom`，`.title.centered` 绝对居中正确落位。

## 创建项目页：移除发起人相关内容与本页演示控制
- 用户要求：创建项目页目前无"是否发起人角色"概念，取消本页发起人相关内容（含本页演示控制），删除冗余代码，不影响其他功能。
- 落地（仅改 `service-miniapp/create-project.html`，删除共 286 行）：
  - HTML：删除 `#promoterBanner`（"成为项目发起人可享受项目分利"入口条）、`#promoterTip`（"您已是项目发起人"提示条）、整块 `#demoControlPanel`（演示控制面板，仅含"发起人认证"开关与状态文案）。
  - CSS：删除 `.promoter-banner`(含 hover/子元素)、`.promoter-badge`、`.promoter-tip`(含 .show/.tip-icon/.tip-text/.inline-badge/.badge-icon) 全部发起人样式；删除 `.demo-control-panel` 系列 + `.toggle-switch` 系列 + `.status-text`(uncertified/certified) 演示控制样式。
  - JS：删除 `let isPromoter`、`checkPromoterStatus()`、`togglePromoterStatus()`、`updatePromoterUI()`、`goToPromoterAuth()` 及初始化中的 `checkPromoterStatus();` 调用。
- 校验：grep 确认 promoter/demo-control-panel/toggle-switch/各函数与元素 id 在 create-project.html 内零残留；div 102/102 平衡；原型导航（含"创建项目"active）与初始化区（initDefaultSelections/initRegionSelect/checkFormValidity 等）完整保留。
- 说明：`promoter-auth.html` 仍被 `project-list.html` 引用（非孤儿页面），按"仅此页面"范围保留未删；其余 `.bak`/`*.capsule-visual.bak` 备份文件中的同名残留属历史备份不动。

## 合同详情（合规版）上传页：导航栏对齐微信小程序默认规范
- 现象/要求：合规版 `service-miniapp/contract-detail.html` 的「上传签约文件」覆盖层（id=signUploadPage，`.sign-upload-page`）同样用自定义 `.page-header`（返回+标题+空占位），**无微信原生胶囊占位**，与同页底层标准微信原生导航栏（status-bar + nav-bar + native-capsule）不一致，不符微信小程序默认规范（与上一轮 worker 独立上传页问题同源）。
- 落地（仅改该覆盖层，不动底层导航栏与其它功能）：
  - 将 sign-upload-page 的 `.page-header` 块替换为与同页底层一致的 `.status-bar`（9:41 📶🔋）+ `.nav-bar`（返回 `closeSignUploadPage()` + 居中标题「上传签约文件」+ `.actions` 内 `.native-capsule` 占位 `⋯｜○`）。`.native-capsule` 样式该页 `<style>` 已存在（line 18-66），无需新增。
  - `<style>` 补 `.sign-upload-page > .status-bar, .sign-upload-page > .nav-bar { flex-shrink:0 }`，与 worker 上传页一致，防止 flex 列布局下收缩。
- 校验：grep 确认 contract-detail.html 已无 `page-header/header-title/header-back/header-placeholder` 残留；sign-upload-page 内 status-bar+nav-bar 已就位，覆盖层出现时顶部导航栏与底层同页一致。
- 备注：业主版 `owner-miniapp/contract-detail.html`（line 1466-1471）存在同样 `.page-header` 未对齐问题，但用户仅点名「合规版」，本次未改；如需业主端一并对齐，待用户确认。

## 工人合同详情右侧导航「演示数据」删除 6 类合同切换 + 重置项
- 用户要求删除「演示数据」分组内的 6 类合同切换入口（拆除/水电/木作/泥瓦/油漆/小零工）及「重置演示数据」按钮，不影响其他已有功能。
- 落地（worker-contract-detail.html）：从「演示数据」status-group-content 中移除 7 个 status-switch-item（6 个 createDemo + 1 个 resetDemo）+ 分隔线；仅保留 A/C 演示页面入口（微信服务通知 C方案 / 应用内顶部通知条 A方案）；计数由 9 改为 2。
- JS 中 createDemo/resetDemo 函数保留（不再被 HTML 调用，属无害死代码）；其余功能（状态切换、收起展开、A/C 入口跳转等）均不受影响。

## 工人合同退出/重选页：删除「退出流程状态预览」分组
- 用户要求删除 `worker-contract-exit.html` 右侧原型导航内「退出流程状态预览」分组的全部 6 个状态项（已确认基线/退出待处理/乙方已退出终态/甲方驳回回已确认/甲方同意重选乙方/甲方主动乙方被替换终态），不影响其他已有功能。
- 落地：移除该 status-group 整块（含分隔线上方容器 + 标题 + 6 个 status-switch-item）；`setExitStatus` 函数保留在 JS 中（不再被 HTML 调用，属无害死代码）；页面主体内容（视角切换/banner/合同信息卡/流程视图/底部操作/弹窗）零改动。

## 工人合同退出/重选页：整页删除
- 用户要求删除 `worker-contract-exit.html` 整个页面，不影响其他已有功能。
- 落地：`git rm -f service-miniapp/worker-contract-exit.html`（该文件含未提交本地改动，故用 -f 强制删除，git 状态 `D` 暂存、未 commit）；并同步删除 `worker-contract-detail.html:928` 原型导航中指向该页的 `<a href="worker-contract-exit.html">工人合同退出/重选</a>` 链接，避免悬空跳转。
- 校验：grep 全工程确认 `worker-contract-exit.html` 引用 NONE；`worker-contract-detail.html` 原型导航其余项（首页/项目列表/项目详情/合同详情合规版/消息/工人合同详情/上传变更签约文件）与状态切换分组、A/C 演示入口均不受影响。

## 工人合同详情页：移动「上传变更签约文件（新页面）」导航项
- 用户要求将右侧原型导航顶部「上传变更签约文件（新页面）」链接，移入「合同状态切换（工人合同）→ 变更阶段」分组、置于「变更签约中」项下方，不影响其他已有功能。
- 落地：删除顶部 page-nav-item 链接（`worker-contract-detail.html:928`）；在「变更阶段」status-group-content 中「变更签约中」项（line 990）之后，新增 `<a class="status-switch-item" href="worker-change-sign-upload.html">上传变更签约文件（新页面）</a>`；该分组计数由 5 改为 6。
- 校验：grep 确认顶部 page-nav-item 已无此链接、新链接位于「变更签约中」下一行（line 991）、变更阶段 count=6；其余导航项、发起方/受邀方/演示数据分组、A/C 演示入口均不受影响。

## 工人合同详情：已确认(发起方)「上传签约文件」改为跳转独立上传页（参考变更签约页）
- 用户要求：已确认(发起方)状态点击「上传签约文件」的流程效果，参考「上传变更签约文件（新页面）」单独页面（worker-change-sign-upload.html）实现，不影响其他已有功能。
- 落地：
  1. 新建 `worker-sign-upload.html`（新增未跟踪文件）：结构/组件完全镜像 `worker-change-sign-upload.html`，内容适配 V1 签约——标题「上传签约文件」、说明「请上传线下已签署的《施工合同》扫描件或照片，上传后合同正式生效（V1 版本）」、文件 input id=`signFileInput`、提交按钮「提交签约文件」；返回→`worker-contract-detail.html?preview=worker_confirmed_sender`，提交成功→`worker-contract-detail.html?preview=worker_signed`（详情页 line 319-321 已支持 preview 参数）。复用 common.css + css/contract-detail.css 的 .sign-upload-page 系列样式。
  2. `worker-contract-detail.js`：将 `worker_confirmed_sender` 状态的 action 由 `'upload'` 改为 `'upload_sign'`；`handleAction` 新增 `if (action === 'upload_sign') location.href='worker-sign-upload.html'`。
  3. 原型导航「发起方视角」分组（count 5→6）在「已确认(发起方)」项下新增 `<a class="status-switch-item" href="worker-sign-upload.html">上传签约文件（新页面）</a>`（与变更阶段内的 change-sign-upload 入口对称）。
- 不影响范围：受邀方「已确认(受邀方)」仍用原 `action:'upload'` → 内联 `pickSignFile()`（`signFileInput` 元素保留，line 754），流程零改动；其余状态、变更签约中→worker-change-sign-upload.html、A/C 演示入口等均不受影响。
- 校验：node --check worker-contract-detail.js 通过；grep 确认 upload_sign/worker-sign-upload.html 在 html 与 js 引用一致、worker_confirmed_sender 配置已改、新增独立页文件存在。

## 工人合同详情：已确认(发起方)/已签约 状态弱化非乙方邀请人
- 用户要求：已确认(发起方)与已签约状态页面，乙方已确定，其他被邀请人应弱化展示浅色，效果类似变更阶段内未被选择的邀请人。不影响其他已有功能。
- 落地：`worker-contract-detail.js` 的 `renderInvitations` 中，将 `isRecord` 判定由 `isChangeView && !isPartyB` 扩展为 `(isChangeView || status==='worker_confirmed_sender' || status==='worker_signed') && !isPartyB`。命中即复用既有弱化样式：`.invite-row.is-record{opacity:.55}` + `.invite-avatar.muted`(灰头像) + `.invite-status.record`(灰色徽标) + 「邀约记录」标签（line 207-210、768 均已存在）。
- 乙方（firstId）仍走 `is-partyb` 高亮分支，不受影响；仅非乙方弱化。

## 工人合同详情：补写入合同模块 PRD（第七章）
- 用户要求：将工人合同详情的现状（场景/流程/页面/字段/交互效果）写入已有 PRD 文档，不影响其他已有功能及 PRD 内容，且更新目录。
- 落地目标：`PRD-合同模块详细规格.html`（合同模块详细规格，非完整文档）。
- 改动：
  1. 左侧目录（line 212）新增「七、工人合同详情页（服务方·新流程）」条目（href=#worker-contract），原一~六章不变。
  2. 滚动高亮脚本 sections 数组新增 `worker-contract`（顺带补齐原遗漏的 `change-function`、`global-spec`，使目录高亮与六章一致，无害）。
  3. 文档尾部（文档结束注释前，line 2878）新增完整第七章：7.1 概述定位 / 7.2 角色视角 / 7.3 核心场景(8 场景表) / 7.4 状态机流程(主流程 9 状态表 + 变更阶段 5 状态表) / 7.5 页面清单(主页面+2 独立上传页+3 演示页) / 7.6 字段数据结构(meta 7 字段 + 其他核心数据 6 对象) / 7.7 交互效果(8 条) / 7.8 原型导航(四分组入口表)。复用既有 .section/.table/.note/.info-grid 等样式类。
- 未改：完整文档 PRD-完整文档.html 未触碰（工人合同属合同模块，由其模块 PRD 承载，完整文档经模块链接可达），避免影响其他已有 PRD 内容。
- 校验：grep 确认 TOC href（212）、section id（2878）、章节标题（2880）、文档结束注释（3039）、高亮数组（3057）一致；`<div class="section">` 计数 6→7；新章节在文档结束前正确闭合（3037 `</div>`）。
- 不影响范围：确认中(发起方 worker_inviting_sender)/已确认(受邀方 worker_confirmed_receiver) 等非「乙方已确定」状态不进入该条件，邀请人展示保持原样；变更阶段逻辑零改动；受邀方终态(lost/rejected)走 renderReceiverEndedView 不渲染名单列表。
- 校验：node --check 通过；grep 确认 isRecord 新条件与弱化 CSS 存在。

## 工人合同 PRD：区分工人/非工人合同 + 工人合同详情按端拆分
- 用户要求：PRD 应区分「工人合同」与「非工人合同（基础/设计合同）」，且工人合同详情页此前未区分端（仅一条「服务方·新流程」），应按端拆分，对齐非工人合同已分 PC/服务方/业主端的做法。不影响其他 PRD 内容。
- 落地目标：`PRD-合同模块详细规格.html`（合同模块详细规格）。
- 改动：
  1. 新增 `.toc-group-title` 分组标题样式（line 82-93），目录 `<ul>` 中插入「非工人合同（基础施工/设计服务合同）」与「工人合同（六类班组服务合同·新流程）」两个分组分隔（line 220/227）。
  2. 目录条目：一~六章（非工人合同，分 PC/服务方/业主端）保留；原第七章拆为「七、工人合同详情页（服务方·工长端）」#worker-contract-sender 与「八、工人合同详情页（工人端·受邀方）」#worker-contract-receiver。
  3. 滚动高亮脚本 sections 数组同步为 8 项（含 worker-contract-sender/receiver）。
  4. 正文：原第七章（合并双视角）重写为第七章（服务方·工长端：7.1 概述+vs非工人合同差异+边界说明 / 7.2 发起方角色 / 7.3 服务方·双方场景 / 7.4 状态机主流程+变更阶段 / 7.5 服务方端meta+核心数据 / 7.6 页面清单 / 7.7 服务方端交互）+ 第八章（工人端·受邀方：8.1 概述+入口 / 8.2 受邀方角色 / 8.3 受邀方状态流程 / 8.4 元信息差异化表 / 8.5 终态轻量视图 / 8.6 A/C/E 简化接收方案 / 8.7 受邀方原型导航）。
- 校验：grep 确认 TOC group-title 两条、七/八条目锚点正确、section 计数 7→8、高亮数组含 sender/receiver、文档结束注释完好、旧 id="worker-contract" 无残留。
- 未改：完整文档 PRD-完整文档.html 未触碰；第一~六章（非工人合同）内容零改动；样式仅新增 .toc-group-title 一处。

- PRD 调整：工人合同变更功能独立成章。在 `PRD-合同模块详细规格.html` 左侧目录「工人合同」分组下新增「九、工人合同变更功能详细规格」(锚点 #worker-contract-change)，滚动高亮脚本 sections 数组同步追加该 id；章节数 8→9，目录/高亮/section 三者一致。
- 第九章内容(9.1-9.8)：概述+与合规版差异(去平台审核/V2/独立上传页/阶段暂停)、角色权限、变更状态机5态(changing/change_confirming/change_confirming_sender/change_signing_wait/change_rejected)、发起变更流程、确认/驳回/撤回流程、上传变更签约文件流程(worker-change-sign-upload.html→V2)、changeProposal 字段结构、原型导航变更阶段分组。
- 收敛引用：第七章 7.4.2 仅保留发起方视角3状态摘要并加「详见第九章」；第八章 8.3 提示语补「变更流程受邀方状态见第九章」。第一~六章及完整文档 PRD 零改动。
- 校验：Python 脚本锚点式编辑(规避 Read 工具当前报错)，12 项断言全部 OK；临时脚本已删。

## PRD 目录重构：全局规格参数决策 + 二级标题入目录 + 一级默认收起可展开
- 用户问题：「全局规格参数（第六章）是否也适用于工人合同？」决策：经核查第六章含「待审核(pending_review)/审核驳回(platform_rejected)」状态机与「审核人/审核时间/审核驳回原因」字段校验，且合同类型枚举为基础/设计/水电分包等**非工人**类型——工人合同走独立状态机、**无平台审核**、六工种、甲方=陈庄，故**不适用**。按用户分支：在工人合同最下方新增「第十章 工人合同全局规格参数」（不移动第六章）。
- 第十章(10.1-10.4) 内容镜像第六章维度但适配工人合同：10.1 Toast（复用）、10.2 分页（复用）、10.3 工人合同状态机（主流程 worker_draft_initial→worker_inviting→worker_confirmed→worker_signed + 撤回/重选；任务状态机同第六章；变更状态机叠加于已签约）+ 明确标注"无待审核/审核驳回"；10.4 字段校验规则（不含审核字段、六工种枚举、甲方固定陈庄、partyAName/partyBName/projectAddress/attachments/invited[]/changeProposal）。
- 目录重构（sidebar-toc-list 重写为嵌套结构）：
  - 为全部 75 个二级标题（.subsection）按编号(如 1.1/3.7.1)注入锚点 id（sec-<编号中划线>），TOC 二级链接指向对应 id。
  - 一级目录项（章节）改为 `<li class="toc-chapter collapsed"><div class="toc-chapter-head"><a>标题</a><span class="toc-caret">▸</span></div><ul class="toc-sublist">...</ul></li>`；每个一级项下挂其二级标题。
  - 一级默认收起（`.toc-sublist{display:none}`），点击 `.toc-chapter-head` 切换 `.expanded`（JS `classList.toggle('expanded')`）展开二级；章节链接仍平滑滚动。
  - 第十章置于「工人合同」分组末尾；第六章标题标注「（非工人合同 · 已确认）」以明确范围。
  - 滚动高亮 sections 数组由 9 项扩为 10 项（新增 worker-contract-global），并修复原数组 'worker-contract-receiver' 与 'worker-contract-change' 之间缺失的逗号。
- 实现约束：Read 工具在本文件持续报错，故全程用 Python 脚本做锚点式精确编辑 + 多重断言校验（85 个 TOC 链接全部能在正文找到对应 id、2 分组/10 章/10 子列表、第十章 4 个子节 id 齐备、footer 文本唯一、第六章范围标注存在）。临时脚本 `_prd_patch.py`/`_prd_fix_toc.py` 已删；发现并修复一处 bug：章节标题误带入 `<span class="badge">文件名</span>` 文本，已改为剥离 badge/section-file 元素后取纯标题。
