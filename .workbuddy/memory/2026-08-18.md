# 2026-08-18 工作日志

## 初始项目详情页（新版）交互效果补充
- 文件：`service-miniapp/project-detail-initial-v2.html`
- 新增「添加架构」入口：`.level-hint` 内加 `#addArchBtn`（＋ 创建工作组/施工组），点击打开底部抽屉 `#createWorkgroupSheet`。
- 创建工作组建档底部抽屉：两步流程（选工种类型 6 种：拆除/水电/木作/泥瓦工/油漆工班组 + 小零工 → 填名称/组长/备注），提交后写入 `localStorage.newProjectData.groups` 并同步更新项目部计数 `#projectDeptCount`，与 `architecture-empty.html` 持久化口径一致。
- 模块初始态预览 `#modulePreviewOverlay`：手机框内子页，覆盖快捷入口（架构/成员/资料/对账单）与「查看全部」（待办/任务/动态）的初始进入效果。快捷入口与三个 more-link 由 `data-href` 改为 `data-module`，点击弹出空态预览（标注「初始态」），并提供「进入完整模块 ›」跳转真实页。
- 约定：初始详情页演示交互一律走「页内预览/抽屉」，不破坏既有 `data-href` 跳转（如项目头部→项目信息页）。

## 初始项目详情页（新版）交互回退（用户要求）
- 用户反馈：原「模块初始态预览」「创建工作组建档抽屉」存在布局问题，且不需要「进入完整模块」按钮与子页预览效果。
- 回退内容（同文件 `project-detail-initial-v2.html`）：
  - 删除「模块初始态预览」遮罩 `#modulePreviewOverlay` 与「创建工作组建档」底部抽屉 `#createWorkgroupSheet`（HTML/CSS/JS 全量移除）。
  - 快捷入口（架构/成员/资料/对账单）与「查看全部」（待办/任务/动态）的 `data-module` 全部改回 `data-href`，直接跳转真实子页，由通用 `[data-href]` 处理器导航。
  - 「＋ 创建工作组 / 施工组」按钮 `#addArchBtn` 改为 `data-href="architecture-empty.html"`，点击直接跳转到架构页（不再弹出创建抽屉，不新增功能）。
  - quick-nav 容器内点击处理改为命中 `.quick-nav-item` 即 `return`，跳转交由通用 `[data-href]` 逻辑。
- 校验：div/script/style 标签闭合平衡（103/103、2/2、2/2），无残留 `data-module`。

## 初始项目详情页（新版）快捷入口/查看全部跳转一致性
- 用户要求：快捷入口（成员/资料/对账单）与「查看全部」（待办/任务/动态）跳转页面效果，需与页内「架构」入口（已用 `architecture-empty.html`）保持一致 —— 即初始态项目内所有模块入口都应跳转到各自 `-empty` 初始态页面。
- 调整 `project-detail-initial-v2.html` 共 6 处 `data-href`：
  - 成员 member.html → member-empty.html
  - 资料 project-files.html → project-files-empty.html
  - 对账单 statement-list.html → statement-list-empty.html
  - 待办 查看全部 todo-list.html → todo-list-empty.html
  - 任务 查看全部 task-list.html → task-list-empty.html
  - 动态 查看全部 activity-list.html → activity-list-empty.html
- 现初始详情页全部模块入口（架构/成员/资料/对账单 + 创建工作组 + 待办/任务/动态查看全部）统一指向 `*-empty.html` 初始态页面，与「进行中-新版」(ongoing-v2) 使用的常规页（非 -empty）形成明确区分。

## 项目详情家族导航补齐
- 用户要求：将「项目详情（初始-新版）」导航项添加到各「项目详情」相关页面的导航内。
- 在 3 个 v2 兄弟页的 `.page-nav` 中（按「进行中 → 进行中-新版 → 初始-新版 → 已完成」顺序，插入到「进行中-新版」之后、「已完成」之前）补充 `<a href="project-detail-initial-v2.html">项目详情（初始-新版）</a>`：
  - project-detail.html（进行中，原标签为「项目详情」）
  - project-detail-ongoing-v2.html（进行中-新版）
  - project-detail-completed.html（已完成）
- 现 4 个「项目详情」状态页（进行中 / 进行中-新版 / 初始-新版 / 已完成）导航互相完整列出彼此。
- 注：`project-detail-new.html` 为更早的「初始项目详情页」旧版，导航未纳入 v2 家族（其 active 名为「初始项目详情页」），本次未改动，待用户确认是否一并纳入。

## PRD-项目模块 §3.1-3.8 对齐「项目详情（进行中-新版）」原型

- 依据：`service-miniapp/project-detail-ongoing-v2.html`（§3.8 明确定位"服务方"，故对齐 service 端 v2；owner v2 为该原型同名业主端，不在 §3 范围）。
- §3.1 页面概述：路径由 `project-detail.html` → `project-detail-ongoing-v2.html`；适用角色补充"业主/项目总/设计师/工长/各工种工等"。
- §3.2 页面结构：重写为 v2 实际顺序——项目基本信息头部 → 待办事项(置于吸顶前、不联动架构) → 吸顶导航(快捷4项+展开/收起+架构选择器) → 合同/任务概览(上下堆叠) → 今日动态；加"布局顺序"说明段。
- §3.3 快捷导航：入口 5→4（项目信息移顶至基本信息头部），新增"展开/收起"切换按钮行；"项目信息"标注为已移出。
- §3.4 层级选择器：补层级类型标签(项目部/工作组/施工组)与概要信息("N个工作组·M份合同")。
- §3.5 展示字段：新增"项目基本信息头部"区域(已开工天数/面积户型)；待办默认显示条数 5→2（与 v2 CSS `nth-child(n+3)` 截断一致）；快捷导航入口数量 5→4。
- §3.6 数据刷新：补"页面加载显示 loading 遮罩"。
- §3.7 今日动态：已与 v2 对齐（角色标签项目总/工长/水电工/设计师、阶段标签取合同配置、展开更多一条半、查看全部跳 activity-list），保持不变。
- §3.8 交互逻辑：导航栏"⋯分享菜单/✕关闭" → 微信原生胶囊(⋯○)；新增项目基本信息头部点击跳 project-info.html；快捷导航展开/收起+滚动>50px吸顶收起；删除"操作菜单"行。
- 附：顺手修复文档既有 3 处非法 `</br>`（§6.11.3 工作组创建逻辑，超出 §3 范围）→ `<br>`，确保全文 HTML 平衡 0 错误。
- 校验：PRD HTML 标签平衡 0 错误；sub-3-1..8 锚点全在；§3 内无"默认5条"/旧自定义菜单残留。
- 遗留提示（超出本次 §3.1-3.8 范围，待用户拍板）：① line 5342 业主端章节"默认显示5条"与 owner v2 原型(2条)不符；② line 1734 §4.4"弹出操作菜单(分享给朋友)"——§4 页面(疑项目信息页)导航栏仍用自定义菜单，若同为新版应改微信原生胶囊。

## 服务方任务详情「已完成」状态补充「查看报告单」按钮
- 用户要求：服务方任务详情已完成状态页面，参考业主端原型，添加「查看报告」按钮及跳转页面交互。
- 对照：业主端 `owner-miniapp/js/task-detail.js` 在「已完成(§6.9)」与「已完成含驳回(§6.12)」的 actionButtons 中均渲染两按钮：`查看报告单`(secondary, `goToTaskReport()`) + `查看评价`(primary, `showEvaluationListModal()`)。
- 服务方 `service-miniapp/js/task-detail.js` 原仅在「已完成(§3.16)」「已完成含驳回(§3.19)」渲染单按钮 `查看评价`；`goToTaskReport()` 虽存在但**初始实现有 bug**：引用了不存在的 `state.currentTaskId`（`state` 在该 IIFE 作用域未定义，正确变量名是 `appState`；且 `appState` 也无 `currentTaskId` 字段），点击抛 `ReferenceError` → 无反应（用户反馈"点击无反应"）。
- 改动1：两处 actionButtons 由单按钮改为与业主端一致的双按钮（先 `查看报告单` 后 `查看评价`），`replace_all` 一次性落地。
- 改动2（修复跳转 bug）：服务方 `goToTaskReport()` 改为与业主端完全一致——`window.location.href = 'task-report.html?taskId=task-001'`（去除 `state` 引用，写死 `task-001`）。`window.goToTaskReport` 已暴露（line 3042）。
- 跳转目标 `service-miniapp/task-report.html` 已存在（与业主端同名共享模板），无需新建。
- 命名采用业主端「查看报告单」以与参考原型保持一致（用户原文「查看报告」为简称）。

## PRD-项目模块「项目详情页」四项计算规则补充 + 业主端/项目信息页导航对齐
- 用户要求：① 一并处理此前遗留两项（业主端「默认显示5条」、§4.4 自定义菜单）；② PRD 内补充四项计算规则。
- 四项计算规则（写入 PRD）：
  - 已开工 N 天：N = 当前日期 − 项目内第一份「已签约」状态合同日期（不足1天按1天计；无已签约合同则显示「新家准备中」）。落在 §3.5 已开工天数行。
  - 总进度百分比：已完成任务总数 / 已签约合同内有效任务总数 × 100%（§3.5 与 §12.5 共 2 处同步）。
  - 全部任务规则：已完成任务数 / 已签约合同内有效总任务数（§3.5 与 §12.5 共 2 处同步）。
  - 层级选择器下方区域数据：按「当前所选架构层级及其隶属子层级」聚合；新增 §3.4 统计口径 note + belong_level_id 三级树（项目部›工作组›施工组）关联关系与计算示例（项目部/工作组/施工组 三档示例）。
- 导航栏/快捷导航对齐微信原生胶囊（消除自定义 ⋯/✕ 菜单反模式）：
  - §4.4 项目信息页「⋯菜单(分享给朋友)」→ 微信原生胶囊(⋯○)（用户遗留项②）。
  - §12.2 业主端布局图 ascii：右侧「⋯更多|✕关闭」→ 微信原生胶囊(⋯○)；快捷导航「项目信息|架构|成员|资料 / 对账单」→ 架构|成员|资料|对账单（项目信息移顶）。
  - §12.4 业主端导航栏「右侧⋯按钮 / 右侧✕按钮」合并改为微信原生胶囊(⋯○)，rowspan 3→2。
  - §12.4 快捷导航入口文案 5→4（项目信息移至顶部）。
  - §12.5 快捷导航栏块：入口数量 5→「1📌+4」、移除「项目信息入口」行、补齐「对账单入口」行（rowspan 仍 5）；待办默认显示 5→2 条（用户遗留项①）。
  - §14.3 附件预览页「导航栏菜单(⋯按钮, 弹出操作菜单仅含项目信息项)」→ 微信原生胶囊（顺带修正的同类反模式，已单列说明）。
- 校验：PRD HTML 标签平衡 0 错误；项目详情家族内自定义菜单/✕导航 0 残留（仅剩 §10 对账单模块布局图「✕关闭」2 处，属其他模块、不在本次范围，保留）；四项公式与胶囊均落地。

## PRD-项目模块 §12.1-12.5 对齐业主端「项目详情（进行中-新版）」原型 + 复用 §3 减少冗余
- 用户要求：将 §12.1-12.5 按 `owner-miniapp/project-detail-ongoing-v2.html` 更新；可复用/引用服务方 §3.x 以减少冗余。
- §12.1：页面路径 `project-detail.html`→`project-detail-ongoing-v2.html`；新增「基本信息头部」note（已开工N天公式 / 面积户型 / 跳 project-info.html），引用 §3.1。
- §12.2：布局图重写为 v2 真实顺序（导航栏→基本信息头部→待办事项卡片→吸顶导航→今日动态→合同/任务概览→TabBar），并补「与服务方结构对齐」note 引用 §3.2/§3.8。
- §12.3：功能差异表新增「基本信息头部（已开工天数/面积户型）」行（服务方/业主均✅）。
- §12.4：交互逻辑表由「全量复述」改为「仅列业主专属/差异项 + 新增项」，共用交互（微信胶囊/快捷导航/层级选择器/项目导航/今日动态/合同概览/任务统计/loading）改引用 §3.8，并补「基本信息头部点击跳 project-info.html」行；底部加「共用交互引用」note。
- §12.5：tbody 顶部新增「基本信息头部（已开工天数/面积户型）」「层级选择器区域数据」两行（引用 §3.4/§3.5）；末尾补「数量统计公式」note 声明与服务方 §3.5/§3.4 口径一致。
- 校验：PRD HTML 标签平衡 0 错误；旧路径 `owner-miniapp/project-detail.html` 与旧布局「快捷导航栏（可收起）」0 残留；§3.1/§3.2/§3.4/§3.5/§3.8/§12.1-§12.5 锚点均在。

## 项目资料页（service-miniapp/project-files.html）重构为「可折叠分组列表」
- 用户要求：手机端小程序适配收纳方案，保留原页面已有的「权限说明」及权限效果，并修复原原型死交互。
- 方案：顶部搜索 + 类型筛选条（全部/图片/视频/音频/文档，真正生效）；主体由「按来源大块三列网格」改为「按项目信息/合同分组的列表流」，每组标题可点击折叠/展开（▾ 箭头旋转），组内为单行文件项（类型图标 + 文件名 + 来源·时间 meta）。
- 修复死交互：原 `.category-grid` 调用未定义的 `filterByCategory()`、隐藏的 `switchTab` 绑定未渲染的 `.tab-item` 均失效；新方案 `switchFilter(type)` 按 `data-type` 真实显示/隐藏文件行并叠加搜索关键词；切换筛选/搜索时自动展开全部分组以保证结果可见。
- 权限原样保留：右下角 ⚠️ 悬浮 → 「权限说明」气泡（文案不变）；木工合同整组 `locked`（🔒 + 灰色「无权限」badge），组内 3 个文件行 `locked`（🔒、半透明、not-allowed、点击 alert「无权限查看此合同资料」）。
- 清理：移除原 `imageContent/videoContent/audioContent/docContent` 冗余类型视图与破损 CSS keyframes（`tagFadeIn`/`to` 多余闭合）。
- 校验：div 113/113、script 1/1、style 2/2 闭合平衡；`node --check` 内联 JS 语法 OK；关键函数 switchFilter/toggleGroup/onRowClick/applyView/togglePermissionTip 均在。

## 项目资料页修复：类型筛选条类名冲突导致底部遮挡
- 现象：顶部类型筛选条误用 `class="tab-bar"/"tab-item"`，与 `common.css` 中微信底部主导航栏 `.tab-bar{position:absolute;bottom:0;...}` 重名，被强制绝对定位到手机框底部，盖住内容；权限浮窗 ⚠️ 贴底又压在它上面（用户反馈"底部 tab 遮内容 / 权限浮窗遮底部 tab"）。
- 修复：筛选条独立改名为 `.type-filter`/`.type-filter-item`（CSS/HTML/JS 三处同步），脱离底部导航样式；并给 `.container` 增加 `padding-bottom:20px` 避免末项被 ⚠️ 压角。
- 校验：project-files.html 内已无 `class="tab-item"`（0 处）、筛选条全部 `type-filter-item`（8 处引用）；div 113/113 平衡；`node --check` JS OK。

## 新建服务方原型「项目详情（已完成-新版）」page
- 用户要求：参考 service `project-detail-ongoing-v2.html` 布局，创建新的 `project-detail-completed-v2.html`，逻辑参考 `project-detail-completed.html`。
- 复用 v2 布局骨架：微信原生胶囊导航、基本信息头部（已完成态改「项目已完工」+ 面积户型，绿调，跳 project-info.html）、待办卡置于吸顶前（空状态「暂无待办事项」badge 0）、sticky-header 4入口快捷导航(项目信息已移顶)+展开收起+arch-half 层级选择器、compact 上下堆叠统计、今日动态 v2 样式与动效。
- 套用 completed 逻辑：合同概览 8/8 签约、总进度 100%（8 份合同均 100%，默认展开）、任务统计 24/24、确认中0、执行中0、待办空态、动态=竣工验收/最终结算对账单/全部任务验收（首条陈庄项目总）。
- 对齐项目命名口径：8 份合同名统一为「班组服务合同」（水电/泥瓦工/木作/油漆工/防水/吊顶/橱柜/墙面），去「分包合同」；动态「李监理」→「陈庄/项目总」，去监理角色。
- 家族导航互相补全：在 ongoing-v2 / completed / initial-v2 三页 page-nav 插入「项目详情（已完成-新版）」入口；新页 nav 含全部 5 个状态页并自身 active。旧 `project-detail-new.html`（早期初始页）未纳入 v2 家族、本次未改动。
- 校验：4 页 HTML 标签平衡均 0 错误；新页「分包合同/监理」0 残留；导航 active 唯一。PRD 未改动（本次仅任务范围内原型）。

## PRD 任务模块同步「任务报告单页」需求（代码定稿后回写）
- 背景：任务报告单相关原型改动（服务方已完成态加「查看报告单」按钮、服务方/业主方「分享报告」已取消）此前未回写 PRD，用户要求按既定范围同步。
- 改动文件：`PRD-任务模块详细规格.html`、`PRD-完整文档.html`。
- 任务模块新增「十、任务报告单页（公共）」章节（置于公共规格组，TOC 加条目，不重排 §7/§8/§9 以免破坏既有锚点引用）：
  - §10.1 页面概述：服务方/业主方共用；进入入口为任务详情页「已完成/已完成含驳回」操作栏「查看报告单」；双原型路径；原型演示链接。
  - §10.2 页面结构与字段：纸质单据风格，标题区「已通过」戳章 + 编号日期 + 七区块（基本信息/执行标准/确认标准/担责标准/执行情况/确认记录/确认人评价及附件）+ 报告生成时间；区块内容对齐 `service/owner-miniapp/task-report.html`。
  - §10.3 操作栏与交互：仅「下载报告」主按钮；进入方式 `goToTaskReport()` 跳 `task-report.html?taskId=…`，与「查看评价」并列。
  - §10.4 生成规则（本轮新增）：任务终态「已完成/已完成含驳回」全部确认通过时系统自动生成；报告编号 `HT-年份-合同序号-任务序号`；生成时间=最后确认人确认时间；七区块实时聚合自任务/阶段/执行记录卡/确认记录卡/评价字段；「已通过」戳章条件；落库留存快照。
- 四个已完成状态操作栏补「查看报告单」：§3.16、§3.19（服务方）、§6.9、§6.12（业主方）的「权限与用户视角」表新增「查看报告单」按钮描述，并各加 1 条「查看报告单→跳任务报告单页」交互说明（§3.19/§6.12 交互段补「查看报告单」）。
- **PRD 不体现取消功能约定（用户 2026-08-18 明确）**：已删 §10.3「分享报告」行与 `PRD-完整文档.html`「任务报告」行「已取消分享报告」字样；TOC 补 §10.1-§10.4 子项。校验：div 564/564、table 76/76、ul 31/31 平衡；shareReport/已取消残留均 0。

## PRD-项目模块 §3.10-3.11 对齐「项目详情（已完成-新版）」原型
- 用户要求：将新建的 `service-miniapp/project-detail-completed-v2.html` 效果更新到项目模块 PRD §3.10-3.11。
- §3.10 概述：页面名称「项目详情页-已完成（新版）」；路径 `/pages/project-detail-completed-v2/index`（链 v2 原型）；适用角色增「各工种工」对齐 §3.1；状态说明改为引用 §3.1/§3.8 v2 布局（原生胶囊/基本信息头部/待办前置/吸顶导航/compact统计/今日动态），差异仅数据状态；合同名口径与角色引用 §3.5/§3.7。
- §3.11 差异表：表头注明（v2）；新增「基本信息头部」行（进行中已开工N天蓝 vs 已完成项目已完工绿、整块跳项目信息页）；合同列表注明班组服务合同口径与 `contract-detail.html?status=completed` 跳转；总进度进行中格补 §3.5 公式；今日动态注明权威角色（陈庄/项目总）；表前补「仅列数据状态差异 + 口径引用 §3.5」note。
- 校验：PRD HTML 标签平衡 0 错误；§3.1/§3.5/§3.7/§3.8/§3.10/§3.11 锚点均在；旧 `project-detail-completed.html`（非-v2）仅剩 owner 端 §12 line 5389（业主端章节，超出本次范围）。

## 新建业主端原型「项目详情（已完成-新版）」page + 同步 PRD §12.7-12.9
- 用户要求：业主端先添加「已完成-新版」原型页，再更新对应 PRD（业主端 §12 已完成状态章节）。
- 新建 `owner-miniapp/project-detail-completed-v2.html`：布局取自 service `project-detail-completed-v2.html`（原生胶囊、基本信息头部「项目已完工」绿调、待办空态前置、4入口快捷导航+展开收起+arch-half层级选择器、compact堆叠统计、今日动态 v2 样式/动效）；逻辑用 owner completed（8/8签约·100%·24/24任务·0确认0执行·待办空态·竣工/结算/验收动态）。业主专属：TabBar「我的家」active（跳 ongoing-v2）、page-nav owner 家族、今日动态「查看全部」跳 owner `activity-list.html`、合同名统一班组服务合同、角色陈庄/项目总（去监理）。脚本与 service v2 同源（自包含 IIFE，无外部 js）。
- 家族导航补全：在 owner `project-detail-ongoing-v2.html` / `project-detail-completed.html` page-nav 插入「项目详情（已完成-新版）」入口；新页 nav 含 owner 家族页并自身 active。
- PRD §12.7-12.9 对齐 v2（原指向旧 `project-detail-completed.html`，本次迁移）：
  - §12.7 概述：名称「项目详情页-已完成（新版）（我的家）」、路径→`owner-miniapp/project-detail-completed-v2.html`、补 v2 布局引用（§12.1/§12.4）+ 合同名口径(§12.5)/角色(§13.2) note。
  - §12.8 差异表：表头（v2）；新增「基本信息头部」行（已完成项目已完工绿 vs 进行中已开工N天蓝）；合同列表补班组服务合同口径 + `?status=completed`；总进度/全部任务补 §12.5 公式引用；待办注「默认2条截断」；今日动态注 owner `activity-list.html`；表尾补「仅列数据差异、口径同 §3.5/§3.4」。
  - §12.9 交互：共用交互改引用 §12.4；业主专属补「基本信息头部跳 project-info.html」「合同项跳 ?status=completed」「待办空态仍可跳转」「今日动态跳 owner activity-list.html」。
- 校验：4 文件（新页/owner ongoing-v2/owner completed/PRD）HTML 标签平衡均 0 错误；新页「分包合同/监理」0 残留；§12.7-12.9 锚点完整、旧 `project-detail-completed.html` 引用已从 §12.7-12.9 移除（§3.10 line 1467 仍正确指向 service v2，不动）。

## 业主端项目资料页与服务端同步
- 用户要求：`owner-miniapp/project-files.html` 与服务端 `service-miniapp/project-files.html` 同步。
- 业主端原为该页**旧版**（同样含 `tab-bar`/`tab-item` 类名冲突致底部遮挡 + 失效 `switchTab` 死交互），数据（项目信息/水电/泥瓦/木工合同、文件清单、木工无权限加密态）与服务端完全一致。
- 操作：直接 `cp` 服务端重构版覆盖业主端文件（最小误差、结构/样式/脚本一次性同步）。
- 校验：业主端副本 div 113/113、style 2/2、script 1/1 平衡；`node --check` JS OK；`class="tab-bar"`/`class="tab-item"`/`switchTab` 均 0 残留；`type-filter`/`switchFilter`/`toggleGroup`/`onRowClick`/`applyView`/`togglePermissionTip` 等关键结构均存在。权限说明悬浮 + 木工无权限加密效果原样保留。

## PRD-项目模块 §8/§17 项目资料页按最新原型更新
- 背景：业主端 `project-files.html` 已与服务端同步（类型筛选生效 + 可折叠分组列表 + 权限原样）；PRD §8（服务方）仍描述旧版（分类网格 图纸/合同/验收/照片/变更 + 文件网格），§17（业主方）仅薄薄一句只读。
- §8 更新：8.1 业务目标改"按来源/合同分组、按文件类型筛选、无权限隔离展示"；8.2 页面结构 code-block 改 搜索栏+类型筛选(全部/图片/视频/音频/文档)+按来源/合同可折叠分组列表(项目信息/水电/泥瓦/木工无权限)+⚠️权限说明悬浮，补「布局说明」4 条；8.3 文件分类规格→**文件类型筛选规格**（删废弃的图纸/合同/验收/照片/变更，改 全部/图片/视频/音频/文档，TOC 文案同步）；8.4 文档格式补 DOCX/XLSX；8.5 权限控制无权限行改"分组级——整组标灰+🔒+灰色无权限badge；组内文件行半透明/🔒/文件已加密/点击提示无权限查看此合同资料"。
- §17 重构为引用式减少冗余：17.1 加「与服务端一致」note 引用 §8.2–§8.5；17.2 由"功能说明"改为「业主端差异说明」仅列 只读模式/权限隔离一致/权限说明悬浮一致 三条并引用 §8.5、§21.5；TOC 文案同步。
- 校验：PRD HTML 标签平衡 0 错误（div 718/718、table 115/115 等）；旧"文件分类规格"0 残留；`sub-8-*`/`sub-17-*` 锚点保留未改名，TOC 跳转不受影响。

## 任务报告单 PRD 补「无确认人」生成场景 + 下载本地交互详情
- 用户要求：① 生成规则"生成时机"补「无确认人时执行人完成即生成报告单」场景，并补该场景原型示例页；② PRD「下载报告」详述手机端小程序下载到本地交互。
- 新建原型 `service-miniapp/task-report-no-confirmer.html`（由 `task-report.html` 复制改）：顶部场景标识条；编号 HT-2024-001-002；「六、确认记录」改为无确认人提示 + 仅执行人「已完成」一行；「七、确认人评价及附件」改为空态「本任务无确认人，暂无确认人评价记录」；底部生成时间改为执行人完成时间 17:00:00；新增 `.scenario-banner/.no-confirmer-note/.empty-evaluation` 样式。校验 div 110/110、style 2/2、script 1/1 平衡。
- PRD §10.4 生成时机：拆分为「有确认人 / 无确认人」两场景——无确认人时执行人点「完成任务」直接流转已完成、即时生成（引用 §3.14(2)），无确认记录/评价，原型示例链 `task-report-no-confirmer.html`；生成日期行补「无确认人时=执行人完成提交时间」。
- PRD §10.3 下载报告：由单行简写扩写为「下载报告·手机端本地保存交互」——①触发渲染长图/PDF+「报告生成中」；② `wx.saveImageToPhotosAlbum` 存相册（首次授权弹窗）/或 `wx.downloadFile`+`wx.openDocument` 存文件；③允许→「已保存至相册」、拒绝→「请在微信设置开启相册权限」；④明确与「分享报告」边界（仅本地保存，分享已取消）；原型 `downloadReport()` 仅模拟 toast。
- PRD §10.1 原型演示链接增「无确认人场景示例」入口。校验：任务模块 PRD div 564/564、table 76/76、ul 35/35 平衡；锚点全部可解析；`task-report-no-confirmer` 引用 2 处；`saveImageToPhotosAlbum` 已入文。

## PRD-项目模块 补「已完成状态·状态流转与回退规则」
- 需求：项目已完成态不锁定原有功能，仍支持发起合同变更/创建架构/创建合同；发起变更或创建新合同→状态回退进行中；全部任务再次完成→自动恢复已完成。
- 新增 §3.13（服务方，完整规则）：核心原则(不锁定、入口权限同进行中)、状态流转 code-block、回退触发条件表(发起合同变更/创建新合同→已完成→进行中；创建架构本身不单独触发)、回退后表现(标签绿→蓝、恢复§3.1–§3.9布局与交互、§3.5统计重算)、再次完成(口径同§3.5、判定逻辑与首次一致)、状态一致性(全局字段 projects.status，业主端§12.10同步)。
- 新增 §12.10（业主端，引用式去冗余）：引用§3.13；差异——业主端不发起变更且架构页不支持创建合同故回退由服务方驱动；状态随服务方回退/再次完成联动；"我的家"入口仅已完成时进入。
- TOC：§3 与 §12 各增 3.13/12.10 子条目；§3.13↔§12.10 双向交叉引用。校验：PRD 标签平衡 0 错误(div 723/723、table 116/116 等)；`sub-3-13`/`sub-12-10` 锚点均在；TOC 与交叉引用一致。

## 任务报告单原型导航 + 无确认人示例页去除「场景示例区域」
- 用户要求（本轮）：① `service-miniapp/task-report-no-confirmer.html` 取消顶部「场景示例区域」（`.scenario-banner` 标识条），并将该页导航项加入「任务报告单」页原型导航，标题「报告单-无确认人任务」；② 重申 PRD 生成规则补「无确认人」场景 + 下载本地交互（已在上一轮 §10.4/§10.3 落地，本次仅复核确认，无改动）。
- `task-report-no-confirmer.html`：删除顶部 `.scenario-banner` 整块 HTML（场景标识条）及对应 `.scenario-banner`/`.scenario-banner strong` CSS；保留正文内「六、确认记录」的无确认人提示块（`.no-confirmer-note`）与「七、确认人评价」空态（属报告内容，非场景示例区）。校验 `.scenario-banner` 残留 0 处。
- `service-miniapp/task-report.html`：原无 page-nav，新增 `.page-nav` 原型导航——主层级（首页/项目列表/项目详情/任务详情/任务报告单[active]）+「报告单场景」分组（报告单-有确认人 → task-report.html；**报告单-无确认人任务 → task-report-no-confirmer.html**），并补 `<script src="js/page-nav.js">` 使导航可折叠。
- 注：`task-report-no-confirmer.html` 本身未加 page-nav（用户仅要求在「任务报告单」页加入口）；如需双向导航可后续补。
