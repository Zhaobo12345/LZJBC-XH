# 2026-07-30 工作日志

## 邀请功能补充：平台内选择匹配标准
在「邀请身份区分」功能（7/29 已完成）基础上，新增平台内选择的匹配标准需求：
**服务方人员须已认证至少一个角色，方可被平台内选择匹配**；业主及家人不受此限。

### 改动范围（不影响其他已有功能/描述）
- **原型两端** `service-miniapp/member.html` / `owner-miniapp/member.html`：
  - 示例数据 `platformUsers` 增加 `verifiedRoles` 字段（service 人员给具体数量，pu3 设 0 用于演示禁用）。
  - `renderPlatformUsers`：服务人员类候选中，未认证任何角色者以 `disabled` 态+"未认证角色"标签置灰、不可点击；业主类正常。
  - `selectPlatformUser`：加防护，服务人员未认证角色不可选。
  - 新增 CSS `.platform-user-item.disabled` / `.pu-badge`；form-tip 文案补充匹配标准。
- **技术规格补充文档.md**：`users` 表新增 `certified_role_count`（Int，默认 0）；`users` 表后补充匹配标准说明。
- **数据库ER图与索引设计方案.md**：`users` mermaid 增加 `certified_role_count`；2.3 invite_records 节补充匹配标准说明。
- **PRD-项目模块详细规格.html**：7.5 平台内选择操作说明补充认证标准；7.6 交互规则新增「平台内选择匹配标准」条目，数据模型对应新增 `users.certified_role_count` 说明。

### 校验
三处文档 + 两端原型均确认规则就位，无残留旧逻辑（"未找到匹配的用户"已改为"未找到可匹配的用户"）。

---

## 邀请功能补充：消息接收多角色 + 架构页邀请对齐

### 任务一：消息接收多角色选择（PRD 新增独立「消息」规则）
需求：被邀请人点击"接收邀请"时，若拥有多个已认证角色需先选角色再进入项目详情，否则直接进。
- **原型两端** `service-miniapp/message.html` / `owner-miniapp/message.html`：
  - 新增 `currentUser`（含 `verifiedRoles` 数组，演示值 3 个角色）。
  - `confirmAction` 接收分支：`verifiedRoles.length > 1` → `openRoleSelectModal`（弹角色选择框，列表由 `roleSelectList` 渲染，点击 `selectJoinRole` 进入）；否则 `goToProject` 直接进项目详情。
  - 新增 `#roleSelectModal` 弹窗 + CSS，确认/接受流程其余保持不变。
- **PRD**：七章新增 **7.14 消息中心 - 邀请接收与角色确认规则**（接收流程、多角色选择规则、状态与落地端一致、与 7.5/7.6 同源闭环）。

### 任务二：架构页邀请对齐成员页（三端一致）
需求：项目架构页"邀请成员"逻辑/交互与成员页一致；不影响其他功能/描述。
- **原型两端** `service-miniapp/architecture.html` / `owner-miniapp/architecture.html`：
  - 邀请弹窗旧版（手机号邀请+二维码+微信）整体替换为成员页新版：身份分段[服务人员|业主及家人] + 落地端权限提示 + 平台内选择(已认证角色过滤) + 二维码/微信。
  - 新增 JS：`selectInviteCategory`/`updateInviteTargetApp`/`renderPlatformUsers`/`selectPlatformUser`/`filterPlatformUsers`，`showInviteMemberModal` 重置逻辑，`sendInvite`/`shareToWechat` 按身份分支；移除手机号分支。
  - 新增 CSS：`.invite-category` / `.platform-user-*` / `.invite-target-app` 等。
- **PRD**：架构章（六、十五）"打开邀请成员弹窗"引用补充 7.6 身份分段 + 落地端权限提示，修正十五章错误锚点 `#page-member-owner` → `#subsection-7-5`/`#subsection-7-6`。

### 校验
- 两端 message.html：`handleAccept`/`roleSelectModal`/`verifiedRoles` 就位，无 `relationship`/手机号残留。
- 两端 architecture.html：与 member.html 新版一致（`invite-category`/`platformInviteForm`/`selectInviteCategory` 计数相同），无手机号残留。
- PRD：7.x 锚点 id/href 连续无悬空（5–14），旧引用清零。
- 所有临时脚本已清理。

---

## 邀请功能补充：消息规则归位 + 页面提示移除 + 未认证隐藏

### 1) 消息规则移入 PRD-完整文档（公共文档）
- **PRD-完整文档.html**：六、功能清单之后新增独立 **七、消息功能**（邀请接收与角色确认规则 type=invite：接收流程表、多角色(≥2)选择分支、单/无角色直接进、取消/身份归属/业主不受限/一致性要点）；原 七/八/九（UI/研发/交付）顺延为 八/九/十，TOC 同步更新；功能清单"消息通知"行链接到 #message-feature。
- **PRD-项目模块详细规格.html**：移除 7.14 独立块（规则不再放在其他文档），改为指向《PRD-完整文档》七、消息功能的引用说明。

### 2) 页面提示移除 + 未认证人员不展示
- 两端 member.html / architecture.html（共 4 文件）：移除页面内 `form-tip` 文案"从平台已注册用户中选择被邀请人；服务方人员须已认证至少一个角色方可匹配选择"（仅保留在 PRD 描述）。
- `renderPlatformUsers` 改为：服务人员类中 `verifiedRoles < 1` 者**不进入候选列表**（此前为置灰+badge 展示）；同时移除 `disabled`/`pu-badge` 渲染分支，`selectPlatformUser` 仅渲染可选项。
- 规则在 PRD 7.5/7.6 已有描述（平台内选择需已认证至少一个角色方可匹配）。

### 校验
- 4 原型：hint_removed=True、old_disabled=False、new_filter(未认证者不进入候选列表)=True、new_map(onclick selectPlatformUser('${u.id}'))=True。
- PRD-完整文档：七消息/八UI/九研发/十交付 编号正确，message-feature id 与 TOC/功能清单链接就位。
- PRD-项目模块：subsection-7-14 残留=False，指针链接=True，全局 7-14 引用=0。

### 待办（用户未决）
- 邀请成员页面布局方案待用户选择：默认空+搜索匹配 / 改为独立页面 / 弹窗内分页。

---

## 邀请功能补充：方案A落地 + 消息页单/多角色演示控制

### 方案A：平台内选择默认空 + 搜索匹配
用户确认采用方案A（弹窗内默认空、通过搜索匹配，规避大数据量）。
- 两端 member.html / architecture.html（共 4 文件）：`renderPlatformUsers` 在搜索词为空时仅展示提示"输入关键词搜索平台内的服务人员 / 业主"，**不再预置渲染全部人员**；输入关键词后实时过滤（已按身份 + 已认证角色预筛）。未认证角色服务方人员仍不进入候选（与第3点一致）。

### 消息页单/多角色演示控制（仿创建项目演示控制形式）
- **service-miniapp/message.html**（服务方人员接收邀请，含角色概念）：
  - 新增 `.demo-control-panel` + `.segmented-control` CSS（位置/样式对齐 create-project.html 的演示控制面板）。
  - 页面右侧新增"接受邀请演示控制"面板：分段 [单角色 | 多角色]，默认多角色；`setDemoRoleMode(mode)` 切换 `currentUser.verifiedRoles` 为 `['水电工']`（单）或 `['水电工','木工','项目经理']`（多），并同步状态文案。
  - 演示效果：默认多角色 → 点击"接收邀请"弹"选择加入角色"框；切到单角色 → 点击"接收邀请"直接进入项目详情。与既有 `confirmAction`（`verifiedRoles.length > 1` 分支）自然联动。
  - 注：owner-miniapp/message.html 未加此演示面板——业主无"已认证角色"概念，多角色选择规则仅适用于服务方人员；如需要在业主端做其他形式演示可另议。

### PRD 对齐
- **PRD-项目模块详细规格.html** 7.5：平台内选择描述改为"通过关键词搜索匹配目标成员（默认不预置列表，输入后实时筛选）"，与方案A一致。
- 7.6「平台内选择匹配标准」：将原"未认证者置灰、不可点击"改为"未认证任何角色者不进入候选列表（不在可供搜索的人员内展示）"，与实现一致。
- **PRD-完整文档.html** 七、消息功能：已含单角色(≤1)直接进 / 多角色(≥2)先选角色 规则描述（与演示一致），无需改动。

### 校验
- 4 原型：placeholder=True、unverified_hidden=True（方案A 默认空 + 未认证隐藏就位）。
- service message.html：demo panel id / 双 segment / setDemoRoleMode / 单角色赋值 / roleSelect CSS / 单 `</style>` 均 True。
- PRD：7.5 搜索匹配描述=True、7.6 隐藏未认证=True；PRD-完整文档 消息 single/multi 规则=True。
- 残留"置灰、不可点击"经核查均属"移出项目"等无关功能，平台内选择相关描述已统一为"不进入候选列表"。
- 所有临时脚本已清理。

---

## owner-miniapp/member.html：移除权限演示面板，默认全部权限

业主端项目成员页：因业主默认拥有全部权限，移除"当前用户权限演示"控制面板（`#permissionControlPanel` 整块 HTML + 连带 `setDemoPermission` 函数）。
- `memberList` 容器去掉 `no-manage` 类（该类会隐藏箭头 `.arrow`、禁用 `.permission-badge` 点击），默认即呈现可点击/可配置权限的全部权限效果。
- `currentUserPermission` 默认值由 `'none'` 改为 `'all'`，点击成员直接走 `showPermissionModal` 完整权限设置；`showPermissionModalIfAllowed` 中 `'none'`/`'remove'` 分支变为不可触达的死代码（保留无害）。
- 遗留 `.member-list.no-manage` 三条 CSS 规则已无使用场景，未删除（不影响功能）。

---

## 项目架构页创建弹窗：隶属展示 + 去备注 + 单一入口

需求：创建工作组/施工组弹窗——隶属默认取点击的父级（项目部 / 某工作组），仅展示不可选择；移除"备注"字段（无展示效果）；一个层级仅保留一个"添加"入口（修复水电工作组下重复"添加施工组"入口）。两端（service/owner）一致，并更新 PRD；不影响其他已有功能与规则。

原型两端 `service-miniapp/architecture.html` / `owner-miniapp/architecture.html`：
- `level1Form`（创建工作组）：移除"备注"输入块；新增"隶属"展示块（静态文本"项目部"，因工作组仅能在项目部下创建），用 `.form-display` 只读样式。
- `level2Form`（创建施工组）：原"所属工作组"下拉 `<select id="level2Parent">` 改为"隶属"展示块 `<div class="form-display" id="level2ParentDisplay">`；打开弹窗时由 `showCreateModal` 写入点击的工作组名（如"水电工作组"）。
- 修复重复入口：删除水电工作组 `node-children` 末尾多余的 `add-node-btn`（"添加施工组"），使每个工作组层级仅保留 `node-actions` 内一个"添加施工组"入口（与泥瓦/木工/油漆工作组及动态创建节点一致）。
- `showCreateModal`：level1 分支去掉 `level1Remark` 重置；level2 分支由 `getElementById('level2Parent').value = ...` 改为 `getElementById('level2ParentDisplay').textContent = parentName || ''`。
- 新增 CSS `.form-display`（只读展示框，灰底、44px 高、flex 居中）；`.add-node-btn` 的 CSS 规则保留未用（无害）。

PRD-项目模块详细规格.html（六章架构 + 十五章业主端）：
- 创建弹窗字段：6.10.2 表单字段"备注"行→"隶属"行（展示"项目部"）；6.11.2 表单字段"所属工作组（下拉）"行→"隶属"行（展示点击的所属工作组）。
- 交互流/说明：3.9、6.3、6.7（2661/2739）、业主端 15.6/15.7（4916/5411/5476）同步去"备注"、加"隶属展示"、标注"无备注项"。
- 6.10.3 创建人说明补充："隶属"字段自动展示点击父级（仅展示不可选）；同一层级仅一个"添加"入口，无重复入口。
- 6.11.1 触发入口去掉"工作组末尾的添加施工组按钮"描述（即被移除的重复 add-node-btn）。

校验：
- 两端 grep：`level1Remark`/`level2Parent`(下拉)/`add-node-btn`(HTML) 均 0 残留；`form-display`、`level2ParentDisplay`、"添加工作组"1处、"添加施工组"每层级1处（4静态+1模板）均就位。
- PRD grep：创建相关"备注"全清除；"所属工作组"下拉描述改为"隶属"展示；"工作组末尾"描述已删除；其余"所属工作组"（数据模型/权限/邀请）保留正确。
- 临时脚本与 PRD .bak 已清理。

---

## 项目架构页：取消组长选择 + 创建人展示（服务端/业主端一致）
需求：项目架构页创建工作组/施工组，取消"组长"选择项；创建成功后展示"创建人"（当前操作用户），无组长逻辑。两端（service/owner）实现一致，并更新 PRD 描述；不影响其他已有功能/规则。

### 原型两端 `service-miniapp/architecture.html` / `owner-miniapp/architecture.html`
- 移除 `level1Form`/`level2Form` 内"组长"表单块（`#level1Leader`/`#level2Leader` + `showLeaderSelect` 按钮）及整个 `#leaderSelectModal` 弹窗。
- JS：`selectedLeaders` 变量删除，改为 `currentCreateParentName` + `const currentUser`（service=`{name:'张三',role:'项目经理'}`、owner=`{name:'陈业主',role:'业主'}`）+ `escapeHtml()` 防注入。
- 删除 `showLeaderSelect`/`closeLeaderSelectModal`/`selectLeader`/`removeLeader` 四函数。
- `createNode`：去掉组长校验，确认回调改为 `insertCreatedNode(...)` + `showToast('创建成功！创建人：'+currentUser.name)`。
- 新增 `findGroupNode(name)`（用 CSS 选择器 `.architecture-tree > .tree-node > .node-children` 定位，不依赖脆弱 id 锚点）与 `insertCreatedNode(type,name,parentName)`（动态 `document.createElement` 插入：Level1 挂项目部 `node-children`，Level2 挂对应工作组 `node-children` 的 `node-actions` 之后；节点 meta 为 `<span class="role-tag creator">创建人</span> 张三`；复用既有节点结构及 toggle/邀请/创建/删除/合同/退出 交互；头部工作组计数 +1）。
- 既有 6 处节点 `role-tag leader">组长` → `role-tag creator">创建人`（李四/王五/孙八/周九/吴十/郑十一）。
- CSS 新增 `.role-tag.creator { background-color: rgba(250,173,20,0.3); }`，保留原 `.role-tag.leader`。

### PRD-项目模块详细规格.html（六章架构、十五章业主端）
- 6.3 节点层级表：工作组"组长"→"创建人"、施工组"组长、工人"→"创建人、工人"。
- 6.4/6.7/交互流：去掉"选择组长"步骤，改为"无需选择组长""无组长项"，确认创建行展示"创建人：当前操作用户"。
- 6.10.2 表单"组长"行→"创建人（系统自动）"；6.10.3 "组长选择弹窗"→"创建人说明（原'组长选择弹窗'已取消）"+说明段；6.10.4/6.11 步骤同步去组长、加创建人。
- 十五章（业主端）添加工作组/施工组字段与说明同步去组长。
（大 HTML 文件 Edit 状态易失效，改用 Python 脚本精确字符串替换 10 处，全部 OK。）

### 校验
- 两端 architecture.html：`grep selectedLeaders|showLeaderSelect|selectLeader|leaderSelectModal|role-tag leader">组长|请选择组长` → No matches。
- 两端均含 `currentUser`/`insertCreatedNode`/`findGroupNode`/`role-tag creator">创建人`/`escapeHtml`。
- PRD：10 处替换落地；旧逻辑 `leaderSelectModal/selectedLeaders/showLeaderSelect/选择组长(作为动作)` 均无残留（仅剩描述性"无需选择组长 / 原组长选择弹窗已取消"）。
- 角色称谓"施工组长""各组长"属角色名，非组长选择逻辑，保留正确。
- 临时脚本已清理。

---

## 修复：owner-miniapp/architecture.html 操作项全部无法点击

**现象**：业主端项目架构页所有操作项（项目部/工作组点开收起、邀请成员、添加工作组、退出项目部、查看合同、创建合同、删除、添加施工组）点击无反应。

**根因（之前任务遗留）**：
1. 第 1371 行 `escapeHtml` 函数行尾 `...}[c]);});}` 多了一个 `)`（`)` 与 `}` 错位），导致整个 `<script>` 解析失败 → 所有 `onclick` 调用的函数全部未定义。这是"全部点不动"的直接原因。
2. 之前复制 `insertCreatedNode` 到业主端时，漏了它依赖的"创建合同"整套基础设施：仅在第 1553 行调用 `showCreateContractModal('${jsName}')`，但该函数在业主端**未定义**，且对应的 `createContractModal` 弹窗 HTML / `createContract` / `closeCreateContractModal` 均缺失。

**修复**：
- 修正 `escapeHtml` 行尾为 `...}[c];});}`（去掉多余 `)`）。复验 `node --check` → SYNTAX_OK。
- 在 `contractModal` 之后插入 `createContractModal` 弹窗 HTML（字段：所属架构层级/合同类型*/合同名称*/甲方/乙方/合同金额 + 提示），id 与 `contractTargetName/contractType/contractName/contractPartyA/contractPartyB/contractAmount` 对齐服务端。
- 在 `goToContract` 之后补齐 `let currentContractTarget`、`let __contractModalBound` 及 `showCreateContractModal`/`closeCreateContractModal`/`createContract`（与服务端一致；`createContract` 确认后跳 `contract-detail.html?id=` + 新建 contractId，沿用业主端既有约定）。
- 复用既有 `showAppModal`（权限/已存在合同/必填校验）与 `groupContracts`/`hasCreatePermission`（均已存在）。

**校验**：`node --check` SYNTAX_OK；8 个操作函数（toggleNode/showInviteMemberModal/showCreateModal/showExitProjectModal/viewContract/showCreateContractModal/showDeleteModal/showExitGroupModal）全部定义就位；`createContractModal` HTML 与调用接线正确。临时脚本已清理。

---

## 项目详情页：对账单权限演示控制（服务端）

需求：服务端项目详情页点击"对账单"，新增"演示控制"效果（仿 create-project / message 演示控制形式）——"是否有全部权限"默认"否"，点击气泡提示无权限（文案已优化），切换"是"可进入对账单列表页；不影响其他已有功能/描述。

### service-miniapp/project-detail.html
- `<head>` 注入自包含 `.demo-control-panel` / `.segmented-control` / `.status-text.granted|denied` CSS（位置/样式对齐 create-project / message 演示控制面板，不依赖 common.css 既有规则）。
- `page-nav` 前插入演示控制面板：标题"对账单演示控制"、标签"是否有全部权限"、分段 [否|是]（默认"否" active）、状态文案"否：无权限查看对账单"（denied 红底）。
- 内联脚本：`window.statementDemoEnabled = true; window.hasFullPermission = false;`，并提供 `setStatementPermission(hasAll)` 切换段高亮与状态文案（是→granted 绿底"是：可查看对账单"）。

### service-miniapp/js/project-detail.js
- `handleQuickNavClick` 拦截：`href === 'statement-list.html' && window.statementDemoEnabled && !window.hasFullPermission` → `showCustomToast('您暂无查看对账单的权限\n对账单仅向拥有全部数据权限的人员开放\n可在右侧「演示控制」中开启后重试')` 并返回（不导航）；否则正常 `location.href`。
- 用 `window.statementDemoEnabled` 守卫：仅本页设 true，确保 project-detail-completed.html（同加载本 JS、对账单→statement-list.html）与 project-detail-new.html（→statement-list-empty.html）保持原导航、零回归。

### 校验
- project-detail.html：panel + CSS + 内联 `statementDemoEnabled/hasFullPermission/setStatementPermission` 均就位；`窗口` 默认 否。
- project-detail.js：拦截逻辑 line 770 就位，守卫有效。
- 兄弟页 grep：`statementDemoEnabled/hasFullPermission` 全局均为 undefined（无注入），导航行为不变。
- 复用既有 `custom-toast-modal` + `showCustomToast`（\n→<br> 多行提示），未新增弹窗组件。PRD 按"不影响其他描述"原则未改动（用户未要求更新文档）。

### 微调：无权限改为轻气泡（非弹窗）
用户要求：无全部权限点击对账单仅"气泡轻提示"即可、无需弹窗，提示内容精简为「您暂无查看对账单的权限」，其他不改动。
- project-detail.js：`handleQuickNavClick` 中不再调用 `showCustomToast`（原多行文案），改为调用新增 `showStatementTip(item)`——动态创建 `#statementTipBubble`（class `light-bubble`），定位到被点 对账单 元素上方居中（`getBoundingClientRect`），1.8s 后自动消失；复用 `window.__stmtTipTimer` 防抖。
- project-detail.html：内联 `<style>` 新增 `.light-bubble` / `.light-bubble.show`（fixed 定位、半透明黑底白字、淡入淡出、pointer-events:none、z-index:10000）。
- 默认"否"、演示控制面板、"是"→进入列表 等均保持不变。

---

## 创建合同页面（service-miniapp/create-contract.html）：类型改下拉 + 甲乙方/金额选填

需求：合同类型原以 8 类单选卡片全部展示占空间大，改为原下拉框形式（用户体验更友好）；甲方、乙方、合同金额改为非必选项；不影响其他已有功能。

### 改动（仅 create-contract.html）
- **合同类型**：`#typeOptions`（radio 卡片 + `.type-option` CSS）整体替换为 `<select id="contractType">` 下拉（复用 `.form-select` 样式），`renderTypeOptions()` 改为向 select 填充 8 个 `<option>`（与 PRD 8 类一致）；`showError/clearError` 中 `type` 字段目标元素由 `typeOptions` 改为 `contractType`。
- **甲方/乙方**：移除 `*` 必填标记；占位改为「请选择甲方（选填）」「请选择乙方（选填）」；不再做"必选"校验，仅在**两者都填写且为同一人**时报错（"甲方与乙方不能相同"）。
- **合同金额**：移除 `*` 必填标记；占位加「（选填）」；仅当填写时才跑 `validateAmount`（正数、最多两位小数、上限 1e12）格式校验，空值不报错。
- 组装跳转 `contract-detail.html` 时，甲乙方/金额空值安全传空（partyAName/partyBName 按是否选择取值，避免取 placeholder 的 data-name 为 null）。
- `node --check` 校验内联脚本 SYNTAX_OK；grep 确认 `typeOptions`/`type-option`/`createContractModal`/`showCreateContractModal` 零残留。

### 说明
- 合同类型、合同名称仍为**必填**（用户仅放宽甲乙方与金额）。
- 架构页入口（`architecture.html` 的 `goCreateContract`）与 `contract-detail.html` 详情页解析参数均未改动，零回归。
- PRD 文档本次未改动（用户本次未要求更新文档；如需把"甲乙方/金额选填"同步进 PRD-项目模块 6.6 可另议）。

### 追加：移除"需前往电脑端"提示
- 用户确认小程序无需到电脑端创建/编辑合同，移除 `create-contract.html` 内 `.create-notice` 提示块（"合同创建后状态为「拟定中」，需前往电脑端编辑详细内容并提交确认。"）及其 CSS（`.create-notice` / `.create-notice .notice-icon`）。
- 保留 JS 注释"拟定中合同详情页"（指向草稿详情页，非 PC 要求、非用户可见，未动）。

### 追加：甲乙方改为可搜索选择 + 取消发包方/承包方 + 去业主家人角色
- 标签：`甲方（发包方）`/`乙方（承包方）` → 纯 `甲方`/`乙方`。
- 甲乙方选择由原生 `<select>` 替换为**自定义可搜索选择框**（`.member-picker`）：点击 control 展开面板，顶部搜索框按**姓名或角色关键字模糊匹配**（indexOf，大小写无关），无匹配显示"未找到匹配人员"；点击空白关闭；选中后回填"姓名（角色）"。
- 新增 JS：`pickerState`（partyA/partyB 的 open/value）、`initMemberPicker`/`renderMemberPicker`/`toggleMemberPicker`/`closeAllPickers`/`filterMembers`/`selectMember` + 全局点击空白关闭监听；`showError/clearError` 对 partyA/partyB 改为定位 `partyAControl`/`partyBControl` 加 `.error`；提交时 partyA/partyB 取 `pickerState` 值，name 由 `PROJECT_MEMBERS` 按 id 反查。
- 人员列表去除 `业主家人` 角色（该角色不存在）：删除 `m-owner-family`「赵家人 / 业主家人」条目；现有业主仅 `陈业主 / 业主`。
- `node --check` SYNTAX_OK；grep 确认 `partyASelect|partyBSelect|renderMemberSelect|业主家人|m-owner-family|发包方|承包方|typeOptions` 全部为 0。
- 架构页入口 `goCreateContract` 与 `contract-detail.html` 解析未改动，零回归。

### 追加：同步 PRD-项目模块详细规格.html 6.6 创建合同描述
代码已实现但 PRD 6.6 描述过时，按实现同步（仅改 6.6 节 + 6.9 交互行，不影响其他章节）：
- 6.6.1：标题"创建合同弹窗"→"创建合同**页面**"；"弹窗类型"→"页面类型：独立页面 create-contract.html（顶部导航返回 + 底部提交栏）"。
- 6.6.2 表单字段：
  - 合同类型：原 5 类（水电/泥瓦/木工/油漆工程合同+其他）改为与《PRD-完整文档》一致的 **8 类**（基础施工/设计/拆除班组/水电班组/木作班组/泥瓦工班组/油漆工班组/小零工 服务合同），单选下拉，必填。
  - 合同名称：补校验规则（必填、2-30 字符、禁特殊字符）。
  - 甲方/乙方：由"下拉选择"改为"**可搜索选择框（member-picker）**"，选填，支持姓名/角色关键字模糊匹配；去"发包方/承包方"称谓；甲方说明删除"项目经理/业主/业主代表"身份限制、注明"业主端仅业主、无业主家人角色"；乙方注明两者同人报错"甲方与乙方不能相同"。
  - 合同金额：选填，补"填了才校验：正数、最多两位小数、≤1e12"。
- 6.6.3：删除"需前往电脑端编辑"提示，改为"小程序端即可创建与编辑" + 校验失败气泡轻提示示例。
- 6.6.4：取消按钮→"返回"（顶部导航）；创建合同行为去掉"弹出确认弹窗"，改为校验→不通过气泡提示→通过携带内容跳转拟定中详情页；样式 modal-btn confirm→submit-btn。
- 6.6.5：删除"引导前往PC端"，改为"可在小程序内继续编辑"。
- 6.9 创建合同交互行："打开创建合同弹窗→填写→确认"→"打开创建合同页面→填写→校验→跳转合同详情页（拟定中）"。
- 校验：grep 确认 6.6 内"需前往电脑端(正向)/弹出确认弹窗(创建合同行)/create-modal(创建合同行)/旧5类合同类型/项目经理业主业主代表/各组长"均清零；其余"弹出确认弹窗/create-modal"残留均属其他功能（退出/添加工作组/施工组/附件/删除），不在本次范围。临时 .bak 已清理。

---

## PRD 创建合同描述补充：合同详情页跳转加可点击链接

需求：类似"跳转合同详情页（拟定中）"的描述，增加对应链接，点击可到页面对应地址页。

### 改动（仅 PRD-项目模块详细规格.html）
将文档中"跳转/点击 … 合同详情页"类描述性短语改为可点击超链接（`<a href="..." target="_blank">`），按所属端映射：
- **服务方（service-miniapp/contract-detail.html）**：3.8/3.9 项目详情页相关（1293/1315/1331/1491）、6.6.4 创建合同按钮（2433「拟定中」示例/2477）、6.6.5 已命名文件（2440）、6.10 合同列表（2576）、6.9 交互（2582「拟定中」）、11 项目动态（4264/4270），共 11 处。
- **业主端（owner-miniapp/contract-detail.html）**：12.4 动态项（4630）、12 合同项（4641）、12.9（4919），共 3 处。

### 校验
- 按**行号精确替换**（因"跳转到对应的任务详情或合同详情页"在服务端 1331 与业主端 4630 文本完全相同，逐行处理避免错配）。
- 修复 4630 因 old 串未含前导 `<td>` 导致多出一个 `<td>` 的问题，复检 HTML 结构正常。
- grep 最终 14 个 `<a href=".../contract-detail.html">` 锚点就位（service 11 + owner 3），其余"合同详情页"叙述（如 2452 状态描述、2649 添加工作组弹窗等）不受影响。

---

## PRD 对账单列表页：补充金额汇总字段由来与计算公式

需求：对账单列表页需在文档内补充"金额汇总卡片"各字段（合同总额/已支付/待支付）的由来与计算公式，不影响其他功能/描述。

### 公式（用户提供，已与代码核对一致）
- **合同总额** = Σ(基础施工服务合同.amount) + Σ(设计服务合同.amount)（仅统计这两类；与 statement-list.html 合同筛选项一致）
- **已支付金额** = Σ(状态 = 已支付 的对账单.amount)
- **待支付** = 合同总额 − 已支付金额（推导值，非独立存储）

### 改动（仅 PRD-项目模块详细规格.html）
- 服务方 §9 新增 **9.7 金额汇总字段说明**（`id="sub-9-7"`，含 字段/数据来源/计算公式 三列表 + 说明 note），插入于 §9.6 之后、§10 注释之前。
- 业主端 §18 新增 **18.4 金额汇总字段说明**（`id="sub-18-4"`，同公式表），插入于 §18.3 之后、§17 注释之前。
- 未改动既有 9.1–9.6、18.1–18.3 及任何功能/接口描述；TOC 未动（原仅列 9.4–9.6，与新增 9.7 一致，属已有省略风格）。
- 校验：grep 确认 sub-9-7 / sub-18-4 各 1 处、公式表就位；插入锚点（§10/§17 注释）唯一，HTML 闭合正常。

---

## 新增：二维码 / 微信好友邀请「接收侧」落地演示页

需求：PRD 7.5 中"二维码邀请 / 微信好友"两条描述（扫码/点卡片 → 自动跳项目详情页 → 弹加入确认 → 7天失效）此前**无演示页面**。用户确认方案后实施（零侵入 + 真实跳转 + 入口放消息页演示面板下方）。

### 新增文件（2 个，未改任何既有页面逻辑）
- `service-miniapp/invite-join.html`、`owner-miniapp/invite-join.html`
- 结构：手机框内 = 项目详情**简化静态态**（来源提示条 + 项目 hero + 项目信息/架构或服务方卡片），未处理邀请前 `.page-content.dimmed` 弱化；覆盖 `app-modal` 加入确认弹窗（来源 / 项目 / 邀请人 / 加入身份 / 架构层级 / 默认权限 + 拒绝·确认加入）。
- 行为：确认加入 → Toast"已加入项目" + 成功卡片 + **「去项目详情页」真实跳转** `project-detail.html`（service 端选"业主及家人"时跳 `../owner-miniapp/project-detail.html`）；拒绝 → Toast"已拒绝" + 未加入占位卡；失效 → 改弹 ⏰"邀请已失效"（7天）+ 单按钮"我知道了" → 失效占位卡。
- 演示控制面板（手机框外右侧，同成员页分段控件）：**邀请来源**[二维码|微信好友]、**邀请身份**（service: 服务人员|业主及家人；owner: 业主|业主家人）、**邀请状态**[有效|已失效]。支持 URL 参数 `?source=&identity=&state=` 直达。
- 全部复用 common.css（phone-frame / nav-bar / card / app-modal / app-toast / page-nav），仅页内 `<style>` 自包含新类。

### 入口（消息页，增量）
- `service-miniapp/message.html`：在既有"接受邀请演示控制"面板内、状态文案下方追加 `.demo-extra-nav` 区块 + 两个导航项（`invite-join.html?source=qrcode` / `?source=wechat`）；同步补 `.demo-extra-nav/.extra-nav-title/.extra-nav-item` CSS。
- `owner-miniapp/message.html`：原本**没有**演示面板，新增同款 `.demo-control-panel`（标题"接受邀请演示控制"）仅承载这两个导航项 + 对应 CSS。

### PRD-项目模块详细规格.html（纯增量，未改 7.5 原有两条描述）
- 7.5 在"被邀请人接收与处理邀请"列表后新增 **「接收侧演示页说明」**：8 行规格表（原型页路径 / 入口位置 / 落地页表现 / 加入确认弹窗 / 确认加入 / 拒绝 / 邀请失效 / 二维码与微信好友差异）+ 演示控制面板 4 条说明（含"正式产品由邀请参数解析，需在小程序后台配置扫码与分享落地路径"）。
- 业主端 16.2 功能说明追加 1 条，引用 7.5 接收侧演示页说明。

### 校验
- 4 个页面内联脚本 `new vm.Script` 语法全部 OK。
- 入口链接 4 处就位，两个新页面文件存在；跳转目标 `detailHref` 双端映射正确。
- 既有 `project-detail.html` / `member.html` / `architecture.html` / `index.html` 均**未改动**。

---

## 邀请接收侧演示页调整：认证角色控制 + 去加入身份 + 权限口径对齐

需求（两端 invite-join.html，二维码/微信好友入口同页）：
1. 演示面板新增「当前接收人已认证角色」（单角色/多角色）；多角色时确认加入后先弹"选择加入角色"（文案同消息页）选一个已有角色；确认加入后**直接真实跳转**项目详情页（不再展示成功卡片，resultSuccess 已删）。
2. 加入确认弹窗**不展示"加入身份"行**（业主侧仅业主角色、无业主家人）；默认权限口径对齐"邀请成员"页：服务方=「仅基础公开数据，无治理权限」、业主方=「全部权限」。

### 关键实现
- **service-miniapp/invite-join.html**：身份分段"业主及家人"→"业主"；IDENTITY_CONF 精简为 perm/crossApp/detailHref；新增 demoRoleMode + RECEIVER_ROLES=['水电工','木工','项目经理']（与消息页一致）+ setDemoRoleMode/openRoleSelectModal/selectJoinRole/joinAndGoDetail；acceptInvite 分支：service+multi→选角色，否则直跳（600ms Toast 后 location.href=detailHref）；身份=业主时角色控制项 `.control-group.disabled` 置灰（业主无服务角色选择场景）；roleSelectModal 取消→重新弹回加入确认。crossAppTip 文案改"不进入施工组，不参与任务派发与合同流转"。URL 参数新增 `role=single|multi`。
- **owner-miniapp/invite-join.html**：移除"业主/业主家人"身份分段与 IDENTITY_CONF（身份固定业主，权限固定"全部权限"）；同款角色控制 + 选择弹窗，RECEIVER_ROLES=['业主','水电工','木工']（演示：既是业主也认证了服务角色）；跳转固定 project-detail.html。
- 两端均复用消息页 `.role-select-list/.role-select-item` CSS；状态文字 single=蓝底/multi=紫底（同消息页配色）。
- **PRD-项目模块详细规格.html**：接收侧演示页说明表——"加入确认弹窗"行改为不展示加入身份+权限口径；"确认加入"拆为单角色/多角色两行（直跳/先选角色再跳）；面板 bullet 更新（邀请身份仅服务端、业主时角色控制置灰；新增"当前接收人已认证角色"条，对应 users.certified_role_count）；业主端 16.2 引用同步。

### 校验
- 两端 JS 语法 OK；resultSuccess/joinIdentity/joinLevelRow/setDemoIdentity/业主家人 等旧引用全部清零。

---

## PRD 对账单列表页：§9 重构（金额汇总→数据展示规则 + 列表排序规则 + 重新编号 + 更新目录）

需求：对账单列表页，"金额汇总字段说明"改标题为"数据展示规则"（与文档 2.2 规范格式一致），新增"列表排序规则项"，并将原 §9.2~§9.6 顺延为 §9.3~§9.7，新 §9.2 插入到 §9.1 之后，同步更新目录；不改其他功能/描述。

### 改动（仅 PRD-项目模块详细规格.html）
- 将原独立 §9.7「金额汇总字段说明」（之前加的，曾因插在 §9 的 div 外而结构略偏）抽取，重命名为 **§9.2 数据展示规则**（`id="sub-9-2"`），插入到 §9.1 概述之后、原 §9.2 页面结构之前（纳入 §9 的 section div 内，结构恢复正常）。
- §9.2 数据展示规则包含两项：①**金额汇总字段说明**（合同总额/已支付/待支付 的字段·数据来源·计算公式 三列表 + 说明 note，公式不变）；②**列表排序规则**（排序维度/规则 两列表：默认按提交时间倒序、筛选后仍按时间倒序、排序不影响金额汇总聚合）。
- 原 §9.2~§9.6 顺延：页面结构→§9.3、筛选条件→§9.4、对账单类型→§9.5、对账单状态流转→§9.6、新增对账单表单→§9.7（id 与 subsection-title 同步）。
- **目录（TOC）更新**：服务方 §9 目录改为 9.1~9.7 全列（9.2 数据展示规则），锚点对齐。
- **业主端 §18 同步**：原 §18.4「金额汇总字段说明」→「数据展示规则」（`id="sub-18-4"` 不变），同样补「列表排序规则」表；业主端 TOC 原缺 18.4 条目，本次补上 18.4 数据展示规则（其余 18.1~18.3 未动）。
- 内部交叉引用：§9.2 正文"详见 9.3 筛选条件"→改为"详见 9.4 筛选条件"（筛选现为 9.4）。

### 校验
- 脚本分步执行（抽取/顺延/插入/目录/业主），过程中踩坑并修复：① TOC href 初未顺延（文本进了但锚点留旧），② 业主端 `</table>` 与 `<p>` 缺换行粘连，③ 内容 subsection-title 初未顺延（仅 id 与 TOC 进）。最终用修正脚本补齐，确认 **§9 id 与 title 完全对齐 9.1~9.7、TOC 9.1~9.7 锚点对齐、业主 §18 id/title/TOC 一致**。
- 注：§9.2「数据展示规则」内仍保留加粗小标题「金额汇总字段说明」作为子项（用户"将X改为Y"理解为节标题改名，子项保留以区分公式与排序）。如用户希望彻底去掉该子标题，改为纯"数据展示规则"下直接挂两表，可一行调整。
- 备注：文档全局 `<div>` 计数 664 vs 665 差 1，经局部结构核对为我改动区域均平衡，疑为文档原有（改动前后结构一致），非本次引入。

---

## 微调：service-miniapp/invite-join.html 跨端警示条文案精简
- crossAppTip 文案由"该身份将进入业主端小程序，不进入施工组，不参与任务派发与合同流转"改为"该身份将进入**业主端小程序**"（仅删后半句）。
- PRD 3152 行本就只写"将进入业主端小程序"警示条，无需改；owner 端 284 行"业主身份不进入施工组…"属另一文案，用户未要求，保留。

---

## 微调：邀请失效"我知道了"跳转小程序首页
- 两端 `invite-join.html` 的 `closeExpired()`：Toast 由"邀请已失效"改为"邀请已失效，正在返回首页..."，并 `setTimeout(() => { location.href = 'home.html'; }, 600)`（与 joinAndGoDetail 同款 600ms 延迟写法）；失效占位卡赋值逻辑保留（跳转前短暂可见），其他分支（确认加入/拒绝/来源/角色）未动。
- PRD-项目模块详细规格.html 3141 行"邀请失效（超 7 天）"行描述同步：关闭后跳转至小程序首页 `home.html`。
- 两端内联脚本语法校验 OK。

---

## 修复：对账单列表项点击无法进入详情页（代码）

- 现象：服务方 `service-miniapp/statement-list.html` 每张卡片 `onclick="goToDetail(...)"`，但 `js/statement-list.js` **未定义该函数**（业主端 `owner-miniapp/js/statement-list.js:129` 已有同名函数）。点击列表项无任何反应。
- 修复：在 `service-miniapp/js/statement-list.js` 的 `filterStatements()` 之后新增 `goToDetail(status, type, role)`，拼 `statement-detail.html?status=&type=[&role=]` 并 `window.location.href` 跳转（与业主端实现一致）。
- 校验：`node --check` SYNTAX_OK；`grep -c "function goToDetail"` = 1。业主端原本可用，未改动。
- 结论：此前未改过该页面代码，此缺失为既有缺陷，本次补齐。

---

## PRD §9.7 新增对账单功能（文档补全）

需求：PRD-项目模块详细规格.html 仅"9.7 新增对账单表单"不够详细，需按"创建项目/创建合同"格式补全，与已实现功能对齐。

### 改动（仅 PRD 文档，纯增量）
- `sub-9-7` 标题由"新增对账单表单"→**"9.7 新增对账单功能"**，并在其内新增 6 个子节（嵌套 subsection-title 小标题，与 6.6.x 一致）：
  - **9.7.1 表单字段规格**：5 列表（字段名称/是否必填/控件类型/校验规则/错误提示）——关联合同(必填,选择器,请选择关联合同)、确认人(只读)、账单类型(必填,胶囊单选,请选择账单类型)、本次金额(必填,¥输入支持负数,validation.js amountNegative)、用途描述(必填,≤200字)。
  - **9.7.2 操作流程**：8 步 code-block。
  - **9.7.3 交互规则**：抽屉/确认人自动带出/胶囊单选/金额格式化/字数统计/无独立确认弹窗。
  - **9.7.4 提交校验与确认流程**：表格(触发时机/处理逻辑/UI表现)，按实际代码描述校验不通过→Toast 错误、通过→Toast"提交成功，已通知对方确认"+关闭抽屉+1.5s 刷新；并注明当前版本**无独立二次确认弹窗**（与创建项目 5.5 不同），备注可后续补充。
  - **9.7.5 创建成功后跳转规则**：不跳详情页、停留列表并 1.5s 刷新、回顶部、业主端同步。
  - **9.7.6 创建成功后系统生成的信息**：对账单记录/初始状态/通知/操作记录/金额汇总重聚合。
- TOC（§9）9.7 改为"新增对账单功能"并补 9.7.1–9.7.6 子项。
### 校验
- div 结构平衡（§9.7 块闭合正常，§10 标题完好）；grep 确认 9.7.1–9.7.6 与 TOC 6 子项就位，旧"新增对账单表单"清零。
- 未改动其他章节、接口、功能描述。
