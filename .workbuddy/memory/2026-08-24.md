# 2026-08-24 工作日志

## 原型导航补充
- `service-miniapp/project-detail-ongoing-v2.html` 右侧原型导航新增「项目详情（工人）」项（链接 `project-detail-worker.html`），插入在「项目详情（进行中-新版）」之后，与项目详情系列页面分组相邻。
- 仅改该页导航，未触及其他页面/功能。

## 业主端合同详情（owner-miniapp/contract-detail.*）改造
- 合同类型 / 合同名称 统一改为「基础施工服务合同」（info-row 335/339、全文合同名称 1257、正文前言「就基础施工服务事宜」、签约文件名 671/686/1538）；JS `updateNewContractDisplay` 仅在 URL 带 `typeName` 时才覆盖类型，默认不覆盖，静态文案生效。
- 合同正文下方新增「补充条款」卡片（contentSection 内，合同正文卡片之后）；contentSection 仅切换 display 不被重写，卡片安全。
- 阶段任务项（stagesSection 共 14 个 `.task-item`）点击行为改造：原全部 `location.href='task-detail.html'` → `onTaskItemClick(this)`；新增 `onTaskItemClick`/`openTaskReadonly`/`parseTaskMeta`/`setText` 及 `TASK_EXEC_STD` 映射（业主端任务名：材料采购/运输/确认、开槽、布管、穿线、阶段确认、开关插座安装、灯具安装、新增点位布线/验收、清理现场、最终确认）。
  - 仅 `state.currentStatus==='signed'`（已签约）跳转 `task-detail.html`；其余状态弹「任务详情」只读弹窗（复用现有 `taskDetailModal`，三标准必有值，执行人/确认人空显「未指定」）。
- 校验：JS `node --check` 通过；HTML div 平衡 0；14 处 onclick 替换完成、0 残留 location.href。

## 业主端变更态"重复"分析（仅分析，未改页面）
- 用户疑 `owner-miniapp` 导航中「变更中(待确认方)」(`change_confirming`) 与「变更确认中(待确认方)」(`change_confirming_receiver`) 重复，且认为后者更符合需求流程。
- 分析结论：**重复感成立（两态动作/出口/横幅文案完全相同，仅差"平台审核已通过"一句）**，但"后者更符合流程"不成立。
- 依据：PRD §5.15 末尾权威线性流程（行 4957）确认方仅确认一次，确认态即 `change_confirming`（待确认变更）；`change_confirming_receiver` 未进权威流程；且 PRD 内部矛盾——§5.15.2路径B/§5.15.3 规定"平台审核通过→change_confirming"，§5.15.6 又规定"平台审核通过→change_confirming_receiver"。冗余/冲突根源在此。
- 修订方向（待确认）：① 循权威流程→合并为 `change_confirming` 单态、删 `change_confirming_receiver`，并修 PRD §5.15.6 与 §5.15.2/§5.15.3 矛盾；② 若产品刻意"双确认"设计，则显式声明并理顺业主端导航命名/顺序。当前不影响既有功能（仅分析）。

## 业主端变更导航重构（采纳方案②的变体）
- 用户决策：删 `变更中(待确认方)`（`change_confirming`）导航项及对应页面；保留 `变更确认中(待确认方)`（`change_confirming_receiver`，平台审核通过后业主确认），并补一个缺失的 `变更审核驳回（平台）`（`change_platform_rejected`）页面，置于 `变更审核中` 下方。
- 新业主变更阶段顺序（分组计数 4）：变更中(发起方)→**变更审核中**→**变更审核驳回（平台）**[新增]→变更确认中(待确认方)→变更签约中。逻辑：平台审核后分流——通过→业主确认(`change_confirming_receiver`)，驳回→`change_platform_rejected`。
- `owner-miniapp/contract-detail.html` 导航：删 `change_confirming` 项（原 1776），在 `change_platform_reviewing` 后插入 `<div onclick="updateContractStatus('change_platform_rejected')">变更审核驳回（平台）</div>`。
- `owner-miniapp/js/contract-detail.js`：
  - 修复 `change_platform_rejected` 状态配置（原为错误 `signed` 占位：text='已签约'/bannerClass='signed'/isChangeFlow=false/showRejectReason=false/actions=[]）→ 改为 text='变更审核驳回'、desc='平台已驳回本次变更申请…（V1版本）'、bannerClass='rejected'、isChangeFlow=true、showRejectReason=true、rejectReason='变更内容不符合平台规范…'、actions=[{text:'重新发起变更', action:'resubmit_change'}]（接既有 `resubmit_change` 弹窗与 `actions['重新发起变更']` 处理器→`change_platform_reviewing`）。
  - `statusTextMap['change_platform_rejected']` 改为 `'变更审核驳回（平台）'` 以匹配导航文字、保证高亮命中（`item.textContent===statusTextMap[status]`）。
  - 注意：`change_confirming` 的 STATUS_CONFIG/versionData/statusTextMap 条目保留（仅移除导航入口，使其不可达，不破坏其他引用）；其 `action` 值须用 `actionTexts` 键（如 `resubmit_change`）而非中文，否则弹窗不显示。
- 校验：JS `node --check` 通过；HTML div 平衡 0；live 文件 `change_confirming` 导航 0 引用、`change_platform_rejected` 导航 1 引用。

## PRD 同步（把近期原型改动补进 PRD-合同模块详细规格.html）
- **§3.4 任务卡片交互规格**：原「点击任务 → 跳转到任务详情页」单行，改为分派规则——已签约态跳转任务详情页；非已签约态（非变更流）弹「任务详情（只读）」弹窗（展示任务名/执行人/确认人/执行标准/确认标准/担责标准，空人员显「未指定」）；并加注该规则同时适用于服务方（基础/设计合同）与业主端合同详情页。同步修正 §3.x 末尾交叉引用（行 4242）。
- **§5.15 变更状态页矛盾修正**（用户决策：平台审核通过→`change_confirming_receiver` 单次确认；`change_confirming` 仅保留「仅阶段任务变更、不经平台审核」确认态）：
  - §5.15.4 标题/目录/原型链接「变更审核驳回（发起方）」统一为「**变更审核驳回（平台）**」。
  - §5.15.2 删除"路径 B（平台审核→change_confirming）"，改为仅阶段任务变更路径，并加红字注：平台审核变更确认方在 §5.15.6 完成最终确认；关键交互加「业主端导航不再单列 change_confirming 项」说明（对应业主端已删该项）。
  - §5.15.3 状态流转：平台审核通过 → `change_confirming_receiver`（§5.15.6）。
  - §5.15.5 进入条件：兼容「change_confirming（阶段任务变更）/ change_confirming_receiver（平台审核）」两路径。
  - §5.15.6 重写与 §5.15.2 的分工说明（本态必经平台审核、5.15.2 不经，二者不再重叠）。
  - §6.3.3 待办生成（行 3465/3467/3474）：平台审核通过→`change_confirming_receiver`、标签/流转同步修正（此前文字写 change_confirming 但 proto-link 指向 change_confirming_receiver，自相矛盾）。
- 未动行 4957（工人合同去平台化状态机，第九章，正确使用 change_confirming）。
- 校验：PRD 标签平衡（div/table/tr/td/ul/li/p/span 差值均 0）。

## PRD §5.15 视角/端展示结构化（采纳推荐方案①）
- 用户确认按推荐方案执行：在 §5.15 开头补「本章范围与视角约定」全局声明 + 「视角↔角色↔原型页」映射表 + 「端展示统一说明」；子节 proto-link 按归属端补 owner-miniapp；删除 §5.15.2 子节内零散"业主端导航说明"（已并入统一说明）。
- 新增内容位置：§5.15 intro 段（原 2934）之后、§5.15-lock 之前插入蓝色 note 块（border-color:#185FA5）。
- 映射表 8 行（5.15.1–5.15.8）：归属端=服务方端（1/4/5/7）、业主端（2/6）、双方（3/8）；对应 proto-link 标注 service-miniapp / owner-miniapp。
- 子节标题 proto-link 补齐 owner-miniapp（共 4 处）：5.15.2 `?status=change_confirming`、5.15.3 `?status=change_platform_reviewing`、5.15.6 `?status=change_confirming_receiver`、5.15.8 `?status=change_confirmed`；业主端导航不再单列 change_confirming 的说明移至统一说明，避免子节散落。
- 全局声明明确：本章仅描述非工人合同（含平台审核）变更；工人合同（去平台审核）变更见第九章、差异见 7.1 对比框，本章不再逐节赘述区别（呼应此前"跳跃凌乱"问题）。
- 任务92（收敛工人合同通用说明）：经核查 §5.15 中工人合同引用仅在 §5.15-lock（已集中一处）与 §5.15.8 合理交叉引用（"见 §9.5.5"），非散落区别说明；lock 已是单一权威处，无需额外删除。
- 校验：PRD 标签平衡（div/table/tr/td/ul/li/p/span/tbody/thead 差值均 0）；owner-miniapp 链接新增 4 处、"业主端导航说明"残留 0、全局声明 1 处、sec-7-1 与 worker-contract-change 引用正确。

## 工人合同详情（新流程）变更阶段导航调整
- 删 `变更中(发起方)`（`changing`）导航项：`service-miniapp/worker-contract-detail.html`「变更阶段」分组移除该 status-switch-item；分组计数 6→5（仅移除预览入口，未删 `changing` 状态机配置）。
- `变更已驳回(受邀方驳回)` → `变更已驳回(发起方)`：该改名此前已在 HTML 原型生效；本次在 PRD §9.7 导航表（原行 4834）同步改名，并修正文案。
- PRD 同步：§9.7「变更阶段」分组计数 6→5、删「变更中(发起方)」行（`changing`→9.5.1）、保留 §9.5.1 章节（因 `changing` 为真实工作流态——`worker-contract-change.html` 提交后 `goDetail('changing')`，删状态会破坏发起变更流程，故按"不影响其他已有功能"保留状态机）；同时删除第九章标题 badge 中指向 `?status=changing` 的「变更中(发起方)」直链。
- 关键判断：`changing`（发起方发起、等待受邀方确认）与 `change_confirming_sender`（受邀方已确认、发起方上传签约文件）是**前后相邻、不同**的态，并非同一态；按用户"同一发起人视角"产品口径，仅移除冗余预览入口，保留 `changing` 状态机与 §9.5.1 文档。若用户确要彻底合并（提交变更直接进 `change_confirming_sender`），属行为变更，需另改 `worker-contract-change.html` 的 `goDetail` 目标并调整该态横幅文案，待确认。
- 校验：HTML 与 PRD 标签平衡均 0；HTML 不再含「变更中(发起方)」「变更已驳回(受邀方驳回)」「计数 6」；PRD §9.7 不再含「变更中(发起方)」「变更已驳回(受邀方驳回)」「计数 6」（§5.15.1 非工人合同同名态未受影响）。

## 工人合同详情（新流程）变更阶段新增受邀方视角页
- 需求：变更阶段缺「变更已驳回（受邀方）」「变更签约中（受邀方）」两个受邀方视角页，需新增导航项及对应页面。
- 实现：`worker-contract-detail.html`「变更阶段」分组在各自发起方项后新增两项——`变更已驳回（受邀方）`（`change_rejected` + `data-viewer="receiver"` + `WCP.updateStatus('change_rejected','receiver')`）、`变更签约中（受邀方）`（`change_signing_wait` + `data-viewer="receiver"` + `WCP.updateStatus('change_signing_wait','receiver')`）；分组计数 5→7。
- 机制：原型 `updateStatus(status, viewer)` 支持显式传入 viewer；导航项 `data-viewer` 用于高亮命中（`updateStatusNavActive` 比对 `dv===state.viewer`）；受邀方视角由既有代码统一渲染（关键条款/项目地址/隐藏非 change_confirming 的邀约列表等）。
- `worker-contract-detail.js`：`renderActions` 受邀方过滤新增 `upload_change_sign`（变更签约中受邀方不应显示发起方"上传变更签约文件"按钮），仅影响受邀方视角、不影响发起方与其他视图。
- PRD 同步：§9.7 导航表新增两行（状态标注 `viewer=receiver`，指向 §9.5.3 / §9.5.5），计数 5→7；第九章标题 badge 补两条 `?viewer=receiver&status=` 直链。
- 校验：JS `node --check` 通过；HTML 与 PRD 标签平衡均 0。

## 工人合同详情（新流程）受邀方变更态体验修复
- 问题：新增「变更已驳回（受邀方）」「变更签约中（受邀方）」导航项后，点击受邀方项时**发起方项与受邀方项同时高亮**；受邀方视角文案错误（如「对方（乙方）已驳回…」是乙方自己驳回的）；底部不应展示「返回已签约」；变更摘要卡片底部不应展示演示数据提示。
- 修复：
  - `service-miniapp/worker-contract-detail.html`：给 `变更已驳回(发起方)` 与 `变更签约中(发起方)` 增加 `data-viewer="sender"`，使高亮命中同时比较 status 与 viewer，避免 receiver 态点亮 sender 项。
  - `service-miniapp/worker-contract-detail.js`：
    - `STATUS_CONFIG.change_rejected` 增加 `descReceiver: '您已驳回本次变更申请，合同保持原已签约状态。'`；移除 actions 中的「返回已签约」（`change_rejected_back`），仅保留发起方「重新发起变更」；受邀方过滤已去掉 `start_change`，故受邀方无底部操作。
    - `STATUS_CONFIG.change_signing_wait` 增加 `descReceiver: '变更已确认，等待发起方上传线下已签约的合同变更文件，上传后变更正式生效（生成 V2 版本）。'`。
    - `renderChangeHighlight` 删除 `cp.demo` 提示行「（演示数据：在「已签约」页点击「发起变更」可发起真实变更并查看实际高亮）」。
  - `PRD-合同模块详细规格.html`：
    - §9.5.3 标题改为「变更已驳回（发起方 / 受邀方）」，增加受邀方原型页链接，横幅说明文字分发起方/受邀方，底部操作去除「返回已签约」，状态流转仅保留「重新发起变更」。
    - §9.5.5 标题改为「变更签约中（发起方 / 受邀方）」，增加受邀方原型页链接，横幅说明文字分发起方/受邀方，底部操作区分发起方上传 / 受邀方无操作。
    - §9.3 变更流程状态机总览表同步更新 `change_signing_wait` 与 `change_rejected` 的视角、说明、主要操作。
    - §7.3 视角推导规则注释更新：说明 `change_rejected` / `change_signing_wait` 默认发起方视角，同时提供 `?viewer=receiver` 原型入口。
    - TOC 9.5.3 / 9.5.5 标题同步；§9.7 导航表「变更签约中」改为「变更签约中(发起方)」以与导航文案一致。
- 校验：JS `node --check` 通过；HTML/PRD 标签平衡均 0；HTML 不再含「变更已驳回(受邀方驳回)」；JS 中「返回已签约」仅残留于 `ACTION_TEXT`/`applyChangeAction` 死代码（已从 `change_rejected` actions 数组移除，不再触发）。

## 工人合同「变更确认中（发起方）」语义与文案修正
- 问题：原型 `change_confirming_sender` 横幅说明「对方已确认变更，请上传变更签约文件」与角色/状态不符——该态位于乙方确认**之前**（发起方提交变更提案、等待乙方确认），上行 `changing` 已被合并移除，故本态即"发起方提交后等待乙方确认"。
- `service-miniapp/worker-contract-detail.js`：`STATUS_CONFIG.change_confirming_sender.desc` 改为「变更申请已提交，等待对方（乙方）确认变更内容（阶段任务已暂停流转）。」（仅改文案，未改状态机/流程/动作）。
- `PRD-合同模块详细规格.html` 同步：
  - §9.5.4 横幅说明、触发条件（改为"发起方提交变更提案进入本态，非对方已确认"）、页面结构（"等待乙方确认"而非"提示上传"）、状态流转（乙方确认→`change_signing_wait` 而非→上传）、关键逻辑（去掉"对方已同意…仅差上传"的误述，改为"与待确认变更为同一进度两端"）。
- 校验：JS `node --check` 通过；HTML/PRD 标签平衡均 0。

## 工人合同变更态摘要卡片状态标签文案修正
- 问题：变更摘要卡片（`changeHighlightCard`）原对所有非驳回变更态统一显示「变更进行中（待上传签约文件生效）」，但 `changing`/`change_confirming`（受邀方待确认）/`change_confirming_sender`（变更确认中·发起方）均位于乙方确认**之前**，尚不能上传签约文件，文案不匹配。仅 `change_signing_wait`（变更签约中）才是"对方已确认、待上传签约文件生效"。
- `service-miniapp/worker-contract-detail.js`：`renderChangeHighlight` 第 1304-1313 行按 `state.status` 分态输出：
  - `change_rejected` → 「变更已驳回（乙方）」（红）
  - `change_signing_wait` → 「变更进行中（待上传签约文件生效）」（绿）
  - 其余变更态 → 「变更进行中（等待对方确认）」（橙，新增 `ch-state confirming` 类）
- `PRD-合同模块详细规格.html` 同步：
  - §9.5.1 变更视图标签描述细化，列出三态标签规则（等待对方确认 / 待上传签约文件生效 / 已驳回）。
  - §9.5.2（待确认变更·受邀方）页面结构补「变更进行中（等待对方确认）」标签说明。
  - §9.5.4（变更确认中·发起方）页面结构补「变更进行中（等待对方确认）」标签说明。
- 校验：JS `node --check` 通过；HTML/PRD 标签平衡均 0。
  - §9.3 状态机总览表 `change_confirming_sender` 说明改为"发起方已提交变更提案，等待对方（乙方）确认变更内容"。
  - §7.5.2 变更阶段状态表同述同步。
  - §9.5.1 变更中状态流转（乙方确认→`change_confirming_sender` 改为→`change_signing_wait`）、§9.5.2 待确认变更状态流转（同改）。
- 校验：JS `node --check` 通过；HTML/PRD 标签平衡均 0。

## 合规版（基础/设计合同）合同详情变更态修复与补充
- 问题1：变更审核驳回（`change_platform_rejected`）页丢失变更原因/变更摘要展示。根因：`service-miniapp/js/contract-detail.js` 的 `updateChangeDisplay` 中 `changeStates` 数组（约 773 行）未含 `change_platform_rejected`，走 else 分支全隐藏。
- 修复1：`changeStates` 加入 `change_platform_rejected`（并加入新态 `change_confirming_rejected`）；该态属非 stageOnly 分支，正常显示变更原因横幅 + 变更摘要卡片。
- 问题2/需求：`变更中(发起方)`（`changing`）与`变更审核中`（`change_platform_reviewing`）同视角统一 → 保留"变更审核中"。
- 实施2：`service-miniapp/contract-detail.html` 变更阶段分组删除「变更中(发起方)」导航项；`changing` 状态配置保留（仅阶段任务变更真实工作流态仍用），分组计数保持 5（删1加1）。
- 需求3：补充"变更确认人驳回"状态（确认方/业主驳回）导航项及页面。
- 实施3：新增状态 key `change_confirming_rejected`（确认方驳回，区别于 `change_platform_rejected` 平台驳回）。
  - `contract-detail.js`：STATUS_CONFIG 加 `change_confirming_rejected`（横幅"变更确认人驳回"、rejected 样式、showRejectReason、actions 重新发起变更）；statusTextMap、stepOrder（=4）、guideConfig、版本/timeline 详细数据（含"变更确认方驳回" error 节点）均补。
  - `contract-detail.html`：变更阶段分组新增「变更确认人驳回」导航项（`updateContractStatus('change_confirming_rejected')`），计数保持 5。
  - `PRD-合同模块详细规格.html`：TOC 加 5.15.9；§5.15 视角映射表加 5.15.9 行；新增 §5.15.9 小节（含进入条件/页面结构/操作/流转/与5.15.4平台驳回区分）；§5.15.6 状态流转"驳回→`change_confirming_rejected`"（原写退回 signed，改为新态）。
- 修正"变更确认人驳回"语境指向：基础施工合同甲方=业主、乙方=服务方；该态是甲方（业主）作为确认方驳回乙方（服务方）变更，原"变更确认方（乙方/业主）"语义颠倒。
  - `contract-detail.js` 第 262 行 desc：`乙方/业主`→`甲方/业主`。
  - `PRD-合同模块详细规格.html` §5.15.9：横幅说明文字（3261）、驳回原因区域（3265）、与5.15.4区分说明（3277）三处 `确认方（业主/乙方）`→`确认方（甲方/业主）`。
  - 校验：JS `node --check` 通过；PRD 标签平衡 0；PRD 内已无"乙方/业主"误指向残留。其余出现"乙方"处（签约上传、待乙方确认等）属乙方=服务方正确语境，不改。
- 校验：JS `node --check` 通过；HTML/PRD 标签平衡均 0。
- 注意：`reject_change_flow` 真实流程去向（confirmAction 中'驳回变更'仍 `updateContractStatus('signed')`）未改动，仅新增预览态；如需真实驳回跳转 `change_confirming_rejected` 需另改 confirmAction（本次未做，遵守"不影响其他已有功能"）。

