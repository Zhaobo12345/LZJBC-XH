# 2026-07-31 工作日志

## 对账单详情页 PRD 补充（按已实现代码）
- 依据 `service-miniapp/statement-detail.html + js/statement-detail.js`、`owner-miniapp/statement-detail.html + js/statement-detail.js`、两端 `css/statement-detail.css`、`shared/validation.js`，向 `PRD-项目模块详细规格.html` 纯增量补充：
  - 服务方新增 **10.5 状态展示规则**（状态横幅配色/图标/文案表、四状态分区显隐矩阵、操作记录时间线规则、通用字段展示规则）与 **10.6 交互流程规格**（传支付凭证抽屉全流程、图片增删/预览、提交/取消、关联合同跳转、URL 演示参数）。
  - 业主端新增 **19.5 状态展示规则**（横幅表——注意业主端已驳回为灰色渐变 #8C8C8C→#BFBFBF、服务端为红色；显隐矩阵含底部操作栏与"等待服务方上传线下支付凭证"提示条；操作记录以"我"展示；19.5.4 底部操作栏可见性=待确认且 role=confirmer）与 **19.6 交互流程规格**（确认弹窗流程、驳回抽屉流程 200 字上限/必填校验、其他交互、URL 参数 role=confirmer|creator）。
  - 修正 10.4 与代码矛盾处：图片格式 JPG/PNG→jpg/jpeg/png/webp，单张 ≤5MB→≤10MB（对齐 20.3 字段规则与 validation.js）。
- 校验：新增区域 table/div 标签平衡；全局差 1 的 `</table>`+`</div>` 位于 6892-6893 行（"已移至3.8节"注释残片），为文档历史遗留，非本次引入，未处理。
- 代码侧零改动（两端 HTML/JS/CSS 均未动）。
- 备注：服务端 js/statement-detail.js 内含 confirm/reject 相关函数但服务端 HTML 无对应 DOM（死代码，不影响功能），PRD 未描述该部分。

## 清理服务端 statement-detail.js 死代码
- 删除 `service-miniapp/js/statement-detail.js` 中无 DOM 支撑的确认/驳回逻辑：`showConfirmModal` / `hideConfirmModal` / `submitConfirm` / `showRejectDrawer` / `hideRejectDrawer` / `submitReject` / `confirmStatement`，以及未被引用的常量 `isConfirmer`、`currentUserRole`。文件 302 → 244 行。
- 确认前提：服务端 statement-detail.html 无 confirmModal / rejectDrawer / rejectReasonInput 等 DOM，也无任何调用；`statement-list.js`、`contract-detail.js`、`task-detail.js` 中的同名函数为各自文件独立作用域，未受影响。
- 保留：`previewAttachment`（预留）、凭证上传全链路、`showToast`、`addLogItem` 等在用逻辑。
- 校验：`node --check` 通过；全文件已无上述标识符残留。
- 遗留（未动）：删除 submitReject 后 `service-miniapp/statement-detail.html` 第 181 行 `<script src="../shared/validation.js">` 在该页已无调用方；validation.js 无自执行副作用，保留无害，如需可后续移除。

## 新增服务方项目详情（进行中-新版）页面
- 新建独立页面 `service-miniapp/project-detail-ongoing-v2.html`，零改动其他文件：
  - 顶部项目基本信息按参考图片重排：左侧「新家已开工 <span style="color:#FA8C16">60</span> 天」，右侧「75.0m² 2室1厅」圆角标签。
  - 下方展示「架构、成员、资料库、对账单」：架构区为独立模块，最大高度 90px（高度减半），内容可滚动兼容；成员/资料库/对账单为快捷入口三列卡片。
  - 「合同概览」「任务统计」采用紧凑样式（padding/字号/条项尺寸压缩，高度减半）。
  - 「今日动态」置于「待办事项」上方，待办默认展示 3 条。
  - 页面完全自包含（内联样式与脚本），复用 `common.css` + `css/project-detail.css`，不影响 `project-detail.html`、`project-detail.js` 及其他已有页面/PRD。
- 校验：HTML 结构 `<div>` 开闭平衡（137/137），`<html>` / `<body>` 标签平衡；`page-nav.js` 语法检查通过。

## 修订：项目详情（进行中-新版）页面（对齐"项目详情进行中"样式）
- 用户反馈首版"新建了一套独立视觉风格，与项目详情（进行中）页面不一致"。改为**严格复用 `project-detail.html` 的结构与 `project-detail.css` 的现有类**（`.card`/`.quick-nav`/`.level-selector`/`.contract-summary`/`.task-stats`/`.todo-content`/`.activity-list`），仅做位置与高度调整，并完全自包含（不引入 `project-detail.js`，自写轻量内联脚本，避免任何跨页副作用）。
- 调整点：① 顶部 `.project-basic-header`（左「新家已开工 60 天」、右「75.0m² 2室1厅」，参考图片）；② `.level-selector.arch-half` 架构选择区域高度减半；③ `.card.contract-half` 合同概览高度减半；④ `.card.task-half` 任务统计高度减半；⑤ 待办事项卡片移至「今日动态」卡片之后，仅渲染 3 条（保留 `id="todoContent"`、`.task-tag` 等原有结构）。
- 校验：`<div>` 开闭平衡（184/184），`<html>`/`<body>`/`<script>` 平衡；需求落点（基本信息头、架构减半、合同减半、任务减半、待办在动态后、3 条待办、快捷入口含架构/成员/资料/对账单）全部 OK。其他文件零改动。

## project-detail-ongoing-v2.html 二次调整（卡片顺序 + 高度再压缩）
- 仅改动 `service-miniapp/project-detail-ongoing-v2.html` 一个文件，其他页面/JS/CSS/PRD 零改动。
- 卡片顺序调整为：项目基本信息 → 快捷入口 → 架构选择区（arch-half）→ **待办事项（3 条）** → 合同概览（contract-half）→ 任务统计（task-half）→ 今日动态。
- 高度再压缩手法（内联 <style> 覆盖，不改 css/project-detail.css）：
  - `.card.contract-half`：padding 6px 8px、card-title margin-bottom 0、`.contract-stat` 由列改行（flex-direction:row + baseline 对齐）、`.contract-list.show` 加 max-height:88px + overflow-y:auto。
  - `.card.task-half`：padding 6px 8px、`.task-stat-item` 由列改行（icon 与文字横排、icon 20px）、value 12px、label 9px、margin 归零。
- 校验：div 184/184 平衡，html/body/script/style 标签平衡，顺序断言（待办<合同<任务<动态）全部 True，待办项恰 3 条。
