# 2026-09-23 工作日志

## 服务方小程序「原型导航」整体梳理与补全
- 背景：用户指出各页右侧原型导航存在不一致/缺失（例：'任务详情' 与 '任务详情（非工人）' 导航项都指向 task-detail.html，但该页自身缺 '任务详情（水电）' 项）。
- 审计方法：脚本提取 service-miniapp 全部 30 个页面 `.page-nav` 容器内导航项（href+文本+active），生成矩阵 `.workbuddy/_tmp/nav_audit.txt`。
- 结论与处理（前提：不改原导航项标签、不乱对应页面）：
  - **任务族双向补全**（已改）：`task-detail.html` 自引「任务详情」后补「任务详情（水电）」→ task-detail-shuidian.html；`task-detail-shuidian.html` 自引「任务详情（水电）」后补「任务详情（非工人）」→ task-detail.html。改前均 `cp` 备份 `.workbuddy/_tmp/*.nav-family.bak`；2 文件 HTML BALANCED 校验通过。
  - 确认 7 个任务相关页（contract-detail/create-project/statement-list/task-list/task-report + 2 个 v2 项目页）已同时含「非工人」+「水电」两项，无需改。
- 其余观察（未改，因涉及改动原导航项标签/去重，需用户另行确认）：
  - contract-detail.html 同时存在「合同详情」(→self) 与「合同详情（非工人）」(→self,active) 同页双标签；且「项目主页（进行中）」重复 ×2（预存）。
  - 2 个 v2 项目页仍以旧标签「任务详情」指向 task-detail.html（与 5 个任务页「任务详情（非工人）」标签不等）。
  - worker-contract-detail.html 导航仅含工人合同状态子集（缺变更/撤回/已拒绝/抢单失败等项，其余工人页齐），属 base 详情页既有设计。
- 规则沉淀：同文件多 Edit 须串行（历史曾因并行写竞争静默丢失编辑）；导航项补充仅在原 `<a class="page-nav-item active">自引</a>` 后追加，不触原项。

## 三类不一致修复（用户确认实施 ①+②，不含 ③）
- ① contract-detail.html：删冗余非 active `合同详情`(node QWPT5Z5ANNg8gU0ayUnUIG) 与重复第二个 `项目主页（进行中）`(node bQjfgCUDlkS0AZHQEi1RzR)，仅留 active `合同详情（非工人）`；改前备份 `.workbuddy/_tmp/contract-detail.html.navfix.bak`。
- ② v2 项目页：project-detail-initial-v2 / project-detail-completed-v2 的 `任务详情`→`任务详情（非工人）`（href 均保持 task-detail.html 不变）；备份 `.workbuddy/_tmp/*-v2.html.navfix.bak`。
- ③ 未做（用户未选）：worker-contract-detail.html 补 8 个工人合同状态项维持现状。
- 校验：3 文件 HTML BALANCED；复验确认合同详情页去重/统一、v2 两页标签已统一为「（非工人）」，与 5 个任务页一致。

## 导航项「项目主页（进行中）」去重（第二轮）
- 用户指出：worker-contract-detail.html（工人合同详情新流程）+ task-detail.html + task-detail-shuidian.html 各存在 2 个「项目主页（进行中）」(→ project-list.html)，需去重。
- 处理：每页删除第二处重复项（保留第一处，href/逻辑不变）。
  - worker-contract-detail.html：删 node i9koHEpqQrGLI8q1KO17fw，留 eJ6osN6o0wnClY86rzDGjV。
  - task-detail.html：删 node Wkc5mCgEXBE3HHrUbTq7KD，留 bcohFb2zF1t3nBAHAvqT8C（单行拼接，无缩进，按 node-id 精确删）。
  - task-detail-shuidian.html：删 node CSqWjaKPTlY6jNh7KHTJJ8，留 9B69aKEAP0MdIGos0KTBG2。
  - 改前 3 页均 `cp` 备份 `.workbuddy/_tmp/*.nav-dedup.bak`。
- 校验：3 文件 HTML BALANCED；复验每页「项目主页（进行中）」=1。

## contract-detail.html 合同全文（查看全文）内容替换+排版
- 用户要求将 `fullTextModal`(.fulltext-content) 内容改为新合同全文（良造家/亿大造平台「全透明服务模式」合同），并整理排版/格式/序号/样式。
- 做法：Python 脚本按平衡 div 扫描，整体替换 `.fulltext-content` 内文（剔除原基础施工合同 7 章+签署信息），保留外层 div 与 data-page-node-id。新结构复用 `.contract-article/.article-title/.article-content/<p>`，新增 CSS 类 `.clause-subtitle`(子标题加粗)/`.clause-item`(悬挂缩进16px)/`.clause-sub2`(二级子项32px)。
- 章节：引言 + 支付约定 + 三、甲乙双方责权利 + 五~十二（质量验收/变更/安全/违约/保修/道德自律/生效终止/平台服务费），共 11 个 contract-article、82 条 clause-item、20 条 clause-sub2。①②③ 子项用 clause-sub2 缩进；同条内联 ①②③ 保留原样。
- 序号整理：支付 (1)(2)(3) 统一为（1）（2）（3）（4）；其余沿用用户提供编号（三、五~十二 原样，未臆造一/二/四）。填空处多空格在 HTML 会折叠（未做下划线占位）。
- 备份：contract-detail.html.fulltext.bak + contract-detail.css.fulltext.bak。校验：HTML BALANCED；CSS 672/672。
- 注：替换脚本 `.workbuddy/_tmp/replace_fulltext.py` 可复跑（改前已备份）。

## contract-detail.html 合同正文预览区与查看全文对齐（部分内容展示）
- 用户指出页面「合同正文」下方展示区（3 处）内容与「查看全文」新合同不一致，要求按查看全文全部内容更新为「部分内容」，详细经查看全文，且不改布局样式。
- 3 处合同正文展示区：
  1. 编辑态 `#contractTextPreview`（原 494-498 行，p+text-title 结构）→ 引言首段 + 合同主要章节目录(一~十一)。
  2. 非编辑态 `contentSection .text-content`（原 677-685，br 拼接）→ 同上；保留 change-highlight-text 变更高亮 span 不动。
  3. 变更态 `#changeContractContentPreview`（原 1224-1237，content-preview + 展开全文 toggle）→ 同上；保留「展开全文」toggle。
- 三处统一为：引言首段（全透明服务模式）+「合同主要章节」+ 一~十一 章标题（部分内容/概览），旧「一、工程概况/水电工程分包/合同总金额/计划开工日期」残留全部清零。
- 不改：布局样式、查看全文链接(=2)、展开全文 toggle(=1)、各 data-page-node-id（仅改文案）。
- 备份：contract-detail.html.preview.bak。校验：HTML BALANCED；旧内容残留=0；合同主要章节=3。

## contract-detail.html 阶段任务示例数据 → 基础施工服务合同（§14 乙方服务内容/任务明细）
- 来源：`D:/接收/大良造/施工项目合同/2026/202609/1基础施工服务合同.docx` §14。7 阶段 / 28 任务。列映射：任务内容→任务名称、执行流程→执行标准、执行标准→确认标准、履约和担责→担责标准。
- 三区域同步替换（仅改示例数据，未动布局/逻辑/交互/执行人/确认人/状态/时间）：
  - Region A 草稿 `#editStageList`→`.stage-card`(7)；Region B 变更编辑 `#stageEditContainer`→`.stage-edit-item`(7)；Region C 只读 `#stagesSection`→`.stage-item`(7，含隐藏演示 `newStageItem`「新增点位安装」)。
  - 每区域 28 任务均携带 `data-exec-standard`/`data-confirm-standard`/`data-liable-standard`（供任务详情弹窗读取）。
- 修复：原 `#stagesSection` 顺序 材料进场→布管布线→安装→newStageItem→收尾阶段，替换区间止于 newStageItem 漏掉尾部「收尾阶段」孤儿块（水电合同残留，5 任务），已用 div 深度计数精准移除，使可见阶段=7、与「阶段任务 (7个阶段)」标签一致。
- 文案：阶段计数 `4个阶段`→`7个阶段`；模板说明 `水电工程标准阶段`→`基础施工标准阶段`。
- 演示语义保留：阶段1 已完成、阶段2 含「确认中」+「已修改」(`id=modifiedTaskItem`)；隐藏 `newStageItem`「新增点位安装」保留为「添加阶段」演示。
- 未改动（属其他功能、非阶段任务示例数据）：「变更记录」demo 文本（布管布线阶段新增任务 / 安装阶段任务金额调整 / 新增"收尾阶段" 等）仍保留原水电合同示例。
- 校验通过：HTML 标签平衡；3 个 `<style>` 大括号均平衡；28 任务×3 区域标准属性齐；7 阶段名齐；旧水电阶段名无残留。
- 备份：`.workbuddy/_tmp/contract-detail.html.stagetasks.bak`、`.workbuddy/_tmp/contract-detail.js.stagetasks.bak`；生成脚本/中间数据 `gen_stage_tasks.py`、`stage_tasks_clean.json`。

## 紧急修复：contract-detail.html 预览空白（内容/导航丢失）
- 现象：交付后用户反馈页面内容与导航整页空白。
- 排查：静态结构其实完整——html5lib（浏览器同款解析）确认 `nav-bar` 位于 `body>div.phone-frame>div.nav-bar`、7 个 stage-card/stage-item/stage-edit-item 均在位、全标签严格平衡（BALANCED）、7 阶段 28 任务与标签齐全。
- 根因：文件历来在 `</body>` 前缺失最外层 `phone-frame`/`page-content` 两个闭合 `</div>`（备份即如此，真实浏览器自动补全故以往能渲染；但内置预览若用严格解析器会整体渲染失败→整页空白）。
- 修复：在 `</body>` 前补 `</div></div>`（close page-content、phone-frame），文档转为严格合法。html5lib 复验 nav 在场、7/7/7。
- 教训（可复用）：校验原型页必须用 html5lib 做浏览器同款解析，而非仅 html.parser 的宽松平衡；本仓库原型 HTML 惯有「末两 div 未闭合」写法，交付前若预览异常优先补这两处闭合标签。

## 复核：预览空白二次确认（用真实浏览器实证）
- 二次 diff（backup→current）：全部 5 处 hunk 均落在预期范围（Region A 草稿 / Region C 只读 / Region B 变更编辑 / 孤儿「收尾阶段」块删除 / 末尾补 2 个 </div>），无任何越界意外编辑。
- 权威诊断：注释 <!--/--> 19/19、<style> 3/3、<script> 5/5、正文(剥 script/style/comment) div 开合 1380/1380 完全平衡；`nav-bar/phone-frame/page-content` 均在。
- JS 依赖核查：`contract-detail.js` 引用的 125 个 id 中，缺失的 11 个（actionMenu/qrCodeBox/...）均为 JS 动态生成元素，非本次改动引入；结构 id（editStageList/stagesSection/stageEditContainer 等）全在。
- **真实渲染实证**：用系统 Chrome 无头模式（`chrome.exe --headless --screenshot`，file:// 加载，无需 playwright）截图 → 状态栏/nav「合同详情」/状态横幅/合同状态流程卡均正常渲染，内容与导航在；注入 window.onerror 后 dump-dom 得 `ERRS##0##NONE`（**零 JS 错误**）。
- **本地静态服务器复核**：`python -m http.server 8912 --bind 127.0.0.1 --directory service-miniapp` → 页面 324902B、common.css 52800B 均 HTTP 200，`阶段任务 (7个阶段)` 在场。
- 结论：文件已健康（结构合法 / 零 JS 错误 / 真实渲染正常）。用户所见空白应为**预览缓存了此前损坏状态**，改由本地 HTTP 入口 + 硬刷新复核。
- 可复用手段：本环境有系统 Chrome `C:/Program Files/Google/Chrome/Application/chrome.exe`，可直接 `--headless --screenshot/--dump-dom` 做真实渲染与错误捕获（pip 装 playwright 会因依赖冲突 ResolutionImpossible 失败，勿再尝试）。

## contract-detail.html 演示数据切换为「基础施工服务合同 / 北京」+ 合同正文三态框高度统一
- 需求：页面为「基础施工服务合同」演示类型；「更换正文模板」弹窗的「当前合同：城市·类型」、模板名称/描述/标签、点击预览的名称与正文，全部改为基础施工服务合同（城市北京），模板正文参照「查看全文」；另：合同正文展示框在拟定中/变更中/只读三态高度统一且不过高。
- JS（`js/contract-detail.js`）改数据层（脚本 `.workbuddy/_tmp/apply_basic_tpl.py`，可复跑）：
  - `contractTemplates`：tpl_001「基础施工服务合同标准版（北京）」/ tpl_002「…简约版（北京）」，type=基础施工服务合同、city=北京，content 按「查看全文」一~十一章重写（标准版含 11 章；简约版为精简条款）。
  - `stageTemplates`：stage_tpl_001「基础施工标准阶段模板」(7 阶段/28 任务，与页面默认阶段一致) / stage_tpl_002「基础施工简约阶段模板」(4 阶段)。
  - 过滤条件 2 处 `tpl.city==='西安'&&tpl.type==='水电工程'` → `'北京'&&'基础施工服务合同'`（断言 count==2）。
- HTML：`templateFilterCity/Type`、`stageTemplateFilterCity/Type` → 北京 / 基础施工服务合同；`contractTemplateInfo` 占位「已选择模板：标准水电服务合同」→「基础施工服务合同标准版（北京）」；合同附件「水电工程图纸.dwg」→「基础施工图纸.dwg」(2 处：显示名 + onclick 参数)。JS 不动态写这些筛选文案，故静态改即可。
- CSS：新增变量 `--contract-text-max-height: 120px`（:root），三态合同正文框统一：`.contract-text-preview`(原150px)、`.change-contract-content .content-preview`(原100px)、新增 `#readonlyContractText`(只读态 `.text-content`，原无上限→过高；加 id 精确作用避免波及 `#supplementaryTerms` 补充条款)。三者均 `max-height:var(--contract-text-max-height); overflow:hidden;` + 底部渐隐 `::after`（只读态需 position:relative）。`.expanded`(展开全文) 规则保持不动。
- 校验：`node --check` OK；HTML div 开合 1380/1380；CSS 大括号 674/674；无残留「水电工程」。真实浏览器（Chrome headless）实测：弹窗「当前合同 北京·基础施工服务合同」、2 个合同模板/2 个阶段模板名称描述标签正确、预览名称/类型/城市/正文(3589字) 正确、三态框 computed max-height 均 120px 且只读态实际渲染 120px、`window.__errs=[]`（零 JS 错误）。三态并用真实样式表做了并排渲染对比截图（120/120/120）。
- 未改：「变更记录」demo 文案(布管布线/收尾阶段等水电残留，属另一功能)。
- 备份：`.workbuddy/_tmp/{contract-detail.html,contract-detail.js,contract-detail.css}.basic-tpl.bak`。

### 可复用技巧（本仓库原型验证）
- **测试用临时 HTML 必须放在 `service-miniapp/` 内**（如 `service-miniapp/_t_test.html`），因为页面用相对路径引 `common.css`/`../common.js`/`js/contract-detail.js`；放 `.workbuddy/_tmp` 会导致 JS 不加载、全局函数缺失（表现为 `window.showXxx is not a function`）。用完即删。
- Chrome headless `--screenshot=` 必须给**绝对路径**，否则静默不产出文件；用 `375,812` 视口截手机框，但 `.phone-frame` 内部滚动，加高 window 也看不到屏外内容 → 需单独做「样式并排 harness」或注入脚本滚动内部容器。
- 注入 `window.addEventListener('load', ...)` 可编程点击弹窗（`window.showTemplatePicker()` 等已在文件末尾挂到 window）并用 `getComputedStyle`/`getBoundingClientRect` 实测，比静态解析更可信。

## 已签约态任务项 → 任务详情页 携带任务上下文（字段值统一）
- 需求：`contract-detail.html` 已签约态点任务项（如「组织全屋放线」）跳 `task-detail.html`，落地页要展示同一任务名称 / 隶属合同 / 隶属阶段 / 执行·确认·担责三项标准；其余功能操作交互不变。
- 口径（用户确认）：标准**按合同内原文整条展示**（保留 `→`，渲染成 1 条编号项）；隶属合同取页面「合同名称」= **基础施工合同0810**。
- 改动（仅 2 个 JS，未动任何 HTML/CSS）：
  - `js/contract-detail.js`：新增 `navigateToTaskDetail(taskItem, taskName)`；`openTaskReadonly()` 把「已签约态早退」移到任务名提取之后（`if(!taskItem)return` 提前），改为调 `navigateToTaskDetail`。URL 参数 `from=contract&taskName&contractName&stageName&execStandard&confirmStandard&liableStandard`；阶段取 `taskItem.closest('.stage-item').querySelector('.stage-name')`；合同名取 `#editContractName`（value=基础施工合同0810，回退同名常量）；三项标准取 `taskItem.dataset.*`。
  - `js/task-detail.js`：新增 `renderStandardFromContract(id,text)`（清空后单条渲染，空值→「未设置」占位，防串本页「穿线」默认数据）与 `applyTaskContextFromUrl()`（**仅当 `from=contract` 才生效**，否则完全不动，保证直接打开/其他入口零影响）；在 `DOMContentLoaded` 里于 `initFromUrl()` 之后调用。
- 注意：`#stagesSection` 内 30 个 `.task-item` 中有 2 个 `task-added`（变更新增示例）**无** `data-exec-standard/confirm-standard/liable-standard` → 按「未设置」展示，未去改合同页数据。
- 校验：`node --check` 双 JS OK。**真实浏览器端到端**（注入脚本 `demoSigned()`→`click()` 首个 `.task-item`，`--dump-dom` 跟随跳转）：落地 `taskName=组织全屋放线`、`stage=📍 基础施工合同0810 > 进场前准备阶段`、三标准与合同字段逐字一致、`window.onerror` 计数 0。回归：拟定中态点击仍弹只读编辑弹窗 `readonly show`、不跳转、`editTaskName=组织全屋放线`、零错误；无参数直接打开 `task-detail.html` 默认数据（穿线 / 水电分包合同 / 布管布线阶段）不变。截图 `.workbuddy/_tmp/shot_std.png` 目视确认三标准排版正常。
- 备份：`.workbuddy/_tmp/{contract-detail.js,task-detail.js}.taskctx.bak`。
- 备注：`task-detail.html` 的 div 开合 311/312 属**仓库既有**（HEAD 即如此，浏览器自动补全不影响渲染），非本次引入。

## 业主方 owner-miniapp/contract-detail.html 同步基础施工服务合同示例数据
- 需求：业主方合同详情（含各状态）内「合同正文 / 查看全文 / 阶段任务示例数据」同步为服务方基础施工服务合同口径，不动功能逻辑交互。
- 业主方只有**一个** `owner-miniapp/contract-detail.html`，所有状态由 `ContractDetailPage.updateContractStatus()` 切换 → 只需改页内共享区块。共 4 处：
  1. **合同正文** `.text-content`（`#contentSection`）：改为与服务方一致的引言段 + 「合同主要章节」一~十一章清单；保留 `#changeHighlightText` 变更高亮块（文案同为「新增水电点位安装工程」，服务方亦然，未动）。
  2. **查看全文** `#fullTextModal .fulltext-content`：保留业主方首个「合同编号 BJSDSWHT000001」卡片，其后 11 条 `contract-article` 整段替换为服务方全文（引言/支付/责权利/质量验收/变更/安全/违约/保修/道德自律/生效终止/平台服务费）。
  3. **只读阶段任务** `#stagesSection`：4 阶段 → **7 阶段 28 任务**；**收尾阶段在隐藏的 `#newStageItem` 之后**（易漏），需按深度平衡连同其后基础阶段一并替换；保留 `#newStageItem`、`#modifiedTaskItem`/`#modifiedTaskTag`（已修改标记落位到「拆除施工完工验收」）、`onclick="onTaskItemClick(this)"`。
  4. **变更阶段编辑** `#stageEditContainer`：4 阶段/12 任务 → 7 阶段/28 任务。
- JS（`owner-miniapp/js/contract-detail.js`）：`TASK_EXEC_STD` 键改为 28 项基础施工任务（值取服务方 data-exec-standard）+ 保留 `新增点位布线/新增点位验收`；`openTaskReadonly()` 改为 **data-\* 优先**（`taskItem.dataset.execStandard/confirmStandard/liableStandard`，缺失回落原映射/通用文案），与服务方口径一致。
- CSS（`owner-miniapp/css/contract-detail.css`）：新增 `.fulltext-modal .article-content .clause-subtitle/.clause-item/.clause-sub2`（服务方同款，原业主方缺这三条，全文分级会塌）；新增 `.text-content#readonlyContractText` 120px 上限 + 底部渐隐（与服务方 `--contract-text-max-height` 对齐，避免正文块过长）。
- 校验：`node --check` OK；HTML div **1080/1080**；CSS 大括号 501/501；非选中 `data-page-node-id` 0；`水电工程` 0。**结构对齐实证**：以备份为基准做 difflib，**每个 hunk 的 div 增减均为 0**，且 `stagesSection/stageEditContainer/changeConfirmModal/fullTextModal/taskDetailModal` 的**累积 div 深度与备份完全一致**。真实浏览器：7 基础阶段+新增点位安装、30 任务、全文 12 条（含合同编号）、82 个 clause-item、正文 max-height 120px、变更编辑 7/28、**零 JS 错误**；状态回归——变更确认中 `#newStageItem`=block、已修改标记=inline、变更高亮=inline；已签约 `#newStageItem`=none；拟定中阶段任务区=none；只读弹窗取到「组织全屋放线 + 合同真实三标准」。
- **本次踩坑（重要）**：用「结束标记 = 下一个兄弟/下一个已知元素」的方式替换区域会**吃掉容器的闭合 `</div>`**（本次 `#stageEditContainer`、以及 `#fullTextModal` 因 `rfind('</div>')` 取到弹窗自身闭合而多出一个）。两次都靠 **difflib 逐 hunk 校验 div 增减 + 关键锚点累积深度对比** 抓出来。**结论：原型区域替换必须按「容器深度平衡」只替换容器内部，并用 hunk 级 div 增量 + 锚点深度对账验收。**
- 未改（与服务方口径一致，属另一功能模块）：变更摘要 Demo 文案（`修改任务：穿线（修改执行标准）`、`安装阶段任务金额调整：开关插座安装`、`布管布线阶段新增任务：线路检测`）、只读任务弹窗默认值 `detailTaskName=材料采购`。
- 备份：`.workbuddy/_tmp/owner-{contract-detail.html,contract-detail.js,contract-detail.css}.sync.bak`；脚本 `.workbuddy/_tmp/sync_owner_{stages,text,js}.py`（可复跑）。

## 业主方 owner-miniapp 阶段任务点击跳转携带上下文（对齐服务方 Task3 口径）
- 需求：`owner-miniapp/contract-detail.html`「已签约（含已履约完成）」态点击阶段任务项跳 `owner-miniapp/task-detail.html` 时，落地页展示任务名称/隶属合同及阶段/执行·确认·担责三标准；其他状态与功能不变。
- 关键定位：owner 合同状态**无「已完成」**，只有 `signed`（已签约）；「已签约（已履约完成）」演示走 `demoFulfilled()`→`updateContractStatus('signed')`，即 `currentStatus` 仍为 `signed`。**故仅 `signed` 分支需带上下文**（`onTaskItemClick` 原判断不变）。
- 改动 3 文件：
  1. `js/contract-detail.js`：新增 `navigateToTaskDetail(taskItem, taskName)`（阶段取 `taskItem.closest('.stage-item').querySelector('.stage-name')`；合同名取 `#contractNameValue`，回退常量 `基础施工服务合同`；三标准取 `taskItem.dataset.*`；URL `from=contract&taskName&contractName&stageName&execStandard&confirmStandard&liableStandard`）；`onTaskItemClick` 的 `signed` 分支改为先取 `.task-name` 文本再 `navigateToTaskDetail`（原先直接 `location.href='task-detail.html'`）。
  2. `contract-detail.html`：合同信息行「合同名称」值 div 加 `id="contractNameValue"`（原仅 `data-page-node-id`，无稳定选择器）——该 div 现值为「基础施工服务合同」。
  3. `js/task-detail.js`：新增 `renderStandardFromContract(id,text)`（清空后单条渲染，空→「未设置」）与 `applyTaskContextFromUrl()`（**仅 `from=contract` 生效**，否则零影响）；在 IIFE 顶层 `initFromUrl()` 之后调用（函数声明提升，位置在调用之后无碍；owner 的 `initFromUrl` 是顶层直接调用，非 DOMContentLoaded 内）。
- 校验：`node --check` 双 JS OK；HTML `BALANCED`。**真实浏览器端到端**：加载 `contract-detail.html?status=signed` + 注入脚本点击「组织全屋放线」→`--dump-dom` 跟随跳转，落地页 `taskName=组织全屋放线`、`📍 基础施工服务合同 > 进场前准备阶段`、三标准逐字命中（放线前交底/按图纸尺寸放线/扣除500元）。JS 错误回归：`signed/draft/pending` 三态 `window.onerror` 均 0。截图 `.workbuddy/_tmp/owner_taskctx_landing.png` 目视确认。
- 与服务方差异：owner 合同名是静态展示 div（非 input），故加 `id` 读取而非 `#editContractName`。
- 备份：`.workbuddy/_tmp/owner-{contract-detail.html,contract-detail.js,task-detail.js}.taskctx.bak`；脚本 `.workbuddy/_tmp/{verify_owner_taskctx,verify_owner_nojerr,shot_owner_taskctx}.py`。

## 业主方 owner-miniapp 取消「上传签约文件」按钮及关联上传页
- 需求：`owner-miniapp/contract-detail.html?status=confirmed` 底部「上传签约文件」按钮，业主方无此需求/功能 → 取消该按钮及其操作（含上传签约文件关联页面），不影响其他页面与功能。
- 定位：底部按钮 = `confirmed` 状态的 `actions:[{text:'上传签约文件',action:'upload'}]`（`STATUS_CONFIG`），点击走 `showStatusModal('upload')`→`showSignUploadPage()` 打开 `#signUploadPage` 全屏上传浮层。**`#signUploadPage` 是唯一关联页**；变更流的 `upload_change_sign` 并不使用它（仅 toast + `updateContractStatus('signed')`），故删除不破坏变更流。
- 改动（HTML -49 行 / JS -100+3 / CSS -200）：
  1. **HTML**：删 `#signUploadPage` 整块（原 1920-1966 + 尾随空行）；删签约文件卡片 `#signFileUploadBtn`（`+ 上传`，`onclick=showSignUploadPage()`）。**保留** `#signConfirmModal`（确认/驳回签约复核弹窗，属另一功能）与流程时间轴里的「上传签约文件」历史记录（记录非操作）。
  2. **JS**：`confirmed` 的 `actions` 改 `[]`、desc 改为「双方已确认，签约文件由乙方上传后合同正式生效」；`updateActionGuide.confirmed` 同步文案；删 `showStatusModal` 的 `upload` 分支、`actionTexts.upload`；删 6 个上传函数（`showSignUploadPage/closeSignUploadPage/handleSignFileSelect/renderSignFileList/removeSignFile/submitSignFile`）及其 exposed-map / window 赋值；`updateSignFileCard` 去掉 `signFileUploadBtn`/`canUploadSignFileStates` 死逻辑（卡片显隐 `showSignFileStates` 不变，`confirmed` 本就不在列表→卡片本就不显）。
  3. **CSS**：删 `.sign-upload-page` 整块（含全部子选择器，原 1805-2003）。
- **要点**：`updateBottomActions` 在 `actions=[]` 时直接 `display:none` 清空——与既有 `readonly` 的 `signed` 态行为一致，非副作用；DOM 中无 `.bottom-dock`/预览合同按钮（仅遗留 CSS），故无连带损失。
- 校验：`node --check` OK（无残留符号引用）；HTML `BALANCED`；CSS 大括号 473/473；difflib 对账 hunk 全为预期删除。**真实浏览器**：`confirmed/signed/change_confirming` 三态 `#bottomActions` 无「上传签约文件」、`#signUploadPage` 不存在、`#signConfirmModal` 保留、JS 错误 0。截图 `.workbuddy/_tmp/owner_confirmed_no_upload.png`（已确认 banner 文案已更新、无上传按钮）。
- 备份：`.workbuddy/_tmp/owner-{contract-detail.html,contract-detail.js,contract-detail.css}.signupload.bak`；脚本 `.workbuddy/_tmp/{rm_signupload,rm_signupload_js,patch_signupload,verify_signupload_removed}.py`。

## 业主/服务方 project-detail 合同概览补充「基础施工服务合同」示例 + 服务方跳转路由（Msg12）
- 需求：业主小程序及服务方小程序「项目详情→合同概览→查看合同」的合同项列表缺少「基础施工服务合同」示例，需补充；服务方点击「基础施工服务合同」跳非工人合同详情页（contract-detail.html）、点击工人合同跳工人合同详情页（worker-contract-detail.html），不影响其他页面/功能。
- 路由口径（与既有一致）：非工人合同（基础施工服务合同）→ `contract-detail.html`（业主方带 `?status=signed` 落到已签约/已履约完成态）；六工种工人合同→ `worker-contract-detail.html`（WCP 合并页，无 URL status 参数）。
- 改动 3 文件：
  1. `service-miniapp/project-detail-completed-v2.html`（唯一 JS 渲染合同概览、含工人/非工人 role 切换）：
     - 8 条工人 `SHARED.contracts` 的 `detail` 由 `contract-detail.html?status=completed` 全改 `worker-contract-detail.html`（replace_all，仅改链接、不动其他字段）。
     - 新增 `baseContracts:[{name:'基础施工服务合同',meta:'基础施工 | 金额：88万',percent:100,badge:'已签约',badgeCls:'green',detail:'contract-detail.html'}]`（非工人视角专用，不进工人渲染）。
     - 非工人 `renderNonWorker` 改 `var contracts = SHARED.baseContracts.concat(SHARED.contracts).map(...)`，每项 `onclick="navigate('"+c.detail+"')"`；工人 `renderWorker` 不变（仅 `contracts`，正确排除基础施工服务合同）。
  2. `owner-miniapp/project-detail-ongoing-v2.html`（`#contractList` 静态列表，4 工人合同）：首位插入「基础施工服务合同」`data-href="contract-detail.html?status=signed"`（meta「基础施工 | 金额：88万 | 已签约」、100% 进度条、顶部分隔线），CRLF 还原保证 diff 最小（+13/-1）。
  3. `owner-miniapp/project-detail-completed-v2.html`（`#contractList show`，8 工人合同）：首位同样插入静态项（+10/0）。
- 正确置之不改：`*-initial-v2.html` 两页合同概览为空态「暂无合同」→ 不插入；`renderWorker` 工人视图保持仅工人合同。
- 校验：`node --check` 双 JS OK；HTML 三文件 `BALANCED`；CSS 大括号逐块平衡。**真实浏览器**：
  - 服务方非工人：`contractListNW` 渲染 9 项（基础施工服务合同首项 + 8 工人）；工人链接 8×=`navigate('worker-contract-detail.html')`、基础施工链接=`navigate('contract-detail.html')`。
  - 服务方工人（临时副本强制）：`baseContracts` 仅在 `<script>` 出现 2×，未渲染；工人链接 8× 正确排除。
  - 业主方 ongoing/completed：基础施工服务合同存在、`data-href="contract-detail.html?status=signed"`。
  - JS 错误：`data-jerr` 属性缺失 → 0 错误；临时文件已清理，miniapp 目录无残留 `_t_*.html`。
- 备份：`.workbuddy/_tmp/owner-pd-ongoing.bak`、`.workbuddy/_tmp/owner-pd-completed.bak`、`.workbuddy/_tmp/svc-pd-completed.bak`；diff -u 确认服务方=8 链接变更+baseContracts+concat，业主方=各单插入项。
## service-miniapp 项目主页/已完成详情页 合同概览补充基础施工服务合同 + 修复"点击不展示"（Msg13）
- 需求：service-miniapp/project-list.html（项目主页）合同概览缺基础施工服务合同；project-detail-completed-v2.html（项目详情-已完成-新版）合同概览"点击不展示"需修复，并同样补充基础施工服务合同。
- 排查：project-detail-completed-v2.html 非工人 contractListNW 折叠默认（CSS max-height:0，需 .show），但 toggleContractNW 仅被 onclick 调用（line 846）却**从未定义** → 点击"查看合同"无反应、合同永不可见。此为"点击不展示"根因。baseContracts+concat 已在先验任务加入（非工人 9 项渲染正常）。
- 改动 2 文件：
  1. `service-miniapp/project-detail-completed-v2.html`：新增 `toggleContractNW(el)` 函数定义（与 project-list.html 一致：toggle contractListNW 的 show 类 + 箭头 open）。原文件纯 LF（备份 0 CRLF），Edit 后仍是 LF，无需转换。**仅 +11 行函数定义**；baseContracts/concat 沿用先验。
  2. `service-miniapp/project-list.html`：SHARED 新增 baseContracts:[{name:'基础施工服务合同',meta:'基础施工 | 金额：88万',percent:100,badge:'已签约',badgeCls:'green',detail:'contract-detail.html'}]；非工人 renderNonWorker 的 SHARED.contracts.map 改 SHARED.baseContracts.concat(SHARED.contracts).map（非工人 3 项：基础施工+水电+泥瓦；工人视图 renderWorker 仅 SHARED.contracts.filter 不含 baseContracts，正确排除）。原文件 CRLF（1145 CRLF+1 bare-LF），从备份 python 重生成保留 CRLF，diff 仅 +baseContracts 块 + concat 行。
- 校验：HTML 两文件 BALANCED；**真实浏览器**：
  - project-list 非工人 DOM：基础施工服务合同存在、链接 navigate('contract-detail.html')（非工人详情）；水电/泥瓦链接各自 ?status=。
  - project-detail 注入测试：data-toggle=contract-list => contract-list show（点击展开生效！）、非工人 contractListNW 9 项含基础施工（data-nwbase=true）、工人 contractListWorker 8 项且 data-wbase=false（排除基础施工）；data-jerr 缺失 → 0 JS 错误。
- 备份：.workbuddy/_tmp/svc-pl-contract.bak、.workbuddy/_tmp/svc-pdc-contract.bak；验证 DOM 临时文件已清。
## 水电工人合同阶段任务示例数据按 docx 更新（worker-contract-detail）
- 需求：依据 docx「D://接收//大良造//施工项目合同//2026//202609//水电工人服务合同 - 阶段任务.docx」更新 service-miniapp/worker-contract-detail.html 工人合同详情页阶段任务示例数据。映射：阶段标题→阶段名称；工序→任务名称；工艺执行标准+存证标准→执行标准（示例「作业面清扫干净。<br/> 存证标准：每个空间拍摄不少于1张照片」）；验收标准→确认标准；违规责任/违约责任→担责标准。执行人/确认人/状态/时间不改。
- 数据位置（关键）：示例数据在 `worker-contract-detail.js` 的 `STAGE_TEMPLATES.shuidian.stages`（非 HTML）。渲染 `renderStagesSection` 读 task 字段 **execStd/confStd/liableStd**（不是 execStandard）写入 data-exec-standard 等属性；弹窗 `viewTaskDetail` 优先取 data 属性→`TASK_STANDARD_MAP[任务名]`→`GENERIC_STD`。
- 改动（仅 worker-contract-detail.js，纯 LF 无需转 CRLF）：
  1. `STAGE_TEMPLATES.shuidian.stages` 替换为 docx 六阶段：给水/排水/强弱电/配电箱安装/桥架铺设/面板安装，共 33 个任务；每任务补 `execStd/confStd/liableStd`，`exec:'张水电'`/`conf:'陈庄'` 保持原值。存证标准为「/」时执行标准只取工艺执行标准；否则 `{工艺执行标准}。\n存证标准：{存证标准}`。保留 docx 工序名的 ✅ 前缀（忠实原文）。
  2. `viewTaskDetail` 三个标准字段由 `textContent` 改为 `innerHTML = escapeHtml(x).replace(/\n/g,'<br/>')`——因 `.detail-value` 无 white-space:pre，原 textContent 会把 <br/> 当纯文本；改后换行正确渲染。
  3. **新增演示种子迁移**（必要）：`ensureDemoSeed` 对已存在种子只补 inviterName/partyAName 等，`ensureDraftFields` 仅在 `!c.stages` 时克隆模板 → 旧 localStorage 种子（key `lzj_worker_contracts_v1`）会残留旧四阶段（材料进场/布管布线/安装/收尾），用户看不到新数据。已在 `ensureDemoSeed` 内加定向迁移：仅当 d.type==='shuidian' 且现有 stages 全为旧四阶段名时才 clone 新模板覆盖，不影响用户编辑过的阶段/其他工种。
- 校验：`node --check` 通过；真实 Chrome 双场景复验——FRESH 首次加载：6 阶段/33 任务、弹窗执行标准含真实 <br> 换行、执行人张水电/确认人陈庄不变；STALE 旧种子：迁移后同样显示新 6 阶段（迁移前为旧 4 阶段）。其他类型（拆除 id=demo-demolition-example）不受影响，仍走 TASK_STANDARD_MAP 兜底。均 0 JS 错误。临时 _t_*.html 与 chrome profile 已清理。
- 备份：`.workbuddy/_tmp/wcd-js-shuidian.bak`。
## 工人合同「各状态页面」阶段任务示例数据全面替换为 docx 六阶段（Msg15）
- 需求：上一条只改了 `worker-contract-detail.js`（合并页数据源），但工人合同还有一批**各状态独立页面**各自硬编码了水电旧四阶段（材料进场/布管布线/安装/收尾），需按同一 docx 规则全部替换。
- **范围（7 个文件，均在 service-miniapp/）**：`worker-contract-change.html`（变更）、`worker-contract-draft-initial-new.html`（拟定中）、`worker-contract-confirming-initiator-new.html`（确认中-发起方）、`worker-contract-signed-initiator-new.html`（已签约-发起方）、`worker-contract-fulfilled-receiver.html`（已履约-受邀方）、`worker-contract-receive-confirmed-new.html`（已确认-受邀方）、`worker-contract-receive-inviting.html`（邀约中-受邀方）。
- **数据源 SSOT**：从 `worker-contract-detail.js` 的 `STAGE_TEMPLATES.shuidian.stages` 用 node eval 提取为 `.workbuddy/_tmp/wcstates/shuidian_stages.json`（6 阶段/33 任务/三标准齐全），再由 python 脚本按各文件缩进生成替换块，保证 7 页数据与合并页完全一致。
- **三类数据结构（关键）**：
  1. `STAGE_TEMPLATES.shuidian.stages`（change / draft-initial）：change 页原本 shuidian 无标准字段，本次补齐 execStd/confStd/liableStd。
  2. `var STAGES = [...]`（confirming / signed）：已是带标准的多行格式，整体替换。
  3. `var DEMO_STAGES` + `TASK_STD_MAP`（fulfilled / receive-confirmed / receive-inviting，受邀方三页）：DEMO_STAGES 任务补三标准字段（**必须**，因「清理基层/放线/开槽/布管固定」在多阶段同名但标准不同，仅靠任务名 Map 无法区分）；同时 TASK_STD_MAP 键更新为 26 个新工序名（旧键如 材料采购/开槽布管 已删除）。
- **弹窗换行渲染**：6 个页面三标准由 `textContent` 改 `innerHTML = fmtStd(...)`（escapeHtml/esc + \\n→&lt;br/&gt;），fmtStd 就近插入；拟定中页走 textarea（天然换行）未改。与 worker-contract-detail.js 口径一致。
- **踩坑**：① 这类 HTML 全是 CRLF，Edit 工具会转 LF → 必须用 python 脚本（newline='' 读写）替换，保 CRLF、diff 最小；② 替换脚本用「含『材料进场阶段』且含 `{ name: `」锚点 + count==1 断言，先 dry-run 再 apply；③ 弹窗三行替换要考虑缩进 → 改正则保留缩进；④ 验证选择器各页不同：确认中/已签约/受邀方用 `.stage-name`/`.task-item`，变更/拟定中页用 `.stage-name-input`/`.task-input`；⑤ **change.html 必须带 `?id=` 才 init**，否则 renderNotFound，直接 dump 无阶段。
- 校验：7 文件 HTML BALANCED；内联 JS `node --check` 全部 OK（脚本提取 `<script>` 合并校验）；真实 Chrome 逐页 dump——7 页均=6 阶段/33 任务，弹窗执行标准含真实 &lt;br&gt;（拟定中页 textarea 为真实换行）；worker-*.html 已无「材料进场阶段/布管布线阶段」残留；非水电工种模板（拆除/木作/泥瓦/油漆/小零工）未动；0 JS 错误。
- 备份：`.workbuddy/_tmp/wcstates/*.bak`（7 份）；脚本 extract.js / patch_stages.py / patch_modal2.py / check_js.py / verify.py / verify2.py；数据源 shuidian_stages.json。临时 DOM 与 chrome profile 已清。
