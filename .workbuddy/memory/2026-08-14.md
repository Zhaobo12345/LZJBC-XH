# 2026-08-14 工作日志

## PRD 英文清理（任务模块详细规格.html）
- 用户要求排查 PRD 内"很多英文"是否必要，无必要则删除。
- 删除/中文化（不必要）：
  - `Tab` → `选项卡`（全局 ≈15 处，含 code-block 原型图与待办筛选表头/交互流程）。
  - `Toast` → `轻提示`（§3.6 两处）。
  - `localStorage.lzj_owner_todos` → 去掉具体 key，仅保留机制名 `localStorage`（§7.9 说明）。
  - `data-status` 代码属性名 → `状态`（§2.3 过滤逻辑说明 + 待办筛选规则表 5 处；状态 key `completed/confirming/pending` 等仍保留）。
- 保留并说明原因（必要）：
  - 状态机/角色/结果 key：`(completed_with_reject)`、`(rejected_pending)`、`(confirming_after_reject)`、`(configurer)`、`confirmed / rejected` —— 项目 2026-08-10 约定（中文为主、英文 key 置于括号内），用于 PRD↔代码/数据库字段唯一对应与追溯，删则断链。
  - `accept`（§3.5 澄清结论主题锚点，HTML 标准属性名，单 token）。
  - `API`/`JSON` 及第九章「API接口汇总」整章 —— 接口规范天然英文，端点/字段名英文才能保证无歧义实现。
  - `task-detail.html?status=…`、`contract-detail.html?status=…` 等跳转 URL 片段 —— 项目约定：URL 片段保持英文。
  - `localStorage` 机制名（标准 Web 存储术语，与 accept 同类）。
- 校验：Grep `Tab|Toast|data-status|<code>状态</code>` 零残留；剩余英文均为上述必要类。
- 改动文件：`PRD-任务模块详细规格.html`。

## 工人合同「已确认·受邀方」取消上传签约文件功能
- 需求：受邀方不支持上传签约文件操作（V1 签约由发起方上传）。涉及两页面：工人合同详情已确认（受邀方）`worker-contract-detail.html` + 接单体验 E 已确认 `worker-contract-receive.html`。
- `worker-contract-detail.js`：`STATUS_CONFIG.worker_confirmed_receiver` 去 `upload` 动作（actions→[]）并改 desc 为"等待发起方上传…"；`renderSignArea` 该分支由可上传 `sign-file-drop` 改为只读 `sign-file-wait` 等待态；删除 `handleAction('upload')` 死分支（注释同步更新）。`pickSignFile`/`onSignFilePicked` 仍被发起方 `worker_confirmed_sender` 内联上传复用，保留。
- `worker-contract-receive.html`：下一步流程第 1 步"上传签约文件"→"等待发起方上传签约文件"；吸底 CTA 去掉可上传的"上传签约文件"按钮，改为只读提示"等待发起方上传签约文件"（cta-secondary + showToast）；删除 `doUpload()`。
- PRD 同步（`PRD-合同模块详细规格.html`）：§7.5.1 状态机表 `worker_confirmed_receiver` 行、§8.2 受邀方页面结构图"已确认：上传签约文件"、§8.4 受邀方状态表 `worker_confirmed_receiver` 行 三处去除"受邀方上传签约文件"表述，改为"等待发起方上传 / 无（不支持上传签约文件）"。
- **纠正（同轮补做）**：上轮误将 `worker-contract-receive.html`（E 验证主页，含状态切换条）当作"接单体验E已确认页"，但实际原型导航（worker-contract-detail.html 1105/1106）链接的「确认中（接单体验 E）」「已确认（接单体验 E）」是两个**独立页面** `worker-contract-receive-inviting.html` 与 `worker-contract-receive-confirmed.html`，二者仍残留 receiver 上传（next-step 第1步"上传签约文件" + 可点击"上传签约文件"按钮 + `doUpload()`）。本轮已补齐：两文件 next-step 第1步与 CTA 均改为"等待发起方上传签约文件"只读提示，并移除 `doUpload()`。三份 E 文件现均无 receiver 上传入口。
- 校验：`node --check worker-contract-detail.js` 通过；Grep `action: 'upload'`/`doUpload`/`上传签约文件`（裸，非"等待发起方…"）在 `worker-contract-receive*.html` 三份零残留；`pickSignFile` 仍被发起方引用。MEMORY.md 产品决策区已追加该决策。
- 改动文件：`worker-contract-detail.js`、`worker-contract-receive.html`、`worker-contract-receive-confirmed.html`、`worker-contract-receive-inviting.html`、`PRD-合同模块详细规格.html`。

## PRD 英文清理（全量 5 份文档）
- 用户要求排查**全部** PRD 文档内"很多英文"是否必要，无必要则删除。覆盖：`PRD-完整文档.html`、`PRD-PC端其他模块详细规格.html`、`PRD-合同模块详细规格.html`、`PRD-项目模块详细规格.html`、`PRD-任务模块详细规格.html`（任务模块本轮仅剩 1 处小写 toast 补漏）。
- 删除/中文化（不必要）：
  - 借词 `Tab` → `选项卡`（全 5 份，含章节标题/目录/ASCII 图，保护 `TabBar` 技术专名 7 处保留）：合同 58、项目 18、其余 0。
  - 借词 `Toast` → `轻提示`（全 5 份，含"Toast 提示"去重、章节标题"Toast规格"→"轻提示规格"）：完整 4、PC 20、合同 34、项目 46、任务 1。
  - PC 模块散落代码标识符（括号注记/状态机图）：`applyFilters`×4、`resetFilters`×2、`showCreateModal()`×2、`editTemplate(id)`×2、`toggleStatus(id)`×1、`/ publishDraft`、`/ toggleStatus`×2、`publishDraft(id)`×1、`updateStageSidebarCounts`×2（含括号形式）、`<code>saveDraft()</code> 与 <code>saveTemplate()</code> 改中文"保存草稿/保存并发布动作（状态=启用）"。
  - 项目模块散落代码表达式：`（member.id === currentUserId）` 删、`currentFilter !== 'all' 时` → `当前架构筛选非全部时`。
  - 合同模块 `（<code>saveDraftOnly</code>）` 冗余注记删（中文"仅保存"已表达）。
- 保留并说明原因（必要）—— 均符合项目 2026-08-10《PRD 书写约定》"技术标识符/组件名/代码表达式/URL 片段可作规格说明"：
  - 状态机/角色/结果 key（`changing`/`platform_reviewing`/`pending_review`/`confirmed`/`rejected`/`pending` 等）、`status`/`role` 状态字段、文件名/URL 片段（`*.html?status=…`）、技术专名（`html`/`TabBar`/`SVG`/`URL`/`localStorage`/`accept`/`patchContract`/`filterContracts`/`data-status`/`data-type` 等）、度量单位（`px`/`MB`）、接口规范（`API`/`GET`/`POST`/`PUT`/`JSON`/`UUID`/`String`/`Enum`/`Boolean`）、版本号（`v1`/`V2`）、占位符（`XX`/`XXX`）、CSS 类名/HTML 属性（`primary`/`secondary`/`active`/`disabled`/`class`/`input`/`textarea`/`modal`/`AND` 等，属组件/样式规格说明）。
  - 合同模块顶部 `<style>` 块（文档自身 CSS 变量 `--primary-color` 等）属文档基础设施，非正文内容，不动。
- 校验：剥离 `<script>/<style>` 后全 5 份正文扫描，借词/散落表达式残留为零；`TabBar` 保留 7 处（技术专名）。
- 改动文件：全部 5 份 PRD-*.html。

## PRD 英文清理「复扫补漏」（全量 5 份）
- 用户追问"涉及哪些 PRD 文档修改、是否还有未处理文档"。先核验：5 份 PRD 上一轮均已落地英文清理（无整篇未处理文档）；`.bak`/`*.bak-*` 共 3 个为备份不计入。
- 用「剥离 script/style → 正文 → 紧贴中文字符的英文 token」三次递进扫描复核验，发现上一轮漏清的**真正正文借词**（此前第 37 行把 AND 误判为"组件/样式规格说明"保留，实为描述筛选逻辑的正文英文连词且有中文"同时满足"解释）：
  - `showToast` ×1（合同模块 L555 正文"确认通过/驳回均 showToast 反馈"）→ `轻提示`（Toast→轻提示本就是清理目标，此为漏网）。
  - `AND` ×8：PC 模块 7 处（`AND 叠加`/`AND（同时满足）`/`AND 关系`）→ `与`；合同模块 1 处（L2608 "变更原因已填写 AND 存在变更内容"）→ `且`（条件语境更自然）。
- 二次复扫确认：除允许的 451 处技术标识符（状态 key / 枚举值如`其他（other）` / 函数名 / CSS 类 / 文件路径 / 组件名 / 媒体格式 / 格式符 / 文档自指 PC·PRD·XX·XXX·LZJ 等）外，全文唯一"夹在中文之间"的英文为 `其他（other）`（枚举值 `中文（英文 key）` 合规写法）。
- 结论：5 份 PRD 已全部清理完毕，无未处理文档、无遗留正文借词。
- 改动文件：`PRD-合同模块详细规格.html`、`PRD-PC端其他模块详细规格.html`。

## 工人合同详情页（新流程）PRD 与原型一致性细化（两子任务）

### 子任务 1：invite-banner-demo.html 通知条背景改为 message.html 真实内容
- 用户要求把 `service-miniapp/invite-banner-demo.html` 通知条效果的背景改为 `message.html` 页面内容，与全局页面统一。
- 实现：原 fake `.msg-card` 背景替换为 `<iframe class="bg-iframe">` 内嵌真实 `message.html?tab=contract`；`.phone-frame` 重构为 `.device-wrap`（定位容器）内含 iframe（`.bg-iframe{width:100%;height:100%;border:0}`）+ 浮层 `.invite-banner`（通知条仍浮于其上，不阻断操作）。message.html 自身不动，无重复样式/逻辑。通知条内容"水电班组服务合同 · 金额 ¥12,600"；demo-panel 注释说明背景即真实 message.html（合同邀约 Tab）。JS（showBanner/closeBanner/replayBanner/goRespond/showToast）未改。

### 子任务 2：依据原型细化工人合同详情各子状态页 PRD
- 用户指出 PRD 第七章/第八章（工人合同详情·服务方/受邀方）存在与原型已实现页面/流程/功能/字段/逻辑/交互的缺失或不匹配，要求按原型再细化；非工人合同模块（第一~六章、任务模块、PC、业主端等）不动。
- 比对方法：直接通读 `worker-contract-detail.js`（STATUS_CONFIG / updateStatus / renderMeta / renderSignArea / renderReceiverEndedView / receiverKeyClausesHTML / renderMoreOps / renderActions / handleAction）+ `worker-contract-detail.html` 原型导航，逐项对照 PRD §七/§八/§九。
- 已落地的 PRD 修订（`PRD-合同模块详细规格.html`，仅工人合同章节）：
  1. **§7.3 视角推导**：新增「视角随状态自动推导（updateStatus 逻辑）」note —— 点击状态导航项按 key 自动定视角（含 _receiver→受邀方 / 含 _sender→发起方 / change 系列除 change_confirming 外→发起方 / 草稿→发起方）；并标注 `change_rejected` 虽被乙方驳回但以发起方视角预览。
  2. **§7.5.3 新增**：状态横幅提示语对照表（14 个状态 STATUS_CONFIG.desc 权威文案，含发起方/受邀方同名状态差异文案）。
  3. **§7.6.6 补充**：明确非草稿态下合同名称/金额移入只读 Meta 区、受邀方增「项目地址」并隐「合同类型/架构层级」，给出只读 Meta 字段顺序。
  4. **§7.9 步骤条**：明确"点亮式步骤条，无进度百分比"。
  5. **§7.10 新增**：签约文件区（renderSignArea）与顶部「更多」菜单（renderMoreOps）按状态渲染规则——已签约=已上传行；已确认(发起方)=内联「选择文件并上传」+底部整页双入口；已确认(受邀方)=只读等待文案；其余(含变更签约中)=暂无签约文件；更多菜单确认中(受邀方)仅「导出合同文件」、其余非终态=版本/变更/导出三项、终态隐藏整行工具栏。
  6. **§8.1 纠错**：原"上传本人签署文件"与 V1 由发起方签约冲突，改为"受邀方不支持上传签约文件，已确认后由发起方上传使合同生效"。
  7. **§8.2 结构图**：关键履约信息补「合同名称 / 合同金额」两行。
  8. **§8.5 元信息差异化表**：补 合同名称/合同金额（双方展示）、甲方（陈庄+📞）、乙方（确认后）四行；原合同类型/架构层级/项目地址/正文四行保留。
  9. **§8.6 终态轻量视图**：补终态隐藏顶部「更多」工具栏/正文/名单/底部操作区；新增两终态对照表（抢单失败 ⚠/已拒绝 ✕，已拒绝额外展示本人拒绝原因块 inv.rejectReason）；补 banner 文案差异注（lost 文案"已确认"但视图不渲染 banner/步骤条）。
  10. **§9.3 纠错**：`change_rejected` 视角 受邀方→发起方（状态 key 不含 _receiver，updateStatus 推导为发起方）；说明列补「乙方驳回原因」块（showRejectReason，演示默认文案）。
  11. **§9.5 驳回变更流程**：补变更已驳回详情页顶部 banner 渲染「乙方驳回原因」块。
- 校验：Grep 确认新增锚点（sec-7-10 / 7.5.3 / 视角随状态自动推导 / 乙方驳回原因 / 仅「导出合同文件」）均存在；非工人合同章节（第一~六章、任务、PC、业主）未触碰。
- 改动文件：`service-miniapp/invite-banner-demo.html`、`PRD-合同模块详细规格.html`（仅 §七/§八/§九 及 §7.9/§7.10）。

## PRD 任务模块 §3.9–§3.19 重构（服务方详情页分视角详述）
- 用户指出：原型页面是**示例数据**，旧描述却"指向唯一确定的效果"（如断言"业主作为确认人之一"），而真实产品**业主可能是/可能不是确认人，取决于配置**；要求按"主逻辑/状态流程前文已介绍，本节仅逐一呈现不同状态×不同视角×不同场景的演示效果"重新梳理 §3.9–§3.19 及相关项。
- 先核对原型 `service-miniapp/task-detail.html`：角色切换器仅 **配置人/执行人/确认人** 三类（无独立业主按钮）；演示数据固定为 执行人李四(工长) + 确认人 张三(项目总)/王五(业主)/赵六(设计师)，并存"暂无确认人"提示。**六、任务详情页（业主）**（业主小程序真实页面，业主专属确认）属"其他已有页面描述"，按用户要求**不动**。
- 重构内容（仅 §3.9–§3.19，未碰 §3.1–§3.8 与第六章）：
  1. §3.9 引言：改为"基于原型示例数据，状态×视角逐一展开 + 不同场景演示"，明确"主逻辑/状态流转前文已介绍，本节仅聚焦呈现"。
  2. 新增「演示数据与本页定位说明」note：原型为示例数据不代表固定人员/唯一效果；业主是否被配置为确认人取决于配置——被配置则与确认人视角一致（确认后可评价），未被配置则归入只读「其他」视角；各状态「业主」行特指"业主被配置为确认人"演示场景。并枚举**演示覆盖场景**：①有确认人(多确认人) ②无确认人(快捷完成) ③业主是否被配置为确认人。
  3. §3.10「四类用户视角」表：业主行改为"业主（被配置为确认人时）"；同步改「视角切换机制」note 为「视角切换机制与业主场景说明」，澄清无独立业主视角按钮。
  4. §3.11–§3.19 共 9 处「业主」/「确认人/业主」行全部改为条件表述（仅当被配置为确认人时与确认人视角一致；否则归入只读「其他」视角）。
- 校验：Grep 旧确定性表述（"业主作为确认人之一"/"原型演示数据中业主「王五」即是一名确认人"/"业主视角在原型中以「确认人=王五（业主）」"）零残留；新条件表述在 9 行 + §3.9/§3.10 全部就位；HTML 标签闭合正确。
- 改动文件：`PRD-任务模块详细规格.html`（仅 §3.9–§3.19）。
- 关联改动提示（已告知用户，待确认）：第六章业主方真实页面与 §3.1–§3.8 均未动，与新框架一致，无需改；若后续要让第六章也显式呼应"业主未必是确认人"可再议。

## 工人合同详情 PRD/原型 二次修正（A方案通知条 + §7.5.3 视角纠偏）

### 子任务 1：invite-banner-demo.html 二次修复
- 用户反馈：① 不需要展示原型导航；② 通知条应在手机框内（此前误在框外）；③ 微信小程序手机端应删除上下左右滚动条、内容在手机框内避免溢出。
- 根因：`message.html` 自身含右侧 `.page-nav`（原型导航，约 736 行）且页面可滚动；iframe 内嵌后会显示导航与滚动条。
- 修复（不改 message.html）：
  - iframe 加 `id="bgFrame"` + `scrolling="no"` + `onload="onIframeLoad()"`；加载后由父页注入样式隐藏 `.page-nav`、设 `html,body{overflow:hidden}` 并隐藏 `::-webkit-scrollbar`/`scrollbar-width`，去滚动条且不溢出。
  - 通知条（`.invite-banner`）保持为 `.device-wrap` 子元素（定位在手机框内、被 `overflow:hidden` 裁剪），未外移。
  - `body{overflow:hidden}` + `.device-wrap{overflow:hidden}` 确保整体无页面级滚动条；`.demo-panel` 设 `max-height:812px;overflow-y:auto` 且隐藏其滚动条。
- 校验：message.html 未改动；iframe 同域可注入样式。

### 子任务 2：§7.5.3 视角纠偏 + 各状态流程/逻辑/交互补全
- 用户指出 §7.5.3 原"状态横幅提示语对照"混入了工人端受邀方视角文案（如"您被邀请参与此合同…""您已成为本合同乙方…""该合同已被其他人员确认（抢单失败）…"），与本章（服务方/工长视角）定位不符，且缺各状态 流程/逻辑/交互条件 说明。
- 修订（`PRD-合同模块详细规格.html`）：
  1. **§7.5.3 重写**：收敛为「发起方视角」状态横幅与状态逻辑详解——新增表列「触发条件（进入逻辑）/ 底部主操作（交互）/ 状态流转（下一态）/ 关键逻辑·交互说明」；仅列发起方可见状态（worker_draft_initial / worker_draft / worker_inviting_sender / worker_confirmed_sender / worker_signed / changing / change_confirming_sender / change_signing_wait / change_rejected 共 9 行）；开头 note 明确"视角随状态 key 自动推导"并指向 §8.4/§8.6 看受邀方对应文案。**不再在第七章混入受邀方视角 desc**。
  2. **§8.4 重写**：受邀方状态表新增「受邀方 banner 文案(desc) / 触发条件与逻辑 / 状态流转 / 关键说明」列，把用户举例的三句受邀方文案正确落位并补 触发（首胜校验/撤回确认/抢单失败/主动拒绝填原因）、流转（confirmed_receiver / rejected_receiver / lost_receiver）、交互（确认后自动加入架构层级、受邀方不可上传签约文件、更多菜单仅导出）。
  3. **§8.6 补列**：两终态表新增「触发条件与逻辑」列（lost=他人首胜校验成功；rejected=本人主动拒绝填原因置终态），补全流程逻辑。
- 校验：Grep 确认新 §7.5.3 标题/9 行发起方状态、§8.4「受邀方 banner 文案（desc）」、§8.6「触发条件与逻辑」均就位；第一~六章/任务/PC/业主 等非工人合同内容未动。
- 改动文件：`service-miniapp/invite-banner-demo.html`、`PRD-合同模块详细规格.html`（§7.5.3 / §8.4 / §8.6）。

## 任务详情原型深链接激活 + PRD §3.11–§3.19 原型链接补充
- 用户需求：PRD 每个状态页面需提供可点击的原型链接，供一对一核对。
- 核查原型：`service-miniapp/task-detail.js` 的 `initFromUrl()` 已实现 `?status=`/`?role=` 深链逻辑，但**从未被调用（死代码）**——DOMContentLoaded 仅调 `initProfileClickEvents()`。已在 DOMContentLoaded 中首行加 `initFromUrl();` 激活深链（node --check 通过）。
- 状态↔角色令牌映射（initFromUrl 真实接受，避免 coerce）：configuring→configurer/other；pending/rejected_pending→executor/nonExecutor；pending_blocked/in_progress→executor/confirmer/configurer/other；confirming/confirming_after_reject/completed/completed_with_reject→confirmer/nonConfirmer。
- PRD 补充（`PRD-任务模块详细规格.html`，仅 §3.9–§3.19）：① §3.9 末尾新增「原型演示链接（一对一核对）」note，说明 `?status=<状态枚举>` / `?role=<视角枚举>` 用法及原型顶部「任务状态切换/用户视角切换」手动浏览；② §3.11–§3.19 每节标题下插入「🔗 原型演示」链接块（共 16 条、16 组合、0 coerce），只读类「业主/其他」行对应原型 非执行人/非确认人/其他用户 视角（按原型实际视角命名，避免跳到被 coerce 视图）。
- 校验：node 脚本扫描 16 链接全部落入有效令牌、零 coerce；HTML 标签闭合正确。
- 改动文件：`service-miniapp/js/task-detail.js`、`PRD-任务模块详细规格.html`（§3.9 note + §3.11–§3.19 链接）。

## 工人合同 PRD 三次修正（导航遮罩 + 状态章节按状态拆分二级标题+中文化+原型链接）

### 子任务 1：invite-banner-demo.html 导航问题最终修复
- 用户反馈（附截图）：message.html 的 `.page-nav`（position:fixed; right:20px）仍在 iframe 内显示于手机框中，覆盖消息内容；要求若无法删除则与全局页面一致在框外展示，通知条须在框内，去除滚动条。
- 根因：CSS 注入方案在同域 iframe 中理论上可行但受时序/浏览器安全策略影响不稳定；用户截图证实注入未生效。
- 最终方案（双层保障）：
  1. **CSS 注入保持**：iframe onload 时尝试注入 `.page-nav{display:none}` + `overflow:hidden` + 去滚动条样式。
  2. **新增兜底遮罩层** `.nav-fallback-cover`：`position:absolute; top:0; right:0; width:120px; height:100%; background:#e9eef3; z-index:40; display:none`。注入成功则隐藏（`display:none`），失败/超时（1.5s 后检查）则显示遮罩覆盖右侧导航区域。
  3. **通知条确认在框内**：`.invite-banner` 为 `.device-wrap` 子元素 + `z-index:50` + `overflow:hidden` 裁剪，始终位于手机框内。
  4. **滚动条清除**：`body{overflow:hidden}` + `.device-wrap{overflow:hidden}` + iframe `scrolling="no"` + 注入 `::-webkit-scrollbar{display:none}`。
- 改动文件：`service-miniapp/invite-banner-demo.html`（未改 message.html）。

### 子任务 2：PRD §7.5.3 / §8.4 按状态拆分二级标题 + 全中文化 + 每节原型链接
- 用户指出 §7.5.3 描述多为英文不适合 PRD；且每个状态应附带原型对应页面链接（可跳转核对）；最清晰的方式是每个工人合同详情状态单独对应一个二级标题进行详细说明。
- 重构内容（`PRD-合同模块详细规格.html`，仅工人合同章节）：
  - **§7.5.3 从大表格拆为 9 个二级子节**（7.5.3.1–7.5.3.9）：拟定中(初始)/拟定中(撤回后)/确认中(发起方)/已确认(发起方)/已签约/变更中/变更确认中/变更签约中/变更已驳回。每节含：🔗原型页链接（worker-contract-detail.html + 右侧导航切换说明）、横幅状态文案、横幅说明文字（全中文）、触发条件、页面结构与可见模块、底部操作按钮、状态流转、关键逻辑与交互细节。
  - **§8.4 从大表格拆为 5 个二级子节**（8.4.1–8.4.5）：确认中(受邀方)/已确认(受邀方)/抢单失败(终态)/已拒绝(终态)/方案 E 接单体验预览态。每节同样含原型链接（含 ?viewer=receiver 和三份 E 页面独立链接）、全中文描述、触发条件、页面结构（信息收敛规则）、操作按钮、状态流转、关键逻辑。
  - **英文表述全面中文化**：「banner」→「横幅」、「key」→「标识」/「状态标识」、「sender/receiver」→「发起方/受邀方」、「actions」→「操作按钮」、「desc」→「说明文字」等；技术代码标识符（STATUS_CONFIG / worker_draft_initial 等 state key / 函数名）保留括号内作为规格追溯锚点。
  - **非工人合同模块完全未动**（第一~六章、任务模块、PC、业主端等）。
- 校验：Grep 确认 14 个二级标题（7.5.3.1–9 + 8.4.1–5）全部就位且各带 🔗 原型链接；旧大表格格式零残留。

## invite-banner-demo.html 彻底重构（放弃 iframe 注入，改同源自包含渲染）
- 用户多次反馈 iframe 注入方案失效：① 通知条在框外/框内侧效果不符；② 内容仅显示一半、底部主导航(tab-bar)被裁切；③ 右侧 .page-nav 原型导航仍显示。多次尝试「单次注入 / 三重保障+多次重试 / 兜底遮罩」均未稳定生效（同域 file:// 下 contentDocument 注入受时序与浏览器安全策略影响不可靠）。**用户明确要求谨慎对待、验证需真实有效。**
- **根因定位（彻底）**：`message.html` 自身包裹 `.phone-frame`（border:10px + margin:20px）且 `.page-content` 高度 `calc(100% - 44px - 50px)` 漏算 status-bar(44)+message-tabs(44)，内部总高 ≈900px >> 812px，溢出 88px 裁掉 tab-bar；且 `.page-nav` 为 position:fixed 始终在侧。
- **最终方案（彻底可靠，无跨文档操作）**：放弃 iframe，改为**同源单文档渲染**——
  1. 链接 `common.css`（状态栏/导航栏/底部 tab-bar/设计变量与 message.html 完全一致）；
  2. 内联 message.html 的「消息卡片」相关样式（.message-card/.message-header/.invite-detail/.message-actions/.message-status 等，与原文件逐字一致）保证视觉统一；
  3. `.device-wrap`(375×812, overflow:hidden) 内直接放 status-bar(44)+nav-bar(44)+message-tabs(44)+page-content(630,overflow-y:auto)+tab-bar(absolute,bottom:0,50px)；流内块 44+44+44+630=762=812−50，精确吻合，**无溢出、无滚动条、无 .page-nav**；
  4. 内容区放 3 张合同邀约卡（水电¥12,600 / 拆除¥4,800 / 木作¥9,800 已接受），与通知条金额一致；
  5. 通知条 `.invite-banner`：top:0、border-radius:40px 40px 0 0（圆角跟随手机框）、z-index:300（高于 status-bar 的 z-index:100，确保覆盖顶部滑出）、transform 滑入动画不变。
- **可靠性说明**：此方案全部元素在同一文档，无任何 iframe / contentDocument / 跨文档注入 / 时序依赖，渲染结果与代码静态可推，不会再出现注入不生效问题。布局数学已静态校验 812px 精确分配。
- 改动文件：`service-miniapp/invite-banner-demo.html`（message.html 仍完全未改）。

## PRD 任务模块 第六章（业主方详情页）补原型链接 + 描述对齐原型
- 用户要求：与服务方 §3.9–§3.19 同样的方式，给「六、任务详情页（业主）」每个状态页补可点击原型链接（一对一核对）；如需描述调整/补充按原型修改，**不改动原型**。
- 核查原型 `owner-miniapp/js/task-detail.js`：`initFromUrl()` 已在 line 2909 调用（深链本就生效，无需改原型）；`getConfigurerViewVisible` 对业主端恒返回 `{roleSwitcher:false, configuring:false}`，故「配置人视角」导航项恒隐藏。角色切换器可见性逐状态核对：仅 `configuring` 隐藏通用 roleSwitcher，其余 8 态（pending/pending_blocked/in_progress/confirming/confirming_after_reject/completed/completed_with_reject/rejected_pending）均 `display:block`（执行人/确认人/其他用户三项可见，配置人隐藏）。故原 §6.3「业主端只有业主这一种固定视角、无角色切换器」与真实原型不符 → 已纠正。
- 改动（`PRD-任务模块详细规格.html`，仅第六章 §6.3–§6.12）：
  1. §6.3 引言：改为「业主端原型不提供配置人视角（导航项隐藏）；待配置之外所有状态顶部保留演示用视角切换（执行人/确认人/其他用户），演示业主作为确认人可操作 vs 其他相关方只读；亦可用 ?role= 锁定」。
  2. §6.3 新增「原型演示链接（一对一核对）」note：解释 `?status=&role=` 参数取值与各链接标注，并说明业主默认被配置为确认人之一（王五·业主），未配置时仅作其他相关方只读（role=other）。
  3. §6.3 头部状态卡 li、§6.4 字段：原「无角色切换器」表述改为准确描述（顶部视角切换不含配置人视角 / 待配置态仅显「其他用户视角」只读切换）。
  4. §6.4–§6.12 每个状态小节标题下插入 🔗 原型演示链接块，共 **13 条链接 / 13 种 state×role 组合**：configuring·other、pending·nonExecutor、pending_blocked·other、in_progress·other、confirming·confirmer/nonConfirmer、confirming_after_reject·confirmer/nonConfirmer、completed·confirmer/nonConfirmer、completed_with_reject·confirmer/nonConfirmer、rejected_pending·nonExecutor。
- 校验：node 脚本逐链接对照 owner `initFromUrl` 映射，13 条全部落入有效令牌、**0 coerce**；全文「无角色切换器」零残留；HTML 标签闭合正确；原型文件零改动。
- 改动文件：`PRD-任务模块详细规格.html`（仅第六章）。

## 任务模块 §3.10「四类用户视角」典型身份修正（2026-08-14）
- 问题：§3.10 视角表「典型身份」列用词错位/乱用。配置人「项目经理 / 工长」与文档上方§3.2/§3.3（配置人=合同甲乙方及合同创建者（服务方））冲突；执行人「施工员 / 工种工人」乱用不存在的「施工员」；确认人「项目总 / 设计师 / 监理 / 业主等」应统一为可选项。
- 修正：① §3.10 三行典型身份 → 配置人「合同甲乙方 / 合同创建者（服务方）」、执行人「项目内人员（可配置选择）」、确认人「项目内人员（可配置选择）」；② §3.7 按钮矩阵表头「项目经理视角」→「配置人视角」（统一术语，该列本就是配置人能力）；③ §6.3 note「服务方配置人（项目经理 / 工长）」→「（合同甲乙方 / 合同创建者）」。
- 保留：演示数据人名角色（张三项目总、李四工长、赵六设计师、王监理等）属示例人物，非典型身份原型，不动。
- 改动文件：`PRD-任务模块详细规格.html`（§3.7 / §3.10 / §6.3）。

## 补充「三段标准卡片」到页面通用区块列表
- 用户指出 §3.10「3. 页面通用区块（各状态共享骨架）」漏列「执行标准 / 确认标准 / 担责标准（三段标准卡片）」。
- 依据：页面布局 ASCII 图（line 774「三段标准卡片」）、§3.4「三段标准显示规格」、原型 `standard-card`（含 `standard-list`/`standard-item`，toggleStandard 折叠）三者一致证明其为通用区块。
- 改动：① 服务端 §3.10 列表（人员区之后、执行记录卡之前）新增「三段标准卡片（standard-card）」li，描述图标/可折叠/编号列表/数据来源/只读；② 业主端「业主端页面通用区块」列表同步补入同构简述。两端一致。
- 改动文件：`PRD-任务模块详细规格.html`（§3.10 + 业主端通用区块）。

## 修复正文裸英文状态/样式类名（PRD规范）
- 用户指出 §3.11–§3.19 / 业主 §6.4–§6.12 正文里把 `current/completed/confirming/in-progress` 当中文直接用（如「流程条仅第 1 步「待开始」为 current」），违反 2026-08-10《PRD 书写约定》（状态 key 应中文在前、代码标识符应「英文（中文规格）」）。
- 修复：统一改为「中文（英文标识符）」——`当前（current）`、`已完成（completed）`、`确认中（confirming）`、`执行中（in-progress）`；共 17 处正文（service 端 16 + owner 端 1）裸英文清零。
- 保留合规项：状态机表格独立「状态 key」列（约定列序 显示文案|状态key）、`?status=`/`?role=` URL 参数值、`&lt;/&gt;/&amp;` HTML 实体、`accept 属性` 等技术标识符（均带中文语境）。
- 改动文件：`PRD-任务模块详细规格.html`（§3.11–§3.19、第六章相关「效果/字段」段落）。

## 服务方任务详情章节移除「业主」单列视角描述
- 用户指出：任务详情页（服务方）§3.9–§3.19 内单列「业主」视角描述（如「仅当被配置为确认人时…归入只读其他视角仅查看」）不合适——有独立「第六章 任务详情页（业主）」，且服务端本不提供独立业主视角，不应喧宾夺主。
- 处理（仅服务方章节，第六章/他处不动）：① §3.9 演示数据说明 note 重写——明确服务端角色切换器仅 配置人/执行人/确认人 三类、不提供独立业主视角，业主专属体验转第六章，删除原「业主是否被配置为确认人」大段与演示覆盖场景③；② §3.9 原型链接说明「只读类『业主』…」措辞删业主；③ §3.10「四类用户视角」→「三类用户视角」并删业主行；④ §3.10 note 标题「视角切换机制与业主场景说明」→「视角切换机制」，删业主专属段落、加第六章交叉引用；⑤ §3.11/3.12/3.15/3.17 纯「业主」行删除；⑥ §3.13/3.14/3.16/3.18/3.19「确认人 / 业主」合并行改为纯「确认人」（删业主括号说明）。
- §3.11 选择器与流转：执行人/确认人选择器「可选范围为项目内人员」补充到两个选择器；「配置完成后由配置人驱动流转至「待开始」（提交配置）。」→「配置执行人自动流转至「待开始」状态。」。
- 保留：§3.15 演示数据「王五（业主）」仅作确认人示例 persona 命名（非视角描述），不动。
- 改动文件：`PRD-任务模块详细规格.html`（§3.9、§3.10、§3.11–§3.19 权限表）。

## §3.11 待配置 权限与用户视角修正（2026-08-14）
- 用户指出：§3.11「待配置」权限表中存在「执行人」「确认人」视角行是错误的——待配置态下执行人与确认人均未配置，**不存在独立的执行人/确认人视角**，仅 配置人 与 其他用户视角 两种。
- 依据原型 `service-miniapp/js/task-detail.js` `initFromUrl()`（line 2617-2618）：`status=configuring` 时 `currentUserRole = role === 'other' ? 'other' : 'configurer'`，即除 other 外一律 coerce 为 configurer，确认原型此态仅支持 配置人/其他用户视角。
- 修正：删除 §3.11 权限表「执行人」「确认人」两行；把原「执行人」行的只读等待内容并入新增「其他用户视角」行；并在表下加注说明待配置态无独立执行人/确认人视角（其余角色回退为配置人视角）。演示链接（configurer/other）本就正确，无需改。
- 改动文件：`PRD-任务模块详细规格.html`（§3.11 权限与用户视角表）。

## §3.12 待开始 原型演示补四视角 + 权限表补「其他用户视角」
- 用户指出 §3.12「待开始」原型演示仅写 2 个链接（executor/nonExecutor），应覆盖四个用户视角（配置人/执行人/确认人/其他用户视角），且权限表须按每个视角单独做可见/可操作说明。
- 原型核查：`getConfigurerViewVisible(PENDING)` 返回 `{roleSwitcher:true, configuring:false}` → 待开始态角色切换器**显示且含配置人视角**；`renderPending` 已完整处理 configurer/executor/confirmer/other 四分支。但旧 `initFromUrl` 对 pending 仅 `role==='nonExecutor'?'nonExecutor':'executor'`，强制把 configurer/confirmer/other 都 coerce 成 executor —— 故此前深链只能落 2 个视图。
- 修复（service-miniapp/js/task-detail.js `initFromUrl`）：把 pending 从 `PENDING || REJECTED_PENDING` 合并分支拆开——pending 现接受 configurer/confirmer/other/nonExecutor/其余→executor（与 in_progress 一致，使四个框架视角深链 0 coerce）；rejected_pending 仍仅 executor/nonExecutor（该态配置人视角隐藏，不可达）。`node --check` 通过。
- PRD 修订（`PRD-任务模块详细规格.html` §3.12）：① 🔗 原型演示链接由 2 扩为 4（configurer/executor/confirmer/other，其中 other 标注「只读」）；② 权限与用户视角表新增「其他用户视角」行（只读、操作栏「等待执行人执行」、固定上传栏隐藏），并补全配置人/执行人/确认人三行的固定上传栏/只读说明，做到每视角独立可见/可操作。
- 改动文件：`service-miniapp/js/task-detail.js`、`PRD-任务模块详细规格.html`（§3.12）。

## 任务模块 PRD 三处修订（§3.11 搜索规则 / §3.12 交互流转 / 角色切换器描述分离）
- **① §3.11 待配置 补充搜索匹配规则**：原型 `renderExecutorOptions`/`renderConfirmerOptions`（task-detail.js line 249/403）过滤逻辑 `m.name.toLowerCase().includes(kw) || m.role.toLowerCase().includes(kw)`，即按**姓名或角色模糊匹配**（不区分大小写、子串包含即命中），空结果显示「未找到匹配的成员」。PRD §3.11 交互末尾新增「搜索匹配规则」note，并修正原 li 中「搜索成员，按角色匹配」的不准确表述（实为姓名或角色）。
- **② §3.12 待开始 交互流转修正**：原描述错把「提交确认/完成任务」当作触发流转到执行中的动作。原型实际：`confirmUploadRecord` 在 `currentTaskStatus!==IN_PROGRESS` 时 `updateTaskStatus(IN_PROGRESS)`（首次上传即流转执行中）；`confirmSubmitToConfirmer` 有确认人→CONFIRMING（确认中）、无确认人→COMPLETED（已完成）；`deleteExecutionRecord` 删至空且处 IN_PROGRESS 时退回 PENDING。`showSubmitConfirmModal` 按 hasConfirmers 切换「提交确认」/「完成任务」文案。PRD §3.12 交互重写：上传记录→（确认上传）流转执行中；上传在提交前支持多次（继续上传/编辑/删除，删空退回待开始）；执行中下 提交确认→确认中 / 完成任务→已完成（详见 §3.14）。
- **③ 「角色切换器显示/隐藏」类描述与 PRD 内容分离**：用户指出此类描述仅供原型查看人员使用、非小程序真实功能操作，不应混入 PRD 内容。处理：① §3.11/§3.12/§3.13 字段段落中「角色切换器显示（通用三者）/显示/隐藏改显配置视角」三处内联描述全部删除；② 第六章 §6.4 字段「通用角色切换器隐藏」一并删除（保留「业主端不提供配置人视角」产品事实）；③ 在 §3.9 新增「原型视角切换说明（仅供原型查看，非小程序功能）」note，统一说明角色切换器仅为核对辅助、不计入各状态「字段」产品内容；原有 §3.10「视角切换机制」与 §3.9「演示数据」note 中角色切换器表述已属 note 范畴、保留。
- 改动文件：`PRD-任务模块详细规格.html`（§3.9 note、§3.11、§3.12、§3.13、第六章 §6.4）。

## 工人合同变更功能 PRD 按状态分节（第九章重构）
- 用户要求：参考 §7.5.3 / §8.4「每个状态单独一章节 + 原型链接」的方式，把工人合同变更功能（第九章）也按每个页面/状态单独章节说明，并附带原型页面链接。
- 重构内容（`PRD-合同模块详细规格.html`，仅第九章）：
  - **目录**：§9 子节重排 → 9.1 概述 / 9.2 角色权限 / 9.3 变更流程状态机总览（原 9.3 更名）/ 9.4 发起变更（变更提案编辑）/ 9.5 各变更状态页详述（9.5.1–9.5.6）/ 9.6 字段数据结构（原 9.7）/ 9.7 原型导航入口（原 9.8）。新增 9.5.1–9.5.6 叶子项并加 `.toc-leaf` 缩进样式。
  - **9.4 发起变更（变更提案编辑）**：入口状态 worker_signed、触发条件、变更编辑面板结构（变更须知/金额新旧对比/正文/阶段任务）、changeProposal 字段、提交后流转 changing、关键逻辑（去平台审核/金额对比/阶段暂停/V2）。
  - **9.5 各变更状态页详述**（替代原 9.4/9.5/9.6 流程章节，按状态拆分）：9.5.1 变更中(changing·发起方) / 9.5.2 待确认变更(change_confirming·受邀方) / 9.5.3 变更已驳回(change_rejected·发起方视角) / 9.5.4 变更确认中(change_confirming_sender·发起方) / 9.5.5 变更签约中(change_signing_wait·双方) / 9.5.6 上传变更签约文件(独立整页 worker-change-sign-upload.html)。每节含 🔗 原型页链接（worker-contract-detail.html + 右侧导航切换说明，整页用独立链接）、横幅文案、进入条件、页面结构、底部按钮、状态流转、关键逻辑（均依据 STATUS_CONFIG 与 ACTION_TEXT 真实值）。
  - **9.7 原型导航入口**：补「对应 PRD 章节」列，串联 6 个预览项 ↔ 9.5.x。
  - 同步修正 §7.5.3 三处旧 `#sec-9-4` 交叉引用（变更中/变更确认中/引言）→ 指向 9.5.2（待确认变更），9.5.3（变更已驳回）。
- 校验：Grep 确认 9.5.1–9.5.6 六个 id 与 🔗 链接就位；旧 `#sec-9-4`/`#sec-9-8` 引用零残留；HTML 标签闭合正确；非工人合同章节未动。
- 改动文件：`PRD-合同模块详细规格.html`（仅第九章 + 目录 + .toc-leaf 样式 + §7.5.3 三处交叉引用）。

## §3.12 待开始 补充「上传记录字段」与「执行记录区展示/操作项/流程」
- 用户指出 §3.12「待开始」未体现：① 上传记录记录哪些字段内容；② 页面内执行记录区展示哪些操作项；③ 操作项的流程交互。
- 原型核查（service-miniapp/js/task-detail.js）：`confirmUploadRecord`（line 700-712）生成 `newRecord = { files:[{name,type,size}], desc, time("MM-DD HH:mm") }`；`handleFileSelect` 按 image/video/audio 三类型 set `accept` 并 push `{name,type,size}`；`deleteExecutionRecord`（731）/ `editExecutionRecord`（739）/ `updateExecutionRecordDisplay`（749）给出逐条操作与展示（「执行记录 #N · 时间」、media grid 📷/🎬/🎵、desc）；操作项显隐条件 `currentUserRole==='executor' && status!∈{confirming,completed,completed_with_reject,confirming_after_reject}`。
- PRD 修订（`PRD-任务模块详细规格.html`）：① §3.12 新增「上传记录（数据结构与提交交互）」块——上传弹窗（图片/视频/音频 + 执行说明选填）、记录三字段表（files[name/type/size]、desc、time）、提交校验 note；② 新增「执行记录区展示与操作项（首传后流转执行中可见）」块——卡片标题「执行人上传的内容」+ 每记录展示（#N/时间/缩略图网格/说明）+ 三操作项表（上传记录/继续上传、编辑✎、删除✕）及各自流程（新增→待开始转执行中/就地更新/删空且执行中则退回待开始）+ 只读视角不显示 note；③ §3.14 交互末尾加 note 反向引用 §3.12 完整说明，保持两节一致、避免重复。
- 改动文件：`PRD-任务模块详细规格.html`（§3.12、§3.14）。

## §3.12 上传记录字段规格补全 + 修正裸英文违规（强约束重申）
- 用户反馈：① 上传记录字段缺「是否必填、校验规则」规格说明；② 上一轮新增内容又出现英文非 PRD 规范情况（裸 `<code>files</code>`/`<code>desc</code>`/`<code>name</code>`/`<code>type</code>`/`<code>size</code>` 等英文标识符前置，违反「中文（英文标识符）」约定）。
- 修正（`PRD-任务模块详细规格.html` §3.12）：① 字段表由 3 列扩展为 5 列——新增「必填性」（媒体文件=条件必填、执行说明=选填、提交时间=系统自动）与「校验规则」（files/desc 至少一项非空；type 须为 image/*、video/*、audio/* 之一；name/type/size 三项均需存在）；② 全部字段名改为中文前置：`媒体文件（files）`、`name（文件名）`、`type（MIME 类型）`、`size（文件大小）`、`执行说明（desc）`、`提交时间（time）`；③ 原独立校验 note 并入字段表，删除裸英文；④ 日期格式 `MM-DD HH:mm` 改为中文「月-日 时:分」以消除裸英文；⑤ 仅保留 `<code>image/*</code>` 等 MIME 字面量（纯技术取值，符合英文前置例外）。
- 已将「PRD 英文规范强约束」作为重申条目写入项目 `MEMORY.md`（PRD 中英文书写顺序约定下），要求每轮 PRD 改动交付前 Grep 自查 `<code>` 与裸英文字母串，禁止向新增内容引入裸英文标识符；后续所有 PRD 增改须遵守。
- 改动文件：`PRD-任务模块详细规格.html`（§3.12）、`MEMORY.md`。

## 工人合同变更态描述去重（§7.5.3 ↔ §9）
- 用户质疑：`工人合同详情页（服务方工长端）`（§7.5.3）与 `工人合同变更功能详细规格`（§9）内的变更相关状态页面说明是否重复，要求将变更相关融合进 §9、避免多处冗余/歧义。
- 核查结论：§7.5.3.6–9（变更中/变更确认中/变更签约中/变更已驳回，4 个发起方变更态）与 §9.5.1/9.5.4/9.5.5/9.5.3 **逐字段重复**（横幅、进入条件、页面结构、按钮、流转、关键逻辑全重述）；§8.4（受邀方）引言已主动声明变更态见第九章、其 5 子节不含 change_confirming，故 §8.4 与 §9 无实质重复。
- 修订（`PRD-合同模块详细规格.html`，仅 §7.5.3 + 引言）：① §7.5.3 引言新增说明——变更流程全部状态（含发起方 4 态与受邀方待确认变更）统一收录于第九章（9.4/9.5.1–9.5.6），7.5.3.6–9 仅保留导航索引；② 将 7.5.3.6–9 四张完整属性表**替换为导航索引段落**（保留子节标题 + 🔗 原型链接 + 一句话指向对应 §9.5.x），详细内容以 §9.5 为唯一权威出处。
- 校验：Grep 确认无任何章节反向引用 `sec-7-5-3-6/7/8/9`（仅定义处与引言），无孤立锚点；§7.5.3.5（已签约·发起变更入口）仍完整并指向 §9；§9.5 锚点（sec-9-5-1/3/4/5）均存在；非工人合同章节未动。
- 改动文件：`PRD-合同模块详细规格.html`（§7.5.3 引言 + 7.5.3.6–9 四节）。

## §3.12 媒体文件（size 文件大小）格式规格补遗
- 用户指出 §3.12 字段表中「媒体文件（files）」仅列出 `size（文件大小）`，未说明其数据类型 / 单位 / 上限。
- 修订（`PRD-任务模块详细规格.html` §3.12 字段表「媒体文件」行）：① 类型/取值列补 size 类型「数值」；② 校验规则新增第 ④ 条「单文件 size 不超过 20MB（与 §3.5 上传限制一致），超出应选完即提示并丢弃；原型当前仅记录大小、尚未落地大小校验，属待补实现」；③ 说明列补「size 为文件原始字节数（取自文件对象 size 属性）」。size 上限口径与 §3.5 执行记录上传规格（单文件 ≤ 20MB）对齐。
- 改动文件：`PRD-任务模块详细规格.html`（§3.12）。

## 删除 §7.5.3.6–9 变更态导航索引（用户要求去啰嗦）
- 用户指出：7.5.3.6/7/8/9 已在第九章变更功能内存在，这些导航索引段落过于啰嗦，要求直接删除。
- 修订（`PRD-合同模块详细规格.html`，仅 §7.5.3）：① 删除 7.5.3.6 变更中 / 7.5.3.7 变更确认中 / 7.5.3.8 变更签约中 / 7.5.3.9 变更已驳回 四个子节（含 subsection-title + 导航索引 <p> 整段）；② §7.5.3 引言同步改写——去掉「7.5.3.6–9 仅保留导航索引」措辞，改为「变更流程相关状态及各变更页面说明已统一收录于第九章，本章不再另列变更态子节」，并保留指向 §9.5 / 9.5.2 / 9.4 的权威出处索引。
- 校验：Grep 确认全文档无遗留 `sec-7-5-3-[6-9]` / `7.5.3.[6-9]` 引用（0 命中）；7.5.3.5（已签约·发起变更入口）仍完整并指向 §9；删除处衔接正常（7.5.3.5 表格闭合后直接接 §7.6）；§9.5 仍为变更态唯一权威出处。非工人合同章节未动。
- 改动文件：`PRD-合同模块详细规格.html`（§7.5.3 引言 + 删除 7.5.3.6–9 四节）。

## §3.13 待开始（前置阻塞）配置人「修改执行人」功能补充
- 用户指出服务方 §3.13「待开始（前置阻塞）」配置人视角应支持「修改执行人」，要求补充该功能交互效果且不影响其他已有功能。
- 原型核查：`renderPendingBlocked`（line 1298）此前对所有角色 `addExecutorBtn.style.display='none'`，即阻塞态配置人无法改派执行人；且 `selectExecutor`（line 276）改派后硬编码 `updateTaskStatus(PENDING)`，若在阻塞态改派会错误解除阻塞。
- 原型最小改动（`service-miniapp/js/task-detail.js`）：① `renderPendingBlocked` 配置人分支显 `修改执行人` 按钮（innerHTML `✎ 修改执行人` + onclick `showExecutorSelector(true)`），其余角色仍隐藏；② `selectExecutor` 末尾 re-render 改为 `currentTaskStatus === PENDING_BLOCKED ? PENDING_BLOCKED : PENDING`，保证阻塞态改派保持阻塞、不影响正常 待开始 / 待配置 流转（node --check 通过）。
- PRD 修订（`PRD-任务模块详细规格.html` §3.13）：配置人视角行补「显执行人卡 + 修改执行人入口（可改派，阻塞态改派不改变阻塞状态）」；交互节新增「配置人修改执行人 → 执行人选择器（姓名/角色模糊匹配）→ 更新执行人卡并通知 → 仍保持前置阻塞」及「阻塞由前置任务决定，与执行人是谁无关」说明；保留原「前置任务完成后自动解除阻塞」说明。
- 改动文件：`service-miniapp/js/task-detail.js`、`PRD-任务模块详细规格.html`（§3.13）。

## §3.13 待开始（前置阻塞）描述简化 + 前置任务可点击跳转
- 用户指出服务方 §3.13 配置人视角「前置任务未完成等待」类描述过多、需简化；要求取消「预计完成：01-18」；并要求点击「开槽布管」前置任务区域可跳转到该任务详情页。
- 原型改动（`service-miniapp/task-detail.html` + `task-detail.js`，最小安全）：
  ① 删除前置任务条目的「预计完成：01-18」（meta 改为仅「执行人：张工」）；
  ② `pre-task-item` 加 `cursor:pointer` + `onclick="goToPreTaskDetail()"` + title，新增 `goToPreTaskDetail()` 函数（演示跳转 `task-detail.html?status=in_progress&role=executor`，即前置任务「开槽布管」进行中态；并挂 `window.goToPreTaskDetail`，与既有 `window.*` 调用约定一致），注释说明真实环境应带 `?taskId=<preTaskId>`；
  ③ 简化等待描述：`block-desc` 由「当前任务设置了按序执行，需等待前置任务完成后才能开始执行」缩为「需等待前置任务完成后才能开始执行」；`preTaskBlockTip` 动态文案由「前置任务完成后将自动通知…」缩为「完成后将自动通知…」（执行人视角「您」/ 配置人视角「执行人」）；顶部 confirmProgress「等待前置任务完成」保留作状态行。
- PRD 修订（`PRD-任务模块详细规格.html` §3.13 逻辑段）：卡片提示改为「完成后将自动通知执行人」，并补「前置任务条目可点击，跳转至该前置任务的详情页」；原文无「预计完成」表述，无需删。
- 校验：node --check 通过；HTML Python 精确替换断言每处命中 1 次，`预计完成` 全文档归零；`onclick="goToPreTaskDetail()"` 就位。
- 改动文件：`service-miniapp/task-detail.html`、`service-miniapp/js/task-detail.js`、`PRD-任务模块详细规格.html`（§3.13）。

## §3.12/§3.13 执行人视角「提交确认/完成任务」前置上传校验
- 用户要求：服务方任务详情页「待开始」与「待开始（前置阻塞）」的执行人视角，未上传执行记录前不支持点击「提交确认/完成任务」，或点击提示先上传记录。在原型补充，不影响其他功能。
- 原型数据模型：`executionRecordsList`（line 81，模块级数组）记录执行记录；待开始态该数组恒为空，首次上传（`confirmUploadRecord` line 719 push + 725 流转 IN_PROGRESS）后才非空。
- 原型改动（`service-miniapp/js/task-detail.js`）：
  ① `renderPending` 执行人分支（line 1254）：以 `const canSubmit = executionRecordsList.length > 0` 判定——无记录时「提交确认/完成任务」渲染为 `btn primary disabled`（opacity:0.5、cursor:not-allowed、title 提示），无 onclick；有记录才渲染可点击按钮调用 `showSubmitConfirmModal`。
  ② `showSubmitConfirmModal`（line 877）顶部加中央守卫：`if (executionRecordsList.length === 0) { showToast('请先上传执行记录，再提交确认 / 完成任务'); return; }`——任何路径提交前均校验，作为安全网（覆盖 执行中 等态不会误伤，因其记录非空）。
  ③ 「待开始（前置阻塞）」执行人视角（line 1343）本就只有禁用态锁按钮「🔒 前置任务未完成」、无「提交确认/完成任务」入口，故该约束天然满足，原型未改动该分支。
- 校验：node --check 通过；`renderInProgress` 执行人提交按钮（line 1469）仍调 `showSubmitConfirmModal`，因 IN_PROGRESS 记录非空不被守卫拦截，正常流转不受影响。
- PRD 修订（`PRD-任务模块详细规格.html`）：§3.12 执行人视角行补「未上传执行记录时提交按钮置灰不可点击」、§3.12 效果补同一说明；§3.13 效果补「前置阻塞态执行人无提交入口、约束天然满足」。
- 改动文件：`service-miniapp/js/task-detail.js`、`PRD-任务模块详细规格.html`（§3.12、§3.13）。

## 合同模块 §3.12 重构为「按原型状态单独章节」
- 用户指出：`PRD-合同模块详细规格.html` 的 §3.12「各状态页面效果与功能规格」仍是「按生命周期阶段分组」的大表格（3.12.1–3.12.5 + 3.12.6/7/8 通用规则），没有按原型「合同状态切换」每个状态单独成节，且每个状态需附原型页链接、需求描述需含操作/流程/交互/字段规格；要求在不影响其他功能模板页面（§3.8/3.9/3.10/3.11/3.13）前提下调整补充。
- 原型核查（`service-miniapp/contract-detail.html` 右侧「合同状态切换」nav + `js/contract-detail.js` `updateContractStatus`/动作映射）：原型可预览态 = draft / draft_submittable / platform_reviewing / platform_rejected / confirming_sender / confirmed / signed / changing / change_platform_reviewing / change_platform_rejected / change_confirming_sender / change_signing_wait（12 项）；其余 draft_party_a / reviewed_pass / reviewed_reject / platform_rejected_modified / confirming_receiver / confirmed_party_a / change_confirming / change_confirming_receiver / change_confirmed 为特定数据视角态（演示开关未单列）。变更流转：发起变更含金额/正文/附件→change_platform_reviewing（需平台审核），仅阶段任务→changing（免审）；确认方确认→change_confirming_sender；上传签约→change_signing_wait→change_confirmed→回到 signed(V2)。
- 重构内容（`PRD-合同模块详细规格.html`，仅 §3.12 + 目录标题）：
  - **§3.12 重写**：标题「各状态页面效果与功能规格（按原型状态拆分）」；引言说明逐状态成节 + 原型链接约定（contract-detail.html 右侧「合同状态切换」预览，未单列态需加载演示数据）；**3.12.1 通用显示规则**（合并原 3.12.6/7/8：流程图5步高亮 / 阶段任务显示 / 变更记录选项卡）；**3.12.2–3.12.14** = 13 个非变更态逐状态成节（draft / draft_party_a / draft_submittable / platform_reviewing / reviewed_pass / reviewed_reject / platform_rejected / platform_rejected_modified / confirming_sender / confirming_receiver / confirmed / confirmed_party_a / signed）；**3.12.15–3.12.22** = 8 个变更态逐状态成节（changing / change_confirming / change_platform_reviewing / change_platform_rejected / change_confirming_sender / change_confirming_receiver / change_signing_wait / change_confirmed）。
  - 每节统一含：🔗 原型页链接（预览/数据态说明）、适用方/视角、横幅文案、横幅说明文字、进入条件、页面结构与可见模块、底部操作按钮、状态流转、关键交互与逻辑（含字段规格 where needed）。
  - 原 3.12.6/7/8 删除，改为 §3.12 末尾 note 指向 3.12.1 + §3.8/§3.9/§3.10。
  - 目录 3.12 标题同步更名。
- **歧义预警（待用户确认）**：本轮回应用户「§3.12 每个状态单独章节」指令，将 8 个变更态也一并写入 §3.12（3.12.15–3.12.22），未单列为独立一级「合同变更」章——这与用户早前「合同变更单独一级标题」诉求不一致，且会与既有的基础合同「第五章 发起变更功能」形成内容分工（§3.12 讲变更态页面、第五章讲发起变更功能/字段/平台审核，互补非完全重复）。若用户仍要「变更单独一级标题 + §3.12 去变更态」，需再抽取并去重。
- 校验：Grep 确认无 `sec-3-12-[2-8]` 孤立锚点引用；新增交叉引用 #sec-3-7/#sec-3-8/#sec-3-9/#sec-3-10/#sec-3-13 锚点均存在；HTML 标签闭合正确；§3.8/3.9/3.10/3.11/3.13 及非合同模块章节零改动。
- 改动文件：`PRD-合同模块详细规格.html`（§3.12 + 目录）。

## §3.14 执行中：补「提交确认 / 完成任务」操作流程与交互
- 用户指出：§3.14 执行中 缺少执行人点击「提交确认」「完成任务」的操作流程与交互（弹窗提示内容、按钮项、点击后续效果），要求分别补充。
- 原型核查（`service-miniapp/js/task-detail.js` + `task-detail.html`）：
  - 底部主按钮文案由 `currentConfirmers.length > 0` 动态决定：有确认人→「提交确认」，无确认人→「完成任务」；两者共用 `submitConfirmModal` 弹窗，`showSubmitConfirmModal()`（line 877）按有无确认人切换标题/文案/按钮/确认人列表显隐。
  - 弹窗结构（HTML line 25935）：标题 `submitModalTitle`、📤 图标、主文案 `submitModalText`、硬编码警示「⚠️ 提交后不支持修改执行记录」、确认人列表区 `submitModalConfirmerList`（有确认人显「确认人列表：」+ 各项，无则隐藏）、底部按钮「取消」（closeSubmitConfirmModal 关闭无变更）+「确认提交/确认完成」（confirmSubmitToConfirmer）。
  - `confirmSubmitToConfirmer()`（line 908）：有确认人→toast「已提交确认，请等待确认」+ `updateTaskStatus(CONFIRMING)`（确认中）；无确认人→toast「任务已完成」+ `updateTaskStatus(COMPLETED)`（已完成）。
- PRD 修订（`PRD-任务模块详细规格.html` §3.14 交互节）：
  - 新增「两按钮共用同一提交确认弹窗、按有无确认人切换」说明 + 提交前置条件（须先上传记录，引 §3.12）。
  - （1）提交确认流程：触发弹窗→弹窗提示内容（主文案/警示/确认人列表区）→按钮项（取消/确认提交）→点击确认提交后续（关闭弹窗→toast→流转确认中→通知确认人→引 §3.15）。
  - （2）完成任务流程：触发弹窗→提示内容（主文案/警示/确认人列表区隐藏）→按钮项（取消/确认完成）→点击确认完成后续（关闭弹窗→toast→直接流转已完成）。
  - note 补「提交后执行记录只读、后续态不再提供提交入口、引 §3.15/§3.16」。
- 遵守英文规范：仅保留「确认中（confirming）」「已完成（completed）」「执行中（in-progress）」中文前置状态 key；无裸英文字段标识符（Grep `<code>(files|desc|name|type|size|...)</code>` 零命中）。
- 改动文件：`PRD-任务模块详细规格.html`（仅 §3.14 交互节，未动原型）。

## §3.14 样式修正 + §3.15 配置人权限与表单字段规格
- 用户反馈：① §3.14 新增内容「左侧已超出标题内容」需修样式（文档规范）；② §3.15 确认中 配置人视角不可增删确认人，删除「可增删确认人（此态原型仍保留入口，但主操作在确认人侧）」；③ 补充确认/驳回表单字段规格（必填性、校验规则）。
- 样式修复（PRD `<style>` + §3.14 HTML）：新增 `.subsection ul { list-style-position: inside; padding-left: 1.4em; }` + `.subflow`（左 border + padding-left 14px 容器）+ `.subflow-title`（14px/600 子流程标题）；将 §3.14 两条子流程（(1) 提交确认 / (2) 完成任务）的 `<p><strong>` 改为 `<div class="subflow"><p class="subflow-title">…</p><ul>…</ul></div>`，杜绝列表序号/内容溢出内容列左侧。
- §3.15 配置人行改写：「只读；操作栏「等待确认人确认」；本态不可增删确认人（确认人配置仅在待配置 / 待开始 / 执行中态开放，确认中主操作在确认人侧）」。
- §3.15 新增「确认 / 驳回表单字段规格」表（表单 | 字段 | 必填性 | 校验规则 | 说明）：
  - 确认弹窗字段（均选填、confirmTask 不校验）：媒体附件（图片/视频/音频）、评语（文本域）、综合评星（1–5 星含半星）、评价标签（责任心棒/服务态度好/专业能力强 可多选）、按钮项（取消/确认通过）；点击「确认通过」即记录，无强制必填。
  - 驳回弹窗字段：媒体附件（选填）、驳回原因（必填，≤500 字符，由 `Validation.validate.textLength` 校验、为空或超出均拦截轻提示，rejectTask 调用）、按钮项（取消/确认驳回→流转 驳回后待开始（rejected_pending））。
- 英文规范：状态 key 中文前置（驳回后待开始（rejected_pending））、代码表达式（confirmTask / rejectTask / Validation.validate.textLength）置于 `<code>`；无裸英文字段标识符。
- 原型核查依据：`service-miniapp/task-detail.html` 的 `confirmModal`/`rejectModal` 结构与 `js/task-detail.js` 的 `confirmTask`/`rejectTask`/`showConfirmModal`/`showRejectModal`，未改动原型。
- 改动文件：`PRD-任务模块详细规格.html`（§3.14 样式、§3.15 配置人权限行 + 表单字段规格表）。

## §3.14 提交后记录生成规则（执行记录 + 动态记录）
- 用户需求：执行人点击「提交确认」或「确认完成」后续，应各生成一条执行记录（提交快照）+ 一条动态记录（将执行记录汇总到一条动态内、时间 = 提交确认时间）；要求补充「生成执行记录规则」与「生成动态规则」。
- 原型核查：`service-miniapp/activity-list.html` 存在独立「项目动态」页，数据模型字段为 user/avatar/avatarColor/time/role/roleClass/type/typeLabel/content/source/hasMedia/mediaCount/hasPermission/date/dateLabel；`task-detail.js` 的 `confirmSubmitToConfirmer` 当前仅流转状态、未自动生成动态（属待补实现，本次仅改 PRD 文档化该规则，未动原型）。
- PRD 改动（§3.14）：
  - 在 (1) 提交确认 / (2) 完成任务 两子流程的「后续」末尾，各加一句指引：同时按「提交后记录生成规则」生成一条执行记录（提交快照）与一条动态记录（时间 = 提交确认时间）。
  - 新增 `<h4>提交后记录生成规则</h4>` + 两个 `.subflow` 子节：
    - (3) 执行记录生成规则（提交快照）：生成时机（提交同时、时间=提交确认时间）、记录内容（汇总执行中全部上传记录为一条不可改删的提交快照）、关键字段（提交动作/提交人/提交时间/files(name/type/size)/desc/来源任务）、说明（聚合封装不改变原逐条记录）。
    - (4) 动态记录生成规则（项目动态）：生成时机（同执行记录、归入 activity-list 对应日期分组）、聚合规则（汇总到一条动态、非逐条上传）、动态记录字段表（操作人 user / 角色 role+roleClass / 类型 type=任务类动态(task) / 类型标签 typeLabel=任务提交或任务完成 / 内容 content / 来源 source / 时间 time+日期 date+日期标签 dateLabel / 是否有媒体 hasMedia+媒体数量 mediaCount / 是否有权限查看 hasPermission），注明系统自动生成、无用户填写入口与校验规则。
- 英文规范：字段名一律「中文（英文标识符）」前置（操作人（user）/ 类型（type）/ 角色（role）/ 内容（content）/ 来源（source）/ 时间（time）/ 类型标签（typeLabel）/ 是否有媒体（hasMedia）/ 媒体数量（mediaCount）/ 是否有权限查看（hasPermission）/ 日期（date）/ 日期标签（dateLabel）/ 头像（avatar）/ 头像底色（avatarColor）/ 角色样式（roleClass）/ 文件大小（size）/ 执行说明（desc））；`task` 写作「任务类动态（task）」、`worker` 写作「施工（worker）」、`activity-list` 为文件名保留英文；Grep 自查 §3.14 新区块无裸英文字段标识符（仅既有 §3.9 URL 参数片段 `role` 合规）。
- 改动文件：`PRD-任务模块详细规格.html`（§3.14 交互节新增「提交后记录生成规则」+ 两子流程后续交叉引用）。

## §3.14 执行记录生成规则修正（去掉汇总/快照）
- 用户反馈：§3.14「执行记录生成规则（提交快照）」中"全部执行记录汇总为一条提交快照执行记录"不符合原型效果，应改为"根据原型效果生成简单记录即可（不需要汇总）"。
- 原型核查：`task-detail.js` 的 `confirmSubmitToConfirmer`（908–919）仅 closeModal + showToast + updateTaskStatus，**不**对 `executionRecordsList` 逐条上传记录做任何汇总/合并；上传记录保持各自独立存在。故"提交快照聚合"是对原型的误述。
- PRD 修正（§3.14）：
  - 子节标题由「（3）执行记录生成规则（提交快照）」改为「（3）执行记录生成规则（提交动作记录）」。
  - 记录内容：改为"系统生成一条「提交动作」执行记录（记录提交行为本身：提交动作/提交人/提交时间/来源任务），不对既有上传记录做汇总/合并；执行中阶段逐条上传记录保持原样、各自独立存在，不被封装为单条快照"。
  - 关键字段：删去 files(name/type/size)/desc 汇总项，仅保留 提交动作/提交人/提交时间/来源任务。
  - 说明：改为"该记录仅标识「执行人已提交」动作节点；既有逐条上传记录继续保留、可被确认人逐一查看、提交后全部只读"。
  - 同步清理交叉引用：(1)(2)「后续」的"执行记录（提交快照）"→"「提交动作」执行记录"；开头段落、动态(4)生成时机、"内容（content）"来源、"说明"处的"提交快照"残留一并改为「提交动作」执行记录 / 提交动作记录。
  - Grep 确认全文"提交快照/汇总为一条/快照聚合"零残留；§3.12 的 媒体文件（files）等字段规格属合规、未动。

## §3.14 动态记录生成规则·汇总补充（媒体汇总 + 文案统一）
- 用户需求：§3.14「动态记录生成规则」关于"汇总"补充详细描述——**媒体文件汇总**，但**其余描述文字不汇总**，统一该操作的动态内容为"完成了{接单名称}{任务名称}的施工"。
- PRD 修正（§3.14 子节 (4)）：
  - 聚合规则重写：动态仅对执行记录中的**媒体文件做汇总**（合并为一条媒体集合，是否有媒体（hasMedia）/ 媒体数量（mediaCount）取合并值）；**文字描述不逐条汇总**，统一为标准动态文案 执行人「完成了{接单名称}{任务名称}的施工」（{接单名称}=所属合同/服务名称（来源）、{任务名称}=当前任务名），不再生成"提交 N 条、含 M 个媒体"之类的逐条汇总描述。
  - 内容（content）字段改写：取值/说明 = 固定文案「完成了{接单名称}{任务名称}的施工」+ 媒体文件按汇总后媒体集合呈现（hasMedia/mediaCount 取合并值）；来源 = 由提交动作记录 + 接单名称 + 任务名称生成。
  - 全文 Grep 确认"提交快照/汇总到一条/汇总说明"零残留；英文标识符一律「中文（英文标识符）」前置（是否有媒体（hasMedia）/ 媒体数量（mediaCount）/ 来源（source）/ 类型（type）/ 任务类动态（task）等），`activity-list` 为文件名保留；无裸英文字段标识符。
- 改动文件：`PRD-任务模块详细规格.html`（§3.14 子节 (4) 聚合规则 + 内容（content）字段行）。
- 改动文件：`PRD-任务模块详细规格.html`（§3.14「提交后记录生成规则」及 (1)(2)(4) 交叉引用）。

## 第五章新增 §5.15 各变更状态页详述 + §3.12 变更态去重
- 用户要求：截图内 5 个变更状态（变更中(发起方)/变更审核中/变更审核驳回/变更确认中(发起方)/变更签约中）需在「发起变更功能详细规格」（第五章）下有独立且详细的需求说明（每状态单独章节、含原型链接、操作/流程/交互/字段规格），同时消除与 §3.12 的重复。
- 改动（`PRD-合同模块详细规格.html`）：
  1. **第五章目录更新**：在 §5.14 后新增 §5.15「各变更状态页详述」+ 8 个叶子目录项（5.15.1–5.15.8：变更中/待确认变更/变更审核中/变更审核驳回/变更确认中(发起方)/变更确认中(确认方)/变更签约中/变更已生效），加 `toc-leaf` 缩进样式。
  2. **§5.15 新增（8 节逐状态详述）**：每节含 🔗 原型链接（contract-detail.html 右侧导航切换或演示数据加载说明）、适用方/视角、横幅文案、进入条件（含 needPlatformReview 分支逻辑）、页面结构与可见模块（摘要卡片/高亮/驳回原因区域等按状态差异）、底部操作按钮、状态流转（含跨节锚点）、关键交互逻辑（阶段任务暂停/平台审核被动等待/V2 版本生成/变更记录闭环/可再次变更）、字段规格（5.15.7 含签约文件上传完整字段）。内容依据 `contract-detail.js` 的 `updateContractStatus` 与 `STATUS_CONFIG` 真实值核对。
  3. **§3.12 变更态去重**：原 3.12.15–3.12.22（8 张变更态简表 + 注释段落）整段删除，替换为一句话导航索引指向 §5.15.x；§3.12 引言已在前轮改为"本章不再另列变更态子节"。
  4. **§5.1–5.14 未动**：功能入口/弹窗结构/检测机制/校验/原因输入/阶段编辑/提交流程/按钮控制/状态流转表/暂停机制/变更记录/权限/异常/API 全部保留为第五章"功能规格"部分，与 §5.15"状态页规格"互补。
- 校验：Grep 确认 3.12.15-22 零残留；sec-5-15 / sec-5-15-[1-8] 锚点全部存在；HTML 标签闭合正确；章节衔接（第三章→第四章→第五章→全局规格）正常。
- 改动文件：`PRD-合同模块详细规格.html`（第五章 TOC+§5.15 新增、§3.12 变更态删除）。

## §3.15 确认中·确认/驳回后记录生成规则（参照 §3.14 风格）
- 用户需求：确认人确认或驳回后需生成一条动态（含全部确认→任务已完成动态），且确认/驳回后均需生成确认记录，补充相关规则说明，风格参照 §3.14 的执行记录与动态规则。
- PRD 修正（`PRD-任务模块详细规格.html`，§3.15）：
  1. **交互（交互 bullet）交叉引用**：确认通过 bullet 末尾加"同时按下方规则生成一条确认记录与一条动态记录（时间=该确认人确认时间；若本次为最后一人确认，则动态为「任务已完成」）"；驳回 bullet 末尾加"同时按下方规则生成一条驳回确认记录与一条「任务驳回」动态记录（时间=驳回时间）"。
  2. **新增「确认 / 驳回后记录生成规则」子节**：承接 §3.14 风格，含 (3) 确认记录生成规则（确认/驳回结果记录，字段对应 §8.3 确认记录表字段（task_confirmations）：确认人（confirmer_id）/ 确认结果（status）：已确认（confirmed）/ 已驳回（rejected）/ 确认备注（remark）/ 确认时间（confirmed_at）/ 媒体附件（files）等，逐人独立、不汇总、始终留存）+ (4) 动态记录生成规则（项目动态（activity-list），字段表与 §3.14 同构：操作人（user）/ 角色（role）/ 类型（type）= 任务类动态（task）/ 类型标签（typeLabel）/ 内容（content）/ 来源（source）/ 时间（time）/ 是否有媒体（hasMedia）/ 媒体数量（mediaCount）/ 是否有权限查看（hasPermission））。
  3. **动态文案细分**：单条确认「{确认人姓名} 确认了 {接单名称}{任务名称}」（类型标签=任务确认）；驳回「{确认人姓名} 驳回了 {接单名称}{任务名称}」（类型标签=任务驳回）；**最后一人确认（全部确认）→ 任务已完成**：「完成了 {接单名称}{任务名称}的施工」（类型标签=任务完成，与 §3.14 完成任务动态完全一致）；确认/驳回动作不产生媒体，hasMedia=false、mediaCount=0。
- 校验：英文标识符全「中文（英文标识符）」前置，新块零 `<code>`；HTML 标签闭合正确；§8.3 字段交叉引用一致。
- 改动文件：`PRD-任务模块详细规格.html`（§3.15 交互交叉引用 + 新增「确认 / 驳回后记录生成规则」子节 (3)(4)）。


