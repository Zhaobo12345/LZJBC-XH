# 2026-09-09

## 09:40 复制发起升级为独立页面（方案 A 确认后实施）
- 用户需求：contract-copy-initiate-demo.html 的「复制发起」抽屉扩展字段口径——①甲乙方可选（甲方默认当前甲方、乙方无默认且候选=项目内匹配合同类型工种角色人员）②文本模板沿用并可更换模板③补充条款沿用可修改④阶段任务沿用并可更换模板⑤附件沿用可修改；并问浮窗 vs 独立页面体验。给 A/B/C 三方案对比，用户确认 **方案 A 独立页面**。
- 新增 `service-miniapp/contract-copy-initiate.html`（独立页，基于 create-contract.html 布局 + 沿用/已调整徽标体系）：
  - 合同基本信息：架构层级只读、类型🔒随复制固定、名称预填可改
  - 合同方：甲方 picker 默认陈庄（工长）（候选=陈庄/孙工长/刘项目总，改回陈庄徽标复位）；意向乙方 1-3 人无默认，候选=项目内水电工（韩水通/杨水明/吴水安，张水电🔒同类型在途合同中）
  - 金额/工期预填沿用可改（badge）；文本模板卡（查看/更换模板→同类型模板选择抽屉→预览抽屉，更换后补充条款按新模板重置）
  - 补充条款 textarea 沿用可改；阶段任务沿用清单（删除/添加/🔄更换任务模板整体替换 STAGE_TEMPLATE）；附件沿用（删除/上传）
  - 底部 取消/确认发起；校验名称/乙方1-3/金额/工期；确认后差异对比结果抽屉（沿用/已调整/已更换逐项 + 查看新合同）
  - 微信导航栏规范、375×812、全局无滚动条
- `contract-copy-initiate-demo.html`：更多菜单「复制发起」改跳转 `contract-copy-initiate.html`；删除旧复制抽屉 HTML/JS（COPY_CANDIDATES~confirmCopyInitiate）与 .copy-* CSS；原页其余布局零改动
- 校验：两页 BALANCED、CSS 102/102 与 240/240、node --check JS_OK、旧抽屉引用零残留
- 待办：architecture-multi-contract-demo.html 的复制发起入口/抽屉是否同步跳独立页（未动，待用户定）；PRD 同步待验收后进行

## 2026-09-09 09:55 复制发起页改为基于拟定中页布局
- 用户需求：contract-copy-initiate.html 布局须与「拟定中（发起方）-新」一致（避免同内容多布局），底部三按钮=取消/仅保存/提交并邀请乙方。\n- 重建 contract-copy-initiate.html = worker-contract-draft-initial-new.html 完整复制后叠加复制特有功能（原拟定中页零改动）：\n  - 复制来源说明卡（copy-source-card）+ 导航标题改「复制发起」\n  - 甲方默认陈庄（徽标「默认当前甲方」，改他人→已调整、改回复位）；意向乙方无默认（editInvited=[]，徽标「待选择（无默认）」），候选水电工中张水电🔒禁用（同类型在途合同中）\n  - 沿用/已调整徽标：名称/金额/工期/违约责任/补充条款/阶段任务/附件/文本模板 8 处（markAdjusted + stagesDirty/attsDirty 脏标记挂接全部任务/附件修改函数）\n  - 补充条款 C.extraClauses 预填原合同值（成品保护/隐蔽验收）；文本模板沿用+「更换正文模板」/「更换任务模板」沿用拟定中原功能\n  - 底部三按钮：doCancelCopy（返回原合同详情）/ doSave（已保存草稿）/ doSubmit（校验后 toast + showCopyDiffModal 差异对比弹窗：甲方/乙方/名称/金额/工期/模板/违约责任/补充条款/阶段/附件逐项 沿用·已调整·已更换）\n- .cta-secondary flex 110→88px 适配三按钮；新增 .copy-badge/.copy-source-card CSS\n- 校验：BALANCED、CSS 199/199、node --check JS_OK、11 项关键点检查全 PASS；git 确认仅 demo 页与新页改动，worker-contract-draft-initial-new.html 未动\n
## 2026-09-09 10:15 架构多合同演示页重建(以原页为底本,完整保留原功能)\n- 用户反馈：演示页破坏了原架构页功能（如无合同组却出现子级架构，违背「无合同不能创建施工组」规则）。\n- architecture-multi-contract-demo.html = 当前 architecture.html(2022行) 完整复制重建后叠加（旧演示页废弃重写）：\n  - 原有功能零删减：邀请成员(平台搜索/二维码/微信+服务/业主分类)/添加工作组·施工组/退出项目部·工作组/删除/查看合同/权限开关\n  - 原规则保留：showCreateModal level2 前置「无合同不能添加施工组」(contractsOf(parent).length)；插入新节点逻辑不变\n  - 新需求叠加：groupContracts 组名→数组(水电1份/泥瓦2份/木工油漆空)+contractsOf/hasSignedContract 工具；退出守卫 some(已签约)、删除守卫 length>0；水电/泥瓦节点徽标「已签约·N份合同」+「📋复制发起」操作项；viewContract 列表化+聚合行(共N份·已签约X份+复制发起按钮)；goCreateContract 有合同→弹「复制发起(推荐)/空白新建」选择(appModalCancel 文案覆写+resetCreateChoiceButtons 复位防残留)；copyContract 跳 contract-copy-initiate.html?group=&from=architecture\n  - 头部新增「N份合同」统计+demo-note 演示说明卡(明示保留原规则)\n- 校验：BALANCED、CSS 155/155、node --check JS_OK、13项检查 12 PASS+1计数口径误报(实际6处引用全对)、原 architecture.html 未改动\n
## 2026-09-09 10:35 架构演示页：说明卡移框外右侧+复制发起改按份\n- 演示说明卡从 page-content 移出→position:fixed right:20 bottom:20（宽300，右侧便于查看，不占手机框内空间；z-index 999 低于 page-nav 1000）。\n- 复制发起逻辑明确为「按份」：viewContract 聚合行仅统计+提示文案，每份合同行内各带「📋复制发起」(.ci-copy)，蓝本=该份合同；copyContract(groupName, contractId) 带 source 参数；节点级模糊「复制发起」操作项移除（避免指向不明）；goCreateContract「创建方式」弹窗确认→默认以组内最新已签约份为蓝本（弹窗文案明示，也可在查看合同中任选）。\n- 教训：present_files 预览会给文件注入 data-page-node-id 属性，二次编辑前必须重新 Read 最新内容。\n- 校验：BALANCED、CSS 156/156、node --check JS_OK、8项检查全 PASS。\n
## 2026-09-09 10:50 架构演示页：说明卡改flex并排+查看合同弹窗紧凑排版\n- 说明卡 fixed 右下在窄视口会盖住居中手机框→改 .demo-stage flex容器(flex-wrap:wrap, gap:16px)包住 phone-frame+aside.demo-note(250px)：宽屏在框右侧，窄屏换行到框下方，任何视口不遮挡；.demo-stage .phone-frame{margin:0}。\n- 查看合同弹窗紧凑化：合同条目改两行结构——第一行=名称(flex单行省略)+「📋复制发起」小按钮(10px/12px圆角)，第二行=状态·乙方·进度(单行省略)；聚合行改「共N份·已签约X份 | 复制发起以所选份为蓝本」11px 单行；contractList 容器 padding 12/16→10/12。消除截图中的多行折行与大空白。\n- 校验：BALANCED、CSS 165/165、node --check JS_OK、8项检查全 PASS。\n
## 2026-09-09 architecture-multi-contract-demo.html 三处修复
- 演示说明卡改「固定右侧」：`.demo-stage` flex-wrap:wrap→nowrap，`.demo-note` width→`flex:0 1 250px; min-width:180px`（窄屏自身收缩，不再换行到框下方）；phone-frame 加 flex-shrink:0。
- 查看合同弹窗滚动区隐藏滚动条（小程序规范）：`#contractModal .modal-content` 三件套（webkit/scrollbar-width/-ms）。
- 弹窗内容横向溢出根因：`.contract-item .info` 缺 `min-width:0`（flex 子项不收缩，长名称/状态行撑破 280px 弹窗），补上即可配合既有 nowrap+ellipsis 生效。
- 校验：HTML BALANCED；CSS 167/167；残留 flex-wrap:wrap 4 处均为原 architecture 页自身样式，勿动。

## 2026-09-09（续）复制发起新增「隶属架构层级」字段
- 页面：contract-copy-initiate.html（两个演示页 architecture-multi-contract-demo.html / contract-copy-initiate-demo.html 均经此独立页发起，故改动集中于此页）。
- 新增独立卡片「🏗️ 隶属架构层级」：默认=被复制合同所属层级（架构页带入 group 参数；详情页用蓝本 C.group），可点「更换 ›」底部抽屉选择同项目其他层级；改选后徽标变「已调整」、Hint 提示已更换。
- 候选层级列表：base 五类工种施工组 + 若默认项不在列表则自动置顶；属「工作组」层级时描述提示归口上级。
- 默认参数解析：新增 getQueryParam，defaultArchLevel = group||C.group；选入复制差异对比弹窗（showCopyDiffModal）新增「隶属架构层级」行，提交 toast 附带层级。
- 未改动其他已有页面/功能；架构演示页 copyContract 已带 group 参数（如 水电工作组），详情演示页不带参数走默认。

## 2026-09-09（续2）隶属架构层级补充「层级 + 完整路径」
- 痛点：原仅显示单一名（如「水电工作组」），三级架构下无法辨别所属层级与归属路径。
- 改为三级数据模型：ARCH_ROOT（一级项目总架构，不可选）+ ARCH_LEVEL_MAP（二级施工组 + 三级子组，各带 level/parent），archFind/archPath 生成「项目总架构 › 上级 › 自身」面包屑。
- 卡片：新增层级徽标（二级=蓝 / 三级=青，al-badge/al-badge-l3）+ 路径行 archLevelPath（单行省略）；选择弹窗每项显示「徽标 + 名称 + 默认标 + 完整路径 + 描述」。
- 提交 toast 与差异对比弹窗「隶属架构层级」行均改用 archPath 完整路径。
- 仅改 contract-copy-initiate.html（两演示页经此页发起，自动生效）；校验 HTML BALANCED / CSS 221/221 / 内联 JS node --check OK。

## 2026-09-09（续6）工人合同详情「更多」菜单新增「复制发起」入口
- 页面：worker-contract-detail.html（合并页，六工种合同通用）。
- renderMoreOps(status) 改为：当 state.viewer==='sender'（发起方视角）时，在更多菜单首位插入「复制发起」（📑 + op-extra「快捷发起同类型新合同」），其余非终态状态维持 版本记录/变更记录/导出；受邀方确认中(worker_inviting_receiver)仍仅 导出；受邀方终态工具栏本就隐藏，不受影响。
- 新增 WCP.openCopyInitiate()：跳 contract-copy-initiate.html?from=detail&source=<workerId>。未传 group（详情页 c.group 为「水电工程」式命名，与复制页架构节点「水电施工组」式命名不一致，避免注入多余层级节点）。
- HTML 加 .more-ops-panel .op-item .op-extra{margin-left:auto;...} 样式；校验 HTML BALANCED / 内联 JS node --check OK。仅改本页，不影响其他页面与功能。

## 2026-09-09（续7）三张发起方 -new 页「更多」补「复制发起」入口
- 上一轮仅在合并页 worker-contract-detail.html 加了入口；用户反馈「拟定中（发起方）-新 / 确认中（发起方）/ 已签约（发起方）-新」三张 -new 页「更多」无此入口。
- 三张页各自 inline renderMoreOps()，无 openCopyInitiate：draft-initial-new(1793 行 items 数组)、confirming-initiator-new(824 行)、signed-initiator-new(901 行)。
- 改动（每页三处）：① CSS 加 .more-ops-panel .op-extra（右对齐小字）；② renderMoreOps 首位插入「📑 复制发起」+ op-extra「快捷发起同类型新合同」（draft 用 items 数组+map 渲染 extra，另两页用字符串拼接）；③ 新增 openCopyInitiate(){ location.href='contract-copy-initiate.html?from=detail'; }。
- 注意：Edit 对「.more-ops-panel .op-icon{font-size:15px}」CSS 三页首轮均静默未落盘，已逐页复核重补；confirming 页 renderMoreOps 首轮亦漏，已重补。校验 HTML BALANCED / 内联 JS node --check OK。
- 仅改三张 -new 页，不影响其他页面与功能；跳转不带 group（详情/各 -new 页合同 group 命名与复制页架构节点命名体系不同，避免注入多余层级）。

## 2026-09-09（续）拟定中提交→确认中按所选乙方动态展示
- 需求：拟定中（发起方）-新 提交并邀请乙方后，确认中（发起方）页按拟定中所选乙方人数展示，并标识状态。
- 拟定中页 worker-contract-draft-initial-new.html：doSubmit 校验通过后 `encodeURIComponent(JSON.stringify(editInvited))` 拼入跳转 URL `worker-contract-confirming-initiator-new.html?invites=...`（editInvited 为 [{userId,name,role}]）。
- 确认中页 worker-contract-confirming-initiator-new.html：① 意向乙方容器加 `id="inviteChips"`（默认 chip 作无参兜底）；② 新增 `getQueryParam` + `renderInvitedFromQuery()`，init 调用；读 invites 后按数组渲染对应数量 chip（首字头像 + 姓名（角色）+ `待确认` 状态徽标），并更新标题为「意向乙方（已邀请 N 人，待确认）」；③ 新增 `.chip-status` / `.pending` / `.confirmed` 样式（confirmed 预留，当前 确认中 态全为 待确认）。
- 校验：两页 node --check JS_OK、HTML BALANCED；grep 确认 invitesParam/renderInvitedFromQuery/id=inviteChips/chip-status pending 均落盘。

## 2026-09-09（续）拟定中（发起方）-新 提交后跳确认中页
- 需求：worker-contract-draft-initial-new.html 点击「提交并邀请乙方」，校验通过后气泡提示消失后自动切到「确认中（发起方）」页。
- 改动：doSubmit() 校验全通过后 showToast('已提交并邀请乙方（N 人）') 之后，追加 setTimeout(2300ms，略长于气泡 2200ms) → window.location.href='worker-contract-confirming-initiator-new.html'。
- 校验：node --check JS_OK、HTML BALANCED；grep 确认跳转语句就位。仅改本页，不影响其他功能。

## 2026-09-09（续2）复制发起页提交后取消差异对比弹窗、直接跳确认中页
- 需求：contract-copy-initiate.html 点击「提交并邀请乙方」校验通过、气泡消失后，**不再展示「复制发起结果（差异对比）」**，直接跳「确认中（发起方）」页。
- 改动：doSubmit() 原 `setTimeout(showCopyDiffModal, 400)` → 改为 `setTimeout(2300ms)` 跳 `worker-contract-confirming-initiator-new.html?invites=` + `encodeURIComponent(JSON.stringify(editInvited))`（与拟定中页同口径，确认中页可据此渲染所选乙方人数与「待确认」状态）。
- 差异对比弹窗 `showCopyDiffModal()` 已全量删除（grep 确认除自身定义外零引用，属死代码；遵循项目「下线功能直接删除，不留版本历史」约定）。如需恢复可重写（原逻辑基于 badgeName/badgeAmount/badgeDuration/badgeBreach/badgeExtra/badgeTplText/badgePartyA 的 adjusted 类 + stagesDirty/attsDirty 脏标记，逐行输出 沿用/已调整/已更换）。
- 校验：node --check JS_OK、HTML BALANCED；grep 确认跳转落盘且 `showCopyDiffModal` 已零残留。仅改复制发起页，不影响其他页面与功能。

## 2026-09-09（续3）演示页「复制发起」能力回迁 architecture.html（正式页）
- 需求：把 architecture-multi-contract-demo.html 在「项目架构」页基础上新增的复制发起功能同步进 `service-miniapp/architecture.html`，且不破坏原页任何功能/交互。
- 关键方法：**先洗后 diff**。演示页被预览工具注入了 312 个 `data-page-node-id`，原始 diff 1055 行几乎全是噪声；用 Python 正则剔除该属性并按行 rstrip 归一后再 diff → 收敛到 355 行 / 15 区块，方才看清真实增量。（原 architecture.html 自身含 0 个该属性。）
- 确认可用规模 proceeded 的事实：architecture.html 时间戳 9/4 早于演示页 9/9 的复制基线，说明原页此后再未改动，演示页 = 原页 + 增量，可安全反向移植。
- 回迁内容（13 处替换，脚本 `.workbuddy/_tmp/port_arch.py`，每处 `count==1` 断言防止静默不落盘）：
  - CSS：`.contract-item` / `.info` 溢出修复；新增 `.contract-aggregate`、`.contract-item .ci-row1`、`.ci-copy`、`.status` 单行省略；`#contractModal .modal-box` 加宽 310px；`.modal-content` 隐藏滚动条（小程序规范）
  - HTML：头部新增「份合同」统计 `id=headerContractCount`；水电/泥瓦徽标改显示份数；`#contractList` padding 12/16→10/12
  - JS 数据模型：`groupContracts` 单对象 → 组名到**合同数组**，新增 `contractsOf()` / `hasSignedContract()`
  - 逻辑适配：`checkCanExitGroup` / `checkCanExitProject` / `showCreateModal(level2 无合同不得加施工组)` / `showDeleteModal` 四处守卫改数组口径
  - `viewContract()` 列表化：聚合行（共 N 份·已签约 X 份）+ 每份合同行内「📋 复制发起」按钮（蓝本=该份）
  - `goCreateContract()`：去掉「一个工作组只能有一个合同」阻断，改弹「创建方式」=复制发起（推荐，默认最新已签约份为蓝本）/空白新建；新增 `resetCreateChoiceButtons()` 复位 appModalCancel 文案与 onclick（showAppModal 只重设 Confirm 不重设 Cancel，不复会污染其他弹窗）；新增 `copyContract()` 跳 `contract-copy-initiate.html?group=&source=&from=architecture`
- **刻意不回迁**：演示标题「…演示」、`.demo-stage`/`.demo-note` 说明卡（CSS+DOM）、312 个 data-page-node-id。
- 验证方法：改造后再做一次「清洗版」diff vs 演示页，剩余差异仅 101 行且**全部**为上述演示专有项 → 证明功能层与演示页完全一致。
- 校验：node --check JS_OK、_validate_html.py BALANCED、CSS 大括号 156/156、15 项关键要素 grep 全 PASS、演示残留 0。
- 备份：原页 `.workbuddy/_tmp/architecture.html.bak`（83,533 字节），可完整回滚。

## 2026-09-09（续4）PRD 补充「复制发起」（两份文档，未动任何原型页）
- 归属确认：**项目架构**在 `PRD-项目模块详细规格.html`（第六章节 6.1~6.11）；**工人合同详情**在 `PRD-合同模块详细规格.html`（第七章 §7.1~7.11）。两份 PRD 此前「复制发起」均为 0 处。脚本 `.workbuddy/_tmp/prd_copy_initiate.py`，13 处带断言替换。
- `PRD-项目模块详细规格.html` 改动：目录加 6.12；6.2 增加多份合同说明段（**架构图 ASCII 保持原样未动**）；6.4 节点操作（查看合同含按份复制 + 新增「复制发起」行 + 创建合同触发条件）；6.6 概述改「一个工作组可存在多份合同」；**6.6.1 前置校验2 删除旧的「一个工作组只能有一个合同」表述**；6.7 查看合同改为「聚合统计行 + 多份列表 + 每份复制发起」；6.8 限制规则判定口径覆盖全部合同；6.9 交互表更新创建合同行 + 新增复制发起行；**新增 §6.12 复制发起功能规格 + 6.12.1 对原有交互的影响**。
- `PRD-合同模块详细规格.html` 改动：目录加 7.12；§7.10 更多菜单表拆分出「发起方视角各非终态」行，菜单为「📑 复制发起」置首 + 版本记录/变更记录/导出合同文件（保留原行 data-page-node-id）；**新增 §7.12 复制发起入口规格（工人合同详情页·发起方）+ 7.12.1 覆盖页面清单**（含受邀方两页不含该入口的注记）。
- 用户硬性要求已落地：①不写历史版本记录与新旧对比描述（6.12.1 三条 bullet 由「由单卡片改为…」「不再展示…」改写为正向陈述）；②**权限规则：复制发起 = 创建合同权限**，两处 PRD 均列为独立规则项；③不改原型页。
- 教训：①PRD 样 sneak 的 ASCII 结构图不能机械补字——原行字符长度不足以容纳新增中文会被截断且破坏等宽，改由说明段落承载信息（已回滚该行）；②新写内容不要携带 `data-page-node-id`（该属性由预览工具注入，PRD-合同模块已有 6481 个，手造 id 有冲突风险）；③写 Python 脚本时勿用 Edit 修改多行字符串字面量首行（曾把 `NEW_712 = '''...` 改坏导致语法断裂，需恢复后再把后处理放到字面量结束之后）。
- 校验：两 PRD `_validate_html.py` 均 BALANCED；锚点 `sub-6-12`/`sec-7-12` 各 1 处且目录链接同步；新增章节 node-id 残留 0；git 状态确认本轮仅改两份 PRD（原型页修改时间均早于本任务）。
- 备份：`.workbuddy/_tmp/PRD-项目模块详细规格.html.bak`、`.workbuddy/_tmp/PRD-合同模块详细规格.html.bak`。

## 2026-09-09（续5）修正 PRD「复制发起」入口表述（用户纠错，仅改 PRD）
- 用户指出 §6.12「次入口」写成「点击『📝 创建合同』且该工作组已存在合同时弹『创建方式』」与实际不符。核对 `architecture.html`：`goCreateContract` 仅在**无合同**节点（木工/油漆/新建节点）可达；**已有合同的节点渲染的是「查看合同」**（1152/1227 行），走 `viewContract` 弹窗按份「📋 复制发起」。故「创建方式」弹窗属代码中不可达的防御分支，不能写成常规入口。
- 脚本 `.workbuddy/_tmp/fix_prd_entry.py`，6 处带断言替换：①§6.12「主入口」→「入口」并写明「已存在合同 → 节点展示查看合同 → 弹窗按份复制发起，蓝本=所选份」；②「次入口」→「无合同时」（展示 📝 创建合同 → 空白新建，**不提供复制发起**，因无蓝本）；③§6.4 创建合同触发条件；④§6.6 前置校验2（保留「创建方式」但降级为「如因数据同步等原因被触发，系统不阻断」）；⑤§6.9 交互表创建合同行；⑥§6.12.1 创建合同 bullet。
- 校验：`_validate_html.py` BALANCED；合同模块 PRD 中「创建合同」仅为权限引用/字段说明，无需同步。备份 `.workbuddy/_tmp/PRD-项目模块详细规格.html.bak2`。

## 2026-09-09（续6）architecture.html 右侧新增「演示开关：创建合同权限」
- 需求：把 §6.12 权限规则「无该权限提示『您没有创建合同的权限』」做成可演示的校验不通过效果，开关放**手机框右侧框外**。
- 实现（脚本 `.workbuddy/_tmp/add_perm_switch.py`，4 处带断言替换）：①`<style>` 末尾加 `.arch-demo-stage` / `.arch-demo-note` / `.adn-*` 样式（沿用 `contract-copy-initiate.html` 的 `.copy-note` 虚线侧栏风格）；②body 内包一层 `.arch-demo-stage`（flex nowrap）裹住 phone-frame；③phone-frame 闭合后插入 `<aside class="arch-demo-note">`（开关 + 状态条 + 两个演示按钮 + 说明）再闭合 stage；④新增 `togglePermSwitch()` / `demoCopyInitiate()` / `demoCreateContract()`。
- **关键坑：`setPermission()` 不可调用**——它引用页面上**不存在**的 `#actionBar`（`getElementById` 返回 null → `.style` 抛错），且 `#permissionNotice` 文案是「您没有创建**架构**的权限」，与「创建合同」语义不符。开关只改 `hasCreatePermission` 变量。
- 演示按钮真实调用 `copyContract('水电工作组','contract-1')` / `goCreateContract('木工工作组')`，有权限则按原流程跳转，无权限弹「您没有创建合同的权限」。
- 校验：`_validate_html.py` BALANCED；JS `node --check` OK；**两个 `<style>` 块分别计数** 156/156 与 25/25 均 BALANCED（此前只 split 第一块会漏检）。备份 `.workbuddy/_tmp/architecture.html.bak_perm`。
- **修复遮盖 bug（同日追加）**：在「查看合同」弹窗内点「📋 复制发起」时，无权限提示看不见。根因：`#appModal` 与 `#contractModal` 同为 `.app-modal`（common.css `z-index:1000`），而 `#contractModal` 在 DOM 中更靠后（1425 行 vs 1381 行）→ 同级后者覆盖前者。修复：本页 style 加 `#appModal { z-index: 1010; }` + `.app-toast { z-index: 1020; }`，形成「普通弹窗 1000 < 提示弹窗 1010 < toast 1020」层级，且保留 contractModal 不关闭。脚本 `.workbuddy/_tmp/fix_modal_zindex.py`。**演示页 `architecture-multi-contract-demo.html` 同结构同源 CSS，存在同一问题，尚未修复（待用户确认）**。

## 2026-09-09（续7）删除 PRD §6.12.1「对原有交互的影响」
- 用户质疑：该节内容是否已在对应小节更新？若已更新则属重复总结（类历史记录），第一版无需保留 → 逐条比对后确认**5 条全部已落地**：①多份合同 → 6.6 概述 + 6.6.1 前置校验2 + 6.7；②头部合同份数 → 6.2 说明段；③查看合同聚合行/多份/按份复制 → 6.7 表格三行；④退出/删除/添加子级判定口径 → 6.8 表末说明；⑤创建合同入口随合同有无而变 → 6.4 触发条件 + 6.9 交互表。
- 已删除 6.12.1（第 3189~3197 行：空行 + 标题 div + ul/5 li）。校验：`_validate_html.py` BALANCED；`6.12.1`/`对原有交互的影响` 残留均为 0；`sub-6-12` 仍 1 处且 6.12 表格结构完整（table→subsection→section 闭合正常）。备份 `.workbuddy/_tmp/PRD-项目模块详细规格.html.bak3`。
- **保留**合同模块 PRD §7.12.1「覆盖页面与示例」——其性质为**规格清单**（哪些原型页含该入口 + 受邀方不含的说明），非"影响说明/历史对比"，已向用户说明待确认。
- 教训：按行号删除时先确认 Read 行号（1-based）与 0-based 索引的偏移，首轮因偏移 1 行导致断言失败（好在断言在写入前触发，文件未被改动）。
- **PRD §6.12.1/6.12.2 复制发起字段规格补充完成**（续前轮因 429 中断的任务）：①「发起页形态」行补「与拟定中页的字段差异见 6.12.1，隶属架构层级选择见 6.12.2」及拟定中页文件名链接；②新增 §6.12.1 字段差异表（11 行：隶属架构层级=复制发起特有独立卡片 / 合同类型静态不可改 / 甲方默认当前甲方可更换 / **意向乙方初始为空须重选 1–3 人且不沿用蓝本乙方** / 名称·金额·工期预填带沿用徽标 / 文本模板·违约责任·补充条款可编辑 / 阶段任务·附件沿用可改 / 底部多「取消」退出 / 提交校验与拟定中一致 / 提交结果不展示差异对比）+ 三类徽标规则（灰沿用 / 橙已调整 / 橙待选择）；③新增 §6.12.2 隶属架构层级选择规格表（9 行：卡片三行构成 / 默认值=被复制合同层级（group 参数或蓝本默认，兜底源工种二级） / 底部抽屉触发 / 抽屉过滤说明 / 顶部模糊搜索（名称·父级·工种·路径，空态文案） / 树形分组列表 / 可选规则=同类型或无合同、异类型且有合同置灰、一级根节点仅作路径起点 / 选择后卡片同步 / 仅决定新归属、原合同零写操作 / 提交气泡追加归入层级）。**落笔前 22 项原型文案逐字核对全部 PASS**（杜绝自造表述）；锚点带注入属性改用正则定位。校验：`_validate_html.py` BALANCED；新章节 node-id 残留 0；仅改该 PRD，原型页未动。备份 `.workbuddy/_tmp/PRD-项目模块详细规格.html.bak4`。
- **字段规格迁移至合同模块 PRD（方案A，用户确认）**：上述 6.12.1→**§7.12.2 复制发起页字段规格（与拟定中页差异）**、6.12.2→**§7.12.3 隶属架构层级选择规格**（插在 7.12.1 覆盖页面清单之后、subsection 闭合前；标题样式改用该文档三级标题 `<h4 style="margin-top:16px;">` 惯例）；内链调整：「详见 6.12.2」→「详见 7.12.3」，引言补「拟定中页（规格见 7.5.3.1）」。项目模块 6.12 删除两节，「发起页形态」行改为跨文档链接 `<a href="PRD-合同模块详细规格.html#sec-7-12" target="_blank">《PRD-合同模块详细规格》7.12.2 / 7.12.3</a>`（跨 PRD 链接惯例=href+target="_blank"，两文档均有先例）。**理由**：拟定中页对比基准规格（§7.5.3.1）在合同模块 PRD，就近对比；复制发起页归合同域；单一权威来源防口径漂移。合同 PRD TOC 不列三级标题（7.12.1 亦不在），无需改 TOC。校验：两 PRD BALANCED；6.12.1/6.12.2 残留双 0；7.12.2/7.12.3 各 1 处且均在 sec-7-12 内。备份 `.bak5`（项目）/ `.bak1`（合同，本轮首份）。教训：插入点锚断言窗口开 40 行过大（实际 8 行），断言先于写入触发故文件无损。
- **§7.12.2 底部操作行补「仅保存」规则**：不做必填校验，保存为新合同草稿（保持拟定中、不发邀约、不影响原合同），提示「已保存草稿」并停留本页——口径对齐原型 doSave()（toast 文案逐字核对）与 §7.5.3.1 拟定中页仅保存语义。BALANCED。
