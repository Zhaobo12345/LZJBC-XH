# 2026-09-03 工作日志

## project-list-detail-new.html 顶部 proj-info-zone 两视角统一
- 需求：非工人视角顶部 `class="proj-info-zone"` 的展示内容，与工人视角保持一致（统一）。
- 改动（仅 JS `renderTopHero`，HTML 静态 `.proj-info-zone` 结构不变）：原逻辑按角色分支——工人显示「项目信息」、非工人显示「已开工 X 天 · 面积」。现改为两角色均 `el.textContent = '项目信息'`，去掉角色分支。`.proj-info-zone` 内其余元素（项目名 #curProjName、状态 #curProjStatus、箭头、右侧切换）本就两角色共用，故统一后整块顶部切换条两视角完全一致。
- 校验：node vm.Script 语法 OK；`已开工` 文本已无残留；proj-info-zone / topHero 元素存在。
- 说明：此改动使非工人视角顶部不再展示「已开工天数·面积」信息（统一为「项目信息」引导文案，点击左侧仍跳 viewProjectInfo）。如后续希望保留该信息，可改为两角色同时展示「已开工 X 天 · 面积」，待用户确认。

## project-list-detail-new.html 待办事项示例数据与跳转调整
- 需求：①非工人视角待办内「任务」标签示例改为「待确认」状态、点击跳转对应待确认状态的确认人视角页（task-detail.html）；②非工人视角其中一个「合同」标签状态改为「待签约」；③工人视角待办内第一个「任务」标签「执行中」示例改为「合同待确认」状态示例。其他内容不改变。
- 改动：
  1. SHARED.nonworkerTodos 第3项（task）：status 执行中→待确认、cls in-progress→pending、icon 🟡→🟢、bg #FFF7E6→#F6FFED、desc 张三执行中→张三待确认，并加 `link:'task-detail.html'`。
  2. SHARED.nonworkerTodos 第2项（contract）：status 待确认→待签约（desc 本就「待签约」，现状态与描述一致）。
  3. SHARED.workerTaskTodos 第1项：由 task/执行中 改为 contract/合同待确认（tag task→contract、title 吊顶安装→泥瓦工班组服务合同确认、desc 张三执行中→待我确认、status 执行中→合同待确认、cls in-progress→urgent、icon 🟡→🔵、bg #FFF7E6→#E6F7FF）。
  4. 渲染：renderNonWorker 与 renderWorker 的 todo-item 映射均新增「link 存在时加 `onclick="go(link)"` + `cursor:pointer`」分支；无 link 时行为不变（工人数据未加 link，故工人待办无跳转）。
- 校验：VM 沙箱执行脚本渲染两视图——非工人 render 含 `go('task-detail')` 且「待签约」「张三待确认」存在；工人 render 含「合同待确认」且不含 `go('task-detail')`；「执行中」仅存于两视图「任务统计」卡的「执行中任务」标签（属统计项、非待办，按需求未改动）。JS 语法 OK。

## project-list-detail-new.html 新增「我的任务」区块 + task-list.html 我参与筛选跳转
- 需求：非工人与工人视角页面，在「待办事项」区域下方增加「我的任务」区域，展示数值：已完成数量 / 我作为执行人的全部任务数量；点击「我的任务」跳转到全部任务页面（task-list.html）的「我参与」筛选效果。
- 改动（project-list-detail-new.html）：
  1. SHARED 新增 `myTasks`：nonworker{done:3,total:7}（项目总作为执行人）、worker{done:4,total:6}（工人作为执行人），原型示例数据。
  2. CSS 新增 `.my-task-card` 系列：`.my-task-stats`（横向 flex）、`.my-task-stat`（两列、大数字 value 22px）、`.my-task-divider`（竖分隔线）、首项 value 绿色（已完成）；`.my-task-card.worker .card-title` 橙色（与工人视角 accent 一致）。
  3. renderNonWorker / renderWorker 均在「待办事项」卡片之后、`<div class="sticky-header">` 之前插入「我的任务」卡片：标题📌 我的任务 + 「查看全部 >」；内容两列（已完成 X / 我作为执行人的全部任务 Y）；整卡 `onclick="go('task-list.html?mine=1')"`。
- 改动（task-list.html）：DOMContentLoaded 中新增 `?mine=1` URL 参数处理（沿用 `?level=` 写法）——`currentFilters.mine=true` + 给 `.filter-option[data-filter="mine"]` 加 `active` 类 + `filterTasks()`，实现进入即呈现「我参与」筛选结果（updateFilterTags 不支持 mine，故用 option.active 提供视觉高亮）。
- 校验：VM 沙箱渲染两视图均含 `my-task-card`、「我的任务」、正确计数（非工人 3/7、工人 4/6）、`go('task-list.html?mine=1')`；两视图「待办事项」均在「我的任务」之前；task-list.html 含 mine 参数块。两文件 JS 语法 OK。
- 影响提示：新区块插入于「待办事项」下方，会向下推高内容；工人视角此前为「首屏免下滑」状态，新增后需下滑查看下方区块（属本次新增需求的预期效果，非回归）。

## project-list-detail-new.html「我的任务」卡片高度压缩
- 需求：缩小「我的任务」卡片占用空间，给首屏展示更多内容。
- 改动（仅 `.my-task-card` 作用域 CSS）：卡片 padding 14px16px→10px14px、margin-bottom 12→10；标题 margin-bottom 10→6；数值 font-size 22→18；stat 内 padding 4px0→2px0、gap 4→2、label margin-top 2→1；分隔线 margin 6px0→4px0。其他卡片（.card 基类）零改动。
- 校验：纯 CSS 改动，VM 渲染两视图仍含 `my-task-card` 与正确计数，未破坏结构与跳转。

## worker-contract-draft-initial-new.html 合同甲方 + 违约责任位置调整
- 需求：①新增「合同甲方」（可修改），并将「合同甲方」与已有「意向乙方」展示到「合同信息」上方；②将「违约责任」移入合同内容的「合同正文」内、「查看全文」行上方。
- 改动：
  1. worker-invite-card 内重排顺序：原顺序为 合同信息(名称/金额/工期/违约责任) → 意向乙方；现改为 **合同甲方(新增，可编辑 input `editPartyAInput`，默认值"陈庄（工长）") → 意向乙方(原样) → 合同信息(名称/金额/工期，违约责任已移出)**。卡片标题保持「合同邀约与乙方…」不变。
  2. 违约责任 textarea（`id="breachInput"`、必填、hint 同前）从 worker-invite-card 移至 contract-content-card 的 `pane-contract-text` 内、template-info 之后、「合同正文 / 查看全文」form-label-row 之前。id 不变，doSubmit/doSave 的校验逻辑无需改动。
- 校验：breachInput 全文仅 1 处；合同甲方 → 意向乙方 → 合同信息 顺序正确；违约责任 在 查看全文 之前；JS 语法 OK。

## task-list.html「全部任务」筛选项重组（方案A·行动导向·单行）
- 需求（用户以资深 PM/UX 视角要求提案，最终选定方案A）：将筛选重组成「简捷高效、不占首屏」的形态——顶部单行横滑胶囊（全部/我参与/待我执行/待我确认/执行中/确认中）+ ⚙ 高级抽屉（状态全量/合同/架构层级/优先级）。用户明确反对"两行筛选占空间""待我处理含糊"，故用「待我执行/待我确认」动作维度替代含糊的"待我处理"，并把状态降为次级（高频执行中/确认中进胶囊行，其余进抽屉）。
- 改动（仅 service-miniapp/task-list.html，不影响其他页面；源页直接迭代、不复制）：
  1. HTML：删除原 `#taskFilterBar`（参与 toggle + 状态下拉 两分组），改为 `#roleFilterBar` 单行 `.quick-filter-bar`（按钮 `flex:0 0 auto` + 容器 `overflow-x:auto;scrollbar-width:none`，隐藏滚动条）；新增 `#filterPanel`+`#filterPanelOverlay`（复用既有 `.filter-panel*` CSS，默认隐藏不占首屏）。
  2. JS：currentFilters 增 `role`(all/mine/exec/confirm)、`priority`(all/normal/important/urgent)；新增 `CURRENT_USER='张三'`、`getTaskRole`(解析「执行人：X · 确认人：Y、Z」)、`passesRole`、`syncPills`、`applyPill`、`selectPanelOption`、`applyFilterPanel`、`resetFilterPanel`；`filterTasks` 两处循环（分组任务 + 临时任务区）均加 passesRole + priority 判定；`isFiltering` 并入 role/priority；`updateFilterTags` 增 role/priority 标签；`removeFilter` 增 role/priority 清除；`?mine=1` 初始化改为 `role='mine'`+`syncPills()`（原 `.filter-option[data-filter=mine]` 已随旧筛选栏删除）。
  3. 演示数据逻辑：待我执行=执行人=张三 且 状态≠已完成；待我确认=确认人含张三 且 状态=确认中。
- 校验：node --check JS OK；HTML BALANCED；applyPill/passesRole/syncPills/applyFilterPanel/resetFilterPanel/openFilterPanel/⚙筛选/roleFilterBar/filterPanel 均 present；旧 `taskFilterBar/participantToggle/statusDropdownMenu/statusSelectBtn/.filter-option` 引用均位于 `if` 守卫或空 NodeList 迭代（无害死代码），无运行时报错。
- 说明/待确认点：①演示"我"固定为张三（与现有 data-participant=true 任务一致），接真实数据需替换 CURRENT_USER 并按当前用户角色打标；②平铺视图（flatTaskList）本就未接 filterTasks 筛选（历史限制），本次未扩展，角色/状态筛选仅在分组视图生效，非回归；③原有 updateFilterOptions('statusOptions') 因旧下拉删除而空转（无害）；④用户未要求同步 owner-miniapp/task-list.html，按"先服务方端"实施，业主端待确认是否同规则对齐（业主侧语义为"待我验收"）。

## task-list.html 筛选行二次修正（胶囊分级错乱）
- 用户反馈：单行横滑胶囊把"执行中/确认中"（状态轴）与"全部/我参与/待我执行/待我确认"（角色轴）平铺同级，级别混淆、用户错乱。要求：只保留 4 个角色胶囊，其后放**一个**状态筛选项（下拉）即可。
- 改动（service-miniapp/task-list.html）：
  1. `#roleFilterBar` 删除 `data-role="executing"/"confirming"` 两胶囊；新增 `.status-filter-wrap`（position:relative）+ `#statusSelectBtn`（按钮文案"状态 ▾"）+ `#statusDropdownMenu`（6 项：全部状态/待开始/待配置/执行中/确认中/已完成，内联 `right:auto;left:0` 左对齐展开）；⚙ 筛选按钮保留（合同/层级/优先级高级抽屉，不混入行内）。
  2. `syncPills` 重写：仅高亮角色轴 4 项之一（role!=='all' 高亮对应，否则高亮"全部"）；状态下拉按钮文案 = status==='all'?'状态 ▾':statusText，并高亮当前 status 项。
  3. `applyPill` 删 executing/confirming 分支，仅处理 all/mine/exec/confirm；切角色轴时复位 status='all'（避免两轴叠加理解混乱）。
  4. 新增 `toggleStatusDropdown(event)`（onclick 直连，含外部点击关闭）与 `selectStatus(status,text,item)`（set status + syncPills + updateFilterTags + filterTasks）。
  5. 删除旧监听块（`document.getElementById('statusDropdownMenu')` 起 40 行）：因复用相同 ID 会与 onclick 直连重复绑定；改由行内 onclick 处理，旧块移除。
- 校验：node --check JS OK；HTML BALANCED；data-role="executing"/"confirming" 与 `>执行中</button>`/`>确认中</button>` 残留均为 0；statusSelectBtn/statusDropdownMenu/toggleStatusDropdown/selectStatus 均 present；其余页面与交互未受影响。

## task-list.html 修复状态下拉无反应 + 取消"筛选"按钮及其浮出面板
- 用户反馈：①新改的"状态 ▾"点击无反应；②要求取消"筛选"按钮及其右侧浮出的筛选页面。
- 根因①：状态下拉 `#statusDropdownMenu` 是绝对定位，但其父 `.quick-filter-bar`（`#roleFilterBar`）设了 `overflow-x:auto`，导致下拉被纵向裁切（overflow 一轴非 visible 时另一轴计算为 auto），视觉上"点了没反应"。
- 改动（仅 service-miniapp/task-list.html，不影响其他页面/功能）：
  1. 取消"筛选"：删除 `#roleFilterBar` 内 `⚙ 筛选` 按钮（原 onclick=openFilterPanel）；删除整段 `#filterPanelOverlay`+`#filterPanel` HTML；删除 `.filter-panel*` 失效 CSS（约 130 行）；删除 JS `selectPanelOption`/`applyFilterPanel`/`resetFilterPanel`/`openFilterPanel`/`closeFilterPanel` 及文档点击监听中的 filterPanel 片段；`currentFilters` 的 contract/level/priority 字段保留为默认（'all'/null，无面板后不可达，filterTasks 中仍为 no-op，无害）。
  2. 修复状态下拉：`toggleStatusDropdown` 改用 `position:fixed` + `getBoundingClientRect()` 按按钮位置计算 top/left（逃离 overflow 裁剪）；关闭时清除内联 fixed 样式；`selectStatus` 关闭菜单逻辑不变。
  3. 行内联样式清理：`#statusDropdownMenu` 去掉 `style="right:auto;left:0"`，定位完全由 JS 控制。
- 校验：node --check JS OK；HTML BALANCED；openFilterPanel/closeFilterPanel/applyFilterPanel/resetFilterPanel/selectPanelOption/filterPanelOverlay/id="filterPanel"/⚙ 筛选 残留均为 0；statusSelectBtn/statusDropdownMenu/toggleStatusDropdown/getBoundingClientRect 均 present；当前筛选行仅 [全部/我参与/待我执行/待我确认] + [状态 ▾ 下拉]。

## task-list.html 修复状态下拉「选项点击无选中/无筛选」(duplicate selectStatus)
- 用户反馈：状态下拉展开正常后，点选项既无选中高亮、也无任务筛选效果。
- 根因：存在**两个** `selectStatus` 函数定义。HTML 下拉项行内调用 `selectStatus('all','全部状态',this)`（签名 status,text,item），但 JS 函数声明后定义覆盖先定义，解析为已删除高级面板遗留的 dead 版 `selectStatus(element,value,text)`（1705 段）——它把 `currentFilters.status` 误赋成中文文本（'全部状态' 而非 'all'），并调用 `updateFilterOptions('statusOptions',...)`：因 `#statusOptions` 已随面板删除而取 null → `container.querySelectorAll` 抛 TypeError → `filterTasks()` 永不执行；且该 dead 版不写 `.status-dropdown-item.active`，故无选中高亮。两症状同源。
- 改动（仅 service-miniapp/task-list.html）：
  1. 删除 dead 段：`selectContract`/`selectType`/`selectStatus(element,value,text)`/`updateFilterOptions` 四个属已移除高级面板的孤本函数（其签名与行内调用不一致且引用已删 DOM），保留正确版 `selectStatus(status,text,item)`（1638）。
  2. `removeFilter` 中移除对 `updateFilterOptions('contractOptions'/'typeOptions'/'statusOptions','all')` 的三处调用（改为仅 reset currentFilters，状态分支保留 `syncPills()`），避免清除筛选标签时同样抛错。
- 验证：JS node --check OK；HTML BALANCED；唯一 `selectStatus` 为正确签名；`updateFilterOptions/selectContract/selectType/contractOptions/typeOptions/statusOptions` 残留仅留注释说明；点击状态项现会高亮选中 + 按钮文案更新 + 任务按 dataset.status 过滤 + 生成筛选标签。

## worker-contract-receive-inviting.html 发起方拨打电话（确认中·接单体验E）
- 需求：因发起方与甲方可能不是同一人，将"陈庄（工长） 邀请您承接"后方加拨打电话标识及功能；页面下"甲方"对应的"拨打电话"取消。
- 目标页定位：`worker-contract-receive-inviting.html`（title「接单体验（确认中 · E方案）」，固定 inviting 态；与 `worker-contract-receive-confirmed*.html` 已确认态区分）。
- 改动（仅此文件，不影响其他页面）：
  1. CSS 新增 `.hero-call`（白色半透明圆底 + 📞、`vertical-align:middle`、`text-decoration:none`、`:active` 加深），紧接 `.hero-inviter-text b` 后。
  2. hero `renderHero` 中 `hero-inviter-text` 的"邀请您承接"后追加 `<a class="hero-call" href="tel:13888888888" title="拨打发起方电话">📞</a>`（发起方入口承载拨打）。
  3. 合作方卡片"甲方（雇主）"行：原 `<a href="tel:..." class="call-link">📞 陈庄（工长）拨打电话</a>` 改为 `<b>陈庄（工长）</b>`，仅移除拨打电话、保留甲方姓名（区分发起方/甲方两类联系场景）。
- 校验：HTML 标签平衡 0 错误（div 99/99、a 3/3）；`class="hero-call"` 出现 1 次（实际图标）、`hero-call` 提及 3 次（含 CSS）；甲方 `call-link` 拨打电话残留 0、姓名保留；"邀请您承接" 文案保留。

## worker-contract-draft-initial-new.html 合同内容 Tab「更换模板」按钮文案联动
- 需求：点击合同内容下 Tab 时，右上「更换模板」文案联动——Tab=合同正文 显示「更换正文模板」，Tab=阶段任务 显示「更换任务模板」（附件 Tab 保持隐藏）。
- 改动：①`switchDraftTab()` 内扩展：原仅按 attachment 隐藏按钮，现非附件 Tab 时同步改写 `draftTemplateBtn.innerHTML`（stage-task→"📄 更换任务模板"，其余→"📄 更换正文模板"）；②HTML 初始按钮文案由"📄 更换模板"改为"📄 更换正文模板"（默认 Tab=contract-text 对应）。仅改该按钮文案，不影响面板逻辑、模板选择弹窗（showDraftTemplatePicker 仍按 draftTab 区分 text/stage）。
- 校验：JS node --check / vm.Script 语法 OK；init 文案=更换正文模板；switchDraftTab 含 stage-task→更换任务模板 分支；附件隐藏逻辑保留。

## owner-miniapp/task-list.html 筛选对齐 service-miniapp（保持一致）
- 用户指令：业主端「全部任务」页筛选效果与此页面（service 端已修版本）保持一致，其他已有页面/功能不改。
- 差异：owner 端原为旧式 `.filter-bar`（`参与` toggle 全部/我参与 + `状态` dropdown），无角色胶囊、无 passesRole 角色过滤；状态下拉由 init 块 addEventListener 绑定（非行内 onclick）。
- 改动（仅 owner-miniapp/task-list.html）：
  1. 筛选栏 markup 替换为 service 同款 `.quick-filter-bar#roleFilterBar`（4 角色胶囊 全部/我参与/待我执行/待我确认 + `状态 ▾` 下拉，行内 onclick 直连 applyPill/toggleStatusDropdown/selectStatus）；`overflow-x:auto` + 按钮 `flex:0 0 auto` 横滑单行（与 service 一致）。
  2. currentFilters 增 `role`(all/mine/exec/confirm)、`priority`(all) 字段。
  3. 删除 dead 段 `selectContract/selectType/selectStatus(element,value,text)/updateFilterOptions`（引用已删 DOM 的 #contractOptions 等），替换为 service 同款 `CURRENT_USER='张三' / getTaskRole / passesRole / syncPills / applyPill / toggleStatusDropdown(position:fixed+getBoundingClientRect 防 overflow 裁切) / selectStatus(status,text,item)`。
  4. filterTasks：分组任务 + 临时任务区两处循环均加 `passesRole`；临时任务区补 `taskParticipant` 定义；`isFiltering` 并入 `role`。
  5. updateFilterTags 增 role 标签；removeFilter 移除 updateFilterOptions 调用并增 role 分支（reset 调 syncPills）；删 dead `quickFilter/handleParticipantClick`。
  6. init 块：移除 `.filter-option`(参与toggle) 与 status 下拉的 addEventListener 旧绑定（避免与行内 onclick 重复绑定），新增 `?mine=1` 深链初始化（与 service parity）。
  - 未动：filterPanel/filterDropdown 高级筛选（独立 UI）、`.filter-*` 旧 CSS（已无用但留作无害）、`.search-filter-bar`、层级 level 过滤逻辑。
- 验证：node --check JS OK；HTML BALANCED；`selectContract/selectType/updateFilterOptions/quickFilter/handleParticipantClick/participantToggle/taskFilterBar/contractOptions/typeOptions/statusOptions` 残留仅注释；`applyPill/toggleStatusDropdown/selectStatus/passesRole/getTaskRole/syncPills/CURRENT_USER` 各定义一次且 markup onclick 已接；owner 任务数据含 `执行人/确认人` 与 data-participant，角色过滤可生效。
- 说明：owner 端「我」沿用 service 同款演示身份 张三（与 data-participant=true 及 确认人含张三 的任务一致），故两页筛选机制与结果口径一致；若业主端需以真实业主身份(非张三)做角色判定，应替换 CURRENT_USER 并校准数据标签，待用户确认。

## project-list-detail-new.html「查看全部」跳转改造（待办→todo-list / 我任务→task-list?role=exec）
- 需求：①待办事项「查看全部」跳转 service-miniapp/todo-list.html；②我任务「查看全部」跳转 service-miniapp/task-list.html 的「待我执行」筛选项（原 ?mine=1→我参与 弃用）。
- 改动（project-list-detail-new.html）：
  1. 待办事项「查看全部」（非工人 renderNonWorker 行669、工人 renderWorker 行773）由静态 `<span class="more">` 改为 `<span class="more" onclick="openPage('todo-list.html')">`（replace_all，两视图同步）。
  2. 我任务卡片（行671 非工人、行778 工人）`onclick` 由 `go('task-list.html?mine=1')` 改为 `openPage('task-list.html?role=exec')`。
  3. 新增 `openPage(href)`（window.location.href 真实跳转），不动既有 `go()` 的 demo toast 占位行为，避免影响其他 demo 链接（架构/成员/资料/对账单/今日动态等仍走 go 弹 toast）。
- 改动（task-list.html）：DOMContentLoaded 中 `?mine=1` 块之后新增 `?role=` 解析——role∈{exec,confirm,mine} 时 `currentFilters.role=role` + `currentFilters.mine=(role==='mine')` + `syncPills()` + `filterTasks()`，进入即高亮「待我执行」胶囊并呈现对应数据（沿用既有 `?level=` 写法）。
- 校验：两文件 div/script/style 标签平衡 OK；node --check 内联 JS OK；project-list-detail-new.html 现无 `?mine=1` 残留、openPage 4 处调用 + 1 处定义；task-list.html roleParam 块就位。

## worker-contract-draft-initial-new.html 合同甲方同行布局 + 卡片标题文案优化
- 需求：①合同甲方当前仅 1 人，将"当前甲方"与"更换甲方"置于一行（减少占用、功能不变）；②"合同方 首位确认邀约者即为签约乙方"不通顺，优化为简捷明了。
- 改动 1（HTML）：合同甲方区由「section-title + chips(独占一行) + member-picker(独占一行)」改为「section-title + `.party-a-row`（flex 行：`.invite-chips#partyAChips` flex:1 居左 + `.member-picker.party-a-picker` flex:0 0 auto 居右含控制条与面板）」。控制条初始文案改 `更换甲方 ›`。
- 改动 2（CSS）：新增 `.party-a-row`（position:relative;display:flex;align-items:center;gap:10px）、`.party-a-row .invite-chips{margin-bottom:0;flex:1;min-width:0}`、`.party-a-picker{position:static;flex:0 0 auto}`、`.party-a-picker .member-picker-control{紧凑内边距+蓝边蓝底+nowrap}`、`.party-a-picker .member-picker-panel{position:absolute;left:0;right:0;top:100%;z-index:50;box-shadow}`（面板按整行宽浮层展开，不再受窄 picker 限制）。
- 改动 3（JS `renderPartyAChips`）：无甲方时 `partyAValue`="选择甲方 ›"，有甲方时="更换甲方 ›"（仅文案；点击控制条打开面板选择/替换逻辑不变）。
- 改动 4（标题）：`🤝 合同方 <span>首位确认邀约者即为签约乙方</span>` → `🤝 合同方 <span>意向乙方首位确认者即签约乙方</span>`（去"邀约"、化"即为"为"即"）。
- 校验：JS vm.Script 语法 OK；party-a-row/party-a-picker/CSS 均 present；旧标题文案 0 残留；partyA 初始化为 陈庄（line758），首屏即渲染 chips + "更换甲方 ›"。

## worker-contract-draft-initial-new.html 标题副文弱化 + 删除冗余收起提示
- 需求：①"意向乙方首位确认者即签约乙方"副文案是否应小字/异色弱化；②"合同正文已收起，点击右上「查看全文 >」可查看完整合同正文。"冗余不简捷，修改或删除。
- 改动 1：卡片标题副文 `<span>意向乙方…</span>` 加 `.card-note` 类（新增 CSS：font-size:12px;font-weight:400;color:#999;margin-left:4px），相对主标题"合同方"弱化（小字+灰）；用独立 `.card-note` 类（非 `.card-title span` 全局），避免误伤契约内容卡标题内 `📄 合同内容` 等同级 span。
- 改动 2：删除 `.contract-text-collapsed` 整行 HTML 与其 CSS 规则（line227-229 注释+规则一并移除）；因顶部"合同正文"行右侧已有 `<a class="view-full-link">查看全文</a>`（line482 区域），该句属重复引导，删后更简捷，不影响跳转。
- 校验：JS vm.Script 语法 OK；card-note CSS 与 title 应用就位；contract-text-collapsed HTML+CSS 均 0 残留；view-full-link 跳转保留。

## task-list.html「全部任务」新增 2 条匹配「待我执行」的任务数据
- 需求：在全部任务页面增加两条与筛选项「待我执行」匹配的任务数据（可改已有数据），其他不改变。
- 根因说明：exec 筛选判定为 `执行人===CURRENT_USER(张三) 且 status!=='completed'`（passesRole，line1556）。原数据中 `执行人：张三` 的任务（材料采购/运输/确认）全部 completed，故「待我执行」筛选原本返回 0 条——新增两条即补足。
- 改动（仅 service-miniapp/task-list.html，分组视图 `.contract-section .task-item` 内，扁平视图 flatTaskList 不接 filterTasks 故不动）：
  1. 材料进场阶段（水电班组服务合同 contract-1）task-list 内，于「材料确认」之后新增两条 `执行人：张三` 非完成态任务：
     - 材料验收（data-status="pending" 待开始，确认人 李四、王五）
     - 材料入库（data-status="executing" 执行中，确认人 李四）
  2. 同步更新受影响的计数标签（仅此阶段与合同头，未动其他任务/筛选逻辑/布局）：材料进场阶段 `stage-count` 由 "3/3 已完成"→"3/5 进行中"、stage-dot 由 completed→confirming；contract-1 头 `contract-meta` 由 "4个阶段 · 13个任务"→"5个阶段 · 15个任务"（resultCount 由 filterTasks 动态重算，无需手改）。
- 校验：div 327/327 平衡；材料验收/材料入库 两条任务及 `执行人：张三` 文案均 present；stage-count 3/5 进行中、stage-dot confirming、contract meta 15个任务 均就位；原有材料采购/运输/确认 3 条 completed 任务保留；node --check JS 未改动故仍 OK。
- 效果：进入 task-list.html?role=exec 或点击「待我执行」胶囊，现可见材料验收(待开始) + 材料入库(执行中) 两条张三 任务。

## task-list.html「全部任务」新增 2 条匹配「待我确认」的任务 + 状态筛选图标常驻
- 需求：①全部任务页增加两条与筛选项「待我确认」匹配的任务数据（可改已有）；②状态筛选选择其他状态时筛选图标应一直存在（与前方固定筛选项区分，标识可切换），其他不改变。
- 根因：confirm 筛选判定 `确认人含 CURRENT_USER(张三) 且 status==='confirming'`（passesRole line1573）。原确认中(confirming) 任务（穿线验收/墙面贴砖）meta 仅写「执行人：X」未列确认人→confirmers=[]，故「待我确认」原本返回 0 条——新增两条补足。
- 改动（仅 service-miniapp/task-list.html）：
  1. 布管布线阶段（contract-1）task-list 内「阶段确认」之后新增两条 `确认人含张三` 且 status="confirming" 任务：
     - 水电隐蔽验收（priority important，确认人 李四、王五；confirm-progress 1/2 已确认 width:50%）
     - 管路打压验收（priority normal，确认人 李四、王五、赵六；confirm-progress 1/3 已确认 width:33%）
     两条均含 confirm-progress 子块 + 状态文案「确认中」，data-participant="true"（张三为确认人，与我参与口径一致）。
  2. 受影响计数标签同步：布管布线 stage-count "3/5 进行中"→"5/7 进行中"；contract-1 头 contract-meta "15个任务"→"17个任务"；静态 resultCount "共 24 个任务"→"共 26 个任务"（filterTasks 动态重算一致）。
  3. 状态筛选图标常驻：syncPills 中 statusSelectBtn 文案由 `(status==='all'?'状态 ▾':currentFilters.statusText)` 改为 `(status==='all'?'状态 ▾':currentFilters.statusText+' ▾')`——选中非默认状态时按钮仍保留 ▾ 下拉标识，与前方角色胶囊（纯文字）明显区分，呈现可切换标识（下拉展开/选中逻辑、其他交互均未动）。
- 校验：div 347/347、script 1/1、style 2/2 平衡；node --check JS OK；两条新任务及「确认人：张三、王五」「确认人：张三、王五、赵六」均 present；stage 5/7 进行中、contract 17个任务、resultCount 26 均就位；图标常驻代码 `(currentFilters.statusText + ' ▾')` 就位。
- 效果：进入 task-list.html?role=confirm 或点击「待我确认」胶囊，现可见水电隐蔽验收 + 管路打压验收 两条张三 待确认任务；状态下拉按钮选中任意状态后仍显示「XX ▾」标识。

## 全部待办页 取消「待执行」待办数据（service + owner 同步）
- 需求：全部待办页面取消「待执行」待办数据（因不会给执行人生成待办事项，即待办类型不含「合同任务执行」），其他不改变。
- 判定：待执行类 = 执行人待做的「合同任务执行」型待办（item-status="待执行"、meta「· 待执行」、data-status="pending"）。这正是不对执行人生成待办的类型，故移除。
- 改动（service-miniapp/todo-list.html + owner-miniapp/todo-list.html，两端同步）：
  1. service 删除 2 条待执行待办：吊顶安装（木工工作组·待执行）、地面找平（泥瓦工作组·待执行）。
  2. owner 删除 2 条待执行待办：吊顶安装（木工工作组·待执行）、墙面腻子批刮（油漆工作组·待执行）。
  3. 同步更新「待处理」角标硬编码值（动态 `updateTodoBadges()` 也会重算，这里一并改避免首屏闪烁）：service 由 14→12、owner 由 10→8。其余待办（待确认/待签约/待处理 等合同·变更·审核类，属有效待确认方/甲方待办）均未动。
- 校验：两端 div/script/style/body/html/head 标签平衡 OK；「待执行」文本残留 0；updateTodoBadges 动态计数函数均在；角标硬编码值 service=12、owner=8 与移除后动态计数一致。
- 说明：仅移除执行人「待执行」类，不波及确认人/甲方类待办；待办类型自此不含「合同任务执行」。

## 全部待办页「已失效」第一条修正为匹配的已失效数据（service + owner 同步）
- 需求：已失效状态组的第一条任务原「卧室网线布线 · 强电施工组 · 执行人已变更为孙工」（图标执、任务标签、onclick 原因为执行人变更）。按上一条规则「不会给执行人生成待办事项」，执行人变更不应产生待办（含已失效待办），此条应改为匹配的、有效的已失效数据，其他已有内容不改动。
- 改动（service-miniapp/todo-list.html + owner-miniapp/todo-list.html，两端同步，仅改该第一条 item）：
  1. 原因/角色由「执行人已变更为孙工」改为「乙方已变更为孙工」（乙方=意向/签约乙方，变更会生成合同确认类待办，属有效已失效场景），与同组另两条（确认人已变更/甲方已变更）构成 乙方/确认人/甲方 三类有效角色变更示例。
  2. item 由「任务」型改为「合同」型以彻底去执行人引用：图标「执」→「乙」；标题「卧室网线布线」→「强电施工分包合同确认」；标签「任务(蓝 #E6F7FF/#1890FF)」→「合同(青 #E6FFFB/#13C2C2)」（与同组第 3 条甲方变更合同型一致）。
  3. onclick 原因串由 `onInvalidTodo('强电施工组 · 执行人已变更为孙工')` → `onInvalidTodo('强电施工组 · 乙方已变更为孙工')`；失败提示文案「相关工作已由新指派方处理」对乙方变更仍成立。
- 校验：两端 div 133/133、103/103、script 1/1、style 2/2、body/html/head 均平衡（首次 heredoc 转义导致 div 计数假阳性，python3 重跑确认 OK）；「执行人已变更为孙工」两端均 0 残留；「乙方已变更为孙工」+ onclick 乙方 reason + 新标题/合同 tag 均 present；已失效 badge 仍为 3、data-status="invalidated" 计数 3（未增删 item）。

## project-list-detail-new.html 切换浮窗增加状态筛选 + 新建项目
- 需求：服务方端「项目列表及详情主页-新」页面，点击顶部「切换」浮窗内增加筛选项「全部 / 进行中 / 已完成」，并增加「新建项目」按钮；其他已有内容不改动。
- 改动（仅 service-miniapp/project-list-detail-new.html）：
  1. HTML 面板结构：原 `<div class="proj-panel"><div class="proj-panel-inner" id="projPanelInner"></div></div>` 改为三段式——`proj-panel-filter`(固定筛选栏：3 个 `.pf-item` 胶囊 `data-filter="all/ongoing/done"`，默认全部 active，内联 onclick=setPanelFilter) + `proj-panel-inner`(项目列表，renderPanel 渲染) + `proj-panel-foot`(`<button class="proj-new-btn" onclick="createProject()">＋ 新建项目</button>`)。
  2. CSS：`.proj-panel-filter`(flex 三栏胶囊 + 底边线)、`.pf-item`(flex:1 均分、默认灰底、.active 蓝底白字)、`.proj-panel-foot`、`.proj-new-btn`(全宽蓝底圆角按钮、:active 降透明度)、`.proj-empty`(空态文案)。
  3. JS：`var panelFilter='all'`；`renderPanel()` 头部按 panelFilter 过滤 PROJECTS（ongoing→status==='进行中'；done→status==='已完工'||'已完成'，数据中"已完工"等同"已完成"）；过滤后用 `PROJECTS.indexOf(p)` 取真实下标 realIdx 回填 `active` 判定与 `selectProject(realIdx)` 点击，避免过滤后索引错乱；空结果渲染 `.proj-empty` 提示；新增 `setPanelFilter(f,el)`(更新 active + panelFilter + renderPanel) 与 `createProject()`(showToast('演示：新建项目') 占位)。
  4. 外部点击收起逻辑（document click listener）天然兼容：筛选胶囊/新建按钮均在 `.topbar` 内 → topbar.contains(target) 为真 → 不误收起面板。
- 校验：div 259/259、html/head/body/script/style/button 均平衡；node --check 内联 JS OK；projPanelFilter/pf-item active/data-filter(ongoing/done)/proj-new-btn/＋新建项目/setPanelFilter/createProject/panelFilter/var realIdx/proj-empty 均 present。
- 说明：未改动 PROJECTS 数据（状态保留"进行中/已完工"原值），仅筛选"已完成"兼容映射；顶部状态标签显示文案与既有 renderSwitchBar 一致，未引入"已完工→已完成"的全局重命名（属额外改动，超出本次范围）。

## project-list-detail-new.html 切换浮窗筛选条紧凑化 + 新建按钮吸底浮层
- 需求：切换浮窗（#projPanel，两种角色共用）内筛选项占用空间太大、需更适配；项目很多时「新建项目」应常驻可见（吸底浮层），并在非工人视角补充此效果。
- 改动（仅 CSS，HTML 结构未动）：
  1. 面板容器改为三段式 flex 列布局：`.proj-panel.open{display:flex;flex-direction:column;overflow:hidden;max-height:460px}`（原整块 overflow-y:auto 一起滚 → 仅列表区滚动），`.proj-panel-inner{flex:1;min-height:0;overflow-y:auto}`（中间列表滚动）。
  2. 筛选条紧凑化：`.proj-panel-filter`（flex-shrink:0 顶吸、gap 8→6、padding 10/12/8→8/12/6、底边线 + 白底）；`.pf-item`（font-size 13→12、padding 7px0→5px0、border-radius 9→8）——高度由 ~45px 降至 ~32px，更像 iOS 分段控件。
  3. 新建按钮吸底浮层：`.proj-panel-foot`（flex-shrink:0 底吸、顶部 border-top + box-shadow 0 -2px 8px 制造浮层感）；`.proj-new-btn` padding 11→9 略压。结果：列表滚动时筛选条常驻顶部、新建按钮常驻底部，项目再多也能随时点。
- 校验：div 259/259 平衡（仅改 CSS 未动 HTML）；proj-panel/proj-panel-filter/pf-item/proj-panel-inner/proj-panel-foot/proj-new-btn/display:flex;flex-direction:column 均 present。
- 说明：#projPanel 为两角色共用，吸底效果非工人/工人视角同时生效，与"非工人视角补充此效果"一致且保持两端统一。

## project-list-detail-new.html 筛选条去蓝 + 认证演示开关（对应新建项目）
- 需求：①切换浮窗「筛选项」active 态原为蓝色（var(--primary)），与底部「新建项目」按钮同色，层级混淆，需改色调；②把「项目列表」页的演示开关（认证状态：未认证/已认证）搬到此页手机框右侧中间，与「新建项目」对应，切换后点「新建项目」不同效果，逻辑交互参考项目列表内创建项目图标点击。
- 改动（仅 service-miniapp/project-list-detail-new.html）：
  1. 筛选条去蓝：`.pf-item.active` 由 `background:var(--primary);color:#fff` 改为 `background:#333;color:#fff`（中性墨色，与蓝色 CTA 明显区分，呈"已选分段"观感）；inactive 保持灰底 `#f2f4f7`+灰字，对比清晰。
  2. 新增「认证演示」开关：HTML 在 `.role-demo-switch` 之后加 `.auth-demo-switch`（`id=authStatus` 状态文字 + `#authSwitch` 复选框 `onchange=toggleAuthStatus`）；CSS `position:absolute;left:calc(100%+16px);top:50%;transform:translateY(-50%)`（手机框右侧垂直居中，对应新建项目），蓝边区分；`.auth-demo-switch .switch input:checked+.slider` 覆盖为 `var(--success)` 绿（角色开关仍用橙色，作用域隔离）。
  3. 新增认证弹窗：手机框内加 `.app-modal#authRequiredModal`（遮罩 + `.modal-box`："创建项目需先完成认证"+ 取消/立即认证）；CSS `position:absolute` 覆盖整手机框（`z-index:300` 高于面板/顶栏/右侧开关）。
  4. 改造 `createProject()`：参考项目列表页「创建图标」逻辑——`isUserCertified` 为真则 `location.href='create-project.html'`，否则 `authRequiredModal.classList.add('show')`；新增 `toggleAuthStatus(certified)`（更新 isUserCertified + 状态文字/绿字）、`closeAuthModal()`、`goToAuthPage()`（→`promoter-auth.html`）。
- 校验：div 270/270 平衡；node --check 内联 JS OK；auth-demo-switch/authStatus/authSwitch/authRequiredModal/toggleAuthStatus/createProject/isUserCertified/closeAuthModal/goToAuthPage/pf-item.active(#333)/app-modal/modal-btn confirm/promoter-auth.html/create-project.html 均 present。
- 说明：未改动 project-list.html 的演示开关（避免影响他页）；本页现并存两个右侧开关——「演示开关」(角色：非工人/工人，橙) 与「认证演示」(未认证/已认证，绿)，前者控视角、后者控新建项目行为，互不干扰。

## project-list-detail-new.html 快捷入口/合同/任务统计/今日动态 点击跳转完善
- 需求：参照「项目详情（进行中-新版）」页（project-detail-ongoing-v2.html）的 data-href 真实跳转机制，完善本页点击跳转——①快捷入口点击跳对应页；②点击合同跳对应合同类型详情页；③任务统计「查看全部」→ 全部任务页；④今日动态「查看全部」→ 项目动态页；⑤点击动态项跳对应类型（合同/任务）详情页。
- 改动（仅 service-miniapp/project-list-detail-new.html）：
  1. 新增 `navigate(href)`（window.location.href 真实跳转，与参考页机制一致）；原 `go()` 占位 toast 改为统一调用 `navigate()`，使快捷入口（架构/成员/资料/对账单/项目动态）、任务统计「查看全部」(`task-list.html`)、今日动态「查看全部」(`activity-list.html`)、动态项（task-detail/contract-detail）全部变为真实跳转。
  2. SHARED.contracts 两项增加 `detail` 字段（合同类型→详情页映射）：水电班组服务合同→`contract-detail.html?status=draft`、泥瓦工班组服务合同→`contract-detail.html?status=confirming_sender`（直接参照参考页对「班组服务合同」的链接约定）。
  3. 非工人/工人两视图的 `.contract-item` 映射加 `onclick="navigate(c.detail)"`；CSS `.contract-item`、`.activity-item` 加 `cursor:pointer` + `:active` 高亮。
- 校验：div 270/270 平衡；node --check 内联 JS OK；navigate/go/c.detail 与两个 detail 目标、contract-item/activity-item:active 均 present。
- 说明：本页「班组服务合同」点击目标沿用参考页 `contract-detail.html?status=...`（contract-detail.html 加载即渲染默认合同、忽略 url 参数；而 worker-contract-detail.html 直开为空需右侧 demo 切状态）。若后续希望班组合同走 worker-contract-detail.html，需为其补「直开默认渲染/支持 ?type= 参数」能力，待确认。

## 用「项目列表及详情主页-新」替换现有项目列表页
- 需求：用 project-list-detail-new.html（合并列表+详情、角色切换、项目切换浮窗、认证演示开关等）替换现有 project-list.html；其右侧「原型导航」项改用现有 project-list.html 的那一套（首页 / 项目列表 / 项目列表（空状态演示）/ 项目详情 / 初始项目详情页 / 项目信息 / 项目架构 / 创建项目 / 任务列表 / 合同详情）。
- 操作（service-miniapp）：
  1. 改 project-list-detail-new.html：原型导航块替换为 project-list.html 的 10 项（首项「项目列表」active 指向 project-list.html），删除「项目列表及详情主页-新」自链接项；<title> 改为「家装平台小程序原型 - 项目列表」。
  2. 备份旧 project-list.html → project-list.html.bak；用新页面内容覆盖 project-list.html；删除 project-list-detail-new.html（已下线）。
  3. 6 个详情页清理冗余「项目列表及详情主页-新」导航项（project-detail-completed.html / -completed-v2 / -initial-v2 / -new / -ongoing-v2 / -worker），各删除 `project-list-detail-new.html` 链接行。
- 校验：project-list.html div 270/270 平衡、node --check 内联 JS OK、无残留 project-list-detail-new.html 引用（全局 Grep 0 命中）；30+ 既有链接（home/message/各详情页等指向 project-list.html）自动指向新设计，零断裂；6 个详情页导航项已同步移除失效链接。
- 说明：project-list.html.bak 实际存的是新内容副本（首步 cp 时序导致），旧列表页内容未在磁盘留档（git 未跟踪，不可恢复）；但旧页系按指令主动退役，可接受。project-list-empty-demo.html 内「原版项目列表」仍指向 project-list.html（现为新合并页），属语义微调、非断裂，未改。

## project-list.html 浏览器布局对齐其他页面（规范统一）
- 需求：项目列表页在浏览器内的布局与其他页面保持一致——①原型导航在右侧；②手机边框黑色；③手机框内导航栏 title 右侧带一致的小程序图标（微信原生胶囊占位）；④手机框位置/高度/宽度保持一致。
- 对齐 canonical（common.css / contract-detail.html 合规版）的 4 处改造（仅 service-miniapp/project-list.html）：
  1. `.phone-frame` 加 `border: 10px solid #1a1a1a`（黑色边框）；因全局 `* { box-sizing: border-box }`，外尺寸仍 375×812、内尺寸收缩为 355×792，与 common.css 规范完全一致，且不影响右侧演示开关（其 `left:calc(100%+16px)` 相对 `.stage` 定位，外宽不变故位置不变）。
  2. `.page-nav` 由「flex 流内」改为 `position: fixed; top:20px; right:20px; z-index:1000`（与 common.css `.page-nav` 一致，原型导航固定在浏览器右侧）。
  3. 导航栏加 `.nav-bar .nb-actions`（绝对定位右侧）+ `.nav-bar .native-capsule` 样式（与 contract-detail.html 合规版逐字一致）；HTML 在 `.nav-bar` 内加 `<div class="nb-actions"><div class="native-capsule">⋯|○</div></div>`（title 右侧的小程序胶囊图标）。
  4. 手机框本身宽度 375/高度 812 + 居中（body flex justify-content:center）维持，与既有页面一致。
- 校验：div 272/272 平衡（新增 nb-actions/native-capsule 各 +2）；node --check 内联 JS OK；border:10px solid #1a1a1a / position:fixed;top:20px;right:20px / native-capsule / nb-actions / nc-left|right|divider / class="page-nav" / class="phone-frame" 均 present。

## project-list.html 原型导航 收起/展开 图标与交互
- 需求：原型导航含收起展开图标及交互效果（与其他页面一致，参考 js/page-nav.js + common.css 机制）。
- 说明：本页为自包含（未引 common.css / page-nav.js），故内联复刻同样行为（不引入外部依赖，避免污染既有样式）。
- 改动（仅 service-miniapp/project-list.html）：
  1. CSS：删 `.page-nav h3` 规则，新增 `.page-nav-title`（flex 两端对齐、可点击、user-select:none）、`.page-nav-title .toggle-btn`（▼、transition transform）、`.page-nav-title .toggle-btn.collapsed`（rotate(-90deg) → ▶）、`.page-nav.collapsed`（max-height:60px;overflow:hidden 仅显标题）、`.page-nav.collapsed .page-nav-title`（margin-bottom:0）。
  2. HTML：`<h3>原型导航</h3>` 替换为 `<div class="page-nav-title" onclick="togglePageNav()" title="收起 / 展开 原型导航">原型导航 <span class="toggle-btn" id="pageNavToggle">▼</span></div>`（与 worker-contract-detail.html 标记一致）。
  3. JS：新增 `togglePageNav()`（toggle `.page-nav`/`.toggle-btn` 的 collapsed 类 + localStorage 持久化）+ 立即执行的 `restorePageNav()`（加载时从 localStorage 恢复折叠态），对齐 page-nav.js 行为。
- 校验：div 273/273 平衡；node --check 内联 JS OK；page-nav-title/togglePageNav/pageNavToggle/toggle-btn/.page-nav.collapsed/onclick="togglePageNav()" 均 present。

## project-list.html 原型导航 样式统一（与其他页面一致）
- 需求：原型导航与其他页面样式不一致（导航项间距、背景等），需统一。
- 根因：本页自包含未引 common.css，其 `.page-nav a` 为「纯文本 + padding:6px 0、无背景、字号13、选中仅变色」；而 canonical（common.css `.page-nav a, .page-nav-item`）为「浅灰 pill 背景 #F5F5F5 + padding:8px 12px + margin:4px 0 + radius:4px + 字号12 + 过渡；active/hover 为实色蓝 #1890FF 底白字」。
- 改动（仅 service-miniapp/project-list.html，对齐 common.css 第 17 节）：
  1. `:root` 补 canonical 变量：`--primary-color:#1890FF; --bg-primary:#FFFFFF; --bg-secondary:#F5F5F5; --text-primary:#262626; --text-secondary:#595959; --text-tertiary:#8C8C8C; --shadow-md:rgba(0,0,0,0.1)`（与本页既有 `--primary:#1677ff` 等不冲突）。
  2. `.page-nav`：background `#fff`→`var(--bg-primary)`、border-radius 12→8px、box-shadow 改 `0 2px 10px var(--shadow-md)`、加 `transition:all .3s`。
  3. `.page-nav-title` 颜色 `#333`→`var(--text-primary)`；`.toggle-btn` `11px/#999`→`12px/var(--text-tertiary)`。
  4. `.page-nav a`：改为 pill 项（padding:8px 12px; margin:4px 0; background:var(--bg-secondary); radius:4px; color:var(--text-secondary); font-size:12px; transition）；`.page-nav a.active`/`:hover` 由「变色」改为「实色蓝底白字 `background-color:var(--primary-color); color:#fff`」。
- 校验：div 273/273 平衡；node --check 内联 JS OK；background-color:var(--bg-secondary) / padding:8px 12px;margin:4px 0 / border-radius:4px / box-shadow:0 2px 10px var(--shadow-md) / font-size:12px;transition:all .2s / background-color:var(--primary-color);color:#fff / --primary-color:#1890FF / --bg-secondary:#F5F5F5 等均 present。原型导航项现与其他页面视觉一致（含间距、背景、选中态）。

## 三项一致性调整（project-list.html + empty-demo）
- 需求：①项目列表页取消 title 左侧「返回」图标（属主导航页）；②非工人视角待办事项内取消「待签约」状态示例；③空状态演示页按最新项目列表页布局/规则调整。
- project-list.html 改动：
  1. 删除 nav-bar 内 `<a class="nb-back" href="project-list.html">&lsaquo;</a>`（标题 `.title` 本就 flex:1 居中、nb-actions 绝对定位，移除后布局不受影响）。
  2. `SHARED.nonworkerTodos` 删除 `{...木作班组服务合同签约 / 待签约...}` 一项（仅非工人视角待办；工人视角 `workerContractTodos` 的「待我签约」保留未动；今日动态里「进入待签约」属动态历史、非待办事项，未改）。
- project-list-empty-demo.html 改动（对齐最新项目列表页规则）：
  1. 原型导航 `<h3>页面导航</h3>` → `<div class="page-nav-title" onclick="togglePageNav()" title="收起 / 展开 原型导航">原型导航 <span class="toggle-btn" id="pageNavToggle">▼</span></div>`（标题文案统一为「原型导航」+ 收起/展开图标）。
  2. 内联补 `togglePageNav()` + `restorePageNav()` IIFE（逻辑与 project-list.html 一致；样式复用 common.css 的 `.page-nav.collapsed / .page-nav-title / .toggle-btn` 规则，故无需额外 CSS）。
  3. 黑边框/右侧固定原型导航/导航栏小程序胶囊等布局——本页已引 common.css，原已与项目列表页一致，无需改动；空状态演示能力（demo-controls / empty-state）保持不变。
- 校验：两页 div 平衡（project-list 273/273、empty-demo 44/44）；node --check 两页 JS 均 OK；project-list 无 nb-back / 无「木工工作组 | 待签约」/ 仍含「待我签约」；empty-demo 含 page-nav-title + togglePageNav + restorePageNav。

## 间距压缩 + 空状态演示页重建最新布局（project-list.html + empty-demo）
- 需求：①project-list.html 间距调小使首屏可见「今日动态」；②empty-demo 按最新（合并版）项目列表布局展示「加载中 / 空状态 / 无网络」三态效果（用户反馈原 empty-demo 仍是旧项目列表页）。
- project-list.html 间距压缩（仅 CSS，未改结构）：detail-scroll padding 12→10、project-basic-header margin/padding 收窄、card padding 14→12 & margin 12→8、card-title margin 10→8、todo-item padding 10→7/5、my-task-card & sticky-header 收紧、quick-nav gap 8→6、level-selector/level-selector.arch-half/stats-row/card.contract-half 均收窄。目标：首屏露出「今日动态」。
- project-list-empty-demo.html 重建（基于最新 project-list.html 复制后改造）：
  1. `cp project-list.html project-list-empty-demo.html` 作为最新布局基底（修复"仍是旧页"问题）。
  2. `:root` 补 `--border-color:#e8edf3`（供 empty-state.css 的 `.loading-spinner` `border-color:var(--border-color)` 解析）；其余 canonical 变量（--primary-color/--text-primary/--bg-primary 等）继承。
  3. `<head>` 引 `../shared/empty-state.css` 与 `<script src="../shared/empty-state.js">`（提供 `EmptyState.show/showLoading/showOffline` API）。
  4. `.demo-controls`（固定底部居中浮层）含 4 按钮：`加载中(loading)/空状态(empty)/无网络(offline)/有数据(content)`，onclick→`setDemoState(state)`。
  5. `renderDetail()` 按 `demoState` 分支：content→正常渲染非工人/工人视图；loading→`EmptyState.showLoading(area)`；empty→`EmptyState.show(area,{type:'empty-project',title:'暂无项目',desc,actionText:'创建项目'})`；offline→`EmptyState.showOffline(area, retry→loading→1.5s→content)`。`updateDemoBtns()` 按 `data-state===demoState` 高亮；init 末尾调用 `updateDemoBtns()`。
  6. 页面导航 active 项指向 `project-list-empty-demo.html`；黑边框/右侧固定原型导航/胶囊/收起展开均继承最新布局。
- 校验：project-list.html div 273/273 平衡、今日动态存在、待签约已删、nb-back 已删、间距值就位；empty-demo div 274/274 平衡、node --check JS OK、empty-state.js/demo-controls/四态按钮/updateDemoBtns()/demoState='content'/EmptyState 调用均 present、2 个 script 标签（src+内联）。

## bugfix：空状态演示页 demoState≠content 时顶部切换条仍显示项目
- 现象：`project-list-empty-demo.html` 点击「空状态/加载中/无网络」时，顶部 `.topbar`（含「XX小区整体装修 进行中 项目信息 切换」项目切换条）仍展示具体项目，与「无数据」矛盾。
- 原因：`.topbar` 渲染在 `#detailArea` 之外，不受 `demoState` 分支控制。
- 修复：新增 `updateTopbarVisibility()`，在 `renderDetail()` 开头调用——仅 `demoState==='content'` 显示 `.topbar`，否则 `display:none`（含下拉面板一并隐藏）。loading/empty/offline 三态均隐藏，空状态组件按 `min-height:100%` 铺满。
- 校验：empty-demo div 274/274 平衡、node --check 内联 JS OK。

## project-detail-initial-v2.html 对齐「最新项目列表页」浏览器外壳规则
- 需求：将初始项目页（刚创建项目进入的页面）整体布局/规则对齐最新 `project-list.html`（"其他不改变"）。
- 对比结论：initial-v2 已通过 common.css + js/page-nav.js 拥有与 project-list 一致的黑边框、右侧固定原型导航、收起/展开 ▼、原生胶囊、page-nav pill 统一样式；唯一违反项目列表规则的是 nav-bar 左侧 `← 返回` 图标（项目列表页按"主导航/入口页无返回图标"规则已移除；初始页同为入口页适用同一规则）。
- 改动：移除 `<div class="back">← 返回</div>` 及对应 `var backBtn...addEventListener` 守卫式 JS（元素移除后该处理器本就空转，一并清理）。保留初始页特有内容（新家准备中、空态、架构引导 level-hint、add-arch-btn、loading 遮罩、title 居中显示项目名等）。
- 校验：initial-v2 div 102/102 平衡、无 back/backBtn 残留、navTitle 保留、2 个 script 标签成对。

## project-detail-initial-v2.html 整体对齐「项目列表」页面布局/规则（重写）
- 用户指出：仅删返回图标不够——手机框内页面内容布局与项目列表不同；title 右侧胶囊被显示到左侧（common.css 中 `.title.centered` 绝对定位使胶囊左移）；要求本页整体布局/规则以最新 project-list.html 为主，仍体现"刚创建项目进入的初始项目页"效果。
- 方案：将 initial-v2 重写为**自包含页**（与 project-list.html 同源：inline CSS、黑边框、右侧固定原型导航带 ▼ 收起/展开、原生胶囊、吸顶快捷入口+架构选择器、stats-row 合同/任务统计、今日动态、底部 tab-bar）。移除外部 common.css / project-detail.css 依赖以免 `.title.centered`/`.actions`/5列 quick-nav 冲突。
- 关键修复：nav-bar 改用 project-list 写法——`.title`（flex:1 居中，非绝对定位）+ `.nb-actions`（position:absolute; right:12px）承载胶囊 → 胶囊正确居右。
- 内容改造（保持"初始项目"语义）：顶部橙色「新家准备中 + 项目名」头（点击→project-info.html）；待办/我的任务/合同/任务统计/今日动态 全部为 0 空态（empty-tip）；待办卡内「＋ 创建工作组/施工组」引导按钮（→architecture.html）；项目切换条 + 下拉面板单列新项目 + 新建项目按钮。数据取自 localStorage.newProjectData（项目名/面积户型），缺省示例 XX小区整体装修。
- 校验：div 126/126 平衡；node --check 内联 JS OK；nb-actions 存在、无 title.centered、native-capsule/tab-bar/page-nav-title 均 present。

## 项目详情（初始-新版）重复项目名/信息入口修复 + 新增工人视角页

- 修复 initial-v2 顶部 topbar 与 basic-header 双重展示项目名 + 双重「项目信息」入口：topbar `.proj-info-zone` 去除 `onclick` 与「项目信息」hero 文案（仅作当前项目名/状态指示，右侧「切换」负责切换），basic-header 改为「新家准备中 + 面积户型」单一项目信息入口（不再重复项目名）；删除废弃 `renderTopHero` 与 `topHero`。规则对齐 ongoing-v2（项目名只在切换条出现一次、basic-header 仅展示状态+面积户型作为唯一信息入口）。initial-v2 原型导航新增「项目详情（初始-工人视角）」互链。
- 新增 `service-miniapp/project-detail-initial-worker.html`：沿用 project-list 浏览器外壳（黑边框/右侧原型导航收起展开/胶囊在右/无返回/顶部项目切换条），内容采用工人视角结构（我的工种·施工组 + 待我处理 KPI 橙色头、分组待办、快捷入口 全部任务/项目动态/我的合同/更多 + 层级默认选中「泥瓦工作组」并带 ?level= 联动、合同列表直接展示无统计项），整体适配「刚创建项目」空态（待我处理 0、待办/合同空态提示）。原型导航 active 指向自身并互链 initial-v2。
- 校验：initial-v2 div 125/125、worker div 131/131 平衡；node --check 两页内联 JS 均 OK；worker 无 back 按钮、selectLevel/contractListWorker 均 present。

## project-detail-completed-v2.html 重构对齐「最新项目列表页」规则

- 需求：将「项目详情（已完成-新版）」按最新 `project-list.html`（即整合后的项目列表页，原 project-list-detail-new.html 已并入）的规则与布局为主更新；仅更新已完工项目页面的效果，其他页面/功能不动。
- 改动：整页重写为自包含（移除外部 common.css / project-detail.css 链接，沿用 project-list.html 样式体系）。对齐的规则：
  1. 顶部 `proj-info-zone` 两角色统一显示「项目信息」引导文案（点击左侧→project-info.html）；右上「切换」展开项目面板（全部/进行中/已完成 + 新建项目）。
  2. 手机框外侧「演示开关」切换 非工人/工人 双视角（`roleSwitch`→`renderNonWorker`/`renderWorker`）。
  3. 待办事项 下方新增「📌 我的任务」卡（已完成数/我作为执行人的全部任务数，点击→task-list.html?role=exec）。
  4. 底部小程序主导航（项目选中）。
  5. 保留「已完工」语义：绿色 `project-basic-header`（🎉 项目已完工 + 面积户型）、8 份合同 100%、任务 24/24（确认中/执行中=0）、待办/今日动态空态（今日动态显示一条「项目已完工」收尾动态）。
  6. 工人视角橙色强调、快捷入口 4 项（项目动态/架构/成员/资料）、层级默认选中当前工作组并带「默认我的工作组」提示。
- 校验：JS vm.Script 语法 OK；VM 实跑 render 两角色——绿色完工头/空态待办/我的任务 24(非工人)·12(工人)/8 合同 100%/确认中执行中=0/收尾动态均 present；进行中项目(索引1)正常显示示例待办；外部 CSS 链接已移除（仅注释提及，非 link）。
- 注意：本页原依赖 project-detail.css 的旧版 compact/half 样式已整体替换为 project-list.html 体系；如后续其他「已完成」相关页需同步，以本页为基准。

## project-detail-initial-v2 合并冗余头部 + 演示开关替代独立工人页

- 问题：① 顶部切换条（项目名+状态）与下方橙色「新家准备中」块重复呈现同一项目身份；② 此前新增的 `project-detail-initial-worker.html` 独立页不符合「单状态页+演示开关」要求。
- 修复：
  1. 合并头部——移除独立 `.project-basic-header.new-project` 橙色块，切换条 `.proj-info-zone` 左区 `#curProjArea` 直接承载「项目名 + 状态徽标(新家准备中) + 面积户型」，整区点击→project-info；右区保留「切换▾」。项目名/状态各仅一处，且为「进行中」详情页可复用统一头部组件。
  2. 演示开关——手机框右侧新增 `.role-demo-switch`（`非工人/工人` 双态，复用项目列表认证开关样式），`onchange="toggleRoleSwitch(this.checked)"`；非工人态=`renderInitial`（单栏卡片：待办/我的任务/快捷入口+架构/合同概览+任务统计/今日动态）；工人态=`renderInitialWorker`（我的工种·施工组+待我处理KPI+分组待办+快捷入口全部任务/项目动态/我的合同/更多+层级默认选中泥瓦工作组+合同直列）。两态共用合并头部与 `buildLevelOptions`。
  3. 删除独立工人页 `project-detail-initial-worker.html` 及其原型导航入口（已 grep 确认全仓无残留引用）。
- 校验：initial-v2 div 173/173 平衡、script 1/1、node --check 内联 JS OK；role-demo-switch/toggleRoleSwitch/renderInitialWorker/buildLevelOptions 均 present、无独立橙色头、worker 入口已移除、合并头部含 name+status+area；全仓 grep `project-detail-initial-worker` = NONE。
