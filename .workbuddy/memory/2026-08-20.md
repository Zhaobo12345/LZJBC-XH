# 2026-08-20 工作日志

## 新建「项目详情（工人）」页面（服务方）
- 新增 `service-miniapp/project-detail-worker.html`，主体仿 `project-detail-ongoing-v2.html`，差异化：
  - 待办事项展示区域增大一倍：默认显示 4 条（`nth-child(n+5)` 隐藏，原 v2 为 n+3 即 2 条），行距/内边距放大，角标改 4。
  - 快捷入口 5 项（网格 `repeat(5,1fr)`）：架构 / 成员 / **全部任务**（🗂️，置于资料前）/ 资料 / **项目动态**（📝，替换对账单）。
  - 取消「今日动态」卡片（整卡删除，含查看全部入口）。
  - 合同直接展示：卡片标题改「合同」、隐藏 `contract-summary` 统计项、`contract-list` 默认展开（`show`+`max-height:280px`），移除「查看合同」折叠按钮。
  - 取消下方「任务统计」卡片（`.card.task-half` 整块删除）。
  - `selectLevel()` 同步把快捷入口「全部任务」「项目动态」`.quick-nav-item` 携带 `?level=`（与既有 `.more` 联动一致）。
  - 已加入原型导航（page-nav，active 标记）。
- 校验：div/style/script/body/html/head 标签全平衡（138/138）；无残留 对账单/任务统计/今日动态 渲染块（仅注释/JS 引用）。
- 同步更新 `MEMORY.md`：页面布局约定新增工人页条目；架构层级联动约定补 worker 页 `selectLevel` 携带层级说明。

## 工人项目详情页优化（按产品设计建议实施）
- 依据专家评估建议，对 `project-detail-worker.html` 二次改造（不影响其他页面）：
  - ① 首屏头部改工人视角：左「我的工种·泥瓦 · 施工组A」、右「待我处理 4」橙色 KPI 胶囊（替换原「开工60天/75㎡」）。
  - ② 待办按「任务执行·确认 / 合同确认·签约」分组；4 条全展示（移除 `nth-child(n+5)` 隐藏）；待办卡标题条改橙色（模块色区分）；待办/合同均加 `.empty-state` 空态，由 `refreshEmptyStates()` 按条目数切换 `.is-empty`。
  - ③ 快捷入口改 4 主 + 更多：全部任务(红角标4, keep)/项目动态(keep)/我的合同(`data-scroll="#contractList"` 平滑滚动)/更多(`data-action="more"` 切换面板)；「更多」面板收纳 架构/成员/资料；吸顶折叠后仅 `.keep` 两项常驻。
  - ④ 架构层级默认选中工人所属「泥瓦工作组」（`levelText/Icon/Type/Breadcrumb` 与 options `selected` 默认泥瓦，项目部取消默认勾选），右侧加「默认我的工作组」提示；`selectLevel(name, skipToggle)` 新增 `skipToggle` 参数，初始化 `selectLevel('泥瓦工作组', true)` 同步层级联动且不展开下拉。
  - ⑤ 合同行加动作徽标：水电「待我上传」(amber)、泥瓦「待我确认」(red)。
  - JS 增强：新增 `toggleMorePanel()/refreshEmptyStates()`，quick-nav 点击处理 `data-action`/`data-scroll`，文档点击与滚动折叠时关闭 more 面板。
- 校验：div/style/script/body/html/head 标签全平衡（150/150）；quickNav 主入口=全部任务/项目动态/我的合同/更多，more-panel=架构/成员/资料；关键特性（nav-badge/action-badge/todo-group-label/level-default-hint/data-scroll/data-action=more/skipToggle）均就位。
- 同步更新 `MEMORY.md` 工人页条目为优化后口径。

## 工人项目详情页：合同点击跳转新工人合同流程（已确认-受邀方）
- `project-detail-worker.html` 全部合同类跳转由旧 `contract-detail.html`（基础/设计合同）改为新工人合同流程 `worker-contract-detail.html?viewer=receiver`：
  - 合同列表两项（水电/泥瓦）`data-href` 改为 `worker-contract-detail.html?id=demo-shuidian-example / demo-niwa-example&viewer=receiver&status=worker_confirmed_receiver`；动作徽标由「待我上传/待我确认」改为「已确认」(green)。
  - 待办中两类合同 todo（泥瓦确认/木作签约）同步改新流程：`worker_inviting_receiver` / `worker_confirmed_receiver`（按各自语义匹配）。
  - 原型导航「合同详情」改「工人合同详情」，指向 `worker-contract-detail.html?viewer=receiver&status=worker_confirmed_receiver`。
- `worker-contract-detail.js` 预播种全部工种演示合同：`init()` 内 `DEMO_TYPES.forEach(d => ensureDemoSeed(d.type))`，保证按 `id=demo-<type>-example` 跳转各工种均能命中（此前仅 DEFAULT_DEMO_TYPE=shuidian 自动播种，niwa/muzuo 等会 404）。
- 校验：worker 页 div 标签 150/150 平衡；worker-contract-detail.js `node --check` 通过；页面内已无指向旧 `contract-detail.html` 的链接。

## 工人合同详情（新流程）受邀方视角新增「已签约」导航项
- `worker-contract-detail.html` 原型导航「合同状态切换（工人合同）→ 受邀方视角」分组底部（原 3 项 + 接单体验E 链接之后）新增导航项：
  `<a href="worker-contract-detail.html?viewer=receiver&status=worker_signed">已签约(受邀方)</a>`（沿用分组内「确认中（接单体验 E）」的 `<a>` 链接样式）。点击强制以 `viewer=receiver` 加载，展示受邀方视角的已签约页面。
- 分组计数 `status-group-count` 由 `3` 改为 `4`（已确认/抢单失败/已拒绝/已签约 四个受邀方状态）。
- 校验：worker-contract-detail.html div 标签 259/259 平衡；新项与 `viewer=receiver&status=worker_signed` 链接均就位。

## 修复：受邀方「已签约」高亮错位 + 受邀方误显「发起变更」
- 问题：点击「已签约(受邀方)」导航后，发起方「已签约」(data-status=worker_signed) 被高亮，且受邀方视角误展示「发起变更」操作。
- 修复（`worker-contract-detail.js` + `.html`）：
  1. 高亮改为视角感知：`updateStatusNavActive()` 匹配规则 `data-status===status && (!data-viewer || data-viewer===state.viewer)`；发起方「已签约」补 `data-viewer="sender"`，新「已签约(受邀方)」链接补 `data-status="worker_signed" data-viewer="receiver"`。同一 worker_signed 状态下仅对应视角项高亮。
  2. 新增 `expandGroupOf(el)`：高亮项所在「状态分组」自动展开（`content.display=''`、`icon=▼`），确保加载即选中项可见；不影响其他分组收起状态。
  3. `renderActions()` 内：当 `state.viewer==='receiver'` 时过滤掉 `action==='start_change'`，受邀方（乙方）不再展示发起方专属的「发起变更」。
- 校验：JS `node --check` 通过；worker-contract-detail.html div 259/259 平衡；两处 data-viewer 属性就位。

## PRD 补充：已签约（受邀方）需求描述
- 文档：`PRD-合同模块详细规格.html`。
- 新增 **8.4.6 已签约（受邀方）** 小节（置于 8.4.5 方案E 之后，未改动任何既有 8.4.1~8.4.5、7.5.3.5 等内容），含横幅文案/说明、触发条件、页面结构（步骤条四步点亮、签约文件区只读无上传入口、正文/阶段任务/附件只读、底部无主操作）、操作按钮（无）、状态流转、关键逻辑（受邀方不支持发起变更、仅发起方可上传、原型导航视角感知高亮）。
- 8.8 原型导航小节新增「补充」段（在原「分享」注记前插入，未改既有表格行）：说明受邀方分组新增「已签约(受邀方)」链接项并锚定 8.4.6。
- 标签平衡校验通过（div/table/tr/ul 等全匹配）。

## 工人合同「已签约（受邀方）」横幅删除「可发起合同变更」提示
- 位置：`service-miniapp/worker-contract-detail.js`。
- `worker_signed` STATUS_CONFIG 新增 `descReceiver: '合同已正式生效。'`（保留 `desc` 原句不动，供发起方视角使用）。
- 渲染处（bannerDesc 赋值，约 line 643）改为视角感知：`(state.viewer === 'receiver' && cfg.descReceiver) ? cfg.descReceiver : cfg.desc`。受邀方视角移除「如需调整工程内容或金额，可发起合同变更（需对方确认后生效，无需平台审核）。」这句（受邀方不支持发起变更）；发起方视角保持不变。
- 不涉及 PRD 改动（8.4.6 横幅说明文字本就不含该句；7.5.3.5 发起方已签约保留原句）。
- 校验：`node --check` 通过。

## 修复：原型导航高亮两处异常（已签约视角切换 / 履约完成残留高亮）
- 现象1：受邀方已签约页点击发起方「已签约」无反应；现象2：点「已签约（已履约完成）」后再点「拟定中（撤回后）」两项同时高亮。
- 根因1：`updateStatus('worker_signed')` 因 worker_signed 无后缀，自动推导不改变 viewer（停留在 receiver），页面无可见变化。根因2：`demoFulfilled()` 手动给无 `data-status` 的「已签约（已履约完成）」加 active，而 `updateStatusNavActive` 只遍历带 data-status 的项，残留 active 不被清除。
- 修复（`worker-contract-detail.js` + `.html`）：
  1. `updateStatus(status, viewer)` 新增 viewer 入参，显式传入时优先设置视角（含 asUserId），且在 `updateStatusNavActive` 调用前完成；导航项发起方「已签约」onclick 改为 `WCP.updateStatus('worker_signed', 'sender')`。
  2. `updateStatusNavActive` 改为遍历全部 `.status-switch-item`，先统一清 active 再仅点亮匹配项（`match = ds && ds===status && (!dv || dv===viewer)`），清除无 data-status 演示项的残留高亮。
- 验证：node 模拟两个场景均通过（场景1 切到 sender 仅高亮「已签约」；场景2 后仅高亮「拟定中(撤回后)」）；`node --check` 通过。不影响其他状态/页面功能。

## 工人合同（新流程）发起方「已签约」页：乙方非确认两人=一抢单失败+一已拒绝
- 位置：`service-miniapp/worker-contract-detail.js` 的 `presetRejectedDemo(invitations)`（仅作用于 `demo-<type>-example` 演示合同）。
- 背景：发起方「已签约」页（worker_signed 视图）两位非乙方受邀人均显示「已拒绝」；用户要求其中一人改显示「抢单失败」。
- 修复逻辑：规范化受邀人状态——乙方（首位）不参与；仅最后一名非乙方设为 `rejected`+`rejectReason`；其余非乙方若曾为 `rejected`（旧数据残留）则 `delete status/rejectReason` 重置为待确认，使已签约/已确认（发起方）视图回退显示为「抢单失败」（`inviteDisplayStatus` 对 worker_signed/worker_confirmed_sender 的逻辑：非乙方 `status==='rejected' ? '已拒绝' : '抢单失败'`）。
- 幂等、不覆盖真实用户数据；每次加载 demo 合同时重新归一化，可自愈旧 localStorage 残留的「双已拒绝」。
- 校验：`node --check` 通过。

## 合同详情页（基础/设计）：非已签约状态任务项点击→只读「编辑任务」弹窗
- 范围：`service-miniapp/contract-detail.html`（基础/设计合同，仅该页有 `task-item` 跳转 `task-detail.html`；工人合同页无此跳转；变更流程内任务项原本即用只读弹窗、不在改动范围）。
- 改动：
  1. 主视图（draftViewContent/stagesSection，非变更、非编辑态展示区）23 个 `task-item` 的 `onclick="location.href='task-detail.html'"` 全部改为 `onclick="openTaskReadonly(this)"`。
  2. 新增 `openTaskReadonly(taskItem)`（`js/contract-detail.js`，全局暴露 `window.openTaskReadonly` 并加入 `ContractDetailPage`，与 `viewTaskDetail/editTaskDetail` 暴露方式一致）：
     - 若 `state.currentStatus === 'signed'` → `location.href='task-detail.html'`（保持已签约页原跳转行为，不改变既有功能）；
     - 否则从任务项 DOM 解析 任务名称/执行人/确认人（`.task-name` + `.task-meta-row` 内 `.role-tag`），填充 `editTaskModal` 字段（执行/确认标准无展示数据置空），给弹窗加 `.readonly` 类、`modal-title` 改「任务详情（只读）」并 `show`。
  3. `closeEditTaskModal()` 增加清理：移除 `.readonly` 类、还原标题为「编辑任务」，确保后续真正「编辑任务」打开时仍为可编辑态。
  4. `css/contract-detail.css` 新增 `.edit-task-modal.readonly` 规则：`modal-body{pointer-events:none}`、`form-input/form-textarea` 灰底、隐藏 `.search-select` 与确认人 `.remove`、隐藏底部「保存修改」按钮（仅保留头部 ✕ 关闭）。
- 未改动：变更流程 12 个 `viewTaskDetail` 任务项、原型导航「任务详情」链接、其他既有功能。
- 校验：`js/contract-detail.js` `node --check` 通过；23 处 onclick 替换完成、0 处残留 `location.href='task-detail.html'`；HTML 标签平衡（注：contract-detail.html line 17 顶层容器 div 未闭合为历史既有，非本次引入）。

## 合同详情页只读任务弹窗：三标准必填值 + 空人员占位（审核/确认阶段）
- 范围：`service-miniapp/js/contract-detail.js` 的 `openTaskReadonly`（基础/设计合同，审核阶段/确认阶段等非已签约、非变更态点击任务项触发）。
- 问题：原实现三项标准（执行/确认/担责）直接置空，且执行人/确认人为空时仅清空标签（无占位，观感像漏填）。
- 改动：
  1. 新增 `TASK_STANDARD_MAP`（任务名→{exec,confirm,liable}），覆盖合同详情页全部 22 个任务名（含带「已修改/新增」标签的「穿线」「新增点位布线/验收」；名称提取已剔除变更标签文字）。
  2. `openTaskReadonly` 标准取值优先级：`taskItem.dataset`（data-exec-standard 等，未来可挂）→ `TASK_STANDARD_MAP[taskName]` → 通用兜底 `GENERIC_STD`，保证三项**必有值**。
  3. 任务名提取过滤 `task-change-tag` 子节点文字，避免「穿线 已修改」类误匹配。
  4. 执行人/确认人无人员时，标签区渲染 `.confirm-person-tag.empty`「未指定」占位（`roleMap` 补 `'业主':'业主'` 以免业主当未知角色）。
  5. `css/contract-detail.css` 新增 `.edit-task-modal .confirm-person-tag.empty`（虚线边框、弱化文字）。
- 不影响：已签约页（仍跳转 task-detail.html）、变更流程 12 个 viewTaskDetail、真正「编辑任务」可编辑态（closeEditTaskModal 仍清 readonly）。
- 校验：`node --check` 通过；22 个任务名全部命中映射键；HTML/CSS 未改结构性逻辑。
