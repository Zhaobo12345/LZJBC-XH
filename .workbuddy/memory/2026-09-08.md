# 2026-09-08

## 工人合同受邀方：导航更名 + 履约中/已履约完成（上午）
- 用户指令：导航项「已确认（受邀方）-新」→「已签约（受邀方）」；该页顶部加「履约中」图标；其下新增「已签约（已履约）」项对应新页（全部任务执行完成 + 「已履约完成」图标）；不影响其他页面。
- 导航（6 页同步）：confirming-initiator-new / detail / draft-initial-new / receive-confirmed-new / receive-inviting / signed-initiator-new 各改名 1 处 + 下方插入「已签约（已履约）」项（→ worker-contract-fulfilled-receiver.html）+ 受邀方视角计数 4→5。
- `worker-contract-receive-confirmed-new.html`：title 改「工人合同详情（已签约（受邀方））」；hero 右上角新增 `.hero-status-badge doing`「🔨 履约中」蓝底 pill（renderHero 注入）；阶段任务保持「待开始」。
- 新页 `worker-contract-fulfilled-receiver.html`（复制自 receive-confirmed-new 改造）：title「已签约（已履约）」；徽标 `.hero-status-badge done`「🏆 已履约完成」；阶段元信息「N 个任务 · 已完成」、折叠计数「N 个阶段 · 全部完成」、任务行右侧 `.task-done-tag`「✓ 已完成」；自身导航 active。校验 JS OK / div 103/103。
- PRD-合同模块详细规格.html 同步：「已确认（受邀方）-新」全局改名 11 处→「已签约（受邀方）」；8.4.6 过时说明修正（原「已签约(受邀方)独立项已移除」已不成立）+ 补履约中标识 li + 引用 8.4.7；受邀方视角分组清单行加「已签约（已履约）」；新增 §8.4.7 已签约（已履约）（插在 sec-8-5 前，含状态标识/触发条件/页面结构差异点/原型导航 5 项）。
- 校验：PRD div 480/479——差 1 为**存量问题**（插入块平衡：sec-8-4-7 与 sec-8-5 处深度均 5），非本次引入，未处理。
- 备注：worker-contract-detail.html/.js 中 CSS/JS 注释仍保留旧名「已确认（受邀方）-新」作页面指代，未改（避免触碰逻辑文件）。

## 受邀方「我要干的活」→「承接任务」（上午）
- 用户问受邀方各状态页「我要干的活」名称是否合适并要推荐替换。分析：与发起方「阶段任务」术语不一致、口语化与合同正式场景不符、未体现阶段结构；给 4 个候选（我的阶段任务/承接任务/履约任务/阶段任务），用户确认选「承接任务」。
- 全局替换 5 文件（replace_all）：`worker-contract-receive-inviting.html` / `worker-contract-receive-confirmed-new.html` / `worker-contract-fulfilled-receiver.html`（标题 + HTML/JS 注释）、`worker-contract-detail.js`（受邀方变更态卡片流标题 + 注释 4 处）、`PRD-合同模块详细规格.html`（卡片流描述 + 结构图 + 8.4.x 各节共 10+ 处）。
- 校验：grep 全仓 0 残留；3 页 div 平衡（115/115、103/103、103/103）；worker-contract-detail.js vm.Script 语法 OK。PRD div 480/479 仍为存量问题未动。
- 教训：并行 Edit 不同文件时两个文件报 EBUSY（资源锁冲突），串行重试即成功——多文件编辑建议串行或分批。

## 拟定中（撤回后）正文模板预览弹窗对齐「拟定中（发起方）-新」（上午）
- 用户指令：`worker-contract-detail.html`（worker_draft 撤回后）的正文模板预览弹窗应与 `worker-contract-draft-initial-new.html` 的预览效果及布局相同。
- 差异：原预览为合规版风格（modal-header 16px / 元信息条 meta-label+value / 横向 modal-btn 双按钮 / max-height 90%）；-新为 wcd-sheet 底部抽屉（sheet-head 15px / 简洁元信息 span / 纵向全宽药丸「取消」「使用此模板」/ max-height 72% / 阶段序号蓝圈+橙色顺序标签）。
- 实施：① `worker-contract-detail.html` 新增 `.wc-wcd-*` 作用域 CSS（-新 同值）；`#wcTemplatePreview` 标记重构为 wcd-sheet 结构（保留全部 id 与 data-page-node-id，遮罩点击可关）；删除旧 `.wc-template-preview-modal` 选择器（保留 `.wc-template-modal` 选择弹窗）与旧 `.preview-meta*` 块。② `worker-contract-detail.js` `previewDraftTemplate`：元信息改 `<span>类型：…</span>` 简洁条；阶段明细改 -新 inline-style（蓝圈序号 + #fa8c16 顺序标签）；注释更新。
- PRD-合同模块 §7.6.3 第 3 条同步：预览弹窗改为对齐「拟定中（发起方）-新」描述，并修正过时内容描述（原写「工程内容+补充条款」，实为「违约责任+固定详细条款（不可修改）」+任务可就地展开三项标准）。
- 校验：JS 语法 OK；detail.html div 381/381、style 大括号 454/454；wc-template-preview-modal / preview-meta-item / preview-stage-* 残留 0。PRD div 480/479 仍为存量差 1。

## 2026-09-08 10:43 PRD项目模块对齐最新原型(项目主导航页)
- 新增「零、小程序入口与角色路由规则」(0.1 启动页进入规则 / 0.2 主导航「项目」页面规则)：仅工人角色(含多工人)进「项目」、含任一非工人角色/仅非工人进「首页」；项目列表排序=进行中前+已完成后+签署时间倒序+首个为默认展示。
- 重写 §2 项目列表页 以匹配 `project-list.html`(项目及详情)：2.1 页面定位 / 2.2 排序规则 / 2.3 三段结构(切换条+面板+详情区) / 2.4 切换面板筛选(原状态选项卡) / 2.5 项目选项规格(原卡片) / 2.6 新建项目入口(原FAB) / 2.7 非工人视角详情区 / 2.8 工人视角详情区 / 2.9 空状态(原2.7)。删除旧顶部状态选项卡+FAB描述；演示开关(RoleSwitch)不写入PRD。
- §3.1 概述同步：进行中详情已内联至项目主页面(§2)，修正失效的 project-detail-ongoing-v2.html 链接→project-list.html，并标注 initial/completed 独立页。
- TOC 同步新增 2.4~2.9 子项。校验：PRD HTML BALANCED。

## 非工人合同「编辑任务默认执行人=乙方」补齐 + PRD 待办表述改写（上午）
- 用户指：非工人合同编辑任务默认执行人=乙方在原型未实现，要求按需求补充；另要求按最新规则改写我提到的「任务执行待办生成」表述。
- 核查：`service-miniapp/js/contract-detail.js` 实际已有 `getPartyBList()`+`editTaskDetail` 默认逻辑，但仅读 `#editPartyB` 草稿下拉（非草稿态靠 display:none 仍在 DOM 可取，脆弱）；且同文件 `state.isWorker` 分支下取不到乙方（合同-detail.html 可 `?worker=1` 走此路径）。
- 实施：`getPartyBList()` 重写——工人路径取 `state.workerContract.partyBName`/invitations（对齐 worker-contract-detail.js）；非工人优先 `#editPartyB`、兜底 `state.newContractData.partyBName`。`editTaskDetail` 默认分支（无执行人+乙方仅 1 人→填乙方）保持不变。校验 vm.Script OK。
- PRD-合同模块 §7.5.3.4（约 3983 行）：原「执行人待办生成（任务执行待办）」按 2026-09-07 最新规则改写为「执行人提醒（微信服务号通知）」，引用《PRD-完整文档》§7.10（锚点 sub-7-10），标注不再生成应用内「合同任务执行」待办；同节 3984 行交叉引用「执行人待办生成」前置约束→改为「微信服务号通知」提醒规则。PRD div 480/479 差 1 仍为存量未动。
- 待确认（超出用户指名的一处）：PRD-合同模块 §3.10.14「执行人待办生成（权威规则）」(1799–1827)、§9.5.5 工人变更生效待办(3241–3246)、3503 行等仍描述已取消的「合同任务执行」待办类型，与最新规则冲突，需用户授权后一并改写。
- 备注：非工人合同默认状态即 `draft`、#editPartyB 默认选中「李四（水电组长）」，草稿态编辑空执行人任务本即可默认填乙方；本次改动主要为健壮性补强与工人路径修复。

## PRD-合同模块「任务执行待办」表述全面改写（上午，用户授权全面清理）
- 用户确认：除 §7.5.3.4 外，§3.10.14 / §9.5.5 / §5.15 等全部「执行人待办生成/失效」表述一并按最新规则改写。
- §3.10.14「执行人待办生成（权威规则）」整段改写为「执行人提醒（微信服务号通知 · 权威规则）」：取消应用内「合同任务执行」待办，改为签约/补配置时经微信服务号通知触达执行人；锁定规则保留（仅"不生成待办"→"不发送提醒"）。同节及 §5.15 锁定说明（1798/1801/1825/1827/1830/2967/2968）同步"生成执行人待办"→"发送微信服务号通知提醒"/"不向执行人发送提醒"。
- §9.5.5「执行人待办生成/失效（作用域限定为变更项）」改写为「执行人提醒（微信服务号通知 · 作用域限定为变更项）」：变更生效仅对变更项任务发服务通知，失效/去重改走 §7.10 服务通知。
- 不动：§6.3.3（3503 行，标签「变更」待办，属有效类型）、§4997（工人合同已与最新规则一致：不生成应用内待办、走服务通知）。
- 校验：grep 残留仅剩 3 处显式"已取消"声明（1799/3241/3983）；PRD div 480/479 差 1 仍为存量未动。MEMORY.md 同步补"合同模块已改写"提示。

## 2026-09-08 11:40 PRD项目模块去重重构(零/二/三章)
- 零章标题「小程序入口与角色路由规则」→「入口与角色路由」并补入左侧目录(TOC 新增 nav-page-entry-route，含 0.1/0.2 子项)；0.2 删除重复的「项目列表排序规则」段落(排序规则仅在 §2.2 保留)。
- §2.3 页面结构：删除详情区详细内容，改为单行「详情区(按角色展示 工人/非工人 视角，布局与差异详见 §3.14)」引用。
- §2.6 删除「与原 FAB 的差异」旧版本差异注记。
- §2.7(非工人视角)/§2.8(工人视角)整段删除，详情区描述移至 §3。
- 新增 §3.14「详情区双视角差异(工人/非工人)」对比表(待办/我的任务/快捷导航/层级选择器/合同/任务统计/今日动态)+「两视角差异要点」注记；§3.1 概述与 §2.1/0.2 引用同步指向 §3.14；TOC 同步(删 2.7/2.8、增 3.14)。
- 校验：HTML BALANCED、CSS 64/64、无 §2.7/§2.8 残留、无旧 FAB 差异注记。

## 2026-09-08 13:28 PRD 编辑任务执行人默认值规则「非工人/工人」分述
- 用户指：§3.5.2.1「编辑任务·执行人默认值规则」此前写成「非工人合同与工人合同通用」有问题，应分别描述——非工人乙方仅能选 1 人，工人可最多选 3 人意向乙方、最后其中 1 人为最终乙方。
- 核查代码：`worker-contract-detail.js` `getPartyBList()` 已确认/已签约取 `partyBName`(唯一)，拟定/邀请中取意向乙方名单(1~3)；`contract-detail.js` `getPartyBList()` 非工人取 `#editPartyB` 单选(恒 1 人)。
- 实施：§3.5.2.1 note-block「通用」写法拆为「① 非工人合同（单选，恒默认填唯一乙方）/ ② 工人合同（意向 1~3 + 最终 partyBName 唯一；拟定/邀请中多人候选时默认空、已确认/已签约默认填最终乙方）」；同步改 §7.5.3.4(3984 行)交叉引用「乙方仅一人情形」→「② 工人合同·已确认/已签约情形」。
- 不动：§3.10.14「执行人提醒（微信服务号通知）」仍标「通用」——该提醒规则确为两类合同通用，保留。
- 校验：grep 「乙方仅一人」0 残留；PRD div 480/479 差 1 仍为存量；p/ul/li 148/148、57/57、273/273 均平衡。

## 2026-09-08 13:33 默认值规则按「章节归属」二次修正（§3.5.2.1 仅非工人 / §7.6 仅工人）
- 用户指：PRD 第一~六章均非工人合同，§3.5.2.1（第三章）内混写工人合同产生歧义，应按「各自章节写各自需求」清晰分置。
- 文档章节归属确认：第一~六章=非工人（基础/设计）；第七章=工人合同（发起方）；第八章=工人（受邀方）；第九章=工人变更。
- 实施：① §3.5.2.1（第三章）改为**仅非工人合同**规则（删除 ② 工人分支），并加一行指针「详见第七章 §7.6 中『编辑任务·执行人默认值规则（工人合同）』」。② §7.6（第七章·拟定中编辑交互，7.6.2 阶段任务 ul 之后、7.6.3 之前）新增**工人合同**专属 note-block（两段式：拟定/邀请中按意向名单计数——仅1人填该意向、多人默认空非必选；已确认/已签约填 partyBName 最终乙方，引用 §7.5.3.4）。③ §7.5.3.4(3984 行)失效交叉引用「§3.5.2.1 ② 工人合同」→ 改为「第七章 §7.6 中『执行人默认值规则（工人合同）』」。
- 校验：§3.5.2.1 行 1175「（非工人合同）」、§7.6 行 4006「（工人合同）」、指针与交叉引用均就位；无「分述/② 工人」残留；div 481/480 差1仍为存量（新增 note-block 成对，未扩大差）；p/ul/li 均平衡。
- 备注：5019 行「② 工人合同动态」属签约动态 note（PC 端操作记录语境），与默认值规则无关，非本次改动。

## 2026-09-08 14:00 工人合同「新建任务」交互统一为弹窗（对齐非工人 addTaskModal）
- 用户指：工人合同与非工人合同详情「新建任务」交互不一致，要求统一为非工人效果（弹窗先填全字段→确定添加），主体功能不变、不影响其他功能。
- 旧差异：非工人版 `addTaskToStage`+`addTaskModal` 弹窗（名称/执行人/确认人/执行标准/确认标准/担责标准全字段）；工人版 `addDraftTask` 仅内联追加一行「新任务」（无弹窗无标准字段）。
- 实施（两个工人合同页统一改造）：
  - `worker-contract-detail.js`：① 确认人逻辑泛化 prefix（'edit'/'new'，`selectConfirmer/removeConfirmer/updateEditConfirmPersonTags→updateConfirmPersonTags`，编辑路径行为不变）；② 新增 `openAddTaskModal(si)/closeAddTaskModal()/confirmAddTask()`（必填校验：名称+三标准；执行人选填默认空；确认人最多5人；写入 `s.tasks.push({name,exec,conf,execStd,confStd,liableStd})`）；③ `renderDraftStageTask` 按钮 `addDraftTask`→`openAddTaskModal(i)`；④ WCP 导出 3 个新函数。`addDraftTask/deleteDraftTask` 函数保留未删。
  - `worker-contract-detail.html`：新增 `#addTaskModal`（复用 `.edit-task-modal` 居中样式与 form-group/search-select/confirm-person-tag 类，与编辑弹窗同视觉；字段集对齐非工人 addTaskModal；执行人下拉静态人员选项+`WCP.` 前缀函数）。
  - `worker-contract-draft-initial-new.html`（拟定中（发起方）-新）：① `addTaskState={current,confirmers}` 新增；② 确认人/执行人逻辑泛化 prefix（`fillPersonDropdown(prefix)` 动态填充、`selectExecutor/removeExecutor/selectConfirmer/removeConfirmer/updateConfirmPersonTags` 全部支持 'new'/'edit'）；③ 新增 `openAddTaskModal/closeAddTaskModal/confirmAddTask`（无 WCP 前缀，confirmAddTask 写入 C.stages[si].tasks）；④ 按钮→`openAddTaskModal(i)`；⑤ 新增 `#addTaskModal` 标记（下拉为空由 `fillPersonDropdown('new')` 填充）。**注：该页弹窗 HTML 在昨日 429 中断时未落盘，今日续作时发现并补齐——教训：长任务中断后续作必须先 grep 复核每个 Edit 是否落盘，不能只信对话记录。**
- PRD 同步：§7.6.2「内联实时改任务名称/addDraftTask」→「添加任务按钮打开添加任务弹窗（全字段，与非工人 §3.5 一致），保存必填校验」；§7.6 提交校验行同步。PRD-合同模块 div 481/480 差1仍为存量。
- 校验：detail.js JS 语法 OK；detail.html div 458/458、CSS 454/454；draft-initial-new div 275/275、CSS 195/195、内联脚本语法 OK；两页 12 个 new* 元素 id 全部就位；旧 `onclick="WCP.addDraftTask|addDraftTask"` 按钮 0 残留。
- 验证路径：两页「拟定中」阶段卡片「+ 添加任务」→ 弹出「添加新任务」弹窗（名称*/执行人选填/确认人≤5/执行*/确认*/担责* 三标准）→「确定添加」→ 任务行追加并 toast「任务已添加」。

## 2026-09-08 14:10 服务方项目详情页规格按角色视角分述
- PRD-项目模块详细规格 §3 按 非工人/工人 视角分述（依据原型 project-list.html renderNonWorker/renderWorker）：
- §3.2 页面结构拆为 ① 非工人视角 / ② 工人视角 两张结构图；补入原缺失的「我的任务卡片」区块（两视角均有，→ task-list.html?role=exec）。
- §3.3 快捷导航规格拆为 ① 非工人（架构/成员/资料/对账单）② 工人（项目动态/架构/成员/资料，不含对账单）③ 共性行为（固定/展开收起/项目信息已移出）三张表；对账单行注明「工人视角不提供该入口」。
- §3.5 字段表：快捷导航栏 rowspan 5→6，入口数量区分视角，新增「项目动态入口（仅工人视角）」行与「我的任务卡片」行。
- §3.7 今日动态加「适用视角」行（仅非工人展示）。
- §3.8 交互表快捷入口区分视角。
- §3.14 去重：引言注明 §3.2/3.3/3.7 已按视角分述，吸顶快捷导航/今日动态两行改 colspan=2 引用式描述。
- §9.1 对账单列表页入口注明仅非工人视角提供（业主端 §18.1 不受影响，未改）。
- 教训：Edit 批量调用中 4 处报成功但未落盘（§3.5 入口数量/§3.8 快捷入口/§3.14 两行），经 grep 复核后重新应用；批量编辑后必须逐项落盘复核。
- 校验：HTML BALANCED，12 项改动点检查全 PASS，旧未区分表述零残留。

## 2026-09-08 14:20 添加任务弹窗「执行人默认值」规则落地（分工人/非工人）
- 需求：非工人添加任务执行人默认=乙方（可修改，可选范围=项目内全部人员）；工人添加任务执行人默认规则与现有编辑任务规则一致。改原型后同步 PRD。
- 非工人（contract-detail.html + js/contract-detail.js）：① `addTaskToStage` 清空后按 `getPartyBList()` 预填执行人标签（默认=乙方，local addRoleMap 渲染角色，removeExecutor('new') 可清除）；② `#newTaskExecutorDropdown` 静态选项 5→7（补 孙八·监理、业主·业主），对齐页面 roleMap 全集=项目内全部人员。
- 工人（worker-contract-detail.js + worker-contract-draft-initial-new.html）：两页 `openAddTaskModal` 预填规则=编辑任务同款——`getPartyBList()`/`getDraftPartyBList()` 仅 1 人→默认填该乙方（ROLE_MAP 渲染），多人→默认空；均可修改/清空。
- PRD-合同模块：① §3.5 交互表「添加任务」行补「执行人默认填当前乙方（可修改/可清空，非必填），可选范围=项目内全部人员」；② §3.5.2.1 note 标题改「编辑 / 添加任务 · 执行人默认值规则（非工人合同）」+ 新增添加任务同规则段；③ §7.6 note 标题改「编辑 / 添加任务 ·（工人合同）」+ 新增添加任务同规则段（可选范围=该工人合同工种候选）；④ §7.6.2 添加任务描述补默认值引用。
- 校验：contract-detail.html div 1109/1109；draft-initial-new div 277/277；两 JS 语法 OK；PRD p/ul/li 150/57/273 平衡（div 481/480 差1存量）。
- 备注：非工人 roleMap 存在 张三（项目经理/兼业主甲方案例）数据瑕疵，遵循现有原型数据未改；执行人标签 remove onclick 合规版走 `ContractDetailPage.removeExecutor('new')`。

## 2026-09-08 14:30 工人合同「编辑任务」执行人下拉修复（可选范围=项目内全部人员）
- 用户报告：工人合同编辑任务点击「请选择执行人」无任何可选人员；要求可选范围=项目内全部人员。
- 根因：`-new` 页（worker-contract-draft-initial-new.html）`editTaskDetail` 从未调用 `fillPersonDropdown('edit')`，编辑弹窗执行人/确认人下拉（空容器）永远无选项；detail 页编辑弹窗为静态 7 人（仅工种代表），与「项目内全部人员」不符。
- 修复：① `-new` 页 `editTaskDetail` 开头补 `fillPersonDropdown('edit')`（执行人+确认人下拉一并修复，名单=WORKER_CANDIDATES 23 人项目全员）；② detail 页 `editTaskExecutorDropdown` 与 `newTaskExecutorDropdown` 两处静态选项 7→23 人（陈庄/陈业主/王设计/刘项目总/孙工长 + 六工种各 3 人，名单与 -new 页 WORKER_CANDIDATES 完全一致），node 脚本按行块替换并 abort-保护；确认人下拉未动（用户未要求）。
- PRD-合同模块 §7.6 note（工人版）：编辑任务 p1 补「执行人可选范围为项目内全部人员（业主/设计师/项目总/工长/各工种人员等，点击「请选择执行人」展开）」；添加任务段「可选范围为该工人合同工种候选人员」→「与编辑任务一致，同为项目内全部人员」。
- 校验：detail.html div 586/586、draft-initial-new div 277/277、内联脚本语法 OK；PRD p 150/150（div 差1存量）；`fillPersonDropdown('edit')` 调用就位；detail 页两下拉各 23 个 `WCP.selectExecutor` 选项。
- 备注：detail 页确认人下拉仍为 7 人静态——若后续要求确认人也扩为项目全员，参照本次脚本同样替换 selectConfirmer 块即可。

## 2026-09-08 15:00 PRD §3.5.2.1 简化 + §3.10.14 补「签约自动归属乙方」规则
- 用户要求：① §3.5.2.1「编辑/添加任务执行人默认值规则」描述过于啰嗦，直接简洁化（不改主要需求）；② 删除「注：工人合同因乙方为「意向候选 1~3 人 + ……」整行；③ 补充规则「当合同状态更新为已签约时，将当前任务无执行人的执行人全部更新为乙方人员」，加到合适位置。
- 改动：① §3.5.2.1 note-block 三段并为两段（编辑/添加合并为一段：无执行人→默认填当前乙方、已有不覆盖、可修改/可清空、非必填；可选范围=项目内全部人员）；② 注行删除，grep 0 残留；③ 新规则 li「无执行人任务自动归属乙方（签约时）」插入 §3.10.14 关键交互第 2 条（任务锁定解除之后、执行人提醒之前），含「仅首次进入已签约触发一次、已有执行人不覆盖」约束 + 交叉引用 §7.5.3.4（工人版同规则）。
- 校验：锚点 sec-7-5-3-4/sec-3-10-14 存在；p/ul/li 149/57/274 平衡（div 481/480 差1存量）。

## 2026-09-08 14:35 「拟定中（撤回后）」执行人规则补同步（非工人编辑 / 工人添加）
- 用户报告：① 非工人「拟定中（撤回后）」编辑任务执行人规则效果未同步；② 工人「拟定中（撤回后）」添加任务执行人规则效果未同步。
- 根因：① 非工人（contract-detail.html）上一轮只把**添加任务**下拉扩为项目全员 7 人，**编辑弹窗** `#editTaskExecutorDropdown` 仍是 5 人（缺孙八/业主）——可选范围未同步；② 工人（worker-contract-detail.js + js/contract-detail.js）`getPartyBList()` 的 `c.invitations || state.editInvited` 有缺陷：撤回后 ContractStore.withdrawConfirm 清空 `invitations=[]`（空数组为 truthy），永不回退 `state.editInvited` → 撤回后即使邀请面板已选 1 名意向乙方，添加/编辑任务也预填失败。
- 修复：① contract-detail.html 编辑弹窗执行人下拉 5→7 人（补 孙八·监理 / 业主·业主，与添加任务下拉对齐=项目内全部人员）；② 两处 `getPartyBList()` fallback 改为 `(c.invitations && c.invitations.length) ? c.invitations : (state.editInvited || [])`——撤回后取邀请面板当前已选意向名单，意向仅 1 人即预填、多人默认空（规则不变，仅修落数据来源）。
- 状态链路确认：非工人撤回确认→`updateContractStatus('draft')`（与拟定中同态，编辑任务 btn 版 editTaskDetail 本有 getPartyBList 兜底，本次补齐下拉后规则全量生效）；工人撤回→`worker_draft`→`renderDraftContent()`→添加/编辑任务均经 `openAddTaskModal`/`editTaskDetail`→`getPartyBList()`。
- PRD：规则本身未变（拟定中按意向乙方名单计数，撤回后属拟定中），无需改文档。
- 校验：两 JS 语法 OK；contract-detail.html div 1117/1117；编辑/添加两下拉各 7 选项（含孙八/业主）；两处 fallback 修复落盘。


## 撤回后添加任务预填二次修复（2026-09-08 下午）
- 用户反馈撤回后添加任务执行人预填仍未生效。复核发现上轮 fallback 不彻底：`getPartyBList()` 仅在 `c.invitations` **为空**时回退 `state.editInvited`；但撤回后页 demo 合同 invitations 仍留 3 条旧记录（多人抢单演示数据），且「重新选择乙方」流程 reselectPartyB 也不清空 invitations——用户在邀请面板把意向乙方调整为 1 人后（未重新提交），getPartyBList 仍读 3 条旧记录 → 按多人规则默认空，**面板实时选择被忽略**。
- 修复（两处镜像）：worker-contract-detail.js 与 js/contract-detail.js 的 getPartyBList 增加优先分支——`state.status==='worker_draft' && state.editInvited.length` 时以邀请编辑面板实时选择为准（旧 invitations 含 replaced/pending 不作默认值依据）。编辑任务(2779)与添加任务(2870)共用该函数，一处修复两处生效。
- 规则不变：面板 2~3 人仍默认空（规则内）；1 人即预填。PRD 无需改。
- 未决（待用户确认）：-新 页 demo editInvited 初始 1 人（预填直接可见），撤回后页 demo 初始 3 人（需面板调整为 1 人后可见）；若希望撤回后 demo 也初始 1 人需另行确认（会改意向乙方面板初始演示）。
- 校验：两 JS 语法 OK；修复落盘 OK；四场景模拟（3人默认空/面板调1人预填/真实撤回预填/已签约预填）全符合预期。

## PRD 工人合同受邀方入口文案对齐原型（2026-09-08 下午）
- §8.7 标题「简化接收体验方案（A / C / E）」→「受邀方邀约入口」（TOC 同步）；全 PRD 清除「方案 A/C/E」「接单体验 E」「简化接收体验」提法，改为直接结果描述：微信服务通知 / 应用内顶部通知条 / 整页接单卡片流。
- §8.4.5 标题 →「整页接单卡片流（确认中页）」；原型导航入口改为「受邀方视角分组『确认中（受邀方）』直接链接本页」（原「分组底部分隔线后」已过时）。
- 导航口径对齐原型：受邀方视角 5 项（确认中/已签约/已签约已履约/已拒绝/抢单失败），演示数据 2 项无方案标注；ch7 主页行「受邀方视角 4」→ 5。
- 删除历史下线备注（原 E 验证主页等）；已删页面（share-navigation 等）PRD 无残留。
- 遗留（原型侧未改）：worker-contract-detail.js STATUS_CONFIG `worker_confirmed_receiver` desc 仍提「上传纸质合同扫描件」，与新流程不符，待用户确认后改。
- 校验：_validate_html.py BALANCED。

## 2026-09-08 15:20 同类型工人合同多份快捷创建-方案讨论（未实施）
- 新需求：一个项目内同类型工人合同多份（一工人一份），字段集合相同但值可不同（金额/工期/任务）。倾向方案：复制发起（沿用类型/文本/任务/工期/金额原值可改，必改=意向乙方）。
- 现状唯一合同规则链：service/owner architecture.html「一个工作组只能有一个合同」阻断（service:1997 / owner:1836）、groupContracts 组名→单合同 1:1（architecture.html:1441）、施工组前置=工作组有合同（:1534）、PRD-项目模块 §6.6.1 前置校验2:2570、乙方确认 joinedArchitecture=组名（contract-store.js:159）。
- 候选改法：①工人合同放开一工作组多合同（groupContracts 改数组+聚合统计口径）②入口放行+聚合展示最小改。基础/设计合同是否同步放开待用户定。方案确认后实施并同步 PRD。

## 工人合同「签约文件区」全面下线 + PRD 第七章对齐（2026-09-08 16:00）
- 用户确认：最新流程已取消上传签约文件环节，工人合同不存在签约文件区域（答复上轮遗留问题）。
- 原型清理：worker-contract-detail.js 删 renderSignArea/pickSignFile/onSignFilePicked + WCP 导出两项 + 附件卡签约文件子卡（现仅渲染合同附件）；STATUS_CONFIG worker_confirmed_receiver desc 去「等待上传扫描件」；worker_signed 版本时间线删「上传签约文件」项（合并为乙方确认即生效）；detail.html 删 #signFileInput 与 .sign-file-* CSS；signed-initiator-new 变更 desc 去「需上传签约文件」；receive-inviting 下一步/CTA 提示改「确认即生效」。
- PRD 第七章：§7.10 改名「更多菜单渲染规则（按状态）」，删 7.10.1 签约文件区表（原 7.10.2 → 7.10.1），TOC 同步；§7.5.3.4 触发条件删签约文件子卡句；§7.9 受邀方视角（4）→（5）；3985 引用文案同步。
- 校验：node --check JS_OK；PRD + 3 个原型页 HTML BALANCED；无 renderSignArea/signFileInput 残留。
- 教训：Edit 删除函数时必须同步清理 WCP 导出引用，否则对象字面量构造即 ReferenceError 整页白屏。

## 2026-09-08 15:40 同类型工人合同多份-方案确认+两个演示页
- 用户确认：①架构放开「一工作组多合同」且适用于全部合同类型（含基础/设计）②复制发起入口=合同详情页「···」+架构工作组节点 ③差异项补充：违约责任/阶段任务/附件均可不同（金额/工期/乙方之外）。
- 新增两个独立演示页（不影响已有页面）：
  - service-miniapp/contract-copy-initiate-demo.html：复制发起交互演示——原合同摘要卡「···」菜单→复制发起底部抽屉（类型锁定/名称/乙方chips含在途工人禁用/金额·工期·违约责任沿用值可改并带「沿用原值→已调整」徽标/阶段任务增删改/附件增删）→确认后生成差异对比结果卡（已调整/沿用）。
  - service-miniapp/architecture-multi-contract-demo.html：一工作组多合同演示——规则说明卡+架构树（泥瓦工作组2份并列+聚合统计签约/总进度/金额合计）+「＋创建合同」放行弹「复制发起(推荐)/空白新建」选择+简化复制弹窗（在途工人禁用）→确认后列表新增第三份、徽标与聚合统计联动。
- 两页均含微信导航栏（状态栏44+nav44+胶囊）、375×812手机框、全局无滚动条、触控区≥38px；link common.css 复用变量，其余自包含。
- 校验：两页 BALANCED、CSS 80/80 与 83/83、node --check JS_OK。
- PRD 同步待演示页验收后进行（架构规则改写 §6.6.1 前置校验2 + 合同模块新增复制发起节）。

## PRD vs 原型一致性审计落地（2026-09-08 17:00）
- 审计确认 5 项不一致并经用户确认全部修正：
  1. 删 PRD §8.4.2「已确认（受邀方）」冗余节（与 §8.4.6 同页重复）；8.4.3~8.4.7 顺延重编号为 8.4.2~8.4.6（标题+注释+id+锚链接+§7.5 引用同步，全文 8.4.7 零残留）。规则内容不丢失：8.4.5（已签约受邀方）触发条件补「group 字段更新自动加入架构层级」、关键逻辑新增「过渡标识与成员登记」li（worker_confirmed_receiver=瞬时过渡标识、首胜或发起方指定、成员登记/权限分配规则全文保留）。
  2. §7.5 状态机表「已确认」行改写为「确认即生效过渡标识」表述（原「等待合同正式生效」与确认即生效矛盾）。
  3. §8.4.1 状态流转改为「首位确认者 → worker_signed（见 8.4.5）」（原 worker_confirmed_receiver 旧流转）。
  4. 确认人可选范围统一=项目内全部人员：worker-contract-detail.js 新增 fillConfirmerDropdown()（WORKER_CANDIDATES 23 人，edit/new 两弹窗，init 调用覆盖静态 7 人选项）；PRD §7.6 工人 note 补「确认人可选范围」段（最多5人、选填、两弹窗一致）。
  5. 删除孤儿文件 worker-contract-exit.js（乙方退出/甲方重选演示 JS，全项目零引用、PRD 无描述，用户确认删）。
- 校验：node --check JS_OK；PRD BALANCED；8.4.x 章节序列 8.4.1~8.4.6 连续。
- 教训：批量结构化 PRD 编辑用 Python 标记式替换 + count 断言 fail-fast 最稳（本次 2 处 count 不符被断言拦截，避免误替换后重跑成功）。

## 2026-09-08 17:40 两个演示页改为基于原页面叠加新需求
- 用户反馈：architecture-multi-contract-demo.html 覆盖了原「项目架构」功能（邀请成员/添加工作组/退出等），contract-copy-initiate-demo.html 与现有合同详情页不一致——均须改为「原页面为底本 + 叠加新需求」。
- architecture-multi-contract-demo.html = architecture.html 完整复制后叠加：①groupContracts 组名→数组（泥瓦2份/水电1份/木工油漆空）②头部新增「N份合同」统计+演示说明卡（不动原5工作组/24成员）③节点徽标「已签约·N份合同」④水电/泥瓦节点新增「复制发起」操作项⑤查看合同弹窗列多份+聚合行⑥退出/删除/添加施工组守卫改数组语义（some已签约/length>0）⑦goCreateContract 有合同时弹「复制发起(推荐)/空白新建」选择⑧复制发起抽屉（乙方在途禁用/金额工期违约责任沿用→已调整徽标）→确认后徽标/统计/列表联动。原有功能零删减。
- contract-copy-initiate-demo.html = worker-contract-signed-initiator-new.html 完整复制后叠加：更多菜单首项「📋复制发起」→ wcd-sheet 底部抽屉（锁定类型/固定条款/阶段任务/附件，乙方chips张水电禁用，金额/工期/违约责任沿用原值可改带徽标）→确认后 toast+差异对比结果（infoModal 复用）。原页布局/交互（步骤条/横幅/合同方卡/三Tab/发起变更CTA）零改动。
- 教训：Edit 消费上下文时误吞 permission-notice 开标签，已修复并复验。
- 校验：两页 BALANCED、CSS 152/152 与 253/253、node --check JS_OK、无「一个工作组只能有一个合同」残留、原 architecture.html 与 worker-contract-signed-initiator-new.html 未改动。

## PRD §7.5.1 状态机去除「已确认」独立状态（2026-09-08 17:40）
- 用户定案：工人合同主流程状态机仅保留「已签约」，不设「已确认」。
- 删除 §7.5.1 状态机表「已确认」行 + 第九章状态权限表「已确认·受邀方」行；§7.5 引言示例 key 改 worker_signed；§2 字段表引用、§7.11.1 总表（确认中行去已确认 key）、§7.11.3 标题、§7.8 只读说明、§7.10 更多菜单枚举、ch7 页面清单行（已确认（受邀方）→已签约（受邀方））、ASCII 流程框图（补空格对齐）、§7.6 执行人默认值 note（已确认/已签约→已签约）共 9 处枚举同步清理。
- 保留：§8.4.5「过渡标识与成员登记」li（解释 worker_confirmed_receiver 为瞬时过渡标识）；各处「无独立已确认状态」否定式说明；§8.4.2/8.4.3 对原型 banner 实际文案的描述。
- 原型同步：worker-contract-receive-inviting.html 确认接单后跳转 status=worker_confirmed_receiver → status=worker_signed（确认即已签约）。STATUS_CONFIG 内部保留过渡态 key。
- 校验：PRD BALANCED；worker_confirmed_receiver 仅剩 §8.4.5 说明 1 处。

## 工人合同三页「更多/合同操作」入口补齐（2026-09-08 11:37 后）
- 需求：①「确认中（发起方）」页（worker-contract-confirming-initiator-new.html）「更多」菜单缺「复制发起」；②「已签约（受邀方）」（worker-contract-receive-confirmed-new.html）与「已签约（已履约）」（worker-contract-fulfilled-receiver.html）顶部缺「合同操作 更多」工具栏（版本记录/变更记录/导出合同文本）。
- 实施：① confirming 页 renderMoreOps 首位补「📑 复制发起」（openCopyInitiate 已存在，跳 contract-copy-initiate.html?from=detail）；② 两接收方页补 `.contract-ops-bar` 工具栏 + `#opsPanel` 下拉菜单（3 项）+ `#opsInfoModal`（版本/变更记录弹窗）+ `#opsExportModal`（导出 PDF/分享微信弹窗），复用本页 `.task-detail-modal` 体系与 `.ops-export-row` 样式；JS 新增 toggleOpsPanel/closeOpsPanel/openInfoModal/closeInfoModal/showExportModal/closeExportModal/exportToPDF/shareToWechat。
- 教训：本轮 Edit 再次出现静默未落盘——两接收方页的弹窗 DOM（opsInfoModal/opsExportModal）首轮编辑未写入，grep 复核才发现并补建；务必对每段 Edit 后 grep 关键 id 复核。
- 校验：三页 _validate_html.py 均 BALANCED；node --check 全部 JS 块 OK；grep 确认 复制发起/contract-ops-bar/opsPanel/opsInfoModal/opsExportModal 全部就位。
