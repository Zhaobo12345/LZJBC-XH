# 2026-09-24 工作日志

## 工人合同详情（新流程）· 取消「已签约（已履约完成）」的「合同正文 查看全文」区域
- 需求：工人合同详情合并页（worker-contract-detail.html + .js）的「已签约（已履约完成）」演示态，应取消「合同正文 查看全文」区域，与工人合同详情内其他状态页面布局保持一致；不改其他页面/功能/交互。
- 定位：`renderContentSection()` 的**标准分支**（line ~1105，非草稿/非变更态共用：fulfilled / signed / confirming / 非变更 receiver）原本在「违约责任/固定详细条款/补充条款」前渲染一块「合同正文」截断预览框（`.contract-text-preview`）+ 「查看全文 >」入口（`WCP.showFullText()` 弹 modal）。变更态分支与 7 个独立 `-new` 参考页（已签约发起方-新 等）本就没有该预览块。
- 改动（仅 `service-miniapp/worker-contract-detail.js`，CRLF 保留）：
  1. 删除标准分支的「合同正文」预览 form-group（含 `viewFullTextLabel`/`preview` 变量）。
  2. 同步删除变为死代码的 `receiverKeyClausesHTML()` 函数（仅被该预览块使用）。
  3. 注释更新说明全文化解至「固定详细条款 → 查看条款」独立全文页（对齐已签约发起方-新）。
- 一致性结果：fulfilled / signed / confirming（发起方+受邀方）现在一律只展示「违约责任 / 固定详细条款 / 补充条款」，不再有合同正文截断预览与「查看全文」入口；全文仍由「固定详细条款 → 查看条款」跳转独立全文页（发起方→worker-contract-draft-fulltext-new.html，受邀方→worker-contract-receive-clauses-new.html）获取——与 `-new` 参考页口径完全一致。
- **未动 CSS**：`.contract-text-preview` 基础规则与 `::after` 仍被变更态只读区 `#changeReadOnlyWrap .contract-text-preview`（840-843 行）使用，故保留，避免影响变更态布局。仅预览内容的 `.contract-text-preview .clause-*` 子规则成为死规则，留待不影响渲染。
- 校验：`node --check` OK；CRLF 3163/0；真实 Chrome headless（探针轮询 #contentSection）6 场景：fulfilled(sender)/signed/confirming(收发) 均 `hasContractTextPreview=false / hasContractTextLabel=false / hasViewFullText=false`，三项字段 true，fixedClause href 正确分流，**已履约完成徽标 `inline-flex`（保留）**，JS 错误 0；变更态分支未触碰。
- 备份：`.workbuddy/_tmp/fulfilcancel/{worker-contract-detail.js.bak, verify.js}`。

## 工人合同详情（新流程）· 发起方各状态「违约责任」文本框高度统一 + 去 PC 拖拽
- 需求：发起方各状态页内「违约责任」文本框高低不一致（请统一），且存在 PC 端文本域鼠标拖拽拉伸（resize），不符合手机端小程序规范，需去除。
- 定位：`worker-contract-detail.html` 内联 CSS。
  - **标准分支**（已签约/已履约完成/邀请中/确认中 + 非变更 receiver）：`违约责任` 文本域 `edit-textarea ro` 渲染进 `#contentSection`，无容器专属规则 → 回退到基础 `.edit-textarea`（`resize: vertical` 即 PC 拖拽 + 无 `min-height` 自动高度），与其他状态高度不一致。
  - **变更态**（change_confirming_sender / change_rejected）：同 class 但位于 `#changeReadOnlyWrap` → `min-height:70px; resize:none; line-height:1.7`（已合规、固定高度）。
  - **拟定中（可编辑）** 与 邀请编辑面板：`#draftContentWrap / #inviteEditPanel .edit-textarea` → `resize: vertical; min-height:96px`。
- 改动（仅 `worker-contract-detail.html`，纯 LF 保留）：
  1. 新增 `#contentSection .edit-textarea { min-height:72px; resize:none; line-height:1.6; }`（对齐变更态、去除 PC 拖拽）。
  2. `#changeReadOnlyWrap .edit-textarea` 由 `min-height:70px; line-height:1.7` 改为 `min-height:72px; line-height:1.6`（与标准分支完全一致）。
  3. `#inviteEditPanel / #draftContentWrap .edit-textarea` 的 `resize: vertical` → `resize: none`（可编辑域同样去除 PC 拖拽，符合手机端规范；min-height 96px 保留供编辑）。
- 校验：Chrome headless 探针测 5 个发起方状态——只读「违约责任」文本域一律 `min-height:72px / resize:none / border-box / readOnly:true`；可编辑 `breachInput` 为 `min-height:96px / resize:none / readOnly:false`；**JS 错误 0**。拟定中匹配到的只读态为 contentSection 冗余渲染（既有），非本次引入。
- 范围：仅改该文本框样式；未动结构、其他页面、功能规则与交互；7 个独立 `-new` 状态页与 `contract-copy-initiate` 系列未触碰。
- 备份：`.workbuddy/_tmp/breachh/{worker-contract-detail.html.bak, verify.js}`。

## 合同详情（非工人）· 只读「任务详情」弹窗三个标准文本框按内容撑开 + 去滚动条
- 需求：非工人合同详情（contract-detail.html）各状态页内，「任务详情（只读）」弹窗中「执行标准/确认标准/担责标准」对应内容的文本框，应按文字量自动撑开、取消滚动条（原滚动条样式且隐藏内容，不符手机端规范）；统一修改，其他页面/功能交互不改动。
- 定位（Chrome headless 实测确认）：
  - 三个标准字段本身是 `.detail-value` div（`viewTaskDetail()` 仅 `textContent` 注入，无 textarea、无 max-height），各自本就随文字撑开、不裁剪。
  - 真正产生滚动条/隐藏内容的是**共用规则** `css/contract-detail.css:3141` `.task-detail-modal .modal-content, .edit-task-modal .modal-content { max-height:80vh; overflow-y:auto }`——弹窗整体被限高 + 内部滚动，长内容被裁切（实测 scrollHeight 888 > clientHeight 387）。
- 改动：**不碰共享外部 css**（它同时被 worker 等多个页面引用，避免波及其他页面）。改为在 `contract-detail.html` 内联 `<style>` 注入**仅作用于本页、更高优先级**的覆盖规则：
  - `.task-detail-modal .modal-content { max-height:none !important; overflow:visible !important; }`（按内容撑开，无内部滚动条）
  - `.task-detail-modal { align-items:flex-start !important; overflow-y:auto !important; padding:8vh 0 !important; }`（内容超高时由遮罩层原生滚动兜底，移动端无可见滚动条）
  - 选择器仅命中 `.task-detail-modal`，**编辑任务弹窗（.edit-task-modal）保持 80vh/auto 原交互不变**；worker 等引用同 css 的页面不加载本页内联样式，故不受影响。
- 校验：复测（注入长文本 + show 弹窗）— modalContent `max-height:none / overflow:visible / scrollHeight==clientHeight（不再裁剪）`；三个标准字段均 `overflow:visible / 不裁剪 / 自动撑开`；遮罩 `overflow-y:auto / align-items:flex-start`；HTML BALANCED；`git status` 仅 `contract-detail.html` 改动，`css/contract-detail.css` 未动。
- 备份：`.workbuddy/_tmp/taskdetail/contract-detail.css.bak`（仅预防性备份，实际未改 css）。

## 工人合同详情 · 阶段任务「✅」完成记号清理 + 任务项独立圆角背景
- 需求：工人合同详情（合并页）「非已签约且非变更对应状态」的页面内，阶段任务项不应出现「✅」完成对号（签约前/待开始任务不支持执行，不应标已完成）；同时在各任务间增加明显分隔样式（对齐非工人合同详情的任务卡独立圆角背景）。
- 根因：截图的「✅」不是 UI 元素，而是**演示任务名 DATA 里硬编码的 `✅` 前缀**（如 `name:'✅放线'`）；且合并页 `renderStagesSection()` 行内曾用 `t.name` 直接渲染可见文本，导致即便清洗了属性，可见文字仍带 ✅。
- 改动：
  1. **合并页 `worker-contract-detail.js`（CRLF）**：
     - `cleanTaskName()` 收紧为：仅当「已签约且已履约完成」(`worker_signed && fulfilled`) 或「变更对应状态」(`status` 以 `change` 开头) 时保留 ✅；其余（拟定中/确认中/已签约未履约·待开始）一律去除。用户明确「非变更对应状态」保留变更态。
     - `renderStagesSection()` 可见文本与 `data-task-name` 均改用清洗后的 `taskName`（修复原 `t.name` 直渲 BUG）。
     - `receiverStageHTML()`（受邀方「承接任务」）属性与文本均改用 `cleanTaskName()`。
     - 草稿态输入框（`renderDraftStageTask`）沿用 `cleanTaskName()`。
     - 合同正文（`buildFullContractHTML`）一律去除 ✅（正式文本不含完成记号）。
  2. **合并页 `worker-contract-detail.html`（LF）CSS**：`#stagesSection / #changeStagesBox / #rcStageWrap` 下 `.stage-tasks .task-item` 加 `background:var(--bg-secondary,#F5F5F5); border-radius:8px; margin-bottom:8px; padding:10px 12px; border-bottom:none`（任务间明显分隔，对齐非工人详情任务卡）；`:last-child` 去 margin。
  3. **独立签约态页数据清洗**（同前 p1/p2/p3 口径，复用 `pm_done_mark_clean.py`）：`worker-contract-signed-initiator-new.html`（去 13 处 `name:'✅'`）、`worker-contract-receive-confirmed-new.html`（去 13 处 `name:'✅'` + 10 处对象键 `'✅':`）——二者均显示「待开始」与 ✅ 自相矛盾，与合并页口径对齐。
  4. **保留 ✅**：`worker-contract-fulfilled-receiver.html`（已完成态）、`worker-contract-change.html`（变更态，用户明确排除）维持原 ✅ 口径。
  5. `worker-contract-receive-inviting.html` 仍余 2 处 ✅ 为合法成功徽标（「已确认接单」「已成为本合同乙方」），非任务完成记号，保留。
- 校验：`node --check` OK（JS）；3 个 HTML BALANCED；CRLF/LF 行尾保留；`cleanTaskName` 单测 6 场景（draft/confirming/signed-not-fulfilled 去✅，signed-fulfilled/change 保留✅）全 PASS。
- 已清理注入到源码目录的临时探针副本 `_t_p1~p4.html`、`_t_ps.html`。
- **待确认（用户）**：合并页变更视图现已去除 ✅（与「仅已完成保留」原则一致），但独立 `worker-contract-change.html` 仍保留 ✅（按用户「非变更对应状态」字面排除）。若希望二者一致，可再将独立变更页也清洗。

## 合同详情（非工人）· 只读任务弹窗滚动条修复补全（用户复测仍见滚动条/文字被遮盖）
- 复测发现首版修复不完整：`.modal-content` 已去限高（`max-height:none; overflow:visible`，短文本 597px 全展开、footer 684<822 不裁切），但**弹窗遮罩层 `overflow-y:auto` 在内容超高时仍显示可见滚动条**——根因是项目「无滚动条」约定只作用于 `.page-content`（`common.css:230`），**未覆盖弹窗内部滚动容器**。
- 补全（仍仅 `contract-detail.html` 内联 `<style>`，不动共享 css/其他页面）：
  - `.task-detail-modal { scrollbar-width:none !important; -ms-overflow-style:none !important; }`
  - `.task-detail-modal::-webkit-scrollbar { display:none !important; }`
  - 与项目「无滚动条」约定一致：长内容时遮罩层仍可滑动查看、但**无可见滚动条**、绝不裁切。
- 实测结论（Chrome headless 375×812）：
  - 短文本 → 不滚动、无滚动条、不裁切（`visible_in_modal:true`）。
  - 长文本（标准×3 各约 200 字）→ `content` 1694px 全展开（无内层滚动条），遮罩 `scrollH 1809 > clientH 792` 可滑动、滚动条隐藏、footer 1782 可滑达（不被裁切）。
  - 说明：375×812 固定手机框内超长文本无法一屏全显，只能滑动查看（已去滚动条样式）；若用户看到旧缓存/或误开 worker 页仍带滚动条，需硬刷新或单独处理 worker 页（不在本需求范围）。
- 清理：`_t_taskprobe.html` 临时探针已删；备份 `.workbuddy/_tmp/taskdetail/`。

## 合同详情（非工人）· 拟定中/拟定中可提交/平台审核驳回 阶段任务数据"错误"根因与修复
- 用户反馈：非工人合同详情页上述三状态阶段任务数据错误，应为「待平台审核」内数据（进场前准备阶段、组织全屋放线等）；要求仅改数据、功能交互（编辑等）不变。
- 排查（Chrome headless 真渲染 + 逐状态切换 dump）：静态 HTML 里 `editStageList`（可编辑区，三状态用）与 `stagesSection`（只读区，待平台审核用）**基础阶段数据本就完全一致**——同 7 阶段 + 28 任务。代码层面无错。
- 真因：`loadDraftSnapshot()`（`js/contract-detail.js` "仅保存"功能）进入可编辑状态时从 localStorage 读取旧草稿并 `renderStagesFromSnapshot` 整体覆盖 `editStageList`；用户浏览器残留旧草稿 → 刷新仍显示旧数据（"刷新后不是最新"）。无痕渲染无 localStorage 故显示规范数据，正好解释"代码对、用户看到错"。
- 修复（仅 `js/contract-detail.js` 的 `loadDraftSnapshot`，不动 HTML/其他逻辑/交互）：
  - 删除 `if (snap.stages && snap.stages.length) renderStagesFromSnapshot(snap.stages);` —— 阶段任务数据始终以页面静态 HTML（规范数据，与待平台审核一致）为准，不再被旧草稿静默覆盖。
  - 保留合同名称/金额/甲乙方/正文等文本字段的草稿恢复；阶段增删改编辑交互不变。
- 验证：注入脚本把 localStorage 草稿替换成"错误阶段A/错误任务A"模拟脏数据，切换 draft/draft_submittable/platform_rejected 均 dump 出规范 7 阶段 28 任务（未被覆盖）；`node --check` OK；临时探针 `_t_stagefix.html` 已删。
- 备注：若用户浏览器文本字段（名称/金额等）也显旧值，需其手动清 localStorage 或我再加清理；本次仅按"阶段数据"诉求修复。

## 合同详情（非工人）· 拟定中(可提交) 切换项"点击无选中效果"修复
- 需求：非工人合同详情（contract-detail.html）顶部 demo 状态切换条中，点击"拟定中(可提交)"无选中高亮效果，请修复（其他状态不受影响）。
- 根因：`updateStatusSwitchHighlight()` 内的 `statusTextMap`（js/contract-detail.js ~1234）**缺 `draft_submittable` 键**（仅有 `draft`→拟定中、`draft_party_a`→拟定中（甲方））。点击该切换项 → `updateContractStatus('draft_submittable')` → 高亮函数查表得 `undefined` → `item.textContent === undefined` 永不成立 → 没有任何切换项加 `active` 类 → 无选中效果。其余切换项在映射表均有对应，仅此项漏配。
- 修复（仅 `js/contract-detail.js` 的 `statusTextMap`，一行）：补 `'draft_submittable': '拟定中(可提交)'`，文本与 HTML 切换项 `textContent` 精确一致（半角括号）。
- 验证：Chrome headless 真渲染，直接 `.click()` 真实切换项元素 → `activeItems` 仅含"拟定中(可提交)"、`targetActive:true`；`.status-switch-item.active` 样式（`css/contract-detail.css:1391` 背景色+主色字+加粗）存在，选中效果可见；`node --check` OK；临时探针 `_t_switchprobe.html` 已删。
- 范围：仅补映射键，未动 HTML 结构、CSS、其他状态高亮逻辑与任何功能交互。

## 合同详情（非工人）·「任务详情（只读）」弹窗三项标准去滚动条、按内容撑开
- 需求：非工人合同详情内「任务详情（只读）」弹窗不应有滚动条（须符合手机端小程序规范，手动上下滑），三项标准内容取消滚动条、默认撑开展示（原为可滚动文本框且内容被遮挡）。
- 关键澄清：用户截图里的弹窗**不是** `#taskDetailModal`（那是 `.detail-value` div、标签无星号），而是 **`#editTaskModal.readonly`**——由 `openTaskReadonly(taskItem)` 打开、JS 将标题改为「任务详情（只读）」，内含 `form-label + .required(红星号) + <textarea class="form-textarea">`（与截图完全吻合）。触发入口是只读阶段任务项 `<div class="task-item" onclick="openTaskReadonly(this)">`。
- 真因：`css/contract-detail.css:3246` `.edit-task-modal .form-textarea { min-height:80px; resize:vertical }` —— 文本框固定 80px，长内容溢出 → 出现原生滚动条 + 右下角 PC 拖拽把手（手机端不规范），内容被遮挡。
- 修复（两处，均只针对只读态）：
  1. `contract-detail.html` 内联 `<style>`（追加于既有 .task-detail-modal 规则后）：`.edit-task-modal.readonly .form-textarea { resize:none; overflow:hidden }`（去拖拽把手/自滚动）；`.edit-task-modal.readonly .modal-content { scrollbar-width:none; -ms-overflow-style:none }` + `::-webkit-scrollbar{display:none}`（弹窗隐藏滚动条、保留手动上下滑）。
  2. `js/contract-detail.js` `openTaskReadonly()` 在 `modal.classList.add('show')` **之后**（需 display:flex 才能量准 scrollHeight）将三项标准 `style.height = scrollHeight + 2 + 'px'` 按内容撑开。
  3. 防泄漏：`editTaskDetail()` 打开编辑态时对三项标准 `style.height = ''`（`#editTaskModal` 只读/编辑共用同一实例，内联高度会残留）。
- 验证（Chrome headless 375×812，点真实 `.task-item`）：
  - 只读：`modalTitle=任务详情（只读）`；执行标准 92 字 → `clientH=scrollH(不裁切) / resize:none / overflow:hidden`；另两标准 80px 同样无滚动条；`modal-content` `scrollbar-width:none`、`overflow-y:auto`（可滑动手动查看）。
  - 编辑态（模拟关闭后再开）：三项 `inlineH='' / resize:vertical / overflow:auto / clientH78`，`readonly=false` —— 原行为完整恢复，互不影响。
  - `node --check` OK；行尾：js=LF(4400)、html=CRLF(2979) 均未混行；临时探针 `_t_readonlyprobe.html`/`_t_editleak.html` 已删。
- 范围：仅 2 文件、仅 3 处改动；`css/contract-detail.css` 未动；`#taskDetailModal` 与编辑任务（非只读）交互不变；worker 等引用同页样式不加载本页内联样式，故不受影响。
