# 2026-09-07 工作日志

## 修复：工人合同详情页右侧原型导航「已签约」误跳到「拟定中（撤回后）」
- 问题：service-miniapp/worker-contract-detail.html 右侧原型导航「发起方视角」分组中的「已签约」项为 `<div onclick="WCP.updateStatus('worker_signed','sender')">` 的页面内重渲染，从「拟定中（撤回后）」预览态点击时停留在合并页（表现为仍显示撤回后视图），未跳转到专用最新页。
- 修复：将该项改为指向专用页的链接 `<a href="worker-contract-signed-initiator-new.html">已签约（发起方）-新</a>`，与同导航内其它「*-new」项（拟定中(发起方)-新 / 确认中(发起方) / 已确认（受邀方）-新）一致。
- 校验：div 368/368 闭合、CSS 括号平衡、signed-new 链接计数 1，均通过。
- 说明：专用页 worker-contract-signed-initiator-new.html（标题「已签约（发起方）-新」）已存在且为正确目标，无需改动。

## 修复：确认中（发起方）-新 撤回确认后误跳「拟定中(发起方)-新」
- 问题：service-miniapp/worker-contract-confirming-initiator-new.html 的 doWithdraw()（点击「撤回确认」→ 确认撤回）通过 setTimeout 跳转至 worker-contract-draft-initial-new.html（拟定中(发起方)-新），但应跳转到「拟定中（撤回后）」状态页。
- 修复：跳转目标改为 worker-contract-detail.html?status=worker_draft（合并页以 URL 参数渲染撤回后视图），该页导航项「拟定中(撤回后)」（data-status=worker_draft）会自动高亮。
- 校验：div 211/211 闭合、CSS 括号平衡、撤回目标指向 worker_draft 均通过。
- 说明：撤回后（worker_draft）状态无独立专用页，仅存在于合并页，故用 ?status=worker_draft 参数回跳。

## 业主端：删除旧版「项目详情」/「项目详情（已完成）」导航项及页面（2026-09-07）

- 需求：删除原型导航内「项目详情」「项目详情（已完成）」两项及对应页面（含代码），避免影响最新需求页面造成冗余；其他页面及功能不改动。
- 删除页面（2 个旧版页）：`owner-miniapp/project-detail.html`、`owner-miniapp/project-detail-completed.html`。保留 `-v2` 新版：`project-detail-ongoing-v2.html`（项目详情（进行中-新版））、`project-detail-completed-v2.html`（项目详情（已完成-新版））。
- 导航项清理（15 个活动页，逐页删除 `<a href="project-detail.html" class="page-nav-item[ active]">项目详情</a>` / `<a href="project-detail-completed.html" ...>项目详情（已完成）</a>` 整行，含前导换行，不残留空行）：activity-list / architecture / contract-detail / create-project / invite-join / member / project-info / statement-detail / statement-list / task-detail / task-list / todo-list（各删 1 条「项目详情」）；project-detail-ongoing-v2 / project-detail-completed-v2（各删「项目详情」+「项目详情（已完成）」2 条，保留两条 -v2 项）。
- **功能跳转重定向（用户确认选「重定向到进行中-新版」）**：旧页 `project-detail.html` 仍是 home/创建项目成功/邀请加入成功/架构返回/成员返回 的功能跳转目标，若只删页会导致死链，故统一改指 `project-detail-ongoing-v2.html`，仅改跳转目标、不动其他逻辑。共 8 处：home.html（217 查看详情 / 219 项目卡片 / 258 快捷入口 / 285 tab）、member.html:2839、create-project.html:819、architecture.html:1792、invite-join.html:494。
- **关键保留项（勿删）**：`css/project-detail.css`、`js/project-detail.js` 虽以 project-detail 命名，但被两个 `-v2` 最新版页面共同引用，属共享资源，删除会破坏最新需求页面，故保留。
- 未改动：message.html 的「项目详情」项（`href="project-detail-ongoing-v2.html"`）本身即指向新版页、非旧页，不在删除范围，保持原样；所有 `.bak` 备份文件未触碰。
- 校验：全仓（非 .bak）grep `project-detail.html` / `project-detail-completed.html` 结果为 0，无死链；-v2 页导航仅剩两条 -v2 项；task-list 等页导航无残留空行。

## 工人合同「变更阶段」导航与流程对齐最新流程（2026-09-07·用户确认后执行）

- 背景：用户要求分析工人合同详情页导航「变更阶段」7 项是否与最新流程匹配（基准＝最近确认的规则：**发起变更 → 对方确认后即已签约 V2，无需上传签约文件**）。
- 分析结论（7 项）：变更确认中(发起方)·待确认变更(受邀方)·变更已驳回(发起方/受邀方) 4 项保留；**变更签约中(发起方)、变更签约中（受邀方）、上传变更签约文件（新页面）3 项废弃**。废弃理由：最新流程已取消上传签约文件；`change_signing_wait` 无任何动作可达（孤立，仅导航预览）；受邀方版本因 `upload_change_sign` 被 receiver 过滤而**只有横幅无按钮、已失效**。
- 用户确认结果（AskUserQuestion）：Q1＝「删3项+删页面+清JS」；Q2＝「没找到变更中(changing)页面，暂时两者都保留」→ **保留 `changing` 与 `change_confirming_sender` 两者，仅修 confirm_change 落地**。
- 执行（范围严格限定 worker-*，未触碰合规版 contract-detail.html / js/contract-detail.js）：
  1. **导航**：5 个页面（worker-contract-detail、signed-initiator-new、confirming-initiator-new、draft-initial-new、receive-confirmed-new）各删 3 项，计数徽标 7→4。
  2. **删页面**：`worker-change-sign-upload.html`。
  3. **清 JS**（worker-contract-detail.js）：删 `STATUS_CONFIG.change_signing_wait`；删 `upload_change_sign` 动作（含 handler 跳转、receiver 过滤）；`confirm_change` 由落到 `change_confirming_sender` 改为 **落到 `worker_signed` + `applyProposal:true`（调用 applyChangeProposal 生成 V2）**，toast/日志同步；`ACTION_TEXT.confirm_change` 文案由「需上传变更签约文件方可生效」改为「确认后即生成新版本的已签约状态」；`onSignFilePicked` 移除 changeProposal 分支（变更不再上传文件，常规签约上传保留）；清理牵连状态集合（`isChangeView`、乙方状态徽标分支、变更内容卡 stateLabel 的 change_signing_wait 分支）与相关注释。
- 未改动/范围外：合规版 `contract-detail.html`+`js/contract-detail.js`（自带平台审核与签约中，属另一套流程）；`todo-list.html:432` 指向的是**合规版** `contract-detail.html?status=change_signing_wait`，不动；`changing` 与 `change_confirming_sender` 两者均保留。
- 校验：worker-contract-detail.js 语法 OK；`change_signing_wait`/`worker-change-sign-upload`/`upload_change_sign` 在该 js 与 5 个页面内均为 0；confirm_change→worker_signed+applyProposal 与 applyChangeProposal() 调用均已接线。
- **遗留待定（已向用户说明）**：`change_confirming_sender` 的描述是「等待对方（乙方）确认」，但乙方状态徽标仍映射为 `change_confirmed`（已确认）——语义仍不一致；因用户选择「暂时两者都保留」，本次未改该映射，待后续确认是否并入 pending。

## 清理：删除工人合同发起方视角三个旧状态（拟定中(发起方)/已确认(发起方)/上传签约文件（新页面））
- 需求（Req C）：依最新发起方流程，删除旧状态「拟定中（发起方）」(worker_draft_initial)、「已确认（发起方）」(worker_confirmed_sender)、「上传签约文件（新页面）」(worker-sign-upload.html) 的页面、导航项及代码，避免冗余影响最新流程；受邀方及变更阶段状态页不动。
- 删除专用页：service-miniapp/worker-sign-upload.html（已 `rm -f`，确认不存在）。注：三个旧状态中仅「上传签约文件（新页面）」有独立专用页；拟定中(发起方)/已确认(发起方) 无独立页，仅为合并页 data-status 重渲染 + STATUS_CONFIG 条目，均随合并页清理移除。
- 导航项清理（含 `worker-contract-detail.html` 发起方视角分组，及 `worker-contract-confirming-initiator-new.html` / `worker-contract-draft-initial-new.html` / `worker-contract-signed-initiator-new.html` 三页）：删除「拟定中(发起方)」(worker_draft_initial)、「已确认(发起方)」(worker_confirmed_sender)、「上传签约文件（新页面）」(worker-sign-upload.html) 三项；三页 status-group-count 8→5。
- 合并页 `worker-contract-detail.html` 发起方视角分组 count 原显示 7（残留），实际剩余 5 项（拟定中(发起方)-新 / 确认中(发起方) / 拟定中(撤回后) / 已签约（发起方）-新 / 已签约（已履约完成）），已修正 7→5。
- JS 清理 `worker-contract-detail.js`：移除 STATUS_CONFIG 的 worker_draft_initial / worker_confirmed_sender 条目；computeStatus 删除对应早退分支（含一处 dead `if (c.status==='worker_confirmed') return 'worker_confirmed_sender'`）；previewConfirmedId / inviteDisplayStatus / updateStatus / 各 isDraft 判断 / isRecord / renderSignArea / renderActions / cancelReselect / getVersionData / handleAction 同步移除旧态引用；删除孤立处理器 `upload_sign`（跳 worker-sign-upload.html）、`worker_reselect`（开重选确认）——保留 `worker_reselect_cancel` / `cancelReselect`（撤回后重选流程）与 `upload_change_sign`（指向保留的 worker-change-sign-upload.html）。
- 保留项：worker_draft（撤回后）、worker_signed（已签约）、受邀方各态、变更阶段各态；worker-contract-draft-initial-new.html 为「拟定中(发起方)-新」保留页（被 5 页链接），其内部 `var currentStatus='worker_draft_initial'` 仅为本地变量命名重叠，页面自包含、不读共享 STATUS_CONFIG，不影响功能（如需改名可另做，风险：可能破坏页内比较逻辑）。
- 校验：全仓 grep `worker_draft_initial|worker_confirmed_sender|worker-sign-upload|upload_sign|worker_reselect[^_]` 仅剩 js/contract-detail.js（合规版，独立文件，不在范围）、todo-list.html:465（待办模块上下文，保留）；worker-contract*.html 内旧态导航标签 0 命中。div/CSS 括号平衡、node --check 均通过。
- 范围外提示（未改）：todo-list.html:465 及 PRD-合同模块详细规格.html / PRD-任务模块详细规格.html 多处仍引用 worker-sign-upload.html，属待办模块与 PRD 文档范畴，需另行同步。

## 修复：工人合同详情页「发起方视角」原型导航项跨页不一致
- 问题：四页（合并页 worker-contract-detail.html + 三专用页 confirming / draft-initial / signed）的「发起方视角」导航组彼此不一致——signed 页缺「确认中(发起方)」且顺序错乱、标签误写为「已签约(发起方)-新」；confirming / draft-initial 页把「已签约」误标且「拟定中(撤回后)」「已签约（已履约完成）」均指向裸 worker-contract-detail.html（无 ?status / 无 demo 触发），点击后落点错位、对应页导航与来源页不一致。
- 方案：统一为同一套「5 项·同序·同标签·同目标」规范（合并页作为参照，其第 3/5 项保持页内 data-status / demoFulfilled 重渲染不变，因合并页是枢纽页、原地切换为正确行为）：
  1. 拟定中(发起方)-新 → worker-contract-draft-initial-new.html
  2. 确认中(发起方) → worker-contract-confirming-initiator-new.html
  3. 拟定中(撤回后) → worker-contract-detail.html?status=worker_draft
  4. 已签约（发起方）-新 → worker-contract-signed-initiator-new.html
  5. 已签约（已履约完成） → worker-contract-detail.html?demo=fulfilled
  每页仅当前页对应项加 `active`。三专用页已按此重写（标签「确认中(发起方)」「已签约（发起方）-新」修正、撤回后/已履约完成改为正确深链）。
- 合并页 worker-contract-detail.js 增加一处**纯增量**深链：`init()` 中 `updateStatus(...)` 之后 `if (getParam('demo') === 'fulfilled') demoFulfilled();`——仅当 URL 带 ?demo=fulfilled 时触发既有 demoFulfilled，既有主流程/状态页内容零改动；使专用页第 5 项与合并页「已签约（已履约完成）」演示态落点一致。
- 未改：合并页「发起方视角」静态结构（其第 3/5 项为页内 div 重渲染，本即正确参照）；受邀方视角、变更阶段导航组；主流程与各状态页内容。
- 校验：三专用页 div/CSS 括号平衡、worker-contract-detail.js node --check 均通过；四页「发起方视角」标签与顺序一致（拟定中(发起方)-新 / 确认中(发起方) / 拟定中(撤回后) / 已签约（发起方）-新 / 已签约（已履约完成））。

## 修复：工人合同详情页「变更阶段」导航组在专用页缺失（跨页不一致）
- 问题：合并页 worker-contract-detail.html 的原型导航含 4 个分组（发起方视角 5 / 受邀方视角 6 / 演示数据 2 / **变更阶段 7**）；而三专用页（confirming / draft-initial / signed）只含前 3 个分组、**缺「变更阶段」组**。故在「发起方视角」各导航项间跳转时，「变更阶段」组在合并页显示、在专用页消失——即用户所述「可能展示可能不展示」。
- 方案：将「变更阶段」组（7 项，同序同标签同目标）补齐到三个专用页，使其与合并页完全一致（均为 4 分组）。合并页的 7 项为页内 `WCP.updateStatus(...)` 重渲染，专用页无 WCP，故转写为深链到合并页：
  1. 变更确认中(发起方) → worker-contract-detail.html?status=change_confirming_sender
  2. 待确认变更(受邀方) → worker-contract-detail.html?status=change_confirming
  3. 变更已驳回(发起方) → worker-contract-detail.html?status=change_rejected&viewer=sender
  4. 变更已驳回（受邀方） → worker-contract-detail.html?status=change_rejected&viewer=receiver
  5. 变更签约中(发起方) → worker-contract-detail.html?status=change_signing_wait
  6. 变更签约中（受邀方） → worker-contract-detail.html?status=change_signing_wait&viewer=receiver
  7. 上传变更签约文件（新页面） → worker-change-sign-upload.html
- 合并页 worker-contract-detail.js 增加一处**纯增量**修正：`init()` 中 `updateStatus(...)` 末尾参数补 `getParam('viewer')`，使 `?status=change_rejected&viewer=receiver` / `?status=change_signing_wait&viewer=receiver` 等「受邀方变更态」深链能正确以受邀方视角渲染（原 updateStatus 仅按状态后缀推导、忽略 URL viewer 参数，会把受邀方变更态错误渲染为发起方）。无 viewer 参数时入参为 falsy，行为不变，主流程零改动。
- 校验：四页「变更阶段」组均 7 项、标签/顺序一致；三专用页 div/CSS 括号平衡、JS node --check 通过；worker-change-sign-upload.html 存在。未改：各状态页内容、主流程、受邀方视角、演示数据分组。

## 清理：工人合同详情页「受邀方视角」导航去重 + 遗留文件删除
- 问题：受邀方视角 6 项中存在重复入口——「已确认(受邀方)」（页内重渲染 worker_confirmed_receiver）与「已确认（受邀方）-新」（专用页 worker-contract-receive-confirmed-new.html）指向同一状态，违背「每状态仅一个规范入口」原则（对齐发起方视角已定规范）。注：worker_confirmed_receiver 是合法保留状态（仍在 STATUS_CONFIG），故只去重入口、不删状态/专用页。
- 执行（用户确认方案）：① 移除四页（合并页 + 三专用 confirming/draft-initial/signed）受邀方视角「已确认(受邀方)」重复项，保留专用页「已确认（受邀方）-新」为唯一入口，分组计数 6→5。② 删除 3 个遗留/孤立文件：worker-contract-receive-confirmed.html（旧·E方案已确认，全仓仅 self-ref）、worker-contract-receive-clauses.html（旧条款页，仅被 receive-inviting 链接）、worker-contract-receive.html（E方案整页接单卡片流验证页，全仓零引用）。③ 将 worker-contract-receive-inviting.html 的「查看详细条款」链接由 clauses.html 改指最新 worker-contract-receive-clauses-new.html（保障条款页可正常跳转）。
- 校验：四页受邀方视角均为 5 项且标签/顺序一致（已确认（受邀方）-新 / 抢单失败 / 已拒绝 / 确认中（接单体验 E）/ 已签约）；全仓 grep 已删三文件名零命中；5 个改动 HTML 文件 div/CSS 括号平衡通过。保留：worker-contract-receive-confirmed-new.html / receive-clauses-new.html / receive-inviting.html。未改：各状态页内容、主流程、发起方视角、变更阶段分组。

## 清理：删除「已签约（受邀方）」冗余入口 + 为「已确认（受邀方）-新」补齐一致原型导航
- 背景：用户判定「已确认（受邀方）-新」页（worker-contract-receive-confirmed-new.html）即最新流程下受邀方的「已签约（受邀方）」规范页，原「已签约(受邀方)」导航项（指向 worker-contract-detail.html?viewer=receiver&status=worker_signed 的页内重渲染）为冗余入口，应删除（含导航项；该状态无独立专用页，worker_signed 状态逻辑与发起方「已签约（发起方）-新」共用、不可删，故仅删导航入口，不破坏主流程）。
- 执行：① 四页（合并页 + 三专用 confirming/draft-initial/signed）+「已确认（受邀方）-新」页，受邀方视角统一移除「已签约(受邀方)」项，分组计数 5→4，现五页受邀方视角均为一致 4 项（已确认（受邀方）-新 / 抢单失败 / 已拒绝 / 确认中（接单体验 E））。② 同步把三专用页「抢单失败(受邀方)」「已拒绝(受邀方)」从裸链接 worker-contract-detail.html 改为深链 `?status=worker_lost_receiver` / `?status=worker_rejected_receiver`（合并页保留页内 WCP.updateStatus 重渲染，符合枢纽页行为）。③ 为原本**无任何右侧原型导航**的 worker-contract-receive-confirmed-new.html 补齐与整体一致的 4 分组导航：发起方视角(5) / 受邀方视角(4,「已确认（受邀方）-新」置 active) / 演示数据(2) / 变更阶段(7)，全部深链；新增内联 WCP.togglePageNav / WCP.toggleStatusGroup 轻量实现（CSS 复用 common.css + css/contract-detail.css，已链接），不引入 worker-contract-detail.js 重依赖。
- 关于「抢单失败(受邀方)」「已拒绝(受邀方)」：二者均为受邀方邀请流程的合法终态——抢单失败(worker_lost_receiver) 在名额/他人先抢导致未获邀约时呈现、已拒绝(worker_rejected_receiver) 在受邀方显式拒接时呈现，两者共用轻量「邀约已结束」视图 #receiverEndedView。属真实必要状态，**建议保留**（用户问询未要求删）。
- 校验：五页受邀方视角均 4 项且标签/顺序完全一致；已签约(受邀方) 全仓零命中；5 个改动 HTML div/CSS 括号平衡通过。未改：各状态页内容、主流程、发起方视角、变更阶段分组。

## 调整：受邀方视角导航项展示顺序
- 用户要求将「受邀方视角」4 项重排为：确认中（接单体验 E）→ 已确认（受邀方）-新 → 已拒绝（受邀方）→ 抢单失败(受邀方)。
- 执行：五页（合并页 + 三专用 confirming/draft-initial/signed + 已确认（受邀方）-新 页）的受邀方视角内容块整体重排；并移除原 `status-switch-divider` 分隔线（用户给的是平铺 4 项有序列表，分隔线不在其中）。
- 标签说明：用户原写第 4 项为「接单失败（受邀方）」，但代码与状态页既有标签为「抢单失败(受邀方)」（对应状态 worker_lost_receiver = 名额被他人先抢），为遵守「不改变状态对应页面内容」约束，沿用「抢单失败(受邀方)」；如需统一改为「接单失败」请另告知。
- 校验：`_validate6.js` 显示五页 div 闭合平衡（362/362、213/213、254/254、226/226、102/102），各 4 项且顺序完全一致（确认中（接单体验 E）> 已确认（受邀方）-新 > 已拒绝(受邀方) > 抢单失败(受邀方)）。

## 调整：受邀方「确认中（接单体验 E）」导航项改名 + 为 receive-inviting 页补齐原型导航
- 改名：用户要求将受邀方视角内「确认中（接单体验 E）」导航项名称改为「确认中（受邀方）」。在 5 个页面（合并页 + 三专用 confirming/draft-initial/signed + 已确认（受邀方）-新 页）的受邀方视角分组中将标签统一改为「确认中（受邀方）」；grep 确认旧标签全仓零命中、新标签 6 处一致（5 入口 + receive-inviting 自页 nav active 项）。
- 补齐导航：worker-contract-receive-inviting.html 原本**无任何右侧原型导航**，已按同类型已补导航的 worker-contract-receive-confirmed-new.html 的 4 分组结构补齐（发起方视角(5) / 受邀方视角(4,「确认中（受邀方）」置 active) / 演示数据(2) / 变更阶段(4)），全部深链；新增内联 WCP.togglePageNav / WCP.toggleStatusGroup 轻量实现（CSS 复用 common.css + css/contract-detail.css，已链接），与 confirmed-new 页导航完全镜像。
- 已知不一致（已更正，见下方「字段级对齐」节）：前注「主流程 4 页变更阶段为 7 项、含上传变更签约文件死链」系误判。实际四个主页面（合并页+三专用 confirming/draft-initial/signed）与 confirmed-new、receive-inviting 的「变更阶段」组**均为 4 项且为同一 4 态**（变更确认中(发起方)/待确认变更(受邀方)/变更已驳回(发起方)/变更已驳回（受邀方）），映射正确、已对齐一致；worker-change-sign-upload.html 与 worker-sign-upload.html 均**已删除且无任何残留引用**，无死链、无未清理代码。
- 校验：`_validate7.js` 显示六页 div 闭合平衡（362/362、213/213、254/254、226/226、102/102、115/115）、<script> 配对正常、page-nav 均存在、各页 nav 的 switch-item 均为 15 个，结构一致。

## 字段级对齐：变更阶段 4 指定态补充「工期」字段
- 需求：将变更阶段 4 个指定状态（变更确认中(发起方)=change_confirming_sender、待确认变更(受邀方)=change_confirming、变更已驳回=change_rejected 含 sender/receiver 视角）的页面字段与最新发起方视角 / 已确认（受邀方）-新 参考页对齐；变更相关字段描述效果及操作项不变；仅覆盖这 4 项，不含 变更中(changing)/变更签约中(change_signing_wait)。
- 改动：worker-contract-detail.js 的 renderMeta() 新增 targetedChangeStates 作用域判定（change_confirming_sender/change_confirming/change_rejected），仅对这 4 态在「合同金额」后注入 metaRow('工期', c.duration+' 天')（c.duration 默认 30）。字段顺序对齐为 合同名称→合同类型→所属架构层级→合同金额→[工期]→甲方→乙方（受邀方视角以 项目地址 替 合同类型/所属架构层级，符合最新约定）。
- 校验：node --check worker-contract-detail.js 通过；grep 确认 showDuration/targetedChangeStates/工期 注入点到位。未改：变更内容高亮卡、乙方驳回原因、撤回/确认/驳回操作、各状态页正文/阶段/附件分区结构。


## 布局级对齐：变更阶段 4 态与最新发起方/受邀方参考页统一
- 需求：变更确认中（发起方）=change_confirming_sender、变更已驳回（发起方）=change_rejected+sender 参考「已签约（发起方）-新」布局；待确认变更（受邀方）=change_confirming、变更已驳回（受邀方）=change_rejected+receiver 参考「已确认（受邀方）-新」= worker-contract-receive-confirmed-new.html 布局。变更相关字段描述效果及操作项不变。
- 发起方侧实现（worker-contract-detail.html / .js）：
  * HTML 新增 #changeReadOnlyWrap（只读合同内容卡：合同正文/阶段任务/附件 三 Tab），CSS 全部以 #changeReadOnlyWrap 前缀限定；插入点在 #attachmentsSection 之后、#draftContentWrap 之前。
  * render()：新增 isSenderChange = isSenderChangeView() 分支，隐藏 readOnlySections、显示 changeReadOnlyWrap，默认切到「合同正文」Tab。
  * renderMeta()：发起方变更态改走独立分支，输出「合同甲方 chip / 乙方（承包方）chip / 其他邀约人 / 合同信息（合同名称·合同金额·工期，均只读 ro 输入）」，两张卡合并为一张「🤝 合同方」卡。
  * renderContentSection()：发起方变更态输出 违约责任(readonly textarea) + 固定详细条款(查看条款链接) + 补充条款(变更后则带 .text-content-change-tag 高亮)，写入 #changePaneContractText，并清空 contentSection 防止重复渲染。
  * renderStagesSection()/renderAttachmentsSection()：发起方变更态分别写入 #changeStagesBox / #changeAttachmentsBox（无外层 card-title，改 .stage-section-title）。
  * 新增 switchChangeContentTab() + 暴露 WCP.switchChangeContentTab。
- 受邀方侧实现（同上两文件）：
  * HTML 新增 #receiverChangeWrap（6 个 rc-card：#rcCoop/#rcDuration/#rcBreach/#rcFullText/#rcStages/#rcAttachments），CSS 以 #receiverChangeWrap 前缀限定，仿 receive-confirmed-new 的 flow-card 视觉。
  * 新增 receiverChangeMode()（viewer=receiver && 状态为 change_confirming/change_rejected）、renderReceiverChangeSections()、toggleReceiverStageWrap()、receiverStageHTML()；WCP 暴露 toggleReceiverStageWrap。
  * render()：isRCChg 时隐藏 readOnlySections、隐藏 #workerContractMeta 与 #inviteListBox，卡片标题改「🤝 合作方」；渲染走 renderReceiverChangeSections()，否则仍走原三分部。
  * 卡片内容顺序严格对齐参考页：合作方（甲方（工长）· 乙方（您）徽标 + 甲方电话链接 + 乙方（您））→ 工期（只读）→ 违约责任（两条）→ 合同全部正文（查看《合同全部正文》› + ✓ 已阅读并同意徽标）→ 我要干的活（N 个阶段，整体折叠，箭头 ▶/展开）→ 合同附件（N 项 + 文件列表/空态）。
  * Bug 修复：原代码只在发起方变更态把卡片标题改「合同方」且不还原，切回其他状态标题错乱；现改为三分支（变更发起方=合同方 / 变更受邀方=合作方 / 其余=原标题）。
- 校验：node --check worker-contract-detail.js 通过；HTML BALANCED；style 块大括号 450/450 平衡；JS 引用的 DOM id 除动态生成（rcStageWrap/rcStageArrow）外均存在。
- 未改：变更高亮卡 #changeHighlightCard、乙方驳回原因 #changeRejectReason、底部操作（撤回/同意变更/驳回变更/返回已签约）、时长/金额/补充条款的变更比对逻辑。


## 修复：变更阶段布局对齐后的三处问题（乙方「?」/其他邀约人空标题/工期变更未体现）
- 乙方「?」根因：变更预览态 previewConfirmedId() 返回空 + 旧本地演示数据缺 partyBName/invitations 时兜底链断裂。新增 resolvePartyB() 统一解析（预览已确认 → status=confirmed 邀约 → c.partyBName → 首位邀约 → getContractTypeCandidates 工种默认候选人），发起方变更态 meta 与受邀方变更态「合作方」卡共用。
- 其他邀约人：原 meta 只输出空标题（真实名单渲染在下方 inviteListBox，位置错开）。改为在 meta「其他邀约人」标题下直接渲染除乙方外的邀约记录行（.invite-row.is-record，样式对齐 renderLists，含拒绝原因），无则整组不渲染；render() 的 showInviteList 增加 !isSenderChangeView() 条件隐藏下方重复名单。
- 工期变更：新增 activeDuration()（变更阶段优先取提案 durationNew，否则取 c.duration）。① 发起方 meta 工期字段、② 受邀方工期卡（变更时附「原 X 天 · 变更后 Y 天」说明）、③ 变更高亮卡新增「工期」行（原 X 天 → 变更后 Y 天）、④ applyChangeProposal() 补落库 durationNew + versionLog 追加工期变化描述（注意 vDesc 判断须用 cp.durationOld，不能读已覆盖的 state.contract.duration）、⑤ getActiveChangeProposal 演示提案补 durationOld/durationNew（+15 天）。
- 校验：node --check 通过、HTML BALANCED、残留变量 grep 无异常引用（meInv 剩余两处属终态视图，无关）。


## 受邀方变更态对齐「已确认（受邀方）-新」：去除状态步骤图 + 补 hero 主卡
- 需求：待确认变更（受邀方）change_confirming、变更已驳回（受邀方）change_rejected+receiver 对齐参考页 worker-contract-receive-confirmed-new.html，明确「不展示状态步骤图」。
- 改动：render() 中 receiverChangeMode() 为真时整张 #invitationCard 隐藏（步骤图 workerFlowBox、卡片标题、workerContractMeta、inviteListBox 均在其中），非受邀方变更态显式恢复 display:block；卡片标题三分支简化为两分支（仅发起方变更态改「合同方」，受邀方变更态因整卡隐藏不再改标题）。
- 补信息卡：新增 #rcHero（HTML + CSS .rc-hero-name/.rc-hero-amount/.rc-hero-old/.rc-hero-label/.rc-hero-meta），在 renderReceiverChangeSections() 中渲染合同名称 + 合同报酬（变更阶段取提案 amountNew，金额变化时附删除线「原 ¥X」）+ 付款说明 + 项目地址，对齐参考页顶部 hero 主接单卡，避免隐藏邀约卡后合同名称/金额缺失。
- 卡片流顺序（最终）：hero 主卡 → 合作方（甲方电话 + 乙方（您））→ 工期（变更后值 + 原值说明）→ 违约责任 → 合同全部正文（链接 + ✓ 已阅读并同意）→ 我要干的活（折叠）→ 合同附件。
- 保留未动：状态横幅、变更高亮卡 #changeHighlightCard、乙方驳回原因、底部操作（同意/驳回变更）。
- 差异待用户确认：合并页顶部「合同操作 › 更多」工具条（版本记录/变更记录）参考页无；底部操作条也是合并页特有（受邀方需演示同意/驳回）。
- 校验：node --check 通过、HTML BALANCED、CSS 457/457 平衡。
