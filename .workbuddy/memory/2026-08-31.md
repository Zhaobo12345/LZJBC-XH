# 2026-08-31 工作日志

## 项目详情页原型导航新增「项目列表及详情主页-新」入口
- 需求：将 `service-miniapp/project-list-detail-new.html` 的导航项（文本「项目列表及详情主页-新」）加入各「项目详情」页面右侧原型导航内，不影响其他页面与功能。
- 范围判定：`project-list-detail-new.html` 仅存在于 service-miniapp（owner-miniapp 无此文件，跨端加链接会成死链，故排除 owner-miniapp）。service-miniapp 6 个带右侧 `.page-nav` 原型导航的项目详情页全部纳入：completed / completed-v2 / new / ongoing-v2 / initial-v2 / worker。
- 改动：在每页「项目列表」(`<a href="project-list.html" ...>`) 项之后插入 `<a href="project-list-detail-new.html" class="page-nav-item">项目列表及详情主页-新</a>`（不带 active，因其非当前页）。缩进 8 空格，与各页既有 `<a>` 一致。
- 校验：6 文件各新增该链接 1 条、无重复；源 `project-list-detail-new.html` 未改动（仅其原有 title 与自身 active 导航）。
# 2026-08-31 工作日志

## project-list-detail-new.html 快捷入口调整（工人视角）
- 需求：取消「我的合同」、取消「更多」及其展开/收起交互，剩余快捷入口一行展示；其他功能效果不变。
- 改动（仅工人视角 `quickNavWork`，非工人 `quickNavNon` 不动）：
  1. HTML：移除「我的合同」item 与「更多」item 及 `.more-panel`(架构/成员/资料)；将架构/成员/资料提升为主行入口，主行变 5 项：全部任务 / 项目动态 / 架构 / 成员 / 资料；给 `.quick-nav` 加 `work-quick-nav` 类。
  2. CSS：新增 `.quick-nav.work-quick-nav{grid-template-columns:repeat(5,1fr)}`（不影响非工人 4 列布局）。
  3. JS：删除 `toggleMore()` 与 `scrollToContract()`（已无引用），更新滚动注释（不再提「更多」面板）。
- 校验：node --check 通过；toggleMore/scrollToContract/morePanel/我的合同/「更多」label 引用均 0；HTML 标签平衡 OK。

## project-list-detail-new.html 视觉与交互修复
- 问题 1（全部任务角标换行）：5 列布局下，角标内联在 label 中导致「全部任务4」折行。
  修复：将 `<span class="nav-badge">` 移入 `.icon.tasks` 内部；`.icon` 加 `position:relative`；`.nav-badge` 改为 `position:absolute; top:-4px; right:-4px`，定位于图标右上方，不再随文字换行。非工人视角无角标，不受影响。
- 问题 2（架构层级切换无效果）：点击 `.level-option` 时会先触发 `pickLevel` 关闭下拉并更新当前层级展示，但事件冒泡到 `.level-dropdown` 的 `toggleLevelOpts` 又把下拉重新打开，导致看起来没有切换。
  修复：`pickLevel(el, e)` 增加可选事件参数并 `e.stopPropagation()`；两处 `level-option` 的 `onclick` 改为 `pickLevel(this, event)`。选择后下拉保持关闭，顶部当前层级正常刷新。
- 校验：node --check 通过；HTML 标签平衡 OK；`pickLevel(this)` 旧调用 0、`pickLevel(this, event)` 2 处；角标已离开 label。

## project-list-detail-new.html 架构层级切换修复 + 非工人任务统计「查看全部」
- 根因：`.level-option` 与 `.level-dropdown` 是 `.level-selector` 下的兄弟节点（非父子）。原 `pickLevel` 用 `el.closest('.level-dropdown')` 取上级下拉头，返回 null → `if(!dd) return` 提前退出，顶部「当前层级」永远不刷新，表现为「切换无效果」。两个视角共用同一结构，故双双失效。
- 修复：`pickLevel` 改为 `el.closest('.level-selector')` 再 `querySelector('.level-dropdown')` 定位；同时修正回写逻辑——类型徽标显示类型文案（项目部/工作组）而非层级名，激活面包屑 `.breadcrumb-item.active` 更新为所选层级（首段「项目部」保留）。保留事件 stopPropagation 作兜底。
- 非工人视角「任务统计」标题右侧新增「查看全部 >」入口（`<span class="more" onclick="go('task-list.html')">`），沿用既有 `.card-title .more`（margin-left:auto）右对齐；工人视角不动。
- 校验：node --check 通过；HTML 标签平衡 OK；不含实际 `el.closest('.level-dropdown')` 代码（仅注释提及）。

## project-list-detail-new.html 工人视角：取消「合同确认·签约」待办
- 需求：工人视角待办事项中移除「合同确认·签约」分组（工人在确认合同前未进入项目、不生成项目内待办）；其他功能/效果不变。
- 改动（仅 `renderWorker`，非工人 `renderNonWorker`、合同汇总卡 `.contract-summary`、底部快捷入口均不动）：
  1. 删除 `var contractTodos = ...`（原用 `SHARED.workerContractTodos` map 渲染合同确认/签约待办）整段变量。
  2. todo-content 渲染去掉 `'<div class="todo-group-label">合同确认 · 签约</div>' + contractTodos`，仅保留「任务执行 · 确认」+ `taskTodos`。
- 保留：`SHARED.workerContractTodos` 数据定义（现未被引用，留作最小改动，无害）；`p.todoCount` 角标与空态逻辑不变（仍按 todoCount 判定；已完工项目 todoCount=0 仍显示空态）。
- 校验：node --check OK；`contractTodos` 引用 0、组标题「合同确认 · 签约」0；非工人视图/合同卡/派发逻辑未改动。
- 注意点：工人视角角标仍按 `todoCount` 显示（如进行中项目=4，但仅展示 2 条任务待办），与「合同确认·签约」已移出显示存在数量不一致；若需角标同步，应改 todoCount 数据或工人专属计数，但会同时影响非工人视角角标，故暂保持。

## project-list-detail-new.html 工人视角：取消「任务执行·确认」分组标签行
- 需求：工人视角待办事项中移除「任务执行 · 确认」分组标签行（工人待办不限于任务执行/确认，可能含变更确认等，不应以该标题限定/归类）。其他功能/效果不变。
- 改动（仅 `renderWorker` todo-content 三元表达式）：删除 `'<div class="todo-group-label">任务执行 · 确认</div>' +` 前缀，仅保留 `taskTodos`（任务待办直接平铺，无分组标题）。
- 保留：`.todo-group-label` CSS 类定义（第176-177行，现未被引用，无害）；非工人 `renderNonWorker`、合同卡、快捷入口、架构层级切换均不动。
- 校验：node --check OK；「任务执行 · 确认」文本残留 0；非工人视图/合同卡/派发逻辑未改动。

## worker-contract-draft-initial-new.html 补充状态进度步骤条
- 需求：服务方工人合同详情「拟定中（发起方-新）」页缺少状态进度标识；要求三步「拟定中 / 确认中 / 已签约」，当前=拟定中；不改变其他内容。
- 改动（仅本页）：① `<style>` 新增自包含 `.worker-flow/.wf-step/.wf-circle/.wf-label/.wf-line` 样式（用具体色：当前 #1677FF、完成 #00B42A、默认 #86909C，不依赖外部变量，避免与 contract-detail.css 冲突）；② 在 `.more-ops-panel` 与「状态横幅」之间插入 `<div class="worker-flow">`：拟定中(current,✏️) → ⟂ → 确认中(🤝) → ⟂ → 已签约(📄)，当前仅第一步高亮，无 done 步。
- 校验：三步 label 顺序正确、拟定中 为 current、2 条分隔线、CSS 齐备；HTML 标签平衡 OK；未触碰页面其他内容/JS。

## worker-contract-draft-initial-new.html 压缩状态条与横幅高度
- 需求：状态进度条与「拟定中」横幅（标题+描述）占用空间大，需缩小以让出下方内容呈现空间；其他内容不变。
- 改动（仅本页 CSS）：① `.wc-banner` margin 6/12/12→4/12/6、padding 12/14→8/12、圆角12→10；`.wc-banner-text` 15px→14px；`.wc-banner-desc` margin-top 4→2、字号 12→11、行高 1.5→1.4。② `.worker-flow` margin 4/12/10→2/12/6；`.wf-circle` 38→34px、字号 18→16 缩高度。
- 校验：HTML 标签平衡 OK；未触碰步骤条语义与其他模块。

## project-list-detail-new.html 新增小程序底部主导航（项目选中）
- 需求：页面底部展示小程序主导航，『项目』为选中态；页面下滑时底部导航做适配，不影响其他页面/功能。
- 改动（仅本页）：① 复用 common.css 既有 `.tab-bar/.tab-item` 样式（4 项：首页🏠 / 项目📋(active) / 消息💬 / 我的👤），结构对齐 project-list.html；插入位置在 `.phone-frame` 内、`.detail-scroll` 之后、闭合 `</div>` 之前（`.phone-frame{position:relative}`，`.tab-bar{position:absolute;bottom:0}` 自然贴底）。② `.detail-scroll` 由 `padding:12px` 改为 `padding:12px 12px 56px`，为底部 50px 导航让出空间，下滑内容不被遮挡。③ 导航跳转沿用本页已有 `showToast`/`go`：`我的`→showToast('个人中心开发中')，首页/消息→location.href。
- 校验：HTML 标签平衡 OK；tab-bar 在 phone-frame 内、项目为 active；变量 --primary-color/--bg-primary/--border-color/--text-tertiary 均来自 common.css（浅色主题已定义）。

## project-list-detail-new.html 底部导航错位修复
- 问题：新增 `.tab-bar` 后，导航项在内容区垂直堆叠，未形成底部横向导航条。
- 根因：`project-list-detail-new.html` 为全自包含内联样式页，**未链接 common.css**，因此 common.css 中的 `.tab-bar/.tab-item` 样式不可用。
- 修复：在页面 `<style>` 末尾补充自包含的 `.tab-bar/.tab-item/.tab-item.active/.tab-item:not(.active)` 样式（使用本页 `:root` 变量：背景 --card、边框 --border、激活 --primary、未激活 --text-3），结构/位置/HTML 不变。
- 校验：HTML 标签平衡 OK；`.tab-bar` 已含 `display:flex` + `position:absolute;bottom:0`；激活态颜色指向本页变量。

## project-list-detail-new.html 工人视角：删合同/加任务统计/去快捷全部任务
- 需求：工人视角内①合同列表删除「水电班组服务合同」；②合同下方展示任务统计（含「全部任务」查看入口）；③快捷入口取消「全部任务」项；整体布局适配；其他功能内容不变。
- 改动（仅工人视角 renderWorker 及 work-quick-nav CSS）：
  1. 合同过滤：renderWorker 的 SHARED.contracts.map(...) 改为先 .filter(c => c.name !== '水电班组服务合同') 再 map；非工人视角 renderNonWorker 仍用未过滤的 SHARED.contracts.map，数据 SHARED.contracts 不变，「水电班组服务合同」仅在工人视图隐藏。
  2. 任务统计：工人 stats-row 在 .card.contract-half 之后新增 .card.task-half（与 renderNonWorker 同构）：标题「📊 任务统计」+「查看全部 >」(go task-list.html)，三项 全部任务(taskDone/taskTotal)/确认中任务(3)/执行中任务(2)，复用既有 .card.task-half .task-stats 样式，置于合同下方。
  3. 快捷入口：移除「全部任务」项（icon tasks + nav-badge + label），剩余 4 项：项目动态/架构/成员/资料；.quick-nav.work-quick-nav 列数 repeat(5,1fr)→repeat(4,1fr) 适配。
- 校验：node --check 通过；HTML 标签平衡 OK；quickNavWork 内无「全部任务」；非工人 SHARED.contracts.map 未过滤、4 列 quickNavNon 不变；class="card task-half" 现 2 处（非工人+工人）。

## project-list-detail-new.html 工人视角：待办+1、任务统计增高
- 需求：工人视角待办事项多展示一条待办项；任务统计高度增高（便于年长用户查看）；其他功能不变。
- 改动（仅工人视角）：
  1. 待办+1：`SHARED.workerTaskTodos` 由 2 项增至 3 项，新增 `{ title:'地砖铺贴', desc:'泥瓦工作组 | 王五待确认', status:'待确认', cls:'pending', icon:'🔵', bg:'#E6F7FF' }`；仅 renderWorker 的 taskTodos 使用该数组，非工人待办不受影响。
  2. 任务统计增高：工人 task-half 卡片加 `worker` 修饰类（`class="card task-half worker"`），新增 `.card.task-half.worker` 作用域 CSS——`.task-stats` gap 6→10；`.task-stat-item` 改为纵向(column)居中、padding 3→10px；`.icon-wrap` 22→30px / 字号 11→15px；`.value` 14→18px、`.total` 11→13px；`.label` 10→12px、`.info` 居中。非工人 task-half 仍用原小尺寸（无 `worker` 类），大小不变。
- 校验：node --check 通过；HTML 标签平衡 OK；task-half.worker=1、plain task-half=1；workerTaskTodos 现 3 项。

## project-list-detail-new.html 工人视角：合同卡 + 任务统计卡 协调、首屏免下滑（最终可实测方案）
- 需求与冲突：用户要求①合同卡与任务统计卡协调、整体适度增高（年长友好大字号），②首屏内容完整显示、无需下滑。这两个目标在 120px 卡高时冲突。
- 关键实测发现（用系统 Chrome + playwright-core 真实渲染测量）：
  - 可用高（detailScroll.clientHeight）= 666px，原内容高（120px 卡高）= 711px，**溢出 45px**。
  - 尝试把 `.min-height` 从 115px 降到 80px，两张卡高度始终 116/127px 不变——说明 `min-height` 未生效，卡片已是**内容自然高度**；要缩短卡片必须压缩内部字号/内边距，而不能靠降低 `min-height`。
- 最终方案（全部改动限定在**工人视角 `.worker` 类作用域**，全局/非工人视角 100% 还原）：
  1. JS：`renderWorker` 的「合同」卡加 `worker` 类（`class="card contract-half worker"`），与任务统计卡 `task-half worker` 配对。
  2. 合同卡（.contract-half.worker）：`.contract-item` 内边距 5px 上下；名称 14px；meta-row 12px；进度条 5px；保持左对齐。
  3. 任务统计卡（.task-half.worker）：统计项改纵向图标在上/数值+标签在下；`icon-wrap` 26px/13px；`value` 15px；`total` 12px；`label` 11px；项内边距 2px。虽比 18px 略小，仍明显大于原始紧凑版（14/10/22）。
  4. 待办卡（.todo-card.worker）：专属压缩——`margin-bottom: 4px`；`.todo-item { padding: 5px 0; }`，为两张卡让出空间。
  5. 全局样式（detail-scroll 的 padding、sticky-header 的 padding/margin、quick-nav 的 padding、level-selector 的 margin-top）全部回滚至原始值，避免影响非工人视角。
- 实测结果（headless Chrome）：
  - 工人视角：`detailScroll.clientHeight=666`，`scrollHeight=666`，**overflow=0，fits=true**；卡高 100/112px，协调且无需下滑。
  - 非工人视角：overflow=194（与原始态一致，页面内容长，设计本身可滚动，未被本次改动影响）。
- 校验：node --check 通过；HTML 标签平衡 OK；全局 CSS 已回滚（用 Grep/Read 核对）；所有改动均带 `.worker` 作用域（除 JS 给合同卡加 `worker` 类）。

## project-list-detail-new.html 两处高度/一致性调整
- 需求：①非工人视角「合同概览」卡高度稍缩小；②工人视角待办事项的布局及高度与非工人视角待办事项保持一致；其他已有功能/内容不动。
- 改动（严格作用域隔离）：
  1. 非工人合同概览缩小：新增 `.card.contract-half:not(.worker){padding:6px 12px}` 与 `.card.contract-half:not(.worker) .contract-summary{padding:1px 0}`（原 `.stats-row>.card` 为 8px 12px、`.contract-summary` 为 2px 0）；用 `:not(.worker)` 精确限定，工人合同卡（.contract-half.worker，无 .contract-summary）不受影响。实测非工人合同卡高 79px（原约 84px）。
  2. 工人待办对齐非工人：`.todo-card.worker .todo-item{padding:5px 0}`→`10px 0`；`.todo-card.worker{margin-bottom:4px}`→`12px`；对齐非工人 `.todo-item{padding:10px 0}` 与默认 `.card{margin-bottom:12px}`。保留工人卡橙色左边框/标题色等视角标识（属「样式」非「布局高度」）。实测两视角待办项高度均 56px，已一致。
- 实测（系统 Chrome + playwright-core，viewport 375×812）：非工人 overflow≈188（设计本可滚动，未受影响）；工人 `clientHeight=666`、`scrollHeight=680`、`overflow=14`（原 0）。即对齐后工人视角比首屏高 14px、需微滑——根因是更高的待办项，且工人合同/任务卡已为自然内容高、无可回收余量（measure：contract slack 1px / task 0px）。已向用户说明此副作用，并提议如需恢复首屏免下滑，可在工人作用域内轻微压缩合同/任务卡内边距（属既有「年长友好」调优区）以回收约 14px，待用户确认是否执行（本次未擅自动其他区块）。
- 校验：node --check 通过；HTML 标签平衡 OK；改动均落在 `:not(.worker)` 或 `.todo-card.worker` 作用域，非工人视角待办与布局零改动。
