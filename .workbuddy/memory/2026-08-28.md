# 2026-08-28 工作日志

## 融合页 project-list-detail-new.html 对齐源页 + 去滚动条
- 用户反馈：此前"待办事项下方"布局/内容未正确对齐源页（项目详情（工人）、项目详情（进行中-新版）），且手机框右侧滚动条不符合小程序规范。
- 修正内容：
  1. **滚动条隐藏**：全局 `::-webkit-scrollbar{display:none}` + `scrollbar-width:none` + `-ms-overflow-style:none`，覆盖手机框详情滚动、下拉面板、原型导航等（手机端小程序规范）。
  2. **非工人 stats-row**：由 `display:flex` 左右并排改为 `display:block` 上下堆叠全宽（与源 v2「首屏布局优化」一致，源明确"375px 半宽过窄故弃并排"）。
  3. **非工人 今日动态**：补回第 4 条（陈设计·设计师·设计服务合同签约完成 + 媒体图）；第 3、4 条默认 `hidden`（tertiary/quaternary）；新增"展开更多"按钮 `toggleActivityList()`（默认显 2 条，展开显 4 条），与源页一致。
  4. **架构选择器**：工人/非工人 `level-selector` 均加 `arch-half` 紧凑样式（icon 28px、text 13px 等），与源页一致。
  5. **非工人 合同概览**："查看全部 >"改为"查看合同 ▼"，`toggleContractNW()` 可折叠合同列表。
  6. **工人「我的合同」**：由 `go('worker-contract-detail.html')` 改为 `scrollToContract()` 滚动定位本页合同列表（与源 `data-scroll="#contractList"` 一致）；合同列表加 `id="contractListWorker"`。
- 不影响其他页面；`node --check` 语法通过。
- 用户截图反馈层级选择器仍错乱（"项目部 项目部 项目部" / "泥瓦工作组 工作组 项目部>泥瓦工作组"）、stats-row 未与源页对齐。二次修正：
  - 层级选择器 `.current` 内补 `.text-row` 容器（`display:flex;gap:6px`），将 `.text` 与 `.type-badge` 横向排列，下方再放 `.breadcrumb-inline`，与源页 DOM 结构完全一致。
  - `stats-row` CSS 完全替换为源 v2 规则：合同概览两统计横向居中带分隔、合同列表默认 `max-height:0` 由 `.show` 展开、任务统计三项横向居中（icon 在上）。
  - 工人/非工人合同列表默认加 `show` 类；`toggleContractNW` 改切换 `show` 类而非 `hidden`。
- 注：顶部一行（项目名+切换+角色摘要）沿用此前决策（非工人显示"已开工N天·面积"，工人显示"项目信息"），不在"待办下方"范围；项目基本信息橙色头部仍在顶部一行承载（未恢复为独立卡片，符合之前合并决策）。

## 融合页快捷入口与架构区域重构（按截图效果）
- 用户反馈：快捷入口与架构级别区域应参考截图效果（彩色圆形图标 + 横向排列 + 下方架构选择器），且收起/展开需结合页面上滑下拉。
- 修正内容：
  1. **快捷入口图标**：非工人 4 项改为彩色圆角图标背景（架构绿 / 成员橙 / 资料紫 / 对账单金）；工人 4 项同样彩色处理（全部任务蓝 / 项目动态青 / 我的合同橙 / 更多灰），与 project-detail.css 源样式一致。
  2. **快捷入口布局**：由 `display:flex` 改为 `grid-template-columns: repeat(4, 1fr)`，图标在上、文字在下，与截图一致。
  3. **架构选择器结构**：修正 `.current` 内 `.text-row`（标题 + type-badge 横向）+ `.breadcrumb-inline` 分行，消除"项目部 项目部 项目部"错乱。
  4. **sticky-header 真正吸顶**：`position: sticky; top: 0; z-index: 100; backdrop-filter blur`。
  5. **滚动联动收起/展开**：`detailArea` 监听 scroll，下滑超过 10px 自动收起快捷入口并加深阴影；上滑自动展开；回顶移除阴影。
  6. **pin 按钮**：改为悬浮右上角、默认隐藏，不占网格位。
- 不影响其他页面；`node --check` 语法通过。

## 新增页面「已确认（受邀方）-新」（worker-contract-receive-confirmed-new.html）
- 需求：工人合同详情（新流程）受邀方视角下，新增「已确认（受邀方）→ 已确认（受邀方）-新」布局优化版，参考「确认中（接单体验E）」（worker-contract-receive-inviting.html）的 hero+卡片流布局；突出主要内容，新增：工期字段（可修改）、违约责任文本域（可修改）、合同正文改「合同全部正文」链接式【勾选】点击单独页查看且提交校验必选。不影响其他已有页面。
- 新建 `service-miniapp/worker-contract-receive-confirmed-new.html`：phone-frame + status-bar + nav-bar（返回→worker-contract-detail.html，标题「合同邀约」）+ receive-scroll（hero confirmed 态含「下一步」流程：确认工期与违约责任→等待发起方上传签约文件→等待开工通知）+ 卡片流（合作方 / 工期 input#durationInput 值30日历天 / 违约责任 textarea#breachTextarea / 合同全部正文 full-text-link 跳 worker-contract-receive-clauses.html + 勾选#fullTextCheck（clause-agree.error 红框）/ 我要干的活 DEMO_STAGES / 附件）+ 吸底 CTA「提交确认」。
- 提交逻辑 doSubmit()：校验 fullTextCheck 必选，未勾选→fullTextAgree 加 error + toast「请先阅读并勾选同意《合同全部正文》」；通过→toast 后跳 `worker-contract-detail.html?viewer=receiver&status=worker_confirmed_receiver`。
- 导航入口：worker-contract-detail.html 右侧「受邀方视角」分组在「已确认(受邀方)」后加 `<a>` 入口「已确认（受邀方）-新」，count 4→6（原 5 项+新 1 项，修正原 4 的计数误差）。
- 校验：新页面 + worker-contract-detail.html HTML 均 BALANCED。
- 未同步 PRD（待用户确认新页定位：正式需求 vs 演示优化）。
- 修正（用户反馈）：① 页面标题/nav-bar 由「合同邀约」改为「工人合同详情」（title「工人合同详情（已确认（受邀方）-新）」、nav-bar「工人合同详情」），导航项名「已确认（受邀方）-新」保持；② 内容定位=工人合同详情「已确认（受邀方）」、布局参考合同邀约（非「合同邀约」页面状态）；③ 加滚动条隐藏（`::-webkit-scrollbar{display:none}` + `scrollbar-width:none` + `-ms-overflow-style:none`）。④ 全局约定写入 MEMORY.md「协作约定」：手机端小程序页面不得出现滚动条。
- 修正（用户反馈「主要内容与原合同详情一致」）：新页面误用接单体验E数据，改为原合同详情（水电 shuidian）数据——hero 合同名称「水电班组服务合同」→「全屋水电改造合同」（原 c.name）、金额 8,800→12,600（原 c.amount）、项目地址补全「XX市XX区XX路XX号 · 阳光里小区 8 栋 2 单元 1101 室」；违约责任 textarea 初始值改为原合同「第六条 违约责任」（甲方逾期付款违约金 / 乙方工期延误质量不符返工）。「合同全部正文」链接原跳接单体验E的 worker-contract-receive-clauses.html（水电班组服务合同），改为新建独立页 `worker-contract-receive-clauses-new.html`（内容=全屋水电改造合同完整正文七条，与原 worker-contract-detail.js buildReceiverContractHTML 一致），避免影响接单体验E共享的 clauses 页。校验：两页 HTML BALANCED，旧名/旧金额残留 0。
- 修正（用户反馈「已确认状态应只读」）：去掉 hero 邀请行（陈庄邀请您承接）、「下一步」区、「您已成为本合同乙方…」徽标（确认时已提示过）；工期/违约责任由 input/textarea 改为只读 flow-line 展示；「合同全部正文」去掉勾选框（确认时已勾选、不可取消），改为链接 + 只读「✓ 已阅读并同意」agreed-badge；吸底 CTA「提交确认」按钮改为只读提示「⏳ 等待发起方上传签约文件，合同将正式生效」；删除 doSubmit 校验逻辑。新增 .cta-status-hint / .agreed-badge CSS。校验：HTML BALANCED，input/textarea/fullTextCheck/doSubmit 残留 0。
- 修正（用户反馈，实为改主页面 confirmed-new.html 而非 clauses-new.html）：① hero 报酬说明「含材料与人工」→「施工完成且验收通过后由线下进行支付」；② 工期「30 日历天」→「30 天」；③ 「合同全部正文」卡去掉重复的 flow-title（标题与链接文字重复），仅保留「查看《合同全部正文》›」链接 + 已阅读标记；④ 「我要干的活」加整体收纳展开：flow-title 加 onclick=toggleStageWrap + 右侧 ▶/▼ 箭头（.fold-arrow，margin-left:auto），stageWrap 默认 display:none，toggleStageWrap() 切换；⑤ 底部吸底 CTA「等待发起方上传签约文件」区域取消：删 sticky-cta div + renderCta 函数及调用 + 死 CSS（.sticky-cta/.cta-btn/.cta-primary/.cta-status-hint）。校验：HTML BALANCED，sticky-cta/cta-status-hint/renderCta 残留 0。
- 修正（用户反馈，仍为主页面 confirmed-new.html）：① 工期卡去掉冗余 field-hint「工期自开工通知之日起计算」，仅留「📅 工期 + 约定工期：30 天」；② 「查看《合同全部正文》」链接与「已阅读并同意」标记缩小以减小高度：full-text-link font 14→12px、padding 8/14→5/10、radius 10→8、去掉 margin-bottom；agreed-badge font 12→11px、padding 6/10→4/8。校验：HTML BALANCED。

## 融合页快捷入口交互优化 + 修复「更多」闪烁
- 优化（针对上划抖动根因）：移除「收起快捷入口」整条开关（连同 applyQuickNavCollapsed/toggleQuickNav 及上一轮遗留的 initScrollCollapse 死代码），4 主入口常驻吸顶，「更多」改为平滑内联展开（max-height/opacity 过渡 + .more-open 图标旋转），hover 专属的「📌 固定」按钮一并删除。
- 修复（用户反馈：展开更多→收起→上划→再点更多仅闪动不展示）：原滚动联动里「scrollTop>40 时自动收起 morePanel」会与手动展开冲突——重新打开后一旦再触发任何 scroll 事件（布局回流/手势惯性）就被立即关掉，表现为闪一下。改为滚动联动只维护吸顶阴影，「更多」面板完全由用户手动展开/收起，不再滚动自动收起。兼顾不再抖动、主入口始终可达。
- 校验：node --check 通过；无 toggleQuickNav/applyQuickNavCollapsed/qnCollapsed/pin-btn/quick-nav-toggle/initScrollCollapse 残留引用。

## 融合页非工人视角首屏压缩（让「今日动态」进入首屏）
- 需求：非工人视角「待办事项」下方各区域高度过大，首屏看不到「今日动态」，要求压缩各区域高度使「今日动态」进入首屏。
- 改动（仅压缩样式 + 收起合同列表默认态，不改布局结构与内容）：
  1. 快捷入口 `.quick-nav` padding 8/8/4→4/8/2；`.icon` 36→32px、font 16→15、mb4→2。
  2. `.level-selector` margin-top/padding-top 8→4；`.arch-half` padding 6→4。
  3. `.sticky-header` padding 10/12→8/12、margin-bottom 12→8。
  4. `.stats-row > .card` padding 10/12→8/12；`.contract-summary` padding 4→2；`.task-stat-item` padding 6/4→4/4；`.contract-item` padding 9→7。
  5. **合同概览列表默认收起**：`contractListNW` 去掉 `show` 类（仅保留「查看合同 ▼」可展开），最大高度由 110px→0，节省约 110px（首屏关键削减项）。
- 效果：非工人视角首屏（≈766px 高度）内「今日动态」标题及首条动态可见（累计 ≈605px 处）。点击「查看合同」仍可展开合同列表，交互不受影响。
- 校验：node --check 通过。

## 已确认（受邀方）-新：工期区域简化为一行
- 用户反馈（仍误指 clauses-new.html，实际工期在主页面 confirmed-new.html）：工期区冗余占空间，要求简化为一行。
- 修正：confirmed-new.html 工期卡由「flow-title📅工期 + flow-line约定工期：30 天」两行，合并为单行「📅 工期：30 天」（去重「工期」+「约定工期」冗余前缀），其余卡片结构不变。clauses-new.html 本身无工期区（仅七条正文）。
- 校验：HTML BALANCED。

## 拟定中(发起方)-新 补齐「意向乙方」与「更换模板」逻辑
- 需求：worker-contract-draft-initial-new.html 缺少「拟定中(发起方)」页面的两类逻辑，需补齐且不影响其他页面：① 意向乙方「可选择 / 可删除 / 最多 3 人上限」；② 合同正文、阶段任务「选择/更换模板」功能（参考原 worker-contract-detail.js 的 renderDraft* / getDraftTemplates / openTemplatePicker / applyTemplate / previewDraftTemplate 等）。
- 实现（仅改 worker-contract-draft-initial-new.html 单文件，内联数据 + 逻辑，移植自 worker-contract-detail.js）：
  - 意向乙方：内联 WORKER_CANDIDATES / TRADE_ROLES / TRADE_CANDIDATES / TRADE_ROLE_BY_TYPE / getContractTypeCandidates；合同类型按工种过滤（仅各工种 6 类）；候选人搜索面板 toggleInvitePanel/filterInvite/renderInviteList；chip 删除（含头像点击查看电子名片 openBusinessCard）；toggleInviteMember 选择+删除，达 3 人拦截 toast「最多邀请 3 名意向乙方」；renderInviteChips 显示「已选 N/3 人」「至少 1 人」约束。
  - 合同内容：合同正文/阶段任务/附件 三 Tab（switchDraftTab，附件 Tab 隐藏「更换模板」按钮）；阶段任务数据驱动渲染 renderDraftStageTask（阶段名/顺序开关/增删任务/增删阶段）；更换模板 showDraftTemplatePicker→openTemplatePicker→renderTemplatePickerList（标准/精简 两模板，按合同类型名生成）；applyTemplate 正文模板记 templateText 标识、阶段模板真正替换 C.stages；previewDraftTemplate 底部抽屉预览（正文富文本 / 阶段明细），applyTemplateFromPreview 应用。
  - 提交校验 doSubmit：合同名称非空、金额>0、违约责任必填（空则红框拦截）、意向乙方 1-3 人；doSave 仅提示。
  - 保留三处差异：工期字段、违约责任必填、合同正文默认收起（view-full-link 跳 worker-contract-draft-fulltext-new.html）。
- 校验：node --check 内联脚本通过；无 switchTab/ContractStore/WCP 旧引用残留；HTML 标签 BALANCED。

## 融合页非工人视角「任务统计」进一步压缩
- 需求：待办事项下方「任务统计」占用空间太大，要求减少高度。
- 改动：`.card.task-half .task-stat-item` 由「icon 在上、文字居中」纵向堆叠改为「icon 居左、数值+标签纵向居右」单行紧凑布局；图标 28→22px、数值 16→14px、标签 11→10px、内边距 4/4→3/2。任务统计卡高度约由 100px 降至 ~55px（约减半）。不影响合同概览卡片与交互。
- 续：用户要求「合同概览」高度与「任务统计」保持一致 → 同步将 `.card.contract-half .contract-stat` 数值 18→14px、总计 13→11px、标签 11→10px，使两张统计卡高度对齐一致。

## 融合页非工人视角：架构层级选中修复 + 今日动态缩进对齐
- 需求1：选择架构层级后下拉框未收起、顶部「当前层级」也未切换。
- 修复 `pickLevel()`：选中后①移除 `.level-options.open` 与箭头 `.open` 收起下拉；②回写 `.level-dropdown .current`（icon/文本/type-badge[project|workgroup]/breadcrumb-item）为所选层级名称。原逻辑仅改选项选中态与对勾，未收起也未回写。
- 需求2：架构下方的「合同概览/任务统计」(.stats-row 带 `margin:0 10px`) 有横向缩进，「今日动态」原满宽 `.card` 不一致。
- 改动：新增 `.card.indent-card { margin: 0 10px 8px; }`，非工人「今日动态」卡片加 `indent-card` 类，与统计区横向缩进对齐（待办事项保持满宽不变）。
- 校验：node --check 通过。

## 间距压缩 + 任务详情只读弹窗（仍误指 clauses-new.html，按实际文件处理）
- 用户需求（指 clauses-new.html）：① 各区域间距减少；② 点击任务项弹窗展示任务详情（只读）。
- 实际落点：① 间距压缩在 clauses-new.html（doc-sec margin 14→10、doc-p margin 4→2/line-height 1.85→1.7、flow-card padding 16→12/margin-top 12→8、doc-h-sub margin/padding 12→8）；② 任务项与只读弹窗在 confirmed-new.html（clauses 无任务项）。
- confirmed-new.html 任务详情只读弹窗：复用 contract-detail.css 既有 .task-detail-modal（position:absolute 覆盖 phone-frame，.show→flex），新增 modal HTML（标题「任务详情（只读）」+ 任务名称/执行人/确认人/执行标准/确认标准/担责标准 + 关闭按钮）+ JS（TASK_STD_MAP 七任务三标准映射、GENERIC_STD 兜底、viewTaskDetail/closeTaskDetailModal）；renderStages 任务项加 onclick=viewTaskDetail + data-* 属性（execStd/confStd/liableStd 预留，优先用 TASK_STD_MAP）。

## 新页面 worker-contract-draft-initial-new.html 与原页面布局对齐修复
- 问题：① 手机框距浏览器上方间距大、切换导航项抖动（需滚动才见全貌）；② 原型导航无滚动条、无收起图标，与 worker-contract-detail.html 不一致。
- 根因：新页面 body 被加 `display:flex; padding:24px` 覆盖 common.css 的 `.phone-frame{margin:20px auto}`；且全局 `::-webkit-scrollbar{display:none}` 把原型导航（common.css `.page-nav{overflow-y:auto}`）滚动条也隐藏；导航标题缺 `toggle-btn` 收起图标与 JS；内联 `.page-nav-title/.page-nav-item` 覆盖破坏 common.css flex 布局与 active 配色。
- 修复：① 移除 body flex/padding 覆盖，回归 `common.css` 的 `.phone-frame{margin:20px auto}`（与原页面居中一致，切换不再跳动）；② 滚动条隐藏作用域收窄为 `.phone-frame *`，原型导航恢复原生滚动条；③ 导航标题加 `onclick="togglePageNav()"`+`<span class="toggle-btn" id="pageNavToggle">▼</span>`，新增 `togglePageNav()` 切换 `.collapsed`/图标旋转（复用 common.css `.page-nav.collapsed`/`.toggle-btn.collapsed`）；④ 删除内联 `.page-nav-title/.page-nav-item` 覆盖，原型导航样式完全由 common.css 接管。
- 校验：node --check 通过；不影响其他已有页面/功能/样式。

## 工人合同详情新增「拟定中（发起方）-新」页面
- 需求：原型导航「合同状态切换（工人合同）」发起方视角，在「拟定中(发起方)」下新增「拟定中(发起方)-新」；并新建对应页面，布局/内容主体与「拟定中(发起方)」一致，区别：① 合同信息内新增「工期」字段（值 30 天）；② 新增「违约责任」文本域（必填项，提交校验）；③ 合同正文默认收起，仅展示「查看全文 >」链接跳转查看合同全文页面；④ 补充条款不受影响。不影响其他页面。
- 改动1：worker-contract-detail.html 发起方视角 status-group-content 内，`拟定中(发起方)` 之后新增 `<a class="status-switch-item" href="worker-contract-draft-initial-new.html">拟定中(发起方)-新</a>`（原 JS 状态项不动）。
- 改动2：新建 `worker-contract-draft-initial-new.html`（自包含，phone-frame + 右侧 common.css 浮动 .page-nav；复用 common.css/css/contract-detail.css 基样式 + 内联草稿区样式）。主体复制拟定中·发起方视图：状态横幅「拟定中」、合同邀约与乙方卡（合同名称/金额/意向乙方 chips）、合同内容卡（合同正文/阶段任务/附件 Tab，4 阶段 + 4 附件）。差异：合同信息增加「工期」输入(30 天)与「违约责任」文本域(必填，带 * 标与提交校验 doSubmit 拦截空值并红框)、合同正文默认收起（仅「查看全文 >」链接 + 收起提示，点击跳 worker-contract-draft-fulltext-new.html）、补充条款文本域保留。
- 改动3：新建 `worker-contract-draft-fulltext-new.html` 作为「查看合同全文」目标页（工程概况[含 1.5 工期 30 天]/价款/双方权责/质量/违约/争议 六条，发起人视角）。
- 校验：python 标签平衡、node --check 内联脚本均通过。
- 校验：两页 HTML 均 BALANCED；viewTaskDetail/taskDetailModal/TASK_STD_MAP 引用齐全，无遗留死代码。

## 拟定中(发起方)-新 导航链接修正
- 问题：新页面 `worker-contract-draft-initial-new.html` 右侧导航「拟定中(发起方)」链接为 `worker-contract-detail.html`（无参数），落到默认态即「拟定中（撤回后）」视图，与标签不符。
- 修复：链接改为 `worker-contract-detail.html?status=worker_draft_initial`（worker-contract-detail.html 经 `getParam('status')` 决定初始视图）。其余状态项同类问题如需统一可后续批量补 `?status=`。

## confirmed-new.html：区域高度（内部留白）压缩
- 背景：用户先纠正「区域间距」指 confirmed-new.html 各卡片间 margin（已 12→8px）；本轮回「减小各区间高度（内间距留白太多占用首屏高度）」→ 实为压缩卡片自身内部留白。
- 改动（仅 confirmed-new.html 内联样式，不动 margin-top:8px 区域间距）：hero-card padding 18/16/16→14/16；hero-trade margin 14/0/4→10/0/3；hero-amount margin 6/0/2→4/0/2；hero-amount-label mb 10→7；hero-meta-row margin 10→7；hero-confirmed-badge mt 12→8 & padding 8/10→6/10；next-flow mt 14→10 & pt 12→8；next-flow-title mb 8→6；next-step mb 10→7；flow-card padding 14/16→11/16；flow-title mb 10→7；flow-line line-height 1.7→1.55；full-text-link padding 5/10→4/9；agreed-badge 4/8→3/7；clause-agree 10/12→8/12；mini-att padding 9/0→7/0。
- 校验：HTML BALANCED。

## 拟定中(发起方)-新 两项体验修复
- 问题A：意向乙方下拉面板打开后无法收起（无外部关闭入口）。修复：`toggleInvitePanel()` 切换正常，新增 document 级 click 监听——点击 `.member-picker` 外部时移除面板 `.open` 收起；面板内部/控制条点击不收起。点击控制条仍可正常切换开/关。
- 问题B：合同内容 Tab 下方选中蓝色横线错位。根因：内联 `.content-tab.active::after` 用 `left:12px;right:12px` 通栏线，覆盖了 contract-detail.css 规范（居中 40px 短横线）。修复：内联 `::after` 改为 `left:50%;transform:translateX(-50%);width:40px`，并给 `.content-tab` 补 `flex:1;text-align:center`，与同级状态页一致。
- 校验：node --check 通过；无 `left: 12px; right: 12px` 残留。
