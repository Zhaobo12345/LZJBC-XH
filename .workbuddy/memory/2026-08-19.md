# 2026-08-19 工作日志

## 消息页移除「任务通知」Tab
- 需求：消息页面删除「任务」Tab（即「任务通知」Tab），不影响其他已有功能。
- 改动文件：
  - `owner-miniapp/message.html`：移除 任务通知 tab div、messages.task 数据、renderMessageCard 内 task 分支、icon 三元 task 分支、viewTask 函数。现仅剩 邀请/系统消息 两 Tab。
  - `service-miniapp/message.html`：同上，现剩 邀请/系统消息/合同邀约 三 Tab（合同邀约 Tab 保留）。
- 校验：两页 HTML 标签平衡均 BALANCED；Grep 残留 switchTab('task')/messages.task/type==='task'/viewTask/任务通知 均 0。
- PRD：PRD-完整文档.html 消息功能节仅写邀请接收规则，未枚举 Tab 结构，无需改；更新 `.workbuddy/memory/MEMORY.md` 第28行约定（owner 现两 Tab）。
- 注意：page-nav 内「任务列表」入口（task-list.html）为独立页面，与消息 Tab 无关，保留不动。

## 服务方消息页移除「系统消息」Tab
- 需求：service-miniapp 消息页删除「系统消息」Tab，不影响其他功能。
- 改动 `service-miniapp/message.html`：
  - 移除 系统消息 tab div、messages.system 数据、renderMessageCard 内 system 分支、viewDetail 函数。
  - 同时清理上一轮遗留的 messages.task 死数据（此前 tab/分支已删，仅数据残留，因当时 grep 用 `messages.task` 漏匹配 `task: [` 所致）——本轮一并删除，service 现仅 邀请/合同邀约 两 Tab。
  - owner-miniapp 不动（仍 邀请/系统消息 两 Tab）。
- 校验：两页 HTML 均 BALANCED；service 残留 scan switchTab('system'/'task')/type:'system'/'task'/task:[/system:[/viewTask/viewDetail/系统消息/任务通知 全 0。
- 更新 `.workbuddy/memory/MEMORY.md` 第28行：services 现两 Tab、owner 现两 Tab 的权威约定。

## 合同已签约/变更成功 → 给已有执行人任务生成待办规则补入 PRD
- 结论：原 PRD 缺此规则（执行人「任务执行」待办此前仅 §3.15(5) 确认人驳回触发）；用户要求补充「合同已签约态（变更成功时同样）给合同下已有执行人任务生成待办」。
- 新增权威描述：PRD-任务模块详细规格.html §3.12 待开始 末尾加「待办生成规则（合同已签约 / 变更生效 → 执行人『任务执行』待办）」——三类触发：①合同已签约(signed) ②变更已生效(change_confirmed) ③可施工态下配置执行人；均扫描该合同下「已配置执行人+待开始」任务为执行人生成 pending 待办。
- 同步：§4.8.2 生成规则行改为双来源（合同可施工触发+§3.15(5)驳回触发），页面逻辑补签约/变更生成说明。
- 交叉引用：PRD-合同模块详细规格.html §3.12.14 已签约「关键交互与逻辑」加「执行人待办生成」bullet；§5.15.8 变更已生效「关键交互与逻辑」加同义 bullet，均链 §3.12。
- 校验：任务模块 div 564/564、合同模块 440/440，均平衡；锚点 #sub-3-12 已存在。

## PRD-完整文档 §七 消息功能 补全
- 需求：将消息页现有功能/逻辑/交互在「七、消息功能」补充完善，布局/列表项/字段规格仅本节描述（他文档不重复），相关跨模块项引用他文档减冗余。
- 改写 PRD-完整文档.html「七、消息功能」：新增 7.0 定位与范围 / 7.1 页面布局 / 7.2 两端 Tab 结构（owner=邀请+系统消息、service=邀请+合同邀约，均无任务通知）/ 7.3 三类消息字段规格（invite/system/contract_invite）/ 7.4 状态与展示规则（邀请4态、合同邀约5态、未读角标）/ 7.5 交互逻辑 / 7.6 邀请接收与角色确认规则（保留原有）/ 7.7 合同邀约来自 ContractStore（引 PRD-合同模块 #page-mini-service 3.7–3.13）/ 7.8 数据来源与持久化。
- 引用减冗余：邀请方式/身份权限 → PRD-项目模块 7.5/7.6；工人合同邀约生成/确认/撤回/抢单失败 → PRD-合同模块；任务提醒 → PRD-任务模块（待办）。
- 同步更新功能导航表消息行（≈line860）备注：邀请/合同邀约(仅service)/系统消息(仅owner)，任务归口待办。
- 校验：PRD-完整文档.html HTML BALANCED、#message-feature 锚点存在、7.0–7.8 子节齐全。

## 待办生成规则 SSOT 重构（合同模块为权威出处）
- 用户要求：签约前配置执行人不生成待办（非工人+工人合同通用）；触发点描述放 PRD-合同模块「已签约/变更生效」；变更生效仅对变更项任务含执行人生成/失效待办；任务模块改为引用（减冗余）。
- 重构：上一轮任务模块 §3.12 的详细规则改为「引用合同模块」；合同模块 §3.12.14 已签约 升级为权威规则（新增 id=sec-3-12-14），含：前置约束(签约前不生成)、签约一次性扫描生成、可施工态下补配置、归属去重；明确非工人+工人合同通用。
- §5.15.8 变更已生效 升级为权威规则：仅对变更项(新增/修改阶段任务)含执行人任务生成/失效(invalidated 引 §4.8.11)，非变更项不动。
- 工人合同两侧补引用：§7.5.3.5 已签约(worker_signed) 关键逻辑加引用 §3.12.14；§9.5.5 变更生效返回 关键逻辑加引用 §5.15.8。
- 任务模块 §3.12 改为 3 段精简引用；§4.8.2 生成规则行改为双来源(①合同模块 §3.12.14/§5.15.8 权威 ②§3.15(5) 驳回)。校验：task 564/564、contract 469/469 平衡；锚点 sec-3-12-14/sec-5-15-8 均存在。

## 业主端消息页移除「系统消息」Tab + PRD 同步
- 需求：owner-miniapp 消息页删除「系统消息」Tab，并同步 PRD，不影响其他功能。
- 改动 `owner-miniapp/message.html`：移除 系统消息 tab div、messages.system 数据、renderMessageCard 内 system 分支、viewDetail 函数、icon 三元 task→邀请分支（简化为 ✉️）。owner 现仅 邀请 一 Tab。
- PRD 同步（PRD-完整文档.html §七）：7.0 覆盖范围去掉「系统公告（仅业主端）」；7.2 Tab 表删 业主端系统消息行、note 改「业主端仅含邀请一个 Tab」；删 7.3.2 系统消息字段子节并将 7.3.3 合同邀约重编号为 7.3.2（同步 §7.7 引用 7.3.3→7.3.2）；删 7.4.3 系统消息角标 bullet；7.8 持久化「邀请消息/系统消息」改「邀请消息」、未读同步同步修正；功能导航表消息行去掉「系统消息（仅业主端）」。
- 校验：owner-miniapp/message.html HTML BALANCED、残留 scan switchTab('system')/type:'system'/system:[/viewDetail/系统消息 全 0；PRD-完整文档.html HTML BALANCED、#message-feature 存在、7.3.2 合同邀约就位、7.3.3 残留 0。
- 更新 MEMORY.md 第28行：owner 现仅 邀请 一 Tab。

## 业主端消息页取消单 Tab 栏，未处理数量移到底部导航「消息」角标
- 需求：业主端消息页现仅一个 Tab 不合理，取消分类 Tab 栏，将未处理消息数量以红色角标展示在底部导航「消息」项右上角，PRD 同步。
- 改动 `owner-miniapp/message.html`：
  - 删除 `<div class="message-tabs">` 单 Tab 栏（含 inviteBadge）；消息直接列表渲染（currentTab 仍 'invite'）。
  - 底部「消息」tab-item 内 icon 外包 `.nav-icon-wrap`（position:relative），加 `<span class="nav-msg-badge" id="navMsgBadge">4</span>` 角标（CSS：红底白字圆角、top-right 绝对定位、.hidden 隐藏）。
  - `updateBadge()` 改为操作 #navMsgBadge（classList hidden 切换，替换原 #inviteBadge 的 display 切换）。
  - 删除死函数 `switchTab(tab)`（含 event.currentTarget，已无 tab 触发）。
  - 保留：邀请接收/角色确认、未读角标、markAllRead、演示控制等全部其他功能。
- PRD 同步（PRD-完整文档.html §七）：7.1 Tab 栏行加「仅多于一个分类时展示，业主端不展示、数量移底部导航」；7.2 业主端行改「无分类 Tab（仅邀请一类）」；note 改「业主端不展示分类 Tab 栏」；7.4.3 增「业主端 底部导航消息角标」条目（红色圆角、updateBadge 维护）。
- 校验：owner-miniapp/message.html HTML BALANCED、inviteBadge/switchTab/message-tabs 残留 0（仅 orphan 的 .message-tab CSS 规则无害保留）；PRD HTML BALANCED、新表述均 present。
- 更新 MEMORY.md 第28行：owner 不展示分类 Tab 栏、未处理数量以底部导航消息项角标呈现。

## 合同任务「锁定→解锁」统一规则补入 PRD（签约前 / 变更期间）
- 用户指出此前「签约前不生成待办」描述正确但有歧义：应圈定为「签约前任务锁定、不支持执行（即使有执行人）」；并补充「发起变更后任务锁定，变更失败或成功到达终态才解锁」。
- 重构 PRD-合同模块详细规格.html §3.12.14 已签约：①「阶段任务恢复流转」→「任务锁定解除（可施工态）」；②原「前置约束（签约前不生成）」bullet 升级为「⚠️ 任务锁定（签约前）（重要）」红色重要标识——明确即使已配执行人并待开始，签约前仍锁定、不可提交完成/确认、不生成待办。
- §3.12.14 表格后新增红色重要 note 框「合同任务『锁定→解锁』统一规则（签约前/变更期间）」整合两类锁定。
- 新增权威小节 §5.15.0「变更期间任务锁定规则（通用）」（锚点 sec-5-15-lock，红字）：发起变更即锁定，覆盖变更全周期（changing/change_platform_reviewing/change_confirming*/change_signing_wait 等）；变更失败（驳回/撤回→回 signed，按 V1 恢复）/变更成功（change_confirmed→按 V2 恢复）达终态才解锁；与签约前锁定同语义、非工人合同与工人合同通用。TOC 加 5.15.0 索引。
- 交叉引用：§5.15.1/§9.5.1「阶段任务暂停流转」→「阶段任务锁定（暂停流转）」并链 sec-5-15-lock；§5.15.8「阶段任务恢复」→「阶段任务解锁恢复」链 sec-5-15-lock；§7.5.3.5/§9.5.5 工人合同加锁定引用。
- 校验：合同模块 div 474/474 平衡；sec-5-15-lock、sec-3-12-14 锚点均存在，跨章链接无悬空。

## 任务列表数据范围补入 PRD（§2.4 列表卡片规格）
- 用户要求：任务模块 PRD 内全部任务补充「数据来源 = 已签约合同内全部有效任务（有效=未被删除）」，补在列表卡片规格内。
- PRD-任务模块详细规格.html §2.4 顶部（§2.4.1 前）新增黄色重要 note 框「任务列表数据范围（全部任务来源·重要）」：①已签约——仅 signed/worker_signed（含变更生效 V2）合同任务入列，未签约/锁定合同不入列（链合同模块 §3.12.14）；②有效(=未被删除)——软删除任务不入列，invalidated 不算删除仍计入并按 §2.7/§4.5 灰化。
- §5.5 业主端任务卡片 补一句明引 §2.4 顶部数据范围说明（覆盖业主端）。
- 校验：task div 566/566 平衡；新 note 与 §5.5 引用均 present。
- 修正：用户质疑 §2.4「有效」行误把 invalidated 归为任务状态——经核查 invalidated（已失效）是**待办(todo)状态**（§4.8.11 待 办失效），任务主状态机(§一)只有 待配置/待开始/执行中/确认中/已完成（已驳回为确认结果非主状态），**任务无 invalidated 状态**。已删除 §2.4「有效」行中 invalidated 表述，仅保留「未被删除(软删除不入列)」。

## 消息页「缺少链接 / 不符合PRD英文」修正（PRD-完整文档.html）
- 用户要求：关联文档/页面缺少链接请补充（点击可跳转）；不符合PRD英文（裸英文）请翻译或处理。
- 缺链接补充：① 文档清单表（1141-1145）5 个 PRD 文件名改为可点击超链接；② §七 7.5「合同邀约查看」中 worker-contract-detail.html 纯文本改为指向 `service-miniapp/worker-contract-detail.html?viewer=receiver` 的超链接（含 `<code>` 包裹）。
- 英文规范化：§七 裸英文 `invite` 改为「邀请」（7.4.3 两处）；标题/正文中的 `type=invite` / `type=contract_invite` 用 `<code>` 包裹（7.3.1/7.3.2/7.4.1/7.6），符合「中文（英文标识符）」约定。
- 校验：PRD HTML BALANCED、`#message-feature` 锚点存在、`<,strong` 残留 0、裸 invite 清零；消息原型页可见文本无英文（英文仅在 `<code>`/属性，符合约定）。
- 跟进完成（2026-08-19 二轮）：两端消息页 page-nav「项目详情」与 goToProject（接受邀请后跳转）旧 `project-detail.html` 已统一为权威新版 `project-detail-ongoing-v2.html`（owner 3 处 v2、service 2 处 v2，旧引用清零；service 底部 TabBar 为 project-list.html 非详情页，不动）。

## 已完成-新版项目详情页「今日动态」改为空态卡片（service/owner）
- 用户要求①：已完成态「今日动态」示例内容（竣工验收/签署结算对账单/全部任务验收）无匹配的动态生成规则，应删除；用户要求②（修正）：卡片本身须保留（含「查看全部」入口），无数据展示「今日暂无动态」空态。
- `service-miniapp/project-detail-completed-v2.html` 与 `owner-miniapp/project-detail-completed-v2.html` 最终状态：保留「📝 今日动态」卡片 + `.more`「查看全部 ›」（data-href→activity-list.html，走通用 [data-href] 委托）；卡片体为 `.simple-empty-state`（id=activityEmpty，📭 今日暂无动态）；fadeUp 入场动画以 `.card:has(#activityEmpty)` 恢复（delay 0.24s）。
- 已删净：3 条示例动态 HTML、`.activity-role-tag`/`.activity-stage-tag` 全套 CSS、v2 布局第 7 节 activity 覆盖样式、头像描边、`toggleActivityList()`/`activityMoreBtn`/`activityExpanded` JS 及绑定。
- 基础 `.activity-*` 样式来自共享样式表（ongoing-v2 等页仍用），未动。
- 校验：示例残留 0；div 平衡 service 192/192、owner 204/204。

## 合同「已履约完成」派生标识（方案A 落地）
- 需求：合同内任务全部完成时是否加「已履约完成」标识 → 用户确认方案A「派生标识（不动状态机）」。口径：判定=已签约态(signed/worker_signed/变更态) 且全部有效任务均「已完成」；实时派生无持久化、不入合同状态机；不阻断变更；任一任务驳回回流则标识自动消失。
- 原型（service + owner 两份对称）：
  - `contract-detail.html`：status-banner 的 #statusText 内加 `<span class="fulfilled-badge" id="fulfilledBadge">✓ 已履约完成</span>`（默认 display:none）；右侧导航「签约阶段」count 1→2，加「已签约（已履约完成）」演示项 → `ContractDetailPage.demoFulfilled()`。
  - `css/contract-detail.css`：加 `.status-banner .fulfilled-badge`（半透明白底白字圆角徽标）。
  - `js/contract-detail.js`：加 `updateFulfilledBadge(status)`（扫描 .task-status 全 completed + signed 态判定）、`demoFulfilled()`（置全部任务 completed + 改 task-desc/stage-meta/stage-progress 为完成态后 updateContractStatus('signed')）；在 `updateContractStatus` 内 `updateStageTasksDisplay(status)` 后挂载 `updateFulfilledBadge(status)`；return 块 + window 别名暴露 demoFulfilled。
- PRD 同步：
  - PRD-合同模块 §3.12.14 已签约 表格后新增绿色 note「✓ 履约完成派生标识（全部任务完成·无独立状态）」：判定/三处呈现位置(详情横幅/项目详情合同概览/合同列表)/不阻断变更/原型演示项。§7.5.3.5 工人合同「关键逻辑」加一条引用 §3.12.14。
  - PRD-项目模块 §3.4 合同概览卡片「合同列表（可折叠）」行补「该合同全部任务完成时叠加绿色『已完工』角标」并链 PRD-合同模块 §3.12.14。
- 校验：service/owner js `node --check` 均 OK；owner contract-detail.html BALANCED、两份 PRD BALANCED；service contract-detail.html 的 1 个 unclosed<div>（line 289 phone-frame）为预先存在（git show HEAD 同样存在，非本次引入，diff 仅 3 增 2 删）。
- 待说明：无独立「合同列表页」（contract-list*.html 不存在），合同列表实为项目详情「合同概览」卡片；项目详情 ongoing-v2 为进行中场景（总进度65%、无100%合同），故「已完工」角标规则仅写入 PRD、原型层未硬造数据，演示由合同详情页「已签约（已履约完成）」演示项体现。工人合同详情页无任务进度展示（.task-status 不存在），徽标规则 PRD 已统一适用，原型暂未加。
- 修复（用户反馈「点击已签约（已履约完成）导航项无选中、页面无徽标」）：① 根因 A：badge 原放在 #statusText 内部，被 `updateContractStatus` 的 `statusText.textContent=config.text` 覆盖清空 → 改为独立兄弟元素，外包 `.status-text-line`（flex 容器）承载「statusText + fulfilledBadge」同行；CSS 加 `.status-banner .status-text-line{display:flex;align-items:center;flex-wrap:wrap;margin-bottom:4px}` 及 `.status-text-line .status-text{margin-bottom:0}`。② 根因 B：`updateStatusSwitchHighlight` 按「文案===已签约」匹配，演示项文案「已签约（已履约完成）」不命中 → `demoFulfilled` 末尾手动 `classList.add('active')` 高亮该演示项。两端（service/owner）对称修复，JS node --check OK、owner HTML BALANCED。
- 修复（用户反馈「两个导航项同时高亮 + 点已签约无法切回非履约完成态」）：根因 = demoFulfilled ①未先清 active 即手动加 active（导致与「已签约」项同时高亮）；②直接改写了 task-status/task-desc/stage-meta/stage-progress 为完成态且不可逆（点「已签约」仍残留全完成）。修复：引入 `_fulfilledSnapshot` 快照（statusClass/statusText/desc/meta/pct/pctColor）+ `snapshotTaskState`/`applyFulfilledState`/`restoreTaskState` 三函数；`demoFulfilled` = 快照→全完成→signed→先清所有 active 再只高亮「已履约完成」项；新增 `demoSigned` = 恢复快照→signed（徽标按真实派生显隐）。「已签约」导航项 onclick 由 `updateContractStatus('signed')` 改为 `demoSigned()`。两端对称；暴露 `demoSigned` 至 return + window。JS node --check OK、owner HTML BALANCED。
- PRD §3.12.14「履约完成派生标识」note 标题追加 proto-link：指向 `service-miniapp/contract-detail.html`（文案「原型页（右侧导航『签约阶段』→『已签约（已履约完成）』）」），点击可跳转。校验 PRD HTML BALANCED。

## 架构层级联动：项目详情 → 全部任务 / 项目动态（方案A 落地）
- 背景：项目详情架构切换选中层级后，「查看全部」跳转裸链接；task-list/activity-list 无层级概念。用户确认方案A：带参跳转 + 目标页可移除层级标签 + 过滤数据，两端同步，不改动既有交互。
- 详情页（3 页）：`service/project-detail-ongoing-v2.html`、`service/project-detail-completed-v2.html`、`owner/project-detail-completed-v2.html` 的 `selectLevel()` 末尾追加——遍历 `.card-title .more`，对 task-list/activity-list 链接按当前层级改 `dataset.href`（`?level=`+encodeURIComponent；项目部=还原不带参）。owner ongoing-v2 层级切换为静态展示（无 selectLevel JS），不携带、未改。
- task-list（service/owner）：4 个 `.contract-section` 加 `data-level`（水电/泥瓦/木工/油漆工作组，合同↔工作组一一对应）；`currentFilters.level:null` 新键；`filterTasks()` 分组层早退（层级不匹配→display:none+return，任务级逻辑零改动）；`updateFilterTags`/`removeFilter` 加 level 分支（文案「架构层级：XX」）；DOMContentLoaded 解析 URLSearchParams level 初始化。
- activity-list（service/owner）：activities 10 项加 `level` 字段（source 映射：水电班组→水电工作组、木作→木工工作组、泥瓦工→泥瓦工作组、设计服务/基础施工→项目部）；`currentLevelFilter`；`renderActivities()` 过滤链头部一行；新增 `.level-filter-row` CSS + `#levelFilterRow/#levelFilterTag` HTML（默认 display:none）；`clearLevelFilter()` + `initLevelFilter()` IIFE。
- 口径：项目部=全部层级不带参；层级与既有筛选（参与/状态/搜索；类型/日期）AND 叠加；计数联动；全部待办 todo-list 不携带层级（按人聚合，无对应关系）。
- PRD：任务模块 §2.5 加「架构层级」筛选行+规则段（触发来源/呈现/整组显隐/AND 叠加/项目部口径/业主差异），§5.6 业主端补引；项目模块 §3.7 查看全部+跳转逻辑说明改携带规则、§3.8 交互表查看全部行、§3.6 任务概览查看全部行（1129/1373 两处）、§11.7 改题「架构层级 / 类型与日期筛选规格」加层级行（覆盖原「层级筛选未实现已移除」note 表述，TOC 同步）、§13.4 业主端同题加层级段、业主详情交互表 查看全部行（5247）、业主任务概览 查看全部行（5370）。
- 校验：9 文件 div/table/tr 全平衡（任务 PRD 566/566、项目 PRD 723/723、task-list 319/319×2、activity-list 51/51×2 等）。

## PRD 合同模块 §3.3 功能差异表按原型重写
- 用户反馈 §3.3「功能差异（与PC端对比）」与实际原型不一致。
- 原表错误：服务方小程序列「❌ 无审核功能 / ❌ 无编辑功能」错误（实际有「提交审核」「拟定中可编辑」）；PC 端列「可编辑合同 / 业主确认 / 甲乙双方确认签约」也错（PC 端=运营审核端，只读、不编辑、不确认、不签约）。
- 重写为：加引言定位「PC端=运营审核端（§2.6/§2.7）；服务方小程序=发起/确认/签约端（§3.12）」；表头列改「PC端（运营审核端）」「服务方小程序」；5 行：审核（运营审核 vs 提交审核-审核由平台完成）、编辑（只读 vs 拟定中可编辑-提交后只读）、变更（审核变更申请 vs 发起变更-已签约）、确认合同（不参与 vs 业主甲方最终确认-服务方发起方待对方确认）、签约（不涉及 vs 乙方服务方上传签约文件生效）。
- 引用锚点修正：§3.12.10/11/12 无 id，改用父锚点 #sec-3-12（「见 3.12.10/3.12.11」「见 3.12.12」），避免失效链接。
- 校验：PRD HTML BALANCED；锚点 sec-3-3/sec-3-12/sec-2-6/sec-2-7 均 present；失效引用 sec-3-12-10/11/12 残留 0。

## 项目详情（进行中）复制并做资深 UX 优化（新页面，不影响其他页）
- 需求：以 service-miniapp 端 `project-detail-ongoing-v2.html` 为蓝本，复制一份新页面做布局/样式/UX 优化，不影响其他页面与功能。
- 新文件 `service-miniapp/project-detail-ongoing-expert.html`（整文件复制后增强）。新增模块均为「仅追加、不覆盖既有选择器」：
  - 头部「进行中」状态药丸（带呼吸点微动效 `uxLivePulse`）——`.status-pill`。
  - 项目阶段进度时间轴卡片（`.stage-timeline`）：6 节点 水电✓/泥瓦(进行中·脉冲)/木工/油漆/安装/完工，连线条 + 整体进度条 65%（`.stage-progress`）。
  - `:root` 设计令牌（仅本页增强用，避免污染全局共享样式 `common.css` / `css/project-detail.css`）。
  - 入场动效复用既有 `fadeUp`；新增 `@media (prefers-reduced-motion: reduce)` 无障碍守卫（关闭脉冲/入场/卡片过渡）。
  - 原型导航新增「项目详情（进行中·专家优化）」并设为 active，原 v2 项取消 active。
- 校验：新文件 HTML BALANCED、新模块 token 均 present；源 `project-detail-ongoing-v2.html` 未改动（仅保留其自身 active 导航）。
