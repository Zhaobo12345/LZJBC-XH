# 2026-08-12 工作日志

## 项目动态页（小程序端）跳转修复
- **现象**：用户反馈"进行中的项目详情，点击查看全部动态"进入的是空项目动态页（无数据）。
- **核实结论**：仓库（含所有 `.bak`）内**从未存在**带数据的「项目动态」独立页面，只有空占位页 `activity-list-empty.html`（"暂无动态"）。不是"数据页丢失"，而是此前链接错误地指向了空占位页。
- **修复**：
  - 新建 `service-miniapp/activity-list.html`（带数据版「项目动态」）：复用 `css/project-detail.css` 的时间轴样式（`.activity-item`/`.activity-header`/`.activity-media` 等），含「今日 01-15」4 条动态（李监理验收/张工长提交验收申请/王施工完成水电改造/陈设计更新图纸）+「更早」4 条历史动态（材料进场验收/阶段开工/创建项目/效果图初稿），条目点击跳转 `task-detail.html`。
  - 将 4 个在用项目详情页的「查看全部」链接由 `activity-list-empty.html` 改为 `activity-list.html`：`project-detail.html`、`project-detail-ongoing-v2.html`（新版）、`project-detail-completed.html`、`project-detail-new.html`。
  - 空占位页 `activity-list-empty.html` 保留，作为"确无数据"时的兜底。
- **注**：上轮 Write `activity-list.html` 曾报 `499 canceled`（状态回传超时），但文件实际已落盘（199 行完整），本轮回传确认后仅补做链接改指。
- 改动文件：`activity-list.html`（新建）、`project-detail.html`、`project-detail-ongoing-v2.html`、`project-detail-completed.html`、`project-detail-new.html`。

## 项目列表页列表项跳转改向
- 用户要求：小程序「项目列表」页（`service-miniapp/project-list.html`）的列表项点击跳转到「项目详情（进行中-新版）」`project-detail-ongoing-v2.html`。
- 改动：将 3 个 `.project-card` 的 `onclick="location.href='project-detail.html'"` 全部改为 `project-detail-ongoing-v2.html`（含 2 条「进行中」与 1 条「已完工」演示卡，按用户"列表项"泛指统一改向；若仅需「进行中」改向可再细化）。原型导航 `<a>` 链接未动。
- 改动文件：`service-miniapp/project-list.html`。

## 业主端「我的家」导航与 服务端初始详情页提示可点击
- 业主端底部主导航「我的家」统一跳转到「项目详情（进行中-新版）」`project-detail-ongoing-v2.html`：
  - `home.html:285`、`message.html:580`：原指向旧版 `project-detail.html` → 改向 `project-detail-ongoing-v2.html`。
  - `project-detail.html:432`（旧版）、`project-detail-completed.html:411`：原 `tab-item active`（无跳转）→ 改为可点击跳向 `project-detail-ongoing-v2.html`。
  - `project-detail-ongoing-v2.html:525`：「我的家」本就是该页，保持 `active` 不变。
  - `.bak` 备份文件保留旧值，不对外访问。
- 服务端「初始项目详情页」`service-miniapp/project-detail-new.html` 顶部引导提示条文案「项目创建成功，请添加架构层级创建合同」增加点击效果：
  - 文案加 `onclick="location.href='architecture-empty.html'"` 并末尾加「→」指示可点；`.tip-text` CSS 加 `cursor:pointer` + hover（opacity 0.7 + 下划线）。
  - 跳转目标与该页自身「架构」快捷入口（line 269）一致，指向 `architecture-empty.html`（创建架构层级的空态页）。close-btn（✕）为独立 span 且其监听在 guideTipClose 上，点击 ✕ 不会误触发跳转。
- 改动文件：`owner-miniapp/home.html`、`owner-miniapp/message.html`、`owner-miniapp/project-detail.html`、`owner-miniapp/project-detail-completed.html`、`service-miniapp/project-detail-new.html`。

## 业主端项目动态页补全 + 架构空态页「创建工作组」跳转
- 业主端「项目详情（进行中-新版）」`owner-miniapp/project-detail-ongoing-v2.html` 今日动态「查看全部」此前已改为 `data-href="activity-list.html"`（本次核实确认生效）。
- 业主端缺独立「项目动态」页，按 service 版 `service-miniapp/activity-list.html` 复制建立 `owner-miniapp/activity-list.html`（标题「项目动态」、11 条动态、筛选头/日期分组/角色·阶段标签/权限弹窗均与 v2 详情字段一致）。
- owner 版 activity-list 的 page-nav 原「项目列表」指向不存在的 `project-list.html`（owner 端无此页），改为「我的家」→ `project-detail-ongoing-v2.html`，避免死链；其余链接（首页/项目详情/任务列表/合同详情）在 owner 端均存在。
- `service-miniapp/architecture-empty.html` 两处「创建工作组」按钮（header 内 `create-btn-inline`、列表底部 `add-node-btn`）原 `onclick="showCreateModal()"` → 改为 `onclick="location.href='architecture.html'"`（跳转到含「添加工作组/施工组」弹窗流程的完整架构页 = 用户所指"点击创建工作组效果页面"）。
- **顺带修复** `architecture-empty.html` 文件内 6 处孤立 `+` 字符损坏（line 181/186/193 在 CSS 中形成非法相邻兄弟选择器、line 485/519/556 在 JS 中形成语法错误导致整段脚本失效）：已清除，脚本语法恢复可用。
- 改动文件：`owner-miniapp/activity-list.html`（新建）、`owner-miniapp/project-detail-ongoing-v2.html`（核实不改）、`service-miniapp/architecture-empty.html`。

## 项目动态页图片替换为施工现场照片
- 用户要求：项目动态（`activity-list.html`）内图片替换为施工现场图片，其他功能不变。
- 原图用 freepik 远程链：taskImages（4 张，已是水电布线/墙面抹灰/水管打压/线管安装施工现场主题）+ contractImages（2 张，办公室签约/合同文档，**非**施工现场）。
- 改为本地生成施工现场照片，落地 `shared/images/site-1.png`~`site-4.png`（写实风格 1024×1024）：site-1 电工布线、site-2 墙面施工、site-3 水管打压、site-4 线管安装。
- **避坑**：ImageGen 并行生成 6 张时文件名按秒时间戳相同会互相覆盖，只剩 1 张；改为**逐张生成后立即重命名**才保住 4 张。用户在第 5 张生成时拒绝了工具调用，故仅保留 4 张，合同类（contractImages）复用其中 site-2/site-4，全部为施工现场主题，未再消耗信用分。
- 两活动页（service-miniapp/owner-miniapp 的 activity-list.html）的 taskImages/contractImages 数组改为引用 `../shared/images/site-X.png` 本地相对路径（两端均位于子目录，相对路径可达），删除全部 freepik 远程链接。
- 改动文件：`service-miniapp/activity-list.html`、`owner-miniapp/activity-list.html`、`shared/images/site-1..4.png`（新增）。

## PRD 项目动态/今日动态 文档定位与补充
- 用户问两处描述位置并要按原型补充：
  - **今日动态规则**：PRD-项目模块详细规格.html 第三章 **3.7 今日动态卡片规格**（line ~1166，含功能/数据/展示/默认3条/媒体/时间/查看全部跳转），另在 3.8 交互规格表有展开更多/查看全部交互项。
  - **项目动态页详细需求**：第十一章 项目动态页（服务方，含 11.1-11.6）+ 第十三章 项目动态页（业主）+ 第二十章 20.5 项目动态表字段（project_activities）。
- 原型比文档多出功能，已**纯新增小节**补充（未改已有正文）：
  - 第11章新增 11.7 类型与日期筛选规格、11.8 角色标签与阶段标签、11.9 权限锁定与无权限弹窗、11.10 媒体展示（施工现场照片）；TOC 同步加锚点。
  - 第13章新增 13.4 类型与日期筛选（业主端）、13.5 角色/阶段标签（业主端）、13.6 权限锁定（业主纯查看）；TOC 同步加锚点。
  - 第3.7节加"展示规则补充"note：角色/阶段标签、施工现场照片、展开更多规则。
- **冲突待确认（未改原有描述）**：11.2 页面结构 & 11.4 数据加载规则写的是"层级筛选器（项目部/工作组/施工组下拉，URL参数level）"，但原型实际落地"类型筛选(全部/任务/合同/系统)+日期筛选(全部时间/今天/本周/本月/自定义)"，**层级筛选未实现**。已在 11.7 note 标注"待产品确认"，需用户决策是否修订 11.2/11.4。
- 改动文件：`PRD-项目模块详细规格.html`（目录+11章+13章+3.7节 纯补充）。

## PRD 今日动态「默认3条」错误描述修正
- 用户指出 §3.7「今日动态卡片规格」描述错误：不是"展示最新3条"，应为"展示今日全部动态、默认折叠一条半（首条完整+次条露半条）、点击展开更多展示今日全部"。与 3.12 差异表、3.8 交互行一并修正。
- 修正 PRD-项目模块详细规格.html 共 4 处错误描述 + 1 处口径统一（均为本次聚焦的"相关描述错误"，未触碰此前待确认的层级筛选冲突）：
  - §3.7 表格「默认显示条数 = 最新3条」→「展示范围与默认折叠：展示今日全部动态，默认折叠约一条半高度（首条完整+次条露半条带截断渐隐）；今日无动态整块不展示」（属性名同步由"默认显示条数"改为"展示范围与默认折叠"）。
  - §3.7「功能说明」"最新动态"→"「今日」产生的动态"（口径统一）。
  - §3.7 展示规则补充「展开更多：动态多于3条…少于3条不显示」→"超过一条半高度时显示，仅1条或更少不显示"。
  - §3.8（服务方）与业主端 §3.8 的「展开更多」按钮交互行（两处文字相同，replace_all=true 同步两端）："展开/收起隐藏的动态项"→"展开解除一条半折叠、显示今日全部动态；仅不足一条半不显示"。
  - §3.12 已完成状态差异表「今日动态-展开更多：动态少于3条时不显示」→"不足一条半（仅1条或更少）时不显示；与进行中一致"。
- 校验：Grep 全文无残留"最新3条/少于3条/多于3条"等错误表述。
- 改动文件：`PRD-项目模块详细规格.html`。

## 11.2/11.4 层级筛选 → 类型+日期筛选（按原型修订）
- 用户确认此前待确认的冲突，选择"以原型为准"：将 11.2/11.4 的"层级筛选器"替换为"类型 + 日期筛选"（与已实现功能一致）。
- 修订 PRD-项目模块详细规格.html：
  - 11.2 结构图：层级筛选器（项目部/工作组/施工组下拉）→ 类型筛选器（顶部 chips：全部/任务/合同/系统）+ 日期筛选器（下拉+自定义：全部时间/今天/本周/本月/自定义）。
  - 11.4 数据加载：数据来源 URL `?level=xxx` → `?type=task|contract|system&date=all|today|week|month|custom`；"层级筛选"行改为"类型筛选"+"日期筛选"两行。
  - 11.5 交互表："层级筛选"交互行 → "类型 / 日期筛选"交互行。
  - 11.7 note：「差异说明（待产品确认）」→「一致性说明」，标注 11.2/11.4 已按原型修订为类型+日期。
  - §3.7（今日动态卡片）："查看全部动态"跳转的"传递层级参数 level"描述 → 改为"默认展示全部、进入后通过类型+日期筛选器过滤"；参数传递/页面逻辑 note 同步去 level。
  - §3.8 交互表："携带当前层级参数" → "无需携带层级参数，进入后通过类型+日期筛选器过滤"。
  - 13.2 功能说明表（业主端）："层级筛选：业主可查看各层级动态" → "类型/日期筛选"（详见 13.4）。
  - 13.3 差异表（业主端）："层级筛选"行 → "类型 / 日期筛选"行。
- 保留未动：第 12 章（项目详情页）的"层级选择器"为详情页架构层级切换真实功能，与项目动态页筛选无关，故不改。
- 校验：Grep 全文"层级筛选/层级参数"仅剩"无需传递层级参数"及 11.7 一致性说明（均正确），无其他层级筛选功能描述。
- 改动文件：`PRD-项目模块详细规格.html`。

## PRD 动态类型删三类 + 页面路径可点击 + 二级目录选中样式
- **删除动态类型规格三类**（§11.3 动态类型规格）：文件上传、成员加入、对账单确认 三行整段删除；剩余 4 类：任务完成/任务提交/合同签署/合同审核。
- **同步相关说明**（确保无残留引用）：
  - §3.7 功能说明"包括任务完成、合同签署、文件上传等" → 去掉"文件上传"。
  - §11.5 交互表"跳转对应详情页（任务/合同/资料等）" → "（任务/合同）"（资料=文件上传）。
  - §11.9 权限锁定"有权限时点击跳转对应详情页（任务 / 合同 / 资料等）" → "（任务 / 合同）"。
  - §20.5 项目动态表字段 `project_activities.type` 枚举：删除 `member_joined（成员加入）` 与 `statement_created（对账单创建）`（与精简后 §11.3 一致），保留 `member_removed`。
- **未改（提示用户）**：§20.6 待办事项表 `todos.type` 枚举仍含 `statement_confirm（对账单确认）`，属"待办类型"非"动态类型"，超出本次删除范围，按用户口径保留；若需一并移除待确认。
- **页面路径链接可点击**（任务②）：§11.1（服务方）/ §13.1（业主）"页面路径"原为纯文本 `/pages/project/activity-list` 无法点击；保留小程序路由文本，下加可点击"原型："链接分别指向 `service-miniapp/activity-list.html`、`owner-miniapp/activity-list.html`（target=_blank，蓝字下划线）。
- **二级目录点击选中样式**（任务③）：原 `.nav-sub a.active` CSS 已存在（#F9F0FF 底+紫字+左边框+加粗），但因 JS 缺陷未生效。修复两处：
  - `updateActiveNav` 子章节判定 `scrollPos < subHeight` 错（subHeight 为区块高度，永远不成立）→ 改为 `scrollPos < subTop + subHeight`，使滚动时正确高亮当前子章节。
  - 二级链接点击处理函数内新增：点击即 `subNavLinks.forEach(remove active)` + `this.classList.add('active')`，立即给出选中反馈（滚动后续由 updateActiveNav 维持一致）。
- 改动文件：`PRD-项目模块详细规格.html`。

## PRD 动态类型新增「任务确认结果」+ 两项规则存在性核查
- **新增动态类型**（§11.3 动态类型规格）：增加「任务确认结果」类（确认人确认或驳回），内容模板「【任务名称】确认通过 / 已驳回（确认人【确认人姓名】）」，图标 ✔️，跳转任务详情页。
- **相关描述同步更新**：
  - §3.7 今日动态功能说明"包括任务完成、合同签署等" → "包括任务完成、任务确认结果、合同签署等"。
  - §20.5 `project_activities.type` 枚举新增 `task_confirm（任务确认）`（原枚举缺此值，且本就缺 §11.3 已有的 `task_submitted（任务提交）`，本次仅补用户要求的 task_confirm）。
- **两项规则存在性核查（用户疑"缺少"，经核实均存在，按"存在→告知位置"未补充）**：
  - **任务统计「查看全部」规则+跳转地址**：服务方 **§3.8 交互规格 line 1329-1332**（跳转到任务列表页 task-list.html）；业主端 **§13.8 line 5280-5283**（跳转到业主端任务列表页）。原型 task-stat 卡 `data-href="task-list.html"` 与文档一致。
  - **合同概览「右侧查看合同→查看全部合同」规则+子级操作**：服务方 **§3.8 line 1318-1327**（"查看合同"按钮切换合同列表展开/折叠=查看全部合同；合同项点击跳合同详情页）；**§3.12 line 1523**（已完成态合同概览-查看合同）；业主端 **§13.8 line 5269-5278**（合同列表展示+合同项点击跳转）。原型合同概览卡右侧"查看合同 ▼"即该折叠入口，合同项为其子级操作。
  - **备注**：上述规则集中在 §3.8/§13.8 交互规格，§3.5 字段展示规则中仅"待办事项"卡重复了"查看全部"行，任务统计/合同概览卡在 §3.5 未重复（如需 §3.5 一致性可再补，待用户确认）。
- 改动文件：`PRD-项目模块详细规格.html`（仅新增任务确认结果 + 同步两处描述；两项核查未改动）。

## PRD §3.5/§12.5/§20.5 确认项落地（查看全部行 + 枚举补 task_submitted + 跳转链接）
- 用户确认上条的两项待确认：① §3.5 与待办卡一致补"任务统计/合同概览"查看全部行；② §20.5 枚举一并补入 `task_submitted（任务提交）`；并"对应增加跳转链接"。
- **§3.5（服务方项目详情页字段表）**：
  - 合同概览卡片 `rowspan="3"`→`"4"`，加"查看全部合同"行：点击"查看合同"展开/折叠全部合同列表，合同项点击跳 `service-miniapp/contract-detail.html`（带可点击链接）。
  - 任务统计卡片 `rowspan="3"`→`"4"`，加"查看全部"行：点击"查看全部"跳 `service-miniapp/task-list.html`（带可点击链接，与服务方 §3.8 一致）。
- **§12.5（业主端项目详情页字段表，按多端一致性同步）**：
  - 合同概览卡片原 `rowspan="4"`（业主专属"合同状态标签"占第4行）→`"5"`，加"查看全部合同"行：跳 `owner-miniapp/contract-detail.html`。
  - 任务统计卡片 `rowspan="3"`→`"4"`，加"查看全部"行：跳 `owner-miniapp/task-list.html`（与业主端 §13.8 一致）。
- **§20.5 项目动态表字段 `project_activities.type` 枚举**：补入 `task_submitted（任务提交）`，现枚举为 contract_signed、task_submitted、task_completed、task_rejected、task_confirm、member_removed（与 §11.3 动态类型规格完全对齐；task_submitted 在 §11.3 已有跳转任务详情页说明）。
- **跳转链接说明**：本次所有新增"查看全部"行均附原型可点击链接（服务方/业主端分别指向对应子目录的 task-list.html、contract-detail.html），与原 §3.5 待办卡"显示条数"行（链接至任务模块 PRD）风格一致。
- 改动文件：`PRD-项目模块详细规格.html`。

