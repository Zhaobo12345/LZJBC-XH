# 2026-08-07 工作日志

## PRD-PC端其他模块详细规格.html 增补
- 将"一、合同文本模板页"与"二、阶段任务模板页"的「页面文件」字段值改为可点击链接（`.file-link` 样式：蓝字虚线下划线，hover 变橙色），分别指向 `pc/pc-contract-template.html`、`pc/pc-stage-template.html`（两文件均位于 `./pc/` 目录，相对 PRD 根文件有效）。
- 新增「1.7 列表页详细说明」小节，对齐真实页面 `pc/pc-contract-template.html` 的列表实现：
  - 1.7.1 列表页布局（左分类树 240px 固定 + 右工具栏/表格/分页）；
  - 1.7.2 左侧分类筛选与「分类」列字段强关联（节点名=标签枚举、count 实时统计、互斥单选、与顶部筛选 AND 叠加）；
  - 1.7.3 顶部搜索/城市/状态匹配规则（名称模糊、城市/状态精确、空=不限、全国≠所有城市、四条件 AND）；
  - 1.7.4 七列字段说明（名称可点击预览、城市、分类标签、版本徽标、状态徽标+disabled 行、更新时间、操作）；
  - 1.7.5 默认排序=更新时间倒序、列头不可排序、分页 10 条/页；
  - 1.7.6 状态切换联动（停用二次确认、行变灰、按钮文案切换、影响范围）。
- 注明：当前原型为静态演示数据，控件已就位但未绑定实时过滤，规则为"应实现规范"。
- 校验：文件 div 84/84 平衡；两链接目标文件存在；1.7 各小节命中。

## PRD 合同文本模板页重构为四个二级标题
- 将"一、合同文本模板页"内部结构重排，使 列表页/新建模板/编辑模板/启用停用 各自成为独立二级标题（`.subsection-title`），并补齐字段说明、规格、校验规则、交互效果：
  - 1.3 列表页：1.3.1 布局 / 1.3.2 七列字段说明 / 1.3.3 左侧分类筛选关联 / 1.3.4 搜索筛选匹配规则 / 1.3.5 排序与分页；
  - 1.4 新建模板：1.4.1 入口弹窗布局 / 1.4.2 字段规格（名称/分类/城市/描述/正文）/ 1.4.3 校验规则 / 1.4.4 交互效果；
  - 1.5 编辑模板：1.5.1 入口 / 1.5.2 与新建差异（回显、版本递增）/ 1.5.3 交互效果 + 版本说明；
  - 1.6 启用/停用：1.6.1 入口 / 1.6.2 停用二次确认 / 1.6.3 启用无确认 / 1.6.4 行联动 / 1.6.5 影响范围；
  - 原 权限/动画/异常 顺延为 1.7/1.8/1.9，并修正动画表（预览滑入、分类高亮等）与异常表（补充分类/城市/正文校验）。
  - 同步更新 1.2 页面结构 code-block 为实际布局（分类树+工具栏+七列）。
- 说明：新建/编辑字段对齐真实页面 `pc/pc-contract-template.html`（模板分类单选枚举、适用城市、合同正文富文本），与旧 PRD"适用类型多选"有出入，以实际实现为准。
- 校验：文件 div 102/102 平衡；四二级标题命中；section 边界与 2. 注释完好；无旧标题残留。

## PRD 目录（左侧页面导航）更新
- 用户反馈"目录未更新"：重构出 1.1–1.9 小节后仍只列三大章。已修复：
  - 给每个 `subsection-title`（1.1–1.9）加锚点 id `s1-1 … s1-9`；
  - 目录 `sidebar-toc` 下「一、合同文本模板页」改为嵌套二级导航（9 个子项 `href="#s1-X"`）；
  - 新增 `.sidebar-toc-sub` 样式（缩进/小字号/悬停·选中高亮），父项 `.toc-parent` 加粗；
  - 升级滚动高亮脚本：由原仅追踪三大章改为基于所有目录锚点动态计算，滚动到任意小节时对应目录项自动高亮；点击目录项平滑滚动到小节锚点均生效。
- 校验：div 102/102 平衡；9 个目录链接与 9 个锚点 id 完全对齐；嵌套 ul 闭合正常。

## 模板适用城市统一为 北京/南阳/西安（与小程序端一致）
- 用户要求模板内适用城市与小程序端相同，仅支持 北京、南阳、西安（去除 全国/杭州/上海/深圳）。
- 改动覆盖两文件：
  - `pc/pc-contract-template.html`：①顶部筛选城市下拉改为「全部城市 / 北京 / 南阳 / 西安」（全部城市=不限）；②新建/编辑弹窗「适用城市」下拉改为「北京 / 南阳 / 西安」，默认值由 全国→北京（line 946 `templateCity.value='北京'`）；③静态表格 6 行城市值同步改为 北京/南阳/西安/北京/南阳/西安；④JS `templateData` 演示数据 5 条 city 同步改为 北京/南阳/西安/北京/南阳。
  - `PRD-PC端其他模块详细规格.html`：1.3.2 城市列说明、1.3.4 城市下拉匹配规则（去掉"全国独立值"说明）、1.4.2 适用城市字段规格（取值+默认值）、1.4.3 校验（默认北京）、1.4.4 打开新建弹窗（城市默认北京）全部更新。
- 校验：grep 确认两文件 杭州/上海/深圳/全国 零残留；div 均平衡（pc 94/94、PRD 102/102）。

## 模板适用城市字段与小程序创建项目地址字段对齐（含"市"行政区划名）
- 用户指出：小程序内创建项目选择的地址字段（service-miniapp/create-project.html 省市区联动，city 值存为 **北京市 / 南阳市 / 西安市** 行政区划全名）与模板内「适用城市」字段（此前 PRD/页面统一为短名 **北京 / 南阳 / 西安**）命名不一致，导致按项目城市匹配适用合同模板时会失配。
- 结论：以小程序地址字段为准（行政区划全名），将合同文本模板「适用城市」统一改为 **北京市 / 南阳市 / 西安市**：
  - `pc/pc-contract-template.html`：①顶部筛选下拉「全部城市 / 北京市 / 南阳市 / 西安市」；②新建/编辑「适用城市」下拉 value+文本改为 北京市/南阳市/西安市，默认值改为 北京市；③静态表格 6 行城市值改为 北京市/南阳市/西安市/北京市/南阳市/西安市；④JS `templateData` 5 条 city 改为 北京市/南阳市/西安市/北京市/南阳市；⑤新建默认 `templateCity.value='北京市'`。
  - `PRD-PC端其他模块详细规格.html`：1.3.2 城市列说明、1.3.4 城市下拉匹配规则、1.4.2 适用城市字段规格、1.4.3 校验默认值、1.4.4 打开新建弹窗城市默认 全部改为 北京市/南阳市/西安市（默认 北京市）。
- 说明：阶段任务模板 `pc/pc-stage-template.html` 的「适用城市」为独立设计（选项 全国/杭州/上海/北京/深圳，含小程序不支持的城市及"全国"语义），不在本条指令范围，未改动，已向用户单独说明并征询是否一并对齐。
- 校验：grep 确认 pc 页面与 PRD 短名 北京/南阳/西安 零残留（仅余行政区划全名）；div 平衡未破坏。

## 需求 B：模板类型统一为 8 类合同 + 阶段任务模板适用城市对齐
- 用户指令：①阶段任务模板适用城市统一为「北京市/南阳市/西安市 + 保留全国」（去 杭州/上海/深圳，北京→北京市）；②合同文本模板与阶段任务模板的"模板类型"字段统一为 8 类：基础施工服务合同、设计服务合同、拆除班组服务合同、水电班组服务合同、木作班组服务合同、泥瓦工班组服务合同、油漆工班组服务合同、小零工服务合同，列表数据及其它功能（表单下拉、侧边分类树、编辑回显、校验文案、预览、JS 演示数据）同步；③PRD 同步。
- 改动文件：
  - `pc/pc-contract-template.html`（合同文本模板）：侧边树标题 模板分类→模板类型、树节点改为 8 类（count 4/3/3/5/2/3/2/2，"全部模板"24）；表单 label 模板分类→模板类型、下拉 id=templateCategory 选项改为 8 类；表头 分类→模板类型；列表 6 行 name+category 同步（水电/泥瓦/木工/油漆分包→对应班组服务合同；主合同标准版→基础施工服务合同标准版；旧版水电→旧版水电班组服务合同并补 data-id=6/inactive）；预览块 title/h3 同步；正文占位 分包事宜→服务事宜、分包合同正文→服务合同正文；JS `templateData` id1-4 category+name 同步、id5 基础施工服务合同、新增 id6 旧版水电班组服务合同（city 西安市）。
  - `pc/pc-stage-template.html`（阶段任务模板）：侧边树标题 工程类型分类→模板类型、树节点改为 8 类（count 3/2/2/3/2/2/2/2）；表单 label 类型分类→模板类型；`#templateType` 下拉改为 8 类；`#templateCity` 下拉 全国/杭州/上海/北京/深圳→全国/北京市/南阳市/西安市；顶部筛选城市下拉同步；4 张列表卡片 类型/城市同步；`editTemplate` 回填 `水电工程`→`水电班组服务合同`；校验文案 请选择类型分类→请选择模板类型。
  - `PRD-PC端其他模块详细规格.html`：全局 模板分类→模板类型、分类树→模板类型树、分类筛选→模板类型筛选、按分类过滤→按模板类型过滤；表格列 分类→模板类型；节点表/枚举改为 8 类（count 24/4/3/3/5/2/3/2/2）；第二节枚举 合同类型→模板类型；字段规格表取值列同步 8 类；业务目标 按城市和模板类型划分。
- 收尾补强（交付前校验发现）：①合同文本模板 `desc` 4 处"分包业务"→"班组服务业务"（与更名后"班组服务合同"一致）；②PRD 阶段任务模板 section 的「适用城市」枚举（表单选项行 + 筛选规则行）补"全国"，合同文本模板 section 的 纯文本（北京市/南阳市/西安市）保持不变（该模板无 全国 选项）；③删除临时批量替换脚本 `_sync_tmp.py`/`_sync_tmp2.py`/`_fix_prd.py`。
- 校验：三文件 杭州/上海/深圳/分包合同/主合同/补充协议/劳务合同/材料采购/水电工程(字段含义)/泥瓦工程/木工工程/油漆工程/其他工程/旧分类命名 零残留；div 均平衡（pc-contract 97/97、pc-stage 160/160、PRD 102/102）。注：合同文本模板正文占位与 desc 中仍保留"水电工程/泥瓦工程/木工工程/油漆工程"作为工种名（如"就水电工程服务事宜""适用于水电工程班组服务业务…"），属正文/描述语义，非 模板类型 字段，保留不改。

## 新建模板「保存并发布 / 保存草稿」功能流程完善（Part A）
- 用户确认需求 B 已完成，要求补全此前两个原型中新建模板的保存流程：合同文本模板与阶段任务模板的「保存并发布」此前为空白 stub（仅 alert），「保存草稿」按钮无任何 onclick 处理器。
- `pc/pc-contract-template.html`：
  - 新增 CSS `.template-status.draft`（橙 #FA8C16 / 底 #FFF7E6）、`.pc-btn-toggle.publish`（橙描边 hover 橙底白字）；`保存草稿` 按钮加 `onclick="saveDraft()"`、`<tbody>` 加 `id="templateTableBody"`。
  - 替换 stub `saveTemplate()` 为完整实现 + 辅助函数集（escapeHtml/formatNow/showToast/getNextId/buildRowHTML/updateSidebarCounts/persistTemplate/saveTemplate/saveDraft/publishDraft）：草稿仅校验名称→生成 `draft` 橙色徽标行（含一键「发布」）；发布全量校验 名称/类型/城市/正文（正文去占位文本）→红框高亮+alert 汇总→`persistTemplate('active')` 追加/替换行、刷新侧边分类树 count、Toast「模板已保存并发布」；`publishDraft(id)` 将草稿行转启用态（徽标转启用、publish 按钮换为停用 toggle）。
- `pc/pc-stage-template.html`：
  - 修复 `editTemplate` Bug：原代码从未设置 `currentEditId`，编辑保存会新建重复卡片。新增 `var currentEditId=null; var stageTemplateMeta={};`（置于 showCreateModal 上方），`showCreateModal` 首行重置 `currentEditId=null`，`editTemplate(id)` 增加 meta 分支回显 名称/类型/城市/描述 并置 `currentEditId=id`。
  - 新增 CSS 草稿态（同合同橙系）；`保存草稿` 加 `onclick="saveDraft()"`。
  - 替换 stub `saveTemplate()` 为完整实现 + 辅助函数集（escapeHtml/formatNow/showToast/getNextStageId/buildStageCardHTML/updateStageSidebarCounts/persistStageTemplate/saveTemplate/saveDraft/publishStageDraft）：阶段任务模板 名称/类型/城市 非强制，仅草稿强制名称；`persistStageTemplate(status)` 写入 `stageTemplateMeta[id]` 并 isNew 后置插卡片 / 否则 `card.outerHTML` 替换，`updateStageSidebarCounts()` 刷新分类树 count + 分页「共 N 条」；草稿卡显示「发布」按钮，`publishStageDraft(id)` 转启用态。
- 校验：pc-contract div 98/98、pc-stage div 169/169 平衡；node JS `--check` 两文件均 OK；各函数仅 1 次定义；无旧 stub 残留。

## PRD-PC端其他模块详细规格.html 文档风格统一补充（Part B）
- 参考 `PRD-项目模块详细规格.html` 文档风格（字段类型表、校验规则表、流程说明、参数规格、交互规则、状态流转图等颗粒度），对本文档做风格统一与章节补齐。
- 修正需求 B 收尾遗留 Bug：前序曾误将 `全国` 加进合同模板 section（line 350 城市下拉、line 387 表单字段），本次恢复为「（北京市 / 南阳市 / 西安市）」/ 取值「北京市 / 南阳市 / 西安市」（合同模板无全国选项，符合需求 A 小程序全名对齐）；经验证 `全国` 现仅 line 629（stage section，阶段模板确有全国选项）。
- 合同文本模板 section 新增 **1.4.5 保存流程与状态流转**（置于 1.4.4 之后）：含 两种保存动作差异表（触发按钮/校验时机/校验强度/生成状态/列表联动/提示/失败处理）+ 草稿→启用→停用 闭环 ASCII 状态流转图 + 3 条 ul 说明（草稿→启用 publishDraft、启用↔停用见 1.6、启用/停用→草稿回退）。
- 阶段任务模板 section 新增章节并重排编号：在 2.3（原动画）前插入 **2.3 新建模板**（2.3.1 入口弹窗布局 / 2.3.2 字段说明与规格表[字段/类型/必填/校验规则/错误提示，含 适用城市 枚举 全国/北京市/南阳市/西安市] / 2.3.3 校验规则表[保存草稿 vs 保存并发布] / 2.3.4 交互效果表 + 状态流转 ASCII 图）；**2.4 编辑模板**（2.4.1 入口[强调 currentEditId 重置] / 2.4.2 与新建差异表 / 2.4.3 交互效果表）；**2.5 启用/停用**（2.5.1 入口[引用确认] / 2.5.2 禁用流程 / 2.5.3 启用流程 / 2.5.4 卡片联动 / 2.5.5 影响范围）。原 2.3 动画→**2.6**、2.4 异常→**2.7**、2.5 任务三段→**2.8**。
- 校验：PRD div 128/128 平衡；subsection-title 共 50 处；stage 子章节编号 2.3–2.8 连续正确；`全国` 仅 stage section 命中。

## 列表页三项完善（搜索/重置、左侧类型树筛选、同城启用重名校验）
- 用户要求对 `pc/pc-contract-template.html` 与 `pc/pc-stage-template.html` 两页补充：① 同名+同城市+启用态不可重复；② 搜索行缺搜索/重置按钮、无法搜索→补按钮并落地搜索逻辑；③ 点击左侧模板类型项右侧列表未筛选→落地点击筛选。并同步更新 PRD 文档。
- **Search/Reset 按钮 + 搜索逻辑（Task2）**：搜索行 input 加 `id=searchName`、城市下拉 `id=filterCity`（修复后）、状态下拉 `id=filterStatus`；新增「搜索」(`applyFilters()`)/「重置」(`resetFilters()`) 按钮；`applyFilters()` 按 名称模糊 / 城市精确 / 状态精确(启用=active·停用=inactive[合同]或disabled[阶段]) / 左侧类型树(active 节点 label，全部模板=不限) 四条件 AND 过滤表格行(合同)或卡片(阶段)，分页「共 N 条」实时刷新；`resetFilters()` 清空三条件并恢复「全部模板」选中。
- **左侧类型树点击筛选（Task3）**：原 click 监听仅切换 active 高亮、未过滤；现点击后调用 `applyFilters()`，按节点名称(合同取 `.template-category-tag`、阶段取卡片「类型：」meta) 精确过滤右侧列表，与顶部条件 AND 叠加。
- **同城启用重名校验（Task1）**：新增 `isDuplicateActiveName(name,city,excludeId)`（合同读 `templateData`、阶段读 `stageTemplateMeta`），仅对 `status===active` 且 同名(trim+小写)同城市 判定，排除自身(编辑)。`saveTemplate()`(发布) 与 `publishDraft()`/`publishStageDraft()`(草稿→启用) 均先校验，命中则阻断并提示「该城市下已存在同名的启用中模板，请勿重复创建」；草稿态不校验。
- **顺带修复 stage 重复 ID Bug**：阶段模板搜索城市 `<select id="templateCity">` 与新建弹窗城市 `<select id="templateCity">` 同文档 ID 冲突，导致弹窗 `getElementById('templateCity')` 误命中搜索框、城市从未正确读写。将搜索框改 `id=filterCity`（唯一化弹窗 `templateCity`），该 Bug 随之消失。
- **状态变更后重算筛选**：`persistTemplate/persistStageTemplate`(新增/发布) 与 `toggleStatus/toggleTemplateStatus`(启用停用) 末尾均调 `applyFilters()`，保证筛选态一致。
- **PRD 文档同步**：1.3 导语改为"已落地"并说明 AND/分页刷新；1.3.1 工具栏补"搜索/重置按钮"；1.3.3 count 说明补"点击节点即按类型过滤"；1.3.4 末条改为搜索/重置按钮触发；1.4.3 模板名称规则精确为"同一适用城市下、已存在启用态其他模板不允许重名(草稿不校验/编辑自身不计入)"；1.4.4/1.4.5 补发布后重算列表与重名阻断提示；阶段页新增 **2.2.1 列表页搜索与左侧类型筛选（落地说明）**；2.3.3/2.3.4 补同城启用重名规则与发布校验失败行。
- 校验：两 HTML div 平衡(98/98、169/169)、node `--check` JS 均 OK；新函数各定义 1 次；合同 `全部城市` 重复项已去(剩 1)；stage `templateCity` 唯一(弹窗)、`filterCity` 唯一(搜索)；PRD div 129/129 平衡。

## 同城启用重名校验失效 Bug 修复（种子数据缺 status 字段）
- 用户反馈：新建合同文本模板，城市相同+模板名称相同+启用状态，仍可创建成功（已存在相同模板应被拦截）。说明上一轮「同城启用重名」校验仅在运行时新建/编辑数据上生效，对**初始静态种子数据未生效**。
- 根因：上一轮 `isDuplicateActiveName` 比对的是 JS 数据对象的 `status==='active'`，但两页种子数据**根本没有 `status` 字段**（合同 `templateData` id1-5 渲染为启用行、id6 停用行，但对象内无 status；阶段 `stageTemplateMeta` 初始 `={}` 仅运行时填充，预渲染种子卡片从未注册进 meta）→ 校验恒为 false → 漏检。
- 修复：
  - `pc/pc-contract-template.html`：给 `templateData` 6 条种子补齐 `status`（id1-5=`active`、id6=`inactive`），与渲染态 `data-status` 对齐。
  - `pc/pc-stage-template.html`：新增 `registerSeedCards()`（加载时遍历 `.template-card[data-id]`，按 DOM 解析 名称/类型/城市/状态/data-refs 写入 `stageTemplateMeta[id]`），并在 `</script>` 前调用，使种子卡片参与重名/计数校验（顺带修复编辑种子卡片时回显为硬编码占位值的问题）。
- 验证：node 实测 `isDuplicateActiveName('水电班组服务合同模板','北京市',null)`→`true`（拦截）；不同城市/不同名/停用种子/编辑自身排除 均 `false`；两 HTML `node --check` OK；div 98/98、169/169 平衡；合同 status 字段 6 处(5 active+1 inactive)；stage `registerSeedCards` 定义+调用 2 处。
- 注：PRD 文档 1.4.3/2.3.3 重名规则说明本身准确（"同一适用城市下已存在启用态其他模板不允许重名"），无需改动，本次仅修复代码执行层。

## PRD 阶段任务模板需求说明细化（对齐合同模板颗粒度）
- 用户要求：参照合同文本模板页（1.x，精细到 1.3.1–1.3.5 / 1.4.1–1.4.5 五级）的风格与规格，将阶段任务模板页（2.x）的"阶段任务模板"需求说明细化，覆盖 列表、搜索筛选、新建模板、编辑模板、启用/禁用。
- 差距核实：合同章节把"列表页"单独拆 1.3.1–1.3.5 五级细化；阶段章节原仅有 2.2 页面结构 + 2.2.1 一个落地说明，缺平行"列表页"小节；新建/编辑/启用禁用已大致对齐。实际数据（grep 阶段页）：类型树「全部模板=18」、八类 count 3/2/2/3/2/2/2/2；`filterStatus` 选项「全部状态/启用/停用」对应 active/disabled；城市含「全国」。
- 改动（`_refine_prd.py` 落盘脚本执行，已 rm）：
  1. 旧 2.3–2.8 顺延为 2.4–2.9（逆向替换 `2.8→2.9`…`2.3→2.4` 防级联）；
  2. 删除旧 2.2.1 落地的 `<ul>`，改为简短 `.note` 指向新 2.3；
  3. 状态流转图提升为 **2.4.5 保存流程与状态流转**（对标 1.4.5：两种保存动作差异表 + ASCII 状态流转图 + 3 条 ul）；
  4. 插入全新 **2.3 列表页**（2.3.1 布局 code-block / 2.3.2 卡片字段表[名称·类型·城市·状态·阶段任务统计·更新时间·操作] / 2.3.3 左侧类型筛选[count 表 18/3/2/2/3/2/2/2/2 + 4 条 ul] / 2.3.4 搜索筛选匹配规则[名称模糊·城市精确含全国·状态 active/disabled 对应启用/停用] / 2.3.5 排序分页）；
  5. 交叉引用更新：2.4.2 引用改 `见 2.4.2 / 2.4.3`、2.4.3 引用改 `见 2.9`。
- 引发的 div 失衡与修复：插入后 div 148/146（净 +2）。定位两处多余未闭合 `<div>`：① 行 612 遗留 `<div class="subsection-title" style="font-size: 16px; margin-top: 20px;">`（删 2.2.1 时误留）；② 行 619 多余 `<div class="subsection">`（与 new_23 包裹 div 重复）。Edit 删除后 div **146/146 平衡**、depth 0、编号连续、交叉引用正确。
- 当前阶段章节结构：2.1 概述 / 2.2 页面结构(+note 指向 2.3) / 2.3 列表页(2.3.1–2.3.5) / 2.4 新建模板(2.4.1–2.4.5) / 2.5 编辑模板(2.5.1–2.5.3) / 2.6 启用停用(2.6.1–2.6.5) / 2.7 动画 / 2.8 异常 / 2.9 任务三段。

## 阶段任务模板页目录（左侧导航）补更新
- 用户反馈：上一轮细化阶段章节（2.1–2.9）后，左侧 `sidebar-toc` 中「二、阶段任务模板页」仍为单条 `<li>` 链接，未像「一、合同文本模板页」那样展开 2.1–2.9 嵌套子导航，且阶段各 subsection-title 均无 `id` 锚点（合同侧有 `s1-1`…`s1-9`）。
- 修复：① 给阶段 2.1–2.9 共 9 个 `subsection-title` 补 `id="s2-1"`…`id="s2-9"`；② 将 TOC 中「二、阶段任务模板页」单 `<li>` 改为 `<li class="toc-parent">` + 嵌套 `<ul class="sidebar-toc-sub">`（9 个子项 `href="#s2-X"`，文案对齐 2.1–2.9）。
- 滚动高亮脚本无需改：`tocLinks` 取 `.sidebar-toc-list a` 全集、按 `getElementById` 匹配，新增锚点自动纳入高亮与平滑滚动。
- 校验：div 146/146 平衡、depth 0；9 个 TOC 链接与 9 个 body 锚点 id 完全一致；`toc-parent`=2、`sidebar-toc-sub` 实际嵌套 ul=2。

## 阶段任务模板三项修正（按钮高度 / 阶段校验 / 新模板置顶）
- 用户反馈：①列表页搜索/重置按钮高度约为搜索项两倍（不协调）；②创建/编辑阶段任务模板缺少"至少一阶段"校验；③新增模板显示在列表最下方，与"创建时间倒序"排序规则矛盾。
- **按钮高度（CSS）**：`pc/pc-stage-template.html` 给 `.template-search` 加 `align-items:center`，并新增 `.template-search .pc-form-select{height:36px}` 与 `.template-search .pc-btn{height:36px;padding:0 16px}`，使搜索输入框/下拉/按钮统一 36px，消除按钮偏高。
- **阶段数据模型重构（核心）**：原弹窗阶段/任务用两套割裂模型（`stageData` 仅种子 + 裸 DOM），且 `persistStageTemplate` 从不保存阶段 → 新建模板阶段任务全部丢失。引入统一 `currentStages` 数组模型（`[{name,order,tasks:[{name,execStandard,confirmStandard,respStandard}]}]`），重写 `renderModalStages/selectStage/renderTaskList/addNewStage/deleteStage/showTaskEditModal/saveTask/editTaskItem/deleteTask`；`showCreateModal`/`editTemplate` 重置或回载（`meta.stages` 或种子 `stageData`），`persistStageTemplate` 将 `stages` 存入 `meta` 并计算真实 `stageCount/taskCount`，`buildStageCardHTML` 据此渲染阶段预览。删除旧 `renderStageList` 及旧 `selectStage/addNewStage/deleteStage`（避免重复定义）。
- **至少一阶段校验**：`saveTemplate`(发布) 与 `saveDraft`(草稿) 均新增 `currentStages.length===0` 拦截，提示"请至少添加一个阶段信息（阶段任务模板必须包含至少一个阶段）"。注：草稿也强制至少一阶段（按用户字面要求），如需草稿豁免可再调整。
- **新模板置顶**：`persistStageTemplate` 新建卡片插入由 `beforeend` 改为 `afterbegin`，最新创建模板置顶，符合创建/更新时间倒序。
- 校验：div 165/165 平衡（净变化因删除了旧 JS 模板字符串里的 `<div>` 计数，DOM 仍平衡）、depth 0；`node --check` JS OK；无 `renderStageList`/旧签名残留；`stageData` 仍在；`afterbegin` 与校验文案各就位。
- **PRD 同步**：2.3.5 默认排序补"新建/编辑模板插入列表最上方"；2.4.2 字段表「阶段任务」改必填+错误提示；2.4.3 校验表两行加"至少 1 个阶段"；2.4.4 交互表更新保存草稿/发布为置顶+持久化并新增"阶段不足校验"行；2.4.5 差异表校验强度/失败处理同步；2.5.3 编辑弹窗改为"由 meta.stages 重建回显阶段与任务"。PRD div 146/146 平衡。

## 阶段任务模板搜索/重置样式对齐合同模板列表
- 用户要求：阶段任务模板列表页的搜索与重置按钮样式"参考合同文本模板列表"。
- 现状：阶段页工具栏 markup 已与合同页一致（`.template-toolbar`→`.template-search`：input + 2 个 `.pc-form-select` + `pc-btn pc-btn-primary`(搜索)/`pc-btn pc-btn-default`(重置)），但 CSS 不一致——前一轮为修按钮偏高，给 `.template-search` 加了 `align-items:center` 并新增 `.template-search .pc-btn`/`.pc-form-select{height:36px}` 作用域覆盖，而合同页并无这些覆盖、靠 flex 默认 `stretch` 自然对齐。
- 修复：① 移除 `.template-search` 的 `align-items:center`（恢复与合同一致的默认 stretch）；② 删除 `.template-search .pc-btn`、`.template-search .pc-form-select` 两个作用域高度覆盖；③ 保留 input 36px 规则并补 `.template-search input:focus`（与合同页 focus 样式一致）；④ 阶段页 `filterCity` 原缺 `width:120px`（合同页有），补上使下拉宽度一致。
- 现在两页共用同一 `.pc-btn`/`.pc-form-select` 默认样式与同一 flex 拉伸行为，搜索输入框/城市·状态下拉/搜索·重置按钮高度与对齐完全一致（唯一有意差异：阶段页城市下拉多"全国"选项）。div 165/165 平衡不受影响。

## 阶段任务模板新建/编辑阶段名称输入不同步修复
- 用户反馈：在"新建阶段任务模板"弹窗右侧已填写阶段名称，保存时仍提示"请至少添加一个阶段信息"。截图显示左侧阶段列表为"共0个阶段"。
- 根因：上一轮重构引入 `currentStages` 统一模型后，阶段名称输入框与"按序执行"复选框**只读不回写**——仅由 `selectStage()` 从模型加载到 DOM，用户编辑时不会同步回 `currentStages`；且未点击左侧"+ 添加阶段"时模型为空，校验自然失败。
- 修复：
  1. 给 `#stageName` 加 `oninput="onStageNameInput()"`：当 `currentStageIndex<0` 且输入非空时，自动向 `currentStages` 插入首个阶段并刷新左侧列表；已有选中阶段时则实时回写名称。
  2. 给 `#stageOrder` 加 `onchange="onStageOrderChange()"`，实时回写当前阶段的 `order` 字段。
  3. 新增 `ensureStageFromInput()` 兜底：在 `saveTemplate`/`saveDraft` 的"至少一阶段"校验前调用，若模型仍为空但阶段名称输入框有内容，自动创建阶段。
- 校验：div 165/165 平衡、depth 0；`onStageNameInput`/`onStageOrderChange`/`ensureStageFromInput` 定义与调用数量一致；`node --check` JS OK。

## 合同文本模板新增置顶（创建时间倒序）修复 + PRD 同步
- 用户反馈：新增合同文本模板显示在列表最下方，与"排序规则按创建时间倒序"不符（与阶段模板此前问题类同）。
- 根因：`pc/pc-contract-template.html` 的 `persistTemplate()` 对新增行用 `templateTableBody.insertAdjacentHTML('beforeend', ...)`（追加到表格末尾）。`applyFilters()` 仅按 DOM 顺序切换 `display`，不做 JS 排序，故显示顺序即插入顺序——新增行天然落到最下方。
- 修复：将新增行插入由 `beforeend` 改为 `afterbegin`（置顶），最新创建模板出现在表格首行，符合"创建 / 更新时间倒序"（编辑现有行仍 `outerHTML` 原地替换，不挪位）。
- 校验：div 98/98 平衡、depth 0；`afterbegin` 计数 1、`beforeend` 残留 0；`node --check` JS OK。
- **PRD 同步**：① 1.3.5 默认排序补"新建保存的模板（保存草稿 / 保存并发布）直接插入列表最上方（创建 / 更新时间倒序），修正新增排在末尾"；② 1.4.4 交互效果「保存草稿」补"列表最上方新增一行（状态草稿）"、「保存并发布」由"列表新增一行"改为"列表最上方新增一行（状态默认启用）"。PRD div 146/146 平衡。

## PRD 补充「每个功能项关联原型页面」
- 用户要求：在文档内为每个功能项补充其对应的具体原型页面，不改变已有功能与描述。
- 实现：新增 CSS `.proto-ref`（浅蓝 chip）；用脚本在 18 个带 `id="s1-*" / "s2-*"` 的功能小节标题（1.1–1.9、2.1–2.9）后各插入一条「关联原型页面」链接 chip——一章节全部指向 `pc/pc-contract-template.html`，二章节全部指向 `pc/pc-stage-template.html`（含裸标题 2.4）。原有 info-grid「页面文件」、所有描述/表格均未改动。
- 校验：div 由 146→164（净 +18，每 chip 一开一闭），164/164 平衡、depth 0；`proto-ref` 计数 18；脚本临时文件已删除。

## PRD 原型关联修正（仅保留页面级功能 + 编辑链接纠正）
- 用户反馈两点：① 上一轮的「关联原型页面」加得"不一致"——应只加在**页面级功能**（列表页 / 新建模板页 / 编辑模板页），而非每个小节都加；② 编辑模板项当前关联的是列表页链接（错误），应关联**编辑模板的页面示例链接**。
- PRD 修正（脚本）：删除 概述/结构/启用停用/权限/动画/异常/任务三段 等非页面小节的 proto-ref（共移除 12 条），仅保留 1.3 列表页、1.4 新建模板、1.5 编辑模板、2.3/2.4/2.5 共 6 条；并把链接区分：列表页→裸文件名，新建模板→`#modal-create`，编辑模板→`#modal-edit`（锚文本也带 hash，视觉可区分）。校验：div 164→152，152/152 平衡、depth 0；6 条 chip 正确落位。
- 原型深链支持：在 `pc-contract-template.html` 与 `pc-stage-template.html` 末尾脚本各加 IIFE，读取 `location.hash`——`#modal-create` 调 `showCreateModal()`、`#modal-edit` 调 `editTemplate(1)`；仅带特定 hash 时触发，不改变默认打开行为（普通打开无 hash 不受影响）。
- 顺带修复阶段模板编辑种子卡片崩溃：`editTemplate(id)` 原 `currentStages = cloneStages(meta.stages)` 在种子 meta 无 `stages` 字段时 `undefined.map` 抛错（点击种子"编辑"亦会崩）。改为 `cloneStages((meta.stages && meta.stages.length) ? meta.stages : stageDataToStages())`，缺阶段时回退到示例阶段。校验：两文件 div 平衡、depth 0、`node --check` JS OK。
