# 2026-08-13 工作日志

## PRD-任务模块详细规格.html 重构
- 目录侧边栏（sidebar-toc-list）按「公共规格 / 服务方小程序 / 业主小程序」三分区，参考项目模块 PRD 的 toc-group-title 模式。
- 服务方各页「适用角色」字段统一改为「全部」（业主侧保持「业主」）。
- 第二章新增 2.7「任务状态详解（服务方全部任务列表）」：按 全部/待开始/执行中/待确认/已完成 五个 Tab 逐一说明卡片字段、角色操作、交互流程。

## 其他已完成的改动（当天早些时候）
- 合同金额脱敏权限控制：common.js 新增 ContractAmountMask；PC 端运营全权限展示，服务方 contract-detail.html 原型导航底部加权限切换演示。
- 服务方合同详情（合规版）原型导航：取消多个状态切换入口（确认中待确认方/已确认甲方/拟定中甲方/平台驳回已修改/变更中待确认方/变更确认中待确认方），分组计数同步更新。
- worker-contract-receive-inviting.html 接单体验页：拒绝/接单按钮跳转修正，global→window，status/viewer URL 参数支持。
- task-detail.html 修复三处破损 `<div<div>` 标签导致弹窗溢出手机框。

## PRD 文档链接化改造（页面路径 + 交叉引用）
- 任务/项目模块 PRD 的 section-title badge（原型文件路径）由纯文本改为可点击链接（<a href target=_blank>），新增 .badge a / a.proto-link CSS。
- 任务 PRD「页面路径」字段中 .html 文件路径改为 proto-link 链接。
- 合同/项目 PRD 文档内「详见 §X.Y / X.Y / 第X章 Y.Z」类引用按 sec-X-Y 锚点逐一校验后转为可点击链接（合同12处、项目16处）。
- 合同 PRD 与 PC 端 PRD 原本已有 badge/file-link 链接，未重复处理。

## PRD 标题badge与页面路径合并（去重）
- 规则：章节同时有 badge + 页面路径/文件字段时，删除 badge、把原型文件路径合并进页面路径（链接）；仅有 badge 无路径字段的章节保留 badge。
- 任务/合同/项目三份 PRD 已处理；无页面路径字段的章节（合同五/七/八章、任务六章）badge 保留；字段校验/API/状态机/全局规格等非页面章节无文件引用。

## PRD §20.6 待办枚举统一「合同审核」命名（contract_review → contract_audit）
- 用户确认此前提出的遗留观察：§20.6 待办事项表 `todos.type` 枚举用 `contract_review（合同审核）`，与本轮刚确立的 §20.5 动态枚举 `contract_audit（合同审核）` 同为"合同审核"却 key 不一致。
- **核查硬编码依赖**：全仓 Grep `contract_review` 仅出现于 PRD 文档正文与 .bak，**无任何原型 JS 以此为字面量** → 改文档不会让原型失配。
- **修订**：PRD-项目模块详细规格.html line 6912 `todos.type` 枚举由 `task_confirm（任务确认）、contract_review（合同审核）、statement_confirm（对账单确认）` 改为 `task_confirm（任务确认）、contract_audit（合同审核）、statement_confirm（对账单确认）`。
- **对齐结果**：`contract_audit（合同审核）` 与 `task_confirm（任务确认）` 在 §20.5（动态）与 §20.6（待办）两字段现已一致；`statement_confirm（对账单确认）` 仅留 §20.6（对账单确认作为动态类型此前已删除，但作为待办类型仍合理，保留）。
- **校验**：Active PRD Grep `contract_review` 无残留（仅 .bak 历史备份保留）。
- **更正（上一轮误判）**：上述"第三套待办词汇"实为 `技术规格补充文档.md` **§1.15 消息表（messages）的 `type` 字段枚举**，并非待办表（该文档无 todos 表，全文 Grep 待办/todos 无匹配）。即 messages.type 与本 PRD §20.6 todos.type 是两回事。
- **用户决策（2026-08-13）**："与原型保持一致，将文档修改" → 对齐 `技术规格补充文档.md` 的 messages.type 枚举到原型消息中心实测消息 `type`。
- **原型实测消息 type**：service-miniapp/message.html = `invite`（邀请）/ `task`（任务通知）/ `system`（系统消息）/ `contract`（合同邀约，service 端专属 Tab）；owner-miniapp/message.html = `invite`/`task`/`system`（无 contract，与记忆一致）。
- **修订**：技术规格补充文档.md line 339 messages.type 枚举由 `contract_pending`/`contract_reviewed`/`task_pending`/`task_rejected`/`statement_pending`/`statement_rejected`/`change_pending`/`system` 改为 `invite（邀请）、task（任务通知）、system（系统消息）、contract（合同邀约，仅服务方小程序存在）`。
- **校验**：Grep 文档全文旧值已清除（仅 line 428-439 的 `change_pending_confirm`/`changing` 为「合同变更状态机」值、与本 messages.type 字段无关，正确未动）；messages.type 新枚举已 Read 确认落盘。
- **未动说明**：同表 `link_type` 枚举（contract/task/statement/project/change）描述消息关联业务对象，与原型消息链接行为一致，属独立字段，本轮未改。
- 改动文件：`PRD-项目模块详细规格.html`、`技术规格补充文档.md`。

## 全部任务页补「待配置」筛选项 + 已驳回回流「待开始」展示效果
- 用户指令：全部任务页面状态筛选项缺少「待配置」，需补充并包含点击筛选到匹配数据的效果；列表缺少确认人「已驳回」对应「待开始」状态的任务展示效果，需补充。
- **原型改动（service-miniapp/task-list.html + owner-miniapp/task-list.html 镜像同步）**：
  - 状态筛选下拉补「待配置」项；contract-1 头部任务数「12→13」+ 待配置徽标；
  - 新增 `.stat-badge.config` / `.task-status.config` / `.task-status-text.config`（灰色虚线样式）/ `.task-tag.rejected`（红色已驳回标签）CSS；
  - 收尾阶段加 `data-status="config"` 任务「智能家居设备预配置」；贴砖阶段加 `data-status="pending"` 且带「已驳回」标签的任务「墙砖空鼓复检」+「已驳回·需重新执行」进度提示；
  - 修复 `filterTasks()` 引用不存在的 `totalCount/pendingCount/confirmingCount/completedCount` 元素导致的 JS 报错；
  - **关键闭环**：默认折叠阶段会导致筛选后匹配任务不可见，新增 `snapshotExpandState`/`expandAncestors`/`restoreExpandState` 函数，实现筛选命中时自动展开祖先 `.task-list` / `.stage-group`、无筛选时还原默认折叠态。
- **PRD 同步**：`PRD-任务模块详细规格.html` §2.7 加入「待配置」明细行、将「待确认」统一为「确认中」、明确已驳回任务归入「待开始」Tab 并展示「已驳回」标识。
- 改动文件：`service-miniapp/task-list.html`、`owner-miniapp/task-list.html`、`PRD-任务模块详细规格.html`。

## 全部任务页合同头部布局优化
- 用户反馈：截图显示合同头部单行 flex 导致图标/名称/状态徽标/箭头拥挤撑开，不美观。
- **优化方案**：将 `.contract-header` 从单行 flex 改为上下两行布局——上行：图标 + 合同名称 + 折叠箭头；下行：合同元信息（左）+ 状态徽标（右）。
- **实现**：
  - CSS：`.contract-header` 改为 `align-items: flex-start`；新增 `.contract-main` 容器（`flex:1; min-width:0`）；新增 `.contract-footer-row`（`justify-content: space-between; flex-wrap: wrap`）；`.contract-stats` 改为 `flex-wrap: wrap`、徽标 `white-space: nowrap`；箭头设 `margin-left:8px; flex-shrink:0`。
  - HTML：4 个 `.contract-header` 结构统一由「icon + contract-info + stats + arrow」改为「icon + contract-main(name + footer-row(meta + stats)) + arrow」，删除 `.contract-info` 容器。
- 改动文件：`service-miniapp/task-list.html`、`owner-miniapp/task-list.html`。

## 创建项目页「发起人」机制下线（原型 + PRD 同步）
- 用户指令：根据目前原型（service 端 create-project.html 已无任何发起人内容，即"已取消发起人"的当前状态），将文档内对应发起人描述删除，**不保留版本记录相关描述**，不影响其他已有功能/描述。
- **原型对齐**：owner-miniapp/create-project.html 此前仍残留发起人块，本次删除以对齐 service 端——
  - HTML：标题内 `.promoter-badge`（项目发起人角标）、`.page-content` 顶部 `.promoter-banner`（分利 Banner）、`.promoter-tip`（已认证提示条）整段删除；
  - CSS：`.promoter-banner*`/`.promoter-badge*`/`.promoter-tip*` 全部样式删除；
  - JS：`isPromoter`/`checkPromoterStatus`/`showPromoterStatus`/`goToPromoterAuth` 函数及 `checkPromoterStatus()` 调用删除。
  - 校验：owner 端 Grep `发起人|promoter|分利|is_promoter` 零残留，与 service 端一致；表单/创建/跳转逻辑未受影响。
- **PRD 删除+重排**（PRD-项目模块详细规格.html）：
  - TOC（line 371 区）删除 `5.3 项目发起人机制` 条目；
  - 正文删除整节 `5.3 项目发起人机制`（定义/标识规则/数据来源/认证入口/认证流程/权益说明 + 注意框）；
  - 原 5.4-5.7 顺序重排为 **5.3 创建流程 / 5.4 提交校验与二次确认 / 5.5 创建成功后跳转规则 / 5.6 创建成功后系统生成信息**（id 同步 sub-5-3~sub-5-6）；
  - 2.2 数据展示规则「自己创建的项目」描述由"当前用户作为项目发起人创建的项目"改为"当前用户创建的项目"（去发起人措辞）；
  - 修复两处旧交叉引用"创建项目 5.5"→"创建项目 5.4"（提交校验章节现编号）；
  - 校验：Active PRD Grep `发起人|分利|is_promoter` 零残留，章节编号连续无空缺；`.bak` 历史备份不动。
- **未动范围**：promoter-auth.html（独立"发起认证-开发中"页，用户仅点名创建项目页内发起人描述，未要求删该独立页）、PC/合同模块/其它文档的"发起人"（合同变更发起人指操作人，非同一概念，非本次范围）。

## 删除文档内「临时任务」相关描述（请求 B）
- 用户指令：原型内已无"临时任务"功能，将文档内所有与"临时任务"相关的描述删除，**不保留版本记录说明**；并澄清任务状态语义——已驳回不是任务主状态，而是"确认中"状态的一个结果：确认人全部通过→已完成；有一人驳回→回退到待开始（状态回退效果，但记录留存）。
- **PRD-任务模块详细规格.html**（已完成于前段）：
  - 任务类型 label 由"正式任务（橙色）/ 临时任务（紫色）"改为"正式任务（橙色）"；
  - 待办表删除"临时任务接受"行，已完成任务行标签"合约/临时"→"合约"；
  - 任务编号格式删去"（临时任务前缀TK-T）"；
  - 校验：Grep `临时任务|TK-T` 零残留。
- **技术规格补充文档.md**（本段完成）：
  - 字段层：`tasks` 表删 `is_temporary` 行；`stage_id` 说明删"（临时任务为null）"；
  - 规则层：删"前置依赖-临时任务无限制"；RBAC 矩阵删"创建临时任务"行；补充说明"不能创建项目、合同或临时任务"→"不能创建项目、合同"；API 清单删 `POST /tasks/temporary`；架构关联删"临时任务可不关联架构节点"；9.2 角色权限表删项目经理/工长列中的"临时任务"字样；
  - 删除整章 **## 13. 临时任务定义**（13.1 概念 / 13.2 与合同阶段任务区别 / 13.3 创建规则）；
  - TOC 删除"13. 临时任务定义"条目，原 14-17 章重排为 **13 活动列表定义 / 14 对账单财务关联规则 / 15 评价体系设计 / 16 异常场景与边界条件**（含 13.x/14.x/15.x/16.x 子节号与 TOC 锚点同步）；
  - 校验：Grep `临时任务|TK-T|is_temporary` 零残留。
- **数据库ER图与索引设计方案.md**（本段完成）：
  - 全局 ER 图 `tasks` 实体删 `Boolean is_temporary` 字段，`stage_id` 注释删"（临时任务为null）"；
  - 关系行 `stages ||--o{ tasks : "stage_id(临时任务可为null)"` → `stage_id`；
  - 分域 ER 图（1.2.4 任务域）`tasks` 实体删 `Boolean is_temporary`；
  - 外键汇总表：`tasks.stage_id` 说明删"临时任务可为null"，`tasks.contract_id` 说明"（可为null(临时任务)）"→"—"；
  - 特殊关系说明表删"tasks.stage_id 可空（临时任务…）/ tasks.contract_id 可空（临时任务可不关联合同）"两行；
  - 校验：Grep 同上零残留。
- **整体校验**：全仓 *.md/*.html Grep `临时任务|TK-T|is_temporary` 零残留（仅 .bak 历史备份保留，按要求不动）。
- **状态语义说明落点**：用户澄清的"已驳回=确认中结果、回退待开始、记录留存"语义在原型 task-list/detail 已实现（被驳回任务归入「待开始」Tab + 额外"已驳回·需重新执行"标识），PRD-任务模块 §一 状态定义表保留 `rejected` 行与其一致，未做删除；本次仅清理临时任务，不改主状态机。

## 「合约」与「合同」类型标签统一（任务模块 PRD）
- 用户质疑：类型标签中"合约（蓝色）"与"合同（青色）"是否重复。核查结论：**无功能区别**——两者都指合同相关待办；原型（service-miniapp/todo-list.html）所有合同类待办统一用"合同"标签，**从未使用"合约"标签**，PRD 此前把同一类待办拆成两个标签属文档内部不一致。
- 用户决策：无区别则"保留合同即可"。本次统一为"合同"：
  - 类型标签枚举（line 1323）删"合约（蓝色）、"，保留 合同（青色）/变更（橙色）/审核（青色）/架构（橙色）；
  - 待办类型表 标签列 3 处「合约」→「合同」；
  - 待办类型名称「合约任务确认/合约任务执行」→「合同任务确认/合同任务执行」（用户选"一并重命名"）；
  - 校验：PRD-任务模块 Grep `合约` 零残留。
- **未动范围**：合同模块 PRD 的"合约库"（工人合同存储）、"合约方"（缔约方）为正常领域名词、非类型标签，保持不动；原型 .html/.js 中"合约库/合约方"亦为名词用法，未改。

## 任务状态语义修正（已驳回=确认结果，非主状态）
- 用户明确需求：① 已驳回 **不是任务主状态**，而是「确认中」状态的一种**确认结果**；② 所有确认人通过 → 确认结果=已确认 → 主状态变为 已完成；③ 任一确认人驳回 → 确认结果=已驳回 → 主状态**回退为 待开始**（状态回退效果），但确认记录（结果=已驳回、驳回原因）留存；④ 被驳回任务在主状态维度归 待开始，列表以"已驳回·需重新执行"标识呈现。
- 修改 PRD-任务模块详细规格.html（与既有 2.7 过滤逻辑"已驳回任务归入待开始Tab"保持一致，消除文档内部矛盾）：
  - §一 任务状态机：新增回退流程箭头（确认中 ↩ 任一确认人驳回→待开始）；状态定义表**删除 `rejected 已驳回` 主状态行**，主状态收敛为 待配置/待开始/执行中/确认中/已完成 5 个；新增「确认结果与主状态映射」小节（confirmed→已完成 / rejected→待开始回退，并注明非新增独立主状态）；重写「驳回流程说明」便签（已驳回是确认结果、主状态回退待开始、记录留存）。
  - §2.7 确认中行：原"任一驳回则任务变为已驳回，在列表中归入待开始"→"任一确认人驳回则确认结果记为已驳回，任务主状态回退为待开始（状态回退效果），在待开始Tab以'已驳回·需重新执行'标识呈现"。
  - §3.6 驳回后：由"任务状态变为已驳回"→"主状态由确认中回退为待开始（状态回退效果）；确认记录记为已驳回留存原因，待开始Tab以'已驳回·需重新执行'标识呈现"。
  - §3.7 操作按钮矩阵：删除 `已驳回` 独立状态行；待开始行执行人视角由"开始执行"→"开始执行（被驳回回退后即为重新执行）"；矩阵后加注说明被驳回任务主状态归待开始、以标识呈现、操作同为开始执行。
  - §10.3 确认记录表：枚举 confirmed/rejected 保留（属确认结果），并加"结果枚举说明"注：该 status 为单条确认记录结果、非任务主状态。
- 校验：全文档 Grep 已驳回 仅剩"确认结果/标识/对账单状态"语义，无把已驳回当任务主状态的写法；对账单 1598 行"已驳回"属对账单状态域，非任务，按要求不动。
- **影响评估**：仅改文档表述，**未改动原型代码**（原型 task-list/detail 已是"被驳回归待开始Tab+已驳回标识"实现，文档现与之一致）。

## 全部任务页合同头部状态徽标改进度条（方案C）
- 用户选定方案C：移除合同头部（contract-header）下方的零散状态徽标（待配置/待开始/确认中），仅保留 meta 总数 + 整体完成进度条（已完成 x/y），去重最简洁。
- **原型改动（service + owner 镜像）**：
  - CSS：删除 `.contract-stats` / `.stat-badge*` 全部样式；新增 `.contract-progress`（进度条容器，flex:1 1 120px）、`.progress-track`（灰底轨道）、`.progress-fill`（绿色填充，width 由 JS 设置）、`.progress-text`（已完成 x/y）。
  - HTML：4 个 contract-header 的 `.contract-stats` 块替换为 `.contract-progress`（含空 progress-fill + "已完成 0/0" 占位）。
  - JS：新增 `renderContractProgress()`，遍历每个 `.contract-section` 内 `.task-item`，按 `dataset.status==='completed'` 统计已完成/总数，计算百分比并写入进度条；`DOMContentLoaded` 中于 `checkSequentialTasks()` 后调用一次（基于全部任务 DOM，不受筛选/分组切换影响，因 changeGroupBy 不移动 task-item）。
- **PRD 同步**：`PRD-任务模块详细规格.html` §2.2 结构 mock「合同卡片头部」由"项目名称 / 待处理·确认中数量统计"改为"元信息：工作组·阶段数·任务总数 / 整体完成进度条（已完成 x/y）"。
- 校验：两端 Node vm.Script JS 校验通过；Grep `contract-stats|stat-badge` 零残留。
- 改动文件：`service-miniapp/task-list.html`、`owner-miniapp/task-list.html`、`PRD-任务模块详细规格.html`。

## 任务详情页「配置人视角」按状态/端取消（原型）
- 用户指令：① 业主方 task-detail 各状态取消「配置人视角」（业主不支持配置任务）；② 服务方 task-detail 在 确认中/已完成/确认中(被驳回后)/驳回后待开始/已完成(含驳回) 5 状态取消「配置人视角」导航项及页面（此时不支持配置任务）；③ 仅取消导航项及页面，其他页面/状态/功能不受影响（待配置/待开始/执行中/前置阻塞 及 配置中切换条 仍保留配置人视角）。
- **实现（service/owner 两份 task-detail.js 镜像，仅 OWNER_SIDE 常量不同）**：
  - 新增 `OWNER_SIDE`（service=false / owner=true）与 `getConfigurerViewVisible(status)`：`OWNER_SIDE` 时两切换条均 false；service 时 CONFIGURING→configuring 条 true、PENDING/PENDING_BLOCKED/IN_PROGRESS→roleSwitcher 条 true、其余 5 状态均 false。
  - `updateTaskStatus` 顶部：若 `currentUserRole==='configurer'` 且当前状态/端不支持配置，则回退默认视角（确认类→confirmer、执行类→executor、其余→other），避免渲染配置页内容。
  - `updateTaskStatus` 末尾：按 cfgView 对 `#roleSwitcher` 与 `#configuringRoleSwitcher` 内文本为「配置人视角」的 `.role-switch-item` 设 display ''/none（仅隐藏 DOM，不删除，保留其他状态/页面的配置人视角）。
- **校验**：两份 JS Node vm.Script 语法通过；模拟 getConfigurerViewVisible 矩阵符合指令（service 5 状态 false、owner 全 false）；Grep 确认「配置人视角」仅存于 task-detail（活文件）+ .bak 历史备份；其他页面的该视角未受影响。
- 改动文件：`service-miniapp/js/task-detail.js`、`owner-miniapp/js/task-detail.js`。

## PRD「全部任务」页面结构与原型一致性核对（最新指令）
- 用户指出：PRD-任务模块详细规格.html 内「全部任务」页结构与当前原型不同（① 文档有"合同筛选下拉框"但原型无此控件；② 文档缺"我是否参与"筛选项；③ 卡片/筛选规格需核对；④ 文档缺失列表排序规则）。要求一切以当前原型为准更新文档。
- **原型真实结构（service/owner 双端一致）**：筛选栏 = 搜索框（任务名称）+「参与」分段切换（全部/我参与，data-filter=all/mine）+「状态」筛选下拉（全部状态/待配置/待开始/执行中/确认中/已完成，data-status）；**无合同筛选下拉框**。卡片列表：任务名称 / 执行人·确认人 / 任务状态（图标+文字），无"任务类型""所属阶段"标签。
- **PRD 逐项修正**（PRD-任务模块详细规格.html）：
  - §2.2 结构 mock：状态栏「Tab 角标」→「状态筛选下拉框（展开单选）」；筛选栏「合同筛选+任务状态+搜索」→「我是否参与(全部/我参与) + 任务状态筛选 + 搜索」；卡片「任务名称+类型标签 / 所属阶段 / 执行人确认人 / 状态标签」→「任务名称 / 执行人确认人 / 任务状态(图标+文字)」。
  - §2.3 标题「状态Tab规格」→「状态筛选规格」；保留 Tab名称/筛选条件/角标列；新增备注：原型以状态筛选下拉呈现（非顶部Tab），下拉暂未渲染角标，数量语义为设计预期。
  - §2.4 任务卡片规格：删「任务类型」行、「所属阶段」行；确认人显示规则由"显示X人点击查看列表"→"显示姓名列表，如 李四、王五"。
  - §2.5 筛选规格：删「合同筛选」行；新增「我是否参与」行（分段切换：全部/我参与，仅显示当前用户为执行人或确认人的任务）；表后新增「列表排序与陈列规则」段落（顶层按合同分组→合同内按阶段→阶段内按录入序；分组视图切换仅改聚合维度不改单任务顺序；被驳回回退待开始任务仍按原阶段顺序陈列仅叠加标识）。
  - §2.7 任务状态详解：note 与 thead「Tab」→「状态筛选」；6 行「卡片展示字段」列全部校正（去所属阶段/截止时间/状态badge，统一为"任务状态"并补执行人/确认人）；filter-logic「Tab切换时/全部Tab/其他Tab/待开始Tab/状态badge」→「状态筛选切换时/全部状态/其他状态筛选项/待开始状态筛选/任务状态」。
- **校验结论**：Grep `合同筛选|状态badge` 零残留；`所属阶段` 仅存于任务详情页（非本次列表范围，保留）；`Tab角标` 仅存 §8/§9 待办模块（非本次范围，保留）；业主端原型 owner-miniapp/task-list.html 同样含"我参与"+状态下拉、无合同筛选，文档两端通用准确。
- **既有差异待确认（超出本轮范围）**：① PRD §2.3/§2.7 状态枚举用 configuring/in_progress 别名，而原型 data-status 实际值 config/executing；② §2.3 仍保留"数量角标"设计预期（原型下拉暂未渲染）。
- 改动文件：`PRD-任务模块详细规格.html`。

## 目录二级标题可点击+选中高亮 + 任务详情页按状态分视角详述
- **需求**：① 将 PRD-任务模块文档的二级标题加入左侧目录，支持点击跳转且点击/滚动时有选中高亮样式；② 依据现有原型 task-detail 页面（service/owner 同构），按每个状态页面结合用户视角，单独详细写逻辑/权限/效果/字段/交互。
- **目录改造（已完成，#40）**：用一次性脚本给全部 44 个二级标题加锚点 id（sub-N-N / sub-xN），目录插入二级可点击条目（`.toc-sub` 缩进样式 `.sidebar-toc-list .toc-sub a` + `.active`），末尾脚本改写为通用版（`querySelectorAll('.sidebar-toc-list a')` 统一处理一/二级：滚动高亮 active + 点击平滑滚动并加 active）。**注意：该 toc_update.py 非幂等（会对已含 id 的二级标题再注入一次 id 造成重复属性），禁止二次运行**；后续新增章节须手动加 id + 目录条目。
- **状态分视角详述（已完成，#41/#42）**：新增「十二、任务详情页按状态分视角详述」章节（id=task-detail-by-status），10 个二级标题（sub-status-1..10）。内容依据 service-miniapp/js/task-detail.js + owner-miniapp/js/task-detail.js 实际渲染逻辑撰写：
  - 覆盖 9 个原型渲染态：待配置 / 待开始 / 待开始（前置阻塞）/ 执行中 / 确认中 / 确认中（被驳回后）/ 已完成 / 已完成（含驳回）/ 驳回后待开始；明确前三者为「确认中」衍生结果态、非 §一 主状态机独立主状态（主状态机仅 5 个）。
  - 四类视角：配置人(configurer)/执行人(executor)/确认人(confirmer)/业主（作为确认人之一，演示"王五·业主"）；含角色切换器机制说明。
  - 每态按 逻辑/权限与用户视角(表格)/字段/交互/效果 五维展开；含演示数据（张三·项目总已确认、王五·业主待确认/已驳回、赵六·设计师待确认；驳回原因"部分线路走向与设计图纸不符"）。
  - 关键行为已与代码对齐：配置人可在 待配置/待开始/执行中 增删确认人、改派执行人；执行人仅 待开始/执行中/驳回后待开始 可上传+提交；确认人仅 确认中/确认中（被驳回后）有「驳回/确认通过」；业主确认后可「去评价」；流程条进度(0/25/50/100%)、头部配色类(in-progress/confirming/completed)、历史记录半透明+「已驳回」红标等均按代码描述。
- **校验**：sub-status-1..10 在正文 id 唯一(各 1 次)；目录 href 与正文 id 一致；脚本通用处理新条目。
- 改动文件：`PRD-任务模块详细规格.html`。

## 拆分「十二章按状态分视角详述」至服务方/业主方各自任务详情页（最新指令）
- **需求**：用户指出上一轮新增的「十二、任务详情页按状态分视角详述」应分别落入「三、任务详情页（服务方）」与「六、任务详情页（业主）」两页内单独呈现；且业主端**只有业主视角、不支持配置任务**，与「2.7 任务状态详解」中待配置态的「合同甲乙方及合同创建者可配置」角色操作不同，需在文档内说明（产品设计特殊约定）。
- **实现（一次性脚本 split_ch12.py，非幂等，存临时目录）**：
  - 从末尾抽取原 chapter12 块（id=task-detail-by-status），重编号为服务方 3.9（导语/状态模型与角色视角）~ 3.19（驳回后待开始），迁入「三」章节结尾；保留原四视角（配置人/执行人/确认人/业主-as-confirmer，因服务端有角色切换器）。
  - 业主端**新写** 6.3（导语+「业主不支持配置」说明便签，明确对比 2.7 待配置角色操作）~ 6.12（驳回后待开始），均为**业主单一视角**（只读为主，仅确认中可确认/驳回、完成后可评价），迁入「六」章节结尾。
  - 目录：删除原「状态分视角」TOC 分组（含 12 章 11 条），在「三」后追加 sub-3-9..3-19、「六」后追加 sub-6-3..6-12；两个「公共规格」分组为文档原有结构（非本次新增）。
- **踩坑与修复**：
  1. 标题重复：sub-status-* → sub-3-* 替换只换了开头标签未换原标题文本，导致 3.10~3.19 标题文本重复两遍 → fix_headings.py 逐条还原为单一正确文本。
  2. 多出一个孤儿 `</div>`：插入 anchor 已含章节闭合 `</div>`，脚本又追加一个，导致块跑到章节外且 div 失衡 → fix_divs.py 移除每个块前「提前闭合」的那个 `</div>`，使 3.9~/6.3~ 正确落在各自章节内。
- **校验**：`<div`/`</div>` 平衡 314/314（diff=0）；TOC 全部 href 命中正文 id；新 sub-3/sub-6 id 全部存在；无重复标题；无残留 `sub-status-*`/`task-detail-by-status`/`状态分视角` group。
- 改动文件：`PRD-任务模块详细规格.html`。

## 目录修复：英文清理 + 章节重排 + 收起展开（最新指令）
- **需求**：① 目录二级标题带英文后缀（如 `3.11 待配置（CONFIGURING）`、`10.2 执行记录表字段（execution_records）`）是否必要、无意义则删；② 一级目录排序错乱（存在"四下面是八"）；③ 一级目录支持收起/展开、初次默认收起。
- **英文清理**：括号英文为原型状态枚举常量名（CONFIGURING/PENDING/IN_PROGRESS/CONFIRMING/COMPLETED/REJECTED_PENDING 及 PENDING_BLOCKED/CONFIRMING_AFTER_REJECT/COMPLETED_WITH_REJECT）与数据库表名（execution_records/task_confirmations），对 PRD 产品叙述属冗余。已全局移除状态枚举英文（TOC + 正文章节标题）；表名英文仅从 TOC 导航标签移除、正文 10.2/10.3 字段标题保留（该章节本就以表为描述对象）。
- **章节重排（renumber_prd.py，非幂等，存临时目录）**：原编号跨组不连续——服务方 二三四八、业主 五六七八九。重排为全局连续：服务方 二三四**五**（待办页 八→五）、业主 **六七八九**（全部任务 五→六、详情 六→七、验收 七→八、待办 九不变）。同步更新章节标题、anchor id（sub-5..8 四循环用 sub-T 临时占位打破）、小节号前缀（5./6./7./8. 同法）。**踩坑**：正文标题后缀为「（服务方小程序）/（业主小程序）」而 TOC 为「（服务方）/（业主）」，整串替换漏掉正文标题 → 补 4 处正文标题 Edit + 2 处 HTML 注释；并重排 body 物理顺序（reorder_body.py 用平衡 div 搬运 service 待办 section 至 四 之后、**九** 之前），使正文阅读顺序亦为一→十一连续。
- **收起展开**：CSS 新增 `.toc-toggle`（▸/▾ 指示器）；重写末尾脚本——每个一级章节收集其下 `.toc-sub` 并插入折叠指示器，点击指示器仅切换、点击章节标题展开自身再跳转、分组标题可整体折叠其下章节；**初次打开默认全部收起**；滚动定位到某子目录时自动展开所属章节；选中高亮（`.active` 绿底）沿用。node --check 语法通过，div 平衡 0。
- **校验**：TOC 一级顺序 一→十一 连续（四下紧跟五）；TOC 76 锚点全命中正文；状态枚举英文 0 残留；正文章节号与小节号前缀对齐无 MISMATCH；div 平衡 0。
- 改动文件：`PRD-任务模块详细规格.html`。

## 目录折叠 bug 修复（一级标题点击无法收起）
- **现象**：一级章节标题展开后再次点击无法收起（一、二、三…）。根因：原脚本中章节标题 `<a>` 的点击处理只有「若折叠则展开」分支（点击标题文本只会展开、从不折叠），而折叠指示器 ▸/▾ 虽能切换但体积小，用户实际点的是标题文本。
- **修复（PRD-任务模块详细规格.html 末尾脚本）**：① 通用链接循环对一级章节标题 `<a>` 跳过（避免折叠时又被滚动）；② 章节标题点击改为**切换**（折叠⇄展开），展开时同步平滑跳转章节、收起时不滚动；③ 折叠指示器 handler 保留 stopPropagation，与标题 handler 互不双触发。分组标题折叠逻辑不变。
- 校验：脚本逻辑自洽（箭头 stopPropagation 防冒泡到标题、章节标题 stopPropagation 防与通用链接重复滚动）；不影响二级目录点击/滚动高亮与默认收起。

## PRD 任务模块 3.3–3.6 规范更新（最新指令）
- **3.3 人员配置规格**：执行人/确认人「配置权限」由"项目经理、施工组长"改为"合同甲乙方及合同创建者（服务方）"；「修改权限」由"提交前可修改"改为"执行人执行任务前可修改"（用户仅明示执行人配置权限，确认人配置权限按同一授权范围同步，保持表格一致）。
- **3.4 三段标准显示规格**：三行「显示方式」统一改为"列表形式，每条编号（具体由模板内配置格式决定，非模板内任务由创建人添加结果决定）"；「来源」统一改为"引用模板或合同编辑时定义"。
- **3.5 执行记录上传规格**：补充"支持的格式"——原型上传 accept=image/*,video/*,audio/*（不限定具体扩展名），已显式列出常见格式（JPG/PNG/WEBP、MP4/MOV、MP3/M4A/WAV）并加 note 说明以系统选择器为准；数量/大小/时长限制维持原表。
- **3.6 确认通过/驳回规格**：原"属性/值"两列表重构为「字段/是否必填/校验规则/校验不通过提示/说明」五列；新增「操作流程说明」「状态流转（通过/驳回）」「相关记录生成与动态生成」三小节。关键对齐原型：驳回原因 必填、非空且≤500字（shared/validation.js textLength 实参 500，校正原文档"200字"），校验提示"请输入驳回原因"/"驳回原因不能超过500个字符"；确认弹窗内 附件/评语/综合评星/评价标签 均为选填，原型未做硬性校验（评语软上限200字、综合评星半星仅在逐确认人评价环节强制并提示"请至少选择半颗星进行评价"）。
- 校验：目录锚点 sub-3-3~sub-3-6 未变，TOC 正常；div 结构平衡。
- 改动文件：`PRD-任务模块详细规格.html`。

## 新增：无确认人快捷完成（状态流转场景）文档化
- 用户要求：当任务不配置确认人时，执行人提交执行记录后状态直接为「已完成」（跳过「确认中」），该场景及需求说明补到文档含任务状态机位置。
- 改动 `PRD-任务模块详细规格.html` 三处：
  1. **一、任务状态机**：① 主流程图后新增分支流程图「执行中 ⇒ 未配置确认人（快捷完成）⇒ 已完成（跳过确认中）」；② 状态定义表「已完成」进入条件补"未配置确认人时执行人提交执行记录后（快捷完成）"；③ 新增子节 `sub-x3`「无确认人快捷完成（状态流转场景）」含触发条件/状态流转/可执行跳过操作/记录生成 四列表 + note（明确其为「执行中→已完成」条件分支，非新增主状态）。
  2. **§3.3 人员配置规格**：表格后加 note「确认人为空时的状态流转」（确认人选填，空则提交即直接完成）。
  3. **§3.6 确认通过/驳回规格**：状态流转后加 note「无确认人场景（本节操作不适用）」（无确认人时确认/驳回操作及必填校验均不适用）。
- **重要待办（超出本轮文档范围）**：原型 `service-miniapp/js/task-detail.js` 的 `submitExecution()`（约 677 行）目前**总是**转 `CONFIRMING`，尚未实现"确认人为空 → 直接 COMPLETED"分支；该需求仅文档化，原型落地待用户确认后实施（两端 task-detail.js 均需改 + owner 端同理）。
- 改动文件：`PRD-任务模块详细规格.html`。

## 删除「任务验收页」两章（四服务方 / 八业主）+ 章节连续重排
- **判定（用户疑问）**：四/八两章内容是否已在任务详情页内详述？结论：**是，且属于重复/冗余**。① 验收行为（确认通过/驳回、状态回退、业主确认后评价）已在「三、任务详情页（服务方）」§3.6 确认通过/驳回规格、§3.15-3.19 与「六（原七）、任务详情页（业主）」§7.2 业主确认按钮规格、§7.8-7.11 完整覆盖；② 两章各仅 1 个二级标题（4.1/8.1 页面概述），实质只是"验收页是详情页简化版 + 底部确认通过/驳回按钮"、"业主验收差异=确认后必须评价"，无独立行为细节；③ **关键**：原型不存在 `service-miniapp/task-acceptance.html` / `owner-miniapp/task-acceptance.html` 独立文件，实际确认流程在 `task-detail.html` 内完成（待办页跳转 task-detail.html 进入确认视角），两章描述的是与当前原型不符的独立页面。
- **执行**：删除四/八两章正文块（含顶部注释）+ TOC 条目（章节 li + 对应 toc-sub sub-4-1/sub-8-1）。剩余章节连续重排：五→四（全部待办页 服务方）、六→五（全部任务 业主）、七→六（任务详情页 业主）、九→七（全部待办页 业主）、十→八（字段校验规则）、十一→九（API接口汇总）；同步重排 sub-id（sub-5-→sub-4- 等）、subsection-title 可见号、toc-sub 可见号、正文标题、（服务方/业主）TOC 标签、编号顶部注释。
- **校验**：div 平衡 0；TOC 72 锚点全命中正文（missing=[]）；Grep `任务验收页|page-task-acceptance` 零残留（仅 .bak-del-20260813 备份保留）；章节号 一→九 连续无空缺；无遗留旧 sub-5/6/7/9/10/11-id（`sub-5-`等现为重排后新章，数量符合预期）。
- 备份：`PRD-任务模块详细规格.html.bak-del-20260813`（本次新增，供回滚；非历史 .bak）。
- 改动文件：`PRD-任务模块详细规格.html`。

## §3.6 多确认人确认与驳回细化补充（用户指令）
- 位置：`PRD-任务模块详细规格.html` §3.6「状态流转」小节后新增 `<h4>多确认人确认与驳回（状态流转细化）</h4>`。
- 内容1（第一人确认后其他确认人效果）：多确认人下第一人确认后主状态仍保持「确认中」，其余确认人保持"待确认"逐一确认，进度实时更新（1/3→2/3→全部确认），每位仅确认一次，全部确认才完成流转。与原型 CONFIRMING 状态（confirmProgress「确认进度：1/3」、其余待确认）一致。
- 内容2（需求，重点）：任一确认人驳回后，不论其他确认人此前是否已确认，**所有确认人确认状态均重置为"待确认"**，执行人重新提交后逐一**重新确认**，此前已确认人不能豁免；被驳回轮次记录标记"已驳回"留存但不计入新一轮进度。
- 标注：该重置为需求规格；原型 `rejectTask`（task-detail.js 661-675）当前仅回退主状态 REJECTED_PENDING、未显式重置各确认人确认状态，实现时需落实。
- 校验：div 结构平衡，h4 衔接"状态流转→多确认人细化→相关记录生成"正常（无确认人 note 已移除）。
- 改动文件：`PRD-任务模块详细规格.html`。

## §3.5 校验口径升级 + §3.6「本节操作不适用」note 清理（用户指令）
- **任务1（note 清理）**：文档原仅 §3.6 有一处「无确认人场景（本节操作不适用）」note（Grep `本节操作不适用` 全文档唯一）。该信息已被 §3.3「确认人为空时的状态流转」note 与状态机「无确认人快捷完成」子节完整覆盖；放在确认/驳回小节易误导读者以为整节不适用 → **删除该 note 整块**。删除后 §3.6 衔接：多确认人 h4 → 相关记录生成 h4，结构正常。
- **任务2（§3.5 校验口径）**：在 §3.5 末尾原有"格式说明" note 之后，新增 note「限制项校验实现口径」：明确大小/时长限制**无法靠 accept 实现**（accept 仅 MIME 过滤，既不限制大小也不限制时长），须显式校验——① 客户端预校验（小程序 wx.chooseVideo/chooseMedia 原生回传 duration/size，或 H5 createObjectURL+loadedmetadata 读元数据）；② 服务端权威校验（ffprobe/FFmpeg 或 COS 数据万象/云点播探针，超限拒收/转码）。并标注原型 handleFileSelect 当前仅记录 file.size、大小/时长校验待补实现。
- 校验：Grep `本节操作不适用` 零残留；`限制项校验实现口径` 已写入；div 结构平衡。
- 改动文件：`PRD-任务模块详细规格.html`。

## §3.5 note 去英文 + §3.6 驳回重置标注改为"已实现"（用户指令）
- **任务1（§3.5 note 去英文）**：用户质疑「限制项校验实现口径」note 内大量英文是否必要。判断：① 大量英文（wx.chooseVideo / wx.chooseMedia(...) / URL.createObjectURL / loadedmetadata / ffprobe / FFmpeg / COS / handleFileSelect 等 API 与代码片段）**无必要**——PRD 是产品需求文档，技术实现细节应归技术设计/开发文档，堆在 PRD 增加非目标读者负担且超出需求范畴；② 但 `accept` 一词**有必要保留**——它是澄清核心结论"限制无法靠 accept 实现"的锚点术语（去掉读者无法理解为何要显式校验），且 accept 是通用 HTML 标准属性名、单个词非代码片段。→ 重写 note：删除所有代码片段与多余英文工具名，仅保留 accept，其余中文化（客户端"选完即拦读取大小/时长"、服务端"媒体探针读取真实大小/时长"）。
- **任务2（§3.6 驳回重置标注）**：用户确认原型已实现"驳回后重置所有确认人重新确认"（对应页面状态：驳回后待开始 · 确认人视角），无需更改。→ 将 §3.6 多确认人第②段末尾标注由"该重置行为为需求规格，原型 rejectTask 当前仅回退主状态…实现时需一并落实"改为"该重置行为原型已实现：驳回后任务进入「驳回后待开始」，于确认人视角下所有确认人均回到「待确认」、需重新确认（对应页面状态：驳回后待开始 · 确认人视角）"。
- 校验：Grep `wx.chooseVideo|URL.createObjectURL|loadedmetadata|ffprobe|handleFileSelect|rejectTask|实现时需一并落实` 零匹配；§3.5 note 仅保留 accept 一词；§3.6 标注已更新。
- 改动文件：`PRD-任务模块详细规格.html`。

## 二级标题缺序号修复（sub-x 占位 id 规范化）
- **用户疑问**：7.6 下方「生成规则/待办内容规格/交互与完成规则」是否为二级标题？若是缺序号需补，若不是不应进目录。→ 分析：三者均有左侧目录 `toc-sub` 条目 + 正文 `subsection-title` 样式 = 二级标题，但缺序号且用占位 id。按规范补序号（保留嵌套在 7.6 内）。
- **顺带发现 BUG**：id `sub-x3` 全文重复（第一章 465 行「无确认人快捷完成」+ 第七章「生成规则」），导致目录 `#sub-x3`（生成规则）实际跳转第一章错误位置。
- **执行（PRD-任务模块详细规格.html）**：
  1. 第七章三项补 7.7/7.8/7.9：TOC（336-338 行 sub-x3/x4/x5→sub-7-7/7-8/7-9 并补可见号）+ 正文（2049/2052/2090 行 id 与标题文本同步）。
  2. 第一章三项补 1.1/1.2/1.3：TOC（273-274 行 sub-x1/x2→sub-1-1/1-2，并补回缺失的 1.3 目录条目 sub-1-3）+ 正文（406/452/465 行 id 与文本同步）。
- **校验**：Grep `id="sub-x` 零残留；TOC 74 锚点全命中正文（broken=NONE）；重复 id=NONE；div 平衡 0。
- 备份：`PRD-任务模块详细规格.html.bak-num-20260813`（本轮新增，供回滚）。
- 改动文件：`PRD-任务模块详细规格.html`。
