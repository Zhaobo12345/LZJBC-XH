# 2026-08-17 工作日志

## PRD 任务模块：{接单名称}→{阶段名称} 重命名 + §3.16~§3.19 补充
- 用户指令：① 文档内 `{接单名称}` 改为 `{阶段名称}`；② 依据 §3.11~§3.15 的标准/风格/规范，将 §3.16~§3.19 补充或调整（主要内容依据对应原型页面 `service-miniapp/js/task-detail.js` 实际渲染）。
- 改动（`PRD-任务模块详细规格.html`）：
  1. **全局重命名**：`{接单名称}`→`{阶段名称}`（含 §3.14 聚合规则/内容字段、§3.15 动态文案细分/内容字段，及源列 bare 引用）；同步将占位定义 `{…}=所属合同 / 服务名称（来源）`→`{阶段名称}=所属阶段名称（来源）` 以保持自洽。注意：动态记录「来源（source）」字段取值仍为「所属合同 / 服务名称」（独立属性，未动），与 `{阶段名称}` 占位（指向阶段名）刻意区分——文本述阶段+任务，source 字段指合同。
  2. **§3.16 确认中（被驳回后）**：权限表拆为 确认人/配置人/执行人 三行（对齐 §3.15）；字段改为「执行记录卡（当前轮）/ 历史记录区（historyRecords, 第1次执行 半透明 opacity 0.6 + 红底驳回原因）/ 确认记录卡（confirm-list, 第2轮当前+第1轮历史半透明）」三点；交互明确第 2 轮确认/驳回与 §3.15 完全一致，并交叉引用 §3.15「确认/驳回后记录生成规则」与「确认/驳回表单字段规格」（生成确认记录+动态，最后一人→任务已完成）。
  3. **§3.17 已完成**：权限表拆 确认人/配置人/执行人；交互细化 查看评价（<code>showEvaluationListModal</code>）/ 去评价（回写本人确认记录，不新增确认记录/动态）；新增「评价表单字段规格」表（综合评星/评价标签/评语（文本域）/按钮项，均选填无校验，对照 §3.15 表单规格风格）；逻辑补充「最后一人确认触发任务已完成动态（见 §3.15）」。
  4. **§3.18 已完成（含驳回）**：权限表拆三行；字段改为「历史记录区（第1次执行 半透明+红底驳回原因）/ 确认记录卡（第2轮最终确认+第1轮历史半透明）」两点；交互交叉引用 §3.17 评价表单规格 与 §3.15 记录生成规则。
  5. **§3.19 驳回后待开始**：字段改为四点（执行记录卡当前轮暂无/历史记录区/确认人列表/确认记录卡）；交互拆为 (1) 上传记录（同 §3.12）、(2) 重新提交确认/完成任务（含提交前置校验——须≥1执行记录否则置灰、触发弹窗同 §3.14、按钮项、后续流转 confirming_after_reject/completed_with_reject 并按 §3.14 提交后记录生成规则生成提交动作执行记录+动态）；保留原有「与 §一 主状态机对应关系」note。
- 校验：Grep 确认 `接单名称` 零残留、定义已更新；新块无裸英文字段标识符（historyRecords/confirm-list/状态 key 均置于括号内，<code>showEvaluationListModal</code> 包裹函数名）；HTML 标签闭合正确；section/subsection 层级完整。
- 改动文件：`PRD-任务模块详细规格.html`（全局重命名 + §3.16~§3.19 重写补充）。

## 用户确认：动态文本 vs source 字段口径（2026-08-17 收尾）
- 用户明确确认：动态**文本**引用仍为 `{阶段名称}{任务名称}`（阶段名+任务名）；动态记录**来源（source）**字段指向「所属合同 / 服务名称」（合同/服务），两者互不影响、刻意区分。
- 据此，文档无需再改动：上一轮重命名只动动态文本模板，未动 source 取值，已符合本确认。后续维护切勿把 source 改为阶段名。

## 业主端任务详情页（第六章 §6.4~§6.12）按服务方标准补齐（2026-08-17）
- 用户指令：根据服务方小程序任务详情页（§3.11~§3.19）的内容标准、风格、规范，将业主小程序任务详情页（第六章）补充或调整，主要内容依据对应原型 `owner-miniapp/`。
- 关键原型核对（`owner-miniapp/js/task-detail.js` + `task-detail.html`）：
  - 业主端**确认通过弹窗**含媒体上传（图片/视频/音频，选填）+ 评语（desc，选填）+ **综合评星（原型标注"非必填"）** + 评价标签（多选：责任心棒/服务态度好/专业能力强）；**与原先 §6.2 写的"必填星级"矛盾，已修正为选填**。
  - 驳回弹窗：媒体（选填）+ 驳回原因（必填 ≤500，`Validation.validate.textLength`）。
  - 去评价弹窗（showEvaluationModal）：综合评星**必填** + 评语（选填）+ 标签（选填）；查看评价弹窗（showEvaluationListModal）只读。
  - 业主端无配置人视角；各状态权限差异已据 `currentUserRole` 逻辑核对。
- 改动（`PRD-任务模块详细规格.html`）：
  1. **§6.2 业主确认按钮规格**：修正"确认弹窗综合评星"由必填→选填（业主专属确认时顺带评价非强制），新增"驳回按钮"行（驳回弹窗含媒体+驳回原因必填≤500）。
  2. **§6.4~§6.7（待配置/待开始/前置阻塞/执行中 业主只读）**：权限由段落改为**逐视角表格**（其他相关方/确认人/执行人[演示切换]三行）；字段拆分（执行记录卡只读、媒体网格📷🎬🎵+说明文字）；明确业主各视角只读无操作。
  3. **§6.8 确认中（业主可确认）**：权限逐视角表；字段拆为确认人列表/执行记录卡只读/确认记录卡/历史记录区(隐藏)；交互含确认通过+驳回弹窗明细；**新增「确认/驳回表单字段规格」表**（媒体/评语/综合评星选填/标签；驳回原因必填≤500）+ **「确认/驳回后记录生成规则」**（交叉引用 §3.15：确认记录+动态，最后一人→任务已完成，动态文案 {确认人姓名} 确认了/驳回了 {阶段名称}{任务名称} / 完成了…的施工）。
  4. **§6.9 确认中(被驳回后)/§6.12 驳回后待开始**：权限逐视角表；字段拆为执行记录卡/历史记录区(historyRecords 半透明 opacity 0.6)/确认记录卡(confirm-list 第2轮+第1轮半透明)/确认人列表；交互交叉引用 §6.8 表单与记录规则。
  5. **§6.10 已完成 / §6.11 已完成(含驳回)（业主可评价）**：权限逐视角表；字段拆为执行记录卡只读/确认记录卡/历史记录区；交互含查看评价(showEvaluationListModal)/去评价(showEvaluationModal)；**新增「评价表单字段规格」表**（去评价：综合评星必填/评语选填/标签选填；查看评价只读）。逻辑补「最后一人确认触发任务已完成动态（§6.8/§3.15）」。
  6. **§6.3 通用区块**：补「历史记录区」一条（含驳回过程状态展示，半透明留存）。
- 校验：Grep 全文档零 `接单名称` 残留、零"必填星级"旧矛盾表述；新块英文标识符一律「中文（英文）」前置（files/desc/rating/tags/reject_reason/confirmer_id/confirmed_at/task_confirmations/activity-list/typeLabel/hasMedia/mediaCount 均置于括号内或 `<code>`），无裸英文；HTML 标签闭合、subsection 层级完整；锚链接 `#sub-3-15` 存在有效。

## PRD 任务模块：待办生成规则 + 表单校验补强 + 动态含确认人媒体（2026-08-17）
- 用户指令（改 `PRD-任务模块详细规格.html`，不改变其他已有功能描述）：① 执行人提交确认后给确认人生成待办（补待办生成规则）；② 确认人驳回后给执行人生成待办（补待办生成规则）；③ §3.15「确认/驳回表单字段规格」中校验规则=「无」的可选项需补合理校验（遵从本项目同类型标准）；④ §3.15 生成动态需包含确认人确认/驳回上传的图片/视频/音频（动态记录字段内补充）。
- 改动：
  1. **§3.14 提交后记录生成规则 新增（5）待办生成规则**：执行人「确认提交」使任务转「确认中」后为**每位确认人**自动生成「合同任务确认」待办（对应 §4.3 类型 confirming，跳转 `task-detail.html?status=confirming&role=confirmer`）；「确认完成」（无确认人）不生成；含状态联动（确认完→completed，驳回回退→移出，重新提交→重生成）。
  2. **§3.15 确认/驳回后记录生成规则 新增（5）待办生成规则**：确认人「确认驳回」使任务回退「驳回后待开始」后为**执行人**自动生成「合同任务执行」待办（对应 §4.3 类型 pending，因 rejected_pending 属待开始家族复用该类型，跳转 `task-detail.html?status=rejected_pending&role=executor`）；单条确认不生成；含状态联动。
  3. **§3.15 表单字段规格 校验规则补强**（原 5 处「无」全部替换，遵从同类型标准）：
     - 媒体附件（确认/驳回）：图片 jpg/jpeg/png/webp、视频 mp4/mov、音频 mp3/m4a/wav（MIME 须 `image/*`/`video/*`/`audio/*` 之一）；单文件≤20MB、数量/时长受 §3.5 限制（图片≤9张、视频≤1个≤3min、音频≤3个≤60s）；name/type/size 三项须齐全；选填未上传不校验。
     - 评语：若填写则 1–500 字符（对应 §8.3 确认备注 remark，String(500)），`Validation.validate.textLength` 最大500非必填；留空不校验。
     - 综合评星：若评分须 0.5–5 半星步进合法值，控件已限范围无需额外交互校验。
     - 评价标签：仅可从预置集合（责任心棒/服务态度好/专业能力强）多选，不支持自定义。
  4. **§3.15 动态记录字段 补充确认人媒体**：新增「媒体文件（files）」行（确认人确认/驳回上传的媒体，📷/🎬/🎵 呈现）；更新「是否有媒体（hasMedia）/媒体数量（mediaCount）」行（取确认人上传集合，有则 true/总数，否则 false/0，区分于 §3.14 执行人媒体）；说明 bullet 增一句点明动态现含确认人媒体。同步修正 §6.8 业主端一致性表述（原"不产生媒体固定 false/0"→改为含确认人媒体、引用 §3.15）。
- 校验：Grep §3.15 表单表已零「无」残留；英文标识符「中文（英文）」前置（`image/*`/`video/*`/`audio/*`/<code>Validation.validate.textLength</code> 等合规）；HTML 标签闭合。注意：§3.17「去评价」评价弹窗仍有 3 处「无」（综合评星/评价标签/评语），属未请求的 §3.17 范围，已按用户明确指定 §3.15 而保留未动。
- 改动文件：`PRD-任务模块详细规格.html`（§3.19 新增「（3）待办生成规则」）。
  - 执行人点击「重新提交确认」（有确认人）使任务流转至「确认中（被驳回后）（confirming_after_reject）」后，为每位确认人生成「任务确认」待办，复用 §4.3「合同任务确认」待办类型、标签「任务」、状态值 confirming_after_reject、跳转 `task-detail.html?status=confirming_after_reject&role=confirmer`；点击「确认完成」（无确认人）不生成；含状态联动（第2轮确认完成→completed，再次驳回→移出，再次重提交→重生成）。风格与 §3.14（5）待办规则对齐；Grep 确认全文档无「标签『合同』」残留。
- 改动文件：`PRD-任务模块详细规格.html`（§3.16~§3.19 顺序调整，内容不变）。
  - 用户指定正确顺序：**3.16 已完成 → 3.17 驳回后待开始 → 3.18 确认中（被驳回后） → 3.19 已完成（含驳回）**（即按主流程自然时序：已完成→驳回回退→第2轮确认→含驳回完成）。原 3.16 确认中被驳回后→新 3.18、原 3.17 已完成→新 3.16、原 3.18 已完成含驳回→新 3.19、原 3.19 驳回后待开始→新 3.17。
  - 用脚本按 div 标签深度配对精确提取四个 `<div class="subsection">` 块重排，并同步重映射所有交叉引用编号（块内与块外）：C 内 `§3.17`→`§3.16`（评价表单字段规格）、D 内 `§3.16`→`§3.18`（确认中被驳回后）、行 1091 `§3.19 交互`→`§3.17 交互`、行 1330 `§3.15、§3.16`→`§3.15、§3.18`、TOC 四项标签与顺序同步；`§3.15–§3.19` 范围引用保持不变（3.19 仍为末节）。校验：14 项断言全 PASS，全文档无残留旧编号引用。

## PRD 任务模块：修复 §3.16~§3.19 布局错位
- 用户指令：文档内文件布局格式存在错位（截图显示左侧导航高亮 3.18，主内容区显示 3.19）。
- 根因：§3.16 / §3.17 / §3.18 三个小节结束标签写成 `</div`（缺右尖括号），导致浏览器自动修复未闭合 div，造成 subsection 边界错乱、导航与内容不匹配。
- 改动（`PRD-任务模块详细规格.html`）：将三处 `</div` 修复为 `</div>`，并统一后续 `<div class="subsection">` 的缩进。
- 校验：Grep 全文档无其他未闭合 `</div` 残留；Python 统计关键标签（div/table/thead/tbody/tr/td/ul/li/p/h4）开合数量全部一致；HTML 标签平衡检查通过。
- 注意：本次修复未改变任何已有内容文字，仅修正 HTML 标签闭合与缩进格式。

## PRD 任务模块：补充多轮（轮次无上限）+ 链接可点击
- 用户指令：① 确认/执行记录轮次原仅写"第 1 轮 / 第 2 轮"，需补充支持多轮（第 N 轮、轮次无上限）的描述；② 文档内"跳转 xxx"文字描述需补充可点击效果可跳转。
- 轮次多轮补充（`PRD-任务模块详细规格.html`）：
  - §3.2 通用区块：执行记录卡补"上传轮次（upload_round）递增，首次为1，第N次轮次无上限，最新一次为当前轮"；确认记录卡"按轮次分组（第 1 轮 / 第 2 轮 / … / 第 N 轮，轮次无上限）"。
  - §3.16 逻辑段新增"轮次机制（支持多轮，无上限）"段落：每被驳回重提一次进新一轮、不设上限、每轮独立分组留存、某轮全通过→已完成含驳回、某轮再驳回→驳回后待开始进下一轮。
  - 各状态小节（§3.16 字段/§3.18 权限与交互/§3.19 字段/业主端 §6.9/§6.11 字段与逻辑）的"按轮次分组""第 2 轮"示例统一扩展为"第 1 轮 / 第 2 轮 / … / 第 N 轮（轮次无上限）"，并补"新一轮（第 N 轮）""第 N 次执行依此类推"等；状态机表确认中（被驳回后）说明改为"进入新一轮（第 N 轮）确认"。
  - §3.17 重新提交"进入第 2 次执行"补"轮次无上限，第 N 次依此类推"；§3.16 执行记录卡当前轮"第 2 次执行"补"（第 N 次执行依此类推）"。
- 链接可点击（`PRD-任务模块详细规格.html`）：
  - §3.14/§3.15/§3.19 待办生成规则"跳转目标"的 `<code>task-detail.html?status=...&role=...</code>` 三处改为 `<a href="service-miniapp/task-detail.html?..." target="_blank">` 真实可点击链接（保留路径文字）。
  - §3.13 前置任务详情页跳转"跳转至该前置任务的详情页"改为 `<a href="service-miniapp/task-detail.html?status=in_progress&role=executor">` 链接（注明真实环境携带 taskId）。
  - §7.2 业主端待办对比表格"跳转 task-detail.html / ?status=completed / ?status=confirming / contract-detail.html?status=changing|change_confirming" 全部改为 `<a>` 链接（服务方列→service-miniapp，业主方列→owner-miniapp）。
  - §4.3 待办类型表格 contract-detail.html?status=changing 改为 service-miniapp 链接；§7 业主端待办交互流程"任务类/合同类/变更类待办：跳转业主端 xxx" 改为 owner-miniapp `<a>` 链接。
- 校验：HTML 标签平衡（div 321/321、a 136/136 等全 OK）；Grep 全文档零残留 `<code>task-detail/owner-miniapp/service-miniapp/contract-detail/todo-list`；新增内容无裸英文字段标识符（轮次/第N轮为中文，upload_round/opacity 0.6/confirm-list/historyRecords 均在括号内或既有规范内）。

## PRD 任务模块：补充「业主作为执行人」场景
- 用户指令：业主存在作为任务执行人场景（示例：室内业主个人用具占用施工场地需规定时间内搬移），需补充业主作为执行人的需求描述。
- 关键发现：`owner-miniapp/js/task-detail.js` 在 `currentUserRole === 'executor'`（含 `initFromUrl` 将 pending/rejected_pending 非 nonExecutor 映射为 executor）时**真实渲染**「上传记录/提交确认/完成任务」按钮——即原型已支持业主作为执行人，但 PRD §6 旧描述写"执行人仅原型演示/业主无执行能力"与代码矛盾。
- 改动（`PRD-任务模块详细规格.html`）：
  1. §3.10 角色模型：执行人「典型身份」由"项目内人员（可配置选择）"改为"含业主"；表后新增 note「执行人可包含业主（业主自办类任务）」含示例，引用 §6.3.1。
  2. §6.1 差异表：「执行记录上传」「任务执行」两行业主单元格补业主自办类任务的执行能力例外（引用 §6.3.1）。
  3. §6.3 引言：将"不具备配置/执行能力"改为"通常扮演确认人/相关方，但被配置为执行人时具备真实执行能力，仅不具备配置能力"。
  4. 新增 §6.3.1「业主作为执行人场景」：场景概述（示例）+配置方式+权限表（按状态）+交互与记录生成（复用 §3.12/§3.14/§3.17）+时限要求（要求完成时限由配置人/模板设定）+原型支持说明+统一注解（消解各状态小节"执行人仅演示"行与本场景的冲突：那些行描述业主默认站位，本任务实际配置业主为执行人时按本场景具备真实能力）。
- 校验：HTML 标签平衡（div 327/327、table 59/59、tr 308/308、td 816/816、ul 28/28、li 168/168、p 159/159、h4 115/115、a 145/145 全 OK）；新块无裸英文 `<code>`，`currentUserRole === 'executor'` 属代码表达式合规保留。

## 业主端任务详情页：待开始/执行中 执行人改为业主（原型）
- 用户指令：业主端原型 task-detail 页，待开始(pending)与执行中(in_progress)状态的「执行人视角」页面，执行人需改为业主（匹配业主为执行人场景），其他流程/功能不改动。
- 改动（`owner-miniapp/js/task-detail.js` + `owner-miniapp/task-detail.html`）：
  1. 新增 `renderExecutorCard()`：按 `window.currentExecutorId` 渲染「执行人」卡片（头像+姓名+角色徽标），避免状态切换残留旧身份。
  2. `updateTaskStatus()` 顶部集中按状态设置演示数据：pending/in_progress → 执行人 id=3（王五·业主）、确认人仅 张三+赵六（不含业主本人，保证同一任务内业主不既是执行人又是确认人）；其余状态 → 执行人 id=2（李四·工长）、确认人含王五，原流程不变；configuring → 无执行人/无确认人。
  3. 移除原 `initFromUrl()` 内冗余的 currentExecutorId/currentConfirmers 赋值（改由 updateTaskStatus 统一驱动，兼顾状态切换条直接调用 updateTaskStatus 的无刷新切换）。
  4. HTML 执行记录「提交人」加 `id="executionSubmitter"`，由 updateTaskStatus 按当前执行人动态填充（in_progress 显示 王五，其余保持 李四）。
- 校验：node --check 语法 OK；自建 DOM-stub harness 实跑 initFromUrl 验证——pending/in_progress 执行人=王五(业主)、确认人=张三+赵六、提交人=王五；confirming/completed 仍=李四、确认人含王五，其他流程未受影响。

## PRD §6.5/§6.7/§6.12 补充「执行人=业主」场景
- 用户指令：业主小程序 §6.5 待开始、§6.7 执行中、§6.12 驳回后待开始，在已有（业主只读）内容基础上补充当执行人为业主时的详细需求，效果及流程参考服务方执行人视角（§3.12/§3.14/§3.17），重复内容直接引用简化以减少冗余。
- 改动（PRD-任务模块详细规格.html）：三处各新增「（补充）执行人 = 业主时（业主自办类任务）」小节，仅列要点并引用 §3.12/§3.14/§3.17 与 §6.3.1，避免重述字段表；保留原「执行人（仅原型演示切换查看）」只读行并加 note 说明其仅适用于业主非本任务执行人的默认演示视角。
- 演示链接：为 §6.5/§6.7 原型演示行新增「业主作为执行人」role=executor 链接（原型 pending/in_progress 已渲染业主(王五)为执行人，链接真实可用）；§6.12 仅将默认链接标签改为「业主只读（默认）」，未加 executor 链接——因 owner-miniapp 原型 rejected_pending 默认演示需保留「业主作为确认人(曾驳回)只读」场景（强制业主为执行人会破坏该演示），故 §6.12 业主作为执行人仅以 §3.17+§6.3.1 文字描述，不误导。
- 顺带修复：为 §6.3.1 标题补 id="sub-6-3-1"（文档多处内链 #sub-6-3-1 此前无锚点目标，属既有失效链接）。
- 校验：HTML 标签开合平衡 OK（div/table/tr/td/ul/li/p/h4/a/thead/tbody 全部匹配）；补充小节计数=3；§6.6 未受影响；无裸英文状态标识符。

## PRD §6.9~§6.12 章节重排（业主端后置状态小节）
- 用户指令：将 6.10 内容放到 6.9、原 6.9 放到 6.11、6.12 内容放到 6.10、原 6.11 放到 6.12；目标顺序「6.9 已完成 / 6.10 驳回后待开始 / 6.11 确认中（被驳回后） / 6.12 已完成（含驳回）」，内容不变。
- 实质为内容身份 4-循环重映射：原 6.10(已完成)→6.9、原 6.12(驳回后待开始)→6.10、原 6.9(确认中被驳回后)→6.11、原 6.11(已完成含驳回)→6.12。
- 实现（Python 脚本，按块提取+改编号+重排+全局引用重映射）：
  1. 全局 prose 引用 `§6.9/§6.10/§6.11/§6.12` 按循环重映射（§6.9→§6.11、§6.11→§6.12、§6.12→§6.10、§6.10→§6.9），并保护 `§6.X~§6.Y` 区间引用（§6.5~§6.12、§6.4~§6.12 保持不变）。
  2. 按 OLD id 提取四个 `<div class="subsection">` 块（深度计数配对括号），仅改标题数字与 `id="sub-6-N"`；内容逐字保留。
  3. 按目标顺序排列四块（sub-6-10→sub-6-12→sub-6-9→sub-6-11），块间保留空行分隔。
  4. TOC 四行按新顺序重写（并修正旧 TOC「6.9 确认中（驳回后再次执行）」与真实标题不一致问题，统一为「确认中（被驳回后）」）。
- 校验：HTML 标签开合平衡 OK（无未闭合/游离标签）；四节新 id 各出现 1 次、对应 href 各 1 次；正文末四节 id 顺序 = sub-6-9→10→11→12、标题正确；§6.9 引用计数=3（旧 §6.10 三处→新 §6.9）、§6.10 计数=0（已全部重映射）、§6.12 计数=2 且均在受保护区；§6.8/§7 等邻接小节未受影响。

## PRD §5（业主全部任务）参考原型与 §2 扩充
- 用户指令：业主小程序「五、全部任务」参考业主方原型（owner-miniapp/task-list.html）更新，并参考服务方全部任务需求内容（重复处直接引用，减少冗余）；不影响其他已有功能/描述。
- 探查发现：业主端 task-list.html 原型与服务端结构几乎完全一致——导航标题「全部任务」、状态筛选下拉框实际提供全部 6 项（全部/待配置/待开始/执行中/确认中/已完成）、「参与」切换（全部/我参与）、搜索框、合同分组列表卡片；但旧 §5.2 差异表「状态选项卡」行写「仅显示：全部、确认中、已完成」，与真实原型不符（ outdated）。
- 改动（PRD-任务模块详细规格.html，仅限 §5 章节内）：
  1. §5.1 概述后补注：页面类型/业务目标引用 §2.1。
  2. §5.2 差异表「状态选项卡」行更正为「全部状态选项卡，与服务方一致（筛选项见 §5.4）」；差异在卡片操作权限。
  3. 新增 §5.3 页面结构（引用 §2.2）、§5.4 状态筛选规格（引用 §2.3，列 6 状态中文（英文）前置）、§5.5 任务卡片规格（引用 §2.4）、§5.6 筛选规格（引用 §2.5）、§5.7 分页规格（引用 §2.6）、§5.8 任务状态详解（业主方）（引用 §2.7 + 业主角色差异表：确认人可确认/驳回见 §6.2、被配置为执行人见 §6.3.1、其余默认只读）；均以「引用+仅补业主差异」方式减少冗余。
  4. TOC 业主小程序分组新增 §5.3~§5.8 六个 toc-sub 条目。
- 校验：HTML 标签开合平衡（仅 br 为 void 元素、无闭合属正常）；六个新 sub-5-x 的 id 与 TOC anchor 各匹配 1 次；旧错误文字已移除；§6 等邻接章节未受影响；§5.4 英文标识符均「中文（英文）」前置，无违规裸英文（既有 status=pending 等属代码表达式，符合约定）。

## PRD 合同模块：补充「接单/确认加入→自动加入架构层级+权限分配」
- 用户指令（`PRD-合同模块详细规格.html`）：核查工人合同详情需求是否已包含「受邀工人点击接单后，系统自动将其加入合同隶属的架构层级（成为其中一员分配基础权限及乙方对应权限）并成为合同乙方」，缺失则补充，已有描述不改。
- 核查结论：文档**已**明确描述「成为合同乙方」与「自动加入项目架构层级（group 字段更新）」——多处存在：§7.5.3.3 关键逻辑（并发首胜机制，行3610）、§7.5.3.4 横幅/触发条件（行3625）、§8.4.1 状态流转（行4002）、§8.4.2 横幅/触发条件（行4023）、§10.4 字段表（行3860/4492）；但「分配基础权限及乙方对应权限」这一子句**未**显式描述（全文无"权限分配"表述）。
- 改动（纯新增 note，不改任何既有文字）：
  1. §7.5.3.3 表格后新增 note「加入架构层级时的权限分配」：明确确认加入（即方案E接单体验的接单动作）成为乙方+自动加入架构层级时**同步完成权限分配**，含两层——① 架构层级基础权限（成为层级成员、持「所属架构及下属层级权限（含合同）」、可看参与架构子树数据、合同金额展示实际金额不再脱敏）；② 乙方（party B）对应权限（查看本合同、收乙方相关待办、参与阶段任务执行与确认流转、变更流程乙方确认/驳回）；并说明撤回/重选/抢单失败/已拒绝时权限自动回收。
  2. §8.4.2 表格后新增短 note 同主题，指向 §7.5.3.3 完整规则与 §2.9/§5.12 权限控制模型。
- 术语对齐：文档动作词为「确认加入」（方案 E 称「接单体验」），与用户口语「接单」映射一致，已在 note 中说明。
- 校验：仅插入两个 `<div class="note">` 块，既有文字/标签均未改动；HTML 标签平衡未受影响。

## PRD 合同模块：补「接单成为乙方 → 同步登记入项目成员名册」
- 用户追问：`PRD-合同模块详细规格.html` 已含「接单成为乙方+自动加入项目架构层级」，是否还包含「同时将该人加入项目成员（项目成员名册）」？
- 核查结论：**未包含**「项目成员名册登记」这一独立子项。文档仅在 §3.7.1 拟定中编辑表单·签约双方写过「甲方/乙方候选人均从项目成员中筛选」（行1121/1126），但从未显式写出「接单成为乙方时同步登记入项目成员名册」这一结论（注意：项目成员名册 与 架构层级 是两个不同概念，文档此前只写了后者）。
- 改动（扩充上一轮两个 note，不改既有文字）：
  1. §7.5.3.3 note「加入架构层级时的权限分配」→ 改名为「接受邀约后的成员登记与权限分配」，将权限「两层」扩为「三层」：新增 ① 项目成员名册登记（成为乙方同时从项目成员名册筛选规则反推——若未在册则新增为项目成员、在册则更新架构层级与乙方身份关联，纳入成员关系管理）；原 ① 架构层级基础权限→②、原 ② 乙方对应权限→③；回收段补充「因本次接单新增的项目成员在失去乙方身份后默认保留为普通项目成员（仅解绑乙方与架构层级归属）」。
  2. §8.4.2 note 同名主题，补充说明「已登记入项目成员名册」触发成员登记与权限分配，指向 §7.5.3.3 + §2.9/§5.12。
- 交叉引用：项目成员筛选出处指向 §3.7.1。
- 校验：仅扩充两个 note 内容（div/tag 数量不变）；Grep 确认「项目成员名册」「登记入项目成员」各出现 2 次（3617/4042）。

## PRD 合同模块：本轮补充后缺口审计 + 收紧
- 用户指令：根据上一轮「接单成为乙方→登记入项目成员名册」的补充，具体分析是否还有缺失。
- 审计结论与处置：
  1. **（已修·真实缺口）悬空引用「项目成员管理规则」**：上一轮 note 末句"具体按项目成员管理规则处理"指向一个**全文档不存在**的章节（仅 §3.7.1 写过乙方候选从项目成员筛选，不构成管理规则章节）。已删除该悬空短语，因 V1 默认保留口径已内联写明。
  2. **（已补·隐性缺口）失败方不登记**：原 note 只描述首胜者登记，未显式排除失败方。已在 ① 项目成员登记 末补"仅首位确认者登记入项目成员并成为乙方；其余受邀方（抢单失败/已拒绝）因未成为乙方，不触发项目成员登记"。
  3. **（保留·待用户决策）横幅文案未提项目成员**：§7.5.3.4(行3625)与 §8.4.2(行4023) 横幅仍只写"已自动加入项目架构层级"，未提项目成员登记。属用户可见状态文案精简，是否同步更新由用户定（已标注为可选）。
  4. **（无需改）§10.4 字段表**：合同字段无"项目成员"字段属正常（成员登记为项目级副作用，非合同字段）；group(架构层级) 字段已存在；无缺口。
  5. **（无需改）权限模型引用 §2.9/§5.12**：锚点真实存在（行884/2744），引用有效。
  6. **（验证建议·超出 PRD 文本）原型一致性**：worker-contract-detail.js / contract-store.js 是否真实将工人写入项目成员名册需另行核对（PRD 文本缺口已闭合，实现一致性建议单独验证）。
- 校验：HTML 标签平衡未受影响（仅 note 内文字增删）；Grep 确认"项目成员管理规则"已零残留。

## PRD §2（服务方全部任务）补充三级卡片字段与交互
- 用户指令：§2「二、全部任务（服务方）」缺少列表页各字段说明（合同名称、隶属工作组名称、阶段数量总数、任务总数、已完成进度条及数量）、缺少交互描述（点击合同展开包含的阶段及任务）；同理阶段字段、任务字段及交互描述缺失，请补充。
- 改动（PRD-任务模块详细规格.html，仅限 §2 章节内）：
  1. §2.2 页面结构 ASCII 图：导航标题「我的任务」修正为「全部任务」（与原型一致）；补充缺失的「阶段分组」层级（阶段卡片头部可展开/收起、阶段状态指示点、阶段进度摘要），使结构图与 §2.4 三级一致。
  2. §2.4 由「任务卡片规格」扩展为「列表卡片规格（合同 / 阶段 / 任务）」，新增 2.4.1 合同卡片字段表（合同图标/合同名称/隶属工作组名称/阶段数量总数/任务总数/整体完成进度条及数量/展开箭头）、2.4.2 阶段卡片字段表（阶段状态指示点/阶段名称/按序执行标识/阶段进度摘要/展开箭头）、2.4.3 任务卡片字段表（状态图标/任务名称/执行人/确认人/确认进度(仅确认中)/任务状态文字）、2.4.4 卡片交互描述表（点击合同展开/收起阶段 toggleContract、展开全部 expandAllContracts、点击阶段展开/收起任务 toggleStage、点击任务跳转详情 goToTaskDetail、按序执行拦截提示、默认展开态）。字段均依据 service-miniapp/task-list.html 原型实测渲染（合同元信息「工作组·N个阶段·N个任务」、progress-text「已完成 x/y」、stage-count「x/y 状态」、confirm-progress「x/y 已确认」等）。
  3. TOC：§2.4 标题更名并新增 2.4.1~2.4.4 四个 toc-sub 子条目。
- 校验：HTML 标签开合平衡 OK（div/table/thead/tbody/tr/td/th/ul/li/p/h4/a/strong 全部匹配）；sub-2-4 及 sub-2-4-1~4 的 id 各 1 次、href 均解析到存在 id；关键词齐全；状态英文标识符均为 status=…/tasks.status 等代码表达式（符合约定，非裸英文标签）；§2.3/§2.5~§2.7 及其他章节未受影响。业主 §5 对 §2.4 的引用仍有效（§2.4 现覆盖三级卡片，task 卡引用更贴切）。

## PRD 合同模块：§6.3/§10.3 盒式状态机 + 待办生成规则（参考任务模块「任务状态机」效果）
- 用户指令：参考 PRD-任务模块详细规格.html 的「任务状态机」盒式流程图效果，为合同模块新增合同状态机（非工人合同、工人合同分别增加），并补充待办项生成规则（例：① 非工人合同平台审核通过后给业主（合同甲方是业主时）生成「合同待确认」待办；② 合同已确认给发起方生成「待上传签约附件」待办）。
- 新增样式：在 `<style>` 内 `</style>` 前追加 `.flow-diagram`/`.flow-step`/`.box.active`/`.box.passive`/`.arrow` 盒式流程图 CSS（变量已存在），复用任务模块 §一 状态机的视觉效果。
- 改动（PRD-合同模块详细规格.html）：
  1. **§6.3 状态机（非工人合同）**：保留原 ASCII 总览图与「驳回态双视角说明」；新增「合同状态机（盒式流程图）」（拟定中→待审核→待确认→待签约→已签约 主流程 + 审核驳回回灌分支 + 变更回灌分支）；新增 **6.3.1 状态定义（非工人合同）** 表（drafting/pending_review/pending_confirm/pending_sign/signed/platform_rejected(含 reviewed_reject 视角)/changing，列：状态值/状态名称/进入条件/可执行操作）；新增 **6.3.2 待办生成规则（非工人合同）** 表（平台审核通过→业主「合同待确认」待办、合同已确认→发起方「待上传签约附件」待办、平台审核驳回→发起方「待修改重新提交」待办），附 note：① 业主待办仅当甲方=业主时生成；② 标签口径对齐任务模块 §4.3。
  2. **§10.3 工人合同状态机（无平台审核）**：保留原三条 ASCII 图；新增「主流程状态机（盒式流程图）」（拟定中→确认中→已确认→已签约 + 发起方重新选择乙方回灌分支）；新增 **10.3.1 状态定义（工人合同，标注视角）** 表（worker_draft_initial/worker_draft/worker_inviting_sender/receiver/worker_confirmed_sender/receiver/worker_signed/变更态，含 视角 列区分发起方/受邀方）；新增 **10.3.2 待办生成规则（工人合同）** 表（邀请发出→受邀方「接单确认」待办、合同已确认·发起方→发起方「待上传签约文件」待办），附 note：① 无平台审核故无业主待确认/PC审核待办；② 上传待办仅发起方生成（受邀方无入口，见产品决策）；③ 标签=合同，对标 §4.3。
- 附加修复（校验中发现、与本次新增无关但同文件，均为 HEAD 预存在结构缺陷，不影响业务流）：§7.5.1/§7.5.2/§9.3 三处 `<thead>` 未闭合（数据行误置于 thead 内、以 `</tbody>` 收尾）已修正为「header 行 `</tr></thead><tbody>` 数据行」；`<div class="main-content">`（body 内 410 行附近）全程未闭合，已在 `</body>` 前补 `</div>`。
- 校验：Python HTML 解析器标签平衡 0 错误；CSS 三规则均存在；sub-6-3-1/2、sub-10-3-1/2 锚点均存在；状态英文标识符均置于「状态值」列（与任务模块 §1.1 表式一致，非裸英文）；邻接 §6.4/§10.4 及 §7.5/§9 其他小节未受影响。

## PRD 合同模块：§6.3/§10.3 状态机按原型校准 + 4 点修订
- 用户反馈（对上一轮 §6.3/§10.3 输出）：① 新输出状态机与原型不一致，须对齐原型真实状态（拟定中、平台审核、确认中、已确认、已签约），且合同状态机缺「发起变更」盒式流程图；② 文档内跳转目标需加可点击链接、非 PRD 规范英文应翻译或取消；③ 变更也会生成待办（平台审核通过→业主确认合同待办、业主确认后→发起方上传签约附件待办、平台审核驳回→发起方合同修改待办）；④ 工人合同不需给受邀方（邀请方）生成待办（受邀方可能非项目内人员，待办无效）。
- 原型权威基准核对：`service-miniapp/contract-detail.html` 步骤条(381-396)=「拟定中/平台审核/确认中/已确认/已签约」；`contract-detail.js` STATUS_MAP(1125-1149)=`draft`/`platform_reviewing`/`confirming_sender`+`confirming_receiver`/`confirmed`/`signed`/`platform_rejected`(含 `reviewed_reject` 视角)/`changing`；变更态（非工人·含平台审核）`change_platform_reviewing`/`change_platform_rejected`/`change_confirming`/`change_confirming_sender`/`change_signing_wait`/`change_confirmed`；工人合同 `contract-store.js` 全程无平台审核（worker_inviting/worker_confirmed/worker_draft/worker_signed + 双视角）。
- 改动（PRD-合同模块详细规格.html）：
  1. **§6.3 状态机对齐原型**：ASCII 总览与盒式主流程图更正为「拟定中(draft)→平台审核(platform_reviewing)→确认中(confirming_sender/receiver)→已确认(confirmed)→已签约(signed)」、`平台审核驳回(platform_rejected/reviewed_reject)`；删除旧的 待审核/待确认/待签约、drafting/pending_review/pending_confirm/pending_sign。6.3.1 状态定义表状态值/名称列同步更正。
  2. **§6.3 新增「发起变更」盒式流程图**（点 1）：`已签约 ⇒发起变更 变更中 → 变更审核中(change_platform_reviewing) ⇒审核通过 变更确认中 ⇒签约完成 已签约(V2)`，外加 `变更审核中 ↓变更审核驳回(change_platform_rejected) 变更已驳回 ⇒重新发起变更 变更中（回灌）`。
  3. **新增 §6.3.3 变更流程待办生成规则**（点 3）：变更平台审核通过→业主「待确认变更」待办（条件 partyA=业主，`?status=change_confirming_receiver`，标签 变更）；变更确认后→发起方「待上传变更签约附件」待办（`?status=change_signing_wait`）；变更平台审核驳回→发起方「变更合同修改」待办（编辑态）；note 明确「工人合同变更去平台化，无此类带平台审核的待办，归第九章」。
  4. **§10.3.2 移除受邀方待办**（点 4）：删除「邀请发出（进入确认中）→受邀方接单确认」待办行；仅保留「合同已确认→发起方待上传签约文件」（`worker-sign-upload.html`）；note ③ 明确「工人合同不向受邀方生成任何应用内待办，因受邀方可能非项目内成员，改由微信服务通知（wechat-service-notice.html）接单确认」。
  5. **跳转目标链接化 + 裸英文清理**（点 2）：章节引用「详见第九章/第八章/第三、四章/第一~六章/§6.3 驳回态说明」全部改为 `<a href="#worker-contract-change">` 等锚点链接；§6.3.2/§6.3.3 跳转目标单元格包 `<a class="proto-link" href="service-miniapp/..." target="_blank">`；裸英文 `SectionTab`/`#rejectModal`/`#confirmModal`/`draftEditForm`/`draftViewContent`/`.disabled`/`primary 样式`/`secondary 样式`/`fullWidth`/`pc-btn-danger`/`showRejectInput` 等一律包 `<code>` 或翻译（`相关API接口`→`相关接口`）；补 15 个 TOC 已引用但正文缺 id 的锚点（sec-5-15-1~8、sec-9-5-1~6、sec-7-5-3）。
  6. **顺带修复 §3.10 内部矛盾**：流程图展示条件文字原写「拟定中/待审核/审核已通过/确认中/已确认」，而其上方步骤图已为「拟定中→平台审核→确认中→已确认→已签约」，已更正为「拟定中/平台审核/确认中/已确认」保持一致。
- 校验：Python HTML 解析器标签平衡 0 错误；锚点解析 0 缺失（`pending_review`/`待审核`/`待签约` 残留均属 PC 运营端模块自有状态 key 或 第十章工人合同对比/展示标签，非 §6.3 错误，保留）；§6.3 旧错名（待审核/待签约/pending_review/pending_confirm/pending_sign/drafting）在状态机区域清零、新正确名（平台审核/确认中/已确认/platform_reviewing/confirming/confirmed/发起变更/change_platform_reviewing）全部就位；无裸英文 `<code>` 违规残留。
- 清理：删除会话内临时脚本 `_extract_en.py`/`_extract_en2.py`/`_validate2.py`/`_add_ids.py`。

## PRD 合同模块：补充「已签约 → 生成项目动态」规则
- 用户指令：工人合同、非工人合同当已签约状态时，生成一条动态：`{合同名称}签约完成`；补充生成动态相关规则描述。
- 背景：合同模块此前只有 PC 端「操作记录」时间线（§2.6/§2.7，signed 态追加"确认签约/合同签约完成"），无「项目动态（activity-list）」动态记录生成规则；任务模块（§3.14(4)/§3.15(4)）已有「动态记录生成规则（项目动态）」数据模型可供对齐。
- 改动（PRD-合同模块详细规格.html）：
  1. 新增 **§6.3.4 动态生成规则（非工人合同）**：触发=状态 `confirmed`→`signed`（乙方上传签约文件生效）；动态文案固定「{合同名称}签约完成」；含「动态记录字段」表（操作人（user）/角色（role）/类型（type）=合同类动态（contract）/类型标签（typeLabel）=「合同签约」/内容（content）/来源（source）/时间·日期·日期标签/媒体文件（files）/是否有权限查看（hasPermission）），字段模型对齐任务模块；note 说明「项目动态」与 PC「操作记录」同源互补（成员活动流 vs 运营审计时间线）。
  2. 新增 **§10.3.3 动态生成规则（工人合同）**：触发=状态 `worker_confirmed`→`worker_signed`（发起方上传签约文件成功）；动态文案同 §6.3.4；字段模型直接引用 §6.3.4（避免重复），仅操作人=发起方（工长·甲方）；note 明确动态对已在项目内的成员（含已确认加入架构层级的乙方工人）可见，与「不向受邀方生成应用内待办」（§10.3.2 补充 ③）不冲突（受邀方确认加入后才可见本项目动态）。
- 命名约定沿用：动态类型（type）值 `contract`（对标任务模块 `task`）；动态写入「项目动态（activity-list）」日期分组（今天/昨天/…）。
- 校验：Python HTML 标签平衡 0 错误；锚点解析 0 缺失；sub-6-3-4/sub-10-3-3 各 1 次、内部无失效引用；`{合同名称}签约完成` 全文 3 处（§6.3.4 文案+内容行、§10.3.3 文案）；新增表格字段标识符均「中文（英文）」前置或 `<code>` 包裹，无裸英文违规。

## 原型 项目动态页（activity-list.html）示例对齐 PRD 动态规则
- 用户指令：原型项目动态页，将'更新了设计图纸'→'设计服务合同签约完成'（标签转合同）、'对泥瓦分包合同提出了变更'→'泥瓦服务合同变更成功'（标签转合同）、'上传了设计效果图初稿'→'设计服务合同签约完成'（标签转合同）；其余按 `PRD-任务模块详细规格.html` §3.14(4)/§3.15(4) 动态生成规则更新页面示例；已有图片保留；其他功能不动。
- 涉及文件：`service-miniapp/activity-list.html` + `owner-miniapp/activity-list.html`（两端 activities 数组完全一致，镜像改）。
- 改动（activities 数组，共 10 条 id1-11 缺9）：
  1. id4 (原 设计更新/设计图纸 v2.0)：type task→contract、typeLabel 设计更新→合同签约、content→'设计服务合同签约完成'、source 设计服务→设计服务合同。
  2. id11 (原 设计上传/效果图初稿)：同上处理（与 id4 同为'设计服务合同签约完成'，两处相同）。
  3. id7 (原 合同变更/对泥瓦分包合同提出了变更)：content→'泥瓦服务合同变更成功'、source 泥瓦分包合同→泥瓦服务合同（type 已 contract、typeLabel 合同变更 保留）。注：用户指令将 分包→服务，视为有意；与全项目约定「泥瓦分包合同」(architecture.html 等) 不一致，已向用户标注，可一键回退。
  4. id1/id5 (原 任务验收·完成了…的验收)：PRD 无「验收」标签 → typeLabel 任务验收→任务确认、content→'确认了{阶段}{任务}'（按 §3.15 单条确认文案，用户名由 user 字段独立渲染故 content 省略用户名）。
  5. id2 (原 任务提交·提交了…验收申请)：content→'完成了{阶段}{任务}的施工'（对齐 §3.14 提交动态文案）。
  6. id8 (原 任务开始·提交阶段开工申请)：typeLabel 任务开始→任务提交、content→'完成了{阶段}阶段开工 的施工'（PRD 无开工动态，按最近似 任务提交 对齐，属判断项）。
  7. id3 (任务完成·完成了水电水电改造 的施工)、id6 (合同签约·签署了 水电分包合同)、id10 (合同确认·确认了 木工分包合同 的签署文件) 维持不变（id3 已合规、id6/id10 为合同类动态不受 任务模块 规则约束）。
- 图片字段（hasMedia/mediaCount）全程未动，符合"已有图片保留示例使用"。
- 校验：两端 Grep 旧词（验收/开工申请/设计图纸/效果图初稿/设计更新/设计上传/任务开始/提出了变更）零残留；新词（设计服务合同签约完成×2/泥瓦服务合同变更成功/确认了…/完成了…的施工/任务确认×2/合同签约×2 for id4/id11）均就位；渲染函数与筛选逻辑未改动。

## PRD 项目模块：补充「今日动态卡片 / 项目动态页」动态生成规则（对齐合同·任务模块）
- 用户指令：依据 PRD-合同模块（§6.3.4/§10.3.3 签约动态）与 PRD-任务模块（§3.14(4)/§3.15(4) 任务动态）的生成规则，在 PRD-项目模块「今日动态卡片（§3.7）/ 项目动态页（§11 服务方、§13 业主）」补充相关动态项、页面逻辑、交互、字段规格及详细规则；无生成规则者不呈现（差异/删减项需确认后再执行）。
- 源模块权威动态类型（typeLabel / 枚举值 / content 模板）：任务提交(task_submitted)/任务完成(task_completed)「完成了{阶段}{任务}的施工」、任务确认(task_confirm)「{确认人} 确认了 {阶段}{任务}」、任务驳回(task_rejected)「{确认人} 驳回了 {阶段}{任务}」、合同签约(contract_signed)「{合同名称}签约完成」。
- 差异项确认（AskUserQuestion 三问）：①「合同审核(contract_audit)」源模块无动态生成规则 → 删除（§11.3 行 + §20.5 枚举移除）；②「创建了合同」动态源模块无规则 → 删除项目动态页呈现（§6.6.1 项目动态行移除，保留合同详情页「操作记录」日志）；③ §11.3 文案保留简洁展示模板，权威 content 写入新增汇总节（不重命名/不拆分行）。
- 改动（PRD-项目模块详细规格.html）：新增 **§11.11 动态生成规则汇总**——动态项汇总表（含生成时机/触发动作/content 模板/跳转/源章节）、通用字段模型表（user/role/typeLabel/content/source/时间·dateLabel/files/hasPermission）、页面逻辑与交互（生成时机一对一、媒体聚合、跳转按 type、可见性 hasPermission、今日动态联动）、删减项说明 note；
  - §11.3 移除「合同审核」行 + 加 note 指向 §11.11；TOC 加 11.11。
  - §20.5 枚举移除 `contract_audit`（project_activities）；补充 typeLabel/content/source/files/role/avatar/avatarColor/dateLabel/hasPermission 字段列。**注意**：`contract_audit` 仍保留于 §20.6 待办事项表（todos.type）——合同平台审核确实生成待办（§6.3.2），属合法待办类型，勿误删。
  - §6.6.1 删「项目动态」行（保留「操作记录」行）；§3.7 功能说明改引用 §11.11 + 加「动态生成规则来源」note；§13.2 加同源引用 note。
- 校验：Python HTML 标签平衡 0 错误；锚点 0 缺失（sub-11-11 已建且被引用）；contract_audit 仅存于 §20.6 待办表与「删减项」说明 note（§11.3/§11.11），§20.5 动态表枚举已清空；「XX 创建了合同」仅存 §11.11 删减说明。
- 修订（本轮）：用户指出 §11.3 与 §11.11 内容重复且不对齐（动态类型不同等）产生歧义。已**以 §11.11 为主将其融合进 §11.3 并删除 §11.11**：§11.3 重写标题「动态类型规格与生成规则（项目动态）」，吸收 §11.11 全部内容（动态项汇总表 5 类型 task_submitted/task_completed/task_confirm/task_rejected/contract_signed、通用字段模型、页面逻辑与交互），原 §11.3 简易 4 类型表（任务完成/任务提交/合同签署/任务确认结果 + 图标 +【任务名称】模板）删除；「删减项说明（按约定已移除）」重构为「排除项说明」当前规则（不写版本记录）。同步：删除 TOC 11.11 条目、TOC 11.3 改名、§3.7(1204/1251) 与 §13.2 链接由 #sub-11-11 改 #sub-11-3 并改文案；§3.7 note「已按约定」改为「无生成规则故不呈现」。校验：0 标签错误 / 0 锚点缺失 / #sub-11-11 引用清零 / 旧简易表文案清零。

## 泥瓦命名统一 + 原型全部待办页 + PRD 全部待办页补充（依据生成规则）
- 泥瓦命名统一：全项目仅 activity-list.html（service/owner 两处）将'泥瓦分包'误写为'泥瓦服务'，已统一改回'泥瓦分包'（content+source 字段），与 architecture.html/project-detail/PRD 等一致。
- 原型全部待办页（service/owner 两处 todo-list.html）：三项'已完成'任务（材料采购/防水施工/墙面基层处理）状态标签+元信息'已完成'→'已执行'（data-status=completed 保留以归入已处理）；各新增 1 条'待执行'示例（service: 地面找平·泥瓦工作组；owner: 墙面腻子批刮·油漆工作组）；硬编码角标 service 13/10→14/11、owner 15/12→16/13（owner 另由 updateTodoBadges() 按 DOM data-status 动态重算）。校验：两端 Grep '已完成' 零残留。
- PRD-任务模块 全部待办页（§4/§7）依据生成规则补充：① §4.3 待办类型表移除无小程序生成规则的'合同平台审核（平台审核中，属 PC运营端范畴，详见项目模块 §20.6 todos.type=contract_audit）'与'已完成任务（非独立生成事件，系既有待办完成归档）'——两项均经 AskUserQuestion 确认移除；② 补入 §6.3.2/§6.3.3 有规则但 §4.3 缺失的 3 类：合同修改重提（平台审核驳回→发起方）、合同变更签约（变更确认后→发起方）、变更合同修改（变更审核驳回→发起方）；③ 新增 §4.8 待办类型详细规则（4.8.1–4.8.11 共 11 类，含生成规则/页面逻辑/交互/字段规格，参照 §7.6–7.9 范式），§7.10 业主端范围与排除项（默认不含任务执行/架构邀请/发起方视角合同类，业主被配置为执行人时见 §6.3.1 例外）；④ §4.5 状态标签补'已处理中已完成态按实际动作呈已执行/已确认/已上传'。
- 校验：Python HTML 标签平衡 div 560/560、table 74/74；sub-4-8-1~11 与 sub-7-10 锚点均存在且被 §7.10 引用；§4.3 现为 10 类、全部有生成规则支撑。
- 注：'合同平台审核' 待办（如有）属 PC运营端审核人视角，存于项目模块 §20.6 todos.type=contract_audit，与小程序全部待办 §4.3 不同范畴，故 §4.3 移除不影响项目模块数据模型。
