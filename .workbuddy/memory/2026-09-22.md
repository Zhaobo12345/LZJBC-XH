# 2026-09-22

## 成员页按架构三层级组织（方案 A 演示页，先演示不改原页）
- 需求：项目成员页对应「架构」模块（三级：项目总架构 L0 / 工作组 L1 / 子施工组 L2），原成员平铺同一层级，需「标识层级、突出各层级人员」。用户确认：方案 A（三级分区）+ 仅服务端 + 先新页面演示、不改原页。
- 新建 `service-miniapp/member-levels-demo.html` = `service-miniapp/member.html` 完整复制后改造（原页零改动，已 git 确认）：
  - 数据层：每位成员补 `level`(0/1/2)；L2 的孙八/钱九补 `subGroup`(强电/弱电施工组)，`group` 仍=父工作组 water，使第三层真正存在（共 9 人：L0×3 / L1×4 / L2×2）。
  - `archLevelModel` 三级模型，配色复用架构页 蓝#1890FF / 绿#52C41A / 紫#722ED1。
  - `renderMemberList` 重写：每级一个 `.level-section`（标题带 L0/L1/L2 徽标+人数），层级内按工作组/子组细分 `.sub-group-header`，成员卡为主。
  - 成员卡新增 `.member-level-tag`（蓝/绿/紫）标识所属组/子组；筛选/搜索/邀请/权限弹窗与权限演示开关全部交互保留。
  - nav 标题加「（演示）」；框外左侧 `.member-demo-note` 说明卡（原页未改）；筛选栏「按组」交互保留（L2 随父组 water 一起筛出）。
- 校验：HTML BALANCED；CSS 两块 259/259、7/7；内联 JS `node --check` OK。
- 待用户确认：确认后把三级分区回迁 `service-miniapp/member.html`（沿用 architecture 演示页回迁法：先洗 data-page-node-id 后 diff，断言防静默）；业主端 member.html 本次未要求改。

## 删除演示页 member-levels-demo.html（用户：不符合预期）
- 已删除 `service-miniapp/member-levels-demo.html`；删除前 grep 全库确认无任何页面引用它，原 `member.html` 完好（git 未改动）。
- 安全做法：先 `cp` 备份到 `.workbuddy/_tmp/_deleted_backup/`（可恢复），再 `rm` 原型目录文件。该文件从未入库（git status 为空），删除不影响版本库与其他页面/功能。
- 当前状态：原型恢复为仅有正式 `member.html`；若后续要做三级分区，需重新出方案并在正式页实施（不再走演示页）。

## member.html 加架构层级标识（L0/L1/L2 徽标，不改整体布局）
- 需求：在正式 `service-miniapp/member.html` 给各架构层级加标识（项目部 L0、水电/泥瓦/木工/油漆工作组 L1、强电/弱电施工组 L2），先加看效果；约束「不改变整体布局」。
- 改法（最小增量，原页已 `cp` 备份到 `.workbuddy/_tmp/_deleted_backup/`）：
  - `groupNames` 各加 `level`（project=0, water/tile/wood/paint=1）；新增 `LEVEL_META`(0/1/2 配色=蓝#1890FF/绿#52C41A/紫#722ED1，复用架构页) 与 `memberLevel()/memberSubGroup()`（由 `role` 推导，强电/弱电=2）。
  - CSS 新增 `.level-badge`(组标题 L0/L1 小药丸+圆点) 与 `.member-level-tag.lv2`(卡内 L2 紫标)。
  - 两处组标题渲染（`renderMemberList` + `handleSearch` 搜索结果）统一追加 `level-badge lv{level}`，title=层级全名。
  - `renderMemberCard`：仅 L2 成员（孙八/钱九）卡内追加 `L2 · 强电施工组/弱电施工组` 紫标；L0/L1 由所属组标题标识，避免冗余。
- 校验：HTML BALANCED；CSS 两块 248/248、7/7；内联 JS `node --check` OK。diff 纯增量。
- 待确认：用户看效果后决定是否要 L2 子组独立成小分区，或统一给每张卡加层级标。

## member.html 移除成员 role 的「·」后缀（无逻辑来源）
- 需求：成员名字下方标识（如「项目部·业主代表」「水电工作组·组长」「强电施工组·施工人员」）的「·」后方职务文字无逻辑来源，需取消，仅保留「·」前方组名。
- 改法：数据源 `members` 9 处 `role` 改为纯组名（项目部 / 水电工作组 / 强电施工组 / 弱电施工组 / 泥瓦工作组 / 木工工作组 / 油漆工作组）。`memberLevel()/memberSubGroup()` 仍靠「强电/弱电施工组」字样判 L2，不受影响；`meta`、`goToProfile`(modalRole)、`actionModalRole`、搜索匹配同步变干净。平台用户 `pu*` 为独立数据且无「·」，未动。
- 校验：HTML BALANCED；成员 role 残留「·」=无；`node --check` OK；层级推导 = [0,0,0,1,2,2,1,1,1]（正确）。

## member.html 去重 L2 成员卡的工作组名
- 问题：L2 成员（孙八/钱九）卡上「强电/弱电施工组」在 `.meta` 行与紫色 `L2 · 强电施工组` 标签两处重复。
- 改法：保留 `.meta` 组名（与其他成员一致），L2 标签精简为纯层级标识「L2」（`title="L2 · 子施工组"` 悬停提示）；并删除因此闲置的 `memberSubGroup()` 死代码。
- 校验：HTML BALANCED；`node --check` OK；`memberSubGroup` 残留=False。

## member.html L2 标签改到组名同行（去独占行）
- 问题：去重后 L2 紫标是独立 `<span>`，排在块级 `.meta` div 之后，独占一行。用户要求与工作组名同一行。
- 改法：`renderMemberCard` 把 L2 标签移入 `.meta` 行内（`<div class="meta">${member.role} <span...>L2</span></div>`）；CSS `.member-level-tag` 去掉 `margin-top:6px` 改 `margin-left:6px` + `vertical-align:middle`，确保行内同行。
- 校验：HTML BALANCED；`node --check` OK；L2 已在 `.meta` 行内；无关 `margin-top:6px` 仅剩 `.demo-control-panel .status-text`（演示权限面板），不影响。

## PRD 补充「成员架构层级标识」规则（§七 7.14 + §十六 16.2 引用 + TOC）
- 需求：将项目成员页中架构层级（L0/L1/L2）展示规则补入 PRD。口径对齐 §6.3 架构层级规格示例（L0 项目部/蓝#1890FF、L1 工作组/绿#52C41A、L2 施工组(子施工组,强电/弱电)/紫#722ED1）。
- 改法（仅 PRD-项目模块详细规格.html，原页/原型零改动）：
  - 新增 §七 小节 `7.14 成员架构层级标识规格`（id=sub-7-14）：说明成员列表按架构三级层级组织展示、层级仅作标识不喧宾夺主；表格列「架构层级/标识位置/标识形式/配色/说明」——L0/L1 由组标题统一带圆点+药丸徽标（悬停全称），L2 子施工组在成员卡姓名下方组名同行加紫色"L2"小标（不独占行）；note 列明配色复用架构页、层级推导依据 group/subGroup（强电/弱电字样判 L2）、筛选与搜索结果同步带徽标、原型页 service-miniapp/member.html。
  - §十六 16.2 功能列表补一条引用 7.14（业主端"与服务方保持一致"口径）。
  - 顶部 TOC 补 `7.14 成员架构层级标识规格` 条目（line 418 之后）。
- 校验：HTML BALANCED；CSS 64/64；新锚点 sub-7-14/a7_14_title/a16_2_lvl/a7_14_toc 均在；data-page-node-id 无重复。
- 备注：`shared/doc-viewer/doc-data.js` 是 PRD 的早前生成缓存快照，未重新生成，故「查看设计文档浮窗」暂不显示 7.14；如需同步需重跑 `.workbuddy/_tmp/gen_doc_data.py`（重生成 781KB doc-data.js），待用户确认再执行。

## PRD 补充「成员卡权限角标」展示规则（§七 7.15 + §十六 16.2 引用 + TOC）
- 需求：成员卡右侧两行权限角标（截图：数据:全部/治理:全部、数据:全部/治理:部分、数据:公开/治理:无、数据:所在层级/治理:部分）需在 PRD 内补充展示规则。
- 事实来源（`service-miniapp/member.html` 实现口径，`renderMemberCard()`）：
  - 数据权限 4 档 → 角标：`all`→「数据: 全部」、`level`→「数据: 所在层级」、`level_with_contract`→「数据: 层级含合同」、`none`→「数据: 公开」；弹窗选项文案分别为 全部数据 / 所在架构层级 / 所在架构及下属层级权限（含合同）/ 仅基础公开数据。
  - 治理权限 3 态 → 角标：含 `all`→「治理: 全部」、非空不含 all→「治理: 部分」、空→「治理: 无」。
  - 配色：`.permission-tag.data`=蓝底蓝字、`.permission-tag.manage`=绿底绿字、`.permission-tag.none`=灰底灰字（数据 none 与治理无共用灰）；角标在卡最右纵向两行（上数据下治理）右对齐，再右为「›」箭头。
  - 可见性：角标文案恒展示成员自身配置，不随当前登录用户权限变；变的是可点击性/箭头（见 7.11：无权限→整区置灰不可点、箭头隐藏）。
- 改法（仅 PRD-项目模块详细规格.html，原型零改动）：
  - 新增 §七 小节 `7.15 成员卡权限角标展示规格`（id=sub-7-15）：7.15.1 数据权限角标映射表（key/角标文案/弹窗文案/说明）、7.15.2 治理权限角标映射表（判定条件/角标文案/说明）、7.15.3 角标配色表、7.15.4 展示与交互规则（位置/展示恒定/点击/保存回写/弱化语义），note 说明与 7.8 分工（7.8=弹窗内可配置项，7.15=卡片外层角标口径）。
  - §十六 16.2 功能列表补一条引用 7.15。
  - 顶部 TOC 补 `7.15 成员卡权限角标展示规格` 条目。
- 校验：HTML BALANCED；CSS 64/64；新锚点 sub-7-15/a7_15_title/a7_15_toc/a16_2_permbadge 均在；data-page-node-id 无重复；div 760/760。
- 备注：doc-data.js 快照仍未重生成（7.14+7.15 均未入浮窗），需同步时一并重跑 gen_doc_data.py。
