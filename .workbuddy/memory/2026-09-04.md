# 2026-09-04 工作日志

## 工人合同详情「拟定中（发起方）-新」页面：阶段任务布局修正 + 预览合同标准版式

- 文件：service-miniapp/worker-contract-draft-initial-new.html
- 阶段任务布局对齐「拟定中（发起方）」状态页：
  - 删除本页内联阶段任务样式，复用 css/contract-detail.css，使 stage 卡片、开关、添加/删除按钮、任务输入项与参考页完全一致（开关改为 transform 动效，「+ 添加任务」左对齐绿色胶囊、「× 删除阶段」红色胶囊）。
  - 顺手清理该页原型导航中重复的「项目主页（进行中）」条目（残留 2 条→1 条）。
- 预览合同内容调整：
  - 新增 `syncContractFromInputs()`，预览前同步页面内「合同名称 / 金额 / 工期 / 违约责任 / 补充条款」当前值。
  - 新增 `buildContractBasicInfoHTML()`，生成「合同基本信息」条款（合同名称、类型、地点、金额、工期、架构层级、甲方、意向乙方）。
  - `buildFullContractHTML()` 开头增加合同类型标题，并按「基本信息 → 合同正文（含违约责任） → 补充条款 → 阶段任务 → 合同附件 → 签署信息」的顺序组合为标准线下合同布局。
- 预览合同「乙方」字段改为状态驱动（2026-09-04 补充）：
  - 新增 `currentStatus`（默认 'worker_draft_initial'，非签约态）与 `getPartyB()` 辅助。
  - 非已签约：`乙方（承包方）` 标签、值置空（不再显示「意向乙方」字样与候选名）。
  - 已签约（currentStatus='signed'/'signed_done'）：`乙方（承包方）` 标签、值为实际乙方姓名（优先 C.signedPartyB，回退 editInvited）。
  - 基本信息章节与签署信息章节均改用 `getPartyB()`，移除原「意向乙方（承包方）」文案。
- 阶段任务三项标准 + 任务点击编辑 + 补充条款（2026-09-04 补充）：
  - `STAGE_TEMPLATES` 全部 6 工种、约 34 个任务均补充 `execStd`（执行标准）/ `confStd`（确认标准）/ `liableStd`（担责标准）默认内容。
  - 新增「编辑任务」弹窗 `editTaskModal`（HTML + 复用 css/contract-detail.css 的 `.edit-task-modal` 样式），交互对齐 `worker-contract-detail.js` 的 `editTaskDetail`：任务行可点击 / ✎ 打开弹窗，含任务名称、执行人（单人下拉搜索）、确认人（多人，最多5）、执行/确认/担责三项标准；保存回写 `C.stages`。
  - `renderDraftStageTask` 任务输入框改 `readonly` 且整行 `onclick` 打开弹窗（对齐参考页），新增 `.task-edit-item{cursor:pointer}` 与 hover 态。
  - 预览合同 `buildFullContractHTML` 每个任务渲染「执行标准/确认标准/担责标准」，并补充 `.wc-task-block` 样式。
  - 补充条款：`C.extraClauses` 与 `#editContractExtra` 默认值改为真实条款内容；预览「补充条款」章节改为始终渲染（空则显示「（无）」）。
- 校验：div 平衡 OK、node --check 内联 JS 语法 OK。未改动其他页面/功能。

## 工人合同详情「拟定中（发起方）-新」页面：阶段任务布局对齐 + 预览合同

- 文件：service-miniapp/worker-contract-draft-initial-new.html
- 阶段任务布局对齐「拟定中（发起方）」状态页（worker-contract-detail.html?status=worker_draft_initial）：
  - stage 卡片 header 两行外包裹 `.stage-card-header`（灰底 #f5f5f5 + 底边框，形成"标题栏"带），`.stage-card` 改 `padding:0; overflow:hidden; background:#fff`，`.task-edit-list` 加 padding；`.sw`→`.switch` 类，新增 `.stage-action-btn.add`（绿色）样式。
- 新增「预览合同」入口与功能（对齐参考页 previewContract）：
  - 合同内容卡片内（附件区后）加 `.wc-preview-btn`（👁️ 预览合同）。
  - 新增全屏 `.fulltext-modal`（`wcFullTextModal` / `wcFullTextTitle` / `wcFullTextContent`）+ 配套 CSS。
  - 新增本地 `previewContract() / closeFullText() / buildFullContractHTML() / buildContractBodyHTML()`，基于本页数据模型 `C`（含违约责任取 `breachInput`、补充条款、阶段、附件、甲乙签署信息）。
- 校验：div 平衡 OK、node --check 内联 JS 语法 OK。未改动其他页面/功能。

## 删除「项目详情（工人）」页面

- 指令：删除 project-detail-worker.html 页面，对应导航项同步删除。
- 关联影响排查：外部仅 initial-v2 / completed-v2 / empty-demo / project-list 四处我之前补的「项目详情（工人）」导航项，无 JS/href 跳转引用；initial-v2 的"演示开关"为页内切换不依赖该页；PRD 未提及。
- 执行：移除 4 处导航项 + 删除主文件（无配套 js/css）。删除后全仓库 grep `project-detail-worker` 零残留。
- 影响：服务方·工人视角项目详情页不再独立存在；工人视角现仅由 initial-v2 的"演示开关"页内模拟。PRD 同步待确认（用户未要求）。

## 原型导航标签「项目列表」→「项目主页（进行中）」

- 指令：导航项内"项目列表"改为"项目主页（进行中）"，其他不改变。
- 范围：service-miniapp 全部工作 .html（不含 .bak），精确替换 `>项目列表</a>` → `>项目主页（进行中）</a>`，共 37 处 / 22 个文件。
- 不动项：href 不变；"项目列表（空状态演示）"条目保留原样；不修导航重复项（用户要求仅改名）。

## 修复原型导航重复项（全局替换污染）

- 根因：删除 4 个旧详情页（project-detail.html / new.html / ongoing-v2 / completed.html / initial-worker.html）时做的「全局引用替换」把导航里的已删除页 href 也改成了 `project-list.html` / `project-detail-initial-v2.html`，不同原条目塌缩成同一 href 产生重复。
- 修复文件（5 个，均 div 平衡校验通过）：project-list.html、project-detail-initial-v2.html、project-detail-completed-v2.html、project-detail-worker.html、project-list-empty-demo.html。
- 处理：删除重复「项目列表」/「项目详情（初始-新版）」条目；移除指向已删除页的入口；统一三个存活详情页入口（初始-新版 / 已完成-新版 / 工人），各页 active 标记归位。
- 注：为闭环导航，在 project-list / initial-v2 / completed-v2 / empty-demo 导航补了「项目详情（工人）」入口（worker 页导航本就指向另两页）；如不需要可移除。

## project-detail-initial-v2 修正「新家准备中」非主体状态

- 问题：本页把 `'新家准备中'` 当作项目主体状态（数据默认 status、切换面板筛选、工人卡副文案），但项目主体状态规范（源自 project-list.html）只有 `进行中` / `已完工`（含 `已完成`），不存在「新家准备中」。
- 修复（布局已与 project-list 统一，仅修正状态口径）：
  1. 数据默认 `status: d.status || '进行中'`（读取 localStorage.newProjectData.status，缺省按项目列表规范回退为 进行中）；
  2. 切换面板 `ongoing` 筛选改为仅 `p.status === '进行中'`（移除 `|| 新家准备中`）；
  3. 工人卡副文案 `'<div class="wr-sub">' + p.status + '，待工长分配任务、发起合同</div>'`（动态引用主体状态）；
  4. CSS 注释去掉「橙色「新家准备中」变体」误导描述。
- 校验：div 173/173 平衡、script 1/1、node --check 内联 JS OK；全页「新家准备中」残留=0；状态徽标现在正确显示主体状态「进行中」（蓝色，与项目列表一致）。

## project-detail-initial-v2 顶栏/下拉面板与项目列表统一（移除内联面积）

- 问题：顶栏用独立 `proj-meta-row` 内联展示「75.0m² 2室1厅」，与 project-list 顶栏（`proj-switch-hero`「项目信息」文案 + ›）不一致；下拉面板地址行误用 `p.area` 占位（project-list 用 `p.address`）。
- 修复（参考 project-list.html）：
  1. 顶栏 HTML 改回与 project-list 同构：`proj-switch-left`(项目名+状态) + `proj-switch-hero#topHero`(项目信息) + `info-arrow›`；删除 `proj-meta-row`/`proj-area`。
  2. `renderSwitchBar` 改为 `topHero.textContent='项目信息'`（移除已失效的 curProjArea 设置）。
  3. `newProject` 数据补充 `address: d.address || 'XX小区***号***室'`。
  4. `renderPanel` 下拉 `po-addr` 改用 `p.address`（与 project-list 一致）。
  5. 删除 `.proj-meta-row` 死 CSS。
- 校验：div 173/173 平衡、node --check 内联 JS OK；proj-meta-row/proj-area/curProjArea 残留均=0、topHero 就位、po-addr 用 address。面积信息现仅在 project-info 页展示（与 project-list 口径一致）。

## project-detail-initial-v2 修复顶栏错位（回归单行）

- 问题：截图显示顶栏元素未在一行，「项目信息」与「›」垂直堆叠在项目名/状态下方；根因是 `.proj-info-zone` CSS 写成了 `flex-direction: column; align-items: flex-start; gap: 2px`，与 project-list 的横向单行布局不一致。
- 修复：将 `.proj-info-zone` 样式改为与 project-list 完全一致：`display: flex; align-items: center; gap: 8px; padding: 8px 10px;`（移除 `flex-direction: column` 与 `align-items: flex-start`）。
- 校验：div 173/173 平衡、node --check 内联 JS OK；`.proj-info-zone` 现在无 `flex-direction`，`align-items: center`、`gap: 8px` 就位，布局回归单行。
- 工人视角统一（project-detail-initial-v2.html）：将演示开关切换出的工人态 `renderInitialWorker` 重写为与 project-list.html `renderWorker` 完全一致的结构——① 待办事项（todo-card worker，0 待办 empty-state）；② 我的任务（my-task-card worker，0/0）；③ 吸顶快捷入口 项目动态/架构/成员/资料（4 项，去「全部任务/我的合同/更多」及更多面板）；④ 层级默认选中 `p.trade||'泥瓦'工作组` 并带「默认我的工作组」hint；⑤ stats-row（合同 contract-half worker 空态 + 任务统计 task-half worker 0/0/0/0）。移除旧 worker-role-card / todo-group-label / worker-more-panel 的 JS 与死 CSS，删除 scrollToEl/toggleWorkerMore 废弃函数。补齐 `.todo-card.worker` / `.level-default-hint` CSS。校验：div 190/190 平衡、node --check OK、死样式全清。

## project-detail-completed-v2 删除冗余「项目已完工」横幅

- 需求：页面布局参考「项目列表」页，删除非工人/工人视角均冗余的「项目已完工...」行。
- 分析：project-list.html 仅在 `proj-info-zone` 内用 `curProjStatus` 绿色 `.done` 徽标承载「已完工」状态，无独立绿色横幅；completed-v2 额外加了 `project-basic-header`「🎉 项目已完工」横幅属冗余（且 `curProjStatus` 已正确置「已完工 · done」）。
- 修改：移除 renderNonWorker/renderWorker 内的 `(isDone ? '<div class="project-basic-header"...' : '') +` 横幅行（两视角）；删除 `.project-basic-header` 整段死 CSS。
- 校验：JS vm.Script 语法 OK；project-basic-header HTML+CSS 残留均=0、🎉 残留=0、横幅 leftCount=0；curProjStatus 仍置「已完工」绿色 `.done` 徽标（状态提示保留，与项目列表一致）。

## project-list.html 原型导航新增两项

- 需求：在服务方小程序「项目列表」页右侧原型导航内，增加「项目详情（初始-新版）」「项目详情（已完成-新版）」两个导航项，对应已有页面。
- 修改：在 `project-list.html` 原型导航区（`<a href="project-detail-new.html">初始项目详情页</a>` 之后、`project-info.html` 之前）追加：
  - `<a href="project-detail-initial-v2.html">项目详情（初始-新版）</a>`
  - `<a href="project-detail-completed-v2.html">项目详情（已完成-新版）</a>`
- 校验：div 273/273 平衡；两链接均 present，目标文件存在（Glob 确认）。

## 删除 4 个旧项目详情页（含代码 + 原型导航清理）

- 需求：删除「项目详情」(project-detail.html)、「初始项目详情页」(project-detail-new.html)、「项目详情（进行中-新版）」(project-detail-ongoing-v2.html)、「项目详情（已完成）」(project-detail-completed.html) 四页（含代码），并清理原型导航引用，避免影响后续 PRD 与最新页面同步；不影响其他存活页面/功能。
- 影响分析：四页被大量存活页面引用——含功能跳转（home 项目卡、create-project 创建成功、message 卡片、invite-join detailHref、各「←返回」按钮）与 ~20 页原型导航 `<a>`。先询问用户重定向方案。
- 用户确认重定向映射：project-detail.html 与 project-detail-ongoing-v2.html → project-list.html；project-detail-new.html 与 project-detail-completed.html → project-detail-initial-v2.html（原型导航链接同此映射替换）。
- 执行：脚本对 service-miniapp 下全部 live *.html（排除 .bak）做 16 条精确替换（导航 `<a>` 9 种标签变体 + 功能跳转 location.href/detailHref），共 48 处；补修 message.html:734 漏网链接（标签「项目详情」指向 ongoing-v2，改为项目列表）；`rm` 删除 4 个文件。
- 结果：存活页对该 4 页的 live 引用归零（仅 invite-join.html:462 `../owner-miniapp/project-detail.html` 为跨小程序引用，属 owner-miniapp 文件、超出 service-miniapp 范围，保留）；initial-v2/completed-v2 自链（active）保留，6 个关键文件 div 标签均平衡。
- 注意：各 .bak 备份文件仍含旧引用（备份，不处理）；owner-miniapp/project-detail.html 是否同步删除属另一小程序范围，未动。PRD 同步需另做。

## 预览合同弹窗补全微信小程序默认导航栏

- 文件：service-miniapp/worker-contract-draft-initial-new.html
- 问题：全屏预览合同弹窗顶部缺失状态栏（时间/信号/电池）与原生胶囊占位。
- 改动：
  - 弹窗 `#wcFullTextModal` 顶部新增 `.status-bar`（9:41 / 📶 🔋）与 `.nav-bar`（返回 + 居中标题 + `.nb-actions` 原生胶囊占位）。
  - 内联补充 `.fulltext-modal .nav-bar` / `.nb-actions` / `.native-capsule` 样式，标题采用 flex 居中、胶囊绝对定位右侧，与新版 project-list 等页面导航栏一致。
  - 删除旧的 `.fulltext-header` / `.fulltext-back` / `.fulltext-title` 简化结构。
- 校验：div 平衡 OK、node --check 内联 JS 语法 OK。

## 编辑任务「执行人默认值 + 工人合同签约自动归属乙方」规则（JS + PRD）

- 指令：非工人/工人合同详情页编辑任务弹窗，校验「乙方仅一人→执行人默认乙方人员；多人→执行人为空（非必选）」；工人合同乙方确认接单→已签约时，无执行人任务执行人更新为乙方人员；规则补入 PRD；其他暂不改动。
- 改动文件（仅 2 个 JS + PRD，未触 HTML 页面结构）：
  - `service-miniapp/worker-contract-detail.js`：新增 `getPartyBList()`（已确认/已签约取 partyBName 唯一；拟定/邀请取意向乙方名单 1~3）；`editTaskDetail` 中任务无执行人且 partyBList 仅 1 人时预填乙方；新增 `assignDefaultExecutorsOnSigned()`（遍历 stages 将空执行人任务置为 partyBName）；`updateStatus` 在进入 `worker_signed` 且上一态非 signed 时触发一次。
  - `service-miniapp/js/contract-detail.js`：新增 `getPartyBList()`（读 `editPartyB` 单选下拉选中项，取括号前姓名）；`editTaskDetail` 中同样逻辑预填（非工人合同乙方为单选，恒为 1 人，故恒默认乙方；多人分支为通用兜底）。
- PRD：`PRD-合同模块详细规格.html` §3.5.2.1 增加「编辑任务·执行人默认值规则（非工人/工人通用）」绿框说明；§7.5.3.5 `worker_signed`「关键逻辑」增加「乙方确认接单→已签约：无执行人任务自动归属乙方」条目（仅首次进入已签约触发一次）。
- 校验：`node --check` 两个 JS 文件均 OK；仅改 JS 与 PRD，未改动其它功能/HTML。

## 修复「拟定中（发起方）-新」新增任务执行人/确认人默认值（按乙方人数）

- 文件：service-miniapp/worker-contract-draft-initial-new.html（仅此页；参考页 worker-contract-detail.js 此前已符合，确认无需改）
- 问题：新增任务/阶段时硬编码 `{exec:'张水电', conf:'陈庄'}`，导致：① 1 名乙方时确认人（陈庄）被默认填入（应为空）；② 2~3 名乙方时执行人仍默认张水电（应按上条需求为空）。
- 修复：
  - 新增 `getDraftPartyBList()`（取当前意向乙方 editInvited 姓名）。
  - `addDraftTask` / `addDraftStage`：新增任务 `exec = 乙方仅 1 人 ? 该乙方 : ''`、`conf: ''`（确认人一律空）。
  - `editTaskDetail` 增加与参考页一致的执行人默认值推导：仅当 exec 为空且乙方仅 1 人时预填该乙方；确认人恒为空。
- 注意：STAGE_TEMPLATES 内各工种模板任务的 `exec/conf` 为模板自带内容（如水电模板默认执行人张水电、确认人陈庄），属有意预设，未改动（用户反馈仅针对「+ 添加任务」新增任务，非模板加载）。
- 校验：div 平衡 OK、node --check 内联 JS OK；全仓 grep 残留 `exec:'张水电',conf:'陈庄'` 仅位于 STAGE_TEMPLATES 模板段（预期内）。

## 「拟定中（发起方）-新」更换正文模板预览改为"违约责任 + 固定详细条款"两区

- 文件：service-miniapp/worker-contract-draft-initial-new.html（仅此页）
- 需求：更换正文模板弹窗点「预览」打开的 wcdPreviewModal，正文模板预览需拆为「违约责任」「固定详细条款」两个区域；固定详细条款仅含标准条款项（双方权利义务/质量标准/验收/争议解决），不含甲乙方/金额/工期基础信息，且不可修改（只读）。
- 改动：
  - 删除原 `buildTemplateTextPreview(id)`（含甲乙方/工程概况/金额/工期/付款占位），改为 `buildTemplatePreviewSections(id)` 返回 `{breach, fixed}` 两段内容。
  - `previewDraftTemplate(kind==='text')` 渲染为两块 `.preview-section`：① 违约责任（sec.breach）；② 固定详细条款（sec.fixed，标题后加"（不可修改）"灰标）；底部加注"甲乙方 / 合同金额 / 工期 等基础信息由合同主信息填写，不在固定条款内。"
  - 固定详细条款区已不含甲乙方/金额/工期；预览本就是只读展示，符合"不支持修改"。lite 版固定条款=质量要求+争议解决；标准版=双方权利义务+质量标准+验收标准+争议解决。
- 校验：div 平衡 OK、node --check 内联 JS OK；全仓无残留 `buildTemplateTextPreview` 引用（仅新函数 2 处）。

## 新建「工人合同详情·确认中（发起方）-新」只读页

- 文件：service-miniapp/worker-contract-confirming-initiator-new.html（新建，由 `worker-contract-draft-initial-new.html` 复制改造，49500 字节）
- 需求：工人合同详情内「确认中（发起方）」状态页，按「拟定中（发起方）-新」页面的状态/布局/字段规则同步；保留确认中状态、仅查看不可编辑、保留撤回确认功能。
- 方案：经 AskUserQuestion 确认采用「新建独立-新页」方式，蓝本复制保证样式 100% 一致、零重建风险。
- 改动要点：
  - 状态进度条：拟定中→`wf-step done`、确认中→`wf-step current`、已签约→默认；首根 `wf-line` 加 `done`。
  - 横幅改 `.wc-banner confirming`（蓝色渐变），文案提示「已提交确认，等待对方（意向乙方）确认，本页仅查看不可编辑，可撤回确认后回拟定中修改」。
  - 甲方/意向乙方改为静态只读 chip（甲方「陈庄（工长）」、乙方「张水电（水电工）」），移除 `party-a-row` picker / `member-picker` / `inviteEditChips` 等可编辑结构。
  - 合同信息三个 input 加 `class="edit-input ro"` + `readonly`，去「* 必填」标记；金额 12600.00。
  - 合同内容卡：移除「更换正文模板」按钮；违约责任 textarea 改 `edit-textarea ro readonly`；补充条款 textarea 改 `ro readonly`；阶段任务 Tab 替换为静态 `.ro-stage-list`（3 项：水电开槽及布线/管道敷设与打压测试/竣工验收 = 已完成/进行中/待开始）；附件 Tab 替换为静态 `.attachment-list`（2 文件）；移除预览合同按钮。
  - 底部操作栏：单按钮 `<button class="cta-btn cta-warning" onclick="openConfirm()">撤回确认</button>`；新增 `#confirmMask` 撤回确认弹窗（取消/确认撤回，调 `doWithdraw()`）。
  - `<script>` 精简为只读版：`switchDraftTab`、只读 `renderMoreOps`/`openInfoModal`、`openConfirm`/`closeConfirm`/`doWithdraw`（`setTimeout` 跳 `worker-contract-draft-initial-new.html`）、`togglePageNav`/`toggleStatusGroup`。
  - 新增 CSS：`.wc-banner.confirming` 蓝渐变、`.edit-input.ro,.edit-textarea.ro` 灰底只读、`.cta-warning` 橙渐变、`.ro-stage-list/.ro-stage/.ro-stage-state.*` 阶段任务只读样式。
  - 原型导航：新增 `<a class="status-switch-item active" href="...confirming-initiator-new.html">确认中(发起方)-新</a>`（仅此 active）；原「确认中(发起方)」普通链接指向新页。
- 联动修改（同一状态口径）：
  - `worker-contract-draft-initial-new.html:692` → 确认中(发起方) 指向 `worker-contract-confirming-initiator-new.html`。
  - `worker-contract-detail.html:1211` → 发起方状态切换「确认中(发起方)」由 `data-status=worker_inviting_sender onclick=WCP.updateStatus` 改为 `<a class="status-switch-item" href="...confirming-initiator-new.html">确认中(发起方)</a>`。
  - 注：worker-contract-detail.js 中 `worker_inviting_sender` 的 `worker_withdraw` 撤回逻辑保留未删（其他入口可能仍依赖），不影响本次新页。
- 校验：JS vm.Script 语法 OK；确认中 banner/current 就位、拟定中 done、甲方陈庄/乙方张水电只读 chip、3 个 ro+readonly input、breach readonly、更换模板按钮已移除、撤回确认按钮+confirm modal、只读阶段任务列表、只读附件、nav active=确认中-新、旧编辑函数（doSubmit/doSave/partyAPanelBox/toggleInvitePanel）零残留均 true。

## 「拟定中（发起方）-新」阶段任务模板预览：去执行/确认人，任务项点击就地展开三项标准

- 文件：service-miniapp/worker-contract-draft-initial-new.html（仅此页）
- 需求：更换阶段任务模板弹窗点「预览」打开的 wcdPreviewModal（kind='stage'）中，阶段任务明细不应展示执行人/确认人；点击任务项应展示该任务的执行标准、确认标准、担责标准。
- 根因（多层浮窗被遮）：`wcd-modal-mask`（模板选择 wcdTplModal + 预览 wcdPreviewModal）与 `info-modal-mask`（查看标准）同为 z-index:320，且 info 浮窗在 DOM 中先于预览浮窗定义 → 同层叠时预览浮窗压在查看标准之上 → 标准浮窗被遮盖/沉底。
- 优化方案（用户选定）：**预览内联展开（手风琴）**，不新增浮窗层。
  - 阶段任务明细任务行：删除原 `执行：xxx · 确认：xxx` 文本；改为可点击卡片（右侧"查看标准 ›"），`onclick="toggleTemplateTaskDetail(i, ti)"`。
  - 每个任务行下方预置隐藏 `tplTask_<i>_<ti>` 展开面板，含 执行标准/确认标准/担责标准 三段（空值"（未填写）"）。
  - 新增全局 `toggleTemplateTaskDetail(si, ti)` 切换 `display` 并切换 toggle 文案「查看标准 › / 收起 ‹」。
  - 删除旧的 `showTemplateTaskDetail` 与全局 `previewStages`（不再需要信息浮窗）。
  - 预览区 `.wcd-sheet-body` 本就 `overflow-y:auto`，展开后内容可滚动不溢出。
- 校验：div 平衡 OK、node --check 内联 JS OK；全仓无残留 `showTemplateTaskDetail`/`previewStages` 引用。
- 备注：参考页 worker-contract-detail.js 的 `previewStageTemplate` 本就只显示任务名、不含执行/确认人（无需改）；"预览合同"全屏页（buildFullContractHTML）阶段任务区仍保留执行/确认+三项标准，属不同页面，本次未动。

## 修复「确认中（发起方）-新」页：更多菜单缺导出 + 阶段任务布局（2026-09-04 补充）

- 文件：service-miniapp/worker-contract-confirming-initiator-new.html（沿用上条新建页）
- 问题（用户反馈）：① 右上「更多」浮窗缺「导出合同文件」项；② 阶段任务布局不对——原 `.ro-stage-list` 仅列 3 个阶段名、无任务、无点击查看详情。
- 修复（对齐原「确认中（发起方）」worker-contract-detail.js 的 renderStagesSection / 更多菜单）：
  - **更多菜单**：`renderMoreOps()` 增加 `导出合同文件`（onclick `exportContract(); closeMoreOps()`）；新增 `#exportModal` 弹窗（导出为PDF / 分享到微信，复用 contract-detail.css 的 `.export-modal`/`.export-option`）+ `exportContract/showExportModal/closeExportModal/exportToPDF/shareToWechat` + `closeMoreOps`。
  - **阶段任务**：移除 `.ro-stage-list` 死结构/CSS，改由 `renderStages()` 从 `STAGES`（水电工种模板：材料进场/布管布线/安装/收尾 4 阶段共 9 任务）渲染「阶段 + 任务」结构（复用 contract-detail.css 的 `.stage-item/.stage-header/.stage-tasks/.task-item`）。阶段头可折叠（`toggleStage`），任务可点击（`viewTaskDetail`）弹出 `#taskDetailModal` 只读任务详情（任务名称/执行人/确认人/执行·确认·担责标准）；非签约态（确认中）任务用 `viewTaskDetail` 弹窗（与原 `worker_inviting_sender` 一致，不跳 task-detail.html）。
  - 清理：删除 `.ro-stage*` 11 行死 CSS，新增 `.task-item{cursor:pointer}` 点击态（与原页交互一致）。
- 校验：JS vm.Script 语法 OK；div 211/211 平衡；ro-stage 残留=0；exportContract/taskDetailModal/stagesSection/renderStages/viewTaskDetail/toggleStage/exportModal/closeMoreOps 均 present。

## 修复「确认中（发起方）-新」页阶段任务布局大小不一致（2026-09-04 补充）

- 文件：service-miniapp/worker-contract-confirming-initiator-new.html
- 问题（用户截图反馈）：阶段任务 Tab 下卡片/字体/间距与原「确认中（发起方）」页（worker-contract-detail.html）效果不一致。
- 修复（对齐用户截图/拟定中页 `.contract-content-card` + `.content-tabs` 尺寸，回退上一步的 16px/变量化改动）：
  - `.worker-invite-card` padding：`16px` → `14px`。
  - `.contract-content-card` padding：`16px` → `14px`。
  - `.card-title`：`15px / var(--text-primary) / margin-bottom:12px` → `14px / #222 / margin-bottom:10px`。
  - `.content-tabs`：`margin-top:12px; border-bottom-color:var(--border-color)` → `margin-bottom:12px; border-bottom-color:#f0f0f0`。
  - `.content-tab`：`14px / var(--text-tertiary)` → `13px / #666`；active 色 `var(--color-primary) / font-weight:500` → `#1677ff / font-weight:600`；下划线 radius `1px` → `2px`。
  - `.content-tab-pane`：移除 `padding-top: 16px`，恢复为仅 `display:none`。
- 校验：JS vm.Script 语法 OK；div 211/211 平衡。

## 修复「确认中（发起方）-新」页阶段任务背景色与宽度对齐（2026-09-04 再次补充）

- 文件：service-miniapp/worker-contract-confirming-initiator-new.html
- 问题（用户截图反馈）：当前效果（图2）与参考图（图1）相比，阶段任务 Tab 下方背景为白色而非浅灰，阶段任务卡片有左右 margin 未与 Tab 下划线对齐。
- 修复（对齐参考图）：
  - 阶段任务 pane 背景：`#pane-stage-task.content-tab-pane` 设为 `background: #f7f8fa`，并通过 `margin: 0 -14px -14px; padding: 12px 14px 16px` 扩展到 `.contract-content-card` 边缘，与 Tab 下划线左右对齐。
  - 阶段任务标题「阶段任务（4个阶段）」移到白色卡片外部，作为灰色背景上的 `.stage-section-title`。
  - 阶段任务整体改为 `.stage-card` 白色圆角卡片，无左右 margin；内部 `.stage-item` 去边框、去圆角、保留底部分隔线，形成一个整体大卡片内多阶段的效果。
- 校验：JS vm.Script 语法 OK；div 211/211 平衡；`#pane-stage-task`/`stage-section-title`/`stage-card`/`background: #f7f8fa`/`margin: 0 -14px -14px` 均 present。

## 清理工人合同右侧导航「确认中」冗余入口（2026-09-04 补充）

- 文件：service-miniapp/worker-contract-confirming-initiator-new.html（右侧原型导航「发起方视角」分组）
- 问题：该分组内「确认中(发起方)」（普通链接）与「确认中(发起方)-新」（active）均指向 `worker-contract-confirming-initiator-new.html`，重复冗余。
- 修复：删除「确认中(发起方)」普通链接项（原 line 756），仅保留「确认中(发起方)-新」active 项。发起方视角分组 `status-group-count` 原标 8，删除 1 个重复项后实际仍为 8 项，一致无需改。
- 保留（非冗余、正常交叉导航，未动）：`worker-contract-draft-initial-new.html:692` 与 `worker-contract-detail.html:1211` 的「确认中(发起方)」跳转入口仍指向新页。
- 校验：JS vm.Script OK；div 211/211 平衡；新页内「确认中(发起方)」普通链接=0、「确认中(发起方)-新」active 链接=1；两处交叉入口 grep 各命中 1 次，未受影响。

## 工人合同详情「拟定中（已撤回）」(worker_draft) 全面对齐「拟定中（发起方）-新」

- 需求：worker-contract-detail.js 内的 `worker_draft`（拟定中·撤回后）视图，按「拟定中（发起方）-新」页（worker-contract-draft-initial-new.html）最新布局/字段规则更新；**保留撤回后的描述**（横幅 + 重新选择乙方历史卡片）；其他页面暂不改动。用户选定"全面对齐（含可编辑阶段任务三项标准）"。
- 改动文件：service-miniapp/worker-contract-detail.js（仅此 JS；配套 worker-contract-detail.html 无需改结构，其 editTaskModal 已有三项标准 textarea）。
- 对齐点：
  1. **正文模板预览两区**：删除原 `buildTemplateTextPreview(id)`（含甲乙方/金额/工期占位），改为 `buildTemplatePreviewSections(id)` 返回 `{breach, fixed}`；`previewDraftTemplate(kind==='text')` 渲染「违约责任」+「固定详细条款（不可修改）」两区 + 底部附注（甲乙方/金额/工期由主信息填写）。lite=质量要求+争议解决；标准=双方权利义务+质量标准+验收标准+争议解决。
  2. **阶段任务模板预览就地展开三项标准**：新增 `tplPreviewStages` 缓存；stage 预览任务行改为可点击卡片（"查看标准 ›"），`onclick="WCP.toggleTemplateTaskDetail(i,ti)"`；预置隐藏 `tplTask_<i>_<ti>` 面板含 执行标准/确认标准/担责标准（空值"（未填写）"）；新增 `toggleTemplateTaskDetail(si,ti)` 并在 `global.WCP` 暴露；不再用独立信息浮窗（消除多层浮窗遮挡）。
  3. **预览合同（全文）状态化乙方 + 三项标准**：`buildFullContractHTML` 乙方改 `var partyB = c.partyBName ? escapeHtml(c.partyBName) : ''`（未签约置空，与原"状态驱动乙方"口径一致）；每任务渲染 执行/确认 + 执行标准/确认标准/担责标准。
  4. **新增任务默认值对齐**：`addDraftTask`/`addDraftStage` 新任务 `exec = state.editInvited.length===1 ? state.editInvited[0].name : ''`、`conf: ''`（移除 `tradeWorkerOf(...)` + `'陈庄'` 硬编码）。
- 经排查，本页 `editTaskDetail`/`confirmEditTask`/`getPartyBList` 及 editTaskModal 三项标准字段**此前已齐备**，故"可编辑阶段任务三项标准"无需新建，仅完成上述 1–4 对齐即达成全面对齐。
- 撤回后描述保留确认：`STATUS_DESC.worker_draft.desc`（L108 "合同已撤回至拟定中…"）、`replacedPartyB` 横幅（L732-733/L1404-1405）、重新选择乙方底部操作（L1119-1121）、历史记录卡片（L2113-2126）均未触碰。
- 校验：node --check worker-contract-detail.js → OK；全仓 grep `buildTemplateTextPreview` 无残留；仅改 worker_draft 视图渲染，未影响其他状态/页面/功能。

## 工人合同详情「拟定中（已撤回）」主面板字段/状态步骤条补齐对齐（2026-09-04 续）

- 背景：用户复核发现上一轮仅对齐了「模板预览弹窗」与「预览合同」，worker_draft 主面板（inviteEditPanel + draftContentWrap）字段与状态步骤条仍与新页面不一致。经逐字段比对确认缺口，用户拍板：步骤条改 3 步对齐新页面、合同甲方新增可编辑 picker。
- 改动文件：service-miniapp/worker-contract-detail.html（结构+样式）、service-miniapp/worker-contract-detail.js（逻辑），其他页面/状态未动。
- 对齐点：
  1. **状态步骤条改 3 步**：`renderFlow` 由 4 步（拟定中→确认中→已确认→已签约）改为 3 步（拟定中→确认中→已签约），确认中/已签约态映射第 3 步，已确认态标记进行中、已签约/变更态标记 done。影响所有工人合同状态步骤条显示（用户已知并选择）。
  2. **inviteEditPanel 增加「合同甲方」可编辑 picker**：复用 `WORKER_CANDIDATES` 与 `memberAvatarHtml`/`openBusinessCard`，新增 `togglePartyAPanel/filterPartyA/renderPartyAList/selectPartyA/renderPartyAChips` 并暴露到 `WCP`；选中写入 `state.contract.partyAName/partyAId`，默认陈庄，自动反映到预览合同（buildFullContractHTML 用 partyAName）。HTML 增加 partyAChips/partyAControl/partyAPanelBox/partyASearch/partyAList/partyAEmpty/partyAValue + `.party-a-row` 样式。
  3. **inviteEditPanel 增加「工期(天)」字段**：`#editDurationInput`，`initEditPanel` 取 `state.contract.duration`，`ensureDraftFields` 默认 '30'，`saveDraftOnly`/`saveAndResubmit` 持久化 duration。
  4. **合同正文 Tab 改为「违约责任(必填)+固定详细条款(查看条款)+补充条款」**：移除原「合同正文预览+查看全文」块（删除 `draftContractTextPreview`），新增 `#breachInput` 必填文本域，`renderDraftContractText` 改为填充 breachInput；`ensureDraftFields` 默认违约责任文案，`saveDraftOnly`/`saveAndResubmit` 持久化 breach；固定详细条款保留「查看条款 >」链接（复用 WCP.showFullText）。
- 撤回后描述保留确认：STATUS_CONFIG.worker_draft.desc「合同已撤回至拟定中…」、replacedPartyB 横幅与历史卡片、重新选择乙方底部操作均未触碰。
- 校验：node --check JS-OK；Python html.parser 校验 HTML div 平衡（剩余 0 / 错误 0）；新元素 ID 9 个齐全、4 个 JS 函数定义并暴露 WCP、HTML 调用到位；`draftContractTextPreview`/`getContentIntro` 无遗留破坏（getContentIntro 仍被 buildFullContractHTML 使用）。

## 新建「工人合同详情·已签约（发起方）-新」只读页

- 文件：service-miniapp/worker-contract-signed-initiator-new.html（新建，由 `worker-contract-confirming-initiator-new.html` 复制改造，~940 行）
- 需求：工人合同详情内「已签约」状态页，按「拟定中（发起方）-新」页面布局/字段规则更新；保留已签约状态、更多内功能、已签约后描述、不可编辑只读、发起变更入口及交互；**其他页面暂不改动**。
- 方案：经 AskUserQuestion 确认「新建独立-新页」；蓝本选用只读的「确认中（发起方）-新」页（已含只读横幅/更多菜单/阶段任务/底部按钮），比复制可编辑的拟定中草稿风险更低。
- 改动要点：
  - 标题：`工人合同详情（已签约（发起方）-新）`。
  - 状态进度条：拟定中→`wf-step done`、确认中→`wf-step done`、已签约→`wf-step current`；两根 `wf-line` 均 `done`。
  - 横幅：`.wc-banner.signed`（紫色渐变 #722ED1→#9254DE），文案「已签约」+ 描述「合同已正式签约生效，双方按约定履行各自权利与义务。本页仅可查看，不可编辑；如需调整可发起变更。」。
  - 字段：甲方 陈庄·工长 / 乙方 张水电·水电工 只读 chip；合同信息三 input `.edit-input.ro`+`readonly`；违约责任/补充条款 `.edit-textarea.ro readonly`；阶段任务只读；附件只读——全部继承蓝本只读态。
  - 底部操作栏：原「撤回确认」按钮改为 `<button class="cta-btn cta-primary" onclick="openChange()">发起变更</button>`。
  - 发起变更交互：`openChange()` → `window.location.href='worker-contract-change.html'`（该独立变更页已完整存在，避免重复实现逻辑）。
  - 移除 `#confirmMask` 弹窗及 `openConfirm/closeConfirm/doWithdraw` 函数（撤回确认仅非签约态需要）。
  - CSS：新增 `.wc-banner.signed` 紫渐变；删除 `.cta-warning` 死样式。
  - 原型导航：active 项改为 `已签约(发起方)-新`；`已签约` / `已签约（已履约完成）` 两项均指向本新页；`已签约(受邀方)` 仍指向 `worker-contract-detail.html`（属不同视角，保留）。
- 校验：JS vm.Script 语法 OK；div 204/204 平衡；present markers 全就位（signed new title/banner.signed/openChange/cta-primary/nav active/进度条 current）；dead markers 全清零（confirmMask/openConfirm/doWithdraw/banner.confirming/cta-warning 均 0）。
- 导航联动说明：按用户「其他页面暂不改动」指令，**未修改** `worker-contract-detail.html` 的已签约入口（:1215/1216 仍走主详情页 JS 切换）。新页仅通过自身原型导航可达；如需主详情页「已签约」入口也指向新页，需后续单独确认再改。

## 修复「已签约（发起方）-新」页三点对齐原「已签约」视图

- 文件：service-miniapp/worker-contract-signed-initiator-new.html
- 依据：原「已签约」(worker_signed 发起方) 视图在 worker-contract-detail.js 的 `renderLists` / `confirmStartChange` 实现；用户反馈该新页三处不符。
- 修复点：
  1. **「意向乙方」→「乙方（承包方）」**：已签约态乙方已确定，不应再用「意向乙方」措辞；保留 张水电（水电工）只读 chip。
  2. **新增「其他邀约人及状态」列表**（对齐 `renderLists` 已签约发起方视图）：
     - 张水电：乙方标签 + 状态「已确认（乙方）」（is-partyb 高亮，非弱化）；
     - 韩水通：邀约记录标签 + 状态「抢单失败」（is-record 弱化 opacity:0.55 + 灰色 record 徽标）；
     - 杨水明：邀约记录标签 + 状态「已拒绝」+ 原因「近期已有其他项目安排，无法承接本合同，敬请谅解。」（is-record 弱化）—— 此"拒绝应展示原有原因"在旧新页丢失，已补回。
     - 复用原 worker-contract-detail.html 的 `.invite-row/.invite-avatar/.invite-info/.invite-name/.invite-role/.invite-status(.confirmed/.rejected/.lost/.record)/.invite-partyb-tag/.invite-record-tag/.invite-reason/.is-partyb/.is-record/.muted` 全套 CSS，配色/弱化与原页一致。
  3. **发起变更交互对齐**：原 `openChange()` 改为先弹「发起合同变更」二次确认框（文案"变更需对方确认，确认后需上传签约文件，上传后生成新版本（V2）。变更确认前阶段任务暂停流转。"，按钮「去填写」/「取消」），确认后跳 `worker-contract-change.html`（对齐 `confirmStartChange`/`goChangePage`）；新增 `#changeConfirmModal` 及 `openChange`/`closeChangeConfirm`/`goChangePage`。
- 校验：JS vm.Script 语法 OK；div 230/230 平衡；present markers 全就位（乙方（承包方）/其他邀约人/乙方 invite-row/抢单失败/已拒绝+原因/invite-reason/changeConfirmModal/goChangePage/closeChangeConfirm/去填写）；dead markers 全清零（意向乙方 label/inviteRoleTitle/card-note 意向/旧直接跳转 openChange）。其他页面未改动。

## 修复「已签约（发起方）-新」页：其他邀约人去重 + 发起变更可正常停留

- 用户反馈：① 其他邀约人列表中重复展示了已确认的乙方（张水电）；② 点击发起变更弹窗「去填写」跳到 `worker-contract-change.html` 后提示「合同数据暂不可用…」并自动返回，无法停留。
- 改动文件：
  - `service-miniapp/worker-contract-signed-initiator-new.html`：
    - 删除「其他邀约人」列表中的张水电（乙方）行，仅保留韩水通（抢单失败）和杨水明（已拒绝+原因）。
    - 新增 `WORKER_ID = 'demo-shuidian-signed'`；`goChangePage()` 改为 `worker-contract-change.html?id=demo-shuidian-signed`。
  - `service-miniapp/worker-contract-change.html`（目标页最小化改动，否则无法修复停留问题）：
    - 新增 `makeDemoContract(id)` 函数，构造包含名称、类型、金额、甲方、乙方、邀约记录、阶段任务（STAGE_TEMPLATES.shuidian）、附件、版本记录的完整 demo 合同。
    - 修改 `init()`：先取 URL `id`；真实 `ContractStore` 存在时优先读库；读不到或 ContractStore 不可用时使用 demo 合同兜底，不再直接 `renderNotFound`。
- 校验：JS vm.Script OK；`worker-contract-signed-initiator-new.html` div 224/224 平衡，`worker-contract-change.html` div 196/196 平衡；signed-new 关键标记（WORKER_ID/带 id 跳转/其他邀约人无 is-partyb 张水电行）均 OK；change-page 关键标记（makeDemoContract/demo fallback）均 OK。
- 说明：因用户原指令「其他页面暂不改动」与修复跳转停留存在冲突——若不改 `worker-contract-change.html`，任何无本地数据的跳转必然被 `renderNotFound` 弹回。本次仅对目标页做最小化 demo 兜底改动，未影响其正常 ContractStore 数据路径。

## 工人合同详情 worker_draft 视图「排版布局 + 字段」统一对齐「拟定中（发起方）-新」

- 根因（排版差异大）：两份页面各自内联一套同名草稿组件 CSS——combined 页 `worker-contract-detail.html` 自带的 inline `<style>`（约 139–349 行及 `.wc-bottom-actions`）用 CSS 变量 + 灰色横幅 + 38px 大圆点步骤条；新页 `worker-contract-draft-initial-new.html` 内联的是 Ant 蓝(#1677ff) + 橙色横幅 + 28px 圆点、特定间距的**另一套**样式。同名不同值 → 视觉不一致。
- 用户需求：排版布局与字段均与「拟定中（发起方）-新」统一，减少设计/研发成本。
- 改动文件：**仅 service-miniapp/worker-contract-detail.html**（结构标签 + 在第一个 `<style>` 末尾注入归一化覆盖样式块）；worker-contract-detail.js 上一轮已对齐逻辑，本轮回填/标签一致即可，未动 JS。
- 对齐手段（单文件、低风险）：
  1. **注入归一化 CSS 块**（第一个 `</style>` 前）：把 `.wc-banner(.draft 橙)/.worker-flow/.wf-*/.worker-invite-card/.card-title/.card-note/.field-required-tag/.field-hint/.muted` 等页面级组件按新页数值覆盖；草稿面板内部规则以 `#inviteEditPanel, #draftContentWrap` 前缀提升特异性，压过 `contract-detail.css` 的 `.draft-edit-form .form-*`；逐项对齐 `.edit-field/.edit-input(38px)/.edit-textarea/.party-a-row/.invite-chip/.member-*/.member-picker*/.contract-content-card/.content-tabs/.content-tab*/.form-label-row/.view-full-link/.template-info/.attachment-*/.wc-preview-btn` 到新页值。
  2. **字段标签对齐**：删冗余「合同方（拟定中可修改）」标题（新页合同甲方直接接卡片标题）；合同信息标题去「（拟定中可修改）」；合同名称/金额补 `* 必填`（`.field-required-tag`）；金额补「（元，最多两位小数）」+ `step="0.01" min="0"`；合同内容卡标题改为「📄 合同内容」、右侧「更换正文模板」文案与 `#1677ff` 对齐新页。
  3. **底部操作栏形状对齐**：`.wc-action-btn` 由 `border-radius:8px/42px` 改为药丸形 `23px/46px`、`.wc-bottom-actions` padding 10→12px（不改 JS 动作 type，避免影响其他状态配色语义）。
- 撤回后描述保留确认：STATUS_CONFIG.worker_draft 横幅文案、replacedPartyB 历史卡片、重新选择乙方底部操作均未触碰。
- 校验：div 0/0 平衡；CSS 大括号 386/386 平衡；全仓 grep 旧文案「拟定中可修改 / 更换模板(非正文) / 合同方（拟定中」均=0。
- 说明：因 combined 页 .worker-invite-card/.worker-flow/.wc-banner/.wc-action-btn 为工人合同各状态共享，本次覆盖会令整个工人合同详情页（非仅 worker_draft）统一到新页设计语言，属预期内的整体统一，未改动其他 HTML 文件/状态逻辑。

## 工人合同详情 worker_draft 视图「查看条款跳转 + 违约责任必填红字」精确对齐（2026-09-04 续）

- 用户复核（仍报"排版布局/字段差异较大"）指出三处精确不一致，要求与「拟定中（发起方）-新」逐项统一：① 查看条款跳转目标不一致；② 违约责任需红字必填且可编辑（新页如此）；③ 排版/字段仍不一致。
- 根因（上一轮 CSS 覆盖未生效的结构原因）：combined 页 `#wcDraftContractText` 内 违约责任/补充条款 用旧 `.form-group`/`.form-label-row`/`.form-label` 包裹，而我注入的覆盖块只重定义了 `.edit-field`/`.edit-input`/`.edit-textarea`，故这两个文本域仍走未覆盖的旧样式 → 与新页 `.edit-field` 结构不一致；且 查看条款 用 `onclick="WCP.showFullText()"` 打开内联模态，而非新页的 `<a href="worker-contract-draft-fulltext-new.html">` 页面跳转。
- 改动文件：service-miniapp/worker-contract-detail.html（仅结构标签 + 2 处 CSS 微调），worker-contract-detail.js 未动（逻辑上一轮已齐备）。
- 精确对齐点：
  1. **查看条款改为页面跳转（与新页一致）**：`<span class="view-full-link" onclick="WCP.showFullText()">查看条款 ></span>` → `<a class="view-full-link" href="worker-contract-draft-fulltext-new.html">查看条款</a>`（:978）。跳转目标与新页完全相同。
  2. **违约责任结构对齐（红字必填 + 可编辑）**：外层 `.form-group` 改为 `.edit-field`，标签 `违约责<label class="form-label">` 改为 `<label class="edit-label" for="breachInput">违约责任<span class="field-required-tag">* 必填</span></label>`，文本域去 `rows="3"`（改由 CSS min-height 96px 控制，与新页一致）。`.field-required-tag` 全局已定义 `color:#ff4d4f`（红字）(:701)，与新页一致；可编辑由 `#breachInput` + `ensureDraftFields` 默认文案 + `renderDraftContractText` 回填保证。
  3. **补充条款结构对齐**：去掉 `.form-group` 包裹，改为 `.form-label-row(margin-top:14px)` + 标签，文本域 `.edit-textarea#editContractExtra` 紧跟（与新页结构一致）。
  4. **标签视觉对齐新页**：新增覆盖 `#inviteEditPanel .edit-field > label, #draftContentWrap .edit-field > label { font-size:13px; color:#333; font-weight:500 }`（:712-713），使合同名称/金额/工期/违约责任 标签统一为 13px/#333/500，匹配新页标签观感。
  5. **文案/类名细节对齐**：`contract-content-card` 去掉多余 `card` 类；`draftTemplateBtn` 去掉 `.more` 类改内联 `float:right`；两处 `form-section-title`（合同甲方/合同信息）去掉内联 style；`view-full-link` 移除 `::after { content:" >" }` 使文案精确等于新页「查看条款」（无箭头后缀）。
- 撤回后描述（横幅/历史卡片/重新选择乙方）未触碰。
- 校验：Python html.parser div 平衡 0/0；CSS 大括号 386/386 平衡；`worker-contract-draft-fulltext-new.html` 链接 present；无残留 `onclick="WCP.showFullText()"` 内联调用（showFullText 函数保留未被删，不影响其他）；三处 `* 必填` 红字标识均在（合同名称/合同金额/违约责任）。

## 工人合同详情 worker_draft 视图骨架顺序对齐新页面（2026-09-04 终轮·结构层级修复）

- 用户复核（第三次报"布局差异极大"）指出骨架级不一致：① 状态步骤条应在「合同操作」下方；② 步骤条下方展示状态及描述（横幅）；③ 下方展示「合同方 意向乙方首位确认者即签约乙方」（卡片标题）；④ 不应有「所属架构层级」；⑤ 富文本框不应展示滚动条（手机端）。前两轮只改了字段/样式覆盖，未处理**整体骨架顺序**，故仍不一致。
- 根因：combined 页 `#workerFlowBox`（步骤条）嵌在 `#invitationCard` 内、且 `#statusBanner`（横幅）在 `mainView` 顶部位于卡片之前——与新页「工具栏→步骤条→横幅→合同方卡片」顺序相反；`renderMeta` 第 753 行 `if(!isReceiver)` 在草稿态（sender 视角）仍渲染「合同类型 + 所属架构层级」两行 meta；富文本框缺 `.phone-frame` 级滚动条隐藏。
- 改动（仅作用于 `worker_draft`/`worker_draft_initial` 两草稿态，不动其他状态/页面）：
  1. **步骤条位置**：新增 `repositionFlowBar(status)`（:828，render 调度中 :716 调用）——草稿态将 `#workerFlowBox` 从卡片内 `mainView.insertBefore(flowBox, statusBanner)` 移至「合同操作」工具栏与状态横幅之间（DOM 顺序：工具栏→步骤条→横幅→卡片）；非草稿态 `invitationCard.insertBefore(flowBox, workerContractMeta)` 还原，确保仅草稿态变化、其他状态布局不变。
  2. **卡片标题对齐**：草稿态 `.card-title.innerHTML = '🤝 合同方 <span class="card-note">意向乙方首位确认者即签约乙方</span>'`；非草稿态还原为「🤝 合同邀约与乙方（意向乙方 · 首位确认者成乙方）」。
  3. **去所属架构层级（及合同类型 meta）**：`renderMeta` 第 753 行 `if(!isReceiver)` → `if(!isReceiver && !isDraft)`；草稿态不再渲染「合同类型 / 所属架构层级」两行；同时草稿态 `workerContractMeta.style.display='none'`（避免冗余 甲方 meta 行，对齐新页草稿卡无 meta 区）。`workerArchNote` 仍由 `renderArchNote()` 始终隐藏。
  4. **富文本框隐藏滚动条**：首个 `<style>` 注入 `.phone-frame ::-webkit-scrollbar{display:none}` + `.phone-frame *{scrollbar-width:none;-ms-overflow-style:none}`（:689-690），作用域限定 `.phone-frame`，与「拟定中（发起方）-新」完全一致，不影响原型导航。
- 撤回后描述保留：STATUS_CONFIG.worker_draft.desc「合同已撤回至拟定中…」、replacedPartyB 横幅与历史卡片、重新选择乙方底部操作均未触碰。
- 校验：node --check JS-OK；Python html.parser div 平衡 0/0；CSS 大括号 388/388 平衡；`repositionFlowBar` 已定义(:828)且被调用(:716)；`if(!isReceiver && !isDraft)` 命中(:754)；滚动条规则 present(:689-690)。

## 工人合同详情 worker_draft 视图「预览合同按钮移出合同内容卡」（2026-09-04 续）

- 用户要求：①「合同方」区域下的蓝色背景框样式不错，应同步到「拟定中（发起方）-新」；②「预览合同」按钮应放到「合同内容」区域外（预览内容含本页全部内容）。
- **PART B（已执行）**：合并页 `worker-contract-detail.html` 将 `#draftContentWrap .wc-preview-btn`（`previewContract()`）从 `.contract-content-card` 内部（原 :1010）移出至卡片之后、仍在 `#draftContentWrap` 内（现 :1011）；CSS `margin-top:14px` → `margin:14px 12px 12px`（移出卡片后补左右留白，与卡片对齐）。校验 div 0/0、CSS 388/388 平衡、按钮确认在卡外。撤回后描述/其他状态未动。
- **PART A（经核查已一致，待用户指认）**：比对两页「合同方」区域浅蓝框（`#f0f7ff`）CSS——甲方选择框（#draftContentWrap .party-a-picker .member-picker-control / .party-a-picker .member-picker-control 均 background:#f0f7ff）、乙方标签（invite-chip 均 #f0f7ff）、模板提示蓝框（template-info 均 #f0f7ff）——**两页已完全一致，无待补同步缺口**。已向用户呈证据并请其指认具体蓝框（避免再次改错）。
- **中断修复（2026-09-04 收尾）**：异常中断后复核——文件未损坏（node --check JS-OK；两页 div 0/0、CSS 大括号均平衡）。PART A 经逐行比对仍判定两页「合同方」区蓝框 CSS 完全一致，无需改动。**新增补齐 PART B 的对称项**：将「拟定中（发起方）-新」`worker-contract-draft-initial-new.html` 的「预览合同」按钮由 `.contract-content-card` 内部（原 :540）移出至卡片之外（:540 后、`</div>` 前），CSS `.wc-preview-btn` 的 `margin-top:14px` 改为 `margin:0 12px 12px`（与合并页 worker_draft 对齐，按钮在卡外、左右留白一致）。校验：new page div 0/0、CSS 194/194 平衡、按钮确认在 contract-content-card 之外。两页「预览合同」按钮现位置一致（均在合同内容卡外）。

## 发起变更页简化与流程更新（2026-09-04 终）

- 用户反馈：① 变更须知描述未对齐最新流程（对方确认后应为已签约，无需上传签约文件）；② 合同金额/工期变更占用空间较大且冗余；③ 合同正文内其他固定条款不应展示示例内容。
- 改动文件：`service-miniapp/worker-contract-change.html`（单文件，未触碰其他页面）。
- 具体变更：
  1. **变更须知**：第一条由「发起变更后需对方确认，确认后需上传签约文件方可生效」改为「发起变更后需对方确认，确认后即生成新版本的已签约状态」；演示数据 `versionLog` 中 V2 描述同步改为「双方确认后生成新版本已签约状态」。
  2. **金额/工期变更简化**：删除各自区块内的「当前合同金额」「当前工期」「金额变动」「工期变动」行（基本信息区已展示当前值）；改为紧凑的 `.edit-field.inline` 单行输入：金额带 `¥` 前缀，工期带 `天` 后缀；新增 `.field-prefix` / `.change-section.compact` 样式。
  3. **固定详细条款去示例**：移除合同正文内的 `changeContractContentPreview` 预览区与「展开全文」切换，仅保留「固定详细条款」标签 + 「查看条款」入口链接；补充条款区域上提与固定条款入口衔接。
- 清理：`init()` 中移除 `amountOldText`/`durationOldText` 预填与 `changeContractContentPreview` 渲染；`updateAmountDiff`/`updateDurationDiff` 因旧 diff 行元素已不存在而自然静默。
- 校验：JS vm.Script OK；div 192/192 平衡；关键标记就位（变更后金额/工期、¥前缀/天后缀、固定详细条款+查看条款）；无旧冗余标记（amountOldText/durationOldText/amountDiffRow/durationDiffRow/changeContractContentPreview/展开全文 UI）。

## 「拟定中（发起方）-新」同步「合同方」蓝色背景框（2026-09-04 收尾·修正 PART A）

- 修正上一轮 PART A 误判：之前仅比对了 chip/甲方选择框/模板提示等**小**蓝元素（#f0f7ff），漏判了合并页 `worker-contract-detail.html` 的 `#inviteEditPanel` 整体**浅蓝容器**（`.invite-edit-panel`：background `var(--status-primary-bg)` #E6F7FF、border `var(--primary-light)`、radius 10px、padding 12px、margin-top 6px）。该容器在合并页包裹「合同甲方/意向乙方/合同信息」全部可编辑字段；而新页此前**完全没有**此包裹层（内容直接裸露在白色 `.worker-invite-card` 内）。
- 用户截图（仅代表背景色）佐证需要此蓝框；页面字段/内容不改，仅加样式容器。
- 改动文件：service-miniapp/worker-contract-draft-initial-new.html（仅此页，单文件）
  - 新增 CSS：`.invite-edit-panel { margin-top:6px; border:1px solid var(--primary-light); border-radius:10px; padding:12px; background:var(--status-primary-bg); }`（复用 common.css 变量，与合并页逐项一致）。
  - 结构：在 `.worker-invite-card`（保留白色外卡 + 🤝合同方 标题）内、从「合同甲方」form-section-title 起、到「工期」edit-field 止，整体包入 `<div class="invite-edit-panel">…</div>`；内部字段/标签/ID 一律未动。
- 校验：div 252/252 平衡、CSS 大括号平衡（node 校验脚本 OK）。两页「合同方」区现均为浅蓝容器包裹，视觉对齐完成。

